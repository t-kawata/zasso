[package]
name = "anthropx"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
thiserror = "2"
http = "1"

[dev-dependencies]
serde_json = "1"
