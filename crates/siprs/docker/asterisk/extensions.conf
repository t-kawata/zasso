; [::TICKET::] P16-10: Asterisk dialplan for the docker integration-test base
; (§62.19 Q9b). Endpoint 1001/1002 calls answer and echo so
; tests/sip_integration.rs can verify CallConnected + media. The inbound
; originate test uses `channel originate ... application echo` directly.
[from-internal]
exten => 1001,1,Answer()
 same => n,Echo()
 same => n,Hangup()

exten => 1002,1,Answer()
 same => n,Echo()
 same => n,Hangup()
