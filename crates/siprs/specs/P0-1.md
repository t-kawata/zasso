# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P0-1: Crate Foundation — Scope, Glossary, M20 Priority Map, Tauri Boundary, and Versioning Policy

**Ticket Key**: P0-1 · **Phase**: 0

**RFC Source**: `RFC-ROOT.md`

---

## RFC Reference

~/shyme/zasso/crates/siprs/RFC-ROOT (§1a, §2, §2.1, §3, §4.1)

## Background

### Goal
Establish the siprs crate as a compilable Rust crate with a defined module hierarchy (lib.rs + mod.rs), documented identity (non-purpose, Tauri boundary, glossary), versioning policy enforcement via compile-time constants, and a published M20 priority roadmap reference. All meta/documentation items that form the crate's identity and governance must be in place.

### Purpose
Without these foundations, the crate lacks: 1) A compilable module structure (no lib.rs = uncompilable), 2) Versioning policy enforcement (breaking change rules, semver contract), 3) Published identity (what the crate does, doesn't do, and how it integrates with Tauri), 4) Shared vocabulary (Client, Account, Call, etc. — essential for all subsequent tickets), 5) Implementation priority visibility (M20 map shows the full P0-P3 roadmap).

### Motivation
Phase P0 starts here because every subsequent ticket (P0-2, P0-3, ..., P3-X) depends on the crate being compilable and having documented boundaries. 39 stub files are structurally orphaned with no module tree. No implementation can proceed until lib.rs and the module hierarchy exist.

### Constraints
- Zero new runtime dependencies (compile-time constants only; env!() macros are free from Cargo.toml)
- Must not break existing test expectations (tests/versioning_policy_test.rs asserts 8 function signatures reachable through siprs::api::versioning_policy::*)
- #[non_exhaustive] on SipEventPayload must be structural (enum-level attribute, checked at compile time), not doc-level prose
- Crate-level documentation must use #[doc = "..."] or inner line comments (#![doc = "..."]), not a separate documentation-only crate
- No Tauri dependency — crate must remain pure Rust, no tauri::ipc or JavaScript FFI
- M20 priority map is a reference table documenting the roadmap, not an implementation commitment
- PJSIP version is a placeholder string until FFI layer (P3 tickets); non_exhaustive assertion must work without SipEventPayload being fully implemented

## Scope

- [Path: src/api/versioning_policy.rs] Action: modify (replace pub trait Service {} stub with 8 public functions + module-level #[doc = "..."] doc comment)
- [Path: src/api/mod.rs] Action: add (new file — pub mod versioning_policy; + pub mod declarations for all 14 files already in src/api/)
- [Path: src/lib.rs] Action: add (new file — #![doc = "..."] crate-level attributes, pub mod api;, pub use crate::api::versioning_policy::*;)
- [Path: docs/M20_PRIORITY_MAP.md] Action: add (new file — M20 implementation priority roadmap table covering P0-P3 items with Q1:A-Q14:A design decision references)
- [Before] no lib.rs => crate uncompilable; versioning_policy.rs has pub trait Service {} stub. [After] lib.rs compiles with pub mod api; versioning_policy.rs exposes 8 pub functions; integration test passes
- [API contract] New public functions: crate_version() -> &'static str, msrv() -> &'static str, pjsip_version() -> &'static str, msrv_semver_range() -> &'static str, versioning_policy_description() -> &'static str, semver_breaking_change_allowance() -> bool, non_exhaustive_sip_event_payload() -> bool, supported_os_list() -> &'static [&'static str] — all pure compile-time constants
- [Data schema] None — no data structures added in this ticket
- [Config/env] None — env!() reads Cargo.toml fields at compile time; no runtime env vars needed
- [Dependency] Zero new runtime dependencies — all constants are compile-time env!() / concat!() values; std::process and regex are not needed
- [Out-of-scope item 1: SipEventPayload enum] Excluded because #[non_exhaustive] structural assertion depends on SipEventPayload type not yet implemented (N0023, P0-3)
- [Out-of-scope item 2: PJSIP FFI] Excluded because pjsip_version() is a placeholder until FFI layer (P3 tickets — N0040, N0031)
- [Out-of-scope item 3: semver crate dependency] Excluded because version string parsing uses compile-time env!(); runtime semver operations are P2-8 scope (N0076)
- [Out-of-scope item 4: README.md full rewrite] Excluded because full README is P0-2 scope; only M20 priority map reference is documented in this ticket

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0002` `N0003` `N0004` `N0005` `N0007`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0002` | requirement | rust | §1a M20 実装優先度マップ |
| `N0003` | architecture | rust | §2 Non-purpose — 非目的 |
| `N0004` | architecture | rust | §2.1 Tauri 統合との責務境界 |
| `N0005` | glossary | rust | §3 Glossary — 用語定義 |
| `N0007` | api_contract | rust | §4.1 バージョニングポリシー |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| depends_on | ☆ §4 準拠要件 (N0006) | → | ★ §4.1 バージョニングポリシー (N0007) |
| refines | ★ §1a M20 実装優先度マップ (N0002) | → | ☆ §1 Purpose — siprs crate の責務 (N0001) |
| refines | ★ §2 Non-purpose — 非目的 (N0003) | → | ☆ §1 Purpose — siprs crate の責務 (N0001) |
| refines | ☆ §58 セマンティックバージョニング運用 (N0076) | → | ★ §4.1 バージョニングポリシー (N0007) |
| references | ★ §3 Glossary — 用語定義 (N0005) | → | ☆ §1 Purpose — siprs crate の責務 (N0001) |
| part_of | ★ §2.1 Tauri 統合との責務境界 (N0004) | → | ★ §2 Non-purpose — 非目的 (N0003) |
| part_of | ★ §4.1 バージョニングポリシー (N0007) | → | ☆ §4 準拠要件 (N0006) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0007` | `src/api/versioning_policy.rs` |

## Investigation

Source code investigation results for P0-1 (Crate Foundation):

[Existing module structure]
- No lib.rs exists: crate has no root module definition — currently uncompilable
- No mod.rs exists in any src/ subdirectory: all 53 source files under src/ are structurally orphaned (flat, no module hierarchy)
- src/api/ contains 14 .rs files, all with identical stub: pub trait Service {}
- src/ subdirectories (audio_mixer, build, config, concurrency_model, error, ffi_layer, model, security, state, tests) each contain 1-4 stub files with Design Artifact headers
- All src/api/*.rs files wrap Design Artifact provenance headers plus identical 1-line "pub trait Service {}" bodies — no actual implementation exists

[Versioning policy (N0007) — src/api/versioning_policy.rs]
- Contains pub trait Service {} stub + [::STUB::] MUST implement NODE_ID=N0007
- Existing integration test (tests/versioning_policy_test.rs) expects 8 public functions accessible via siprs::api::versioning_policy::*:
  - crate_version() -> &str: returns env!("CARGO_PKG_VERSION")
  - msrv() -> &str: returns env!("CARGO_PKG_RUST_VERSION")
  - pjsip_version() -> &str: compile-time placeholder (no FFI yet)
  - msrv_semver_range() -> &str: returns ">=1.95"
  - versioning_policy_description() -> &str: doc text
  - semver_breaking_change_allowance() -> bool: true for 0.x
  - non_exhaustive_sip_event_payload() -> compile-time assertion (returns true placeholder)
  - supported_os_list() -> &[&str]: platform list
- Integration test has 2 tests: cargo_toml_version_matches_crate_version (assert_eq) and all_functions_are_send_sync_safe (10-thread concurrent access)

[Cargo.toml]
- name = "siprs", version = "0.1.0", rust-version = "1.95", edition = "2021", license = "MIT OR Apache-2.0"
- [dependencies] section exists but is entirely empty — zero runtime dependencies

[Related RFC nodes — design evidence (queried via query.js)]
- N0002 (M20 priority map): §1a — 15 P0-P3 implementation items, 14 design decisions Q1:A-Q14:A. 5 items at P0 (NativeEvent→SipEventPayload, RegistrationStateChanged, DtmfSentInfo, blocking_read fix), 5 at P1 (SubscribeAudio, ConfConnect/Disconnect, configure_codecs auto, CallStateChanged, CallMediaStateChanged), 4 at P2 (Shutdown routing, EventBus split, Transport/ICE, Dual Client TestContext), 2 at P3 (Docker CI, Prebuilt Refresh)
- N0003 (Non-purpose): §2 — 7 excluded features: SIP server, PBX, custom RTP stack, recording file output, GUI/Windowing, persistent config saving (Note: runtime config is in scope), video processing
- N0004 (Tauri boundary): §2.1 — crate provides Rust API only; #[cfg(feature = "serde")] Serialize/Deserialize optional; no tauri::ipc::Channel; no JavaScript FFI; AudioChunkPair binary transfer (Base64) is consumer responsibility
- N0005 (Glossary): §3 — 7 terms defined: Client (SipClient instance), Account (SIP REGISTER/auth context), Call (single SIP session), Media Session (RTP/RTCP/codec/ICE/SRTP per Call), Source (OUT-direction audio input), Chunk Pair (time-aligned IN/OUT audio buffers), Raw SIP Event (full SIP message + parsed metadata)
- N0007 (Versioning policy): §4.1 — 0.x: flexible semver (breaking changes allowed with CHANGELOG); 1.0+: strict semver (MAJOR=breaking, MINOR=additive, PATCH=fixes); SipEventPayload #[non_exhaustive] = MINOR additions allowed without breaking change; security fixes may bypass MAJOR semver

[Semver (N0076) — src/build/semver_policy.rs]
- Separate stub file with fn main() {} + [::STUB::] marker
- Cross-referenced by N0007 via refines edge: P0-1 versioning_policy.rs semver_breaking_change_allowance() must be compatible with semver_policy.rs when implemented later (P2-8)

[Key architectural insight]
This ticket establishes the crate's public API foundation. All 14 src/api/*.rs files currently share identical stubs — P0-1's lib.rs + mod.rs + versioning_policy.rs implementation is the first real code in the crate. All other stubs remain [::STUB::] for subsequent P0/P1 tickets. The versioning_policy.rs functions must all be purely compile-time — no runtime I/O, no FFI, no external crate dependencies.

## Acceptance Criteria

- M20 priority map is documented as module-level reference (docs/M20_PRIORITY_MAP.md or crate doc)
- Non-purpose definitions are accurate and complete (lib.rs #![doc = "..."])
- Tauri integration boundary is clearly documented (lib.rs #![doc = "..."])
- Glossary terms are defined and consistent across crate documentation (lib.rs #![doc = "..."])
- Versioning policy constants compile correctly (cargo build --release passes)
- **[Happy path] cargo build --release succeeds with no warnings; cargo test passes (both unit tests in versioning_policy.rs and integration test tests/versioning_policy_test.rs); cargo doc --no-deps generates documentation with all 4 sections (non-purpose, Tauri boundary, glossary, versioning policy)
- **[Error case] If env!("CARGO_PKG_VERSION") macro fails (theoretically impossible in normal build), compilation fails with a clear compile-time error — no silent fallback or default value is used; the same applies to env!("CARGO_PKG_RUST_VERSION")
- **[Edge case] semver_breaking_change_allowance() returns true for version 0.1.0 (pre-1.0); if Cargo.toml version were changed to 1.0.0+, the function would automatically return false (via env!("CARGO_PKG_VERSION_MAJOR") == "0" check) — this is a compile-time adjustment, no code change needed

## Invariants

- 【Normal establishment】All compile-time version constants (crate_version, msrv, pjsip_version) must resolve at compile time via env!() or static &'static str literals; module declarations (lib.rs + mod.rs) must be syntactically valid for cargo check to succeed; #[doc = "..."] attributes must be valid Rust syntax and not break compilation
- 【Invariant on error】Since all values are compile-time constants (no fallible runtime operations), there is no error path at runtime; a compile-time failure (via env!() on missing env var) is a hard error that blocks compilation entirely — no silent fallback, no default value substitution is permitted
- 【Internal state invariant】The module has zero internal mutable state — all functions return compile-time constants; no static mut or thread_local; all functions are pure (same input → same output) and automatically Send + Sync via &'static str / &'static [&'static str] / bool return types
- 【Boundary invariant】semver_breaking_change_allowance() returns true iff Cargo.toml version major == "0" (version < 1.0.0); msrv_semver_range() output must start with ">=" (valid semver::VersionReq prefix); supported_os_list() must return at least 1 entry (OS platform cannot be empty)

## Boy Scout Rule

Translatability improvements for code touched by this ticket, per Boy Scout Rule:

[src/api/versioning_policy.rs — existing stub]
- Replace pub trait Service {} (meaningless stub shared across 14 files, creating confusing domain concept) with 8 self-documenting verb-phrase functions: crate_version(), msrv(), pjsip_version(), msrv_semver_range(), versioning_policy_description(), semver_breaking_change_allowance(), non_exhaustive_sip_event_payload(), supported_os_list()
- Each function name reads as a complete statement: "get the crate version", "get the MSRV", "check if breaking changes are allowed" — code translates directly to natural language without comments
- Version strings (0.1.0, 1.95, >=1.95) are extracted as named constants with SCREAMING_SNAKE_CASE: CRATE_VERSION, MSRV, MSRV_SEMVER_RANGE — eliminates magic string literals
- 0.x vs 1.0+ phase detection uses a single compile-time boolean check: env!("CARGO_PKG_VERSION_MAJOR") == "0" — self-documenting without inline comments
- Module-level #[doc = "..."] annotation provides versioning policy prose that reads as a specification: "0.x = flexible semver, 1.0+ = strict semver, SipEventPayload #[non_exhaustive]"

[src/lib.rs — new file]
- lib.rs restricted to module declarations + re-exports per Rust conventions (never put logic in lib.rs)
- Module declarations grouped by domain (api::, config::, etc.) with section comments as paragraph separators
- Crate-level #![doc = "..."] prose reads as a book introduction: "siprs is a safe asynchronous SIP voice communication crate... This crate does NOT provide... Glossary terms: ..."

[src/api/mod.rs — new file]
- All 14 files in src/api/ declared via pub mod statements without renames — each module name reflects its file name and domain concept
- Section comments group declarations: "// API entry points" for client-facing types, "// Event system" for event types, "// Audio" for audio API, etc.

[Outside scope — existing code noted for future attention]
- src/build/semver_policy.rs has fn main() {} stub — not modified in P0-1 but flagged for cleanup when N0076 (P2-8) is implemented
- The 13 remaining src/api/*.rs files with identical pub trait Service {} stubs — each will be individually replaced with real implementations in subsequent P0/P1/P2 tickets; no action needed now

## Test Plan

### Unit Tests

- UT: [Normal] crate_version() returns a non-empty semver-formatted &str ("0.1.0") matching env!("CARGO_PKG_VERSION") — verified with !is_empty() + .contains("."); msrv() returns env!("CARGO_PKG_RUST_VERSION") as &str ("1.95"); pjsip_version() returns a non-empty &str documenting the targeted PJSIP version placeholder ("targeting PJSIP 2.x"); versioning_policy_description() returns a non-empty &str with 0.x flexible-semver / 1.0+ strict-semver policy; semver_breaking_change_allowance() returns true for 0.x phase (assert_eq!(true)); supported_os_list() returns &[&str] with at least 1 entry (assert!(!supported_os_list().is_empty())); non_exhaustive_sip_event_payload() runs without panic (assertion-only function with void return — #[test] fn signature confirms no panic)
- UT: [Error] All 8 public functions return compile-time constants — no runtime fallible paths exist by design. If env!("CARGO_PKG_VERSION") or env!("CARGO_PKG_RUST_VERSION") fails (broken build env), compilation aborts with hard error before any test can run — test execution cannot even be reached. msrv_semver_range() output starts with ">=" (prefix check ensures valid semver::VersionReq format — assert!(msrv_semver_range().starts_with(">=")))
- UT: [Boundary] semver_breaking_change_allowance() returns true when crate major version == "0" (pre-1.0 flexible mode). If Cargo.toml version were "1.0.0", same function returns false (assert_eq!(false)) — verified via compile-time macro check: const MAJOR_ZERO: bool = env!("CARGO_PKG_VERSION_MAJOR") == "0" (compile-time constant, no dynamic test env manipulation needed). non_exhaustive_sip_event_payload() asserts compile-time presence of #[non_exhaustive] on SipEventPayload — since SipEventPayload is a [::STUB::] in P0-1, the function returns true unconditionally as a placeholder (structural #[non_exhaustive] check deferred to P0-3 when SipEventPayload is materialized). pjsip_version() returns a non-empty &str which cannot be zero-length — compile-time guaranteed
- UT: [Invariant] All 8 public functions are Send + Sync (verified via fn is_send<T: Send>() {} / fn is_sync<T: Sync>() {} compile-time helper assertions in #[cfg(test)] mod). Concurrent access from 10 threads (100 iterations each) returns identical values — verified via std::thread::spawn + join pattern (same pattern as integration test but as unit test). Zero internal mutable state — no static mut, no thread_local!, no Mutex/once_cell/lazy_static — all return compile-time constants via const fn or static &str. All function signatures return types that auto-implement Send + Sync: &'static str, bool, &'static [&'static str]

### Integration Tests

- IT: [Integration point] Cargo.toml package.version vs versioning_policy::crate_version() — call siprs::api::versioning_policy::crate_version() and assert_eq! with env!("CARGO_PKG_VERSION") to verify compile-time constant propagation. Identical pattern for msrv() vs env!("CARGO_PKG_RUST_VERSION"). Both tests exercise the full compile chain: Cargo.toml -> env!() macro -> lib.rs re-export -> versioning_policy.rs function -> test binary
- IT: [Verification] Module path accessibility — all 8 functions are reachable through siprs::api::versioning_policy::{crate_version, msrv, pjsip_version, msrv_semver_range, versioning_policy_description, semver_breaking_change_allowance, non_exhaustive_sip_event_payload, supported_os_list}. cargo test --test versioning_policy_test passes with the existing integration test fixture. Tests/versioning_policy_test.rs currently contains 2 integration tests (cargo_toml_version_matches_crate_version + all_functions_are_send_sync_safe) — no modifications needed to existing tests
- IT: [Prerequisites] Requires lib.rs declaring pub mod api; requires src/api/mod.rs declaring pub mod versioning_policy; requires each function to be marked pub fn in versioning_policy.rs; requires pub use crate::api::versioning_policy::*; in lib.rs (or equivalent re-export chain). No async runtime needed (all functions are sync compile-time constants). No external crate dependencies required for integration test crate
- IT: [Related tickets] P0-1 (this ticket — crate foundation: lib.rs, mod.rs, versioning_policy.rs, docs/M20_PRIORITY_MAP.md — establishes the module structure that makes the integration test compilable); P0-2 (requirements and documentation foundation — crate split policy, crate-level doc elaboration); P0-3 (SipEventPayload implementation — resolves non_exhaustive_sip_event_payload() stub to actual compile-time structural check — integration test may be extended when SipEventPayload is implemented)

### Exceptions

- Exception 1 — Item: MSRV runtime detection via compiling a temp crate and parsing its Cargo.toml at runtime. Reason: MSRV is a compile-time constant set in Cargo.toml rust-version field ("1.95"); runtime detection of own crate MSRV is impossible because the crate is already compiled — runtime code cannot re-read its own Cargo.toml without filesystem access, which breaks determinism and adds unnecessary API surface (filesystem I/O in a pure configuration module). This is NOT a "deterministic yet untestable" defect: the compile-time constant env!("CARGO_PKG_RUST_VERSION") is verified at build time by cargo, and the unit test confirms the constant propagates correctly by asserting msrv() == env!("CARGO_PKG_RUST_VERSION") — the correctness boundary is the cargo build toolchain, not runtime code logic
- Exception 2 — Item: PJSIP version runtime detection via FFI (pj_get_version()). Reason: PJSIP library is not yet linked (FFI layer is P3 tickets — N0040, N0031); pjsip_version() returns a compile-time placeholder string ("targeting PJSIP 2.x") until FFI bindings exist. This IS a "deterministic yet temporarily untestable" situation by design: the placeholder is intentional and documented as [::STUB::]. Alternative verification: integration test confirms the placeholder constant value via assert!(!pjsip_version().is_empty()); when PJSIP FFI is linked (P3 tickets), upgrade to call pj_get_version() via FFI and remove this exception entry
- Exception 3 — Item: cargo doc --no-deps rendering correctness of crate-level doc sections (non-purpose, Tauri boundary, glossary, M20 map). Reason: Doc comments (#[doc = "..."]) are rendered by rustdoc, a separate toolchain invocation — there is no runtime assertion mechanism for doc content beyond doc-tests (which test code examples inside docs, not the prose itself). This is NOT a "deterministic yet untestable" defect: doc content correctness is a documentation review concern, not a code correctness concern. Alternative verification: run cargo doc --no-deps and manually review the generated HTML in target/doc/siprs/index.html to confirm all 4 sections render correctly; add deny(missing_docs) or #[warn(missing_docs)] as a lint-level check; integrate doc build into CI

## Related Tickets

| Ticket KEY | Relation | Description |
|--------|----------|-------------|
| P3-1 | refines | Refinement target: Core Architecture — Crate Purpose and Concurrency Model |
| P3-1 | references | Reference target: Core Architecture — Crate Purpose and Concurrency Model |
| P0-2 | depends_on | Dependency source (dependent): Requirements, Crate Split Policy, and Documentation Foundation |
| P0-2 | part_of | Part (parent): Requirements, Crate Split Policy, and Documentation Foundation |
| P2-8 | refines | Dependency source (dependent): Test Layer 5 and Semver Operations |

### To show related tickets details

```
node .claude/scripts/tickets/show-ticket-context.js --ticket-key=<Ticket KEY to show (e.g. P0-1)> --for-spec --no-implementation-order
```

## Notes

- [Implementation steps] 1) Create src/lib.rs with #![doc = "..."] crate-level docs (non-purpose, Tauri boundary, glossary) + pub mod api;, 2) Create src/api/mod.rs with pub mod versioning_policy; + pub mod declarations for all 14 existing api/*.rs files, 3) Replace src/api/versioning_policy.rs stub with 8 pub functions returning compile-time constants, 4) Create docs/M20_PRIORITY_MAP.md with priority table covering P0-P3 items + Q1:A-Q14:A design decision table, 5) Run cargo build --release and cargo test to verify, 6) Run cargo fmt and cargo clippy — ensure zero warnings
- [Risks] Risk 1: src/api/ files containing identical pub trait Service {} stubs may cause name collision when all declared in the same module — mitigated by keeping each in its own submodule file (mod.rs only declares pub mod, no trait re-export). Risk 2: env!("CARGO_PKG_VERSION_MAJOR") returns "0" as string — comparison with == "0" is safe because major version "10" would not match the literal "0". Risk 3: Integration test calls siprs::api::versioning_policy::* — must ensure correct pub mod + pub use chain from lib.rs through mod.rs to versioning_policy.rs
- [Caveats] Caveat 1: non_exhaustive_sip_event_payload() cannot check #[non_exhaustive] on SipEventPayload at compile time because SipEventPayload is a stub; returns a boolean constant placeholder until P0-3. Caveat 2: pjsip_version() returns a placeholder string ("targeting PJSIP 2.x") until FFI layer exists (P3 tickets — N0040, N0031). Caveat 3: All 14 api/ files are declared in mod.rs but remain [::STUB::] — this is expected and intentional for Phase P0; each file will be implemented in subsequent P0/P1/P2 tickets
- [Open items] Open 1: Should non_exhaustive_sip_event_payload() reference the SipEventPayload type for a compile-time structural check, or just return a boolean constant? Deferred to P0-3 when SipEventPayload is materialized. Open 2: docs/M20_PRIORITY_MAP.md format — full markdown table preferred for completeness (includes all 15 P0-P3 items + all Q1:A-Q14:A design decisions)
- [Future improvements] 1) When PJSIP FFI exists (P3 tickets): upgrade pjsip_version() from placeholder to pj_get_version() FFI call. 2) When semver crate is added (P2-8): upgrade msrv_semver_range() from &str to semver::VersionReq. 3) When SipEventPayload is implemented (P0-3): upgrade non_exhaustive_sip_event_payload() to structural compile-time assertion using #[cfg(not(any(...)))] or a compile-time marker trait. 4) CHANGELOG.md entry for 0.1.0 documenting initial public API surface

## P0-1 — 0 locations, not implemented

Not found
