# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P15-5: Registration state machine production wiring — typed RegistrationState, M20 converter drive, register_on_start consumption

**Ticket Key**: P15-5 · **Phase**: 15

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
AccountEntry.registration を typed RegistrationState（§33）に変更し、M20 変換器（m20_registr_cmd_pat.rs）が NativeEvent::RegistrationStateChanged の結果を消費して registr_state_machine.rs を駆動することで、登録状態が実 REGISTER の成否にのみ基づいて遷移する production 配線を完成させる。さらに register_on_start をランタイムで消費し、UpdateAccount 後に再登録/解除を発行する。

### Purpose
現状、AccountEntry.registration は String で保持され、process_native_event は get_account_info → registration_status_to_payload で RegistrationSucceeded/Failed イベントを publish するだけで ClientState.accounts[].registration を更新しない。このため registration_state() API は常に初期値（Disabled）を返し、既に実装済みの状態機械（§17）が production で一切駆動されない RESIDUE が残っている。本チケットは §62.4（N0073）の決定に従い、状態機械を production 配線し、登録状態を型安全かつ観測可能にする。

### Motivation
P15-4 でイベントバスが一元化され、P15-3 で TestBackend の §62.2 セマンティクス（add_account 初期 Disabled、set_registration で Registering/Unregistering）が確立された。次段階として登録状態の型安全化と状態機械駆動が必須。また register_on_start は AccountConfig（§10）に定義されているが未消費のまま（§62.4 の RESIDUE）。

### Constraints
- AccountEntry.registration の型変更（String→RegistrationState）は crate 内の複数モジュール（client.rs / public_api_design.rs / backend.rs / m20_runtime_command_error.rs / backend_selection.rs / state.rs）に波及する破壊的変更。
- SipEventPayload は #[non_exhaustive] の公開 enum — 新バリアント追加は可能だが、既存バリアント（RegistrationSucceeded/Failed 等）の削除は避ける。registration_status_to_payload 関数のみ削除対象。
- 実 REGISTER の成否は pjsua-native（Layer 3+）でのみ確定するため、Layer 2 ではネイティブ status を TestBackend の get_account_info が注入する。
- TDD Red→Green→Refactor の順序厳守（supreme law §1）。

## Scope

- AccountEntry.registration を String から typed RegistrationState（§33）に変更する（§62.4）。
- M20 変換器（m20_registr_cmd_pat.rs）が NativeEvent::RegistrationStateChanged を消費し、registr_state_machine.rs を駆動して実 REGISTER 成功時のみ Registered へ遷移させる。
- Mock/TestBackend の初期値を §17 どおり Disabled / Idle に修正する。
- register_on_start をランタイムで消費し、UpdateAccount 後に再登録 / 解除を発行する。
- Scope of changes（変更スコープ）:
- src/runtime/state.rs — [modify] `AccountEntry.registration` の型を `String` → `RegistrationState` に変更。Before: `pub registration: String` / After: `pub registration: RegistrationState`。API: 公開構造体フィールドの型変更（crate 内破壊的）。
- src/runtime/backend.rs — [modify] TestBackend の `add_account`（初期 Disabled）、`set_registration`（Registering/Unregistering）、`mark_registered`（テストヘルパー）、`account_entry_to_snapshot`（`entry.registration == RegistrationState::Registered`）を typed registration に更新。
- src/client.rs — [modify] `account_snapshot_from_entry` の `from_storage_str(&entry.registration)` を `entry.registration == RegistrationState::Registered` に変更。
- src/api/public_api_design.rs — [modify] `registration_state()` の `from_storage_str` を typed 直接比較に変更、`update_config` で `patch.register_on_start` を `submit_update_account` に渡す。
- src/error/m20_runtime_command_error.rs — [modify] `AccountInfo::from_entry` の `from_storage_str` を typed 比較に変更。
- src/state/registr_state_machine.rs — [remove] `from_storage_str` とその 2 テスト（String ストレージ契約の消滅）。Display impl はログ用に維持。
- src/state/m20_registr_cmd_pat.rs — [add] `registration_state_from_status(u32) -> RegistrationState`（200→Registered, 0→Idle, else→Failed）と `registration_transition_from_native(current: RegistrationState, &AccountInfoSnapshot) -> Result<RegistrationState, TransitionError>`。[remove] `registration_status_to_payload` とそのテスト（RegistrationStateChanged フローに置換）。
- src/state/mod.rs — [modify] re-export を `registration_status_to_payload` → `registration_state_from_status` / `registration_transition_from_native` に変更。
- src/api/event_model_payload_bus.rs — [add] `SipEventPayload::RegistrationStateChanged(RegistrationState)` バリアント（#[non_exhaustive] 維持）。
- src/runtime/command.rs — [modify] `RuntimeCommand::UpdateAccount` / `DispatchCommand::UpdateAccount` に `register_on_start: Option<bool>` を追加。`RuntimeCommand::SetRegistration` を dedicated `DispatchCommand::SetRegistration` バリアントへ変更（ClientState 更新のため）。Display / from_runtime_command 更新。
- src/runtime/handle.rs — [modify] `submit_update_account(account_id, config, register_on_start: Option<bool>)` に引数追加。
- src/runtime/reactor.rs — [modify] `process_native_event` に `accounts: &mut BTreeMap<AccountId, AccountEntry>` 引数追加。RegistrationStateChanged アームを状態機械駆動（current→transition→next→ClientState 更新→RegistrationStateChanged publish）に変更。UpdateAccount ハンドラで register_on_start 消費（backend.update_account 後に set_registration）。SetRegistration ハンドラで ClientState registration を Registering/Unregistering に更新（can_transition_to でガード）。
- src/runtime/backend_selection.rs — [modify] テストの `assert_eq!(entry.registration, "Disabled")` を `RegistrationState::Disabled` に更新。
- tests/verify_spec_p10_1.rs — [modify] registration の String ストレージ契約に依存する統合テストを typed に更新。
- Out of scope（非変更スコープと理由）:
- 実 PJSUA FFI コールバック `on_reg_state` からの status 直接取得と Layer 3+ 結合検証 — 別レイヤー（pjsua-native feature）の責務。本チケットは Layer 2 の状態機械配線を対象。
- `RegistrationExpired` への遷移（expiry コールバック）— NativeEvent に expiry イベントが未定義のため別チケット。
- AddAccount 時の register_on_start 自動消費 — §62.4 のスコープは UpdateAccount 後。AddAccount での自動登録は将来チケット。
- `SipEventPayload::RegistrationSucceeded/Failed` バリアントの削除 — 公開 API 互換性のため維持（生産パスは RegistrationStateChanged に移行）。
- 実 metrics 基盤の導入 — instrumentation は将来拡張ポイントとして記録のみ。
- Affected areas（影響範囲）:
- src/state/registr_wiring.rs — [実装ターゲット N0073] 状態機械配線のハブファイル。STUB 解決の実装がここに入る。
- src/api/event_model_payload_bus.rs — SipEventPayload に新バリアント追加 → サブスクライバの match は #[non_exhaustive] により wildcard 必須（既存設計）。
- src/runtime/reactor.rs — process_native_event のシグネチャ変更と既存 3 登録フローテストの期待値変更。
- tests/verify_spec_p10_1.rs — AccountEntry.registration の String ストレージ契約に依存 → typed に更新（要変更 Y）。
- src/runtime/backend.rs / backend_selection.rs — TestBackend の registration 型変更に伴うテスト更新（要変更 Y）。

## STUBs — Must Be Fully Implemented in This Ticket (1 items — resolved: 1)

### TS-001 — `src/state/registr_wiring.rs — resolved

- **Marker**: `// [::TICKET::] P15-5 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-5 --for-spec --no-implementation-order`.`
- **Contracts**: C073, C085
- **Resolution Plan**: Implemented §62.4 (N0073): AccountEntry.registration typed RegistrationState; M20 converter registration_state_from_status + registration_transition_from_native; SipEventPayload::RegistrationStateChanged; registr_wiring.rs owns process_registration_state_changed + apply_registration_command_state wired from reactor.rs; UpdateAccount register_on_start delta consumed; SetRegistration dedicated DispatchCommand updates ClientState; removed from_storage_str + registration_status_to_payload. Verified by make test — ~1143 tests green.

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0073`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0073` | state_machine | rust | 62.4 登録状態機械の production 配線 |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| refines | ★ 62.4 登録状態機械の production 配線 (N0073) | → | ☆ §17 Registration State Machine (N0025) |
| part_of | ★ 62.4 登録状態機械の production 配線 (N0073) | → | ☆ §62 実装整合設計 — RESIDUE 解消のための設計判断確定 (N0068) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0073` | `src/state/registr_wiring.rs` |

## Investigation

- [Evidence 1] src/runtime/state.rs:68 — `AccountEntry.registration` は `pub registration: String`。P10-1 の「canonical storage string」契約により `client.rs:442` / `public_api_design.rs:130` / `error/m20_runtime_command_error.rs:144` が `RegistrationState::from_storage_str(&entry.registration)` で String→enum 逆変換している。
- [Evidence 2] src/state/registr_state_machine.rs — `RegistrationState`（7 バリアント）と `transition(self, target) -> Result<..., TransitionError>`（§17.1 の 10 エッジ）が P4-1/P8-3/P10-1 で完全実装済み。`from_storage_str`（P10-1）が唯一 String 契約に依存する API。
- [Evidence 3] src/runtime/reactor.rs:580-639 — `process_native_event` は `RegistrationStateChanged { acc_id }` に対して `backend.get_account_info(acc_id)` → `registration_status_to_payload(&snapshot)` → `RegistrationSucceeded/RegistrationFailed` を publish するのみで、`ClientState.accounts[].registration` を更新しない。`CallStateTables`（calls + call_directions）しか受け取らず accounts にアクセスできない。
- [Evidence 4] src/state/m20_registr_cmd_pat.rs — `registration_status_to_payload(&AccountInfoSnapshot) -> Option<SipEventPayload>`（200→Succeeded, else→Failed）。reactor.rs:590 と state/mod.rs:19 の re-export のみが呼び出し元。
- [Evidence 5] src/runtime/backend.rs — TestBackend `add_account` は `registration: "Disabled".into()` + `registrations: {Disabled}`（P15-3 §62.2）。`set_registration` は Registering/Unregistering を設定。`account_entry_to_snapshot` は `entry.registration == "Registered"` で 200/0 を決定。`mark_registered` はテストヘルパー。
- [Evidence 6] src/runtime/command.rs:117,131 — `RuntimeCommand::UpdateAccount { account_id, config, reply }` は merged config のみ保持し register_on_start の delta を持たない。`RuntimeCommand::SetRegistration` は `DispatchCommand::Execute` クロージャに写像され、reactor は ClientState を更新できない。
- [Evidence 7] src/api/public_api_design.rs:165-198 — `update_config(patch)` は `patch.apply(&entry.config)` で merged config を作り `submit_update_account(self.id, merged)` を送る。patch の register_on_start（Option<bool>, account_config_spec.rs:317）は reactor に届かない。
- [Evidence 8] src/api/event_model_payload_bus.rs:291-310 — `SipEventPayload`（#[non_exhaustive]）は RegistrationStarted/Succeeded/Failed/UnregistrationSucceeded/UnregistrationFailed/RegistrationExpired を持つが RegistrationStateChanged は無い。
- [Evidence 9] src/runtime/reactor.rs:875-990 — 既存 3 テスト（200→Succeeded, Err→Failure/Error, 403→Failed）が RegistrationStateChanged フローへの変更で期待値更新が必要。
- [Evidence 10] tests/verify_spec_p10_1.rs:100-145 — `registration_state()` / `get_account_info` が String ストレージ契約に依存。
- [Evidence 11] Makefile — `make test` = `cargo test --features test-util`（P15-3 で TestBackend 選択）。

## Acceptance Criteria

- Happy path: add_account（初期 Disabled）→ register() → client_state.accounts[aid].registration == Registering → ネイティブ status 200 の NativeEvent::RegistrationStateChanged 注入 → SipEventPayload::RegistrationStateChanged(Registered) が publish され、query_state().accounts[aid].registration == Registered になる。unregister() → ClientState == Unregistering → status 0 のイベント → RegistrationStateChanged(Idle) が publish され ClientState が Idle に遷移する。
- Error case: ネイティブ status 403 のイベント → RegistrationStateChanged(Failed) が publish され ClientState が Failed に遷移する。get_account_info が Err を返す場合 → SipEventPayload::Error が publish され、ClientState は不変、reactor は生存する。
- Edge case: Disabled/Idle 状態のアカウントに status 200 のイベントが届いた場合 → 不正遷移として tracing::warn! され、状態は不変、Registered には絶対に遷移しない（実 REGISTER 成功時のみ Registered の不変条件）。update_config で register_on_start を指定しない patch の場合 → set_registration は呼ばれない。

## Invariants

- 【Normal establishment】AccountEntry.registration は常に RegistrationState（§17 の 7 バリアント）のいずれかであり、String への変換なしに typed 比較できる。add_account 直後の初期値は Disabled（§17 初期化）。register() / set_registration(true) 後に Registering、実 REGISTER 成功（status 200）後にのみ Registered へ遷移する。
- 【Invariant on error】get_account_info が Err を返しても process_native_event は SipEventPayload::Error を publish し、ClientState.accounts[].registration を変更しない（パニック・状態破壊なし）。不正遷移（例: Disabled→Registered）は TransitionError として記録され、状態は不変のまま reactor は生存する。
- 【Internal state invariant】ClientState.accounts[].registration と TestBackend の registrations マップは lockstep を保つ。set_registration は Registering/Unregistering のみを設定し、直接 Registered を設定しない（mark_registered はテスト専用ヘルパーであり production 配線では使用しない）。
- 【Boundary invariant】registration_state_from_status は 200 のみ Registered、0 のみ Idle、それ以外（4xx/5xx/6xx/1/199/201）は Failed に写像する。Registered へ遷移できるのは Registering 状態のときのみ（§17.1 エッジ）。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C073 — N0073→N0068

- **Precondition**: AccountEntry の registration フィールドは RegistrationState typed enum である（String ではない）
- **Postcondition**: 62.4 の配線は process_native_event で SipEventPayload::RegistrationStateChanged を publish し ClientState を更新する
- **Invariant**: 登録状態機械は §17 と §33 に整合する（RegistrationState は 7 バリアント、transition は §17.1 の 10 エッジのみ許可）

### C085 — N0073→N0025

- **Precondition**: §17 登録状態機械が RegistrationState enum として実装済みである
- **Postcondition**: M20 変換器 registration_state_from_status が status を RegistrationState に写像し registr_state_machine を駆動する
- **Invariant**: 実 REGISTER 成功時のみ Registered へ遷移する（全 current×status 組合せで Ok(Registered) は status==200 && current==Registering のときのみ）

## Boy Scout Rule

- src/runtime/reactor.rs の process_native_event RegistrationStateChanged アームを、ネイティブ取得→status 写像→状態機械遷移→ClientState 更新→publish の段落に分割し、各段落を訳せる関数名で抽出する（例: `resolve_account_by_native_id`, `apply_registration_transition`, `publish_registration_state`）。
- CallStateTables に倣い、accounts へのアクセスを `RegistrationStateTable<'a>`（または明示的な `&mut BTreeMap<AccountId, AccountEntry>` 引数）で束ね、引数数の肥大を防ぐ。
- TestBackend::set_registration の `next.to_string()` / `entry.registration == "Registered"` などの String 比較を typed 比較に置換し、翻訳可能性を高める。
- backend_selection.rs テストの `assert_eq!(entry.registration, "Disabled")` を `RegistrationState::Disabled` に更新し、文字列マジック値を排除する。
- registr_state_machine.rs の from_storage_str（String 契約）を削除し、Display impl の「ログ用」という役割をコメントで明確化する。
- 既存の process_native_event 登録テストの期待値を RegistrationStateChanged に更新する際、テスト名も振る舞いを正確に表す名前（..._publishes_registration_state_changed_registered 等）に改名する。

## Test Plan

### Unit Tests

- registr_state_machine::transition が全状態遷移（Disabled→Registering→Registered→...）を正しく決定すること。
- AccountEntry.registration が RegistrationState enum 型であること。
- 初期値が Disabled であること。
- UT: [Normal] M20 変換器（m20_registr_cmd_pat.rs）の status→RegistrationState 写像と状態機械駆動の正常系:
- `registration_state_from_status(200) == RegistrationState::Registered`（実 REGISTER 成功の写像）
- `registration_state_from_status(0) == RegistrationState::Idle`（未登録/解除成功の写像）
- `registration_transition_from_native(RegistrationState::Registering, &snapshot(status=200)) == Ok(RegistrationState::Registered)`（§17.1 Registering→Registered エッジ）
- `registration_transition_from_native(RegistrationState::Unregistering, &snapshot(status=0)) == Ok(RegistrationState::Idle)`（§17.1 Unregistering→Idle エッジ）
- `AccountEntry { registration: RegistrationState::Disabled, .. }` がコンパイルでき、`entry.registration == RegistrationState::Disabled` が直接比較可能（typed enum 化のコンパイル検証, src/runtime/state.rs）
- `SipEventPayload::RegistrationStateChanged(RegistrationState::Registered)` が構築・Debug フォーマット可能（src/api/event_model_payload_bus.rs）
- UT: [Error] 失敗・不正遷移の検証:
- `registration_state_from_status(403) == RegistrationState::Failed`, `registration_state_from_status(503) == RegistrationState::Failed`（REGISTER 失敗の写像）
- `registration_transition_from_native(Registering, &snapshot(status=403)) == Ok(Failed)`（§17.1 Registering→Failed）
- `registration_transition_from_native(Unregistering, &snapshot(status=403)) == Ok(Failed)`（§17.1 Unregistering→Failed）
- 不正エッジ: `registration_transition_from_native(Disabled, &snapshot(status=200)) == Err(TransitionError{from: Disabled, to: Registered})`（Disabled は Registered へ直接遷移できない）
- 不正エッジ: `registration_transition_from_native(Idle, &snapshot(status=200)) == Err(TransitionError{from: Idle, to: Registered})`（§17.1: Idle→Registered エッジは存在しない）
- reactor `process_native_event` で `backend.get_account_info` が Err を返す場合 `SipEventPayload::Error` が publish され、`ClientState.accounts[].registration` は不変・reactor は生存
- UT: [Boundary] 境界値の検証:
- status 境界: `registration_state_from_status(200)` のみ `Registered`、`registration_state_from_status(0)` のみ `Idle`、`199` / `201` / `1` / `u32::MAX` は全て `Failed`（200 以外は非成功）
- `registration_state_from_status(0)` が唯一の Idle 写像（解除成功と未登録の区別境界）
- `registration_transition_from_native(Unregistering, &snapshot(status=0)) == Ok(Idle)` が唯一の Idle 到達経路
- `RegistrationState::can_transition_to(Registering)` が真なのは Disabled/Idle/Expired/Failed の 4 状態のみ（既存 registr_state_machine UT と整合）
- UT: [Invariant] §17/§33 整合と『実 REGISTER 成功時のみ Registered へ遷移する』:
- C085 invariant: 全 7 状態 × 代表 status（200/0/403）の 21 組合せ table-driven test で、`Ok(Registered)` は (current=Registering, status=200) の 1 組のみ、`Ok(Idle)` は (current=Unregistering, status=0) の 1 組のみ、他は `Ok(Failed)` または `Err` であることを assert（実 REGISTER 成功時のみ Registered へ遷移する）
- C073 invariant: 登録状態機械は §17/§33 と整合する — `RegistrationState` は 7 バリアント、`transition` テーブルは §17.1 の 10 エッジのみ許可（`transition_table_matches_rfc_edges` と整合）
- `AccountEntry.registration` は常に `RegistrationState` 型であり String パース（`from_storage_str`）不要（型保証）

### Integration Tests

- TestBackend 上で register() を実行し RegistrationStateChanged(Registering→Registered) が publish されること。
- unregister() で Unregistering→Idle に遷移しイベントが発火すること。
- update_config で register_on_start 変更後に set_registration が呼ばれること。
- IT: [Integration point] モジュール間インターフェース:
- `SipAccountHandle::register()/unregister()/update_config()`（src/api/public_api_design.rs）→ `RuntimeHandle::submit`（src/runtime/handle.rs）→ `RuntimeCommand::SetRegistration/UpdateAccount`（src/runtime/command.rs）→ reactor ループ（src/runtime/reactor.rs）→ `SipBackend`（TestBackend, src/runtime/backend.rs）
- FFI コールバック相当の `NativeEvent::RegistrationStateChanged` 注入（`RuntimeHandle::enqueue_native_event`, reactor.rs 既存パターン）→ `process_native_event` → `SipBackend::get_account_info` → `m20_registr_cmd_pat` 変換器（`registration_state_from_status` / `registration_transition_from_native`）→ `registr_state_machine::transition` → `ClientState.accounts[].registration` 更新 → 単一 `EventBus` publish（src/runtime/state.rs, src/state/registr_state_machine.rs, src/state/m20_registr_cmd_pat.rs, src/api/event_model_payload_bus.rs）
- IT: [Verification] 各統合テストの検証内容:
- TestBackend + live reactor で、add_account（初期 Disabled）→ `register()` → `client_state.accounts[aid].registration == Registering` → `enqueue_native_event(NativeEvent::RegistrationStateChanged{acc_id})`（get_account_info が 200 を返す設定）→ subscribe が `SipEventPayload::RegistrationStateChanged(Registered)` を受信し、`query_state().accounts[aid].registration == Registered` を確認
- `unregister()` → ClientState == Unregistering → status 0 のイベント → `RegistrationStateChanged(Idle)` publish + ClientState == Idle
- `update_config(AccountConfigPatch { register_on_start: Some(true), .. })` → TestBackend `registration_state(id) == Some(Registering)` かつ ClientState == Registering（set_registration 呼び出しの観測）
- `update_config(AccountConfigPatch { register_on_start: Some(false), .. })` → TestBackend `registration_state(id) == Some(Unregistering)`
- `update_config(AccountConfigPatch { register_on_start: None, .. })`（register_on_start を含まない patch）→ `set_registration` は呼ばれず、TestBackend の registration_state は不変
- Disabled 状態に status 200 のイベントを注入 → publish されず、ClientState は Disabled のまま、reactor は生存（不正遷移の耐性）
- IT: [Prerequisites]
- 検証コマンドは `make test`（= `cargo test --features test-util`）。§62.2 により test-util feature 有効時は TestBackend が選択される
- 既存 reactor テストの更新: `process_native_event_registration_200_publishes_succeeded` は `RegistrationStateChanged(Registered)` 期待に、`process_native_event_registration_403_publishes_failed` は `RegistrationStateChanged(Failed)` 期待に変更。各テストに `accounts: BTreeMap<AccountId, AccountEntry>` を構築して `process_native_event` に渡す
- `process_native_event` のシグネチャ変更（accounts 引数追加）に伴う全テスト呼び出しの更新
- IT: [Related tickets]
- P15-3（backend selection — TestBackend §62.2 セマンティクス、add_account 初期 Disabled）
- P15-4（event bus unification — 単一 EventBus publish、`enqueue_native_event` 経路）
- P10-1（AccountEntry.registration の String ストレージ契約 — P15-5 で typed 化し supersede）
- P4-1（RegistrationState enum + transition テーブル実装）
- P0-5（M20 registration command pattern — AccountInfoSnapshot 基盤）

### Exceptions

- 実 REGISTER の成否（PJSUA が実際に 200 OK / 4xx を受信するか）は pjsua-native（Layer 3+ FFI）の実行時にのみ確定するため、Layer 2 の Rust テストではテスト不能（cannot test）である。ただしこれは設計欠陥（design defect）ではなく、ネイティブ境界の検証を Layer 3+ に委譲するレイヤリング判断である。
- Exception entry:
- Item: 実 REGISTER トランザクションの成否決定（on_reg_state コールバックと pjsua_acc_get_info による status 取得）
- Reason: `on_reg_state` コールバックと `pjsua_acc_get_info` は `pjsua-native` feature（Layer 3+ / FFI 実装）の実行時にのみ存在する。TestBackend ベースの Rust 単体・統合テスト（Layer 2）では実際の REGISTER を送信できないため、ネイティブ層での成否決定を観測・検証することは不可能（impossible to test）
- Alternative verification: これは「決定論的だが原理的にテスト不能」な設計欠陥（design defect）ではなく、ネイティブ境界を Layer 3+ に委譲するレイヤリング判断。Layer 2 では M20 変換器 `registration_state_from_status` が status（200/0/4xx）を §17 の `RegistrationState` に写像し、`registration_transition_from_native` が §17.1 の遷移エッジを強制することを単体テストで検証する。TestBackend の `get_account_info`（または `get_account_info_result` による status 注入）で 200→Registered / 0→Idle / 403→Failed の全経路をネイティブなしに駆動・検証する。実 PJSUA との結合は `pjsua-native` feature 有効時の Layer 3+ 統合テストで行う

### Plan Test Code (concrete code)

- UT: [C073-pre] AccountEntry.registration は typed RegistrationState enum（String ではない）— 入力構築コード
  ```rust
  let entry = AccountEntry {
      id: 1,
      native_id: 1,
      config: AccountConfig::default(),
      registration: RegistrationState::Disabled, // typed enum — String から変更後
  };
  assert_eq!(entry.registration, RegistrationState::Disabled);
  ```
- UT: [C073-post] process_native_event が RegistrationStateChanged を publish し ClientState を更新する — 出力アサーションコード
  ```rust
  let mut accounts = BTreeMap::new();
  accounts.insert(
      AccountId::from_u64(1).unwrap(),
      AccountEntry {
          id: 1,
          native_id: 1,
          config: AccountConfig::default(),
          registration: RegistrationState::Registering,
      },
  );
  let mut backend = TestBackend::new();
  backend.add_account(&AccountConfig::default()).unwrap();
  backend.mark_registered(1); // get_account_info -> status 200
  let bus = EventBus::new(16, None);
  let mut rx = bus.subscribe_control();
  let mut calls = BTreeMap::new();
  let mut call_state = CallStateTables {
      calls: &mut calls,
      call_directions: &mut empty_directions(),
  };
  process_native_event(
      &backend, &bus,
      NativeEvent::RegistrationStateChanged { acc_id: 1 },
      &mut call_state, &mut accounts,
  );
  let ev = rx.recv().await.unwrap();
  assert!(matches!(
      ev.payload,
      SipEventPayload::RegistrationStateChanged(RegistrationState::Registered)
  ));
  assert_eq!(
      accounts[&AccountId::from_u64(1).unwrap()].registration,
      RegistrationState::Registered
  );
  ```
- UT: [C073-inv] 登録状態機械は §17/§33 と整合する — 不変条件述語
  ```rust
  assert_eq!(ALL_STATES.len(), 7);
  for from in ALL_STATES {
      for to in ALL_STATES {
          assert_eq!(
              from.transition(to).is_ok(),
              EXPECTED_EDGES.contains(&(from, to)),
              "edge {from:?}->{to:?} must match §17.1"
          );
      }
  }
  ```
- UT: [C085-pre] §17 登録状態機械が RegistrationState enum として実装済み — 入力構築コード
  ```rust
  let states = [
      RegistrationState::Disabled,
      RegistrationState::Idle,
      RegistrationState::Registering,
      RegistrationState::Registered,
      RegistrationState::Unregistering,
      RegistrationState::Failed,
      RegistrationState::Expired,
  ];
  assert_eq!(states.len(), 7);
  ```
- UT: [C085-post] M20 変換器が status を RegistrationState に写像し registr_state_machine を駆動する — 出力アサーションコード
  ```rust
  assert_eq!(registration_state_from_status(200), RegistrationState::Registered);
  assert_eq!(registration_state_from_status(0), RegistrationState::Idle);
  assert_eq!(registration_state_from_status(403), RegistrationState::Failed);
  let snap = AccountInfoSnapshot {
      acc_id: AccountId::from_u64(1).unwrap(),
      registration_status: 200,
      registration_expires: Some(3600),
      online_status: true,
      uri: String::new(),
  };
  assert_eq!(
      registration_transition_from_native(RegistrationState::Registering, &snap),
      Ok(RegistrationState::Registered)
  );
  ```
- UT: [C085-inv] 実 REGISTER 成功時のみ Registered へ遷移する — 不変条件述語（table-driven）
  ```rust
  let statuses = [200u32, 0, 403];
  for current in ALL_STATES {
      for &status in &statuses {
          let snap = AccountInfoSnapshot {
              acc_id: AccountId::from_u64(1).unwrap(),
              registration_status: status,
              registration_expires: if status == 200 { Some(3600) } else { None },
              online_status: status == 200,
              uri: String::new(),
          };
          let result = registration_transition_from_native(current, &snap);
          if current == RegistrationState::Registering && status == 200 {
              assert_eq!(result, Ok(RegistrationState::Registered));
          } else {
              assert_ne!(result, Ok(RegistrationState::Registered));
          }
      }
  }
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| AccountEntry.registration が String | typed RegistrationState（§33）に変更 | 登録状態の型安全化 |
| MockBackend が Registered をハードコード | M20 変換器が NativeEvent を消費し状態機械を駆動 | 実 REGISTER 成功時のみ Registered へ遷移 |
| register_on_start が未消費 | UpdateAccount 後に set_registration を発行 | register_on_start のランタイム消費 |
| TestBackend 初期値が Registered | §17 どおり Disabled / Idle に修正 | 初期状態の整合 |
| AccountEntry.registration が String で ClientState が更新されず registration_state() が常に Disabled | AccountEntry.registration が typed RegistrationState（§17/§33）。ClientState が状態機械駆動で更新される | state.rs の型変更 + 全 String 消費箇所を typed 化、from_storage_str 削除 |
| registration_status_to_payload が Succeeded/Failed を publish するのみ | M20 変換器（registration_state_from_status / registration_transition_from_native）が §17 状態機械を駆動し RegistrationStateChanged を publish | m20_registr_cmd_pat.rs に変換器追加、SipEventPayload::RegistrationStateChanged 追加 |
| register_on_start が未消費・SetRegistration が ClientState を更新しない | UpdateAccount で register_on_start delta を消費し set_registration 発行。SetRegistration は dedicated DispatchCommand として ClientState を Registering/Unregistering に更新 | command.rs / handle.rs / reactor.rs の配線 |
| registr_wiring.rs が空 STUB | §62.4 配線ロジック（process_registration_state_changed / apply_registration_command_state）を実装し reactor から委譲 | N0073 の実装ターゲットファイルを解決 |

## Notes in Prior Implementation Rounds

- [Implementation steps] TDD 順序:
① Red: m20_registr_cmd_pat.rs に registration_state_from_status / registration_transition_from_native のテストを追加（200→Registered, 0→Idle, 403→Failed, 不正エッジ Err）。
② Red: event_model_payload_bus.rs に RegistrationStateChanged バリアント追加 + reactor の publish テスト期待値更新。
③ Red: state.rs の AccountEntry.registration を typed にするコンパイルテスト追加。
④ Green: 型変更を実施し全 call site（client / public_api / error / backend / backend_selection / state テスト）を更新。
⑤ Green: process_native_event を状態機械駆動に変更（accounts 引数追加、get_account_info → registration_state_from_status → transition → ClientState 更新 → RegistrationStateChanged publish）。
⑥ Green: UpdateAccount / SetRegistration の register_on_start 消費と ClientState 更新（can_transition_to ガード付き）。
⑦ Green: registration_status_to_payload / from_storage_str とそのテストを削除。
⑧ Refactor: make test で全 green 確認、cargo fmt / clippy 適用。
- [Risks] ① process_native_event シグネチャ変更は多数のテスト呼び出しに波及（機械的だが広範囲）。② RuntimeCommand / DispatchCommand のフィールド追加は Display impl / from_runtime_command / 関連テストに波及。③ registration_state_from_status(0)→Idle の写像は PJSUA の status 0 が「未登録」を意味する前提に依存（Layer 3+ で確認要）。④ 既存 reactor テストの期待値変更を忘れると CI が赤になる。
- [Caveats] ① AccountEntry.registration の typed 化により from_storage_str は不要だが、Display impl はログ用に残す。② mark_registered はテスト専用ヘルパーとして維持（production 配線は状態機械経由のみ）。③ SipEventPayload::RegistrationSucceeded/Failed は公開 API 互換のため維持。
- [Open items] ① AddAccount 時の register_on_start 自動消費（今回は UpdateAccount 後のみ）。② RegistrationExpired への遷移（expiry NativeEvent が未定義）。③ ネイティブ status 0 の意味（Idle vs Failed）の Layer 3+ での確定。
- [Future improvements] ① process_native_event の accounts 引数を構造体（例: RegistrationStateTable<'a>）に束ねて引数数を削減。② 登録状態遷移の metrics カウンタ基盤。
Implementation summary (P15-5): changed files registr_wiring, m20_registr_cmd_pat, event_model_payload_bus, registr_state_machine, state/mod, state.rs, backend.rs, command.rs, handle.rs, reactor.rs, client.rs, public_api_design.rs, m20_runtime_command_error.rs, backend_selection.rs, verify_spec_p9_1.rs. AccountEntry.registration typed RegistrationState; M20 converter drives state machine; RegistrationStateChanged published; register_on_start consumed on UpdateAccount; SetRegistration dedicated variant. make test green (~1143 tests).
Review report (P15-5):
- Crimes: 0 open (scan-crimes.sh)
- No-excuse gate: 4/4 pass
- STUB evaluation: P15-5 stubs 0 remaining (TS-001 resolved); other stubs (P15-6/7/8/9) correctly deferred
- Annotation verify: 15/15 changed source files carry P15-5 provenance; 0 AMBIGUOUS
- Incomplete-implementation scan: 7 patterns — none found in changed code
- Compile: cargo check --tests clean; clippy 0 warnings
- Tests: make test — 1143 passed, 0 failed (all Test Plan items implemented)
- Completeness: all 4 scope items implemented with material evidence
- Static quality: 91 unwrap/expect flags, all in test modules or doc examples (project rules permit test-code unwrap); production code zero unwrap
- Translatability: new functions are verb phrases, no generic vars, no debug output, no magic numbers
- Fix applied during review: stale doc comment in reactor.rs updated to §62.4 behavior

## P15-5 — implemented at 43 locations

### src/api/event_model_payload_bus.rs

- Line 584
```rust
    fn payload_registration_state_changed() {
```

### src/api/public_api_design.rs

- Line 40
```rust
impl SipAccountHandle {
```

### src/client.rs

- Line 434
```rust
fn account_snapshot_from_entry(
```

### src/error/m20_runtime_command_error.rs

- Line 124
```rust
impl AccountInfo {
```

- Line 193
```rust
    fn registered_entry() -> AccountEntry {
```

- Line 477
```rust
    fn account_info_from_entry_maps_id_and_registered() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 491
```rust
    fn account_info_from_entry_zero_id_returns_account_not_found() {
```

### src/runtime/backend.rs

- Line 121
```rust
    fn conf_disconnect(&mut self, source: i32, sink: i32) -> Result<(), ReactorError>;
```

- Line 180
```rust
impl TestBackend {
```

- Line 232
```rust
    fn add_account(
```

- Line 273
```rust
    fn set_registration(
```

- Line 417
```rust
fn account_entry_to_snapshot(entry: &AccountEntry) -> Result<AccountInfoSnapshot, ReactorError> {
```

- Line 509
```rust
    fn add_account(
```

- Line 865
```rust
    fn test_backend_add_account_starts_disabled() {
```

- Line 886
```rust
    fn test_backend_set_registration_transitions_registration_map() {
```

- Line 1269
```rust
    fn mock_backend_get_account_info_derives_idle_snapshot(
```

### src/runtime/backend_selection.rs

- Line 78
```rust
    fn create_backend_returns_test_backend_in_test_build() -> Result<(), Box<dyn std::error::Error>> {
```

### src/runtime/command.rs

- Line 253
```rust
type BackendFn =
```

- Line 387
```rust
impl DispatchCommand {
```

- Line 509
```rust
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
```

- Line 1002
```rust
    fn from_runtime_command_converts_update_account() {
```

- Line 1061
```rust
    fn runtime_command_display_shows_new_lifecycle_variants() {
```

- Line 1083
```rust
    fn runtime_command_debug_exposes_payload_field_names() {
```

### src/runtime/handle.rs

- Line 58
```rust
impl RuntimeHandle {
```

### src/runtime/reactor.rs

- Line 87
```rust
impl CoreReactor {
```

- Line 2049
```rust
    fn account_with_registration(
```

### src/runtime/state.rs

- Line 194
```rust
    fn account_entry_links_id_to_native_id() {
```

- Line 234
```rust
    fn account_entry_registration_is_typed_enum() {
```

- Line 247
```rust
    fn client_state_after_account_add_accounts_populated() {
```

### src/state/m20_registr_cmd_pat.rs

- Line 113
```rust
    fn account_info_snapshot_clone_and_debug() {
```

- Line 115
```rust
        fn assert_cd<T: Clone + std::fmt::Debug>() {}
```

- Line 123
```rust
    fn snapshot_with_status(status: u32) -> AccountInfoSnapshot {
```

- Line 149
```rust
    fn registration_state_from_status_maps_200_0_else() {
```

- Line 186
```rust
    fn registration_transition_drives_state_machine() {
```

- Line 221
```rust
    fn only_real_register_success_yields_registered() {
```

### src/state/mod.rs

- Line 1
```rust
// [::TICKET::] P4-1 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P4-1 --for-spec --no-implementation-order`.
```

### src/state/registr_state_machine.rs

- Line 1
```rust
// ============================================================================
```

### src/state/registr_wiring.rs

- Line 125
```rust
    fn test_account(value: u64) -> AccountId {
```

- Line 132
```rust
    fn account_with_registration(
```

- Line 152
```rust
    fn registration_state_changed_drives_registering_to_registered(
```

- Line 178
```rust
    fn registration_state_changed_ignores_invalid_disabled_edge(
```

- Line 200
```rust
    fn set_registration_command_edge_advances_client_state() {
```

### tests/verify_spec_p9_1.rs

- Line 1
```rust
// [::TICKET::] P9-1: Layer 5 API integration tests for the example flows.
```
