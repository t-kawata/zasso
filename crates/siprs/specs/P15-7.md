# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P15-7: Media path architecture — per-call AudioMixer, AudioWorkerTask spawn, ChannelSelector injection, tap push, mic source

**Ticket Key**: P15-7 · **Phase**: 15

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
RFC §62.6 のメディア経路アーキテクチャを実装する。per-call AudioMixer（§24.4）を採用し、クライアント初期化で AudioWorkerTask を spawn する。AddAudioSource コマンドに call_id と channels: ChannelSelector(IN/OUT/BOTH) を付与し、reactor 内で IN 経路（受話取得）と OUT 経路（送話 mix & 送信）へ分岐する。AudioTapSender::push を PjsuaBackend のメディアコールバック（conf port put_frame）から呼び、subscribe_audio の tap を実データで駆動する（OMISSIONS F9 解消）。open_default_microphone_source を cpal-input feature のデフォルト化で実入力機器へ接続する。

### Purpose
§62 の 8 設計判断（BREAKING_CHANGE_ORDER）のうち 62.6 を production 配線する。現行は AddAudioSource に call_id/channels が無く単一グローバル mixer（reactor.rs:114 の audio_mixer）に source を追加するのみで、per-call 経路・IN/OUT 分岐・tap の実データ駆動・マイク接続が未配線。RESIDUE 根因（RFC §62.0）のメディア経路不整合を解消し、I/O 境界「AddAudioSource/SubscribeAudio → AudioChunkPair・conf port 送話フレーム」を成立させる。

### Motivation
zasso が通話中に受話（IN）と送話（OUT）を独立に制御するには per-call AudioMixer と ChannelSelector による経路分岐が必須。subscribe_audio（P9-2）は tap を作るだけで実データが流れず、OMISSIONS F9 として未解決のまま。open_default_microphone_source（P8-7）は cpal-input feature がデフォルト外（Cargo.toml default=["serde","tls"]）のため既定ビルドで実入力機器へ接続できない。

### Constraints
既存の source_id (u64) 返却規約（DispatchCommand::AddAudioSource reply 型 Result<u64, ReactorError>）を維持。AudioWorkerTask::spawn の既存シグネチャ（mixer: Arc<AudioMixer>, call_id: u64, frame_duration: Duration）を維持。新規依存クレートなし（cpal は既存 optional dependency、cpal-input = ["dep:cpal"] 定義済み）。TDD Red-Green-Refactor 必須。pjsua-native の実メディアコールバック（conf port put_frame）は Layer 3+ の検証範囲とし、Layer 2 は TestBackend + 手動 ProcessedFrame で決定論検証する。

## Scope

- [Path] src/audio/media_path_arch.rs（N0075 ターゲット、[::STUB::] P15-7 解消）: ChannelSelector enum 定義とメディア経路ヘルパー
- [Path] src/runtime/audio_worker.rs: AudioMixer に in_sources/out_sources 分離と add_in_source/add_out_source 追加
- [Path] src/runtime/command.rs: RuntimeCommand::AddAudioSource に call_id/channels フィールド追加
- [Path] src/runtime/handle.rs: submit_add_audio_source に call_id/channels 追加
- [Path] src/runtime/reactor.rs: 単一 audio_mixer → per-call mixer map（HashMap<CallId, Arc<AudioMixer>>）+ AudioWorkerTask spawn
- [Path] src/client.rs: SipClient::add_audio_source(call_id, source, channels) 追加
- [Path] src/api/audio_subscribe_bp.rs: AudioTapSender::try_push（同期 Realtime push）追加
- [Path] src/runtime/backend.rs: SipBackend に push_media_frame(call_id, frame) 追加 + TestBackend 記録 + PjsuaBackend tap レジストリ
- [Path] src/model/audio_format_chunkpair.rs: AudioChunkPair::from_processed_frame 変換追加
- [Path] src/audio/mod.rs / src/lib.rs: media_path_arch モジュール宣言 + ChannelSelector 再 export
- [Path] Cargo.toml: default feature に cpal-input 追加
- [Path] tests/runtime_audio_lifecycle.rs: add_audio_source 統合テスト拡張
- [Action] add: ChannelSelector enum、SipClient::add_audio_source、AudioTapSender::try_push、SipBackend::push_media_frame、AudioMixer::add_in_source/add_out_source、AudioChunkPair::from_processed_frame
- [Action] modify: RuntimeCommand::AddAudioSource（call_id/channels 追加）、DispatchCommand::AddAudioSource、RuntimeHandle::submit_add_audio_source、reactor の mixer 管理（グローバル→per-call）、Cargo.toml default feature
- [Action] remove: [::STUB::] P15-7（media_path_arch.rs）
- [Action] rename: なし（既存公開 API の破壊的改名なし）
- ChannelSelector{In,Out,Both} を media_path_arch.rs に定義し audio/mod.rs 経由で lib.rs から再 export。Clone/Copy/Debug/PartialEq/Eq を導出。
- AudioMixer に in_sources/out_sources の DashMap を追加し add_in_source/add_out_source を実装。既存 add_source は out_sources へ委譲（後方互換）。remove_source/set_gain/mute は両マップを横断して解決。
- RuntimeCommand::AddAudioSource{ call_id: u64, source: DebugBox<dyn AsyncAudioSource + Send>, channels: ChannelSelector, reply } に拡張。DispatchCommand も同型。from_runtime_command を更新。
- reactor は audio_mixers: HashMap<u64, Arc<AudioMixer>> を保持し、handle_add_audio_source が get_or_create で per-call mixer を取得（無ければ新規作成し AudioWorkerTask::spawn）。ChannelSelector の match で In→add_in_source、Out→add_out_source、Both→両方登録（IN の source_id を返す）。
- SipClient::add_audio_source(call_id, source, channels) が CallId→u64 変換後、RuntimeCommand::AddAudioSource を submit し Ok(source_id) を返す。
- AudioTapSender::try_push(pair) は Realtime 分岐を同期実行（evict-oldest、非ブロック）。async push の Realtime アームが try_push に委譲。
- SipBackend::push_media_frame(call_id: u64, frame: ProcessedFrame) を追加。TestBackend は push_media_frame_calls に記録。PjsuaBackend は audio_taps レジストリから tap を引いて AudioChunkPair::from_processed_frame → try_push。
- AudioChunkPair::from_processed_frame(frame, call_id, account_id) で stereo_interleaved (L=IN/R=OUT) を in_chunk/out_chunk の AudioChunk::I16 に分離。
- Cargo.toml default = ["serde", "tls", "cpal-input"]。
- Before: RuntimeCommand::AddAudioSource{ source, reply }、reactor は単一 audio_mixer.add_source()、SipClient に add_audio_source なし
- After: AddAudioSource{ call_id, source, channels, reply }、reactor は per-call mixer に add_in_source/add_out_source で分岐、SipClient::add_audio_source(call_id, source, channels) 追加
- Before: AudioTapSender::push は async のみで RT コールバックから駆動不可（OMISSIONS F9 未解決）
- After: try_push（同期 Realtime）追加、SipBackend::push_media_frame 経由でメディアコールバックから駆動（subscribe_audio の tap が実データを受信）
- Before: Cargo.toml default=["serde","tls"]（cpal-input デフォルト外）
- After: default=["serde","tls","cpal-input"]（open_default_microphone_source が既定ビルドで利用可能）
- 追加: SipClient::add_audio_source(call_id: CallId, source: Box<dyn AsyncAudioSource>, channels: ChannelSelector) -> Result<u64, SipError>
- 追加: AudioTapSender::try_push(&self, pair: AudioChunkPair) -> Option<AudioChunkPair>（同期）
- 追加: SipBackend::push_media_frame(&mut self, call_id: u64, frame: ProcessedFrame) -> Result<(), ReactorError>（trait メソッド）
- 追加: AudioMixer::add_in_source / add_out_source / in_source_count / out_source_count
- 追加: ChannelSelector 公開 enum（media_path_arch.rs → audio → lib.rs 再 export）
- 変更: RuntimeHandle::submit_add_audio_source(source) -> (call_id, source, channels)
- 破壊的変更なし（既存公開メソッドのシグネチャは維持。RuntimeCommand の内部フィールド変更のみ）
- RuntimeCommand::AddAudioSource に call_id: u64 と channels: ChannelSelector を追加（enum フィールド）
- DispatchCommand::AddAudioSource も同型
- AudioMixer の内部マップが sources（単一）→ in_sources + out_sources（分離）へ
- CallEntry / CallState / AudioChunkPair の公開フィールドは不変（C046 / C031）
- Cargo.toml [features] default = ["serde", "tls"] → ["serde", "tls", "cpal-input"]
- BootConfig への変更なし。AudioWorkerTask の frame_duration は既定 20ms（既存 DEFAULT_QUEUE_CAPACITY / AudioPipelineConfig 規約を利用）
- 追加なし。cpal は既存 optional dependency（cpal-input = ["dep:cpal"] 定義済み）。新規クレートなし
- [Excluded item] 実 conf port put_frame/get_frame の FFI コールバック登録（pjsua_conf_set_callback）
- [Excluded item] IN/OUT の独立ゲイン・ミュート管理（per-path gain）
- [Excluded item] per-call mixer のライフサイクル自動破棄（call 終了時の mixer/worker 削除）
- [Why excluded] 実 FFI メディアコールバックは pjsua-native feature 有効ビルドでのみ動作し、本チケットは Layer 2 の tap push 配線（SipBackend::push_media_frame + try_push）をスコープとする。FFI 登録は Layer 3+ 統合テスト範囲（testException）
- [Why excluded] remove/set_gain/mute の既存 4 コマンドは source_id 基準のまま維持し、経路指定は将来拡張とする（既存シグネチャを変えない）
- [Why excluded] call 終了時の mixer 破棄は call ライフサイクル配線（P15-6 の call_state 遷移）と組み合わせて別チケットで実施
- [Affected component] AudioMixer / AudioWorkerTask（runtime/audio_worker.rs）
- [Affected component] reactor の audio command dispatch（runtime/reactor.rs）
- [Affected component] SipClient の audio API 面（client.rs）
- [Affected component] subscribe_audio の tap producer（api/audio_subscribe_bp.rs）
- [Affected component] SipBackend trait / TestBackend / PjsuaBackend（runtime/backend.rs）
- [Affected component] Cargo.toml feature 定義（ビルド面）
- [Nature of impact] 構造面 — AudioMixer が IN/OUT 分離され、reactor の mixer 管理がグローバル→per-call に変わる
- [Nature of impact] API 面 — SipClient::add_audio_source が新設され、subscribe_audio が実データ駆動になる
- [Nature of impact] ビルド面 — cpal-input が default feature に加わり、実入力機器へ接続可能になる
- [Corresponding change] Yes — audio_worker.rs の既存テストを IN/OUT 分離に合わせて更新
- [Corresponding change] Yes — tests/runtime_audio_lifecycle.rs に add_audio_source 統合テストを追加
- [Corresponding change] Yes — README に add_audio_source / ChannelSelector / cpal-input 既定化を追記

## STUBs — Must Be Fully Implemented in This Ticket (1 items — resolved: 1)

### TS-001 — `src/audio/media_path_arch.rs — resolved

- **Marker**: `// [::TICKET::] P15-7 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-7 --for-spec --no-implementation-order`.`
- **Contracts**: C075, C087
- **Resolution Plan**: RESOLVED: replaced the [::STUB::] with the §62.6 media-path implementation: ChannelSelector{In,Out,Both} enum, split_stereo_interleaved helper, AudioChunkPair::from_processed_frame, per-call mixer registry (get_or_create/mixer_owning_source), AudioMixer IN/OUT split with add_in_source/add_out_source/add_in_source_shared/add_out_source_shared, RuntimeCommand::AddAudioSource{call_id, channels}, SipBackend::push_media_frame + try_push tap wiring, SipClient::add_audio_source, Cargo.toml default cpal-input.

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0075`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0075` | architecture | rust | 62.6 メディア経路アーキテクチャと統一音声注入 |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| extends | ★ 62.6 メディア経路アーキテクチャと統一音声注入 (N0075) | → | ☆ §24 AudioMixer Design & Real-Time Boundary (N0033) |
| part_of | ★ 62.6 メディア経路アーキテクチャと統一音声注入 (N0075) | → | ☆ §62 実装整合設計 — RESIDUE 解消のための設計判断確定 (N0068) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0075` | `src/audio/media_path_arch.rs` |

## Investigation

- ターゲット src/audio/media_path_arch.rs（N0075）は 21 行の空ファイルで `// [::STUB::] P15-7` のみ。audio/mod.rs は `pub mod pipeline;` のみで media_path_arch 未宣言。lib.rs:69-71 は `pub mod audio;`（コメントで「Audio mixer は runtime/audio_worker.rs」と明記）。
- 既存 AudioMixer（runtime/audio_worker.rs:144）は単一 `sources: DashMap<u64, Arc<Mutex<Box<dyn AsyncAudioSource + Send>>>>` + gains/mutes + lock-free out_queue/in_queue（DEFAULT_QUEUE_CAPACITY=64）。メソッド: add_source/remove_source/set_gain/mute/source_count。IN/OUT 分離なし。AudioWorkerTask（:265）は `spawn(mixer: Arc<AudioMixer>, call_id: u64, frame_duration: Duration) -> Self` で blocking pool 駆動（P3-2）。
- RuntimeCommand::AddAudioSource（command.rs:216）は `{ source: DebugBox<dyn AsyncAudioSource + Send>, reply }` — call_id/channels なし。DispatchCommand::AddAudioSource（:308）も同型。reply 型は Result<u64, ReactorError>（source_id 返却）。
- reactor（reactor.rs:114）は単一 `audio_mixer: Arc<AudioMixer>` を spawn 時に生成し、:211 で `audio_mixer.add_source(source.into_inner())`。RuntimeHandle::submit_add_audio_source（handle.rs:487）は source のみ受取り DispatchCommand::AddAudioSource を送信。
- SipClient（client.rs）には add_audio_source が存在しない。subscribe_audio（client.rs:471）は call 検証 → tap_channel(capacity, mode) → `tap_senders: Arc<Mutex<HashMap<CallId, AudioTapSender>>>` に登録。AudioTapSender::push（audio_subscribe_bp.rs:160）は async で Realtime 分岐は await 無し（同期的）、Lossless 分岐は await。同期 try_push は無い。
- SipBackend trait（backend.rs:63-100）に push_media_frame は無い。PjsuaBackend は空構造体（backend.rs:482、#[derive(Default)]）。TestBackend は conf_connect_calls 等の記録を持つが push_media_frame 記録なし。ffi/callback.rs に on_call_media_state（:183）が NativeEvent::MediaStateChanged を enqueue するが、per-frame conf port コールバックは無い。
- open_default_microphone_source（asyncaudiosrc_adapter.rs:174）は `#[cfg(feature="cpal-input")]` で `pub async fn open_default_microphone_source(format: AudioFormat) -> Result<Box<dyn AsyncAudioSource>, SipError>` — CpalMicrophoneSource::new 経由。Cargo.toml: default = ["serde", "tls"]（cpal-input はデフォルト外、cpal-input = ["dep:cpal"] は定義済み）。
- AudioChunkPair（audio_format_chunkpair.rs:215）は call_id/account_id/timestamp/in_chunk/out_chunk。ProcessedFrame（pipeline.rs:164）は stereo_interleaved（L=IN/R=OUT）/negotiated_codec/timestamp。from_process 変換は無い。
- DesignDecisionId::MediaPathArchitecture（impl_integration_design.rs:43）は section()="62.6"、label()="62.6 メディア経路アーキテクチャと統一音声注入"、BREAKING_CHANGE_ORDER（:227）に含まれる。I/O 境界 row: consumes "AddAudioSource / SubscribeAudio"、produces "AudioChunkPair、conf port 送話フレーム"（graph_node_ids: N0031/N0033/N0034）。
- 関連型: AudioSourceId（model/id_design_newtype.rs:179）は NonZeroU64 newtype。CallId も NonZeroU64。カレントの AddAudioSource は u64 source_id を返す（既存規約）。
- 既存テスト: tests/runtime_audio_lifecycle.rs（P8-1）が ConfConnect/AddAudioSource を CoreReactor + TestBackend で統合検証。audio_worker.rs の O-002 テストが process_frame → out_queue を直接検証。

## Acceptance Criteria

- **[Happy path]** `make test` が全緑。SipClient::add_audio_source(call_id, source, ChannelSelector::Out) が Ok(source_id) を返し、per-call mixer の out_sources に登録される。add_audio_source(call_id, source, Both) が IN/OUT 両経路に注入される。subscribe_audio(call_id, ...) → push_media_frame(call_id, frame) → AudioTapHandle::recv() が AudioChunkPair（IN/OUT 分離）を返す。open_default_microphone_source が cpal-input default feature で利用可能。
- **[Error case]** 実在しない call_id で add_audio_source が Err(SipError::not_found) を返し submit しない（fail-fast）。terminated reactor で Err(ReactorError::ReactorDown) に写像され hang しない。AudioTapSender::try_push が capacity 満杯で最古 frame を evict して overflow report を返し、新 frame は必ず受理される。
- **[Edge case]** add_audio_source(call_id, source, Both) の返却 source_id が IN 登録の id（OUT 登録の id と重複しない）。複数 call で per-call mixer が独立（call 間混入なし）。capacity=1 の tap で try_push 2 回目が 1 回目を evict。ChannelSelector が 3 値のみで match が exhaustive。

## Invariants

- 【Normal establishment】入力は API 境界で検証される — add_audio_source の call_id は実在する通話を指し（実在しない場合は Err）、channels は ChannelSelector の 3 値（In/Out/Both）に限定される。AudioTapSender::try_push は Realtime モードで最新 frame を必ず受理し、容量満杯時は最古 frame を evict して overflow report を返す（非ブロック）。
- 【Invariant on error】fail-fast — 検証失敗（不正 call_id / terminated reactor）は RuntimeCommand を submit せず Err を返し、per-call mixer の状態（in_sources/out_sources）は不変・reactor は生存する。push_media_frame が未登録 tap へ当たっても panic せず no-op（RT スレッドの安全境界）。
- 【Internal state invariant】per-call mixer が authoritative — reactor の audio_mixers: HashMap<CallId, Arc<AudioMixer>> が per-call の唯一の mixer 源であり、source_id は同一 id 空間で一意（IN/OUT で重複しない）。AudioMixer の out_queue/in_queue は lock-free のまま RT 境界を維持する。RuntimeCommand の reply は全経路で必ず 1 回送信される。
- 【Boundary invariant】ChannelSelector は正確に 3 値（In/Out/Both）で、match が exhaustive（catch-all 禁止）。Both 登録の返却 source_id は IN 登録の id。source_id は u64（fetch_add 規約、wrap は 0 へ）。AudioChunkPair::from_processed_frame は stereo_interleaved の L=IN / R=OUT を忠実に分離する。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C075 — N0075→N0068

- **Precondition**: §62 親セクションが存在する — DesignDecisionId::MediaPathArchitecture が src/architecture/impl_integration_design.rs に定義され、section()=="62.6" を返し、BREAKING_CHANGE_ORDER（8 設計判断）に含まれる。
- **Postcondition**: 62.6 が §62 の一部として記述される — DesignDecisionId::MediaPathArchitecture::label() が「62.6 メディア経路アーキテクチャと統一音声注入」を返し、specs/P15-7.md が 62.6 節として per-call AudioMixer・ChannelSelector 統一注入・tap push（OMISSIONS F9 解消）・mic source 接続を規定する。
- **Invariant**: メディア経路は §22/§24 と整合する — AudioChunkPair が src/model/audio_format_chunkpair.rs の既存型（C031）を使用し、AudioMixer が src/runtime/audio_worker.rs の既存実装（P0-6/P3-2）を IN/OUT 分離へ拡張する（新規に経路を代替しない）。

### C087 — N0075→N0033

- **Precondition**: N0033 が §24 AudioMixer リアルタイム境界を定義する — src/audio/audiomixer_rt_boundary.rs が N0033 の Initial Design Artifact ヘッダを持ち、RT 境界（i32 accumulation → saturating i16 混合、lock-free out_queue/in_queue、RT スレッド非ブロック）が既存 AudioMixer に実装済みである。
- **Postcondition**: 62.6 が per-call AudioMixer と ChannelSelector 統一注入を規定する — reactor が call_id→Arc<AudioMixer> の map を保持し、AddAudioSource{call_id, source, channels} が ChannelSelector に応じて IN 経路（受話取得）と OUT 経路（送話 mix & 送信）へ分岐する。
- **Invariant**: IN/OUT/BOTH が独立経路として分岐する — ChannelSelector::In は add_in_source のみ、Out は add_out_source のみ、Both は両方へ登録し、source_id が経路間で重複しない（同一 id 空間）。

## Boy Scout Rule

- media_path_arch.rs の `[::STUB::] P15-7` を実装に置き換え、ChannelSelector を「経路を選択するフラグ」というドメイン概念の enum として定義する（stub 解消が第一）。
- AudioMixer の単一 `sources` を in_sources/out_sources に分離し、`add_in_source`/`add_out_source` を「受話経路へ追加」「送話経路へ追加」という動詞句で命名 — 関数名を動詞、データ構造を名詞としてコードが散文として読めるようにする。process_frame の pull ループを「IN 経路の pull → OUT 経路の pull → mix → out_queue push」という処理順の関数群に分割する。
- reactor の単一 `audio_mixer` 変数を `audio_mixers: HashMap<CallId, Arc<AudioMixer>>` + `get_or_create_mixer(call_id)` に抽出し、「call ごとに mixer を取得（なければ新規）」という読みに揃える。
- `submit_add_audio_source(source)` の仮引数を (call_id, source, channels) に拡張し、u64 変換（CallId→u64）は facade 境界の名前付きヘルパーに 1 点集中する（散在を防ぐ）。
- AudioTapSender::push の Realtime 分岐を try_push として抽出し、async 本体は Realtime/Lossless を「モードに応じて分岐する」という構造に整理する（翻訳可能性向上）。
- ハードコード値の定数化: tap のデフォルト capacity、AudioWorkerTask のデフォルト frame_duration（20ms）を名前付き定数に集約する。
- 既存コードで触れる箇所の unwrap を Result 伝播へ置換し、false コメントを更新する（Boy Scout Rule 適用）。

## Test Plan

### Unit Tests

- UT: [Normal] ChannelSelector::In が AudioMixer::add_in_source を呼ぶ — handle_add_audio_source の分岐で in_sources にのみ登録される（in_source_count==1 / out_source_count==0）。
- UT: [Normal] ChannelSelector::Out が AudioMixer::add_out_source を呼ぶ — out_sources にのみ登録される（in_source_count==0 / out_source_count==1）。
- UT: [Normal] AudioMixer::add_in_source(source) が in_sources に登録し増加する u64 source_id を返す。add_out_source も同様に out_sources へ登録する。
- UT: [Normal] SipClient::add_audio_source(call_id, source, ChannelSelector::Out) が RuntimeCommand::AddAudioSource{ call_id, source, channels: Out } を submit し Ok(source_id) を返す（facade が CallId→u64 へ変換）。
- UT: [Normal] AudioTapSender::try_push(pair) が Realtime モードで容量未満のとき pair をキューに追加し None を返す（非ブロック）。
- UT: [Normal] AudioChunkPair::from_processed_frame(frame, call_id, account_id) が stereo_interleaved [L0,R0,L1,R1,...] を in_chunk/out_chunk の AudioChunk::I16 に分離する（L=IN / R=OUT）。
- UT: [Normal] SipBackend::push_media_frame(call_id, frame) が TestBackend で push_media_frame_calls に (call_id, frame) を記録する。
- UT: [Normal] PjsuaBackend::push_media_frame(call_id, frame) が登録済み tap へ try_push を呼び、subscribe_audio の AudioTapHandle::recv が AudioChunkPair を受信できる（tap push 配線、OMISSIONS F9 解消）。
- UT: [Normal] DesignDecisionId::MediaPathArchitecture::section()=="62.6" かつ label()=="62.6 メディア経路アーキテクチャと統一音声注入" かつ BREAKING_CHANGE_ORDER に含まれる — §62 親セクションが存在する（impl_integration_design.rs の typed design data）。
- UT: [Normal] open_default_microphone_source(format) が cpal-input feature 有効時に Ok(Box<dyn AsyncAudioSource>) を返す（CpalMicrophoneSource::new 経由のシグネチャ検証）。
- UT: [Error] add_audio_source が実在しない call_id で Err(SipError::not_found) を返し RuntimeCommand を submit しない（subscribe_audio と同方針の call 検証）。
- UT: [Error] AudioTapSender::try_push が Realtime モードで容量満杯のとき最古 frame を evict して返し、新 frame を必ず受理する（overflow report、producer は非ブロック）。
- UT: [Error] terminated reactor への add_audio_source が Err(ReactorError::ReactorDown) に写像され Err を返し hang しない。
- UT: [Error] AudioMixer::remove_source が in_sources/out_sources の両方を横断して無い source_id で Err(ReactorError::BackendError) を返す（既存規約の拡張）。
- UT: [Error] PjsuaBackend::push_media_frame が未登録 call_id のとき panic せず no-op（Ok）で RT スレッドの安全境界を維持する。
- UT: [Boundary] add_audio_source(call_id, source, Both) が add_in_source と add_out_source の両方へ登録し、返る source_id が IN 登録の id であり OUT 登録の id と重複しない（同一 id 空間）。
- UT: [Boundary] 複数 call_id（例: 2 通話）で add_audio_source したとき per-call mixer が独立し、call A の source が call B の mixer に混入しない（audio_mixers map の分離）。
- UT: [Boundary] AudioTapSender::try_push が capacity=1 のとき 1 件目は None、2 件目は 1 件目を evict して返す。
- UT: [Boundary] AudioChunkPair::from_processed_frame が空 stereo_interleaved で空 in_chunk/out_chunk を返す。
- UT: [Boundary] ChannelSelector が 3 値のみ（In/Out/Both）で match が exhaustive — 想定外の selector はコンパイル時に拒否される（enum 網羅）。
- UT: [Invariant → C087 Invariant] ChannelSelector::In/Out/Both が独立経路として分岐する — In は in_sources のみ、Out は out_sources のみ、Both は両方へ登録され、各 selector で in_source_count/out_source_count が期待値になる。
- UT: [Invariant → C075 Invariant] メディア経路は §22/§24 と整合する — AudioChunkPair が src/model/audio_format_chunkpair.rs の既存型（C031）を使用し、AudioMixer の i32 accumulation → saturating i16 混合（mix_i16_frame）が既存 process_frame を維持する。
- UT: [Invariant → C075 Postcondition] 62.6 が §62 の一部として記述される — DesignDecisionId::MediaPathArchitecture::label() が「62.6 メディア経路アーキテクチャと統一音声注入」を返し、specs/P15-7.md が 62.6 節として per-call AudioMixer・ChannelSelector・tap push・mic source を規定する（spec 存在 assert）。
- UT: [Invariant] src/audio/media_path_arch.rs の [::STUB::] P15-7 マーカーが解消される — ソーススキャンテストで stub マーカーが存在しないことを assert。
- UT: [Invariant] RuntimeCommand::AddAudioSource の channels フィールドが DispatchCommand::from_runtime_command で網羅的に match される（コンパイル時 exhaustive）。

### Integration Tests

- IT: [Integration point] SipClient::add_audio_source → RuntimeHandle::submit_add_audio_source → DispatchCommand::AddAudioSource → reactor per-call mixer → AudioMixer::add_in_source/add_out_source の直列経路（Layer 2 TestBackend）。
- IT: [Integration point] subscribe_audio の AudioTapSender → SipBackend::push_media_frame → AudioTapSender::try_push → AudioTapHandle::recv の tap 配線（実データ駆動）。
- IT: [Verification] add_audio_source(call_id, source, Both) 後、AudioWorkerTask の process_frame が in_sources と out_sources の両方から pull し、out_queue に混合 frame が入る（IN 受話取得 / OUT 送話 mix & 送信の分岐）。
- IT: [Verification] 2 つの call_id で add_audio_source したとき per-call mixer が独立し、各 mixer の source_count（in/out 合計）が正しい。
- IT: [Verification] subscribe_audio(call_id, format, capacity, Realtime) で tap を張り、push_media_frame(call_id, processed_frame) で AudioTapHandle::recv が AudioChunkPair を返す（IN/OUT 分離された pair、tap が実データで駆動）。
- IT: [Prerequisites] TestBackend（P15-3）が cfg(test)/test-util で使用可能 — PJSUA 不要の決定性 Layer 2 実行。CoreReactor::spawn(BootConfig::default()) が pjsua-native 無しで起動する。
- IT: [Prerequisites] tokio テストランタイムと `make test` 経由の実行（cargo test 直接実行は禁止）。
- IT: [Related tickets] P15-3（TestBackend / create_backend cfg 分岐）、P15-4（単一 EventBus）、P15-5（登録状態機械 production 配線）、P15-6（call-control API / call_state）、P0-6/P3-2（AudioMixer / AudioWorkerTask 基盤）、P8-1（audio lifecycle command dispatch O-003）、P9-2（subscribe_audio / tap_channel）、P8-7（cpal-input / open_default_microphone_source）。

### Exceptions

- **Exception entry:**
  - Item: 実 conf port put_frame/get_frame の FFI メディアコールバック（pjsua_conf_set_callback）による実音声フレーム駆動
  - Reason: pjsua-native feature 有効かつ libpjsua 導入済みビルド（Layer 3+ FFI）でのみ PJSIP のメディアコールバックが実フレームを供給する。デフォルトの `cargo test`（feature なし）では FFI が stub alias（RFC §28, C058）に置き換わり実フレームが発生しないため、実 FFI コールバック経路は自動テスト不能（cannot test）。これは外部依存（ネイティブ境界）による非テスト可能性であり、not a design defect — tap push の論理（SipBackend::push_media_frame → AudioTapSender::try_push → AudioTapHandle::recv）は Layer 2 で TestBackend + 手動 ProcessedFrame により決定論テスト可能である。
  - Alternative verification: TestBackend の push_media_frame_calls 記録と、subscribe_audio で張った AudioTapHandle へ push_media_frame で生成した ProcessedFrame を注入し recv を検証する。実 FFI 結合は pjsua-native 有効時の Layer 3+ 統合テストで行う。
- **Exception entry:**
  - Item: open_default_microphone_source が実 OS のデフォルト入力機器（マイク）から実際に PCM を捕捉すること
  - Reason: 実デバイス入力は CI 環境に物理マイクが存在せず、実ハードウェア依存の動作は決定論テスト不能（cannot test deterministically）。これは設計欠陥ではない — open_default_microphone_source 自体は cpal-input feature 有効時に Ok(Box<dyn AsyncAudioSource>) を返すシグネチャと、CpalMicrophoneSource::new のエラー経路（device-open / format-mismatch）を単体テスト可能であり、実 PCM 捕捉はネイティブ境界（OS オーディオ）の検証範囲である。
  - Alternative verification: cpal-input feature を有効にしたビルドで open_default_microphone_source の型シグネチャをコンパイル検証し、エラー経路を単体テストする。実デバイス結合は開発マシン上の Layer 3+ 検証。
- **Exception entry:**
  - Item: AudioWorkerTask が実 20ms 周期で per-call mixer を駆動する実時間挙動
  - Reason: 実時間スケジューリング（frame_duration 周期）は OS スレッドスケジューラに依存し、厳密な周期は決定論テスト不能（cannot test deterministically）。これは設計欠陥ではない — ワーカーのロジック（process_frame が in_sources/out_sources から pull して out_queue へ push）は frame_duration を短縮または手動 process_frame 呼び出しで Layer 2 決定論テスト可能であり（既存 P8-1 O-002 テストが process_frame を直接検証）、spawn/shutdown のライフサイクルは既存テストで検証済みである。
  - Alternative verification: process_frame を直接呼び、out_queue の混合 frame と in_queue の受信 frame を検証する。spawn 後の is_running() / shutdown のライフサイクルを BootConfig テストで検証する。

### Plan Test Code (concrete code)

- UT: [Normal] C075-Pre §62 親セクションが存在する
  ```rust
  let decision = DesignDecisionId::MediaPathArchitecture;
  assert_eq!(decision.section(), "62.6");
  assert!(BREAKING_CHANGE_ORDER.contains(&DesignDecisionId::MediaPathArchitecture));
  ```
- UT: [Invariant → C075 Post] 62.6 が §62 の一部として記述される
  ```rust
  assert_eq!(DesignDecisionId::MediaPathArchitecture.label(), "62.6 メディア経路アーキテクチャと統一音声注入");
  let spec = std::fs::read_to_string("specs/P15-7.md").expect("spec must exist");
  assert!(spec.contains("ChannelSelector") && spec.contains("per-call"));
  ```
- UT: [Invariant → C075 Inv] メディア経路は §22/§24 と整合する
  ```rust
  // AudioChunkPair is the existing model type (C031); AudioMixer is extended, not replaced.
  let pair: AudioChunkPair = AudioChunkPair {
      call_id: CallId::from_u64(1).unwrap(),
      account_id: AccountId::from_u64(1).unwrap(),
      timestamp: SystemTime::UNIX_EPOCH,
      in_chunk: AudioChunk::I16(vec![]),
      out_chunk: AudioChunk::I16(vec![]),
  };
  let mixer = AudioMixer::new();
  mixer.add_out_source(Box::new(MockAsyncAudioSource::new(vec![0i16; 160])));
  assert_eq!(mixer.out_source_count(), 1);
  assert_eq!(mixer.in_source_count(), 0);
  ```
- UT: [Normal] C087-Pre N0033 が §24 AudioMixer リアルタイム境界を定義する
  ```rust
  let header = std::fs::read_to_string("src/audio/audiomixer_rt_boundary.rs").expect("read");
  assert!(header.contains("NODE_ID=N0033"));
  // RT boundary: lock-free queues and i32-accumulation mixing exist on AudioMixer.
  let mixer = AudioMixer::with_queue_capacity(64);
  assert_eq!(mixer.out_queue.capacity(), 64);
  assert_eq!(mixer.in_queue.capacity(), 64);
  ```
- UT: [Normal → C087 Post] 62.6 が per-call AudioMixer と ChannelSelector 統一注入を規定する
  ```rust
  // Per-call get-or-create: AddAudioSource for a fresh call_id yields an independent mixer.
  let (handle, join) = CoreReactor::spawn(BootConfig::default()).expect("spawn");
  let source = Box::new(MockAsyncAudioSource::new(vec![0i16; 160]));
  let sid = handle.submit_add_audio_source(42, source, ChannelSelector::Out).await.expect("add");
  let mixer = handle.audio_mixer_for(42).expect("per-call mixer created");
  assert_eq!(mixer.out_source_count(), 1);
  assert!(sid > 0);
  drop(handle); let _ = Arc::try_unwrap(join);
  ```
- UT: [Invariant → C087 Inv] IN/OUT/BOTH が独立経路として分岐する
  ```rust
  // Table-driven: In → in_sources only, Out → out_sources only, Both → both.
  let m_in = AudioMixer::new();
  m_in.add_in_source(Box::new(MockAsyncAudioSource::new(vec![0i16; 160])));
  assert_eq!((m_in.in_source_count(), m_in.out_source_count()), (1, 0));

  let m_out = AudioMixer::new();
  m_out.add_out_source(Box::new(MockAsyncAudioSource::new(vec![0i16; 160])));
  assert_eq!((m_out.in_source_count(), m_out.out_source_count()), (0, 1));

  let m_both = AudioMixer::new();
  let id = m_both.add_in_source(Box::new(MockAsyncAudioSource::new(vec![0i16; 160])));
  m_both.add_out_source(Box::new(MockAsyncAudioSource::new(vec![0i16; 160])));
  assert_eq!((m_both.in_source_count(), m_both.out_source_count()), (1, 1));
  // Both returns the IN registration id; shared id space keeps ids distinct.
  assert!(id < u64::MAX);
  ```
- UT: [Normal] AudioTapSender::try_push drives AudioTapHandle::recv with a real pair
  ```rust
  let (tx, mut rx) = tap_channel(4, AudioTapMode::Realtime);
  let pair = AudioChunkPair::from_processed_frame(
      CallId::from_u64(1).unwrap(), AccountId::from_u64(1).unwrap(),
      &ProcessedFrame { stereo_interleaved: vec![1, 2, 3, 4], negotiated_codec: codec(), timestamp: Instant::now() },
  );
  assert!(tx.try_push(pair).is_none());
  let got = rx.recv().await.expect("tap receives the pushed pair");
  assert_eq!(got.in_chunk, AudioChunk::I16(vec![1, 3]));
  assert_eq!(got.out_chunk, AudioChunk::I16(vec![2, 4]));
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| AddAudioSource に call_id とチャネル指定がない | call_id と channels: ChannelSelector(IN/OUT/BOTH) を追加 | 統一音声注入 API（§24.4） |
| AudioMixer がグローバル単一 | per-call AudioMixer を call_id で管理し AudioWorkerTask を spawn | per-call メディア経路 |
| AudioTapSender::push を呼ぶ生産コードがない | PjsuaBackend のメディアコールバックから tap.push を呼ぶ | subscribe_audio の実データ駆動（OMISSIONS F9 解消） |
| cpal-input がデフォルト feature 外 | デフォルト feature 方針を確定し実入力機器へ接続 | マイク source の接続 |
| AddAudioSource に call_id とチャネル指定がない | call_id と channels: ChannelSelector(IN/OUT/BOTH) を追加 | 統一音声注入 API（§24.4 / §62.6） |
| AudioMixer がグローバル単一 | per-call AudioMixer を call_id で管理し AudioWorkerTask を spawn | per-call メディア経路 |
| AudioTapSender::push を呼ぶ生産コードがない | PjsuaBackend のメディアコールバックから tap.try_push を呼ぶ | subscribe_audio の実データ駆動（OMISSIONS F9 解消） |
| cpal-input がデフォルト feature 外 | デフォルト feature 方針を確定し実入力機器へ接続 | マイク source の接続 |
| SipClient に add_audio_source がない | SipClient::add_audio_source(call_id, source, channels) を追加 | §62.6 統一音声注入の公開 API |

## Notes in Prior Implementation Rounds

- [Implementation steps] ① ChannelSelector enum を media_path_arch.rs に定義（Red: 単体テスト先行）② AudioMixer に in_sources/out_sources と add_in_source/add_out_source 追加（既存 add_source は out_sources 委譲）③ RuntimeCommand/DispatchCommand::AddAudioSource に call_id/channels 追加 + from_runtime_command 更新 ④ RuntimeHandle::submit_add_audio_source(call_id, source, channels) 更新 ⑤ reactor の audio_mixer → per-call mixer map 化 + handle_add_audio_source で ChannelSelector 分岐 + AudioWorkerTask spawn ⑥ SipClient::add_audio_source 追加 ⑦ AudioTapSender::try_push 追加 + push の Realtime アーム委譲 ⑧ AudioChunkPair::from_processed_frame 追加 ⑨ SipBackend::push_media_frame 追加 + TestBackend 記録 + PjsuaBackend tap レジストリ ⑩ Cargo.toml default feature に cpal-input 追加 ⑪ audio/mod.rs / lib.rs 再 export ⑫ API surface テスト・README 更新 ⑬ make test 全緑確認
- [Risks] RuntimeCommand::AddAudioSource のシグネチャ変更は破壊的 — 全呼び出し箇所（handle.rs・reactor.rs・テスト）を一括更新。reactor の mixer 管理をグローバル→per-call に変えると、既存 P8-1 O-003 テスト（単一 mixer を直接 assert）が影響を受ける。AudioMixer の sources→in_sources/out_sources 分離で process_frame の pull ループが両マップを巡回するよう変更が必要。
- [Caveats] push_media_frame は RT スレッドから呼ばれるため非ブロック必須 — try_push の Realtime 分岐のみを使用し、Lossless モードの tap への push は非対応（subscribe_audio は Realtime を推奨）。Both 登録の source_id は IN 登録の id を返す（RFC スケッチ準拠）。out_sources の登録 id は同一 id 空間で一意。
- [Open items] per-call mixer の call 終了時破棄（RemoveCallMixer コマンド）。IN/OUT 独立ゲイン（set_gain の channels 指定）。実 conf port FFI コールバック登録（Layer 3+）。audio_taps レジストリの SipClient と backend 間の共有方法の最終決定。
- [Future improvements] ChannelSelector を remove_audio_source / set_audio_source_gain / mute_audio_source にも拡張。per-call AudioPipelineConfig（in/out format）の mixer 生成時指定。tap push の Lossless モード対応（RT スレッド外からの push 経路）。
Implementation summary (P15-7):
- Changed files: src/audio/media_path_arch.rs (TS-001 resolved: ChannelSelector{In,Out,Both} + split_stereo_interleaved), src/audio/mod.rs, src/lib.rs, src/runtime/audio_worker.rs (AudioMixer IN/OUT split: in_sources/out_sources + add_in_source/add_out_source + _shared variants + shared id counter + process_frame two-path), src/runtime/command.rs (RuntimeCommand::AddAudioSource{call_id, channels}), src/runtime/handle.rs (submit_add_audio_source(call_id, source, channels), audio_mixer_for(call_id), per-call mixer map), src/runtime/reactor.rs (per-call mixer registry get_or_create/mixer_owning_source, ChannelSelector branch In/Out/Both), src/api/audio_subscribe_bp.rs (AudioTapSender::try_push sync Realtime + Debug impl), src/model/audio_format_chunkpair.rs (AudioChunkPair::from_processed_frame), src/runtime/backend.rs (SipBackend::push_media_frame + TestBackend recorder + PjsuaBackend audio_taps registry), src/runtime/backend_selection.rs (create_backend(audio_taps)), src/client.rs (SipClient::add_audio_source + shared tap registry in BootConfig), Cargo.toml (default += cpal-input), examples/tts_source.rs, tests/runtime_audio_lifecycle.rs, tests/verify_feature_additive_build.rs, tests/verify_spec_p9_1.rs, src/architecture/impl_integration_design.rs.
- Key changes: ChannelSelector unified IN/OUT/BOTH injection; per-call AudioMixer map with global source-id counter; AudioMixer IN/OUT source separation (process_frame pushes OUT→out_queue, IN→in_queue); RuntimeCommand::AddAudioSource carries call_id+channels; reactor get_or_create_mixer + mixer_owning_source; SipBackend::push_media_frame (TestBackend records, PjsuaBackend pushes to subscribed tap via try_push); AudioChunkPair::from_processed_frame splits stereo L=IN/R=OUT; SipClient::add_audio_source facade; subscribe_audio stores (account_id, sender) in the shared tap registry shared with backend via BootConfig.audio_taps; cpal-input now a default feature.
- Contract coverage: C075 (@verifies media_path_architecture_is_part_of_section_62 in impl_integration_design.rs), C087 (@verifies audio_mixer_both_registrations_share_id_space in audio_worker.rs). verify-red-coverage: 2/2 covered.
- Test results: make test — 1097 lib + all integration + doc tests green (exit 0).
- Quality: new production code is clean (no unwrap/expect in non-test paths); remaining run-quality-checks findings are the pre-existing test-code unwrap/commented-code baseline carried from prior tickets (P15-6 precedent).
Review report (P15-7):
- Step 4 Stub evaluation: media_path_arch.rs TS-001 resolved (replaced with ChannelSelector + split_stereo_interleaved). Remaining 2 stubs correctly deferred: error_native_status.rs (P15-9), shutdown_wiring.rs (P15-8) — both reference future active tickets, no-excuse gate passes (2/2).
- Step 5 Incomplete-implementation scan: 7 patterns clean in changed code (no todo!/unimplemented!/FIXME/#[allow] without STUB). Annotation verify: 17 files, 0 missing / 0 ambiguous. AMBIGUOUS markers re-resolved via resolve-ambiguous-markers.js (5 files, line 1).
- Step 6 Compile+test: cargo check --all-targets (0 warnings/errors), make test all green.
- Step 7 Completeness: caught and FIXED a gap — per-call AudioWorkerTask::spawn wiring was missing. Added audio_workers map to reactor closure, spawn in AddAudioSource handler, shutdown_audio_workers on Shutdown/channel-close. All tests still green. All other spec scope items verified present: per-call AudioMixer map, ChannelSelector IN/OUT/BOTH branch, AddAudioSource{call_id, channels}, SipBackend::push_media_frame + AudioTapSender::try_push (OMISSIONS F9), AudioChunkPair::from_processed_frame, SipClient::add_audio_source, cpal-input default feature.
- Step 8 Static quality: 109 baseline findings (test unwraps + false-positive commented-code), no new production issues.
- Step 9 Translatability: verb-phrase function names, no generic/single-char vars in production, no debug output, no magic numbers in production.
- Issues found and fixes applied: per-call AudioWorkerTask::spawn wiring added (Step 7).
- Contract coverage: C075 (impl_integration_design media_path_architecture_is_part_of_section_62), C087 (audio_worker audio_mixer_both_registrations_share_id_space) + integration per_call_mixers_are_independent.

## P15-7 — implemented at 70 locations

### examples/tts_source.rs

- Line 1
```rust
// [::TICKET::] P15-7 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-7 --for-spec --no-implementation-order`.
```

- Line 2
```rust
// AI TTS source injection (RFC §41.5): implement a TtsStreamSource that yields
```

### src/api/audio_subscribe_bp.rs

- Line 134
```rust
impl std::fmt::Debug for AudioTapSender {
```

- Line 140
```rust
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
```

- Line 173
```rust
impl AudioTapSender {
```

- Line 302
```rust
        fn assert_send<T: Send>() {}
```

### src/architecture/impl_integration_design.rs

- Line 388
```rust
    fn media_path_architecture_is_part_of_section_62() {
```

### src/audio/media_path_arch.rs

- Line 67
```rust
    fn channel_selector_has_exactly_three_variants() {
```

- Line 80
```rust
    fn channel_selector_is_copy() {
```

- Line 89
```rust
    fn split_stereo_interleaved_separates_left_and_right() {
```

- Line 99
```rust
    fn split_stereo_interleaved_empty_input() {
```

- Line 108
```rust
    fn split_stereo_interleaved_discards_odd_trailing_sample() {
```

### src/audio/mod.rs

- Line 1
```rust
// [::TICKET::] P15-7 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-7 --for-spec --no-implementation-order`.
```

- Line 2
```rust
// ============================================================================
```

- Line 36
```rust
/// §62.6 media path architecture — ChannelSelector unified audio injection.
```

- Line 43
```rust
pub use media_path_arch::ChannelSelector;
```

### src/client.rs

- Line 87
```rust
impl SipClient {
```

- Line 992
```rust
        fn assert_call_state_result(_: &Result<crate::state::call_state_model::CallState, SipError>) {}
```

- Line 1169
```rust
    fn microphone_is_optional_feature() -> Result<(), std::io::Error> {
```

### src/lib.rs

- Line 1
```rust
// [::TICKET::] P15-7 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-7 --for-spec --no-implementation-order`.
```

- Line 2
```rust
// [::TICKET::] P4-1 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P4-1 --for-spec --no-implementation-order`.
```

### src/model/audio_format_chunkpair.rs

- Line 228
```rust
impl AudioChunkPair {
```

- Line 389
```rust
    fn from_processed_frame_splits_lr_into_in_out() {
```

- Line 407
```rust
    fn from_processed_frame_preserves_call_and_account() {
```

- Line 425
```rust
    fn from_processed_frame_empty_stereo_yields_empty_chunks() {
```

### src/runtime/audio_worker.rs

- Line 173
```rust
impl AudioMixer {
```

- Line 466
```rust
impl AudioWorkerInner {
```

- Line 597
```rust
        fn enter(&self, _: &tracing::span::Id) {}
```

- Line 599
```rust
        fn exit(&self, _: &tracing::span::Id) {}
```

- Line 819
```rust
    fn audio_mixer_add_in_source_registers_on_in_path() {
```

- Line 831
```rust
    fn audio_mixer_add_out_source_registers_on_out_path() {
```

- Line 845
```rust
    fn audio_mixer_both_registrations_share_id_space() {
```

- Line 859
```rust
    fn audio_mixer_remove_source_removes_from_either_path() {
```

- Line 871
```rust
    fn audio_mixer_remove_source_missing_id_errors() {
```

### src/runtime/backend.rs

- Line 128
```rust
    fn conf_connect(&mut self, source: i32, sink: i32) -> Result<(), ReactorError>;
```

- Line 132
```rust
    fn conf_disconnect(&mut self, source: i32, sink: i32) -> Result<(), ReactorError>;
```

- Line 141
```rust
    fn push_media_frame(
```

- Line 248
```rust
impl SipBackend for TestBackend {
```

- Line 453
```rust
    fn push_media_frame(
```

- Line 527
```rust
impl PjsuaBackend {
```

- Line 547
```rust
impl Default for PjsuaBackend {
```

- Line 549
```rust
    fn default() -> Self {
```

- Line 557
```rust
impl SipBackend for PjsuaBackend {
```

- Line 923
```rust
    fn push_media_frame(
```

- Line 1169
```rust
    fn test_backend_push_media_frame_records_invocation() {
```

- Line 1185
```rust
    fn pjsua_push_media_frame_unsubscribed_call_is_noop() {
```

### src/runtime/backend_selection.rs

- Line 91
```rust
    fn create_backend_returns_test_backend_in_test_build() -> Result<(), Box<dyn std::error::Error>> {
```

### src/runtime/command.rs

- Line 279
```rust
type BackendFn =
```

- Line 446
```rust
impl DispatchCommand {
```

- Line 806
```rust
    fn add_audio_source_variant_constructs() {
```

- Line 1037
```rust
    fn from_runtime_command_converts_audio_source_variants() {
```

- Line 1377
```rust
    fn from_runtime_command_preserves_reply_and_debugbox() {
```

### src/runtime/handle.rs

- Line 60
```rust
impl RuntimeHandle {
```

- Line 650
```rust
    fn runtime_handle_is_clonable() {
```

- Line 688
```rust
    fn is_terminated_reflects_atomic_flag() {
```

- Line 1095
```rust
    fn thread_handle_returns_same_arc_allocation() {
```

- Line 1114
```rust
    fn thread_inspection_reports_finished_after_thread_exits() {
```

- Line 1137
```rust
    fn cloned_handle_shares_same_arc_allocation() {
```

- Line 1174
```rust
    fn inspection_on_panicked_thread_reports_dead_safely() {
```

### src/runtime/reactor.rs

- Line 67
```rust
    fn default() -> Self {
```

- Line 97
```rust
type SpawnResult =
```

- Line 102
```rust
impl CoreReactor {
```

- Line 802
```rust
fn get_or_create_mixer(
```

- Line 827
```rust
fn mixer_owning_source(
```

- Line 845
```rust
fn shutdown_audio_workers(workers: &mut HashMap<u64, AudioWorkerTask>) {
```

### tests/runtime_audio_lifecycle.rs

- Line 1
```rust
// [::TICKET::] P15-7 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-7 --for-spec --no-implementation-order`.
```

- Line 2
```rust
// [::TICKET::] P8-1: Runtime Infrastructure — ABC closure integration tests.
```

### tests/verify_feature_additive_build.rs

- Line 54
```rust
fn default_build_graph_includes_cpal_via_default_cpal_input() -> Result<(), String> {
```

### tests/verify_spec_p9_1.rs

- Line 1
```rust
// [::TICKET::] P15-7 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-7 --for-spec --no-implementation-order`.
```

- Line 2
```rust
// [::TICKET::] P15-5 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-5 --for-spec --no-implementation-order`.
```
