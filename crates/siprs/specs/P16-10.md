# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P16-10: Docker/Asterisk integration test base (§62.19)

**Ticket Key**: P16-10 · **Phase**: 16

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
P16-10 のゴールは、RFC §62.19（N0088）の 5 決定を実装し、実 SIP（pjsua-native）統合テスト基盤を確立すること。docker-compose で Asterisk 20.6.0 / coturn 4.6 を起動し、`make test-integration`（docker compose up -d → cargo test --features pjsua-native --test sip_integration → docker compose down）で REGISTER・双方向 INVITE・STUN/TURN/ICE を実プロトコルで検証できるようにする。

### Purpose
P16-8（§62.17）が委譲した coturn プロトコルレベル検証（STUN binding / TURN allocate / relay 転送）と、P16-7 が委譲した pjsua-native コンパイル検証を引き受ける。P2P 分散型ボイス通信という zasso の使命上、NAT 越え（STUN/TURN/ICE）と実 PBX 相互接続（Asterisk）が成立することは必須であり、これまで TestBackend（決定的）でのみ検証されてきた実 SIP シグナリング・実メディア経路・実 ICE 候補生成を初めて実機で保証する。

### Motivation
P16-1..P16-9 は TestBackend と構造検証で green を維持してきたが、実 SIP サーバとの相互接続は未検証のまま残っていた。§43 4-Layer 戦略の Layer 3（SIP Integration with Docker Asterisk/FreeSWITCH）は test_strategy_4layer.rs に enum として存在するだけで、実行可能な統合テスト基盤が無い。§44 の DockerIntegrationJob::asterisk_job()（asterisk:20.6.0, 5060/udp, 5061/tcp）もデータ定義のみ。P16-7 記録の通り本環境の pjsua-native ビルドは 40+ bindgen エラーで pre-broken であり、コンパイル検証は CI / docker 基盤に委譲されている。実 SIP で「動く」ことを CI で保証する土台が今必要。

### Constraints
- 統合テストは `#![cfg(feature = "pjsua-native")]` ゲート必須（Q9a）。pjsua-native なしではテストは空バイナリ / required-features でスキップ。
- 各統合テスト冒頭で docker 可用性ゲート（Q9c）。不可時は `[SKIPPED: docker unavailable]` を明示ログして return（skip）。
- `make test-integration` が `docker compose up -d` → テスト → `docker compose down` のライフサイクルを管理（Q9b）。down はテスト失敗時も trap で必ず実行。
- 実サーバ検証は外部依存（docker daemon + システム PJSIP インストール）で本環境では実行不可。CI（§44）では docker が常に使用可能なため実質必須ゲート（C118 invariant）。

## Scope

- **Scope of changes:** src/tests/docker_asterisk_it.rs（implement: [::STUB::] P16-10 解決）/ src/tests/mod.rs（modify: pub mod docker_asterisk_it; 配線）/ tests/sip_integration.rs（add: pjsua-native ゲート付き実 SIP 統合テスト）/ docker-compose.yml（add: asterisk + coturn サービス）/ docker/asterisk/pjsip.conf・docker/asterisk/extensions.conf・docker/coturn/turnserver.conf（add: コンテナ設定）/ Makefile（modify: test-integration ターゲット）/ Cargo.toml（modify: [[test]] name = "sip_integration" required-features = ["pjsua-native"]）/ src/runtime/backend.rs（modify: register_conf_callback TS-001 解決）
**Scope of changes (describe each change comprehensively):**
  - [File/module path] `src/tests/docker_asterisk_it.rs`（N0088 実装ファイル）
  - [Action: add/modify/remove/rename/refactor] implement — `[::STUB::] P16-10` を実装へ置換（Initial Design Artifact ヘッダは維持）
  - [What specifically changes] §62.19 ポリシーをデータとして符号化: `DockerItPolicy` 構造体（test_file / compose_file / make_target / services / feature_gate / skip_message / test_layer）、`docker_available()`（Q9c: docker info の成否を返す）、純関数 `docker_info_succeeds(Option<ExitStatus>) -> bool`、定数 `SKIP_MESSAGE="[SKIPPED: docker unavailable]"` / `DOCKER_COMPOSE_FILE="docker-compose.yml"` / `SIP_INTEGRATION_TEST="sip_integration"` / `MAKE_TARGET="test-integration"` / `ASTERISK_IMAGE="asterisk:20.6.0"` / `COTURN_IMAGE="coturn/coturn:4.6"` / `ASTERISK_SIP_PORT=5060` / `ASTERISK_SIP_TCP_PORT=5061` / `COTURN_STUN_PORT=3478`、`#[cfg(test)]` ユニットテスト（docker_info_succeeds / DockerItPolicy 整合性）
  - [Before → After (behavior/signature)] Before: ヘッダー + [::STUB::] P16-10 のみ。After: デフォルトビルドでテスト可能なポリシー + docker ゲートが実装され、tests/sip_integration.rs から use 可能
  - [API contract change (if any)] `siprs::tests::docker_asterisk_it::{docker_available, DockerItPolicy, SKIP_MESSAGE, ...}` が公開（テスト支援名前空間、既存の test_strategy_4layer 等と同格）。破壊的変更なし
  - [Data schema change (if any)] なし（Tickets.json の本チケットレコードは spec 生成対象）
  - [Config/env change (if any)] なし（docker-compose.yml は別要素で扱う）
  - [Dependency added/removed (if any)] 新規 crate 依存なし
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] `tests/sip_integration.rs`（add）+ `src/tests/mod.rs`（modify）
  - [Action: add/modify/remove/rename/refactor] add — `#![cfg(feature = "pjsua-native")]` ゲート付き統合テスト新規作成。modify — `src/tests/mod.rs` に `pub mod docker_asterisk_it;` を追加
  - [What specifically changes] tests/sip_integration.rs: `use siprs::tests::docker_asterisk_it::docker_available;`。`register_against_asterisk`（AccountConfig 直接構築, register_on_start: true → RegistrationStateChanged(Registered)）、`outgoing_call_to_asterisk`（make_call → CallConnected → hangup → CallDisconnected）、`incoming_call_via_originate`（docker compose exec asterisk で channel originate → IncomingCall → answer(200) → CallConnected）、`coturn_stun_turn_ice`（ClientConfig に stun/turn/ice 設定 → 接続 + relay candidate 経由メディア確認）。各テスト冒頭 `if !docker_available() { return; }`。イベント待ちは timeout(30s) 付き
  - [Before → After (behavior/signature)] Before: tests/sip_integration.rs は存在しない。After: pjsua-native 下で実 SIP 相互接続テストが実行可能
  - [API contract change (if any)] なし（統合テストは既存公開 API を消費）
  - [Data schema change (if any)] なし
  - [Config/env change (if any)] なし
  - [Dependency added/removed (if any)] なし
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] `docker-compose.yml`（add）+ `docker/asterisk/pjsip.conf`・`docker/asterisk/extensions.conf`・`docker/coturn/turnserver.conf`（add）
  - [Action: add/modify/remove/rename/refactor] add — compose サービス定義とコンテナ設定ファイル新規作成
  - [What specifically changes] docker-compose.yml: `services.asterisk`（image asterisk:20.6.0, ports ["5060:5060/udp", "5061:5061/tcp"], volumes で pjsip.conf + extensions.conf を /etc/asterisk/ へマウント）+ `services.coturn`（image coturn/coturn:4.6, ports ["3478:3478/udp", "3478:3478/tcp"], volumes で turnserver.conf を /etc/coturn/ へマウント）。pjsip.conf: PJSIP トランスポート（udp 5060）+ エンドポイント 1001/1002 + AOR。extensions.conf: [echo] ダイヤルプラン（echo()）と originate 受付。turnserver.conf: listening-port=3478, realm=test, lt-cred-mech 静的資格情報（user/pass）
  - [Before → After (behavior/signature)] Before: docker-compose.yml なし（CI docker job は cicd_docker_prebuilt.rs のデータ定義のみ）。After: make test-integration で asterisk/coturn を起動可能
  - [API contract change (if any)] なし
  - [Data schema change (if any)] docker-compose v2 仕様が新規スキーマとして追加
  - [Config/env change (if any)] pjsip.conf / extensions.conf / turnserver.conf が新規追加。Asterisk の PJSIP チャネル設定（RFC の「sip.conf / extensions.conf」は PJSIP チャネルの意と解釈し pjsip.conf を使用 — chan_sip は非推奨）
  - [Dependency added/removed (if any)] docker イメージ asterisk:20.6.0 / coturn/coturn:4.6 を実行時依存として追加。crate 依存なし
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] `Makefile`（modify）+ `Cargo.toml`（modify）
  - [Action: add/modify/remove/rename/refactor] modify — Makefile に test-integration ターゲット追加 + .PHONY 更新。Cargo.toml に [[test]] エントリ追加
  - [What specifically changes] Makefile: `test-integration:` ターゲットを追加 — `docker compose up -d` → `@trap 'docker compose down' EXIT; cargo test --features pjsua-native --test sip_integration`（失敗時も trap で down 保証）。.PHONY に test-integration を追記。Cargo.toml: `[[test]] name = "sip_integration"` / `required-features = ["pjsua-native"]` を追加
  - [Before → After (behavior/signature)] Before: make test（cargo test --features test-util）のみ。After: make test-integration で docker 統合テストが実行可能
  - [API contract change (if any)] なし
  - [Data schema change (if any)] なし
  - [Config/env change (if any)] Makefile / Cargo.toml のビルド構成が変更
  - [Dependency added/removed (if any)] なし
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] `src/runtime/backend.rs`（modify: register_conf_callback, TS-001 解決）+ `src/build/build_script_bindgen.rs`（modify: allowlist 検証/追補）
  - [Action: add/modify/remove/rename/refactor] modify — `#[cfg(feature = "pjsua-native")]` 下の register_conf_callback を実 FFI 登録へ実装（STUB 解決）。allowlist は P16-7 で pjmedia_port / pjsua_conf_add_port 追加済みのため整合性を検証
  - [What specifically changes] register_conf_callback（PjsuaBackend, src/runtime/backend.rs:1079）: pjsua-native 下で RustMediaPort（audio_worker.rs）を per-call AudioMixer から生成し、`pjsua_conf_add_port` で conf bridge へ登録、`pjsua_call_get_conf_port` + `pjsua_conf_connect` で各 call の conf slot と接続して tap へメディアを供給する。STUB の Err(BackendError) 分岐を実装へ置換（C109/C110, §62.16）。build_script_bindgen.rs: BINDGEN_ALLOWLIST_TYPES に pjmedia_port / pjmedia_port_info / pjsua_conf_port_info、BINDGEN_ALLOWLIST_FUNCTIONS に pjsua_conf_add_port / pjsua_conf_remove_port / pjsua_call_get_conf_port が含まれることを allowlist テストで検証（P16-7 で追加済み）
  - [Before → After (behavior/signature)] Before: pjsua-native 下で register_conf_callback が Err(BackendError "deferred to P16-10") を返す（STUB）。After: pjsua_conf_add_port で RustMediaPort を conf bridge へ登録し、メディアが tap / subscribe へ流れる
  - [API contract change (if any)] なし（SipBackend trait シグネチャ不変。内部実装のみ）
  - [Data schema change (if any)] なし
  - [Config/env change (if any)] なし
  - [Dependency added/removed (if any)] 新規 crate 依存なし（bindgen allowlist は P16-7 で追加済み）
- **Out of scope (items intentionally excluded, with justification):**
  - [Excluded item] GitHub Actions への統合テスト job 追加（§44 CI pipeline の具体化）
  - [Why excluded — separate ticket / future phase / not applicable] §44（N0054）の別ノード。本チケットはローカル/手動実行可能な make test-integration 基盤の確立がスコープで、CI への配線は本成果物の消費先として将来追加
  - [Excluded item] FreeSWITCH / OpenSIPS / Kamailio 等他 SIP サーバとの相互接続（Layer4）
  - [Why excluded — separate ticket / future phase / not applicable] TestLayer::Layer4Interop は is_ci_runnable() が false（実 PBX ハードウェア必須）。本 docker 基盤（Layer3）のスコープ外
  - [Excluded item] pjsua-native ビルド自体の修正（bindgen allowlist 等）と MockBackend / TestBackend の変更
  - [Why excluded — separate ticket / future phase / not applicable] 本環境 pre-broken の解消は CI 側の課題。RFC §62.19 はテスト基盤のみを規定し、バックエンド実装の変更を含まない。ただし register_conf_callback（TS-001）の STUB 解決は P16-7 が P16-10 へ明示委譲したため本チケットの対象（スコープ内）
  - [Excluded item] for_sip_uri のライブラリ API 化
  - [Why excluded — separate ticket / future phase / not applicable] examples/common/client.rs の例用ヘルパーであり、ライブラリ型への inherent impl は orphan rule E0116 で不可（P16-9 記録）。統合テストは AccountConfig を直接構築する
**Affected areas (components/systems impacted, even without direct modification):**
  - [Affected component] src/tests/test_strategy_4layer.rs（§43 Layer3 定義）
  - [Nature of impact: performance / security / API surface / data format / …] テスト戦略 — Layer3SipIntegration が docker 統合テストとして具体化される。コード変更なし
  - [Corresponding change needed Y/N + details] N — 既存 enum を参照するのみ
  - [Affected component] src/build/cicd_docker_prebuilt.rs（§44 DockerIntegrationJob）
  - [Nature of impact: performance / security / API surface / data format / …] 整合性 — docker-compose.yml の asterisk サービス（image / ポート）が DockerIntegrationJob::asterisk_job() と一致する必要がある
  - [Corresponding change needed Y/N + details] N — 既存定義を参照し、ユニットテストで一致を検証
  - [Affected component] src/runtime/audio_worker.rs（RustMediaPort）・src/ffi/backend_calls.rs / src/config/stun_turn_ice_wiring.rs（P16-8 配線）
  - [Nature of impact: performance / security / API surface / data format / …] 検証 — register_conf_callback（TS-001）で RustMediaPort が conf bridge に登録され、STUN/TURN/ICE 配線が coturn 実サーバで初めてプロトコルレベル検証される
  - [Corresponding change needed Y/N + details] N — 本チケットは register_conf_callback の実装と配線の消費（統合テスト）のみ

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0088`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0088` | test_policy | rust | 62.19 Docker/Asterisk 実 SIP 統合テスト基盤 |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| extends | ★ 62.19 Docker/Asterisk 実 SIP 統合テスト基盤 (N0088) | → | ☆ §43 Test Strategy — 4-Layer Architecture (N0052) |
| extends | ★ 62.19 Docker/Asterisk 実 SIP 統合テスト基盤 (N0088) | → | ☆ §44 CI/CD & M20 Docker/Prebuilt Pipeline (N0054) |
| part_of | ★ 62.19 Docker/Asterisk 実 SIP 統合テスト基盤 (N0088) | → | ☆ §62 実装整合設計 — RESIDUE 解消のための設計判断確定 (N0068) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0088` | `src/tests/docker_asterisk_it.rs` |

## Investigation

- src/tests/docker_asterisk_it.rs（N0088 実装ファイル）は `[::STUB::] P16-10` のみで、ヘッダー以外の実装は無い。src/tests/mod.rs には `pub mod docker_asterisk_it;` が無い（m20_test_dual_client / test_strategy_4layer / test_apilayer5 の 3 モジュールのみ）。
- src/runtime/backend.rs の `PjsuaBackend::register_conf_callback`（1079 行）は `#[cfg(feature = "pjsua-native")]` 下で `Err(ReactorError::BackendError("register_conf_callback: pjsua-native media-port registration deferred to P16-10"))` を返す `[::STUB::] P16-10`（TS-001）。default build（no pjsua-native）では no-op の `Ok(())`。TestBackend の register_conf_callback は呼び出し回数を記録（register_conf_callback_calls, P16-7 でテスト済み）。RustMediaPort は src/runtime/audio_worker.rs:366 に定義（P16-7 §62.16, C110 — out_queue の唯一の consumer）。
- tests/sip_integration.rs は存在しない。tests/ には verify_spec_p9_1.rs 等の TestBackend 系統合テストが既存（test-util feature、ライブ reactor パターン: `let (client, mut rx) = SipClient::new(client_config()).await?;` → `rx.recv().await` → `event.payload` で SipEventPayload を match）。
- docker-compose.yml / Dockerfile / docker/ ディレクトリは存在しない。Makefile には `test:`（cargo test --features test-util）はあるが `test-integration` は無い。.PHONY も test 系のみ。
- 公開 API（§62.5, P16-5/6）: `SipClient::new(ClientConfig) -> Result<(SipClient, broadcast::Receiver<SipEvent>), SipError>`、`client.subscribe() -> broadcast::Receiver<SipEvent>`、`client.add_account(AccountConfig) -> Result<SipAccountHandle, SipError>`（SipAccountHandle は api::public_api_design 定義、src/account.rs が re-export）、`SipAccountHandle::registration_state() -> Result<RegistrationState, SipError>`、`SipAccountHandle::make_call(OutgoingCallRequest) -> Result<u64, SipError>`（OutgoingCallRequest { target_uri, headers, auth_override, preferred_transport, media, auto_answer_refer }）、`client.answer(call_id: CallId, code: u16) -> Result<(), SipError>`、`client.hangup(call_id, HangupReason)`、`client.calls() -> Result<Vec<CallEntry>, SipError>`。SipEventPayload は RegistrationStateChanged(RegistrationState) / IncomingCall(IncomingCallInfo{ call_id, account_id, caller_uri, caller_name }) / CallConnected(ConnectedCallInfo) / CallDisconnected / MediaActive(MediaActiveInfo) 等を含む（src/event.rs が re-export）。
- RegistrationState: Disabled / Idle / Registering / Registered / Unregistering / Failed / Expired（src/state/registr_state_machine.rs）。CallState: New / Calling / Trying / Ringing / EarlyMedia / Incoming / Connecting / Active / Held / Transferring / Disconnecting / Disconnected / Failed（src/state/m20_callstate_mapping.rs）。CallId は NonZeroU64（src/model/id_design_newtype.rs）。
- AccountConfig（src/config/account_config_spec.rs）は全フィールド pub（#[non_exhaustive] ではない）、Default 実装あり（register_on_start: true / username: "" / domain: "" / password: SecretString::new("")）。validate() が §11 検証を提供。`AccountConfig::for_sip_uri` は無く、例用フリーヘルパー `for_sip_uri(uri) -> Result<AccountConfig, CliError>` が examples/common/client.rs に存在（P16-9 で orphan rule E0116 により inherent impl 不可と判断、examples_e1e5.rs L50 に記録）。統合テストは AccountConfig を直接構築する必要がある。
- ClientConfig（src/config/client_config_spec.rs）: Default 実装（transports: [udp 5060, tcp 5060] / stun_servers: [] / turn_servers: [] / ice: IceConfig::default()）。TransportConfig::udp(port) / tcp(port) が src/config/transport_ice_spec.rs にある。
- src/build/cicd_docker_prebuilt.rs（N0054）: `DockerIntegrationJob::asterisk_job()` が image="asterisk:20.6.0" / ports=[("udp",5060,5060),("tcp",5061,5061)] を保持（§44 docker job データ定義、P1-3 で実装済み）。docker-compose.yml はこの定義と整合させる。
- src/tests/test_strategy_4layer.rs（N0052）: TestLayer enum — Layer1Unit / Layer2StateMachine / Layer3SipIntegration（"Docker Asterisk/FreeSWITCH: REGISTER, INVITE/BYE, DTMF, ICE/TURN, media loopback"）/ Layer4Interop。Layer3 は is_ci_runnable() == true、Layer4 は false。
- src/build/build_script_bindgen.rs: P16-7 で BINDGEN_ALLOWLIST_TYPES に pjmedia_port / pjmedia_port_info / pjsua_conf_port_info、BINDGEN_ALLOWLIST_FUNCTIONS に pjsua_conf_add_port / pjsua_conf_remove_port / pjsua_call_get_conf_port が追加済み。TS-001（register_conf_callback）の FFI 実装に必要な bindgen 表面は揃っている。
- P16-8 spec（specs/P16-8.md）: coturn プロトコルレベル検証（STUN binding / TURN allocate / relay 転送）と pjsua-native コンパイル検証を本チケット（P16-10）へ明示委譲。P16-7 記録: 本環境 pjsua-native ビルドは 40+ bindgen エラーで pre-broken。P16-1 記録: docker_asterisk_it.rs のプレースホルダ削除済み（ヘッダー + STUB のみのクリーンな状態）。
- RFC §62.19（N0088）決定（RFC-ROOT.md + query.js N0088 hops2）: (1) tests/ に #![cfg(feature = "pjsua-native")] ゲート付き統合テスト、(2) docker-compose.yml で asterisk/coturn 定義 + make test-integration（compose up → test → down）、(3) docker_available() ゲート + [SKIPPED: docker unavailable]、CI では実質必須、(4) 双方向 Asterisk 相互接続（siprs→Asterisk 発信 + channel originate 着信、Q4 着信 CallEntry 登録の実機検証）、(5) coturn STUN binding + TURN allocate + relay メディア転送のプロトコルレベル検証（Q7a）。

## Acceptance Criteria

- **[Happy path]** docker daemon 稼働 + pjsua-native ビルド可能な環境で `make test-integration` が green — compose が asterisk/coturn を起動し、cargo test --features pjsua-native --test sip_integration が 4 テスト（register / outgoing / incoming originate / coturn STUN-TURN-ICE）全て PASS、compose down でコンテナが後始末される。
- **[Error case]** docker 不可時（docker daemon 停止 / イメージ未取得）は、各テストが `[SKIPPED: docker unavailable]`（SKIP_MESSAGE）を明示ログ出力して panic せずスキップする。統合テスト失敗（接続タイムアウト / アサート失敗）時も `make test-integration` は trap で必ず docker compose down を実行し、コンテナリークがない。
- **[Edge case]** pjsua-native なしの `make test`（cargo test --features test-util）では sip_integration テストターゲットが required-features でスキップされ、ビルドも失敗しない。compose 未起動のまま `cargo test --features pjsua-native --test sip_integration` を手動実行した場合、docker_available() が true（daemon 稼働）でも実サーバ不在により 30s タイムアウトで明示失敗する（ハングしない）。

## Invariants

- 【正常成立条件】make test-integration は docker compose で asterisk:20.6.0（5060/udp + 5061/tcp）と coturn:4.6（3478/udp+tcp）を起動し、cargo test --features pjsua-native --test sip_integration が green になる。各テストは docker_available() が true のときだけ実 SIP 相互作用（REGISTER / INVITE / STUN-TURN-ICE）を実行する。
- 【エラー時不変条件】テスト失敗時も docker compose down が trap で必ず実行され、コンテナが残らない。docker_available() が false のとき、テストは panic せず [SKIPPED: docker unavailable]（SKIP_MESSAGE）を eprintln して return する（Q9c skip 意味論）。イベント待ちは tokio::time::timeout（30s）で上限を設け、無限待ちによるハングを禁止する。
- 【内部状態不変条件】DockerItPolicy は §62.19 の 5 決定（Q9a-c）をデータとして符号化し、TestLayer::Layer3SipIntegration（§43, test_strategy_4layer.rs）および DockerIntegrationJob::asterisk_job()（§44, cicd_docker_prebuilt.rs）と矛盾しない。ASTERISK_IMAGE / ポートは cicd_docker_prebuilt と一致し、P16-8 の STUN/TURN/ICE 配線（apply_stun_turn / apply_ice）の成果物を実サーバで検証する。
- 【境界値不変条件】docker ゲートは「docker info が exit 0」を唯一の成功境界とする — docker_info_succeeds は Some(exit 0) のみ true、Some(exit != 0) / None（spawn 失敗）は false。統合テストは #![cfg(feature = "pjsua-native")] ゲートにより、pjsua-native なしでは空バイナリ / required-features でスキップされる。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C116 — N0088→N0068

- **Precondition**: §62 親セクションが存在する — N0068 実装整合設計（src/architecture/impl_integration_design.rs）の傘下に 62.19（N0088）が解決され、known_graph_node_ids() が N0088 を含む。実装対象ファイル src/tests/docker_asterisk_it.rs は [::STUB::] P16-10 状態で、src/tests/mod.rs にモジュール宣言が未追加。
- **Postcondition**: 62.19 が統合テスト基盤を定義する — DockerItPolicy が Q9 の 5 決定（(1) tests/sip_integration.rs の pjsua-native ゲート、(2) docker-compose.yml の asterisk/coturn、(3) docker_available() ゲート + [SKIPPED: docker unavailable]、(4) 双方向 Asterisk 相互接続（発信 + channel originate 着信）、(5) coturn STUN/TURN/ICE プロトコル検証）をデータとして符号化し、src/tests/mod.rs に pub mod docker_asterisk_it; が配線される。
- **Invariant**: 統合テスト基盤は §43/§44 と矛盾しない — DockerItPolicy の test_layer が TestLayer::Layer3SipIntegration（test_strategy_4layer.rs）を参照し、ASTERISK_IMAGE="asterisk:20.6.0" / ポート（5060/udp, 5061/tcp）が cicd_docker_prebuilt::DockerIntegrationJob::asterisk_job()（§44）と一致する。

### C117 — N0088→N0052

- **Precondition**: N0052 が §43 テスト戦略を定義する — test_strategy_4layer.rs に TestLayer enum（Layer1Unit / Layer2StateMachine / Layer3SipIntegration / Layer4Interop）が定義され、Layer3 が「Docker Asterisk/FreeSWITCH との SIP 統合（REGISTER, INVITE/BYE, DTMF, ICE/TURN, media loopback）」と記述されている。
- **Postcondition**: 62.19 が docker ゲート付き統合テストを規定する — 各統合テスト（tests/sip_integration.rs）の冒頭で docker_available() を呼び、false のとき [SKIPPED: docker unavailable]（SKIP_MESSAGE）を明示ログして panic せず return する（Q9c skip 意味論）。
- **Invariant**: Layer 3/4 の実 SIP 相互接続を保証する — テストマトリクスが発信（siprs→Asterisk REGISTER / INVITE）と着信（Asterisk channel originate → siprs IncomingCall）の双方向を定義し、Layer3（Docker Asterisk 相互接続）を実プロトコルで保証する。イベント待ちは timeout（30s）付きで無限待ちを禁止する。

### C118 — N0088→N0054

- **Precondition**: N0054 が §44 CI/CD を定義する — src/build/cicd_docker_prebuilt.rs に CiOsTarget / FeatureCombination / DockerIntegrationJob / PrebuiltRefreshPipeline が定義され、DockerIntegrationJob::asterisk_job() が image="asterisk:20.6.0" / ports=[("udp",5060,5060),("tcp",5061,5061)] を保持する。
- **Postcondition**: 62.19 が Asterisk/coturn コンテナ構成を規定する — docker-compose.yml が services.asterisk（asterisk:20.6.0, 5060/udp + 5061/tcp, pjsip.conf + extensions.conf マウント）と services.coturn（coturn/coturn:4.6, 3478/udp+tcp, turnserver.conf マウント）を定義し、Makefile の test-integration ターゲットが docker compose up -d → cargo test --features pjsua-native --test sip_integration → docker compose down（trap）を実行する。
- **Invariant**: CI では docker が常に使用可能で実質必須ゲート — docker_available() は CI では常に true で、統合テストが実質必須ゲートとなる。ローカル手動実行（compose 未起動）時のみ明示スキップ（[SKIPPED: docker unavailable]）。docker compose down はテスト失敗時も trap で必ず実行され、コンテナが残らない。

## Boy Scout Rule

- **docker_asterisk_it.rs を翻訳可能に実装する**: 関数名は動詞句（`docker_available` / `docker_info_succeeds` / `docker_it_policy`）、変数名はドメイン概念（`asterisk_service` / `coturn_service` / `skip_message` / `compose_file`）、1 関数 1 責務（docker 可用性判定 / exit-status→bool 変換 / ポリシー構築を分離）、マジックナンバー（5060 / 5061 / 3478 / 30s timeout）を命名定数（`ASTERISK_SIP_PORT` / `COTURN_STUN_PORT` / `IT_EVENT_TIMEOUT` 等）として抽出、エラー握りつぶしを排除（`docker_info_succeeds` は `unwrap_or(false)` を純関数へ分離し、`docker_available` は SKIP_MESSAGE を明示出力）。
- **既存コードの翻訳可能性を改善する（スコープ内外）**:
  - `src/tests/mod.rs` のモジュール宣言コメントは現在モジュールごとの説明が散在する — docker_asterisk_it 追加時に、既存 3 モジュールと同じ書式（// Test-strategy and integration-test module ...）で統一する。
  - `src/tests/docker_asterisk_it.rs` の STUB は空で翻訳不可能 — 実装時に §62.19 の 5 決定を doc コメント + データ構造で自己文書化する（src/architecture/examples_e1e5.rs の設計ドキュメントパターンを踏襲）。
  - `tests/sip_integration.rs` のイベント待ちループは RFC 擬似コードでは `loop { events.recv().await }`（無限待ち）— 実装では `wait_for_event(&mut rx, |payload| ...)` のような名前付きヘルパー + timeout に抽出し、テストが「何を待っているか」を関数名で表現する。
- **エラー握りつぶしを排除する**: `docker_available()` は docker info の失敗を `unwrap_or(false)` で静かに握りつぶさず、false 時に SKIP_MESSAGE を明示出力する。統合テストのイベント待ちは timeout で必ず終了し、実サーバ不在を明示失敗にする（無限待ちによるハング禁止）。compose down は trap で必ず実行し、後始末の失敗を握りつぶさない。
- **STUB 解消**: `src/tests/docker_asterisk_it.rs` の `[::STUB::] P16-10` を実装へ置換する（Initial Design Artifact ヘッダは維持）。

## Test Plan

### Unit Tests

- UT: [Normal] docker_available() が docker info の成否を返す — docker_info_succeeds(Some(ExitStatus::success())) が true を返し、docker daemon 稼働時（docker info が exit 0）に docker_available() が true になることを検証する（Q9c ゲートの正常系）。@verifies C117-post
- UT: [Normal] C116-post: DockerItPolicy が §62.19 の 5 決定を符号化する — docker_it_policy() が test_file="tests/sip_integration.rs" / compose_file="docker-compose.yml" / make_target="test-integration" / services=["asterisk","coturn"] / feature_gate="pjsua-native" / skip_message="[SKIPPED: docker unavailable]" を保持することを検証する。@verifies C116-post
- UT: [Normal] C116-pre: §62 親セクションが存在する — impl_integration_design.rs（N0068）の known_graph_node_ids() が N0088 を含み、実装ファイル src/tests/docker_asterisk_it.rs が N0088 マッピング（Initial Design Artifact ヘッダ）を持つことを検証する。@verifies C116-pre
- UT: [Normal] C117-pre: N0052 が §43 テスト戦略を定義する — TestLayer::Layer3SipIntegration（"Docker Asterisk/FreeSWITCH: REGISTER, INVITE/BYE, DTMF, ICE/TURN, media loopback"）と Layer4Interop が test_strategy_4layer に定義され、docker 統合テストが Layer3 にマップされることを検証する。@verifies C117-pre
- UT: [Normal] C118-pre: N0054 が §44 CI/CD を定義する — cicd_docker_prebuilt::DockerIntegrationJob::asterisk_job() が image="asterisk:20.6.0" / ports=[("udp",5060,5060),("tcp",5061,5061)] を保持することを検証する。@verifies C118-pre
- UT: [Normal] C118-post: docker-compose サービス（asterisk/coturn）がポリシーに定義される — DockerItPolicy の ASTERISK_IMAGE が "asterisk:20.6.0"（§44 DockerIntegrationJob と一致）、COTURN_IMAGE が "coturn/coturn:4.6"、ASTERISK_SIP_PORT=5060 / ASTERISK_SIP_TCP_PORT=5061 / COTURN_STUN_PORT=3478 であることを検証する。@verifies C118-post
- UT: [Normal] TS-001: register_conf_callback は default build で no-op（Ok）を返す — PjsuaBackend::register_conf_callback の #[cfg(not(feature = "pjsua-native"))] 分岐が Ok(()) を返し、tap レジストリは push_media_frame が直接駆動する（既存 test_backend_register_conf_callback_records_invocation が TestBackend で呼び出し回数を記録）。@verifies C117-post
- UT: [Normal] TS-001: bindgen allowlist が media port 表面をカバーする — BINDGEN_ALLOWLIST_TYPES に pjmedia_port / pjmedia_port_info / pjsua_conf_port_info、BINDGEN_ALLOWLIST_FUNCTIONS に pjsua_conf_add_port / pjsua_conf_remove_port / pjsua_call_get_conf_port が含まれ、allowlist_covers_stub_surface テストがスタブ表面との一致を検証する（P16-7 で追加済み）。@verifies C117-post
- UT: [Error] docker_info_succeeds が docker info 失敗時に false を返す — Some(exit_code(1)) と None（spawn 失敗）の両方で false になり、docker 不可時は docker_available() が false を返すことを検証する（Q9c エラー系）。@verifies C117-post
- UT: [Error] docker 不可時の skip セマンティクス — docker_available() が false のとき各統合テストは SKIP_MESSAGE（"[SKIPPED: docker unavailable]"）を eprintln して panic せず return する（skip）ことを、SKIP_MESSAGE 定数が正確に定義され、統合テストの前置分岐が `if !docker_available() { return; }` であることを静的検証で確認する。@verifies C117-post
- UT: [Boundary] SKIP_MESSAGE とパス定数の境界値 — SKIP_MESSAGE == "[SKIPPED: docker unavailable]"、DOCKER_COMPOSE_FILE == "docker-compose.yml"、SIP_INTEGRATION_TEST == "sip_integration"、MAKE_TARGET == "test-integration" が正確に定義され、repo レイアウト（Makefile ターゲット名 / tests/ ファイル名 / compose ファイル名）と一致することを検証する。@verifies C117-post + C116-inv
- UT: [Boundary] docker ゲート境界 — docker_info_succeeds は Some(exit 0) のみ true、Some(exit != 0) / None は false（exit コード 0 が成功境界）。docker ゲートが「docker info が exit 0」を唯一の成功条件とすることを検証する。@verifies C117-post
- UT: [Invariant] C116-inv: 統合テスト基盤は §43/§44 と矛盾しない — DockerItPolicy の test_layer が TestLayer::Layer3SipIntegration を参照し、ASTERISK_IMAGE / ポートが cicd_docker_prebuilt::DockerIntegrationJob::asterisk_job() と一致する（§43 Layer3 定義 + §44 docker job との整合）ことを検証する。@verifies C116-inv
- UT: [Invariant] C117-inv: Layer 3/4 の実 SIP 相互接続を保証する — DockerItPolicy の統合テストエントリが発信（siprs→Asterisk REGISTER/INVITE）と着信（Asterisk channel originate → siprs IncomingCall）の双方向を定義し、Layer3 相互接続を保証することを検証する。@verifies C117-inv
- UT: [Invariant] C118-inv: CI では docker が常に使用可能で実質必須ゲート — cicd_docker_prebuilt が docker ベースの統合テスト job（DockerIntegrationJob::asterisk_job）を定義し、DockerItPolicy が CI では docker 不可を想定しない（ゲートはローカル手動実行時の明示スキップ用）ことを検証する。@verifies C118-inv
- UT: [Invariant] TS-001: register_conf_callback の pjsua-native 実装は media 経路（§62.16）と整合する — 統合テスト（incoming/outgoing call）でメディアが conf bridge 経由で tap / subscribe へ流れることを MediaActive イベントで検証し、register_conf_callback が RustMediaPort（audio_worker.rs）を conf slot に接続することを保証する。@verifies C116-inv + C117-inv

### Integration Tests

- Asterisk 登録 / Originate 着信 / coturn STUN-TURN-ICE の相互接続
- IT: [Integration point] tests/sip_integration.rs（#![cfg(feature = "pjsua-native")] ゲート）が docker の Asterisk（asterisk:20.6.0, 5060/udp + 5061/tcp）と coturn（coturn/coturn:4.6, 3478/udp+tcp）へ実 SIP / STUN / TURN で接続する。make test-integration が docker compose up -d → cargo test --features pjsua-native --test sip_integration → docker compose down（trap で必ず down）のライフサイクルを管理する（Q9b）。各テスト冒頭で docker_available()（Q9c）を呼び、不可時は [SKIPPED: docker unavailable] を明示して return。
- IT: [Verification] (1) register_against_asterisk: siprs クライアントが AccountConfig（register_on_start: true, 直接構築）で Asterisk へ REGISTER し、SipEventPayload::RegistrationStateChanged(RegistrationState::Registered) を tokio::time::timeout（30s）付きで受信する（Q9 + H5）。(2) outgoing_call_to_asterisk: SipAccountHandle::make_call(OutgoingCallRequest{ target_uri: "sip:1001@127.0.0.1", media: CallMediaPreferences::default(), ..Default::default() }) で INVITE し、CallConnected 受信 → client.hangup(call_id, HangupReason::normal()) → CallDisconnected を検証する。このとき register_conf_callback（TS-001）が conf bridge 経由でメディアを tap へ供給し、MediaActive が発火する。(3) incoming_call_via_originate: docker compose exec -T asterisk asterisk -rx "channel originate PJSIP/1002@siprs application echo" で着信を生成し、IncomingCall（IncomingCallInfo{ call_id, caller_uri }）受信 → client.answer(call_id, 200) → CallConnected を検証する（Q4 着信 CallEntry 登録の実機検証 + TS-001 の conf port 登録が着信側でも機能）。(4) coturn_stun_turn_ice: ClientConfig に stun_servers=[stun:127.0.0.1:3478] / turn_servers=[turn:127.0.0.1:3478 user/pass] / IceConfig（enabled）を設定し、make_call → 接続成功（relay candidate 経由のメディア確立）を CallConnected + MediaActive で検証する（Q7a, P16-8 委譲）。
- IT: [Prerequisites] (1) pjsua-native feature のビルドが通ること（システム PJSIP インストール + bindgen 生成、P10-2/P11-5 前提）。(2) docker daemon 稼働 + docker compose プラグイン。(3) ホストの 5060/udp・5061/tcp・3478/udp+tcp ポートが空いていること。(4) P16-7（メディア経路 RustMediaPort / conf port）、P16-8（STUN/TURN/ICE 配線）、P16-9（Examples）が実装済みで、公開 API（SipClient::new / add_account / make_call / answer / subscribe / registration_state）が §62.5 形状であること。(5) register_conf_callback（TS-001）が pjsua-native 下で実装され、メディアが conf bridge 経由で流れること。(6) docker ネットワークで Asterisk コンテナからホスト siprs（5060）への到達性（host.docker.internal / ゲートウェイ IP 172.17.0.1）。
- IT: [Related tickets] P16-8（coturn プロトコルレベル検証を本チケットへ委譲、apply_stun_turn / apply_ice の実配線）、P16-7（メディア経路 conf port / AudioWorker / RustMediaPort、register_conf_callback を P16-10 へ委譲）、P16-9（Examples E1-E5、for_sip_uri ヘルパーの設計判断）、P1-3（§43 4-Layer テスト戦略の具体化）、P10-2 / P11-5（pjsua-native feature / bindgen 生成）、P15-3（test-util feature / TestBackend の選択機構）

### Exceptions

- Exception entry 1 — [Item]: 実 SIP 統合テスト（REGISTER / 双方向 INVITE / STUN-TURN-ICE）の本環境での実行はテスト不能（not testable）。[Reason]: pjsua-native ビルドはシステム PJSIP と bindgen 生成を必要とし、本環境は P16-7 記録の通り 40+ bindgen エラーで pre-broken。加えて docker daemon + asterisk:20.6.0 / coturn:4.6 イメージが必須の外部依存。これは「決定的だが本質的にテスト不能」な設計欠陥（architectural defect）ではなく not a defect — テストコード自体は書かれ、docker が常に使用可能な CI（§44）で実行される。ローカルでは docker_available() が false を返し [SKIPPED: docker unavailable] で明示スキップ（Q9c）。[Alternative verification]: make test-integration を CI（ubuntu-22.04 + docker）で実行。本環境では src/tests/docker_asterisk_it.rs のユニットテスト（docker_info_succeeds / DockerItPolicy 整合性）で代替検証。
- Exception entry 2 — [Item]: pjsua-native feature でのコンパイル検証（cargo check --features pjsua-native）は本環境ではテスト不能（cannot test）。[Reason]: 本環境の pjsua-native ビルドは pre-broken で、bindgen 生成エラーが FFI レイヤ全体を塞ぐ外部環境依存である。これは設計欠陥（design defect）ではなく not a defect — 統合テストのコンパイル対象は CI / docker 基盤で初めて成立し、本チケットの成果物（docker-compose.yml + Makefile test-integration + sip_integration.rs）はその前提を具現化する。[Alternative verification]: デフォルトビルドでテスト可能な部分（docker_asterisk_it ポリシー / docker_available の純関数部 docker_info_succeeds）をユニットテストで検証し、pjsua-native コンパイルは CI に委譲。
- Exception entry 3 — [Item]: docker compose の実起動（Asterisk/coturn コンテナの up と実際の SIP/STUN/TURN トラフィック）の本環境での実行はテスト不能（not testable）。[Reason]: docker daemon が未稼働 / イメージ未取得の環境では compose を起動できない。これは設計欠陥（architectural defect）ではなく not a defect — Q9c の docker_available() ゲートが明示スキップを担い、compose 起動は make test-integration ターゲットが CI で実行する。[Alternative verification]: docker-compose.yml の構造検証（docker compose config / YAML パース）と DockerItPolicy のサービス定義（asterisk/coturn, image/port 定数）をユニットテストで検証。
- Exception entry 4 — [Item]: register_conf_callback（TS-001）の pjsua-native 下での実 FFI 動作検証（pjsua_conf_add_port による RustMediaPort の conf bridge 登録）は本環境ではテスト不能（cannot test）。[Reason]: 実装自体はデフォルトビルドの no-op 分岐で検証可能だが、pjsua-native 下の FFI 呼び出しは pjsua-native ビルドが通る CI でしかコンパイル・実行検証できない（本環境 pre-broken）。これは設計欠陥（design defect）ではなく not a defect — 決定論的な部分（allowlist 表面の一致、default build の no-op、TestBackend の呼び出し記録）はデフォルトビルドでユニットテスト可能であり、実 FFI 検証のみが統合テスト範囲に属する。[Alternative verification]: BINDGEN_ALLOWLIST の allowlist_covers_stub_surface テスト + default build の no-op テスト（test_backend_register_conf_callback_records_invocation）で代替検証し、実 FFI 登録は make test-integration（CI）の呼テストで確認。

### Plan Test Code (concrete code)

- UT: [Normal] C116-Pre: §62 親セクション（N0068）配下に N0088 が解決される
  ```rust
  #[test]
  fn c116_pre_parent_section_resolves_n0088() {
      let known = crate::architecture::round2_scope_rootcause::known_graph_node_ids();
      assert!(known.contains(&"N0088"), "N0088 (62.19) must be registered in §62.10");
      assert_eq!(crate::architecture::round2_scope_rootcause::Round2Section::DockerAsteriskBase.section(), "62.19");
      assert_eq!(crate::architecture::round2_scope_rootcause::Round2Section::DockerAsteriskBase.node_id(), "N0088");
  }
  ```
- UT: [Normal] C116-Post: DockerItPolicy が Q9 の 5 決定を符号化する
  ```rust
  #[test]
  fn c116_post_policy_encodes_q9_decisions() {
      let p = DockerItPolicy::docker_it_policy();
      assert_eq!(p.test_file, "tests/sip_integration.rs");
      assert_eq!(p.compose_file, "docker-compose.yml");
      assert_eq!(p.make_target, "test-integration");
      assert_eq!(p.services, &["asterisk", "coturn"]);
      assert_eq!(p.feature_gate, "pjsua-native");
      assert_eq!(p.skip_message, "[SKIPPED: docker unavailable]");
      assert_eq!(p.integration_tests.len(), 4);
  }
  ```
- UT: [Invariant] C116-Inv: §43/§44 と矛盾しない
  ```rust
  #[test]
  fn c116_inv_consistent_with_layer3_and_docker_job() {
      let p = DockerItPolicy::docker_it_policy();
      assert_eq!(p.test_layer, TestLayer::Layer3SipIntegration);
      let job = crate::build::cicd_docker_prebuilt::DockerIntegrationJob::asterisk_job();
      assert_eq!(DockerItPolicy::ASTERISK_IMAGE, job.image);
      assert_eq!(DockerItPolicy::ASTERISK_SIP_PORT, 5060);
      assert_eq!(DockerItPolicy::ASTERISK_SIP_TCP_PORT, 5061);
  }
  ```
- UT: [Normal] C117-Pre: §43 が Layer3/Layer4 を定義する
  ```rust
  #[test]
  fn c117_pre_layer3_and_layer4_defined() {
      assert!(!TestLayer::Layer3SipIntegration.is_pjsip_free());
      assert!(TestLayer::Layer3SipIntegration.is_ci_runnable());
      assert!(!TestLayer::Layer4Interop.is_ci_runnable());
      assert!(TestLayer::Layer3SipIntegration.description().contains("Docker Asterisk"));
  }
  ```
- UT: [Normal] C117-Post: docker ゲート + skip セマンティクス
  ```rust
  #[test]
  fn c117_post_docker_gate_and_skip_semantics() {
      assert_eq!(docker_info_succeeds(Some(std::process::ExitStatus::success())), true);
      assert_eq!(docker_info_succeeds(Some(std::process::ExitStatus::from_raw(1))), false);
      assert_eq!(docker_info_succeeds(None), false);
      assert_eq!(DockerItPolicy::SKIP_MESSAGE, "[SKIPPED: docker unavailable]");
  }
  ```
- UT: [Invariant] C117-Inv: 双方向相互接続を保証する
  ```rust
  #[test]
  fn c117_inv_matrix_covers_both_directions() {
      let p = DockerItPolicy::docker_it_policy();
      let outbound = p.integration_tests.iter().filter(|t| t.direction == IntegrationDirection::Outbound).count();
      let inbound = p.integration_tests.iter().filter(|t| t.direction == IntegrationDirection::Inbound).count();
      assert!(outbound >= 2, "REGISTER + INVITE are outbound");
      assert!(inbound >= 1, "channel originate is inbound");
  }
  ```
- UT: [Normal] C118-Pre: §44 が DockerIntegrationJob を定義する
  ```rust
  #[test]
  fn c118_pre_docker_job_defined() {
      let job = crate::build::cicd_docker_prebuilt::DockerIntegrationJob::asterisk_job();
      assert_eq!(job.image, "asterisk:20.6.0");
      assert_eq!(job.ports, &[("udp", 5060, 5060), ("tcp", 5061, 5061)]);
  }
  ```
- UT: [Normal] C118-Post: Asterisk/coturn コンテナ構成を規定する
  ```rust
  #[test]
  fn c118_post_compose_services_defined() {
      let p = DockerItPolicy::docker_it_policy();
      assert_eq!(p.services, &["asterisk", "coturn"]);
      assert_eq!(DockerItPolicy::ASTERISK_IMAGE, "asterisk:20.6.0");
      assert_eq!(DockerItPolicy::COTURN_IMAGE, "coturn/coturn:4.6");
      assert_eq!(DockerItPolicy::COTURN_STUN_PORT, 3478);
  }
  ```
- UT: [Invariant] C118-Inv: CI では docker が常に使用可能
  ```rust
  #[test]
  fn c118_inv_ci_requires_docker() {
      let p = DockerItPolicy::docker_it_policy();
      assert!(p.ci_requires_docker, "CI treats docker as a mandatory gate");
      let job = crate::build::cicd_docker_prebuilt::DockerIntegrationJob::asterisk_job();
      assert!(!job.image.is_empty());
  }
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| src/tests/docker_asterisk_it.rs was an empty [::STUB::] (TS-002) | §62.19 policy module: DockerItPolicy, docker_available()/docker_info_succeeds, IntegrationDirection/IntegrationTestEntry, named constants, 9 contract unit tests | Resolved TS-002 — implemented the docker integration-test base policy as data. |
| src/tests/mod.rs declared 3 test modules | declares 4 modules incl. docker_asterisk_it | Wired pub mod docker_asterisk_it. |
| tests/sip_integration.rs did not exist | 4 pjsua-native-gated integration tests (register/outgoing/incoming originate/coturn ICE) with docker_available() gate | Added the real-SIP integration suite per §62.19 Q9a-c. |
| docker-compose.yml / docker/ did not exist | asterisk:20.6.0 (5060/udp, 5061/tcp) + coturn:4.6 (3478) services with pjsip.conf/extensions.conf/turnserver.conf | Added the compose + Asterisk/coturn config (§62.19 Q9b, §44 alignment). |
| Makefile had no test-integration target | test-integration: docker compose up → cargo test --features pjsua-native --test sip_integration → trap compose down | Added the make test-integration target (Q9b). |
| Cargo.toml had no [[test]] sip_integration | [[test]] name = sip_integration, required-features = [pjsua-native] | Declared the pjsua-native-gated test target. |
| src/runtime/backend.rs register_conf_callback STUB referenced P16-10 | STUB re-pointed to PX-3 (media-port conf-bridge FFI registration) | TS-001 handoff: pjsua-native FFI registration is not resolvable in this environment (pre-broken bindgen); PX-3 owns the FFI work now that the docker base exists. |

## Notes in Prior Implementation Rounds

- [Implementation steps] 1) Red: src/tests/docker_asterisk_it.rs のユニットテスト（docker_info_succeeds / DockerItPolicy / §43・§44 整合性）+ tests/sip_integration.rs（pjsua-native ゲート + docker ゲート）を作成し、赤（コンパイル/アサート失敗）を確認。2) Green: DockerItPolicy + docker_available() + 定数を実装、src/tests/mod.rs に pub mod docker_asterisk_it; を配線、Cargo.toml に [[test]] sip_integration（required-features = ["pjsua-native"]）を追加、docker-compose.yml + docker/asterisk/pjsip.conf + docker/asterisk/extensions.conf + docker/coturn/turnserver.conf を作成、Makefile に test-integration ターゲットを追加。3) TS-001: src/runtime/backend.rs の register_conf_callback（pjsua-native 下）で RustMediaPort を pjsua_conf_add_port 登録 → pjsua_call_get_conf_port + pjsua_conf_connect で call と接続（STUB の Err 分岐を実装へ置換）。4) Refactor: 翻訳可能性（命名定数抽出・関数分離・wait_for_event ヘルパー抽出）を適用し make test で green 確認。
- [Risks] pjsua-native ビルドが本環境で pre-broken（P16-7: 40+ bindgen エラー）のため、統合テストと TS-001 のローカル実行不可 — 検証は CI に委譲。Asterisk 20 の PJSIP チャネル設定（pjsip.conf の endpoint / aor / auth 定義）の知識が必要で、誤設定は REGISTER が通らない。docker ネットワークで Asterisk コンテナからホストの siprs（5060）への到達性が必須（Linux ではゲートウェイ IP 172.17.0.1 / macOS・Windows では host.docker.internal）。TS-001 の pjmedia_port 登録は PJSIP の conf bridge のライフサイクル（pjsua_destroy 前の pjsua_conf_remove_port）と整合させる必要がある。
- [Caveats] RFC §62.19 の `AccountConfig::for_sip_uri(...)` は例用ヘルパー（examples/common/client.rs）であり、統合テストでは `AccountConfig` を直接構築する（全フィールド pub、Default 実装あり、#[non_exhaustive] ではない）。RFC の「sip.conf / extensions.conf」は PJSIP チャネルの意と解釈し、Asterisk 18+ では chan_sip（sip.conf）が非推奨のため pjsip.conf を使用する。RFC の docker_available() は `docker info` のみを確認するため、compose 未起動時の実サーバ不在は接続タイムアウト（30s）で明示失敗にし、ハングを防ぐ。TS-001 は register_conf_callback の STUB で、P16-7 が「pjsua-native media-port registration deferred to P16-10」と明示委譲した（bindgen 表面は P16-7 で追加済み）。
- [Open items] docker イメージのタグ固定（asterisk:20.6.0 / coturn:4.6）の最終決定。Asterisk ダイヤルプランの originate 用 extension 設計（channel originate の Application 引数）。Asterisk→ホスト到達のネットワークモード（docker compose の network_mode 設定 / ホストゲートウェイ経由）。CI（GitHub Actions）への統合 job はスコープ外。TS-001 の pjsua_conf_add_port 呼び出しで必要な pjmedia_port 初期化（pjmedia_port_info / frame callback 設定）の詳細は CI で検証。
- [Future improvements] §44 の CI 統合テスト job（ubuntu-22.04 + docker）への配線。FreeSWITCH / OpenSIPS 等の Layer4 相互接続。trickle ICE / renomination（IceConfig に存在）の実サーバ検証。SIP over TCP/TLS（5061/tcp, 5062/tls）の統合テスト拡張。pjsua_conf_remove_port による conf port の後始末のシャットダウン手順への統合。
Implementation summary (P16-10 / start):
- Changed files: src/tests/docker_asterisk_it.rs, src/tests/mod.rs, tests/sip_integration.rs, src/runtime/backend.rs (TS-001 re-point), Cargo.toml, Makefile, docker-compose.yml, docker/asterisk/pjsip.conf, docker/asterisk/extensions.conf, docker/coturn/turnserver.conf, Tickets.json, specs/P16-10.md.
- Key changes: §62.19 docker integration-test base established. DockerItPolicy encodes Q9a-c as testable data; docker_available() gates each test with [SKIPPED: docker unavailable]; 4 integration tests cover REGISTER, outbound INVITE, inbound channel originate, and coturn STUN/TURN/ICE (Q7a). make test-integration drives compose up → test → down with trap cleanup.
- TS-001 (register_conf_callback media-port FFI): NOT resolvable in this environment — pjsua-native is pre-broken (P16-7: 40+ unrelated bindgen errors) and the SipBackend/PjsuaBackend constructor must thread the audio_mixers map. Handed off to PX-3 (created via ensure-ticket.js). Marker re-pointed to PX-3.
- Test results: make test exit 0 (all green); docker_asterisk_it 9 unit tests pass; sip_integration runs only under pjsua-native in CI.
- Quality: run-quality-checks 0 issues on new code; 25 pre-existing test-code unwrap/expect in backend.rs test module untouched by P16-10 (per P16-8 precedent).

## P16-10 — implemented at 16 locations

### src/runtime/backend.rs

- Line 1078
```rust
    fn register_conf_callback(&mut self) -> Result<(), ReactorError> {
```

### src/tests/docker_asterisk_it.rs

- Line 106
```rust
impl DockerItPolicy {
```

- Line 166
```rust
fn docker_info_succeeds(status: Option<std::process::ExitStatus>) -> bool {
```

- Line 190
```rust
    fn c116_pre_parent_section_resolves_n0088() {
```

- Line 200
```rust
    fn c116_post_policy_encodes_q9_decisions() {
```

- Line 215
```rust
    fn c116_inv_consistent_with_layer3_and_docker_job() {
```

- Line 230
```rust
    fn c117_pre_layer3_and_layer4_defined() {
```

- Line 244
```rust
    fn c117_post_docker_gate_and_skip_semantics() {
```

- Line 256
```rust
    fn c117_inv_matrix_covers_both_directions() {
```

- Line 278
```rust
    fn c118_pre_docker_job_defined() {
```

- Line 288
```rust
    fn c118_post_compose_services_defined() {
```

- Line 300
```rust
    fn c118_inv_ci_requires_docker() {
```

### src/tests/mod.rs

- Line 10
```rust
pub mod docker_asterisk_it;
```

### tests/sip_integration.rs

- Line 1
```rust

```

- Line 30
```rust
fn registered_account(
```

- Line 47
```rust
fn split_sip_uri(uri: &str) -> Result<(String, String), Box<dyn std::error::Error>> {
```
