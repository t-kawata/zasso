# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P17-1: Implementation Integration Blueprint — Round 3 Scope (§62.21)

**Ticket Key**: P17-1 · **Phase**: 17

**RFC Source**: `RFC-ROOT.md`

---

## Background

- 【Goal】round 3 の進化スコープと根因（§62.21）を round2_scope_rootcause.rs と同型の型付き定数データとして src/architecture/round3_scope_rootcause.rs に記録し、Round3Section 解決表（62.21→N0090 … 62.30→N0099）と vendored PJSIP 2.17 検証をテストで固定する。
- 【Purpose】round 2（§62.10-62.20）で設計・実装された経路の残存ギャップ（H5/H8/H11/H12/H13/H14）を列挙し、各ギャップを §62.22-62.29 の設計判断へ割り当てる blueprint を提供する。round 3 実装チケット（P17-2〜P17-10）が参照する『解決表の唯一の情報源』となる。
- 【Motivation】vendored PJSIP 2.17.0 に pjsua_callback.on_rx_msg が存在しない（実ヘッダ検証済み）ため、round 2 の前提（PJSIP 2.13+ で on_rx_msg 利用可能）が成立しない。実 API に基づく配線の確定（pjsip_module フック / RustMediaPort port ops）とイベントの網羅的実装・契約確定を round 3 で行う必要がある。
- 【Constraints】vendored PJSIP 2.17.0（vendor/pjsip + vendor/prebuilt/aarch64-apple-darwin）に固定。pjsua_callback.on_rx_msg 非依存（存在しない API を使わない）。round 2 の確定済み設計判断（§62.10-62.20）を再決定しない。破壊的変更は v0.x 開発期に完了。本チケットはランタイム実装を行わない（純 blueprint データ）。

## Scope

- 【変更1-path】src/architecture/round3_scope_rootcause.rs
- 【変更1-action】modify（STUB → 実装）: 現在は `// TODO: [::STUB::] MUST implement NODE_ID=N0090` のみのスタブ。round2_scope_rootcause.rs と同型の型付き定数データへ置換する。
- 【変更1-detail】Round3Section enum（62.21-62.30 → N0090-N0099 の 10 バリアント、section()/label()/node_id() メソッド）+ ROUND3_ROOT_CAUSES（6 根因: H5/H8/H11/H12/H13/H14 を RC1..RC6 で表現、evidence は実ソースパス）+ Round3Policy（4 方針）+ ROUND3_BREAKING_CHANGES（2 件: CallResumed ペイロード化 / subscribe 系 Subscription<T> 化）+ known_graph_node_ids()（N0068/N0089/N0090..N0099）+ round3_sections()（10 セクション）。
- 【変更1-before-after】before: 空スタブ（ヘッダコメント + TODO STUB のみ）。after: 全フィールドが round 2 と同型の型付き定数データで充足され、round3_scope_rootcause::tests が green。
- 【変更1-api】architecture モジュールの公開データ追加: Round3Section / ROUND3_ROOT_CAUSES / ROUND3_POLICY / ROUND3_BREAKING_CHANGES / known_graph_node_ids / round3_sections。既存の公開 API のシグネチャ変更なし。
- 【変更1-schema】データスキーマ変更なし（Rust の型付き定数のみ）。
- 【変更1-config】設定変更なし。
- 【変更1-dep】依存クレート追加なし。
- 【非変更1-item】src/architecture/io_boundary_round3.rs（N0099 §62.30 I/O 境界参照情報）の実装。
- 【非変更1-why】P17-10 のスコープ。本チケット（P17-1）は §62.21 blueprint の解決表定義のみで、I/O 境界参照情報の実装は含まない。io_boundary_round3.rs の STUB は P17-10 が解決する。
- 【影響1-component】src/architecture モジュール（mod.rs への pub mod round3_scope_rootcause 追加）、round 3 実装チケット P17-2〜P17-10。
- 【影響1-nature】設計データ追加による API サーフェス拡張（公開 const データ）。ランタイム実装なしのため性能/セキュリティ/データ形式への影響なし。
- 【影響1-response】P17-2〜P17-10 が本 blueprint の ROUND3_ROOT_CAUSES / Round3Section を参照して実装する。P17-10 が io_boundary_round3.rs を mod.rs に追加する。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0090`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0090` | architecture | rust | 62.21 ラウンド 3 進化スコープと根因（H5 / H8 / H11 / H12 / H13 / H14 の残存ギャップ） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.21 ラウンド 3 進化スコープと根因（H5 / H8 / H11 / H12 / H13 / H14 の残存ギャップ） (N0090) | → | ☆ 62.20 I/O 境界参照情報 (N0089) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0090` | `src/architecture/round3_scope_rootcause.rs` |

## Investigation

- 証拠1（vendored PJSIP 2.17 確定）: vendor/pjsip/pjlib/include/pj/config.h:1671-1677 に '#define PJ_VERSION_NUM_MAJOR 2' / '#define PJ_VERSION_NUM_MINOR 17' / '#define PJ_VERSION_NUM_REV 0' を確認。PJSIP 2.17.0 であることを実ヘッダで確定。
- 証拠2（on_rx_msg 不在）: vendor/pjsip/pjsip/include/pjsua.h を grep 検索した結果 on_rx_msg は存在しない（exit 1）。pjsua_callback の全 on_* フィールド（pjsua.h:1300-2100）に on_rx_msg は無く、raw SIP 取得はモジュール層/トランスポート層フックによる（§62.22 N0091 の判断根拠）。
- 証拠3（RFC グラフ）: RFC-ROOT-GRAPH.json から round 3 ノード N0090-N0099 のタイトルを確認。N0090（62.21）→ N0089（62.20）references。N0091→N0080/N0038、N0092→N0021/N0038、N0093→N0071、N0094→N0083/N0045、N0095→N0026、N0096→N0084/N0029、N0097→N0085/N0049、N0098→N0031/N0074、N0099→N0089 の references エッジを確認。N0079→N0068 part_of（round 2 親関係）も確認。
- 証拠4（RFC 見出し）: RFC-ROOT.md:4549-4947 に §62.21-62.30 の見出しを確認。§62.21 は『残る RESIDUE の根因は 6 つに集約される』と記し、8 項目のギャップを H5/H8/H11/H12/H13/H14 の 6 根因へ集約する。
- 証拠5（ソース現状）: ①src/ffi/callback.rs:145 enqueue_raw_sip_bytes 定義、非テスト呼び出しゼロ（テスト呼び出し 702/714/715/727 のみ）②src/ffi/callback.rs:182-197 register_callbacks が 8 コールバックのみ Some() 登録（on_transport_state/on_call_tsx_state/on_call_replaced/on_nat_detect 未登録）③src/runtime/backend.rs:379 TestBackend::set_registration が内部状態更新のみで NativeEvent::RegistrationStateChanged を生成しない ④src/runtime/reactor.rs:1046 process_native_event が publish のみで CallEntry.state を変更しない ⑤src/api/event_model_payload_bus.rs:335 CallResumed が unit variant で構築箇所ゼロ ⑥src/api/m20_dtmfsent_twophase.rs:64 DEFAULT_DTMF_SENT_TIMEOUT_MS == 500 ⑦vendor/pjsip に pjsua_conf_set_callback が存在しない ⑧src/runtime/backend.rs:773 RustMediaPort::new(mixer, call_id) をコールごとに conf port 登録。
- 証拠6（round 2 参照実装）: src/architecture/round2_scope_rootcause.rs（500 行）が Round2Section enum（section/label/node_id）+ ROUND2_ROOT_CAUSES[5] + Round2Policy + ROUND2_BREAKING_CHANGES[3] + known_graph_node_ids + round2_sections() の構造。round 3 はこの型付き定数データパターンを 62.21-62.30 / N0090-N0099 に対して踏襲する。
- 証拠7（配線）: src/architecture/mod.rs に round3_scope_rootcause は未宣言。P17-1 実装時に pub mod round3_scope_rootcause; を追加する必要がある。io_boundary_round3.rs（N0099）は P17-10 のスコープ。

## Acceptance Criteria

- 【Happy path】Round3Section が 62.21→N0090 … 62.30→N0099 を解決し、vendored PJSIP 2.17（config.h の PJ_VERSION_NUM_MINOR == 17 と pjsua.h の on_rx_msg 不在）を検証する。make test で round3_scope_rootcause::tests が全 green。
- 【Error case】ROUND3_ROOT_CAUSES が 6 件でない / id 重複 / 必須フィールド欠落がある場合、テスト（round3_root_causes_are_defined / round3_root_causes_reject_wrong_count）が red になり blueprint の不整合を検出する。
- 【Edge case】解決表の範囲境界（62.21 / 62.30 / N0090 / N0099）と、round 2（62.11-62.20）/ round 1（62.1-62.8）との非重複を検証する。PJSIP 2.17 前提の固定（PJ_VERSION_NUM_MINOR == 17 以外なら red）を確認する。

## Invariants

- 【Normal establishment】Round3Section の解決表が RFC 見出しと完全一致する — 各バリアントの section()/label()/node_id() が RFC-ROOT.md:4549-4947 の見出しと RFC-ROOT-GRAPH.json のノード ID（N0090..N0099）に一致する。round3_sections() は 10 セクション。
- 【Invariant on error】解決表に範囲外参照（62.21..62.30 外の section、N0090..N0099 外の node_id）が含まれる場合、テスト（round3_section_node_ids_are_known / round3_sections_map_to_graph_nodes）が red になり make 完了不能。エラー時も blueprint データは一貫性を保ち、部分的な STUB 置換では green にならない。
- 【Internal state invariant】ROUND3_ROOT_CAUSES は 6 根因（RC1..RC6）で id 一意、必須フィールド（title/evidence/rfc_ref/resolving_sections）全て非空。ROUND3_BREAKING_CHANGES は 2 件。各根因の resolving_sections は round 2（62.11-62.20）/round 1（62.1-62.8）と重複しない。
- 【Boundary invariant】section() は 62.21..62.30、node_id() は N0090..N0099 の範囲内。resolving_sections は 62.22..62.29 の範囲内。vendored PJSIP は PJ_VERSION_NUM_MINOR == 17 固定（config.h 検証）。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C121 — N0090→N0089

- **Precondition**: Round 2 evolution settled (§62.10–62.20) — round2_scope_rootcause::ROUND2_ROOT_CAUSES が 5 根因、round2_sections() が 10 セクション（62.11..62.20）で確定済みである。テスト: round2_evolution_is_settled が ROUND2_ROOT_CAUSES.len()==5 かつ round2_sections().len()==10 を検証する。
- **Postcondition**: Round 3 scope derives the remaining RESIDUE gaps from round 2 outcomes — ROUND3_ROOT_CAUSES の 6 根因（H5 / H8 / H11 / H12 / H13 / H14）が rfc_ref で §62.10–62.20 を参照し、各根因が §62.22–62.29 の resolving_sections へ解決される。テスト: round3_scope_derives_from_round2 が各根因の rfc_ref に round 2 参照を含むことを検証する。
- **Invariant**: Round 3 never re-decides a settled round 2 design — 各根因の resolving_sections が 62.22–62.29 に限定され、round 2（62.11–62.20）および round 1（62.1–62.8）と重複しない。テスト: round3_scope_does_not_overlap_round2 が範囲外・重複を検出して red にする。

### C122 — N0090→N0089

- **Precondition**: vendored PJSIP 2.17 headers present — vendor/pjsip/pjlib/include/pj/config.h が PJ_VERSION_NUM_MINOR 17 を定義し、pjsua_callback に on_rx_msg が存在しない。テスト: vendored_pjsip_is_2_17 が config.h の PJ_VERSION_NUM_MINOR==17 と pjsua.h の on_rx_msg 不在を検証する。
- **Postcondition**: Round3Section resolution table maps sections 62.21..62.30 to graph node IDs N0090..N0099 — round3_sections() の全 10 バリアントが正確な section() / label() / node_id() を返す。テスト: round3_sections_map_to_graph_nodes が期待値文字列と完全一致することを検証する。
- **Invariant**: Round3Section resolution table exactly matches RFC headings — section() が 62.21..62.30 の範囲内、node_id() が N0090..N0099 の範囲内、label() が RFC-ROOT.md の見出しと完全一致する。テスト: round3_sections_map_to_graph_nodes が境界値と範囲を検証する。

## Boy Scout Rule

- round3_scope_rootcause.rs は round2_scope_rootcause.rs と同型の翻訳可能な構造にする: enum Round3Section → section()/label()/node_id() の各メソッドは『62.21 は ラウンド3 進化スコープ』のようにそのまま自然言語へ翻訳できる命名。
- マジックナンバー禁止: セクション範囲（62.21..62.30）、ノード範囲（N0090..N0099）はリテラル直書きせず、テストの期待値配列と enum バリアントの対応で表現（round2 の expected_nodes 配列パターンを踏襲）。
- 証拠ファイルパスはハードコードせず、テスト内の名前付き定数（CONFIG_H_PATH / PJSUA_H_PATH 等）として抽出し、round3_evidence_matches_actual_source テストから参照する。
- src/architecture/mod.rs に pub mod round3_scope_rootcause; を追加する際は [::TICKET::] P17-1 注釈を付与し、変更の出典（show-ticket-context.js コマンド）を残す。
- round2_scope_rootcause.rs の既存テストに追加変更はしない（スコープ外）。ただし本チケットのテスト名は round2 の命名規則（round2_*）に合わせ round3_* で統一する。

## Test Plan

### Unit Tests

- UT: [Normal] round3_scope_rootcause::Round3Section が各セクション（62.21..62.30）をノード ID（N0090..N0099）へ解決する — round3_sections() が 10 セクションを返し、各バリアントの section() / label() / node_id() が非空で、section() が 62.21..62.30 の範囲内、node_id() が N0090..N0099 の範囲内であることを検証する。RFC-ROOT.md:4549-4947 の見出しと RFC-ROOT-GRAPH.json のノードタイトルが一致することを期待値文字列の比較で固定する。
- UT: [Normal] vendored PJSIP の config.h に PJ_VERSION_NUM_MINOR == 17 が含まれることを確認する（PJSIP 2.17 前提）— vendor/pjsip/pjlib/include/pj/config.h:1674 の '#define PJ_VERSION_NUM_MINOR    17' と :1671 の '#define PJ_VERSION_NUM_MAJOR    2' を std::fs::read_to_string で読み取り検証する。§62.21 の前提をテストで固定する。
- UT: [Normal] pjsua_callback に on_rx_msg が存在しないことを確認する — vendor/pjsip/pjsip/include/pjsua.h を読み取り、'on_rx_msg' 文字列が含まれないことを検証し、『raw SIP はモジュール層 / トランスポート層フックで取得』という §62.21 の設計判断を固定する。
- UT: [Normal] Contract C121 前置条件『Round 2 evolution settled (§62.10–62.20)』→ round2_scope_rootcause::ROUND2_ROOT_CAUSES.len() == 5 かつ round2_scope_rootcause::round2_sections().len() == 10 を検証し、round 2 が 5 根因・10 セクションで確定済みであることを確認する（round2_evolution_is_settled）。
- UT: [Normal] Contract C121 後置条件『Round 3 scope derives the remaining RESIDUE gaps from round 2 outcomes』→ ROUND3_ROOT_CAUSES の各根因の rfc_ref が '§62.10'〜'§62.20'（または 62.10..62.20）の round 2 成果を参照し、6 根因（H5 / H8 / H11 / H12 / H13 / H14）が round 2 成果から派生していることを検証する（round3_scope_derives_from_round2）。
- UT: [Boundary] ROUND3_ROOT_CAUSES.len() == 6 で根因を過不足なく網羅する — 各 id（RC1..RC6）が一意で、必須フィールド（title / evidence / rfc_ref / resolving_sections）が全て非空であることを検証する（round3_root_causes_are_defined）。
- UT: [Boundary] ROUND3_POLICY の 4 要素（real_api_wiring / event_completeness / no_missing_api_dependency / extension_point_wiring）が全て非空であることを検証する（round3_scope_defines_policy）。
- UT: [Boundary] ROUND3_BREAKING_CHANGES.len() == 2 で v0.x 破壊的変更（CallResumed ペイロード化 / subscribe 系 API の Subscription<T> 化）を一度ずつ含むことを検証する（round3_breaking_changes_defined）。
- UT: [Error] disguised-green 防止 — ROUND3_ROOT_CAUSES が 5 件以下・7 件以上・id 重複・必須フィールド欠落のいずれかなら red になる。#[should_panic(expected = "must cover exactly the 6 RFC §62.21 root causes")] で要素数逸脱を検出する（round3_root_causes_reject_wrong_count）。
- UT: [Error] section 参照 typo 検出 — 各根因の resolving_sections が既知の round 3 section 集合（62.22–62.29）外なら red になる（round3_root_causes_reject_out_of_range_sections）。
- UT: [Error] node_id 参照 typo 検出 — 各 Round3Section の node_id が既知 graph node 集合（N0068 / N0089 / N0090..N0099）外なら red になる（round3_section_node_ids_are_known）。
- UT: [Invariant] Contract C121 不変条件『Round 3 never re-decides a settled round 2 design』— 各根因の resolving_sections が 62.22–62.29 の範囲内にあり、round 2（62.11–62.20）および round 1（62.1–62.8）と重複しないことを検証する（round3_scope_does_not_overlap_round2）。
- UT: [Invariant] ソース整合 — 各根因の evidence が実ソースと一致することを検証する: ①src/ffi/callback.rs:145 の enqueue_raw_sip_bytes が定義され、プロダクション（非テスト）呼び出しが src/ffi/callback.rs 内に存在しない（テスト呼び出し 702/714/715/727 のみ）②src/ffi/callback.rs:182-197 の register_callbacks が 8 コールバックのみを Some() 登録し、on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect が未登録 ③src/runtime/backend.rs:379 の TestBackend::set_registration が NativeEvent::RegistrationStateChanged を生成しない ④src/api/event_model_payload_bus.rs:335 の CallResumed が enum 定義のみで構築箇所ゼロ ⑤src/api/m20_dtmfsent_twophase.rs:64 の DEFAULT_DTMF_SENT_TIMEOUT_MS == 500 ⑥vendor/pjsip に pjsua_conf_set_callback が存在しない（round3_evidence_matches_actual_source）。

### Integration Tests

- IT: [Integration point] src/architecture/mod.rs に pub mod round3_scope_rootcause; を追加し、Round3Section / ROUND3_ROOT_CAUSES / ROUND3_POLICY / ROUND3_BREAKING_CHANGES / known_graph_node_ids / round3_sections をクレートの architecture モジュールから公開する。親モジュール src/architecture/impl_integration_design.rs（ラウンド1 blueprint）および src/architecture/round2_scope_rootcause.rs（ラウンド2 blueprint）との整合。
- IT: [Verification] 各根因の evidence が実ソースに存在することを読み取り検証する: ①vendor/pjsip/pjlib/include/pj/config.h の PJ_VERSION_NUM_MINOR 17 ②vendor/pjsip/pjsip/include/pjsua.h に on_rx_msg 不在 ③src/ffi/callback.rs:145 の enqueue_raw_sip_bytes 定義と非テスト呼び出しゼロ ④src/ffi/callback.rs:182-197 の 8 コールバックのみ ⑤src/runtime/backend.rs:379 の TestBackend::set_registration ⑥src/api/event_model_payload_bus.rs:335 の CallResumed unit variant ⑦src/api/m20_dtmfsent_twophase.rs:64 の DEFAULT_DTMF_SENT_TIMEOUT_MS == 500 ⑧vendor/pjsip に pjsua_conf_set_callback 不在。
- IT: [Prerequisites] 既存ソースが現状維持であること（本チケットは破壊的変更を実行しない）。architecture モジュールの配線（src/lib.rs の pub mod architecture; と src/architecture/mod.rs の pub mod round3_scope_rootcause;）が完了していること。round 2 実装（P16-1〜P16-11）のテストが green であること。
- IT: [Related tickets] P16-1（62.10 round 2 scope/rootcause）が本 blueprint の親。P17-2（62.22 raw SIP）〜P17-9（62.29 文書化）、P17-10（62.30 I/O 境界参照）が本 blueprint を参照して実装する。P17-2 が pjsip_module フックを実装した際、本チケットのソース整合テスト（enqueue_raw_sip_bytes の非テスト呼び出しゼロ）が red になり blueprint 更新を促す。

### Exceptions

- 実装コードなし（設計基準の符号化のみ） — 本チケットはランタイム実装を行わない純設計データのモジュールであり、実行時例外経路（ネットワーク・FFI・タイマー等）が存在しないため実行時例外系はテスト不能（not testable）。これは設計欠陥（design defect）ではなく、blueprint を型付き定数データ（Round3Section / ROUND3_ROOT_CAUSES / ROUND3_POLICY / ROUND3_BREAKING_CHANGES）としてテスト可能な形で表現しており、設計矛盾はテストの red として検出される。
- 実 SIP サーバー（Asterisk / coturn / STUN / TURN）とのエンドツーエンド相互接続検証（実 REGISTER / INVITE / raw SIP キャプチャ / メディア tap） — 本チケットは §62.21 blueprint の定義のみであり、実 FFI 配線（pjsip_module フック、P1/P2 コールバック登録、TestBackend 発火、CallEntry.state 遷移、RustMediaPort tap）は P17-2〜P17-9 の各実装チケットで実装されるため、本チケット時点では実 SIP/メディア動作は実行不能でテスト不能（cannot test）。代替検証として、設計データの assertion と実ヘッダの静的検査（config.h の PJ_VERSION_NUM_MINOR 17、pjsua.h の on_rx_msg 不在、pjsua_conf_set_callback 不在）で代替する。これは設計欠陥（design defect）ではなく、外部依存（実サーバー）に起因するテスト不能項目であり、blueprint をテスト可能な型付きデータとして表現している。
- vendored PJSIP プリビルドバイナリ（vendor/prebuilt/aarch64-apple-darwin/libpj*.a）がヘッダ（config.h）の 2.17 と実際に一致することの実行時検証 — ヘッダのマクロはテキスト検証可能だが、プリビルド静的ライブラリが同一バージョンでコンパイルされているかはリンク後の実行時出力（pj_get_version 等）との比較が必要となり、ユニットテストではテスト不能（not testable）。これは設計欠陥（design defect）ではなく、外部依存（プリビルドバイナリのリンク時整合）に起因する項目であり、実 SIP 相互接続検証（P17-3 以降の docker ゲート付き統合テスト）で検証されるべき検証項目である。代替検証として、ヘッダ検証（config.h の PJ_VERSION_NUM_MINOR == 17）+ 将来の統合テストでバイナリ整合を確認する。

### Plan Test Code (concrete code)

- UT: [Normal] Contract C121 前置条件『Round 2 evolution settled (§62.10–62.20)』→ テスト入力コード
  ```rust
  // RED 確認: round2_scope_rootcause が存在しない/未確定ならコンパイル不能で red。
  // GREEN 実装: round2_scope_rootcause は P16-1 で実装済み。
  #[test]
  fn round2_evolution_is_settled() {
      assert_eq!(
          round2_scope_rootcause::ROUND2_ROOT_CAUSES.len(),
          5,
          "round 2 must be settled with exactly 5 root causes (§62.10)"
      );
      assert_eq!(
          round2_scope_rootcause::round2_sections().len(),
          10,
          "round 2 must be settled with sections 62.11..62.20"
      );
  }
  ```
- UT: [Normal] Contract C121 後置条件『Round 3 scope derives the remaining RESIDUE gaps from round 2 outcomes』→ アサーションコード
  ```rust
  #[test]
  fn round3_scope_derives_from_round2() {
      for cause in &ROUND3_ROOT_CAUSES {
          let references_round2 = cause.rfc_ref.contains("62.1")
              || cause.rfc_ref.contains("62.20")
              || cause.rfc_ref.contains("62.2");
          assert!(
              references_round2,
              "root cause {} rfc_ref '{}' must reference round 2 outcomes (§62.10-62.20)",
              cause.id, cause.rfc_ref
          );
          assert!(
              !cause.resolving_sections.is_empty(),
              "root cause {} must resolve via at least one §62.22-62.29 section",
              cause.id
          );
      }
  }
  ```
- UT: [Invariant] Contract C121 不変条件『Round 3 never re-decides a settled round 2 design』→ 不変条件述語
  ```rust
  #[test]
  fn round3_scope_does_not_overlap_round2() {
      let round1_sections: [&str; 8] = [
          "62.1", "62.2", "62.3", "62.4", "62.5", "62.6", "62.7", "62.8",
      ];
      let round2_section_numbers: Vec<&str> =
          round2_scope_rootcause::round2_sections().iter().map(|s| s.section()).collect();
      for cause in &ROUND3_ROOT_CAUSES {
          for section in cause.resolving_sections {
              assert!(
                  !round1_sections.contains(&section.section()),
                  "cause {} resolving section {} must not overlap round 1",
                  cause.id, section.section()
              );
              assert!(
                  !round2_section_numbers.contains(&section.section()),
                  "cause {} resolving section {} must not re-decide round 2 (§62.11-62.20)",
                  cause.id, section.section()
              );
              assert!(
                  section.section() >= "62.22" && section.section() <= "62.29",
                  "cause {} resolving section {} must be within 62.22..62.29",
                  cause.id, section.section()
              );
          }
      }
  }
  ```
- UT: [Normal] Contract C122 前置条件『vendored PJSIP 2.17 headers present』→ 入力スキーマ検証コード
  ```rust
  // RED 確認: ファイル不存在 or バージョン不一致で read_to_string/assert が失敗し red。
  #[test]
  fn vendored_pjsip_is_2_17() -> Result<(), std::io::Error> {
      let config_h = std::fs::read_to_string("vendor/pjsip/pjlib/include/pj/config.h")?;
      assert!(
          config_h.contains("#define PJ_VERSION_NUM_MAJOR    2"),
          "vendored PJSIP major version must be 2"
      );
      assert!(
          config_h.contains("#define PJ_VERSION_NUM_MINOR    17"),
          "vendored PJSIP minor version must be 17 (§62.21 premise)"
      );
      let pjsua_h = std::fs::read_to_string("vendor/pjsip/pjsip/include/pjsua.h")?;
      assert!(
          !pjsua_h.contains("on_rx_msg"),
          "pjsua_callback must not contain on_rx_msg in any PJSIP 2.x"
      );
      Ok(())
  }
  ```
- UT: [Normal] Contract C122 後置条件『Round3Section resolution table maps sections 62.21..62.30 to graph node IDs N0090..N0099』→ 出力アサーションコード
  ```rust
  #[test]
  fn round3_sections_map_to_graph_nodes() {
      let sections = round3_sections();
      assert_eq!(sections.len(), 10, "round 3 spans sections 62.21..62.30");
      let expected_nodes = [
          "N0090", "N0091", "N0092", "N0093", "N0094", "N0095", "N0096", "N0097", "N0098", "N0099",
      ];
      for (i, section) in sections.iter().enumerate() {
          assert_eq!(
              section.node_id(),
              expected_nodes[i],
              "section {} maps to {}",
              section.section(),
              expected_nodes[i]
          );
          assert!(!section.label().is_empty(), "section {} label must be non-empty", section.section());
      }
  }
  ```
- UT: [Invariant] Contract C122 不変条件『Round3Section resolution table exactly matches RFC headings』→ 境界値・完全一致述語
  ```rust
  #[test]
  fn round3_section_labels_match_rfc_headings() {
      let expected_labels = [
          "62.21 ラウンド 3 進化スコープと根因（H5 / H8 / H11 / H12 / H13 / H14 の残存ギャップ）",
          "62.22 raw SIP 生産経路: pjsip_module フックによる配線（Q1 / Q1a）",
          "62.23 P1/P2 FFI コールバック登録の完了（Q2）",
          "62.24 TestBackend 登録イベント発火と account_register example 完走（Q3）",
          "62.25 CallEntry.state のネイティブ遷移反映（Q4）",
          "62.26 CallResumed の実装機構（Q5 / Q5a）",
          "62.27 DtmfSent 送出完了契約の確定（Q6）",
          "62.28 tap 駆動の生産経路: RustMediaPort port ops（Q7）",
          "62.29 文書化決定: マイク source / Subscription unsubscribe（Q8 / Q8a）",
          "62.30 I/O 境界参照情報（graphify / boundify 用）— round 3",
      ];
      for (i, section) in round3_sections().iter().enumerate() {
          assert_eq!(section.label(), expected_labels[i], "label must exactly match RFC heading");
          assert!(
              section.section() >= "62.21" && section.section() <= "62.30",
              "section {} must be within 62.21..62.30",
              section.section()
          );
          assert!(
              section.node_id() >= "N0090" && section.node_id() <= "N0099",
              "node_id {} must be within N0090..N0099",
              section.node_id()
          );
      }
  }
  ```
- UT: [Boundary] ROUND3_ROOT_CAUSES.len() == 6 で根因を過不足なく網羅する → 境界アサーション
  ```rust
  #[test]
  fn round3_root_causes_are_defined() {
      let root_causes: &[Round3RootCause] = &ROUND3_ROOT_CAUSES;
      assert_eq!(root_causes.len(), 6, "must cover exactly the 6 RFC §62.21 root causes");
      let mut ids: Vec<&str> = root_causes.iter().map(|r| r.id).collect();
      ids.sort_unstable();
      ids.dedup();
      assert_eq!(ids.len(), 6, "root cause ids RC1..RC6 must be unique");
      for cause in root_causes {
          assert!(!cause.title.is_empty());
          assert!(!cause.evidence.is_empty());
          assert!(!cause.rfc_ref.is_empty());
          assert!(!cause.resolving_sections.is_empty());
      }
  }
  ```
- UT: [Error] disguised-green 防止 → should_panic による要素数逸脱検出
  ```rust
  #[test]
  #[should_panic(expected = "must cover exactly the 6 RFC §62.21 root causes")]
  fn round3_root_causes_reject_wrong_count() {
      let mut causes = ROUND3_ROOT_CAUSES.to_vec();
      causes.truncate(5);
      assert_eq!(causes.len(), 6, "must cover exactly the 6 RFC §62.21 root causes");
  }
  ```
- UT: [Boundary] ROUND3_POLICY 4 要素 + ROUND3_BREAKING_CHANGES 2 件 → 方針・破壊的変更の検証
  ```rust
  #[test]
  fn round3_scope_defines_policy_and_breaking_changes() {
      let policy = ROUND3_POLICY;
      assert!(!policy.real_api_wiring.is_empty());
      assert!(!policy.event_completeness.is_empty());
      assert!(!policy.no_missing_api_dependency.is_empty());
      assert!(!policy.extension_point_wiring.is_empty());
      let breaking: &[Round3BreakingChange] = &ROUND3_BREAKING_CHANGES;
      assert_eq!(breaking.len(), 2, "RFC §62.21 lists 2 breaking changes");
      let labels: Vec<&str> = breaking.iter().map(|b| b.label()).collect();
      assert!(
          labels.iter().any(|l| l.contains("CallResumed")),
          "must include CallResumed payload-ization"
      );
      assert!(
          labels.iter().any(|l| l.contains("Subscription")),
          "must include subscribe API Subscription<T> generalization"
      );
  }
  ```
- UT: [Invariant] ソース整合 — evidence が実ソースと一致する → 実ファイル読み取り検証
  ```rust
  #[test]
  fn round3_evidence_matches_actual_source() -> Result<(), std::io::Error> {
      let callback = std::fs::read_to_string("src/ffi/callback.rs")?;
      assert!(callback.contains("pub fn enqueue_raw_sip_bytes"), "RC1: enqueue_raw_sip_bytes defined");
      assert!(callback.contains("config.cb.on_incoming_call"), "RC2: register_callbacks wired");
      let backend = std::fs::read_to_string("src/runtime/backend.rs")?;
      assert!(backend.contains("fn set_registration"), "RC3: TestBackend::set_registration exists");
      let payload_bus = std::fs::read_to_string("src/api/event_model_payload_bus.rs")?;
      assert!(payload_bus.contains("CallResumed,"), "RC4: CallResumed unit variant exists");
      let dtmf = std::fs::read_to_string("src/api/m20_dtmfsent_twophase.rs")?;
      assert!(dtmf.contains("DEFAULT_DTMF_SENT_TIMEOUT_MS: u64 = 500"), "RC5: 500ms timeout");
      let pjsua_h = std::fs::read_to_string("vendor/pjsip/pjsip/include/pjsua.h")?;
      assert!(!pjsua_h.contains("pjsua_conf_set_callback"), "RC6: pjsua_conf_set_callback absent");
      Ok(())
  }
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| src/architecture/round3_scope_rootcause.rs は STUB マーカーのみ | Round3Section enum（62.21-62.30→N0090-N0099）+ ROUND3_ROOT_CAUSES[6] + Round3Policy + ROUND3_BREAKING_CHANGES[2] + known_graph_node_ids + round3_sections + テスト 13 件 | N0090 §62.21 blueprint を round2_scope_rootcause.rs と同型の型付き定数データとして実装 |
| src/architecture/mod.rs に round3_scope_rootcause 未宣言 | pub mod round3_scope_rootcause; を P17-1 注釈付きで追加 | architecture モジュールへの配線 |
| raw_sip_module.rs / io_boundary_round3.rs の STUB がキーなし | P17-2 / P17-10 のキー付き STUB に修正（make フェーズのゲート修正） | no-excuse ゲートの stale-key STUB 修正 |

## Notes in Prior Implementation Rounds

- 【Implementation steps】①src/architecture/round3_scope_rootcause.rs に Round3Section enum + ROUND3_ROOT_CAUSES[6] + Round3Policy + ROUND3_BREAKING_CHANGES[2] + known_graph_node_ids + round3_sections() を round2 と同型で実装 ②src/architecture/mod.rs に pub mod round3_scope_rootcause; を追加 ③PJSIP 2.17 検証テスト（config.h の PJ_VERSION_NUM_MINOR == 17、pjsua.h の on_rx_msg 不在）④ソース整合テスト（evidence と実ソースの一致）⑤make test で全 green 確認 ⑥make review で品質チェック。
- 【Risks】RFC §62.21 の『6 つに集約』と 8 項目の対応が曖昧 — 6 根因（H-code 単位）に集約する解釈を本 blueprint で固定。evidence が実装チケット（P17-2 以降）の実装で変化した場合、ソース整合テスト（round3_evidence_matches_actual_source）が red になり blueprint 更新が必要（設計意図）。
- 【Caveats】round2_scope_rootcause.rs と同型にするが、round 2 の P16-1 テスト（round2_evidence_matches_actual_source）は round 2 実装が進んだ時点で意図的に red になる設計（blueprint 更新トリガー）。本チケットも同様の設計。io_boundary_round3.rs（N0099）の STUB は本チケットのスコープ外。
- 【Open items】io_boundary_round3.rs（N0099）の mod.rs 宣言は P17-10 に委譲。round 3 の各実装チケットが解決した時点で、本 blueprint の ROUND3_ROOT_CAUSES の resolving_sections と evidence を更新し、解決済みギャップを削減する。
- 【Future improvements】P17-2〜P17-9 の実装完了後、本 blueprint は『解決済み』マークへ更新し、残存ギャップを削減する。P17-10 で io_boundary_round3.rs の I/O 境界参照情報を追加した際、known_graph_node_ids に N0099 の追加（既に含まれる）を確認する。
Implementation summary:
- Changed files: src/architecture/round3_scope_rootcause.rs, src/architecture/mod.rs, src/ffi/raw_sip_module.rs, src/architecture/io_boundary_round3.rs
- Key changes: Round3Section resolution table (62.21→N0090..62.30→N0099), 6 root causes (H5/H8/H11/H12/H13/H14), PJSIP 2.17 verification (config.h PJ_VERSION_NUM_MINOR 17, pjsua.h no on_rx_msg)
- Test results: make test — 1272 lib + integration + doctests all green, 0 failed; cargo check --all-targets no warnings; run-quality-checks 0 issues
Review report:
- Static quality check: passed (run-quality-checks 0 issues)
- Translatability: no issues (section/label/node_id verb-phrase accessors, no debug output)
- Dependencies: no new crates added; round3_scope_rootcause references existing round2_scope_rootcause
- Crime scan: 0 crimes; stubs: 2 correctly deferred (P17-2 raw_sip_module, P17-10 io_boundary_round3)
- Annotation check: 4/4 changed source files annotated with [::TICKET::] P17-1
- Contracts C121/C122: all pre/post/invariant covered by 8 @verifies annotations across 13 tests
- verify-red-coverage: 2/2 contracts covered
- Issues found and fixes applied: none (review found no defects)

## P17-1 — implemented at 18 locations

### src/architecture/io_boundary_round3.rs

- Line 22
```rust

```

### src/architecture/mod.rs

- Line 37
```rust
pub mod round3_scope_rootcause;
```

### src/architecture/round3_scope_rootcause.rs

- Line 45
```rust
impl Round3Section {
```

- Line 206
```rust
impl Round3BreakingChange {
```

- Line 258
```rust
    fn round2_evolution_is_settled() {
```

- Line 274
```rust
    fn round3_scope_derives_from_round2() {
```

- Line 295
```rust
    fn round3_scope_does_not_overlap_round2() {
```

- Line 327
```rust
    fn vendored_pjsip_is_2_17() -> Result<(), std::io::Error> {
```

- Line 348
```rust
    fn round3_sections_map_to_graph_nodes() {
```

- Line 374
```rust
    fn round3_section_labels_match_rfc_headings() {
```

- Line 409
```rust
    fn round3_root_causes_reject_out_of_range_sections() {
```

- Line 427
```rust
    fn round3_section_node_ids_are_known() {
```

- Line 441
```rust
    fn known_nodes_cover_round3_graph() {
```

- Line 453
```rust
    fn round3_root_causes_are_defined() {
```

- Line 475
```rust
    fn round3_root_causes_reject_wrong_count() {
```

- Line 487
```rust
    fn round3_scope_defines_policy_and_breaking_changes() {
```

- Line 508
```rust
    fn round3_evidence_matches_actual_source() -> Result<(), std::io::Error> {
```

### src/ffi/raw_sip_module.rs

- Line 22
```rust

```
