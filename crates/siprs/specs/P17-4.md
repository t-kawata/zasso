# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P17-4: TestBackend registration event firing (§62.24)

**Ticket Key**: P17-4 · **Phase**: 17

**RFC Source**: `RFC-ROOT.md`

---

## Background

TestBackend は set_registration 時にネイティブ登録イベントを発火しないため account_register example がタイムアウトする（H5）。実 FFI 経路と同じイベント系列を TestBackend 上で成立させる（Q3）。

- 【Goal】TestBackend::set_registration が NativeEvent::RegistrationStateChanged を発火し、SipBackend::take_native_events（既定空）で回収できるようにし、account_register example が TestBackend 上で 30 秒タイムアウトせず RegistrationStateChanged(Registered) を受信して完走する（§62.24 / N0093）。
- 【Purpose】実 FFI 経路（on_reg_state2 → NATIVE_EVENT_QUEUE → drain task §62.13 → DispatchCommand::NativeEvent → process_native_event）と同じイベント系列を TestBackend 上でも成立させ、test-util ビルド（make test）で example・統合テストが登録結果イベントを受信できるようにする。TestBackend の native_events バッファが FFI キューの代役を担い、take_native_events が reactor との回収境界になる。
- 【Motivation】H5（round 3 根因 RC3、src/architecture/round3_scope_rootcause.rs:141-147）: TestBackend::set_registration（backend.rs:379）は set_registration_calls 記録 + Registering/Unregistering 遷移のみで NativeEvent を生成しないため、account_register example の await_registration（examples/account_register.rs）が RegistrationStateChanged を 30 秒待ち続けてタイムアウトする。実 FFI 経路と同じ event 系列を TestBackend で成立させることが round 3 の Q3 要求。
- 【Constraints】実 FFI 経路の take_native_events は既定空のまま（RFC §62.24『実 FFI 経路ではキュー drain §62.13 が担うため既定は空』）。SipBackend trait に破壊的変更を加えない（デフォルト実装付きメソッド追加のみ）。§62.2 登録状態機械・§17.1 遷移表（registr_state_machine.rs:101-111）・§62.12 統合 RegistrationStateChanged イベント・§62.4 production 配線（registr_wiring.rs）を再設計しない。example の公開 CLI インターフェース（--username/--domain/--password）は不変。make test は --features test-util で実行される（Makefile:19-20）。

## Scope

- 【変更1-path】src/runtime/backend.rs（SipBackend trait L57-180 に take_native_events 追加、TestBackend struct L203-269 に native_events フィールド追加、TestBackend::set_registration L379-399 改修、take_native_events 実装追加）/ src/runtime/reactor.rs（SetRegistration アーム L741-794 に drain + process_native_event 追加）/ examples/account_register.rs（subscribe_account を register() より前へ移動）/ tests/verify_spec_p9_1.rs（Layer 5 テストの期待値更新 + RegistrationStateChanged 受信テスト追加）
- 【変更1-action】modify（SipBackend trait にデフォルト実装付きメソッド追加）/ modify（TestBackend struct フィールド追加）/ modify（TestBackend::set_registration にイベント発火 + outcome 遷移追加）/ add（TestBackend::take_native_events 実装）/ modify（reactor SetRegistration アーム）/ modify（example の購読順序）/ modify（既存テスト 4 件の期待値更新）
- 【変更1-detail】SipBackend に fn take_native_events(&mut self) -> Vec<NativeEvent> { Vec::new() } を追加（既定空 — §62.13 キュー drain が実経路を担う）。TestBackend に pub native_events: Vec<NativeEvent> を追加。set_registration は既存の記録/失敗注入を維持しつつ、(a) accounts[id].registration と registrations[id] を outcome（enabled → Registered / disabled → Idle）へ遷移、(b) self.native_events.push(NativeEvent::RegistrationStateChanged { acc_id: native_acc_id as u32 }) を実行。take_native_events は std::mem::take(&mut self.native_events) を返す。reactor SetRegistration アームは backend.set_registration → apply_registration_command_state（既存）の後に、CallStateTables を構築して backend.take_native_events() を for で回し process_native_event(&mut *backend, &event_bus, event, &mut call_state, &mut client_state.accounts) を実行する（NativeEvent アーム L845-860 と同型）。
- 【変更1-before-after】before = TestBackend::set_registration は set_registration_calls 記録 + Registering/Unregistering 遷移のみで NativeEvent を生成せず、account_register example が 30 秒タイムアウト（H5）。after = set_registration が RegistrationStateChanged を発火し accounts を Registered/Idle へ遷移、reactor が drain → process_native_event で publish し、example が RegistrationStateChanged(Registered) を受信して完走する。
- 【変更1-api】SipBackend に take_native_events（デフォルト実装付き）を追加。既存 22 メソッドのシグネチャは不変（破壊的変更なし）。NativeEvent / SipEventPayload enum は変更なし。TestBackend の pub フィールドに native_events を追加（#[cfg(any(test, feature = "test-util"))] スコープのみ）。
- 【変更1-schema】データスキーマ変更なし（Rust のフィールド/メソッド追加のみ。DB・シリアライズ形式は不変）。
- 【変更1-config】設定変更なし（ClientConfig / AccountConfig は不変。feature フラグも不変 — test-util / pjsua-native の意味を変更しない）。
- 【変更1-dep】依存クレート追加・削除なし。
- 【非変更1-item】PjsuaBackend の take_native_events オーバーライドは追加しない（既定空のまま）。
- 【非変更1-why】実 FFI 経路のネイティブイベント回収は §62.13 の NATIVE_EVENT_QUEUE → spawn_native_event_drain → DispatchCommand::NativeEvent が担う。PjsuaBackend に別の回収口を追加すると二重回収・順序競合のリスクがあるため、RFC §62.24 の『既定は空』を忠実に守る。
- 【影響1-component】src/runtime/backend.rs（trait + TestBackend）/ src/runtime/reactor.rs（SetRegistration アーム）/ examples/account_register.rs（購読順序）/ tests/verify_spec_p9_1.rs（2 テスト）と、TestBackend::set_registration 後状態に依存する単体テスト src/runtime/backend.rs:1293（test_backend_set_registration_transitions_registration_map）および src/runtime/backend_selection.rs:155（test_backend_set_registration_transitions）。
- 【影響1-nature】動作変更: TestBackend::set_registration 後の登録状態が Registering/Unregistering → Registered/Idle へ変化し、reactor 経由の register() 後 ClientState もイベント同期 publish により Registered へ達する。公開 API シグネチャは不変（trait メソッド追加のみ）で性能/セキュリティ影響なし。
- 【影響1-response】要 — 既存テスト 4 件の期待値を新契約へ更新（backend.rs:1293 は Registered/Idle へ、backend_selection.rs:155 は Registered/Idle へ、verify_spec_p9_1.rs:98/122 は register() 後の registration_state() を Registered へ）。make test で全 green を確認する。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0093`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0093` | test_policy | rust | 62.24 TestBackend 登録イベント発火と account_register example 完走（Q3） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.24 TestBackend 登録イベント発火と account_register example 完走（Q3） (N0093) | → | ☆ 62.2 バックエンド選択機構（PjsuaBackend 完全統一・MockBackend 削除） (N0071) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0093` | `src/runtime/backend.rs` |

## Investigation

実装対象の全証拠を調査で確認した。

【現状のイベント系列】(1) 実 FFI 経路: SipAccountHandle::register()（public_api_design.rs:56-72）→ RuntimeCommand::SetRegistration → reactor SetRegistration アーム（reactor.rs:741-794）→ backend.set_registration → pjsua_acc_set_registration → on_reg_state2 コールバック（callback.rs:251-253）が NativeEvent::RegistrationStateChanged{acc_id} を NATIVE_EVENT_QUEUE へ enqueue → drain task（event_path_wiring.rs:64-82, 5ms ポーリング §62.13）が DispatchCommand::NativeEvent として reactor へ転送 → process_native_event（reactor.rs:1045）→ process_registration_state_changed（registr_wiring.rs:53-88）→ get_account_info（実 status）→ 遷移 → SipEventPayload::RegistrationStateChanged を publish。(2) TestBackend 経路（現状）: set_registration（backend.rs:379-399）は set_registration_calls 記録 + Registering/Unregistering 遷移のみで NativeEvent を生成しない → account_register example（examples/account_register.rs）の await_registration が RegistrationStateChanged を 30 秒待ちタイムアウト（H5 / RC3 / round3_scope_rootcause.rs:141-147）。

【変更対象の正確な位置】SipBackend trait（backend.rs:57-180, 22 メソッド、take_native_events なし）。TestBackend struct（backend.rs:203-269, native_events フィールドなし、set_registration_calls L255, set_registration_result L261）。TestBackend::set_registration（backend.rs:379-399）。PjsuaBackend::set_registration（backend.rs:901-919, FFI 呼び出し、take_native_events をオーバーライドしない = 既定空）。reactor SetRegistration アーム（reactor.rs:741-794, 現状 set_registration → apply_registration_command_state のみで drain なし）。NativeEvent アーム（reactor.rs:845-860, CallStateTables 構築 + process_native_event 呼び出しの既存パターン）。process_native_event の RegistrationStateChanged 分岐（reactor.rs:1053-1057）。

【設計ギャップの根拠】RFC N0093 のリテラル実装（entry.registration = Registering のままイベント発火）は、m20_registr_cmd_pat.rs:49-54 の registration_state_from_status（0 → Idle）と registr_state_machine.rs:101-111 の遷移表（Registering→Idle は false）により、process_registration_state_changed（registr_wiring.rs:53-88）が Err(TransitionError) → None を返してイベントが drop される。account_entry_to_snapshot（backend.rs:564-580）は status 200 を Registered のときのみ返す（Registering なら 0）。従って example 完走には set_registration が outcome 状態（Registered/Idle）へ遷移し get_account_info が publish 可能な status を返す必要がある（C127）。

【購読タイミングの根拠】example は現在 register() 後に subscribe_account する（examples/account_register.rs:37-40）。tokio::sync::broadcast の新規購読者は過去イベントを再送しないため、register() 内の同期 publish（SetRegistration アームの drain）は購読前に失われる。従って example の subscribe を register() より前へ移動する必要がある（scope に含む）。

【影響を受ける既存テスト】backend.rs:1293 test_backend_set_registration_transitions_registration_map（registration_state() と accounts[&id].registration が Registering/Unregistering を期待）→ Registered/Idle へ更新必須。backend_selection.rs:155 test_backend_set_registration_transitions（registration_state() が Registering/Unregistering を期待）→ Registered/Idle へ更新必須。verify_spec_p9_1.rs:98-110 / 122-138（register() 後の registration_state() が Registering を期待）→ Registered へ更新必須（TestBackend が同期完結で成功イベントを発火するため）。public_api_design.rs:263-283 / 285-335 は add_account 直後の Disabled / 欠落アカウント / シャットダウン後のテストであり、register() を呼ばないため影響なし。reactor.rs:3053 update_account_register_on_start_consumes_set_registration は reactor ClientState（apply_registration_command_state が設定）を検証しており TestBackend 内部状態には非依存のため影響なし。

【バックエンド選択の根拠】backend_selection.rs:56-84 — pjsua-native feature で PjsuaBackend、test/test-util で TestBackend、どちらも無ければ unsupported_backend_error。Makefile:19-20 の make test は --features test-util で実行される。Cargo.toml:37 の test-util は「テスト用の決定論的 TestBackend を gating」。

## Acceptance Criteria

- account_register example が TestBackend 上で RegistrationStateChanged を受信して完走する。
- 【Happy path】account_register example 相当のフローが TestBackend 上で完走する: SipClient::new(config)（test-util で TestBackend 選択）→ add_account → subscribe_account（register より前）→ register() → reactor SetRegistration アームが backend.set_registration → apply_registration_command_state → take_native_events drain → process_native_event の順で処理し、AccountEventReceiver が SipEventPayload::RegistrationStateChanged(Registered) を 30 秒タイムアウト内に受信する。example は 'registration succeeded' を出力して exit 0。make test（--features test-util）が全 green。
- 【Error case】set_registration_result = Some(Err(..)) の失敗注入時、TestBackend::set_registration はイベントを発火せず Err を返し、reactor の SetRegistration アームは ClientState を変更せず reply へ Err を送る（既存 P16-3 経路を維持）。process_native_event 内で get_account_info が Err を返す場合、SipEventPayload::Error が publish され reactor ループは継続する。イベントが drop されることはなく、reactor が panic で終了しない。
- 【Edge case】set_registration(false) は accounts[id].registration を Idle へ遷移し、RegistrationStateChanged(Idle) が publish される（Unregistering→Idle 遷移が有効）。未知の native_acc_id への set_registration はイベントを発火せず Ok(()) を返す。take_native_events() は drain 後空（2 回目は空 Vec）。実 FFI 経路（PjsuaBackend）の take_native_events は既定空のまま（§62.13 キュー drain が担う）。acc_id の i32→u32 変換は 0..=i32::MAX で無劣化。

## Invariants

take_native_events は実 FFI 経路で既定空のまま（キュー drain §62.13 が担う）。

- 【Normal establishment】TestBackend::set_registration が成功した場合、native_events バッファへちょうど 1 個の NativeEvent::RegistrationStateChanged { acc_id } が積まれ、accounts[id].registration / registrations[id] が outcome 状態（enabled → Registered / disabled → Idle）へ遷移する。take_native_events() がそのイベントを返し、バッファを空にする。
- 【Invariant on error】set_registration_result = Some(Err(..)) の失敗注入時はイベントを発火せず短絡する（バッファ不変）。process_native_event 中の get_account_info エラーは SipEventPayload::Error として publish され、reactor ループは停止しない（既存 NativeEvent アームと同じ耐障害性）。
- 【Internal state invariant】SipBackend::take_native_events の既定は空 Vec（実 FFI 経路は §62.13 キュー drain が担い、二重回収しない）。TestBackend の native_events は FFI キューの代役であり、reactor SetRegistration アームが apply_registration_command_state の後に drain する（ClientState が Registering の状態でのみ Registering→Registered 遷移が有効になるため）。drain は reply 送信前に完了し、イベントが drop されない。
- 【Boundary invariant】take_native_events() は複数イベントを FIFO で返し、drain 後の再呼び出しは空（std::mem::take の完全 drain）。未知の native_acc_id への set_registration はイベントを発火しない。acc_id は i32→u32 変換（native_acc_id as u32）で無劣化（0..=i32::MAX 全域）。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C126 — N0093→N0071

- **Precondition**: TestBackend is the deterministic simulator for test-util builds (create_backend selects TestBackend when pjsua-native is absent and test/test-util is active — backend_selection.rs:68-75)
- **Postcondition**: TestBackend::set_registration fires NativeEvent::RegistrationStateChanged into its native_events buffer, recoverable via take_native_events() which drains the buffer to empty
- **Invariant**: take_native_events defaults to empty for the real FFI path — SipBackend trait default returns Vec::new(); PjsuaBackend does not override it (the §62.13 FFI queue drain owns the real path)

### C127 — N0093→N0071 (design refinement)

- **Precondition**: TestBackend::set_registration(acc_id, true) is called on an existing account with no injected set_registration_result failure; the reactor SetRegistration arm has already advanced ClientState to Registering via apply_registration_command_state
- **Postcondition**: The account entry reports a publishable native outcome: enabled → accounts[id].registration = Registered (get_account_info → status 200), disabled → Idle (status 0). The fired NativeEvent::RegistrationStateChanged therefore drives process_registration_state_changed to publish SipEventPayload::RegistrationStateChanged(Registered/Idle) instead of being dropped on an invalid Registering→Idle edge
- **Invariant**: The TestBackend's native_events buffer mirrors the §62.13 FFI queue one-to-one: one set_registration call pushes exactly one RegistrationStateChanged event; take_native_events() returns events FIFO and empties the buffer (std::mem::take)

### C128 — N0093→N0071 (reactor drain)

- **Precondition**: backend.set_registration returns Ok and buffers native events; the SetRegistration arm runs inside the reactor's serial command loop
- **Postcondition**: The SetRegistration arm drains backend.take_native_events() and passes every buffered event to process_native_event BEFORE sending the reply; no event is dropped or double-processed
- **Invariant**: The drain occurs after apply_registration_command_state (ClientState must be Registering for the Registering→Registered transition to be valid); the real FFI path is unaffected because PjsuaBackend::take_native_events returns empty

## Boy Scout Rule

本チケットで触れるコードの翻訳可能性を改善する。

【TestBackend::set_registration（backend.rs:379-399）】既存メソッドは「記録 → 失敗注入短絡 → 遷移 → イベント発火」の一連の処理を 1 関数に持つ。outcome 状態の導出（enabled→Registered / disabled→Idle）を関数 outcome_registration_state(enabled) -> RegistrationState として抽出し、関数名を動詞句にして「set_registration が outcome を計算し、アカウントを更新し、イベントを発火する」という流れをコード自体が語るようにする。

【reactor SetRegistration アーム（reactor.rs:741-794）】現状の catch_unwind クロージャは「backend.set_registration → apply_registration_command_state」の 2 責務を持つ。P17-4 で drain + process_native_event が加わり 3 責務になるため、drain 部分を drain_and_process_native_events(backend, event_bus, client_state, call_directions) ヘルパーとして抽出し、アームが「登録コマンドを backend へ適用し、発生したネイティブイベントを処理する」という物語として読めるようにする。NativeEvent アーム（reactor.rs:845-860）と同じ CallStateTables 構築パターンを再利用する。

【examples/account_register.rs】subscribe_account を register() より前に移動する際、変数名 account_events は意味が明確なため維持しつつ、イベント購読の意図（「登録前に subscribe し、同期 publish を見逃さない」）をコメントでなくコード順序自体が示すようにする（subscribe を add_account_and_resolve 直後に置く）。

【定数・マジックナンバー】既存の REGISTRATION_TIMEOUT（examples/account_register.rs）は名前付き定数のまま維持。新規に追加する値（outcome 状態の分岐）にマジックナンバーを導入しない。

【コメントの真実性】RFC §62.24 のコードに 'async fn handle_set_registration' とあるが実装は同期（catch_unwind 内）であるため、reactor アームのコメントは実装実態（同期 drain → process_native_event）を記述し、RFC の async 記述をそのまま移植しない。既存の「kept in lockstep with accounts[id].registration」コメント（backend.rs:226-228）は、set_registration で accounts と registrations の両方を outcome へ遷移するため lockstep が維持される（コメントは真実のまま）。

## Test Plan

### Unit Tests

- UT: [Normal] TestBackend::set_registration(acc_id, true) が NativeEvent::RegistrationStateChanged { acc_id } を native_events バッファへ 1 個 push し、accounts[id].registration を Registered へ進める（get_account_info が status 200 を返し、Registering→Registered 遷移が有効になる）。take_native_events() がそのイベントを返し、バッファを空にする。→ C126 Postcondition / C127 Postcondition を検証。
- UT: [Normal] TestBackend::set_registration(acc_id, false) が accounts[id].registration を Idle へ進め、native_events へ RegistrationStateChanged を push する（get_account_info が status 0 を返し、Unregistering→Idle 遷移が有効）。→ C127 Boundary。
- UT: [Normal] 実 FFI 経路（PjsuaBackend）の take_native_events は既定実装（Vec::new()）のまま — SipBackend trait のデフォルト実装が空 Vec を返し、PjsuaBackend はオーバーライドしない（イベント回収は §62.13 キュー drain が担う）。→ C126 Invariant。
- UT: [Normal] reactor の SetRegistration アームが backend.set_registration → apply_registration_command_state → take_native_events() drain → process_native_event の順で処理し、SipEventPayload::RegistrationStateChanged(Registered) を EventBus へ publish する。drain は apply_registration_command_state の後でなければならない（ClientState が Registering でないと遷移が無効）。→ C128 Precondition/Postcondition。
- UT: [Error] set_registration_result = Some(Err(..)) が注入された場合、TestBackend::set_registration は短絡してイベントを発火せず Err を返す（既存の失敗注入経路を維持 — P16-3 §62.12）。reactor は ClientState を変更せず reply へ Err を送る。→ エラー系。
- UT: [Error] process_native_event(RegistrationStateChanged) で backend.get_account_info が Err を返す場合、SipEventPayload::Error が publish され、reactor ループは継続する（reactor.rs:1537 既存テスト process_native_event_registration_err_publishes_error と整合）。→ エラー系。
- UT: [Boundary] 未知の native_acc_id に対する TestBackend::set_registration はイベントを発火せず Ok(()) を返す（accounts.get_mut が None の分岐 — §62.2 のレジストリ不在時）。→ C127 Boundary。
- UT: [Boundary] take_native_events() は複数イベントを FIFO で返し、drain 後の 2 回目の呼び出しは空 Vec を返す（std::mem::take による完全 drain）。→ C126 Invariant / C128 Invariant。
- UT: [Invariant] create_backend が test-util ビルド（pjsua-native なし、test または test-util feature）で TestBackend を返す（backend_selection.rs 既存テスト create_backend_returns_test_backend_in_test_build）。→ C126 Precondition。
- UT: [Invariant] TestBackend::set_registration は set_registration_calls を (native_acc_id, enabled) で記録し、イベント発火前でも記録が完了している（unregister-first 順序の検証 — P16-3 §62.12）。→ 内部状態不変条件。

### Integration Tests

- IT: [test-util] account_register example 相当のフローが TestBackend 上で RegistrationStateChanged を受信して 30 秒タイムアウトせずに完走する（tests/verify_spec_p9_1.rs の Layer 5 テストを拡張）。
- IT: [Integration point] SipClient::new(config) → create_backend（test-util で TestBackend 選択）→ SipClient::add_account（RuntimeCommand::AddAccount）→ SipAccountHandle::register()（RuntimeCommand::SetRegistration）→ reactor SetRegistration アーム（backend.set_registration → apply_registration_command_state → take_native_events drain → process_native_event）→ EventBus → subscribe_account の AccountEventReceiver が RegistrationStateChanged(Registered) を受信、の一連の公開 API 境界。
- IT: [Verification] register() より前に subscribe_account した AccountEventReceiver が、register() の同期 publish による SipEventPayload::RegistrationStateChanged(RegistrationState::Registered) を 30 秒タイムアウト内に受信すること。受信後、handle.query_state() で ClientState.accounts[id].registration が Registered に達していることを検証（TestBackend が同期完結で成功イベントを発火するため）。
- IT: [Prerequisites] make test が --features test-util で実行される（Makefile:19-20）。create_backend が pjsua-native なし + test-util で TestBackend を返す（backend_selection.rs:68-75）。EventBus の subscribe_control が AccountEventReceiver へ配送される（eventbus_receiver.rs）。
- IT: [Related tickets] P17-4 は P16-3（§62.12 統合 RegistrationStateChanged イベント）、P15-5（§62.4 登録状態機械 production 配線）、P16-4（§62.13 FFI drain 経路）、P17-3（§62.23 P1/P2 コールバック登録）、P9-1（account_register example の Layer 5 フロー）に依存/関連する。

### Exceptions

- Item: 実 FFI 経路（pjsua-native）で account_register example が実 SIP レジストラに対して完走すること。Reason: 実際の REGISTER は Asterisk 等の外部 SIP レジストラと実ネットワーク応答が必要であり、単体/統合テスト（make test は test-util で TestBackend を使用）で決定論的に再現できない（テスト不能）。これは『決定論的だが原理的にテスト不能（設計欠陥）』ではなく、外部ネットワーク依存による環境要因であり、設計欠陥ではない。TestBackend 上での完走は本チケットで固定し、実 FFI 経路は §62.13 の on_reg_state2 → キュー → drain → process_native_event が既存テスト（ffi/callback.rs on_reg_state_enqueues_registration_state_changed、event_path_wiring.rs drain_pending_native_events_forwards_fifo、reactor.rs enqueue_native_event ラウンドトリップ）で担保されている。Alternative verification: docker Asterisk 統合テスト（make test-integration / P16-10、tests/sip_integration.rs）を pjsua-native ビルドで実行し、実 REGISTER 応答での RegistrationStateChanged publish を確認する。
- Item: FFI ネイティブイベントキュー（NATIVE_EVENT_QUEUE, §62.13）の 5ms ポーリング drain の実時間タイミング。Reason: spawn_native_event_drain は tokio::time::interval の 5ms ポーリングでキューを drain する（event_path_wiring.rs:64-82）。FFI コールバックスレッドからの挿入と drain の実時間順序はスレッドスケジューリング依存で、単体テストで決定論的に固定できないためテスト不能である。これは設計欠陥ではなく（not a design defect）、非同期ポーリングの本質による非決定性であり、TestBackend の同期イベント発火（native_events Vec + take_native_events）がこの非決定性を排除する設計である。Alternative verification: drain_pending_native_events の FIFO 順序/空キュー（event_path_wiring.rs 既存テスト）と、enqueue_native_event 経由の reactor ラウンドトリップ（P12-7 既存テスト）で決定論的に代替検証する。
- Item: 実 PJSUA の on_reg_state2 コールバックが REGISTER 応答を受けて非同期的に発火するタイミング（成功/失敗の実ネットワーク結果）。Reason: 実経路では REGISTER 送信後の応答タイミングはネットワーク遅延とレジストラ状態に依存し、決定論的にテストできない（テスト不能）。これは設計欠陥ではなく（not a design defect）、外部ネットワーク応答に起因する非決定性である。本チケットは TestBackend でこれを『set_registration 内の同期完結』に圧縮してシミュレートする（イベント発火 + accounts[id].registration を Registered/Idle へ遷移）。これは決定論性を損なう設計ではなく、シミュレータの意図的な抽象化であり、RFC §62.24 の『実 FFI 経路と同じイベント系列』の趣旨に沿う。Alternative verification: TestBackend の同期イベント発火 + 状態遷移を単体テストで固定し、実経路の on_reg_state2 enqueue は ffi/callback.rs の単体テスト、実 200/403 応答の publish は reactor.rs の process_native_event テスト（reactor.rs:1491/1577）で担保する。

### Plan Test Code (concrete code)

- UT: C126-Pre (TestBackend is deterministic simulator for test-util builds)
```rust
#[test]
fn create_backend_selects_test_backend_in_test_util_build() -> Result<(), Box<dyn std::error::Error>> {
    let audio_taps: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
    let audio_mixers: AudioMixerMap = Arc::new(RwLock::new(HashMap::new()));
    let mut backend = create_backend(&ClientConfig::default(), audio_taps, audio_mixers)?;
    let (id, _entry) = backend.add_account(&AccountConfig::default())?;
    assert_eq!(id, 1, "test-util build must select the concrete TestBackend");
    Ok(())
}
```
- UT: C126-Post (set_registration fires RegistrationStateChanged, take_native_events drains)
```rust
#[test]
fn test_backend_set_registration_fires_event_and_drains() -> Result<(), Box<dyn std::error::Error>> {
    let mut backend = TestBackend::new();
    let (id, _) = backend.add_account(&AccountConfig::default())?;
    backend.set_registration(id, true)?;
    assert_eq!(
        backend.native_events,
        vec![NativeEvent::RegistrationStateChanged { acc_id: id as u32 }]
    );
    let drained = backend.take_native_events();
    assert_eq!(drained, vec![NativeEvent::RegistrationStateChanged { acc_id: id as u32 }]);
    assert!(backend.take_native_events().is_empty(), "drain must empty the buffer");
    Ok(())
}
```
- UT: C126-Inv (real FFI path take_native_events defaults empty)
```rust
#[test]
#[cfg(feature = "pjsua-native")]
fn pjsua_backend_take_native_events_defaults_empty() -> Result<(), Box<dyn std::error::Error>> {
    let taps: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
    let mixers: AudioMixerMap = Arc::new(RwLock::new(HashMap::new()));
    let mut backend = PjsuaBackend::with_registries(taps, mixers);
    assert!(backend.take_native_events().is_empty(),
        "PjsuaBackend must not override take_native_events — §62.13 queue drain owns the real path");
    Ok(())
}
```
- UT: C127-Pre (set_registration(true) on existing account; reactor already advanced ClientState to Registering)
```rust
#[test]
fn set_registration_precondition_setup() -> Result<(), Box<dyn std::error::Error>> {
    let mut backend = TestBackend::new();
    let (id, _) = backend.add_account(&AccountConfig::default())?;
    backend.set_registration(id, true)?;
    assert_eq!(backend.accounts[&id].registration, RegistrationState::Registered,
        "enabled outcome must be Registered so get_account_info reports 200");
    Ok(())
}
```
- UT: C127-Post (get_account_info reports publishable outcome → process_registration_state_changed publishes Registered)
```rust
#[test]
fn set_registration_makes_event_publish_registered() -> Result<(), Box<dyn std::error::Error>> {
    let mut backend = TestBackend::new();
    let (id, _) = backend.add_account(&AccountConfig::default())?;
    backend.set_registration(id, true)?;
    let snapshot = backend.get_account_info(id as u32)?;
    assert_eq!(snapshot.registration_status, 200, "enabled outcome must report status 200");
    let mut accounts = account_with_registration(id as u32, RegistrationState::Registering);
    let event = process_registration_state_changed(&backend, id as u32, &mut accounts).unwrap();
    assert!(matches!(event.payload,
        SipEventPayload::RegistrationStateChanged(RegistrationState::Registered)));
    Ok(())
}
```
- UT: C127-Inv (one event per set_registration; take_native_events returns FIFO and empties)
```rust
#[test]
fn set_registration_pushes_exactly_one_event_fifo() -> Result<(), Box<dyn std::error::Error>> {
    let mut backend = TestBackend::new();
    let (id, _) = backend.add_account(&AccountConfig::default())?;
    backend.set_registration(id, true)?;
    backend.set_registration(id, false)?;
    assert_eq!(backend.native_events.len(), 2, "one event per set_registration call");
    assert_eq!(backend.native_events[0], NativeEvent::RegistrationStateChanged { acc_id: id as u32 });
    assert_eq!(backend.native_events[1], NativeEvent::RegistrationStateChanged { acc_id: id as u32 });
    assert_eq!(backend.take_native_events().len(), 2);
    assert!(backend.take_native_events().is_empty(), "second drain returns empty");
    Ok(())
}
```
- UT: C128-Pre/Post (reactor SetRegistration arm drains take_native_events and processes before replying)
```rust
#[tokio::test]
async fn register_through_public_facade_publishes_registered() -> Result<(), Box<dyn std::error::Error>> {
    let (handle, join) = spawn_reactor();
    let account_id = handle.submit_add_account(AccountConfig::default()).await?;
    let mut rx = handle.event_bus().subscribe_control();
    let account = SipAccountHandle::new(client, account_id);
    account.register().await?;
    let ev = tokio::time::timeout(Duration::from_secs(30), rx.recv()).await??;
    assert!(matches!(ev.payload,
        SipEventPayload::RegistrationStateChanged(RegistrationState::Registered)));
    shutdown_reactor(handle, join).await;
    Ok(())
}
```
- UT: C128-Inv (drain after apply_registration_command_state — ClientState must be Registering for Registering→Registered)
```rust
#[test]
fn drain_must_follow_client_state_advance() {
    // Disabled -> Registered is an invalid edge (transition table row 0).
    // If the SetRegistration arm drained before apply_registration_command_state,
    // current == Disabled and process_registration_state_changed returns None.
    let mut backend = TestBackend::new();
    let (id, _) = backend.add_account(&AccountConfig::default()).unwrap();
    backend.set_registration(id, true).unwrap();
    let mut accounts = account_with_registration(id as u32, RegistrationState::Disabled);
    let event = process_registration_state_changed(&backend, id as u32, &mut accounts);
    assert!(event.is_none(), "Disabled→Registered is invalid; the arm must advance ClientState to Registering first");
    let mut accounts = account_with_registration(id as u32, RegistrationState::Registering);
    let event = process_registration_state_changed(&backend, id as u32, &mut accounts);
    assert!(event.is_some(), "Registering→Registered (status 200) is the valid publish path");
}
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| TestBackend::set_registration は内部状態更新のみでイベント非発火 | NativeEvent::RegistrationStateChanged を発火し、example が TestBackend 上で完走 |  |
| SipBackend trait に take_native_events が存在しない。TestBackend::set_registration は内部状態更新のみで NativeEvent を生成しない。reactor SetRegistration アームは set_registration 後のネイティブイベントを回収しない。account_register example は register() 後に subscribe する。 | SipBackend に take_native_events（既定空 Vec::new()）を追加。TestBackend に native_events バッファを追加し、set_registration が outcome 状態（enabled→Registered / disabled→Idle）へ遷移 + NativeEvent::RegistrationStateChanged を発火。TestBackend::take_native_events は std::mem::take で drain。reactor SetRegistration アームが apply_registration_command_state 後に take_native_events() を drain し process_native_event で publish。account_register example は subscribe を register() より前に移動。 | P17-4 §62.24: TestBackend が登録イベントを発火し、example が TestBackend 上で完走する。 |

## Notes in Prior Implementation Rounds

- 【Implementation steps】(1) src/runtime/backend.rs の SipBackend trait に fn take_native_events(&mut self) -> Vec<NativeEvent> { Vec::new() } を追加（L57-180、use に NativeEvent を追加）。[::TICKET::] P17-4 の provenance アノテーションを付与。(2) TestBackend struct（L203-269）に pub native_events: Vec<NativeEvent> を追加し、derive(Default) で初期化（既存フィールドの並びに合わせる）。(3) TestBackend::set_registration（L379-399）を改修: 既存の set_registration_calls 記録 / set_registration_result 短絡を維持し、その後に outcome 状態（enabled → Registered / disabled → Idle）を accounts[id].registration と registrations の両方へ設定、self.native_events.push(NativeEvent::RegistrationStateChanged { acc_id: native_acc_id as u32 }) を実行。(4) TestBackend に fn take_native_events(&mut self) -> Vec<NativeEvent> { std::mem::take(&mut self.native_events) } を実装。(5) src/runtime/reactor.rs の SetRegistration アーム（L741-794）の catch_unwind クロージャ内、apply_registration_command_state の後に、NativeEvent アーム（L845-860）と同型の CallStateTables を構築し、for event in backend.take_native_events() { process_native_event(&mut *backend, &event_bus, event, &mut call_state, &mut client_state.accounts); } を実行。(6) examples/account_register.rs で subscribe_account を register() の前に移動（add_account_and_resolve 直後）。(7) 既存テスト更新: backend.rs:1293 と backend_selection.rs:155 の期待値を Registered/Idle へ、verify_spec_p9_1.rs:98/122 の registration_state() 期待値を Registered へ（Red フェーズで先に更新）。新規テスト追加（testUnit/testIntegration に定義）。(8) cargo fmt / make check-be / make test で検証。
- 【Risks】RFC N0093 のリテラル実装（entry.registration = Registering のままイベント発火）は状態機械の検証で drop される。m20_registr_cmd_pat.rs:49-54（status 0 → Idle）と registr_state_machine.rs:101-111（Registering→Idle は無効遷移）の組み合わせにより、process_registration_state_changed が None を返し example が依然タイムアウトする。本仕様は outcome 状態（Registered/Idle）への遷移で解決（C127 として契約化）。register() 後の ClientState が Registered へ変わるため、既存テスト 4 件の期待値更新が必須（影響範囲は scope に列挙）。
- 【Caveats】reactor SetRegistration アームの drain は apply_registration_command_state の後でなければならない。ClientState が Registering でない状態（Disabled 等）で process_native_event が走ると Registering→Registered 遷移が無効になりイベントが drop される（C128 Invariant）。example は subscribe_account を register() より前に移動必須 — tokio::sync::broadcast は新規購読者へ過去イベントを再送せず、register() 内の同期 publish は購読前に失われる。RFC §62.24 のコードは 'async fn handle_set_registration' と記すが、実際の reactor アームは同期（catch_unwind 内）であり process_native_event も非 async — 仕様は実装実態に合わせる。
- 【Open items】実 FFI 経路で on_reg_state2 が 403 等を返す Failed パスは、TestBackend では get_account_info_result = Some(Err(..)) 注入で代替検証可能だが、status 403 を返す既定シミュレーションは本チケットのスコープ外。ネイティブ復旧（H1）時に PjsuaBackend::get_account_info の実 status 解決との整合を確認する。make_call example など他 example の TestBackend 完走は同パターンで後続対応。
- 【Future improvements】TestBackend に登録失敗シミュレーション（registration_status 403 注入用の設定フィールド）を追加し、example の Failed パスを TestBackend 上で完走させる。TestBackend::take_native_events のログ/メトリクス（drain 数）を observability 層へ接続する。P17-10（§62.30 I/O 境界）で TestBackend イベント系列を I/O 境界参照情報へ反映する。
Implementation summary (P17-4):
- Changed files: src/runtime/backend.rs, src/runtime/reactor.rs, src/runtime/backend_selection.rs, src/ffi/bindings.rs, src/ffi/raw_sip_module.rs, examples/account_register.rs, tests/verify_spec_p9_1.rs
- Key changes: (1) SipBackend::take_native_events default (Vec::new()) added; (2) TestBackend.native_events field + set_registration outcome-state transition (Registered/Idle) + event firing (only for known accounts, mirroring the real FFI path where invalid acc_id fires no event); (3) TestBackend::take_native_events = std::mem::take; (4) reactor SetRegistration arm drains take_native_events after apply_registration_command_state and processes via process_native_event (publishes RegistrationStateChanged(Registered)); (5) example subscribes before register(); (6) updated 4 existing tests (backend.rs:1293, backend_selection.rs:155, verify_spec_p9_1.rs:98/122) to the new Registered/Idle contract; (7) added 7 backend unit tests + 1 reactor test + 1 integration test (account_register_receives_registered_event); (8) boy-scout: fixed pre-existing test-isolation races on CONF_ADD_PORT_STATUS (with_conf_add_port_status mutex guard in stub_test_hooks) and the raw-SIP queue (RAW_SIP_QUEUE_GUARD).
- Test results: make test --features test-util — lib 1295 passed, all integration binaries pass (0 failures); 3 consecutive lib runs green (flaky races resolved).
- Quality: new code uses ? (no unwrap), no new unsafe; pre-existing FFI unsafe has SAFETY comments; test unwraps permitted by project Rust rules.
Review report (P17-4):
- Static quality check: passed
- Translatability: fixed 1 noun-phrase function name (outcome_registration_state → resolve_registration_outcome); no single-char/generic vars; no magic numbers in source; no debug output
- Dependencies: consistency verified — no dependency changes
- Issues found and fixes applied during review: (1) re-injected missing P17-4 provenance annotations in src/ffi/raw_sip_module.rs (start-phase flaky-fix edits) and resolved recurring AMBIGUOUS markers in examples/account_register.rs + tests/verify_spec_p9_1.rs (changes inside #[tokio::main]/#[tokio::test] functions the parser does not recognize — annotation placed before the enclosing fn); (2) renamed outcome_registration_state to resolve_registration_outcome for verb-phrase translatability
- Pre-existing flaky test races fixed during implementation (boy-scout): CONF_ADD_PORT_STATUS shared global (with_conf_add_port_status mutex guard in stub_test_hooks) and raw-SIP queue (RAW_SIP_QUEUE_GUARD) — both serialized via Mutex; 3 consecutive lib runs green
- Contracts: C126/C127/C128 covered with @verifies; verify-final-contracts 100% coverage; validate-ticket-targets verifiedEmpty

## P17-4 — implemented at 31 locations

### examples/account_register.rs

- Line 27
```rust
// [::TICKET::] P17-4 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P17-4 --for-spec --no-implementation-order`.
```

- Line 28
```rust
// [::TICKET::] P17-4 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P17-4 --for-spec --no-implementation-order`.
```

- Line 29
```rust
    let args = cli::parse(std::env::args().skip(1))?;
```

### src/ffi/bindings.rs

- Line 948
```rust
    static CONF_ADD_PORT_HOOK_GUARD: Mutex<()> = Mutex::new(());
```

- Line 964
```rust
    pub(crate) fn with_conf_add_port_status<T>(status: i32, f: impl FnOnce() -> T) -> T {
```

- Line 1387
```rust
    fn stub_test_hooks_can_force_conf_add_port_failure() {
```

### src/ffi/raw_sip_module.rs

- Line 174
```rust
    fn capture_raw_sip_message_roundtrips_packet_bytes() {
```

- Line 242
```rust
    fn capture_skips_non_positive_length_without_panic() {
```

### src/runtime/backend.rs

- Line 167
```rust
    fn push_media_frame(
```

- Line 181
```rust
    fn register_conf_callback(&mut self) -> Result<(), ReactorError>;
```

- Line 190
```rust
    fn take_native_events(&mut self) -> Vec<NativeEvent> {
```

- Line 331
```rust
#[cfg(any(test, feature = "test-util"))]
```

- Line 333
```rust
fn resolve_registration_outcome(
```

- Line 347
```rust
impl SipBackend for TestBackend {
```

- Line 415
```rust
    fn set_registration(&mut self, native_acc_id: i32, enabled: bool) -> Result<(), ReactorError> {
```

- Line 447
```rust
    fn take_native_events(&mut self) -> Vec<NativeEvent> {
```

- Line 1343
```rust
    fn test_backend_set_registration_reaches_outcome_state() -> Result<(), Box<dyn std::error::Error>>
```

- Line 1378
```rust
    fn test_backend_set_registration_fires_event_and_drains() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1404
```rust
    fn test_backend_set_registration_enable_reports_registered() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1420
```rust
    fn test_backend_set_registration_disable_reports_idle() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1436
```rust
    fn test_backend_set_registration_unknown_account_no_event() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1449
```rust
    fn test_backend_take_native_events_fifo_and_empty() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1471
```rust
    fn test_backend_set_registration_failure_injection_no_event() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1488
```rust
    fn test_backend_set_registration_records_calls() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1786
```rust
    fn register_media_ports_for_calls_registers_and_connects_every_audio_mixer_entry(
```

- Line 1820
```rust
    fn register_media_ports_for_calls_surfaces_native_error_on_conf_add_failure() {
```

### src/runtime/backend_selection.rs

- Line 154
```rust
    fn test_backend_set_registration_reaches_outcome_state() -> Result<(), Box<dyn std::error::Error>> {
```

### src/runtime/reactor.rs

- Line 113
```rust
impl CoreReactor {
```

### tests/verify_spec_p9_1.rs

- Line 99
```rust
// [::TICKET::] P17-4 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P17-4 --for-spec --no-implementation-order`.
```

- Line 100
```rust
// [::TICKET::] P17-4 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P17-4 --for-spec --no-implementation-order`.
```

- Line 101
```rust
    let (client, _events) = SipClient::new(client_config()).await?;
```
