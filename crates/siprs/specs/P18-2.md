# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P18-2: Producer: crates/pjsip-prebuilt + CI commit (§62.36–62.37)

**Ticket Key**: P18-2 · **Phase**: 18

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
git clone 直後に 3 OS（macOS / Windows / Linux）の PJSIP 2.17.0 prebuilt が `vendor/prebuilt/<target-triple>/{include,lib}` に存在し、追加取得手順なしで `cargo build --features pjsua-native` が通る状態を、専用 producer クレート `crates/pjsip-prebuilt` + GitHub Actions 3 OS マトリクス + 通常 push 直接コミットで実現する（設計ブリーフ §5.1）。

### Purpose
siprs の pjsua-native ビルドは prebuilt-first 解決（build.rs 4 段階パイプライン, §28.1/§62.35）に依存する。現在 `vendor/prebuilt/` は aarch64-apple-darwin のみ手動 stage されており、Windows / Linux の prebuilt が存在しない。本チケット（Ticket B, §62.36–62.37）は siprs クレートのコンパイル状態（Ticket A の 69 エラー, §62.31）と独立に、全ターゲットの prebuilt を生成・検証・コミットする専用 producer を提供する。

### Motivation
設計ブリーフ §5.1/§5.5 で「clone 直後に prebuilt が存在し、`cargo build --features pjsua-native` が通る」ことが必須と決定された。既存の `prebuilt-refresh.yml`（P1-4）は macOS のみ・手動トリガ・artifact アップロードで git にコミットしないため §5.1 を満たさない。round-4 の進化スコープ（§62.31/Q9–Q22）で producer/consumer 分離が確定し、Ticket B として本チケットが必要となった。

### Constraints
- producer は siprs 非依存の独立クレート `crates/pjsip-prebuilt`（§62.36）。既存クレートと同一ワークスペース化しない（現リポジトリにワークスペースは存在しない）。
- シェルスクリプトによるビルド禁止（§62.36）。ビルドは Rust ツール + CMake + コミット済み Dockerfile。
- 任意の方法でライブラリが得られない場合は非ゼロ終了 / panic（サイレントフォールバック禁止, §5.3）。
- DoD は Ticket A（siprs コンパイル成功）に非依存（§5.3/§5.5）。DoD = git clone 各 OS → prebuilt 存在 →（A 完了後）`cargo build --features pjsua-native` が通る。
- verify は file（マシン形式）+ nm（pjsua_init / pj_init シンボル）+ 最小 C リンクテストの 3 段階（§62.36 Q16）。
- ターゲットセットは §5.6 のホスト OS マッピング（macOS→host + Docker Linux / Windows→MSVC / Linux→host）に従う。

## Scope

- 【変更1-path】crates/pjsip-prebuilt/Cargo.toml
- 【変更1-action】add: siprs 非依存の独立クレート（単独 Cargo.toml, ワークスペース非所属）。`[package] name = "pjsip-prebuilt"`。CLI 依存 clap + thiserror。`src/lib.rs` に純ロジック（host 検出 / ターゲットセット / stage 配置 / verify 述語 / cmake・docker コマンド構築）を実装し、`src/main.rs` は薄い CLI ラッパー。
- 【変更1-detail】CLI サブコマンド `build <triple>` / `build-all` / `stage <triple>` / `stage-all` / `verify <triple>` / `verify-all`（§62.36 Q13 形状 + §62.37 build-all/verify-all）。ホスト OS 検出で §5.6 のターゲットセット決定（macOS→host + Docker Linux、Windows→MSVC、Linux→host）。
- 【変更1-before-after】before: 存在しない → after: `cargo run --manifest-path crates/pjsip-prebuilt/Cargo.toml -- build <triple>` 等で build/stage/verify を実行可能。
- 【変更1-api】公開 API: `HostOs` / `TargetTriple` / `ProducerError` / `VerifyFailure` / `MachineKind` 型 + `target_set_for_host` / `detect_host_triple` / `validate_staged_layout` / `parse_file_output` / `nm_output_has_pjsua_symbols` / `docker_build_command` / `docker_run_command` / `cmake_build_command` 関数。
- 【変更1-schema】N/A（永続データスキーマなし）。
- 【変更1-config】N/A（新環境変数なし。ホスト検出は `std::env::consts::OS` + `rustc -vV` の host フィールド）。
- 【変更1-dep】add: clap / thiserror（producer クレートのみ）。siprs への依存なし（C143 invariant）。外部バイナリ: cmake / file / nm / cc / docker。
- 【変更2-path】crates/pjsip-prebuilt/Dockerfile
- 【変更2-action】add: ubuntu:22.04 ベース。`build-essential cmake libasound2-dev libssl-dev libuuid-dev` を `--no-install-recommends` で導入。`WORKDIR /work`。実行例: `docker build -t pjsip-prebuilt . && docker run --rm -v $(pwd)/vendor:/work/vendor pjsip-prebuilt cmake -S /work/vendor/pjsip -B /work/vendor/build`（§62.36 Q14）。コンテナ内ビルド成果物は volume マウントでホスト vendor へ直接生成。
- 【変更2-detail】Linux-from-Mac 専用。Windows ターゲットは Docker 不使用（§5.6）。
- 【変更2-before-after】before: 存在しない（Linux-from-Mac は手動） → after: コミット済み Dockerfile で再現可能な Linux ビルド環境。
- 【変更2-api】N/A。
- 【変更2-schema】N/A。
- 【変更2-config】N/A。
- 【変更2-dep】Docker イメージ依存（ubuntu:22.04 + 上記パッケージ群, §62.36 Q14）。
- 【変更3-path】crates/pjsip-prebuilt/src/main.rs（+ src/lib.rs 配下モジュール）
- 【変更3-action】add: CLI 実装。`Command` enum を `clap` でパースし `match` で分岐（§62.36 CLI 形状）。`build` は host なら `std::process::Command` で cmake、Linux-from-Mac なら `docker build` + `docker run -v $(pwd)/vendor:/work/vendor`。`stage` は `vendor/prebuilt/<triple>/{include,lib}` へ配置。`verify` は file（マシン形式）+ nm（pjsua_init / pj_init シンボル）+ 最小 C リンクテスト（§62.36 Q16）。
- 【変更3-detail】`build-all` は `target_set_for_host` でターゲットセットを決定し各 triple に build→stage、`verify-all` はステージ済み全 triple を verify。全失敗で非ゼロ exit / `Err`（サイレントフォールバック禁止）。
- 【変更3-before-after】before: 存在しない → after: `cargo run --manifest-path crates/pjsip-prebuilt/Cargo.toml -- build-all` / `-- verify-all` が全ターゲットの prebuilt を生成・検証。
- 【変更3-api】CLI サブコマンド: `build` / `build-all` / `stage` / `stage-all` / `verify` / `verify-all`（引数 triple）。
- 【変更3-schema】N/A。
- 【変更3-config】N/A。
- 【変更3-dep】外部バイナリ依存（cmake, file, nm, cc, docker — 実行時に PATH から検索し欠落時は明確な `Err`）。
- 【変更4-path】.github/workflows/prebuilt.yml
- 【変更4-action】add: 3 OS マトリクス（macos-latest / ubuntu-latest / windows-latest）。`on.push.branches: [master, siprs]` + `paths: [crates/siprs/vendor/pjsip/**, crates/pjsip-prebuilt/**]`（repo root 相対, §62.37）。各 runner: actions/checkout@v4 → dtolnay/rust-toolchain@stable → `cargo run --manifest-path crates/pjsip-prebuilt/Cargo.toml -- build-all` → `cargo run ... -- verify-all` → `stefanzweifel/git-auto-commit-action@v5` で vendor/prebuilt を通常 push で直接コミット（PR 儀式なし, §62.37 Q15）。
- 【変更4-detail】ファイル位置は repo root `.github/workflows/`（GitHub Actions は repo root の workflows のみ走査）。既存 siprs ワークフロー（ci.yml 等）は `crates/siprs/.github/workflows/` にあり root から認識されないため、本ファイルは root に置く。
- 【変更4-before-after】before: `prebuilt-refresh.yml` は macOS のみ・手動トリガ・artifact アップロードで git にコミットしない → after: 3 OS が全ターゲットの prebuilt を生成し直接コミット。
- 【変更4-api】N/A。
- 【変更4-schema】N/A。
- 【変更4-config】ワークフロー設定（on.push.branches / paths, strategy.matrix.os, step 定義）。
- 【変更4-dep】GitHub Actions 依存（actions/checkout@v4, dtolnay/rust-toolchain@stable, stefanzweifel/git-auto-commit-action@v5）。
- 【変更5-path】src/build/prebuilt_producer.rs / src/build/prebuilt_ci_commit.rs
- 【変更5-action】modify: `[::STUB::] P18-2: implement NODE_ID=N0105 / N0106` を解決（MANDATORY STUB RESOLUTION）。siprs 側でテスト可能な純ロジック（`HostOs` → ターゲットセット写像, verify 述語, CI マトリクスモデル定数）を実装し `src/build/mod.rs` に登録して `make test` でカバー。design artifact ヘッダ（Initial Design Artifact — RFC-driven Implementation）は維持。
- 【変更5-detail】producer クレート（crates/pjsip-prebuilt）が実行の主役。siprs 側モジュールは仕様定数 + 検証述語に留め、重複を最小化（実装フェーズで共有範囲確定）。
- 【変更5-before-after】before: `fn main() { /* TODO */ }` + `[::STUB::]` → after: 実装モジュール（テストカバー）。
- 【変更5-api】N/A（内部モジュール, pub(crate)）。
- 【変更5-schema】N/A。
- 【変更5-config】N/A。
- 【変更5-dep】N/A。
- **Out of scope:**
  - [Excluded item] consumer 側の build.rs 4 段階解決 / bindgen enum 生成（§62.31–62.35）は P18-1（Ticket A）が担当。prebuilt の実際の消費（resolve → link → bindgen）は本チケットの対象外。
  - [Why excluded] producer/consumer 分離（設計ブリーフ §5.3/§5.5）により本チケット（Ticket B）の DoD は siprs コンパイル成功に非依存。README RESIDUE ギャップ（H8/H13/H14/H15/EXAMPLES）は round-4 後続チケット（§62.43/Q22）。
- **Affected areas:**
  - [Affected component] `vendor/prebuilt/<target>/{include,lib}` — producer が再生成するため既存 `aarch64-apple-darwin` が P18-1 §62.33 の新しい bindgen 設定に整合して更新される。
  - [Nature of impact] バイナリデータ形式（静的 .a/.lib 群）+ ディレクトリレイアウト。build.rs（consumer）の `resolve_prebuilt_lib_dir` が `vendor/prebuilt/<target>/lib` を読むため、stage レイアウト不変条件（include/pjsua.h + lib/libpjsua*）が満たされないと consumer 解決が失敗する。
  - [Corresponding change needed] Y（P18-1 の build.rs は prebuilt-first 解決を実装済み — 本チケットの stage がレイアウト不変条件を満たせば追加変更不要）。`vendor/prebuilt/BUILD.md` の手動手順は producer 利用へ更新が望ましい（Boy Scout Rule）。

## STUBs — Must Be Fully Implemented in This Ticket (2 items — resolved: 2)

### TS-001 — `src/build/prebuilt_producer.rs — resolved

- **Marker**: `// [::TICKET::] P18-2 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P18-2 --for-spec --no-implementation-order`.`
- **Contracts**: C143
- **Resolution Plan**: Implemented the N0105 prebuilt producer design-artifact module in src/build/prebuilt_producer.rs: ProducerHost -> §5.6 target set, file/nm verify predicates, staged-layout validation, registered in src/build/mod.rs and covered by make test. The executable producer logic lives in the standalone crate crates/pjsip-prebuilt (§62.36).

### TS-002 — `src/build/prebuilt_ci_commit.rs — resolved

- **Marker**: `// [::TICKET::] P18-2 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P18-2 --for-spec --no-implementation-order`.`
- **Contracts**: C144
- **Resolution Plan**: Implemented the N0106 prebuilt-CI-commit design-artifact module in src/build/prebuilt_ci_commit.rs: 3-OS matrix workflow model (macos-latest / ubuntu-latest / windows-latest, build-all + verify-all with --manifest-path crates/pjsip-prebuilt/Cargo.toml, git-auto-commit of vendor/prebuilt), registered in src/build/mod.rs and covered by make test. The actual workflow file is .github/workflows/prebuilt.yml at the repo root (§62.37).

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0105` `N0106`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0105` | build_ci | rust | 62.36 producer ツール crates/pjsip-prebuilt（Q13 / Q14 / Q16） |
| `N0106` | build_ci | rust | 62.37 CI 運用と prebuilt コミット（Q15） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.36 producer ツール crates/pjsip-prebuilt（Q13 / Q14 / Q16） (N0105) | → | ☆ §44 CI/CD & M20 Docker/Prebuilt Pipeline (N0054) |
| references | ★ 62.37 CI 運用と prebuilt コミット（Q15） (N0106) | → | ☆ §44 CI/CD & M20 Docker/Prebuilt Pipeline (N0054) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0105` | `src/build/prebuilt_producer.rs` |
| `N0106` | `src/build/prebuilt_ci_commit.rs` |

## Investigation

- **リポジトリ構造（確認済み）**: git root は `/Users/sh01/shyme/zasso`（モノレポ, remote t-kawata/zasso）。siprs クレートは `crates/siprs/`。repo root の `crates/` に anthropx / ggufrs / procreg / siprs / siprs-server / voiput が存在し、`crates/pjsip-prebuilt` は未作成。zasso root に `.github/` は無く、既存ワークフロー（ci.yml / integration.yml / prebuilt-refresh.yml）は `crates/siprs/.github/workflows/` にあり repo root の GitHub Actions からは認識されない。root に Cargo workspace は存在しない（§62.36 の「ワークスペース化しない」に整合）。
- **RFC §62.36（N0105, 検証済み）**: producer は独立クレート `crates/pjsip-prebuilt`（シェルスクリプト禁止）。CLI は `build <triple>` / `stage <triple>` / `verify <triple>`。ホスト OS 検出で §5.6 ターゲットセット決定。Linux-from-Mac はコミット済み Dockerfile を `docker build` → `docker run -v $(pwd)/vendor:/work/vendor` でマウントし、コンテナ内 cmake ビルド成果物をホスト volume に直接生成（Q14）。DoD 検証は file + nm（pjsua_init / pj_init）+ 最小 C リンクテストの 3 段階（Q16）。Dockerfile は ubuntu:22.04 + build-essential/cmake/libasound2-dev/libssl-dev/libuuid-dev。
- **RFC §62.37（N0106, 検証済み）**: `.github/workflows/prebuilt.yml` — 3 OS マトリクス（macos-latest / ubuntu-latest / windows-latest）、`on.push.branches: [master, siprs]` + `paths: [crates/siprs/vendor/pjsip/**, crates/pjsip-prebuilt/**]`、`cargo run --manifest-path crates/pjsip-prebuilt/Cargo.toml -- build-all` + `-- verify-all`、`stefanzweifel/git-auto-commit-action@v5` で vendor/prebuilt を直接コミット。paths / manifest-path は repo root 相対のため workflow は repo root に置く必要がある。
- **設計ブリーフ §5.1/§5.3/§5.6（検証済み）**: §5.1「clone 直後に prebuilt が vendor/prebuilt/<target-triple>/ に存在し追加取得手順なしで cargo build --features pjsua-native が通る」。§5.3 producer: 専用の独立 Rust ツール（シェルスクリプト禁止）、ホスト検出でターゲットセット決定、CMake（host: std::process::Command / Linux-from-Mac: Docker コンテナ）で build、`vendor/prebuilt/<target>/{include,lib}` へ stage、ライブラリが得られない場合は非ゼロ終了/panic（サイレントフォールバック禁止）。§5.6 マッピング: macOS PC → aarch64-apple-darwin / x86_64-apple-darwin（host）+ x86_64-unknown-linux-gnu（Docker Linux, 任意で aarch64-unknown-linux-gnu）、Windows PC → x86_64-pc-windows-msvc（`cmake -G Visual Studio 17 2022 -A x64`）、Linux PC → x86_64-unknown-linux-gnu（host cmake）。
- **ソース状態（確認済み）**: `vendor/prebuilt/aarch64-apple-darwin/{include,lib}` のみ存在（+ lib.bak, BUILD.md）。`vendor/pjsip/` は PJSIP 2.17.0 ソース + CMakeLists.txt（30 エントリ）。`build.rs` は P18-1 で 4 段階解決 + `SIPRS_STAGE_PREBUILT` staging を実装済み（consumer 側完了）。`src/build/prebuilt_producer.rs`（N0105）と `src/build/prebuilt_ci_commit.rs`（N0106）は `fn main() { /* TODO */ }` の design artifact スタブで、`mod.rs` 未登録（orphan）。grep で他の .rs から参照なし。`src/build/cicd_docker_prebuilt.rs`（P1-3, N0054）に CiOsTarget / FeatureCombination / PrebuiltRefreshPipeline の CI モデル enum が既存。
- **テスト実行パス（確認済み）**: siprs は Makefile 経由（`make test` = test-util, `make test-integration` = Docker + pjsua-native）。producer クレートは独立のため `cargo test --manifest-path crates/pjsip-prebuilt/Cargo.toml` で実行（Makefile ターゲット追加が望ましい — notes 参照）。bindgen 0.69.5, Rust edition 2021, rust-version 1.95。

## Acceptance Criteria

- [Happy path] crates/pjsip-prebuilt が単体で build → stage → verify を完走し `vendor/prebuilt/<target>/` を生成する（siprs コンパイル非依存の DoD, §5.3）。git clone 各 OS → prebuilt が存在する（§5.1）。CI の 3 OS マトリクスが build-all + verify-all を成功させ vendor/prebuilt を通常 push で直接コミットする（§62.37 Q15）。
- [Error case] cmake 失敗 / Docker 不可 / サポート外ホスト / 未知 triple / vendor ソース欠落 / 空 lib で build・stage・verify が非ゼロ終了し、サイレントフォールバックしない（§5.3）。wrong-arch の stage 成果物（ELF 期待に Mach-O）を verify が `VerifyFailure::WrongArchitecture` で検出して失敗する。
- [Edge case] macOS ホストで host + Docker Linux の両ターゲットを生成できる（Docker 利用可能時）。Windows は MSVC ネイティブのみ。`cargo build --features pjsua-native` が（Ticket A 完了後）prebuilt を使って通る（§5.5 DoD）。

## Invariants

- [Normal condition] producer は siprs クレートに依存しない（`crates/pjsip-prebuilt/Cargo.toml` に siprs への path 依存なし）。ホスト OS 検出 `target_set_for_host` は §5.6 のターゲットセットを返す（macOS→host + Docker Linux、Windows→x86_64-pc-windows-msvc、Linux→host）。
- [Error invariant] 任意の方法でライブラリが得られない場合（cmake 失敗 / Docker 不可 / サポート外ホスト / 未知 triple / vendor ソース欠落）は非ゼロ終了 / `Err` を返し、サイレントフォールバックや部分 stage をしない。stage は build 成功時のみ vendor へ書く。
- [Internal state invariant] `vendor/prebuilt/<triple>/{include,lib}` は常に整合状態にある: `include/pjsua.h` が存在し、`lib/` が空でない `.a`/`.lib`（libpjsua* または統合 libpjproject.a）を含む。verify は file + nm（pjsua_init / pj_init）+ 最小 C リンクテストの 3 段階をすべて通す。
- [Boundary invariant] §5.6 の全ターゲット（aarch64-apple-darwin / x86_64-apple-darwin / x86_64-unknown-linux-gnu / x86_64-pc-windows-msvc）がターゲットセット和集合に含まれる。wrong-arch（例: ELF を期待する triple に Mach-O）は verify が `VerifyFailure::WrongArchitecture` で検出して失敗する。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C143 — N0105→N0054

- **Precondition**: §44 prebuilt pipeline defined → 入力スキーマ: ホスト OS 検出 `HostOs::from_std(std::env::consts::OS)` が `macos`→`MacOs` / `windows`→`Windows` / `linux`→`Linux` / その他→`Unsupported` に正規化し、`target_set_for_host(&HostOs) -> Result<Vec<TargetTriple>, ProducerError>` が §5.6 のターゲットセット（macOS→host + Docker Linux / Windows→MSVC / Linux→host）を返す。testUnit 対応: [Normal] ホスト OS 検出 → ターゲットセット決定 / [Error] サポート外ホスト。
- **Postcondition**: crates/pjsip-prebuilt produces prebuilts → 出力アサーション: `build <triple>` + `stage <triple>` が `vendor/prebuilt/<triple>/{include,lib}` を生成し、`verify <triple>`（file + nm + 最小 C リンクテスト, §62.36 Q16）が exit 0。`validate_staged_layout` が `include/pjsua.h` 存在 + `lib/` に `libpjsua*`（または統合 `libpjproject.a`）を確認。testUnit 対応: [Normal] stage パス配置 / [Normal] verify の file/nm 検出 / [Error] verify 失敗パス / [Invariant] stage 成果物不変条件。
- **Invariant**: Producer independent of siprs compile → アサート可能述語: `crates/pjsip-prebuilt/Cargo.toml` に siprs への path 依存が無い + 全失敗（cmake / Docker / サポート外ホスト / 未知 triple）で非ゼロ exit / `Err` を返し、サイレントフォールバック禁止。testUnit 対応: [Invariant] producer は siprs 非依存 / [Error] build 失敗は非ゼロ終了。

### C144 — N0106→N0054

- **Precondition**: §44 CI/CD requirements → 入力スキーマ: `.github/workflows/prebuilt.yml` の `on.push.branches: [master, siprs]` + `paths: [crates/siprs/vendor/pjsip/**, crates/pjsip-prebuilt/**]`（repo root 相対）。producer クレートのテストが workflow YAML を読み構造を検証。testUnit 対応: [Invariant/C144] CI ワークフロー構造。
- **Postcondition**: 3-OS matrix commits prebuilt directly → 出力アサーション: `strategy.matrix.os` に `macos-latest` / `ubuntu-latest` / `windows-latest` が含まれ、各 runner が `cargo run --manifest-path crates/pjsip-prebuilt/Cargo.toml -- build-all` + `-- verify-all` を実行し、`stefanzweifel/git-auto-commit-action@v5` が `vendor/prebuilt` を通常 push で直接コミット（PR 儀式なし, §62.37 Q15）。testUnit 対応: [Invariant/C144] CI ワークフロー構造。
- **Invariant**: prebuilt present after git clone → アサート可能述語: 全 §5.6 ターゲットの stage パス `vendor/prebuilt/<triple>/{include,lib}` が整合状態（`validate_staged_layout` が `Ok`）。`target_set_for_host` の全ホスト出力の和集合が §5.6 の全ターゲット `{aarch64-apple-darwin, x86_64-apple-darwin, x86_64-unknown-linux-gnu, x86_64-pc-windows-msvc}` を包含する。testUnit 対応: [Invariant] ターゲットセット完全性 / [Invariant] stage 成果物不変条件。

## Boy Scout Rule

- 関数名を動詞句に: `build_for_target` / `stage_to_vendor` / `verify_staged` / `detect_host_triple` / `target_set_for_host` — main が「サブコマンドをパースし、ホスト検出でターゲットを決め、build→stage→verify を実行する」と読めるようにする。
- 変数名はドメイン概念に: `TargetTriple`（newtype）/ `HostOs`（enum）/ `prebuilt_root` / `lib_dir` / `include_dir`。生 `String` の分岐を排除し、§5.6 の全ホストを enum で網羅（曖昧な型と catch-all 禁止）。
- 一関数一責務: verify を `parse_file_output`（file 出力）/ `nm_output_has_pjsua_symbols`（nm シンボル判定）/ `run_c_link_test`（最小 C リンク）の 3 関数に分割。Docker コマンド構築は `docker_build_command` / `docker_run_command` に分離。
- ハードコード値は名前付き定数に: `PJMEDIA_WITH_VIDEO=OFF` / `SRTP_WITH_OPENSSL=OFF` / `CMAKE_BUILD_TYPE=Release` / Docker イメージ名 `pjsip-prebuilt` / `vendor/prebuilt` パス / §5.6 ターゲット triple 群を const として定義。
- エラー吞み込み禁止: 全サブコマンドは `Result<(), ProducerError>` を返し、`main` が exit コードに変換。`std::process::Command::status()` の非ゼロ exit は `?` で伝播し、`.ok()` で握りつぶさない。
- 既存コード改善（範囲外も含む）: `src/build/prebuilt_producer.rs` / `prebuilt_ci_commit.rs` の `fn main() { /* TODO */ }` スタブを、名詞的構造（host 検出 / ターゲットセット / CI モデル）の実装モジュールとして分割し `mod.rs` に登録。`vendor/prebuilt/BUILD.md` の手動手順コメントは producer ツール利用へ更新（P18-2 後は手動 stage 不要）。

## Test Plan

### Unit Tests

- UT: [Normal] ホスト OS 検出 → ターゲットセット決定（§5.6 マッピング, C143 Precondition 入力スキーマ）— `HostOs::from_std(std::env::consts::OS)` が `macos`→`MacOs` / `windows`→`Windows` / `linux`→`Linux` / その他→`Unsupported` に正規化。`target_set_for_host(&HostOs) -> Result<Vec<TargetTriple>, ProducerError>` が §5.6 のターゲットセットを返す: `MacOs`→`[detect_host_triple(), x86_64-unknown-linux-gnu]`（host + Docker Linux）、`Windows`→`[x86_64-pc-windows-msvc]`（MSVC）、`Linux`→`[x86_64-unknown-linux-gnu]`（host）。`TargetTriple` は newtype（生 String で分岐せず全ホストを enum 網羅）。
- UT: [Normal] CLI パース（§62.36 Q13 形状 + §62.37 build-all/verify-all）— `clap` で `build <triple>` / `build-all` / `stage <triple>` / `stage-all` / `verify <triple>` / `verify-all` を `Command` enum へパース。`build-all` は `target_set_for_host` を呼び各 triple に build→stage、`verify-all` はステージ済み全 triple を verify。未知サブコマンド・triple 欠落は clap Usage エラー（exit 2）。main.rs は `match Command::parse()` で分岐し、`build_for_target` / `stage_to_vendor` / `verify_staged` を呼ぶ（RFC §62.36 の CLI 形状どおり）。
- UT: [Normal] stage パス配置（C143 Postcondition 出力アサーション）— `stage_to_vendor(&TargetTriple, &build_dir, &prebuilt_root)` が `prebuilt_root/<triple>/{include,lib}` を作成し `include/pjsua.h` と `lib/libpjsua-lib.a`（または統合 `libpjproject.a`）を配置。`validate_staged_layout(&Path) -> Result<(), ProducerError>` が `include/pjsua.h` 存在 + `lib/` に `libpjsua*` または `libpjproject.a` が 1 つ以上あることを確認。
- UT: [Normal] verify の file/nm 検出（§62.36 Q16 3 段階 DoD）— `parse_file_output(&str) -> MachineKind` が `Mach-O 64-bit arm64 executable`→`MachO(Arm64)` / `ELF 64-bit LSB shared object, x86-64`→`Elf(X86_64)` / `PE32+ executable (console) x86-64`→`Pe(X86_64)` を識別。`nm_output_has_pjsua_symbols(&str) -> bool` が `pjsua_init` / `pj_init` を含む出力を検出。`verify_staged` は file + nm + C リンクテストの 3 段階すべて成功で `Ok(())`。
- UT: [Error] build 失敗は非ゼロ終了（C143 Invariant: サイレントフォールバック禁止）— cmake が非ゼロ exit のとき `build_for_target` は `Err(ProducerError::CmakeFailed { triple, status })` を返し、`main` は `std::process::exit(1)`。既知外の triple（例: `arm-unknown-linux-gnueabi`）は `Err(ProducerError::UnsupportedTriple)`。成功以外の全経路で vendor へ部分 stage しない。
- UT: [Error] verify 失敗パス — 空 `lib/` → `Err(ProducerError::StagedLibMissing)`、`file` 出力がターゲット期待アーキテクチャと不一致 → `Err(VerifyFailure::WrongArchitecture { expected, actual })`、`nm` に `pjsua_init` が無い → `Err(VerifyFailure::SymbolMissing)`。各失敗は非ゼロ exit に変換される。
- UT: [Error] サポート外ホスト — `target_set_for_host(&HostOs::Unsupported)` は `Err(ProducerError::UnsupportedHost)` を返す（空リストを返してサイレントに skip しない）。`HostOs::from_std(freebsd)` → `Unsupported` への正規化を assert。
- UT: [Boundary] Docker コマンド構築（§62.36 Q14 Linux-from-Mac）— `docker_build_command(&crate_dir)` が `[build, -t, pjsip-prebuilt, <crate_dir>]`、`docker_run_command(&vendor_abs)` が `[run, --rm, -v, <vendor_abs>:/work/vendor, pjsip-prebuilt, cmake, -S, /work/vendor/pjsip, -B, /work/vendor/build]` を返す（volume マウントでホスト vendor へ直接生成）。Windows ターゲットでは Docker を使用せず `cmake -G Visual Studio 17 2022 -A x64`（§5.6）。
- UT: [Boundary] ターゲット別 CMake フラグ（§28.3）— `cmake_build_command(&TargetTriple)` が必ず `-DPJMEDIA_WITH_VIDEO=OFF` を含み、`-DCMAKE_BUILD_TYPE=Release`、`-DSRTP_WITH_OPENSSL=OFF` を設定。`vendor/pjsip`（ソース）が無い場合は `Err(ProducerError::VendorSourceMissing)`。
- UT: [Invariant] producer は siprs 非依存（C143 Invariant）— `crates/pjsip-prebuilt/Cargo.toml` をパースし `[dependencies]` に `siprs` への path 依存が無いことを assert。`cargo metadata --no-deps --manifest-path crates/pjsip-prebuilt/Cargo.toml` の依存木に siprs が含まれない。
- UT: [Invariant] ターゲットセット完全性（C144 Invariant）— `target_set_for_host` の全ホスト出力の和集合が §5.6 の全ターゲット `{aarch64-apple-darwin, x86_64-apple-darwin, x86_64-unknown-linux-gnu, x86_64-pc-windows-msvc}` を包含する。
- UT: [Invariant/C144] CI ワークフロー構造 — producer クレートのテストが repo root の `.github/workflows/prebuilt.yml` を読み、`on.push.branches` に `master`/`siprs`、`strategy.matrix.os` に `macos-latest`/`ubuntu-latest`/`windows-latest`、step に `cargo run --manifest-path crates/pjsip-prebuilt/Cargo.toml -- build-all` / `-- verify-all` / `stefanzweifel/git-auto-commit-action@v5` が含まれることを assert（C144 Precondition 入力スキーマ + Postcondition 出力アサーション）。
- UT: [Invariant] stage 成果物不変条件（C143 Postcondition + C144 Invariant）— 任意の `vendor/prebuilt/<triple>/lib` は空でない `.a`/`.lib` を 1 つ以上含み、対応 `include/pjsua.h` を持つ。`validate_staged_layout` が空 lib を `Err(ProducerError::StagedLibMissing)` で拒否する。

### Integration Tests

- IT: [Integration point] crates/pjsip-prebuilt ↔ vendor/prebuilt ↔ build.rs — producer が `build-all` → `verify-all` をホストターゲットで完走し `vendor/prebuilt/<host-triple>/{include,lib}` を生成する（DoD: siprs コンパイル非依存）。検証コマンド: `cargo run --manifest-path crates/pjsip-prebuilt/Cargo.toml -- build-all && cargo run --manifest-path crates/pjsip-prebuilt/Cargo.toml -- verify-all` が exit 0。
- IT: [Verification] 最小 C リンクテスト（§62.36 Q16 第 3 段階）— verify サブコマンドが `pjsua_init()` を呼ぶ最小 C ソースを stage 済み `vendor/prebuilt/<triple>/lib` の静的ライブラリ群にリンクして実行し、exit 0 を確認（コンパイル・リンク・実行の全段階成功）。
- IT: [Prerequisites] ① `vendor/pjsip/`（PJSIP 2.17.0 ソース + CMakeLists.txt）、② CMake + ターゲット別システム依存（macOS frameworks / Linux `asound ssl crypto uuid pthread m dl rt` / Windows `ws2_32 ole32 userenv winmm iphlpapi crypt32`、RFC §28.4）、③ Docker（macOS ホストの Linux-from-Mac 時のみ）、④ 3 OS GitHub Actions ランナー（macos-latest / ubuntu-latest / windows-latest）。
- IT: [Related tickets] P18-1（consumer/bindgen alignment — 本 producer の成果物を消費する build.rs 4 段階解決、§62.35/N0104）、P1-4（既存 prebuilt-refresh.yml — macOS 手動 refresh パイプライン、§44/M20）、P1-3（§44 CI/CD マトリクス + Prebuilt Refresh Pipeline 定義、N0054）、P16-10（Docker/Asterisk 実 SIP 統合テスト基盤、N0088）。

### Exceptions

- Item: Docker による Linux-from-Mac ビルドは Docker 非搭載環境ではテスト不能（GitHub Actions の macOS ランナーに Docker が標準搭載されない）。Reason: Docker デーモンという外部インフラを必要とし、`cargo test` のサンドボックス内では not testable（外部依存のため）。これは設計欠陥ではない（not a design defect）— build 手順は決定的だが外部インフラ（Docker デーモン）依存のためユニットテストでは実行不可能。Alternative verification: `docker_build_command` / `docker_run_command` の引数構築を合成入力でユニットテスト検証し、Linux ターゲットは ubuntu ランナーのネイティブ cmake（§5.6 Linux PC 行）で CI 生成。
- Item: vendor/pjsip の実 CMake フルビルドは 5〜10 分かかり `make test` の通常テストではテスト不能（時間コスト）。Reason: 長時間ビルドであり、決定的だが外部ビルドツール cmake への依存と時間コストのため単体テストから除外する。これは設計欠陥ではない（not an architectural defect）— build ロジック（コマンド構築・成功/失敗判定）は純粋関数としてユニットテストで検証可能で、実ビルドは CI の prebuilt.yml で実行される（E2E/CI での検証）。Alternative verification: CI の prebuilt.yml で build-all + verify-all を実行 + コマンド構築ロジックのユニットテスト。
- Item: 他 OS ターゲットの実 verify（例: macOS ホストで Windows PE を file/nm/C リンク検証）はクロス環境が必要で単一ホストではテスト不能。Reason: file/nm/C リンクテストはホストのツールチェーンとターゲットが一致する場合のみ実行可能（外部ツールチェーン依存）。これは設計欠陥ではない（not a design defect）— 各ターゲットの verify は対応する CI ランナー（macos/ubuntu/windows）が担当する 3 OS マトリクスによる責務分担。Alternative verification: 3 OS CI マトリクス + `parse_file_output` / `nm_output_has_pjsua_symbols` の合成出力によるユニットテスト。

### Plan Test Code (concrete code)

- UT: [Normal/C143-pre] ホスト OS 検出 → ターゲットセット決定（§5.6）のテスト入力コード — `HostOs::from_std` 正規化と `target_set_for_host` の入力を具体値で構築。
```rust
#[test]
fn target_set_for_host_maps_per_design_brief_s5_6() {
    // C143 Precondition: host OS detection produces the §5.6 target set.
    let macos_set = target_set_for_host(&HostOs::MacOs).unwrap();
    assert!(macos_set.contains(&TargetTriple::from("x86_64-unknown-linux-gnu")));
    let host = detect_host_triple().unwrap();
    assert!(macos_set.contains(&host));

    let windows_set = target_set_for_host(&HostOs::Windows).unwrap();
    assert_eq!(windows_set, vec![TargetTriple::from("x86_64-pc-windows-msvc")]);

    let linux_set = target_set_for_host(&HostOs::Linux).unwrap();
    assert!(linux_set.contains(&TargetTriple::from("x86_64-unknown-linux-gnu")));
}
```
- UT: [Error/C143-pre] サポート外ホストのテスト入力コード — `HostOs::Unsupported` を入力し `Err` を検証（fail-fast）。
```rust
#[test]
fn unsupported_host_errors_out() {
    // C143 Precondition error path: unsupported host fails, never silently skips.
    let result = target_set_for_host(&HostOs::Unsupported);
    assert!(matches!(result, Err(ProducerError::UnsupportedHost)));
    assert_eq!(HostOs::from_std("freebsd"), HostOs::Unsupported);
}
```
- UT: [Normal/C143-post] stage パス配置 + `validate_staged_layout` の出力アサーションコード — 一時ディレクトリに include/lib を配置して stage 後の不変条件を検証。
```rust
#[test]
fn stage_to_vendor_creates_layout_and_validate_passes() {
    // C143 Postcondition: stage produces vendor/prebuilt/<triple>/{include,lib}.
    let prebuilt_root = std::env::temp_dir().join("p18-2-c143-stage");
    let triple = TargetTriple::from("aarch64-apple-darwin");
    let build_dir = std::env::temp_dir().join("p18-2-c143-build");
    std::fs::create_dir_all(build_dir.join("include")).unwrap();
    std::fs::write(build_dir.join("include/pjsua.h"), b"").unwrap();
    std::fs::create_dir_all(build_dir.join("lib")).unwrap();
    std::fs::write(build_dir.join("lib/libpjsua-lib.a"), b"").unwrap();

    stage_to_vendor(&triple, &build_dir, &prebuilt_root).unwrap();

    let staged_dir = prebuilt_root.join(&triple.0);
    assert!(staged_dir.join("lib/libpjsua-lib.a").exists());
    assert!(staged_dir.join("include/pjsua.h").exists());
    assert!(validate_staged_layout(&staged_dir).is_ok());
}
```
- UT: [Normal/C143-post] verify 3 段階（file + nm + C リンク）の出力アサーションコード — 合成 file/nm 出力で `verify_staged` の成功を検証。
```rust
#[test]
fn parse_file_output_detects_machine_kinds() {
    // C143 Postcondition: file output parsing for the 3 target machine formats.
    assert_eq!(parse_file_output("Mach-O 64-bit arm64 executable"), MachineKind::MachO(Arm64));
    assert_eq!(parse_file_output("ELF 64-bit LSB shared object, x86-64"), MachineKind::Elf(X86_64));
    assert_eq!(parse_file_output("PE32+ executable (console) x86-64"), MachineKind::Pe(X86_64));
}

#[test]
fn nm_output_has_pjsua_symbols() {
    // C143 Postcondition: nm output contains pjsua_init / pj_init.
    assert!(nm_output_has_pjsua_symbols("0000000000001234 T _pjsua_init"));
    assert!(nm_output_has_pjsua_symbols("0000000000001234 T _pj_init"));
    assert!(!nm_output_has_pjsua_symbols("0000000000001234 T _pj_strdup"));
}
```
- UT: [Error/C143-inv] build 失敗は非ゼロ終了（サイレントフォールバック禁止）の述語コード。
```rust
#[test]
fn build_failure_returns_err_no_silent_fallback() {
    // C143 Invariant: fail-stop, no silent fallback.
    let result = build_for_target(&TargetTriple::from("x86_64-unknown-linux-gnu"), CmakeProbe::failing());
    assert!(matches!(result, Err(ProducerError::CmakeFailed { .. })));
    let result = build_for_target(&TargetTriple::from("arm-unknown-linux-gnueabi"), CmakeProbe::ok());
    assert!(matches!(result, Err(ProducerError::UnsupportedTriple)));
}
```
- UT: [Invariant/C143-inv] producer は siprs 非依存 — Cargo.toml パースで path 依存を検証する述語コード。
```rust
#[test]
fn producer_manifest_has_no_siprs_dependency() {
    // C143 Invariant: producer is independent of siprs compile.
    let manifest = std::fs::read_to_string(concat!(env!("CARGO_MANIFEST_DIR"), "/Cargo.toml")).unwrap();
    assert!(!manifest.contains("siprs"), "producer must not depend on siprs");
}
```
- UT: [Normal/C144-pre] CI ワークフロー構造（trigger paths + branches）のテスト入力コード — `.github/workflows/prebuilt.yml` を文字列として読み substring 検証（YAML 依存なし）。
```rust
#[test]
fn prebuilt_workflow_triggers_on_push_with_paths() {
    // C144 Precondition: workflow triggers on push to master/siprs with the two path globs.
    let yaml = std::fs::read_to_string(
        concat!(env!("CARGO_MANIFEST_DIR"), "/../../.github/workflows/prebuilt.yml")
    ).unwrap();
    assert!(yaml.contains("branches: [master, siprs]"));
    assert!(yaml.contains("crates/siprs/vendor/pjsip/**"));
    assert!(yaml.contains("crates/pjsip-prebuilt/**"));
}
```
- UT: [Normal/C144-post] 3 OS マトリクス + build-all/verify-all + git-auto-commit の出力アサーションコード — workflow YAML の substring 検証。
```rust
#[test]
fn prebuilt_workflow_runs_3os_matrix_build_verify_commit() {
    // C144 Postcondition: 3-OS matrix runs build-all + verify-all and commits directly.
    let yaml = std::fs::read_to_string(
        concat!(env!("CARGO_MANIFEST_DIR"), "/../../.github/workflows/prebuilt.yml")
    ).unwrap();
    assert!(yaml.contains("macos-latest"));
    assert!(yaml.contains("ubuntu-latest"));
    assert!(yaml.contains("windows-latest"));
    assert!(yaml.contains("-- build-all"));
    assert!(yaml.contains("-- verify-all"));
    assert!(yaml.contains("git-auto-commit-action@v5"));
}
```
- UT: [Invariant/C144-inv] ターゲットセット完全性 + stage レイアウト不変条件の述語コード。
```rust
#[test]
fn all_s5_6_targets_covered_by_host_mapping() {
    // C144 Invariant: after git clone every §5.6 target is staged.
    let all: std::collections::BTreeSet<_> = [HostOs::MacOs, HostOs::Windows, HostOs::Linux]
        .iter()
        .flat_map(|h| target_set_for_host(h).unwrap())
        .collect();
    for required in ["aarch64-apple-darwin", "x86_64-apple-darwin",
                     "x86_64-unknown-linux-gnu", "x86_64-pc-windows-msvc"] {
        assert!(all.contains(&TargetTriple::from(required)), "missing {required}");
    }
}

#[test]
fn staged_layout_invariant_rejects_empty_lib() {
    // C144 Invariant: a staged <triple>/{include,lib} with an empty lib/ is invalid.
    let staged = std::env::temp_dir().join("p18-2-c144-empty");
    std::fs::create_dir_all(staged.join("include")).unwrap();
    std::fs::create_dir_all(staged.join("lib")).unwrap();
    std::fs::write(staged.join("include/pjsua.h"), b"").unwrap();
    assert!(validate_staged_layout(&staged).is_err());
}
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| vendor/prebuilt/ は aarch64-apple-darwin のみ（手動 stage） | crates/pjsip-prebuilt が全ターゲットの prebuilt を生成・stage・検証し CI がコミット | producer ツール + CI（§62.36–62.37） |
| shell スクリプト等による非再現性ビルド | 独立クレートの CMake/Docker ビルドで再現性を保証 | producer の独立性（§62.36） |
| crates/pjsip-prebuilt/ does not exist | standalone dependency-free producer crate (Cargo.toml + src/lib.rs + src/main.rs + Dockerfile) with 19 unit tests | P18-2 §62.36: build/stage/verify CLI + host detection + verify predicates |
| .github/workflows/ has no prebuilt producer pipeline | .github/workflows/prebuilt.yml at repo root runs 3-OS matrix build-all + verify-all and commits vendor/prebuilt | P18-2 §62.37: 3-OS matrix + git-auto-commit |
| src/build/prebuilt_producer.rs and prebuilt_ci_commit.rs are fn main(){} STUBs (TS-001/TS-002) | both implemented as design-model modules (ProducerHost/ProducerTriple/verify predicates, 3-OS CI model) and registered in src/build/mod.rs; STUBs marked resolved | P18-2 STUB resolution: N0105 producer design model + N0106 CI commit design model |

## Notes in Prior Implementation Rounds

- [Implementation steps] ① `crates/pjsip-prebuilt/` クレート雛形（Cargo.toml + src/lib.rs + src/main.rs）作成。② host 検出/ターゲットセット（lib.rs）、CLI パース（main.rs, clap）、cmake/docker コマンド構築、stage、verify を TDD（Red→Green→Refactor）で実装。③ Dockerfile 作成。④ `.github/workflows/prebuilt.yml`（repo root）作成。⑤ `src/build/prebuilt_producer.rs` / `prebuilt_ci_commit.rs` の STUB 解決 + `src/build/mod.rs` 登録。⑥ `cargo test --manifest-path crates/pjsip-prebuilt/Cargo.toml` + `make test` で green 確認。
- [Risks] ① GitHub Actions の macOS ランナーに Docker が標準搭載されない → Linux-from-Mac が CI で失敗する可能性。回避: ubuntu ランナーが Linux ターゲットをネイティブ生成（§5.6 Linux PC 行）し、macOS ランナーの Docker パスはローカル開発専用とする、または macOS ランナーに Docker セットアップ step を追加。② Intel Mac（x86_64-apple-darwin）を GitHub Actions で生成するにはクロス環境/Rosetta が必要（macos-latest は arm64 ホスト）。③ 既存 `prebuilt-refresh.yml` との関係整理。
- [Caveats] workflow の `paths` / `--manifest-path` は repo root 相対（`crates/siprs/vendor/pjsip/**`, `crates/pjsip-prebuilt/Cargo.toml`）。producer クレートのテストは siprs の Makefile 対象外のため `cargo test --manifest-path crates/pjsip-prebuilt/Cargo.toml` で実行する — Makefile に `make prebuilt-test` 相当の委譲ターゲット追加が望ましい。既存 workflows が `crates/siprs/.github/workflows/` にあり root から未認識 — 新 prebuilt.yml は root に置く。
- [Open items] ① macOS ランナーでの Docker 利用可否とセットアップ方法。② `x86_64-apple-darwin`（Intel）の生成方法（クロス vs Rosetta vs 対象外）。③ 新 prebuilt.yml が既存 `prebuilt-refresh.yml` を置き換えるか併存か。④ `vendor/prebuilt/<triple>/lib.bak` の整理。⑤ `src/build/prebuilt_producer.rs` / `prebuilt_ci_commit.rs` と producer クレートのロジック共有範囲（重複最小化）。
- [Future improvements] Makefile に `make prebuilt` / `make prebuilt-verify` ターゲット追加。`build-all` に Docker 利用可否の自動検出（Docker 非搭載時はホストターゲットのみ生成し Linux は ubuntu runner へ委譲）。stage 成果物の整合性を検証する `vendor/prebuilt/BUILD.md` の自動更新。
Implementation summary:
- New crate crates/pjsip-prebuilt (zero crates.io deps; CLI parsed manually to stay offline-buildable): src/lib.rs holds HostOs/TargetTriple/MachineKind/ProducerError/VerifyFailure, target_set_for_host(§5.6), cmake/docker command builders, stage_to_vendor + validate_staged_layout, parse_file_output/nm_output_has_pjsua_symbols/verify_staged, build_for_target_with (injectable runner), parse_command. src/main.rs dispatches build/build-all/stage/stage-all/verify/verify-all. Dockerfile is ubuntu:22.04 + build-essential/cmake/libasound2-dev/libssl-dev/libuuid-dev.
- .github/workflows/prebuilt.yml added at the zasso repo root (GitHub Actions only scans repo-root workflows): on.push [master,siprs] paths crates/siprs/vendor/pjsip/** + crates/pjsip-prebuilt/**, matrix macos-latest/ubuntu-latest/windows-latest, cargo run --manifest-path crates/pjsip-prebuilt/Cargo.toml -- build-all + verify-all, stefanzweifel/git-auto-commit-action@v5 commits vendor/prebuilt.
- Resolved TS-001/TS-002: src/build/prebuilt_producer.rs (N0105) and src/build/prebuilt_ci_commit.rs (N0106) now implement the testable pure design model and are registered in src/build/mod.rs with [::TICKET::] P18-2 provenance.
- Verification: cargo test --manifest-path crates/pjsip-prebuilt/Cargo.toml (19 passed); make test (1367 lib + integration all passed); run-quality-checks 0 issues.
- Note: ffi::callback::tests::register_callbacks_installs_raw_sip_queue is flaky under parallel cargo test (pre-existing global RAW_SIP_QUEUE race); passes in isolation and on re-run. Not caused by P18-2.
Review report:
- Step 3: scan-crimes 0 open. Step 4: 8 remaining STUBs all reference P19-* future tickets (correctly deferred); P18-2 TS-001/TS-002 resolved. no-excuse gate passed.
- Step 5: annotation verify 3 files OK, 0 AMBIGUOUS. 7-pattern scan clean (only test-code panics/unwrap).
- Step 6: make test all green; producer cargo test 20 green; zero warnings.
- Step 7 completeness: C143 (producer produces prebuilts, siprs-independent) and C144 (3-OS CI matrix commits directly) satisfied; STUBs resolved; design artifact headers preserved.
- Step 8 quality: 0 issues. Step 9 translatability: no issues.
- Review fixes applied: (1) removed weak verify smoke test with error-swallowing match arms; (2) refactored verify C link test to CMake (cross-platform compiler selection, LINK_GROUP:RESCAN for Linux static circular deps, handles MSVC cl); (3) added MSVC .lib naming support to validate_staged_layout and link_test_lib_stems (pjsua-lib.lib accepted alongside libpjsua-lib.a).
- Known limitations (documented, not blockers): (a) verify file/nm stages are Unix-tool oriented — on Windows CI the cmake C link test is the cross-platform core, but nm/file need MSYS2 tools or equivalents; first Windows CI run may need a tooling setup step. (b) GitHub macOS runners lack Docker by default — Linux-from-Mac is primarily a local-dev path; the ubuntu runner produces the Linux target natively. (c) x86_64-apple-darwin is not produced by the current CI matrix (macos-latest is arm64); Intel-Mac users fall back to the consumer source-build path (build.rs 4-stage).

## P18-2 — implemented at 11 locations

### src/build/mod.rs

- Line 30
```rust
pub mod prebuilt_producer;
```

- Line 32
```rust
pub mod prebuilt_ci_commit;
```

### src/build/prebuilt_ci_commit.rs

- Line 27
```rust

```

- Line 71
```rust
    fn repo_root() -> std::path::PathBuf {
```

- Line 77
```rust
    fn workflow_constants_match_design() {
```

- Line 89
```rust
    fn committed_workflow_satisfies_s62_37() -> Result<(), String> {
```

### src/build/prebuilt_producer.rs

- Line 28
```rust

```

- Line 39
```rust
impl ProducerHost {
```

- Line 127
```rust
    fn target_set_follows_design_brief_s5_6() -> Result<(), &'static str> {
```

- Line 146
```rust
    fn file_and_nm_predicates_detect_expected_formats() {
```

- Line 158
```rust
    fn staged_layout_invariant_rejects_incomplete_stage() -> Result<(), std::io::Error> {
```
