# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P19-1: H8: raw SIP 実 PJSIP 検証経路（§62.38）

**Ticket Key**: P19-1 · **Phase**: 19

**RFC Source**: `RFC-ROOT.md`

---

## Background

H8 RESIDUE は「subscribe_raw_sip() の受信コードは成立しない」と断言。実 pjsua-native 経路の完全な統合テストで固定する（ユーザー判断: ダミー実装禁止）。

### Goal
pjsua-native ビルドで subscribe_raw_sip() が実 SIP メッセージを受信できることを、実 PJSIP 経路（pjsip_module フック → enqueue_raw_sip_bytes → RAW_SIP_QUEUE → drain_and_publish_raw_sip → subscribe_raw_sip）の完全な統合テストで証明する（§62.38 / Q17）。

### Purpose
H8 RESIDUE が指摘する「subscribe_raw_sip() の受信コードは成立しない」という懸念を、テスト可能な実証へと昇格させる。src/ffi/raw_sip_module.rs（P17-2）で実装済みの観測専用フックと、src/ffi/callback.rs の RAW_SIP_QUEUE、src/runtime/event_path_wiring.rs の publisher が、実際の SIP トランザクション（Asterisk への REGISTER とその応答）で一体として機能することを、単体テストではなく実環境の統合テストで固定する。

### Motivation
- H8 は「テスト専用フック（ダミー実装）で隠蔽された受信経路」を禁止し、実 pjsua-native 経路での検証を要求している（ユーザー判断）。
- P17-2 で生産経路（raw_sip_module.rs 登録）は実装済みだが、実 SIP で受信を確認する統合テストが未整備（N0107 の存在理由）。
- P16-10 の Docker/Asterisk 統合テスト基盤（N0088）が利用可能になったため、実環境での検証が実行可能。

### Constraints
- TestBackend のテスト専用フック等のダミー実装は禁止（Q17）。検証は実 pjsua-native 経路のみ。
- pjsip_module フックは観測専用（PJ_FALSE 返却）を維持し、メッセージを消費・改変しない。
- 実統合テストは tests/sip_integration.rs に #![cfg(feature = "pjsua-native")] ゲート付きで配置（Q9a / N0088）。
- make test-integration（compose up → cargo test --features pjsua-native --test sip_integration → compose down）をゲートとする。
- TestBackend / 既定ビルドの subscribe_raw_sip() が無音チャネルを返す事実は仕様として維持。
- RawSipMessage に raw フィールドは存在しない（RFC 擬似コードの msg.raw は実 API と不一致）。text() 等のアクセサで検証する。

## Scope

- 【変更1-path】src/tests/mod.rs — N0107 テストモジュールの宣言追加。
- 【変更1-action】modify: pub mod raw_sip_real_test; を追加し、src/tests/raw_sip_real_test.rs（現スタブ）をコンパイル対象へ組み込む。
- 【変更1-detail】現状 src/tests/mod.rs は m20_test_dual_client / docker_asterisk_it / test_strategy_4layer / test_apilayer5 のみ宣言。raw_sip_real_test を追加しない限り [::STUB::] P19-1 のスタブファイルは lib に含まれず、検証も不能。
- 【変更2-path】src/tests/raw_sip_real_test.rs — N0107 実装ターゲット（Implementation Target File）。
- 【変更2-action】modify（スタブ置換）: Initial Design Artifact ヘッダー（N0107）を保持したまま、test_example スタブと末尾の [::STUB::] P19-1 マーカーを除去し、§62.38 決定をテスト可能データとしてエンコードする RawSipRealTestPolicy（test_file/test_name/make_target/feature_gate/skip_message/register_call_site/module_file/docker_gate/production_path/observation_only）と、C145/C146 の契約テスト群（既定ビルドで実行可能）を実装。
- 【変更2-detail】docker_asterisk_it.rs（N0088）のパターンを踏襲: 実 SIP テスト本体は tests/sip_integration.rs に置き、本モジュールは「決定のエンコード + 静的契約検証」を既定ビルド（make test）で担う。constants: SKIP_MESSAGE 再利用、RAW_SIP_RX_TEST="raw_sip_rx_reaches_subscriber"、MAKE_TARGET="test-integration"、REGISTER_CALL_SITE="src/ffi/backend_calls.rs"、OBSERVATION_ONLY="PJ_FALSE"、PRODUCTION_PATH_STEPS=[pjsip_module hook, enqueue_raw_sip_bytes, drain_and_publish_raw_sip, subscribe_raw_sip]。
- 【変更3-path】tests/sip_integration.rs — 実 raw SIP 受信の統合テスト追加（スコープ記載の主変更）。
- 【変更3-action】add: raw_sip_rx_reaches_subscriber 統合テスト + wait_for_raw_sip ヘルパー + SipMessageDirection 等の import 追加。
- 【変更3-detail】手順: (1) docker_available() ゲート（不可時 SKIP_MESSAGE を残して早期 return）→ (2) client_against_asterisk() で実 PjsuaBackend 起動 → (3) subscribe_raw_sip() を先に取得（.expect("raw sip enabled")）→ (4) registered_account("sip:1001@127.0.0.1:5060", "password") で add_account → (5) wait_for_event で RegistrationStateChanged::Registered を待機 → (6) wait_for_raw_sip で REGISTER を含む RawSipMessage を 30s タイムアウト内に受信 → (7) assert msg.text().contains("REGISTER") かつ msg.direction==SipMessageDirection::Response → (8) client.shutdown()。TestBackend のテスト専用フックは一切使用しない。
- 【変更3-before-after】Before: sip_integration.rs は register_against_asterisk / outgoing_call_to_asterisk / incoming_call_via_originate / coturn_stun_turn_ice の 4 テストのみ。After: raw_sip_rx_reaches_subscriber が追加され、実 PJSIP 経路での subscribe_raw_sip() 受信が検証される。
- 【変更3-api】API 変更なし。subscribe_raw_sip() -> Option<Subscription<RawSipMessage>>（client.rs:211）、RawSipMessage のアクセサ（text()/start_line()/headers()/direction）は既存のまま。
- 【変更3-schema】データスキーマ変更なし。RawSipMessage のフィールド（direction/transport/start_line/headers/body/text/content_length/remote_addr/local_addr）は不変。
- 【変更3-config】設定変更なし。RawSipEventConfig 既定値（enabled=true, include_bodies=true, max_body_bytes=64KiB, redact_authorization=true）を利用。docker-compose.yml は既存 asterisk サービスを再利用（変更しない）。
- 【変更3-dep】依存追加なし。tokio（time/broadcast）、crossbeam-queue は既存。Cargo.toml [[test]] sip_integration（required-features=[pjsua-native]）は既存。
- 【変更4-path】src/tests/docker_asterisk_it.rs — DockerItPolicy の統合テストマトリクス更新（P16-10 成果物への整合変更）。
- 【変更4-action】modify: DockerItPolicy::policy().integration_tests に raw_sip_rx_reaches_subscriber（direction=Outbound, description="REGISTER response reaches subscribe_raw_sip() via real PJSIP hook"）を追加し、C116-post の assert_eq!(policy.integration_tests.len(), 4) を 5 に更新。
- 【変更4-detail】コメントはコードの一部（Everything as Code）。docker 統合テストマトリクスを正確に保つため、追加テストをエンコードしないと C116-post が不整合になる。
- 【非変更-item】production 実装（src/ffi/raw_sip_module.rs, src/ffi/callback.rs, src/ffi/backend_calls.rs, src/runtime/event_path_wiring.rs, src/client.rs, src/api/eventbus_receiver.rs）は P17-2/P16-4/P3-1 で実装済みのため変更しない。TestBackend / 既定ビルドの subscribe_raw_sip() 無音チャネル仕様も変更しない。docker-compose.yml / Makefile test-integration ターゲット / Cargo.toml features も変更しない。
- 【非変更-why】Q17 は「検証を実 PJSIP 経路で行う」ことのみ要求し、生産経路の再実装やダミー追加を求めない。生産経路は既に配線済み（backend_calls.rs:84）で、本チケットの責務は実環境での固定検証。
- 【影響-component】reactor の raw_sip publisher（spawn_raw_sip_publisher, event_path_wiring.rs:118）、EventBus::publish_raw_sip / subscribe_raw_sip（eventbus_receiver.rs:79,94）、Makefile test-integration、DockerItPolicy（docker_asterisk_it.rs の integration_tests 数 4→5）、sip_integration.rs の既存 4 テスト。
- 【影響-nature】テスト追加のみでランタイム挙動・API サーフェス・データ形式は不変。CI（make test-integration）のテスト数が 1 増える。
- 【影響-response】C116-post の DockerItPolicy::policy() に関するアサーション（integration_tests.len()==4 等）を 5 に更新（変更4）。既存テストへの回帰は生じない想定だが、追加後に make test / make test-integration で確認する。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0107`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0107` | test_policy | rust | 62.38 raw SIP 実 PJSIP 検証経路（Q17） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.38 raw SIP 実 PJSIP 検証経路（Q17） (N0107) | → | ☆ 62.22 raw SIP 生産経路: pjsip_module フックによる配線（Q1 / Q1a） (N0091) |
| references | ★ 62.38 raw SIP 実 PJSIP 検証経路（Q17） (N0107) | → | ☆ 62.19 Docker/Asterisk 実 SIP 統合テスト基盤 (N0088) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0107` | `src/tests/raw_sip_real_test.rs` |

## Investigation

- Production path fully wired (P17-2): src/ffi/backend_calls.rs:78-86 — after pjsua_init, pjsua_get_pjsip_endpt() → raw_sip_module::register(endpt); failure aborts startup (C111). src/ffi/raw_sip_module.rs — static RAW_SIP_MODULE (name mod_siprs_raw_sip, priority 63 = PJSIP_MOD_PRIORITY_APPLICATION-1, id -1) with on_rx_request/on_rx_response → capture_raw_sip_message reads pkt_info.packet[0..len] → enqueue_raw_sip_bytes(raw.to_vec()), returns PJ_FALSE_BOOL=0 (observation-only). src/ffi/callback.rs:62,73,77,145,163,178 — RAW_SIP_QUEUE_CAPACITY=64, AtomicPtr<ArrayQueue<Vec<u8>>>, RAW_SIP_DROPPED, install_raw_sip_queue, enqueue_raw_sip_bytes (loss-tolerant drop), try_pop_raw_sip_bytes. src/runtime/event_path_wiring.rs:36,89,118,125 — RAW_SIP_DRAIN_POLL_MS=5, drain_and_publish_raw_sip (parse via RawSipMessage::parse_with_config, malformed dropped with warn), spawn_raw_sip_publisher gated on RawSipEventConfig.enabled. src/client.rs:211 subscribe_raw_sip() -> Option<Subscription<RawSipMessage>>; src/api/eventbus_receiver.rs:79,94 EventBus::subscribe_raw_sip/publish_raw_sip.\n- Types: RawSipMessage (src/model/raw_sip_message_spec.rs:44) has NO raw field — fields direction/transport/start_line/headers/body/text/content_length/remote_addr/local_addr; accessors text()/start_line()/headers()/header()/body(); exported via siprs::model::{RawSipMessage, SipMessageDirection}. Subscription<T>::recv(&mut self) -> Result<T, broadcast::error::RecvError> (eventbus_receiver.rs:210). RawSipEventConfig default: enabled=true, include_bodies=true, max_body_bytes=64KiB, redact_authorization=true (client_config_spec.rs:93-111).\n- Integration base (N0088/P16-10): src/tests/docker_asterisk_it.rs — SKIP_MESSAGE, DOCKER_COMPOSE_FILE, SIP_INTEGRATION_TEST, MAKE_TARGET=test-integration, ASTERISK_IMAGE=asterisk:20.6.0, ASTERISK_SIP_PORT=5060, IT_EVENT_TIMEOUT=30s, DockerItPolicy::policy() (integration_tests.len()==4 currently), docker_available() gate. tests/sip_integration.rs — #![cfg(feature="pjsua-native")], helpers registered_account/split_sip_uri/wait_for_event/client_against_asterisk (ephemeral UDP port 0, registers to sip:1001@127.0.0.1:5060)/originate_call_to. docker-compose.yml — asterisk:20.6.0 on 5060/5061 with host.docker.internal; coturn. Makefile test-integration: docker compose up -d → trap down → cargo test --features pjsua-native --test sip_integration. Cargo.toml [[test]] sip_integration required-features=["pjsua-native"].\n- Stub state: src/tests/raw_sip_real_test.rs holds Initial Design Artifact header (N0107) + #[cfg(test)] mod tests { test_example } + // TODO: [::STUB::] P19-1 marker; src/tests/mod.rs does NOT declare raw_sip_real_test yet. Sibling src/tests/real_pjsip_itest.rs is a stub for P19-5 (N0111).\n- Key design decision: real test goes in tests/sip_integration.rs (Q9a pjsua-native gate); src/tests/raw_sip_real_test.rs encodes the decision as testable data + C145/C146 contract tests in the default build (mirrors N0088/docker_asterisk_it.rs). Adding the 5th docker integration test requires updating DockerItPolicy.integration_tests and the C116-post len()==4 assertion to 5.

## Acceptance Criteria

- [Happy path] make test（既定ビルド）で raw_sip_real_test の C145/C146 契約テストが green。make test-integration（docker compose up → cargo test --features pjsua-native --test sip_integration → compose down）で raw_sip_rx_reaches_subscriber が green になり、subscribe_raw_sip() が実 PJSIP 経路で REGISTER を含む RawSipMessage を受信できる（msg.text().contains("REGISTER") かつ direction==Response）。
- [Error case] docker デーモン不可 / compose 未起動時、テストは [SKIPPED: docker unavailable] を明示出力して早期 return（ハングしない）。30s タイムアウト（EVENT_TIMEOUT / wait_for_raw_sip）で panic する。REGISTER 応答が来ない等の失敗時はクリーンに shutdown() してスタックを破棄。
- [Edge case] raw チャネルには 401/200 等の複数メッセージが流れるため、REGISTER を含むメッセージまでループする（順序非依存）。parse 失敗メッセージは publisher が drop しバスに載らない。RAW_SIP_QUEUE 溢れ（capacity 64）は drop + カウンタで許容されトランスポートスレッドをブロックしない。pjsip_module フックは観測専用（PJ_FALSE）のままメッセージを消費しない。

## Invariants

raw SIP 検証は実 pjsua-native 経路のみで行う（TestBackend のダミーフック禁止）。
pjsip_module フックは観測専用（PJ_FALSE 返却）を維持する。

- [Normal condition] 実 pjsua-native 起動時、backend_calls::initialize（backend_calls.rs:78-86）が pjsua_get_pjsip_endpt 経由で raw_sip_module::register を成功させ、pjsip_module フック（on_rx_request/on_rx_response）→ enqueue_raw_sip_bytes → RAW_SIP_QUEUE → drain_and_publish_raw_sip（5ms ポーリング）→ EventBus::publish_raw_sip → subscribe_raw_sip() の一連が生きた状態で subscribe_raw_sip() が Some を返す。
- [Error invariant] フックは観測専用のまま（PJ_FALSE 返却）で、メッセージを消費しない。queue 満杯時は drop + RAW_SIP_DROPPED++ で許容し、トランスポートスレッドをブロック・パニックさせない。raw_sip_events.enabled=false 時は publisher 非起動のため無音チャネル（仕様）。
- [Internal state invariant] RAW_SIP_QUEUE は capacity=64 の crossbeam ArrayQueue（callback.rs:62）で、install_raw_sip_queue により initialize 前に常に設置済み。enqueue は drop でも決して panic しない。
- [Boundary invariant] 検証は make test-integration ゲート（compose up → pjsua-native test → compose down）でのみ成立。TestBackend / 既定ビルドの subscribe_raw_sip() は無音チャネルのまま（仕様）。CI では docker 常時可用のため実質必須ゲート。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C145 — N0107→N0091

- **Precondition**: N0091 defines the pjsip_module hook: src/ffi/raw_sip_module.rs exists with the static RAW_SIP_MODULE (on_rx_request/on_rx_response handlers capturing pkt_info.packet[0..len] → enqueue_raw_sip_bytes, returning PJ_FALSE) and register(endpt); backend_calls::initialize registers it after pjsua_init (backend_calls.rs:78-86, P17-2).
- **Postcondition**: The real integration test raw_sip_rx_reaches_subscriber in tests/sip_integration.rs verifies subscribe_raw_sip(): a real SIP message (Asterisk REGISTER response) traverses hook → enqueue_raw_sip_bytes → RAW_SIP_QUEUE → drain_and_publish_raw_sip → EventBus::publish_raw_sip → subscribe_raw_sip() and is received on the Subscription<RawSipMessage> channel.
- **Invariant**: No TestBackend dummy hooks: verification is real-pjsua-native only (SipClient::new under #![cfg(feature = "pjsua-native")]); the pjsip_module hook stays observation-only (PJ_FALSE) and never consumes the message.

### C146 — N0107→N0088

- **Precondition**: N0088 defines the docker integration base: src/tests/docker_asterisk_it.rs provides docker_available(), SKIP_MESSAGE="[SKIPPED: docker unavailable]", DockerItPolicy, IT_EVENT_TIMEOUT=30s; tests/sip_integration.rs is gated by #![cfg(feature = "pjsua-native")]; docker-compose.yml defines asterisk:20.6.0 on 5060/5061 with host.docker.internal; helpers registered_account/wait_for_event/client_against_asterisk exist.
- **Postcondition**: raw SIP is verified against real PJSIP in Docker: the test gates on docker_available() (skip with SKIP_MESSAGE when docker is absent), registers to the compose Asterisk, and receives a RawSipMessage whose text contains "REGISTER".
- **Invariant**: make test-integration gate: Makefile target does docker compose up -d → cargo test --features pjsua-native --test sip_integration → docker compose down (trap); Cargo.toml declares [[test]] name="sip_integration" required-features=["pjsua-native"]; CI treats the docker gate as mandatory.

## Boy Scout Rule

- src/tests/raw_sip_real_test.rs: スタブの test_example / TODO コメントを除去し、RawSipRealTestPolicy（関数名=動詞: policy()/production_path_steps()/observation_only()、変数名=ドメイン概念: register_call_site, module_file, docker_gate）として翻訳可能性を満たす。ハードコード文字列（"tests/sip_integration.rs", "raw_sip_rx_reaches_subscriber", "src/ffi/backend_calls.rs"）は名前付き定数に抽出。\n- tests/sip_integration.rs: wait_for_raw_sip ヘルパーを wait_for_event と同型の述語ループとして抽出し、タイムアウト 30s を既存 EVENT_TIMEOUT 定数へ統合（ハードコード禁止）。raw_sip_rx_reaches_subscriber 内の処理（docker ゲート→subscribe→register→待機→assert→shutdown）を 1 関数 1 責務の手順で並べ、実行可能散文として読めるようにする。\n- src/tests/docker_asterisk_it.rs: 追加テストを IntegrationTestEntry としてマトリクスに組み込むことで、コメントとコードの整合（Everything as Code）を維持する。\n- 対象外（ボーイスカウト適用外）: production 実装（raw_sip_module.rs / callback.rs / backend_calls.rs / event_path_wiring.rs）は P17-2 等で既に翻訳可能性を満たしており、本チケットでは変更しない。

## Test Plan

### Unit Tests

- UT: [Normal] RawSipRealTestPolicy::policy() encodes the §62.38 (Q17) decision — test_file=="tests/sip_integration.rs", test_name=="raw_sip_rx_reaches_subscriber", make_target=="test-integration", feature_gate=="pjsua-native", skip_message=="[SKIPPED: docker unavailable]".
- UT: [Error] Observation-only invariant — the raw SIP module handlers return PJ_FALSE (P17-2 asserts in raw_sip_module.rs), and the production register call site is recorded as "src/ffi/backend_calls.rs" (the initialize() call at backend_calls.rs:84).
- UT: [Boundary] RAW_SIP_QUEUE capacity is 64 (src/ffi/callback.rs:62) — loss-tolerant queue (drop + RAW_SIP_DROPPED++) never blocks or panics on the transport thread.
- UT: [Invariant] No TestBackend dummy hooks — policy asserts the production path steps ["pjsip_module hook","enqueue_raw_sip_bytes","drain_and_publish_raw_sip","subscribe_raw_sip"] and that TestBackend / default build keeps subscribe_raw_sip() as a silent channel (spec, not a bug).
- UT: [C145-Pre] N0091 defines the pjsip_module hook — src/ffi/raw_sip_module.rs exists with on_rx_request/on_rx_response handlers; policy records module_file=="src/ffi/raw_sip_module.rs".
- UT: [C145-Post] The real integration test raw_sip_rx_reaches_subscriber verifies subscribe_raw_sip() — policy asserts test_name and that the test lives in the pjsua-native-gated tests/sip_integration.rs binary.
- UT: [C145-Inv] No TestBackend dummy hooks — the production path is wired through backend_calls::initialize (policy asserts register_call_site=="src/ffi/backend_calls.rs"); the integration test uses SipClient::new (real PjsuaBackend), never a test-only hook.
- UT: [C146-Pre] N0088 defines the docker integration base — src/tests/docker_asterisk_it.rs::docker_available() exists (policy references docker_gate=="docker_available").
- UT: [C146-Post] raw SIP is verified against real PJSIP in Docker — policy asserts test_file is the pjsua-native-gated binary and the test gates on docker_available(); CI treats the gate as mandatory (make test-integration).
- UT: [C146-Inv] make test-integration gate — policy asserts make_target=="test-integration" and Cargo.toml [[test]] declares required-features=["pjsua-native"] for the sip_integration binary.

### Integration Tests

- IT: [Integration point] tests/sip_integration.rs::raw_sip_rx_reaches_subscriber — the real pjsua-native path: pjsip_module hook (on_rx_request/on_rx_response, registered in backend_calls::initialize at backend_calls.rs:78-86) → enqueue_raw_sip_bytes → RAW_SIP_QUEUE (capacity 64, callback.rs:62) → drain_and_publish_raw_sip (event_path_wiring.rs:89, 5ms poll) → EventBus::publish_raw_sip → subscribe_raw_sip() (client.rs:211).
- IT: [Verification] Asterisk responds to the client REGISTER with 401/200 — each response carries CSeq: N REGISTER and is captured by the hook; assert msg.text().contains("REGISTER") and msg.direction == SipMessageDirection::Response (raw channel, loop until found, EVENT_TIMEOUT=30s).
- IT: [Prerequisites] docker + Asterisk (docker-compose.yml asterisk:20.6.0, ports 5060/5061, host.docker.internal); #![cfg(feature = "pjsua-native")] compile gate (Cargo.toml [[test]] required-features); docker_available() runtime gate (SKIP_MESSAGE on absence); make test-integration drives compose up → cargo test --features pjsua-native --test sip_integration → compose down (trap).
- IT: [Related tickets] N0107→N0091 (P17-2 raw_sip_module.rs production wiring), N0107→N0088 (P16-10 docker Asterisk integration base, docker_asterisk_it.rs); sibling P19-5 (N0111 real-PJSIP protocol + RTP test) is a separate ticket.

### Exceptions

- [Item+Reason] TestBackend / 既定ビルドでは subscribe_raw_sip() が無音チャネル（Some を返すが実メッセージは流れない）。これは既定ビルドでテスト不能である（TestBackend は pjsip_module フックを持たず enqueue_raw_sip_bytes を呼ばないため RawSipMessage が生成され得ない）が、not a design defect — 生産経路は fully wired（backend_calls.rs:84 で raw_sip_module::register 済み）であり、実検証は pjsua-native 統合テストが担う。無音チャネルは明示された仕様である。
- [Reason] 実 SIP 受信経路（pjsip_module フック → enqueue_raw_sip_bytes → subscribe_raw_sip）の検証には pjsua-native（実 PJSIP endpoint + 登録済み raw SIP module）と実在する SIP ピア（docker Asterisk）という外部依存が必須のため、既定ビルドではテスト不能。これは not an architectural defect — 同経路は production として実装済み（backend_calls.rs:78-86, event_path_wiring.rs:89）で、同一経路を raw_sip_rx_reaches_subscriber が end-to-end で検証する。テストギャップではなく実環境ゲートの仕様である。
- [Alternative verification] (1) raw_sip_rx_reaches_subscriber（tests/sip_integration.rs, #![cfg(feature="pjsua-native")]）を make test-integration で実行し、実 PJSIP 経路の受信を end-to-end 検証 — これが決定版の代替検証であり、default build のテスト不能は not a design defect。 (2) 既定ビルドでは src/tests/raw_sip_real_test.rs の C145/C146 契約テストが、policy エンコード / register 呼び出し点 / 観測専用返却 / docker・make ゲートを静的に検証する。

### Plan Test Code (concrete code)

- UT: [C145-Pre] pjsip_module hook exists — policy encodes the module file
```rust
let policy = RawSipRealTestPolicy::policy();
assert_eq!(policy.module_file, "src/ffi/raw_sip_module.rs");
assert!(std::path::Path::new(policy.module_file).exists(), "raw_sip_module.rs must exist (P17-2)");
```
- UT: [C145-Post] real integration test verifies subscribe_raw_sip — policy encodes the test name/file and the test body receives a RawSipMessage
```rust
let policy = RawSipRealTestPolicy::policy();
assert_eq!(policy.test_name, "raw_sip_rx_reaches_subscriber");
assert_eq!(policy.test_file, "tests/sip_integration.rs");
```
- UT: [C145-Inv] no TestBackend dummy hooks — production path wired via backend_calls::initialize; observation-only PJ_FALSE
```rust
let policy = RawSipRealTestPolicy::policy();
assert_eq!(policy.register_call_site, "src/ffi/backend_calls.rs");
assert_eq!(policy.observation_only, "PJ_FALSE");
assert_eq!(policy.production_path_steps(), &[
    "pjsip_module hook",
    "enqueue_raw_sip_bytes",
    "drain_and_publish_raw_sip",
    "subscribe_raw_sip",
]);
```
- UT: [C146-Pre] docker integration base — docker_available gate exists
```rust
let policy = RawSipRealTestPolicy::policy();
assert_eq!(policy.docker_gate, "docker_available");
assert_eq!(policy.skip_message, crate::tests::docker_asterisk_it::SKIP_MESSAGE);
assert!(std::path::Path::new("src/tests/docker_asterisk_it.rs").exists(), "docker_asterisk_it.rs must exist (P16-10)");
```
- UT: [C146-Post] raw SIP verified against real PJSIP in Docker — the integration test body (Red-phase blueprint)
```rust
#[tokio::test]
async fn raw_sip_rx_reaches_subscriber() -> Result<(), Box<dyn std::error::Error>> {
    if !docker_available() {
        return Ok(());
    }
    let (client, mut events) = client_against_asterisk().await?;
    let mut raw = client.subscribe_raw_sip().expect("raw sip enabled");
    client
        .add_account(registered_account("sip:1001@127.0.0.1:5060", "password")?)
        .await?;
    wait_for_event(&mut events, |p| {
        matches!(
            p,
            SipEventPayload::RegistrationStateChanged(RegistrationState::Registered)
        )
    })
    .await;
    let msg = wait_for_raw_sip(&mut raw, |m| m.text().contains("REGISTER")).await;
    assert!(msg.text().contains("REGISTER"));
    assert_eq!(msg.direction, SipMessageDirection::Response);
    client.shutdown().await?;
    Ok(())
}
```
- UT: [C146-Inv] make test-integration gate — policy encodes the make target and feature gate
```rust
let policy = RawSipRealTestPolicy::policy();
assert_eq!(policy.make_target, "test-integration");
assert_eq!(policy.feature_gate, "pjsua-native");
assert!(policy.make_target.starts_with("test-"), "Makefile gate target");
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| raw SIP の検証経路が存在しない（H8 ギャップ） | 実 pjsua-native 統合テストで subscribe_raw_sip() の受信を固定 | raw SIP 実検証（§62.38） |
| src/tests/raw_sip_real_test.rs は [::STUB::] P19-1 スタブ（test_example のみ）で、src/tests/mod.rs に未宣言。 | RawSipRealTestPolicy（§62.38 決定のエンコード）+ C145/C146 契約テスト 6 件を実装。src/tests/mod.rs に pub mod raw_sip_real_test を宣言。 | N0107 実装ターゲットのスタブ解決（P19-1） |
| DockerItPolicy.integration_tests は 4 件（C116-post が len()==4 を検証）。 | raw_sip_rx_reaches_subscriber を 5 件目に追加し、C116-post を len()==5 に更新。 | 統合テストマトリクス整合（P19-1） |
| tests/sip_integration.rs に raw SIP 受信の統合テストが無く、subscribe_raw_sip() の実 PJSIP 受信は未検証（H8 RESIDUE）。 | raw_sip_rx_reaches_subscriber（docker_available ゲート + subscribe_raw_sip 購読 + REGISTER 応答受信アサーション）+ wait_for_raw_sip ヘルパー + SipMessageDirection import を追加。 | 実 PJSIP 経路での subscribe_raw_sip() 受信検証（P19-1） |
| tokio に process フィーチャが無く、tests/sip_integration.rs の tokio::process::Command（既存 originate_call_to）が pjsua-native ゲート下でコンパイル不能だった。 | tokio features に process を追加（Cargo.toml）。sip_integration バイナリが pjsua-native でコンパイル可能に。 | test-integration ゲート前提の修復（P19-1） |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) src/tests/mod.rs に pub mod raw_sip_real_test; を追加。 (2) src/tests/raw_sip_real_test.rs のスタブを置換: RawSipRealTestPolicy + 定数 + C145/C146 契約テスト（既定ビルドで make test 可能）。 (3) src/tests/docker_asterisk_it.rs の DockerItPolicy::policy().integration_tests に raw_sip_rx_reaches_subscriber を追加し C116-post の len()==4 を 5 に更新。 (4) tests/sip_integration.rs に raw_sip_rx_reaches_subscriber + wait_for_raw_sip ヘルパー + use siprs::model::SipMessageDirection を追加。 (5) make test（既定ビルド）で契約テスト green を確認。 (6) make test-integration（docker + pjsua-native）で実 SIP 受信を確認。
- [Risks] docker デーモン不在時は test-integration が skip（SKIP_MESSAGE）になり、実検証は CI でのみ有効。REGISTER 応答（401/200）の到着タイミングにより raw チャネル受信順が不定なため、wait_for_raw_sip で REGISTER を含むメッセージまでループする。broadcast 容量 4096 に対しメッセージ数は数件のため Lagged は実質発生しない。
- [Caveats] RawSipMessage に raw フィールドは存在しない（RFC 擬似コード msg.raw は実 API と不一致）ため text() で検証する。TestBackend/既定ビルドの subscribe_raw_sip() は Some を返すが無音チャネル（仕様）。C116-post の更新を忘れると P16-10 のテストが red になる。
- [Open items] なし。production 経路の変更を伴わないため追加の設計判断は不要。
- [Future improvements] P19-5（N0111）で実 PJSIP の protocol + RTP 統合テスト（発着信・メディア）を追加。INVITE 方向（on_rx_request 捕捉）の raw SIP 検証も同基盤で拡張可能。
Implementation summary (P19-1):
- Changed files: src/tests/mod.rs, src/tests/raw_sip_real_test.rs, src/tests/docker_asterisk_it.rs, tests/sip_integration.rs, Cargo.toml (+Cargo.lock).
- Key changes: (1) RawSipRealTestPolicy + C145/C146 contract tests in raw_sip_real_test.rs; (2) pub mod raw_sip_real_test declared; (3) DockerItPolicy matrix 4->5 + C116-post len()==5; (4) raw_sip_rx_reaches_subscriber integration test + wait_for_raw_sip helper; (5) tokio process feature added (fixes pre-existing compile failure of tokio::process::Command in originate_call_to under pjsua-native).
- Test results: make test (default build, test-util) — ALL GREEN (raw_sip_real_test 6/6, docker_asterisk_it 9/9 incl. C116-post len()==5). cargo check --features pjsua-native --test sip_integration — compiles cleanly.
- Quality checks: run-quality-checks.js — 0 issues.
- Environmental block: make test-integration could NOT be executed in this sandbox because Docker Hub is unreachable (docker pull hangs even for hello-world; Docker Desktop proxy http.docker.internal:3128). The integration test is gated by docker_available() (Q9c) and will run in CI (P18-2 docker job) where the asterisk:20.6.0 image is pullable.

## P19-1 — implemented at 12 locations

### src/tests/docker_asterisk_it.rs

- Line 106
```rust
impl DockerItPolicy {
```

- Line 205
```rust
    fn c116_post_policy_encodes_q9_decisions() {
```

### src/tests/mod.rs

- Line 13
```rust
pub mod raw_sip_real_test;
```

### src/tests/raw_sip_real_test.rs

- Line 86
```rust
impl RawSipRealTestPolicy {
```

- Line 118
```rust
    fn c145_pre_pjsip_module_hook_defined() {
```

- Line 131
```rust
    fn c145_post_real_test_verifies_subscribe_raw_sip() {
```

- Line 141
```rust
    fn c145_inv_no_testbackend_dummy_hooks() {
```

- Line 162
```rust
    fn c146_pre_docker_integration_base_defined() {
```

- Line 176
```rust
    fn c146_post_raw_sip_verified_against_real_pjsip_in_docker() {
```

- Line 186
```rust
    fn c146_inv_make_test_integration_gate() {
```

### tests/sip_integration.rs

- Line 84
```rust
async fn wait_for_raw_sip(
```

- Line 158
```rust
async fn raw_sip_rx_reaches_subscriber() -> Result<(), Box<dyn std::error::Error>> {
```
