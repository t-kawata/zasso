# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P19-2: H8: on_ice_transport_error 登録と IceTransportError 経路（§62.39）

**Ticket Key**: P19-2 · **Phase**: 19

**RFC Source**: `RFC-ROOT.md`

---

## Background

H8 RESIDUE は「IceTransportError は未配線（P17-3 スコープ外）」と断言。pjsua_callback に実在する on_ice_transport_error を登録済みコールバックと同一パターンで配線する。

### Goal
PJSIP 2.17 の pjsua_callback に実在する on_ice_transport_error を、P17-3 で登録済みの P1/P2 コールバック（TransportStateChanged / CallTsxStateChanged / CallReplaced / NatDetected）と同一パターンで配線し、ICE メディアトランスポートエラー（現状 TURN Refresh エラー）を NativeEvent::IceTransportError として reactor へ届ける。

### Purpose
H8 RESIDUE（Q18）は「IceTransportError は未配線（P17-3 スコープ外）」と断言。NativeEvent::IceTransportError バリアント（P16-4 導入）は存在するが FFI 生産経路がゼロで、実 PJSIP が on_ice_transport_error を呼んでもイベントが失われていた。本チケットで production code を実装し、ICE トランスポート障害の可観測性を確立する。

### Motivation
residue 監査で「IceTransportError の生産コードゼロ」が検出された。RFC N0108（62.39 on_ice_transport_error 登録）が Q18 として配線を要求。P17-3 は明示的にスコープ外としたため、本チケットがその残項目を解消する。

### Constraints
(1) RT スレッド制約（lock / alloc / await 禁止）のため NativeEvent は scalar-only（String 不可、既存コールバックと同一）。(2) stub と native（bindgen）の ABI 一致を cfg-paired alias で担保（P18-1 / N0102 先例）。(3) RFC N0108 スニペットのシグネチャ（pjsip_error_info 付き）は pjmedia_ice_cb.on_ice_strans_error のものであり、pjsua_callback の実シグネチャ (index, op, status, param) に従う。

## Scope

- 【変更1-path】src/ffi/bindings.rs
- 【変更1-action】modify: pjsua_callback ミラーに on_ice_transport_error フィールド（Option<unsafe extern "C" fn(index: c_int, op: u32, status: pj_status_t, param: *mut c_void)>）と pj_ice_strans_op const モジュール（INIT=0/NEGOTIATION=1/KEEP_ALIVE=2/ADDR_CHANGE=3）を追加。
- 【変更2-path】src/ffi/callback.rs（+ src/ffi/ice_transport_error.rs 実装、src/ffi/mod.rs モジュール宣言、src/state/m20_native_event_conv.rs 変換修正、src/runtime/reactor.rs extract_event_ids 修正）
- 【変更2-action】modify: register_callbacks で on_ice_transport_error を登録。ice_transport_error.rs のコールバックが実 bindgen シグネチャ (index, op, status, param) の index / op / status を NativeEvent::IceTransportError{index, operation, status} に詰めて reactor へ enqueue（P17-3 残項目の解消）。RFC N0108 の pjsip_error_info 抽出は実シグネチャと不一致のため不採用（pjmedia_ice_cb 側の別コールバック）。
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] src/ffi/bindings.rs
  - [Action] add (modify)
  - [What specifically changes] pj_ice_strans_op const モジュール追加 + pjsua_callback に on_ice_transport_error フィールド追加（13 番目、P17-3 の 4 コールバック群の直後に配置）
  - [Before → After] Before: 12 フィールドで on_ice_transport_error なし。After: 13 フィールドで ABI 互換の fn-pointer を持つ。pj_ice_strans_op は未定義 → const モジュール定義
  - [API contract change] なし（bindings は内部 FFI 面。stub のフィールド名・型は vendored pjsua.h をミラー）
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし（pj_status_t は既存 i32）
  - [File/module path] src/ffi/ice_transport_error.rs
  - [Action] add (implement — [::STUB::] P19-2 を解決)
  - [What specifically changes] IceStransOpParam cfg-paired alias（native=bindings::pj_ice_strans_op / stub=u32）+ on_ice_transport_error コールバック（#[no_mangle] pub unsafe extern "C" fn）。param は非 dereference、index/op/status を NativeEvent::IceTransportError に詰めて enqueue_native_event
  - [Before → After] Before: header + [::STUB::] のみ。After: 完全なコールバック実装
  - [API contract change] crate::ffi::ice_transport_error モジュールが公開（ffi/mod.rs に pub mod 追加）。コールバック関数は pub
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/ffi/callback.rs
  - [Action] modify
  - [What specifically changes] use crate::ffi::ice_transport_error::on_ice_transport_error; + register_callbacks に config.cb.on_ice_transport_error = Some(on_ice_transport_error) 追加
  - [Before → After] Before: 12 コールバック配線。After: 13 コールバック配線（on_nat_detect の直後）
  - [API contract change] なし（register_callbacks は内部関数）
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/state/m20_native_event_conv.rs
  - [Action] modify
  - [What specifically changes] NativeEvent::IceTransportError を {call_id: u32} → {index: i32, operation: u32, status: i32} へ変更。変換 arm を None 化（call 文脈なし）。P16-4 テスト native_event_ice_transport_error_maps_to_failure を None 検証へ置換
  - [Before → After] Before: IceTransportError{call_id} → Some(IceNegotiationFailed(IceFailureInfo{call_id, reason:""}))。After: IceTransportError{index, operation, status} → None（transport-level）
  - [API contract change] NativeEvent::IceTransportError の shape 変更（siprs::event 経由で公開。P16-4 契約の修正を伴う）
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/runtime/reactor.rs
  - [Action] modify
  - [What specifically changes] extract_event_ids の match で IceTransportError を call_id グループから (None, None) グループへ移動。reactor.rs:1072 の stale doc 修正
  - [Before → After] Before: IceTransportError{call_id} → (None, CallId::from_u64(call_id))。After: IceTransportError{..} → (None, None)
  - [API contract change] なし
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
- **Out of scope (items intentionally excluded, with justification):**
  - [Excluded item] pjmedia_ice_cb.on_ice_strans_error の配線（RFC N0108 スニペットのシグネチャ (op, comp_id, e, err: *mut pjsip_error_info) を持つコールバック）
  - [Why excluded] 本チケットのスコープは pjsua_callback.on_ice_transport_error（H8 residue）。pjmedia_ice_cb は別の登録面（pjmedia 層）であり、pjsip_error_info の status/reason を扱えるため別チケットで検討（Future improvements 参照）
  - [Excluded item] transport-level ICE エラーをアプリへ公開する新 SipEventPayload バリアント
  - [Why excluded] convert_native_event_to_payload が None を返す現設計では不要。公開 API 変更を伴うため、アプリ要件が出た時点で別チケット
- **Affected areas (components/systems impacted, even without direct modification):**
  - [Affected component] src/state/m20_native_event_conv.rs（P16-4 契約）
  - [Nature of impact] IceTransportError → IceNegotiationFailed の call-scoped 変換が None 化される（実コールバックに call 文脈が無いため）。P16-4 テスト・doc を更新
  - [Corresponding change needed] Y — P16-4 テスト置換 + チケット注釈に P19-2 を追加
  - [Affected component] src/runtime/reactor.rs extract_event_ids
  - [Nature of impact] IceTransportError の id 抽出が (None, None) 化
  - [Corresponding change needed] Y — match arm 移動 + extract_event_ids テスト追加
  - [Affected component] src/ffi/callback.rs register_callbacks_fills_all_callback_pointers（C050）
  - [Nature of impact] 全コールバック充填テストに on_ice_transport_error の is_some 検証が加わる
  - [Corresponding change needed] Y — 既存テストを拡張
  - [Affected component] src/ffi/mod.rs モジュールツリー
  - [Nature of impact] pub mod ice_transport_error が追加され、モジュールがコンパイル対象になる
  - [Corresponding change needed] Y — 宣言追加

## STUBs — Must Be Fully Implemented in This Ticket (1 items — resolved: 1)

### TS-001 — `src/ffi/ice_transport_error.rs — resolved

- **Marker**: `// [::TICKET::] P19-2 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P19-2 --for-spec --no-implementation-order`.`
- **Contracts**: C147, C148, C149
- **Resolution Plan**: Resolved: on_ice_transport_error extern "C" callback implemented in src/ffi/ice_transport_error.rs (IceStransOpParam cfg-paired alias + enqueue NativeEvent::IceTransportError{index, operation, status}), registered via config.cb.on_ice_transport_error in callback.rs, m20 conversion → None, reactor extract_event_ids → (None, None).

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0108`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0108` | architecture | rust | 62.39 on_ice_transport_error 登録（Q18） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.39 on_ice_transport_error 登録（Q18） (N0108) | → | ☆ 62.23 P1/P2 FFI コールバック登録の完了（Q2） (N0092) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0108` | `src/ffi/ice_transport_error.rs` |

## Investigation

- **実 bindgen シグネチャ（PJSIP 2.17.0, target/debug/build/siprs-*/out/bindings.rs:21194-21201）**: `pjsua_callback.on_ice_transport_error: Option<unsafe extern "C" fn(index: c_int, op: pj_ice_strans_op, status: pj_status_t, param: *mut c_void)>`。doc: "report error in ICE media transport. Currently used to report TURN Refresh error. @param param ... always set to NULL"。
- **RFC N0108 スニペットの不一致**: RFC は `(op, comp_id, e, err: *mut pjsip_error_info)` を示すが、これは `pjmedia_ice_cb.on_ice_strans_error`（vendor/pjsip/pjmedia/include/pjmedia/transport_ice.h:58,72）のシグネチャ。`pjsip_error_info` は bindgen 出力に存在しない（grep 0 件）。本 spec は実 pjsua_callback シグネチャに従う。
- **pj_ice_strans_op（bindgen Rust enum, repr(u32)）**: PJ_ICE_STRANS_OP_INIT=0 / NEGOTIATION=1 / KEEP_ALIVE=2 / ADDR_CHANGE=3（bindings.rs:17211-17220）。stub ミラーは P11-9 パターン（pjsip_transport_state と同様の u32 const モジュール）で追加が必要。
- **pj_status_t**: bindgen = c_int（out/bindings.rs:97）、stub は既存 `pub type pj_status_t = i32`（src/ffi/bindings.rs:298）。
- **現状 pjsua_callback stub（src/ffi/bindings.rs:674-710）**: 12 フィールドで on_ice_transport_error なし。P17-3 が on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect を追加済み。
- **register_callbacks（src/ffi/callback.rs:201-222）**: 12 コールバックを配線、on_ice_transport_error なし。テスト register_callbacks_fills_all_callback_pointers（L557）が各 is_some を検証。
- **NativeEvent::IceTransportError は現状 `{ call_id: u32 }`（src/state/m20_native_event_conv.rs:88）**。全 NativeEvent バリアントは scalar-only — どのコールバックも String を enqueue しない（RT スレッド no-allocation 制約。on_call_transfer_status は *const pj_str_t の reason を _st_text として読まない）。
- **変換（m20_native_event_conv.rs:215-222）**: `IceTransportError{call_id}` → `Some(IceNegotiationFailed(IceFailureInfo{call_id, reason:""}))`。テスト native_event_ice_transport_error_maps_to_failure（L584）。実コールバックに call 文脈が無いため、この call-scoped マッピングは意味的に誤りであり None へ修正する。
- **extract_event_ids（src/runtime/reactor.rs:1209）**: IceTransportError が call_id グループ → `(None, CallId::from_u64(call_id))`。`(None, None)` グループへ移動が必要。reactor.rs:1072 の stale doc（"P1/P2 events convert to None"）も修正（P16-4 以降 P1/P2 は Some。IceTransportError が None 例外）。
- **モジュール配線**: `src/ffi/ice_transport_error.rs` は Initial Design Artifact header + `[::STUB::] P19-2` のみ。`src/ffi/mod.rs` に `pub mod ice_transport_error;` の追加が必要（現状 bindings / constants / pj_str / callback / backend_calls / raw_sip_module / media_port_adapter / transport_wiring のみ）。
- **cfg-paired alias パターン（P18-1 / N0102 先例）**: callback.rs の `type TransportStateParam = bindings::pjsip_transport_state`（native）/ `= u32`（stub）。op 引数にも同パターン `type IceStransOpParam = bindings::pj_ice_strans_op`（native）/ `= u32`（stub）を適用する。
- **テストパターン**: callback.rs テストは install_test_queue でキューを隔離。every_callback_enqueues_exactly_one_event（L845）と on_transport_state_enqueues_transport_state_changed（L876）が新ハンドラテストの雛形。

## Acceptance Criteria

- on_ice_transport_error が register_callbacks で登録される（config.cb.on_ice_transport_error = Some(on_ice_transport_error)、P17-3 と同一パターン）
- NativeEvent::IceTransportError の生産経路が実装される（ice_transport_error.rs のコールバック → enqueue_native_event、実 bindgen シグネチャに準拠）
- **[Happy path]** register_callbacks 後に config.cb.on_ice_transport_error.is_some() が真。ICE メディアトランスポートエラー（TURN Refresh 失敗）発生時、on_ice_transport_error(index, op, status, param) が NativeEvent::IceTransportError{index, operation, status} を enqueue し、reactor の extract_event_ids が (None, None)、convert_native_event_to_payload が None を返す（panic なし）
- **[Error case]** param が非 null（dangling pointer）でも dereference されず panic しない。キュー満杯時は enqueue_native_event が drop して native_event_dropped_count を増加（RT スレッドはブロックしない — C052 不変条件の拡張）
- **[Edge case]** index=i32::MIN / i32::MAX、status=i32::MIN / 0 / i32::MAX、operation=0..=3（pj_ice_strans_op 全値）が無劣化で NativeEvent::IceTransportError に運ばれる。stub（op: u32）と native（op: pj_ice_strans_op enum）の両ビルドで register_callbacks の fn-pointer 代入が型一致でコンパイルされる（IceStransOpParam cfg-paired alias）

## Invariants

on_ice_transport_error は登録済みコールバック（TransportStateChanged 等）と同一パターンで実装する。

- 【Normal establishment】register_callbacks（src/ffi/callback.rs）に config.cb.on_ice_transport_error = Some(on_ice_transport_error) を追加し、pjsua_callback ミラー（src/ffi/bindings.rs）に ABI 互換フィールドを追加する。P17-3 の 4 コールバック群と同一の登録・enqueue パターン。
- 【Invariant on error】param は常に dereference しない（PJSIP は NULL 保証。非 null でも読まない）。コールバックは RT スレッド上で lock / alloc / await を行わず enqueue のみ。キュー満杯時は enqueue_native_event が drop して native_event_dropped_count を増加（ブロックしない — 既存 C052 不変条件の拡張）。
- 【Internal state invariant】NativeEvent::IceTransportError は scalar-only {index: i32, operation: u32, status: i32} で call_id を持たない（transport-level / TURN Refresh）。convert_native_event_to_payload は None、extract_event_ids は (None, None)。
- 【Boundary invariant】stub の op 型（u32）と native の pj_ice_strans_op（bindgen Rust enum）が cfg-paired IceStransOpParam alias で一致し、両ビルドで register_callbacks の fn-pointer 代入が型検証される。op 値 0..=3、index / status は i32 全域で無劣化。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C147 — N0108→N0092

- **Precondition**: N0092 registers P1/P2 callbacks (on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect)
- **Postcondition**: on_ice_transport_error is added to the pjsua_callback mirror and registered in register_callbacks
- **Invariant**: Same registration pattern as the P17-3 callbacks (config.cb.<field> = Some(<handler>))

### C148 — N0108→pjsua.h bindgen ABI

- **Precondition**: The stub pjsua_callback declares on_ice_transport_error with an ABI-compatible fn-pointer type; the stub pj_ice_strans_op const module (INIT/NEGOTIATION/KEEP_ALIVE/ADDR_CHANGE = 0..3) is present in src/ffi/bindings.rs
- **Postcondition**: The callback's op parameter type reconciles stub (u32) vs native (bindgen pj_ice_strans_op Rust enum) via the cfg-paired IceStransOpParam alias; register_callbacks assignment type-checks in both feature modes
- **Invariant**: The on_ice_transport_error signature matches the bindgen pjsua_callback field type in both modes; operation is carried as u32 in NativeEvent::IceTransportError

### C149 — N0108→callback enqueue

- **Precondition**: A native event queue is installed by register_callbacks
- **Postcondition**: on_ice_transport_error(index, op, status, param) enqueues exactly one NativeEvent::IceTransportError { index, operation: op as u32, status }
- **Invariant**: param is never dereferenced (PJSIP documents it as always NULL); the callback performs no allocation/locking/await on the RT thread (scalar-only event, no String)

### C150 — N0108→M20 conversion

- **Precondition**: NativeEvent::IceTransportError carries no call_id (transport-level, TURN Refresh errors)
- **Postcondition**: convert_native_event_to_payload(NativeEvent::IceTransportError { .. }, _) returns None
- **Invariant**: The P16-4 IceTransportError → IceNegotiationFailed call-scoped mapping is superseded; no call_id is fabricated

### C151 — N0108→reactor extract_event_ids

- **Precondition**: NativeEvent::IceTransportError is matched in extract_event_ids
- **Postcondition**: extract_event_ids(&NativeEvent::IceTransportError { .. }) == (None, None)
- **Invariant**: The reactor doc comment no longer claims P1/P2 events convert to None (stale since P16-4); IceTransportError is documented as the transport-level None exception

## Boy Scout Rule

- register_callbacks_fills_all_callback_pointers（src/ffi/callback.rs L557）の P17-3 コメント「P17-3 §62.23: the four P1/P2 callbacks」を P19-2 §62.39 の 13 番目登録へ拡張し、コールバック群の登録が「全て」であることを名前で示す。
- reactor.rs L1072 の stale doc（"P1/P2 events convert to None and are silently not published"）を修正する。P16-4 以降 P1/P2 は Some に変換され、唯一 IceTransportError（transport-level）が None 例外である実態へ合わせる。
- m20_native_event_conv.rs L106-115 の doc（"P0/P1/P2 variants produce Some ... sole exception is RegistrationStateChanged"）を修正し、IceTransportError が 2 つ目の None 例外であることと理由（call 文脈なし）を明記する。
- P16-4 テスト native_event_ice_transport_error_maps_to_failure を意味に合う名前（例: native_event_ice_transport_error_is_transport_level_and_yields_none）へリネームし、P16-4 に加えて P19-2 のチケット注釈を付与する。
- op 引数の型調停を IceStransOpParam alias として抽出し、「op パラメータは ICE ストリーム操作として型付けされる」とコードが散文として読めるようにする（P18-1 の TransportStateParam と同一パターン）。
- RFC N0108 の pjsip_error_info スニペットが実シグネチャと不一致である旨を、spec の testExceptions / investigation に明記し、コードに誤ったコメントを残さない（No-Justification Rule）。

## Test Plan

### Unit Tests

- UT: [Normal] register_callbacks が on_ice_transport_error を Some で配線する（C147-Post / C050 拡張）: register_callbacks_fills_all_callback_pointers（src/ffi/callback.rs L557）に assert!(config.cb.on_ice_transport_error.is_some()) を追加。P17-3 の 4 コールバック群と同一パターンで 13 番目のコールバックとして登録される
- UT: [Normal] pjsua_callback ミラーに on_ice_transport_error フィールドと pj_ice_strans_op const モジュールが存在する（C148-Pre）: src/ffi/bindings.rs の pjsua_callback（L674-710）に Option<unsafe extern "C" fn(index: c_int, op: u32, status: pj_status_t, param: *mut c_void)> 型の on_ice_transport_error を追加。pj_ice_strans_op モジュール（INIT=0 / NEGOTIATION=1 / KEEP_ALIVE=2 / ADDR_CHANGE=3）を追加し、register_callbacks テストがコンパイル可能であること
- UT: [Normal] on_ice_transport_error が NativeEvent::IceTransportError{index, operation, status} をちょうど 1 個 enqueue する（C149-Post）: install_test_queue(4) 後に on_ice_transport_error(0, 0, 0, null_mut()) を呼び、queue.pop() が Some(NativeEvent::IceTransportError{index:0, operation:0, status:0})、次の pop が None。every_callback_enqueues_exactly_one_event（L845）の不変条件を新ハンドラに適用
- UT: [Error] param が非 null（dangling pointer 0x1）でも dereference されず panic しない（C149-Inv / null ガード）: on_ice_transport_error(1, 1, 1, 0x1 as *mut c_void) が null 渡しと同一の NativeEvent::IceTransportError を enqueue する。PJSIP は param を常に NULL で渡す（bindgen doc）ため、null / 非 null で動作が同一であることを検証
- UT: [Boundary] index / operation / status の境界値が無劣化で運ばれる（C149-Boundary / C148-Inv）: index=i32::MIN / i32::MAX、operation=0..=3（pj_ice_strans_op 全値）、status=i32::MIN / 0 / i32::MAX を渡し、enqueue された NativeEvent::IceTransportError が同一値を保持することを表引きで検証
- UT: [Invariant] IceTransportError は scalar-only で RT スレッド制約（lock / alloc / await 禁止）を満たす（C149-Inv）: NativeEvent::IceTransportError は {index: i32, operation: u32, status: i32} のみで String を持たない。コールバックは enqueue_native_event 以外の副作用（ログ・メモリ確保・ロック）を一切行わないことをテストで確認
- UT: [Normal] NativeEvent::IceTransportError の変換は None を返す（C150-Post、P16-4 契約の修正）: convert_native_event_to_payload(NativeEvent::IceTransportError{index:0, operation:0, status:0}, None) が None を返す。実コールバックに call 文脈が無いため IceNegotiationFailed への call-scoped 変換を廃止し、m20_native_event_conv.rs L584 の native_event_ice_transport_error_maps_to_failure テストを置換する
- UT: [Invariant] extract_event_ids が IceTransportError に (None, None) を返す（C151-Post）: extract_event_ids(&NativeEvent::IceTransportError{index:1, operation:2, status:3}) が (None, None)。reactor.rs L1209 の call_id グループから (None, None) グループへ移動したことを検証
- UT: [Boundary] pj_ice_strans_op の stub 定数値の検証（C148-Pre / 境界）: bindings::pj_ice_strans_op::{PJ_ICE_STRANS_OP_INIT, PJ_ICE_STRANS_OP_NEGOTIATION, PJ_ICE_STRANS_OP_KEEP_ALIVE, PJ_ICE_STRANS_OP_ADDR_CHANGE} がそれぞれ 0,1,2,3 であり、on_ice_transport_error の operation: op as u32 が無劣化変換される

### Integration Tests

- IT: [pjsua-native] ICE メディアトランスポートエラー発生時に on_ice_transport_error → NativeEvent::IceTransportError → reactor 処理（extract_event_ids で (None, None)）が成立する（§62.39 / N0108）
- IT: [Integration point] register_callbacks（src/ffi/callback.rs）→ pjsua_callback.on_ice_transport_error フィールド（src/ffi/bindings.rs）→ on_ice_transport_error コールバック（src/ffi/ice_transport_error.rs）→ NativeEvent::IceTransportError（src/state/m20_native_event_conv.rs）→ extract_event_ids / process_native_event（src/runtime/reactor.rs）
- IT: [Verification] cargo check --features pjsua-native が、bindgen 生成 pjsua_callback の on_ice_transport_error: Option<unsafe extern "C" fn(index: c_int, op: pj_ice_strans_op, status: pj_status_t, param: *mut c_void)> に対して config.cb.on_ice_transport_error = Some(on_ice_transport_error) が型一致でコンパイルされることを確認（IceStransOpParam cfg-paired alias の検証）。stub ビルドではユニットテストで全経路を検証。実 PJSIP スタック下で TURN Refresh 失敗を発生させ、コールバックが NativeEvent::IceTransportError を enqueue することを確認
- IT: [Prerequisites] pjsua-native feature（P18-2 prebuilt パイプライン、またはローカル PJSIP 2.17 ビルド）; register_callbacks で native event queue が install 済み; pjsua_init 後、ICE トランスポートが TURN Refresh 失敗を起こす環境（STUN/TURN サーバー停止等）
- IT: [Related tickets] P17-3（N0092: P1/P2 コールバック登録パターン — 本チケットはその残項目）; P16-4（NativeEvent→Some 変換 — IceTransportError は本チケットで None 化し契約修正）; P18-1（bindgen enum 型 / cfg-paired alias パターン N0102）; P18-2（pjsua-native prebuilt 3-OS パイプライン）; P19-5（transport id 解決 — resolve_transport_id の STUB は本チケットと独立）

### Exceptions

- **Exception 1 — 実 PJSIP の RT スレッドからのコールバック発火統合**
  - [Item] 実 PJSIP が on_ice_transport_error を RT スレッドから起動する統合経路（TURN Refresh 失敗の実発火）は決定論的ユニットテスト不能（cannot test）
  - [Reason] コールバックの挙動自体は完全に決定論的で、直接呼び出し + 境界引数（null / dangling param、index / status 境界、op 4 値）で全分岐を網羅できる。テスト不能なのは「PJSIP がいつ・どの実引数で呼ぶか」という外部依存トリガーのみであり、これは not a design defect（設計欠陥ではない）— 再設計を要する untestable な内部ロジックは存在しない
  - [Alternative verification] on_ice_transport_error の直接呼び出しユニットテスト（全分岐）+ cargo check --features pjsua-native による ABI コンパイル検証 + pjsua-native IT（実スタックでの発生確認）
- **Exception 2 — RFC N0108 の pjsip_error_info 抽出のテスト不能**
  - [Item] RFC N0108 スニペットが示す pjsip_error_info からの status / reason 抽出（comp_id / err 引数）は、実 pjsua_callback シグネチャに存在しないためテスト不能（not testable）
  - [Reason] bindgen 出力（PJSIP 2.17.0, target/debug/build/siprs-*/out/bindings.rs:21194-21201）の pjsua_callback.on_ice_transport_error は (index: c_int, op: pj_ice_strans_op, status: pj_status_t, param: *mut c_void) であり、RFC スニペットの (op, comp_id, e, err: *mut pjsip_error_info) は pjmedia_ice_cb.on_ice_strans_error（vendor/pjsip/pjmedia/include/pjmedia/transport_ice.h:58,72）のシグネチャ。これは not a design defect（設計欠陥ではなく spec-vs-code の不一致）であり、実シグネチャに従う設計で解消する
  - [Alternative verification] bindgen 出力を根拠に実シグネチャを spec 化。param が null / 非 null の両方で dereference されないことをユニットテストで検証（C149-Inv）
- **Exception 3 — IceTransportError → アプリ可視 SipEventPayload の E2E 変換のテスト不能**
  - [Item] IceTransportError からアプリ可視の SipEventPayload への E2E 変換はテスト不能（cannot test）— convert_native_event_to_payload が None を返すため publish されない
  - [Reason] 実コールバックが call 文脈を持たない（transport-level / TURN Refresh エラー）ため、call_id 必須の IceNegotiationFailed(IceFailureInfo{call_id, reason}) への変換を維持できない。これは not a design defect（意図的な設計決定: P16-4 の call-scoped 前提を修正）でありテストギャップではない。変換自体の挙動（None を返す）は決定論的にユニットテスト可能
  - [Alternative verification] UT が convert_native_event_to_payload(IceTransportError{..}) → None と extract_event_ids → (None, None) を検証。将来 transport-level の SipEventPayload バリアント追加時に当該変換テストを追加

### Plan Test Code (concrete code)

- UT: [C147-Post] register_callbacks が on_ice_transport_error を Some で配線する（register_callbacks_fills_all_callback_pointers を拡張）
  ```rust
  let queue = crossbeam_queue::ArrayQueue::new(8);
  let mut config: bindings::pjsua_config = unsafe { std::mem::zeroed() };
  register_callbacks(&mut config, queue);
  assert!(config.cb.on_ice_transport_error.is_some(), "13th callback must be wired (C147)");
  ```
- UT: [C148-Pre] pj_ice_strans_op const モジュールが 0..=3 を公開する（src/ffi/bindings.rs stub ミラー）
  ```rust
  assert_eq!(bindings::pj_ice_strans_op::PJ_ICE_STRANS_OP_INIT, 0);
  assert_eq!(bindings::pj_ice_strans_op::PJ_ICE_STRANS_OP_NEGOTIATION, 1);
  assert_eq!(bindings::pj_ice_strans_op::PJ_ICE_STRANS_OP_KEEP_ALIVE, 2);
  assert_eq!(bindings::pj_ice_strans_op::PJ_ICE_STRANS_OP_ADDR_CHANGE, 3);
  ```
- UT: [C149-Post] on_ice_transport_error が NativeEvent::IceTransportError{index, operation, status} をちょうど 1 個 enqueue する（src/ffi/callback.rs tests）
  ```rust
  let queue = install_test_queue(4);
  unsafe { on_ice_transport_error(0, 0, 0, std::ptr::null_mut()) };
  assert_eq!(
      queue.pop(),
      Some(NativeEvent::IceTransportError { index: 0, operation: 0, status: 0 })
  );
  assert_eq!(queue.pop(), None, "exactly one event");
  ```
- UT: [C149-Inv] param が非 null（dangling pointer）でも dereference されず同一イベントを enqueue する
  ```rust
  let queue = install_test_queue(4);
  unsafe { on_ice_transport_error(1, 1, 1, 0x1 as *mut std::os::raw::c_void) };
  assert_eq!(
      queue.pop(),
      Some(NativeEvent::IceTransportError { index: 1, operation: 1, status: 1 }),
      "param must never be dereferenced"
  );
  assert_eq!(queue.pop(), None);
  ```
- UT: [C150-Post] convert_native_event_to_payload が IceTransportError に None を返す（P16-4 契約修正）
  ```rust
  let result = convert_native_event_to_payload(
      NativeEvent::IceTransportError { index: 0, operation: 0, status: 0 },
      None,
  );
  assert!(result.is_none(), "transport-level ICE error has no call-scoped payload");
  ```
- UT: [C151-Post] extract_event_ids が IceTransportError に (None, None) を返す（src/runtime/reactor.rs tests）
  ```rust
  let (account_id, call_id) = extract_event_ids(&NativeEvent::IceTransportError {
      index: 1, operation: 2, status: 3,
  });
  assert_eq!(account_id, None);
  assert_eq!(call_id, None, "IceTransportError is transport-level (C151)");
  ```
- UT: [C148-Inv / C149-Boundary] index/operation/status の境界値が無劣化で運ばれる（表引き）
  ```rust
  for index in [i32::MIN, -1, 0, i32::MAX] {
      for status in [i32::MIN, 0, i32::MAX] {
          let queue = install_test_queue(2);
          unsafe { on_ice_transport_error(index, 2, status, std::ptr::null_mut()) };
          assert_eq!(
              queue.pop(),
              Some(NativeEvent::IceTransportError { index, operation: 2, status }),
              "index/status must survive i32::MIN..=i32::MAX"
          );
          assert_eq!(queue.pop(), None);
      }
  }
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| pjsua_callback ミラーに on_ice_transport_error が無く IceTransportError の生産コードゼロ | フィールド追加 + register_callbacks 登録 + NativeEvent::IceTransportError 生成経路を実装 | on_ice_transport_error 配線（§62.39） |
| pjsua_callback ミラーに on_ice_transport_error が無く IceTransportError の生産コードゼロ | フィールド追加 + register_callbacks 登録 + NativeEvent::IceTransportError 生成経路を実装 | on_ice_transport_error 配線（§62.39） |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) src/ffi/bindings.rs: pj_ice_strans_op const モジュール + pjsua_callback.on_ice_transport_error フィールド追加。(2) src/ffi/ice_transport_error.rs: IceStransOpParam cfg-paired alias + on_ice_transport_error extern C コールバック実装（[::STUB::] 解決）。(3) src/ffi/mod.rs: pub mod ice_transport_error 追加。(4) src/ffi/callback.rs: register_callbacks に登録 + use 追加。(5) src/state/m20_native_event_conv.rs: NativeEvent::IceTransportError を {index, operation, status} へ変更 + 変換を None 化 + P16-4 テスト置換。(6) src/runtime/reactor.rs: extract_event_ids を (None, None) グループへ移動 + stale doc 修正。
- [Risks] (a) RFC N0108 の pjsip_error_info スニペットに従うとコンパイル不能（実シグネチャと不一致）。実 bindgen シグネチャに従うこと。(b) NativeEvent::IceTransportError の shape 変更は P16-4 契約（IceNegotiationFailed への Some 変換）を修正する — P16-4 テストと doc を更新し、理由を spec に明記すること。(c) native ビルド（pjsua-native）で bindgen の pj_ice_strans_op enum と callback fn-pointer 型が一致しない場合は IceStransOpParam alias で吸収（P18-1 と同一手法）。
- [Caveats] on_ice_transport_error は現状 TURN Refresh エラーのみ（PJSIP doc）。ICE ネゴシエーション失敗等は pjmedia_ice_cb 側の別コールバックであり、本チケットのスコープ外。param は常に NULL のため reason は取得不可。
- [Open items] transport-level の ICE エラーをアプリへ公開する SipEventPayload バリアント（IceTransportErrorInfo 等）は未設計。convert_native_event_to_payload が None を返すため現状は reactor 内で消費・ログのみ。
- [Future improvements] pjmedia_ice_cb.on_ice_strans_error の配線（pjsip_error_info の status/reason を取得可能）は別チケット。transport id から call_id への解決（P19-5）が実装されれば、IceTransportError を call 文脈へ紐付ける再設計の余地あり。
Implementation summary:
- Changed files: src/ffi/bindings.rs, src/ffi/ice_transport_error.rs, src/ffi/callback.rs, src/ffi/mod.rs, src/state/m20_native_event_conv.rs, src/runtime/reactor.rs
- Key changes: (1) pjsua_callback に on_ice_transport_error フィールド + pj_ice_strans_op const モジュール追加。 (2) ice_transport_error.rs に on_ice_transport_error extern C コールバック実装（IceStransOpParam cfg-paired alias、[::STUB::] TS-001 解決）。 (3) register_callbacks で登録。 (4) NativeEvent::IceTransportError を {index, operation, status} へ変更し convert_native_event_to_payload は None（transport-level、P16-4 契約修正）。 (5) reactor extract_event_ids で (None, None) 化。
- Test results: make test 全 1378 lib テスト + 統合テスト合格（新規 6 テスト含む）、cargo check --features pjsua-native 合格、verify-red-coverage 合格（C147-C151）、validate-ticket-targets 11 checks 合格、no-excuse gate 合格
Review report:
- Static quality check: passed (pre-existing findings only, no P19-2 new issues)
- Translatability: no issues (verb-phrase fns, domain vars, no magic numbers/debug output)
- Tests: make test exit 0 (1378 lib + integration), native check exit 0
- Contracts C147-C151: all fulfilled (registered / ABI cfg-paired alias / enqueue-one / conversion None / extract (None,None))
- STUBs: TS-001 resolved; P19-3/4/5/6 stubs correctly deferred
- Annotations: 6 changed files annotated, no AMBIGUOUS
- Crimes: 0
- Issues found and fixes applied: RFC N0108 pjsip_error_info snippet (pjmedia_ice_cb signature) not followed — actual bindgen signature used; P16-4 IceTransportError→IceNegotiationFailed contract corrected to None (transport-level); stale reactor/m20 docs fixed

## P19-2 — implemented at 17 locations

### src/ffi/bindings.rs

- Line 672
```rust
    pub mod pj_ice_strans_op {
```

- Line 674
```rust
        /// Initialization (candidate gathering).
```

- Line 735
```rust
        pub on_ice_transport_error: Option<
```

- Line 737
```rust
            unsafe extern "C" fn(
```

### src/ffi/callback.rs

- Line 560
```rust
    fn register_callbacks_fills_all_callback_pointers() {
```

- Line 990
```rust
    fn pj_ice_strans_op_const_values() {
```

- Line 1000
```rust
    fn on_ice_transport_error_enqueues_ice_transport_error() {
```

- Line 1017
```rust
    fn on_ice_transport_error_param_never_dereferenced() {
```

- Line 1036
```rust
    fn on_ice_transport_error_boundary_values_preserved() {
```

### src/ffi/ice_transport_error.rs

- Line 32
```rust
type IceStransOpParam = bindings::pj_ice_strans_op;
```

- Line 35
```rust
type IceStransOpParam = u32;
```

### src/ffi/mod.rs

- Line 43
```rust
pub mod ice_transport_error;
```

- Line 45
```rust

```

### src/runtime/reactor.rs

- Line 109
```rust
type SpawnResult =
```

- Line 1199
```rust
fn extract_event_ids(event: &NativeEvent) -> (Option<AccountId>, Option<CallId>) {
```

- Line 2500
```rust
    fn extract_event_ids_ice_transport_error_yields_none_none() {
```

### src/state/m20_native_event_conv.rs

- Line 588
```rust
    fn native_event_ice_transport_error_yields_none() {
```
