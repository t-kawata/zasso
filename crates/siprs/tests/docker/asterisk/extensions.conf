; [::STUB::] M20-1: 結合テスト用ダイヤルプラン
;
; test_context:
;   1001 -> test_user_1（自己ダイヤル = media loopback）
;   1002 -> test_user_2
;   _1XXX -> 内線間通話

[test_context]

; test_user_1 の内線 — 自己ループバックで media 確認
exten => 1001,1,Answer()
 same => n,Echo()
 same => n,Hangup()

; test_user_2 の内線
exten => 1002,1,Answer()
 same => n,Echo()
 same => n,Hangup()

; 内線間通話
exten => _1XXX,1,Answer()
 same => n,Dial(PJSIP/${EXTEN},30)
 same => n,Hangup()
