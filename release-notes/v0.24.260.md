# Release v0.24.260

## Summary

Completed Phase 1 of the voiput crate — all pure-logic pipeline components ported from MYCUTE with 61 unit tests. Stage 4/6 in the development demo tool.

## What's Changed

- **feat**: Ported PostCorrectionProcessor (text buffering + LLM correction state machine) with 9 tests
- **feat**: Extracted signal quality filter `is_worthy_to_run_asr()` as a standalone pure function with 7 tests
- **feat**: Ported replace dictionary interceptor `apply_replaces()` (longest-match priority) with 6 tests
- **feat**: Updated `test-run.rs` with POST_CORRECT, SIGNAL_FILTER, INTERCEPTOR demo sections (Stage 4/6)
- **fix**: Removed unnecessary `mut` warnings in test-run.rs (3 occurrences)
- **docs**: Added release notes for v0.24.259
- **chore**: Completed 6 tickets (M0-1 through M1-4), all reviewed
