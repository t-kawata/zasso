# Release v0.24.270

## Summary

Added SpeechRecognizer to unify all three backends (OpenAI, macOS, Windows) with an interceptor task for text replacement. Renamed `VoiceKit*` types to `Voiput*` for crate naming consistency.

## What's Changed

- **feat(voiput)**: Added `SpeechRecognizer` (M5-1) — unified backend lifecycle manager with interceptor task (text replacement via `apply_replaces`), `rebuild_pc_backend` helper for PostCorrection, and 5 new unit tests
- **refactor(voiput)**: Renamed `VoiceKitConfig` → `VoiputConfig`, `VoiceKitConfigBuilder` → `VoiputConfigBuilder`, `VoiceKitError` → `VoiputError` across all source files, specs, and documentation for crate naming consistency
- **chore**: 1 ticket completed and reviewed (M5-1)
