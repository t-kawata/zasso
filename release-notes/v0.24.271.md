# Release v0.24.271

## Summary

Added the public `Voiput` API (M5-2) as the crate's entry point with flush(), event loop, and engine switching. Implemented auto-build for native libraries (M6-1) — Swift/C# sources from MYCUTE, build scripts, and sherpa-onnx runtime library collection.

## What's Changed

- **feat(voiput)**: Added `Voiput` public API (M5-2) — `Voiput::new(config)`, `start()`/`stop()`, `next_event().await`, `flush()` (stop→drain→start), `set_engine()`, `set_locale()`, `update_replaces()`, `health_check()`, `is_running()` with 17 new unit tests
- **feat(voiput)**: Added `is_running()` getter to `SpeechRecognizer` for public API support
- **feat(voiput)**: Added auto-build system for native libraries (M6-1) — copied Swift/C# sources from MYCUTE (1,555 lines), created `native/swift/build.sh` and `native/cs/build.ps1`, updated `build.rs` with automatic build + Swift Concurrency runtime check + `libs/<platform>/` runtime library collection + `cargo:rerun-if-changed` for native/ and libs/
- **fix(voiput)**: Fixed `build.rs` JSON parsing for `swiftc -print-target-info` (nested `paths` object), fixed stub archive accumulation (remove old .a before ar crs)
- **chore**: 2 tickets completed and reviewed (M5-2, M6-1)
