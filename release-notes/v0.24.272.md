# Release v0.24.272

## Summary

Completed the final tickets for the voiput crate: integration tests (M6-2), README (M6-3), and cleanup of build warnings. All 27 tickets are now complete.

## What's Changed

- **test(voiput)**: Added integration tests (M6-2) — 14 tests in `tests/integration_test.rs` covering VoiputConfig, Voiput lifecycle, SttEvent, LocaleCode, and VoiputError via public API (`use voiput::*;`)
- **docs(voiput)**: Added `README.md` (M6-3) — quick start, API reference, platform support, permissions, model files, development guide, and license
- **refactor(voiput)**: Changed build.rs `cargo:warning=` to `cargo:note=` for informational messages; extracted `generate_stub_macos()`; suppressed redundant auto-build when Swift Concurrency unavailable (macOS 15)
- **chore**: 3 tickets completed and reviewed (M6-2, M6-3, plus post-completion build.rs cleanup)
