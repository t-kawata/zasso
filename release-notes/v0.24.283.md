# Release v0.24.283

## Summary
Phase 6 RFC compliance fixes: RFC document synced with implementation (M7-0), SpeechRecognizer API refactored (M7-2) — arguments consolidated and OS validation added.

## What's Changed
- **docs: RFC sync (#61)** — Updated §4.3 (VoiputConfig.model_dir), §6.1 (build.sh), §6.2 (build.ps1), §7.6 (denoiser sherpa_onnx safe API), §8 (sherpa-onnx deps), §9 (build.rs overview); fixed all naming discrepancies (OpenAi→OpenAI, libspeech_helper→libSpeechHelper, channel 256→100, &mut self→&self)
- **feat: Cargo.toml include config** — Added `include = [...]` for prebuilt library distribution
- **feat: README license fix** — `MIT` → `MIT OR Apache-2.0`
- **refactor: SpeechRecognizer::new() arg reduction (#64)** — Changed from 6 individual args to `(tx, &VoiputConfig, replaces_map)`
- **feat: validate_config OS check (#64)** — `SttEngine::Os` now returns `Err` on unsupported platforms (Linux etc.) via `#[cfg]` gating
- **chore: VAD helper relocation** — `build_vad_processor_config()` / `resolve_vad_model_path()` moved from voiput.rs to recognizer.rs with test coverage preserved
