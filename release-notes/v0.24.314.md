# Release v0.24.314

## Summary

Added the new `trate` crate as an abstract ASR backend trait layer, along with the ticketing infrastructure for implementing the Qwen3-ASR local speech recognition backend (M0 milestone).

## What's Changed

- **feat: add `trate` crate skeleton** — new workspace-agnostic crate with `anyhow`-only dependency, providing the foundation for the `AsrBackend` trait extraction from voiput
- **docs: add ticket decomposition for trate/Qwen3-ASR RFC** — created `Tickets.md` with 27 tickets across 4 phases, plus CLAUDE.md design overview map
- **tickets: create and complete M0-1 and M0-2 tickets** — trate scaffold (Cargo.toml + lib.rs) implemented and verified (both reviewed)
- **docs: fix RFC.md path in ticket references** — normalized all design document references to the full project path
- **tickets: add ticket queue entries** — queued M0 milestone tickets for tracking
