# Release v0.24.317

## Summary

Completed the M2-5 path resolution milestone and finished the Phase 1 type system. Added `resolve_qwen3_model_paths` and `resolve_qwen3_asr_config` pure functions with 5 unit tests, plus Qwen3 model file name constants. Eliminated all compiler warnings from the voiput crate.

## What's Changed

- **feat: add Qwen3 model file name constants (M2-4)** — 5 `pub(crate)` constants in `constants.rs` for encoder/decoder/joiner/tokens filenames and subdirectory
- **feat: add path resolution functions (M2-5)** — `resolve_qwen3_model_paths()` and `resolve_qwen3_asr_config()` pure functions with `#[allow(dead_code)]` suppression and `[::STUB::]` markers noting M4-2 resolution
- **test: add 5 unit tests for path resolution** — covering absolute/relative paths, with/without model_dir, None config, and subdirectory path verification
- **fix: eliminate unused import warning** — moved `LocalAsrKind` import to test-only scope
- **docs: update Tickets.md** — added stub resolution notes for M4-2
- **tickets: complete M2-4 and M2-5** — both tickets completed and reviewed
