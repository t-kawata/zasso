# Release v0.24.323

## Summary

Completed the M5 and M6 milestones: implemented `LocalRecognizer` Facade and `LocalRecognizerAdapter`, added `SttEngine::Local` dispatch to `SpeechRecognizer`, and added ConfigBuilder validation. 26 of 27 tickets are now complete.

## What's Changed

- **feat: add LocalRecognizer Facade (M5-1)** — wraps `Box<dyn LocalAsrBackend>` with `AsrBackend` trait implementation for PseudoAsrStreamer compatibility
- **feat: add LocalRecognizerAdapter (M5-2)** — integrates LocalRecognizer with SpeechRecognizer via start/stop/tick/set_locale/update_config interface
- **feat: add SttEngine::Local dispatch to SpeechRecognizer (M6-1)** — resolved 5 `[::STUB::]` markers, `local_recognizer` field added to SpeechRecognizer, all match arms now have real Local backend dispatch
- **feat: add VoiputConfigBuilder Local validation (M6-2)** — `SttEngine::Local` requires `qwen3_asr_config` to be set
- **fix: add Sync requirement to trate::LocalAsrBackend** — required by PseudoAsrStreamer's `B: Send + Sync` type constraint
- **tickets: complete M5-1, M5-2, M6-1, M6-2, M6-3** — all five tickets completed and reviewed

## Remaining

- 2 `[::STUB::]` (resolve_qwen3_model_paths, resolve_qwen3_asr_config) — M8-2
- 4 tickets (M7-1 build.rs, M7-2 fixtures, M8-1 integration tests, M8-2 final verification)
