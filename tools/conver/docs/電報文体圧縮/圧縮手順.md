# 電報文体圧縮手順

## 1. AI に規則を与える
- docs/電報文体圧縮/skill_md_telegram_style_guide_v2_en.md を AI に読ませる。
```
まずは理解せよ
```

## 2. 圧縮したい command file を添付して圧縮計画
```
添付した Claude Code のスラッシュコマンド定義の command markdown file を、既に理解した電報記法ルールで、機能を損なわず、意味論を欠損せず、構成は同一で、ワークフローを絶対に壊さず、スクリプトの実行も完璧で、元のスラッシュコマンドと同一の動作をさせることができる command markdown file でありながら、電報文体に書き直すことで厳格さと正確さを飛躍させると同時に、 command markdown file を大きく圧縮することが可能か？ 圧縮計画を書け。 尚、電報文体で表現しようとすると元の機能や厳格さや意味論を破壊する可能性があると判断する部分は例外的に変更したいことを選択できるものとする。下記のような適切な変換工程と等価性検査を行い、圧縮と厳格化を両立する方法でなければならない。（記述言語は英語）

## 変換の実施方法
1. **仕様台帳を作る**
2. **実行契約を固定する**
3. **制御グラフを固定する**
4. **圧縮する**
5. **例外を選択する**
6. **差分検証する**
```

## 3. 圧縮実行
```
慎重に作業開始せよ
```

## 4. 出力された圧縮結果を別AIに点検させる
```
電報文体を使用して Claude Code のスラッシュコマンドを、機能を損なわず、意味論を欠損せず、構成は同一で、ワークフローを絶対に壊さず、スクリプトの実行も完璧で、元のスラッシュコマンドと同一の動作をさせることができる command markdown file でありながら、電報文体に書き直すことで厳格さと正確さを飛躍させると同時に、 command markdown file を大きく圧縮することを試みた。二つのファイルを読み比べて、電報文体への圧縮が成功していると言えるか答えよ。無闇な粗探しをするのではない。その点注意せよ。
```

## 5. 上記4のフィードバックを元のAIに渡す
```
以下、第三者のフィードバック。慎重に修正せよ。patchを出すのは禁止。修正した全文を出さねばならない。修正版であることを記述してはならない。
```

- 以上を繰り返し、
- 点検側のAIが圧縮成功と述べる時点のものを採用

## 6. 次の圧縮候補へ
```
次を添付する。圧縮計画せよ。
```

```
慎重に作業開始せよ。
```
