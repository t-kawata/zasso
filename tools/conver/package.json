{
  "name": "conver",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/conver.ts --bundle --platform=node --external:node:* --outfile=dist/conver.js --format=esm",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.4.0"
  }
}
