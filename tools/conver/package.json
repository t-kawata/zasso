{
  "name": "conver",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/entry.ts --bundle --platform=node --external:node:* --outfile=dist/conver.js --format=esm --minify",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "^0.52.0",
    "@agentclientprotocol/sdk": "^1.0.0",
    "node-cron": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "@types/node-cron": "^3.0.11",
    "esbuild": "^0.28.1",
    "typescript": "^5.4.0"
  }
}
