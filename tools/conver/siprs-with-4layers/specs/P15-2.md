# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P15-2: Config unification — RFC §10 ClientConfig promotion, STUN/TURN/ICE type unification, Vec-ization

**Ticket Key**: P15-2 · **Phase**: 15

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
RESIDUE 根因 R1（公開 API が RFC 型でない）を解消する。RFC §10 準拠 ClientConfig を唯一の公開型として昇格し、旧 src/config.rs の ClientConfig / StunServerConfig / ClientConfigBuilder を完全削除する。StunServerConfig / TurnServerConfig の二重定義（config.rs:71 と transport_ice_spec.rs:143）を一元化し、stun_servers/turn_servers を Vec 化して §13 の ICE 既定値（enabled=true / aggressive_nomination=true / max_host_candidates=16）に一致させる。

### Purpose
破壊的変更を v0.x 開発期の今一度に完了させ、旧型・二重定義を残さない（RFC §62 の方針）。公開 API が RFC §10/§13 と完全に一致することで、アプリケーションは transports / stun_servers / turn_servers / timeouts / ice を型安全に利用できる。§42 の validate() を SipClient::new で fail-fast 実行し、不正設定を早期に検出する。

### Motivation
README の RESIDUE が示す乖離: 公開 ClientConfig が旧 config.rs 型のまま（§10 の transports / stun_servers / turn_servers / timeouts が存在しない）。`turn_server: Option<StunServerConfig>` は TURN を STUN 型で表す型バグ（impl_integration_design.rs R1 に記録）。§13 ICE 既定値（true/true/16）と現実装（false/false/5）が乖離している。

### Constraints
設計判断 62.1 は §10（N0013）/§13（N0015）と矛盾しないこと（C080/C081 invariant）。破壊的変更は許容（v0.x）だが、62.2〜62.8 の他設計判断（バックエンド選択 / イベントバス一元化等）のスコープを侵さない。実装対象ファイルは src/config/client_config_unify.rs（N0070）。`Initial Design Artifact` ヘッダの削除・編集は禁止（Supreme Law §4）。

## Scope

- client_config_spec 版 ClientConfig を唯一の公開型に昇格し lib.rs で再公開する（§62.1）。
- 旧 src/config.rs の ClientConfig / StunServerConfig を完全削除し、SipClient::new のシグネチャを RFC 型に移行する。
- StunServerConfig / TurnServerConfig の二重定義（config.rs:71 と transport_ice_spec.rs:143）を一元化し、stun_servers/turn_servers を Vec 化する。
- §13 の ICE 既定値（enabled=true / aggressive_nomination=true / max_host_candidates=16）に一致させる。
- §42 の validate() を SipClient::new 冒頭で fail-fast 実行する。
- **Scope of changes:**
  - [File/module path] src/config/transport_ice_spec.rs
  - [Action] modify
  - [What specifically changes] IceConfig.max_host_candidates を u8→usize へ、Default を §13（enabled=true / aggressive_nomination=true / trickle_ice=false / renomination=false / max_host_candidates=16）へ。StunServerConfig を {host,port} → {uri: String} へ。TurnServerConfig を {host,port,username:String,password:SecretString,transport} → {uri, username:Option<String>, password:Option<SecretString>, transport} へ。
  - [Before → After] 旧形状・旧既定値（enabled=false/aggressive=false/max_host_candidates=5）→ §13 準拠（enabled=true/aggressive=true/max_host_candidates=16, usize）
  - [API contract change] StunServerConfig/TurnServerConfig/IceConfig の公開フィールド変更（host/port→uri、max_host_candidates u8→usize、username/password→Option）。破壊的変更（v0.x で許容）。
  - [Data schema change] なし（シリアライズ導出なし）
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/config/client_config_spec.rs
  - [Action] modify
  - [What specifically changes] ClientAudioConfig.default_delivery_format を String → AudioFormat（P4-2 実装済み型）へ。validate() に §42 sample-rate 検査を追加。
  - [Before → After] "16000Hz/i16/stereo" 文字列 → AudioFormat{Hz16000,I16,StereoInOut,20}。validate に sample-rate 規則を追加。
  - [API contract change] ClientAudioConfig.default_delivery_format の型変更（String→AudioFormat）。
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/config.rs
  - [Action] remove + re-export
  - [What specifically changes] 旧 ClientConfig / StunServerConfig / ClientConfigBuilder / LogLevel 定義と旧 validate / consts（MIN_SIP_PORT/MAX_SIP_PORT/MAX_USER_AGENT_BYTES）/ 旧テストを削除。client_config_spec / transport_ice_spec の型を config ルートに再エクスポート。DtmfConfig / AuthCredentials は維持。
  - [Before → After] config::ClientConfig が旧型を提供 → config::ClientConfig が RFC 型に解決
  - [API contract change] 旧 ClientConfig/ClientConfigBuilder/StunServerConfig/LogLevel の公開パスが消滅し RFC 型へ置換。
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/lib.rs
  - [Action] modify
  - [What specifically changes] 公開再エクスポートを RFC 型へ変更。ClientConfig/LogLevel → client_config_spec、StunServerConfig → transport_ice_spec。ClientConfigBuilder / 旧 StunServerConfig を除去。StunServerConfig 衝突コメント（L145-147）を解消。
  - [Before → After] siprs::ClientConfig が旧型 → RFC §10 型（唯一の公開型）
  - [API contract change] 公開型の解決先変更。ClientConfigBuilder を公開 API から削除。
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/client.rs
  - [Action] modify
  - [What specifically changes] SipClient::new の #[instrument] フィールドを旧 sip_proxy_host/sip_proxy_port → RFC フィールド（user_agent/max_calls 等）へ。旧ビルダー依存テストを RFC 型構築（struct リテラル + ..Default::default()）へ書き換え。
  - [Before → After] config.sip_proxy_host 参照 → config.user_agent 等の RFC フィールド参照
  - [API contract change] SipClient::new の引数型は config::ClientConfig（RFC 型に自動解決）
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/runtime/reactor.rs
  - [Action] modify
  - [What specifically changes] BootConfig に dtmf_sent_timeout_ms: u64 を追加。boot_config.config.dtmf.sent_timeout_ms 参照（L98）を BootConfig フィールドへ移行。テスト L1606-1607 を更新。
  - [Before → After] config.dtmf.sent_timeout_ms 参照 → BootConfig.dtmf_sent_timeout_ms
  - [API contract change] BootConfig（内部型）のフィールド追加
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/architecture/impl_integration_design.rs
  - [Action] modify
  - [What specifically changes] evidence_matches_actual_source テスト（L387-396）の config.rs アサーションを R1 解決後の状態（旧 ClientConfig が存在しない）へ更新。
  - [Before → After] 旧型存在を evidence assert → 旧型削除後の状態を assert
  - [API contract change] なし
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
  - [File/module path] src/runtime/command.rs / src/runtime/backend.rs / src/api/standalone_server_config.rs / src/api/public_api_design.rs
  - [Action] modify (test adaptation)
  - [What specifically changes] crate::config::ClientConfig 参照が config.rs 再エクスポート経由で RFC 型に自動解決。旧ビルダー依存テストを RFC 型構築へ適合。
  - [Before → After] 旧型参照 → RFC 型参照（シグネチャは自動解決）
  - [API contract change] なし
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
- **Out of scope (items intentionally excluded, with justification):**
  - [Excluded item] イベントバス一元化（EventBus::new の引数を config.event_bus_capacity / raw_sip_capacity 化、単一バス化）
  - [Why excluded] 62.3 イベントバス一元化トポロジのスコープ。P15-2 では client.rs:111 の EventBus::new(DEFAULT_EVENT_BUS_CAPACITY, None) を変更しない。
  - [Excluded item] バックエンド選択機構（MockBackend 削除 / PjsuaBackend 完全統一）
  - [Why excluded] 62.2 のスコープ。reactor.rs:75 の MockBackend は現状維持。
  - [Excluded item] resampler_quality の String プレースホルダー解消
  - [Why excluded] ResamplerQuality 型が未実装（全ツリーに定義なし）。型が揃うチケットで対応。
  - [Excluded item] SipClient::new の戻り値変更（Result<(Self, Receiver)> の形状）
  - [Why excluded] 公開 API 形状の変更は本チケットのスコープ外（62.5 公開 API 拡充のスコープ）。
- **Affected areas (components/systems impacted, even without direct modification):**
  - [Affected component] siprs 公開 API コンシューマ（アプリケーション層）
  - [Nature of impact] API surface — ClientConfig フィールド/ビルダーが消滅し RFC §10 型へ置換。StunServerConfig が uri 形式へ変更。
  - [Corresponding change needed] Y — コンシューマは ClientConfig::default() + struct リテラル（..Default::default()）で構築。v0.x の破壊的変更として許容。
  - [Affected component] runtime/reactor.rs（BootConfig）
  - [Nature of impact] 内部構造 — dtmf_sent_timeout_ms が BootConfig へ移動。
  - [Corresponding change needed] Y — 本チケットで対応。
  - [Affected component] architecture/impl_integration_design.rs（evidence テスト）
  - [Nature of impact] テスト — R1 の evidence が解決後の状態に変わる。
  - [Corresponding change needed] Y — 本チケットで対応。
  - [Affected component] 62.2〜62.8 の後続設計判断
  - [Nature of impact] 依存 — 62.2（backend initialize の config 型）、62.3（EventBus 引数）、62.6（audio config）が RFC 型を前提にできる。
  - [Corresponding change needed] 後続チケットで対応。

## STUBs — Must Be Fully Implemented in This Ticket (1 items — resolved: 1)

### TS-001 — `src/config/client_config_unify.rs — resolved

- **Marker**: `// [::TICKET::] P15-2 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-2 --for-spec --no-implementation-order`.`
- **Contracts**: C070, C080, C081
- **Resolution Plan**: Implement §62.1 ConfigUnification in client_config_unify.rs: RFC §10 ClientConfig promotion, old config.rs deletion, STUN/TURN unification + Vec-ization, §13 ICE defaults, §42 validate() fail-fast.

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0070`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0070` | config | rust | 62.1 公開設定 API の一本化（ClientConfig / STUN/TURN/ICE） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| extends | ★ 62.1 公開設定 API の一本化（ClientConfig / STUN/TURN/ICE） (N0070) | → | ☆ §10 ClientConfig Full Specification (N0013) |
| extends | ★ 62.1 公開設定 API の一本化（ClientConfig / STUN/TURN/ICE） (N0070) | → | ☆ §12 TransportConfig & §13 ICE/STUN/TURN Spec (N0015) |
| part_of | ★ 62.1 公開設定 API の一本化（ClientConfig / STUN/TURN/ICE） (N0070) | → | ☆ §62 実装整合設計 — RESIDUE 解消のための設計判断確定 (N0068) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0070` | `src/config/client_config_unify.rs` |

## Investigation

### 調査で得られた根拠 (Evidence)

**1. 旧 config.rs（src/config.rs, 577 行）— 削除対象**:
- L71: `pub struct StunServerConfig { host: String, port: u16 }` — transport_ice_spec.rs L143 と二重定義（identical shape）。
- L92: `LogLevel` enum — client_config_spec.rs L37 と重複（同一形状）。
- L108: `DtmfConfig { sent_timeout_ms }` — P7-2 の O-002 用。reactor.rs:98 が `boot_config.config.dtmf.sent_timeout_ms` で参照。DELETE_TARGETS に含まれないため維持。
- L140-170: 旧 `ClientConfig { sip_proxy_host, sip_proxy_port, credentials, user_agent, stun_server: Option<String>, turn_server: Option<StunServerConfig>, ice_enabled, srtp_enabled, tls_enabled, log_level, dtmf }` — `turn_server: Option<StunServerConfig>` は TURN-as-STUN 型バグ（impl_integration_design.rs R1 に記録）。
- L238: `ClientConfigBuilder`（fluent API）。
- L46-50: consts MIN_SIP_PORT/MAX_SIP_PORT/MAX_USER_AGENT_BYTES — 旧 ClientConfig.validate 専用。削除後は未使用。
- L188-210: 旧 validate()（sip_proxy_host 空 / port<MIN / user_agent>256B）。

**2. client_config_spec.rs（RFC §10 = N0013, 409 行）— 昇格対象**:
- L137-162: `ClientConfig` が §10 準拠（user_agent, log_level, max_calls, event_bus_capacity, raw_sip_event_capacity, audio, transports: Vec<TransportConfig>, stun_servers: Vec<StunServerConfig>, turn_servers: Vec<TurnServerConfig>, ice: IceConfig, raw_sip_events, timeouts）。
- L51: `ClientAudioConfig.default_delivery_format: String // placeholder for AudioFormat (P4-2)` — P4-2 が AudioFormat を実装済み（src/model/audio_format_chunkpair.rs:106）だがプレースホルダーが残存。§42 sample-rate 検査には AudioFormat 型が必要。
- L61: `resampler_quality: String // placeholder for ResamplerQuality` — ResamplerQuality 型は未実装（全ツリーで定義なし）。
- L193-223: 現 validate(): event_bus_capacity>=16 / raw_sip_event_capacity>=event_bus_capacity（raw_sip enabled）/ max_calls>0 / transports 非空。§42 の sample-rate 検査なし。

**3. transport_ice_spec.rs（RFC §12/§13 = N0015, 372 行）— §13 乖離**:
- L114-125: `IceConfig { enabled, aggressive_nomination, trickle_ice, renomination, max_host_candidates: u8 }`。
- L128-138: Default = enabled=false / aggressive_nomination=false / trickle_ice=false / renomination=false / max_host_candidates=5 — **RFC §13（RFC-ROOT.md L584-593）は enabled=true / aggressive_nomination=true / max_host_candidates=16（usize）**。
- L143-148: `StunServerConfig { host, port }` — **RFC §13（L596-598）は `{ uri: String }`**。
- L160-171: `TurnServerConfig { host, port, username: String, password: SecretString, transport }` — **RFC §13（L600-605）は `{ uri, username: Option<String>, password: Option<SecretString>, transport }`**。

**4. client_config_unify.rs（N0070 実装対象, 23 行）**: L20 `pub struct Config {}` + L23 `[::STUB::] P15-2: implement NODE_ID=N0070`。

**5. lib.rs（147 行）— 公開 API 移行点**:
- L102-105: `pub use config::{ AuthConfig, AuthCredentials, AuthMode, ClientConfig, ClientConfigBuilder, ConfigError, LogLevel, ServerConfig, StunServerConfig }` — ClientConfig/ClientConfigBuilder/LogLevel/StunServerConfig が旧 config.rs 型。
- L137: `pub use config::client_config_spec::{ClientAudioConfig, RawSipEventConfig, TimeoutConfig}` — ClientConfig/LogLevel が含まれていない。
- L140-143: `pub use config::transport_ice_spec::{ IceConfig, TcpTransportConfig, TransportConfig, TurnServerConfig, TurnTransport, UdpTransportConfig }`。
- L145-147: StunServerConfig 衝突コメント（一元化で解消）。

**6. client.rs（1254 行）— SipClient::new**:
- L18: `use crate::config::ClientConfig`（旧型）。
- L94: `#[instrument(skip(config), fields(sip_host = %config.sip_proxy_host, sip_port = config.sip_proxy_port))]` — 旧フィールド参照。
- L95-98: `pub async fn new(config: ClientConfig) -> Result<(Self, Receiver), SipError>` + `config.validate()?`。
- L111: `EventBus::new(DEFAULT_EVENT_BUS_CAPACITY, None)` — 62.3 のスコープのため現状維持。
- テスト: L457/476/487/507/559/579/624/676/1133 が `ClientConfig::builder()` 依存。

**7. runtime 層 — 型参照**:
- command.rs L105: `RuntimeCommand::Initialize { config: crate::config::ClientConfig }` — config.rs 再エクスポートで RFC 型に自動解決。
- reactor.rs L33: `BootConfig { pub config: ClientConfig }`、L98: `boot_config.config.dtmf.sent_timeout_ms`（RFC 型に dtmf が無いため要移行）、L1606-1607: テストが `config.dtmf.sent_timeout_ms = 50` を設定。
- backend.rs L24/180/421: `SipBackend::initialize(&mut self, config: &crate::config::ClientConfig)` — 型自動解決。テスト L838/849/1038 は `ClientConfig::default()` 使用（RFC 型でも Default 実装済みで動作）。

**8. 他テスト影響**: standalone_server_config.rs L374 `SipClient::new(ClientConfig::default())`（動作）、L1046/1066（要確認）。public_api_design.rs L213 import（要確認）。impl_integration_design.rs L387-396 `evidence_matches_actual_source` が旧 config.rs の `pub struct ClientConfig` / `turn_server: Option<StunServerConfig>` の存在を assert — P15-2 後に壊れるため更新必須。

**9. RFC 根拠（RFC-ROOT.md）**:
- L573-608: §13 ICE/STUN/TURN 完全仕様（上記 §13 乖離）。
- L433-449: §10.1 既定値（user_agent=tauri-siprs/0.1, log_level=Info, max_calls=32, event_bus_capacity=2048, raw_sip_event_capacity=4096, transports=[udp(5060),tcp(5060)], audio=AudioFormat{Hz16000,I16,StereoInOut,20}）。
- L2704-2719: §42 validate_client_config（event_bus_capacity>=16 / raw_sip_event_capacity>=event_bus_capacity / sample_rate∈{8k,16k,24k,48k}）。
- L3780-3825: §62.1 設計判断（client_config_spec 版昇格、二重定義一元化、Vec 化、§13 既定値一致、SipClient::new での validate fail-fast）。

## Acceptance Criteria

- **[Happy path]** `SipClient::new(ClientConfig::default())` が Ok((SipClient, Receiver)) を返し、購読した最初の制御イベントが SipEventPayload::ClientInitialized である。`siprs::ClientConfig` が RFC §10 型（client_config_spec）に解決され、`IceConfig::default()` が §13（enabled=true / aggressive_nomination=true / max_host_candidates=16）と一致する。旧 config.rs の ClientConfig/StunServerConfig が削除され、`crate::config::ClientConfig` が RFC 型に解決される。
- **[Error case]** `event_bus_capacity < 16` の ClientConfig で `SipClient::new` が `Err(SipErrorKind::InvalidConfig)` を返し、message に "event_bus_capacity" を含む。reactor は spawn されない（fail-fast）。
- **[Edge case]** `event_bus_capacity == 16`（最小境界）と `raw_sip_event_capacity == event_bus_capacity`（等値境界）が validate() を通過する。`default_delivery_format` の全 SampleRate（Hz8000/Hz16000/Hz24000/Hz48000）が validate() を通過する。stun_servers/turn_servers が空 Vec でも `ClientConfig::default().validate()` が Ok。

## Invariants

### 正常成立条件 (Normal condition)
RFC §10 ClientConfig が唯一の公開設定型であり、`siprs::ClientConfig`・`crate::config::ClientConfig`・`client_config_spec::ClientConfig` がすべて同一型に解決される。IceConfig 既定値が §13（enabled=true / aggressive_nomination=true / max_host_candidates=16）と一致する。

### エラー時不変条件 (Error invariant)
validate() が InvalidConfig を返す場合、reactor や EventBus への副作用は発生しない — fail-fast は SipClient::new 冒頭で spawn 前に実行される。

### 内部状態不変条件 (Internal state invariant)
StunServerConfig / TurnServerConfig は transport_ice_spec.rs にのみ定義され、旧 config.rs に二重定義が残らない。IceConfig.max_host_candidates は usize（§13 準拠）。

### 境界値不変条件 (Boundary invariant)
event_bus_capacity >= 16、raw_sip enabled 時 raw_sip_event_capacity >= event_bus_capacity、sample_rate ∈ {8000, 16000, 24000, 48000}（AudioFormat 型により型レベル保証）。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C070 — N0070→N0068

- **Precondition**: §62 親セクションが存在する — src/architecture/impl_integration_design.rs に DesignDecisionId::ConfigUnification が定義され、RESIDUE 根因 R1（公開 ClientConfig が RFC 型でない）が記録されている（evidence: src/config.rs:141 ClientConfig; turn_server: Option<StunServerConfig>）。
- **Postcondition**: 62.1 が §62 の一部として記述される — src/config/client_config_unify.rs に §62.1 の型付き設計レコード（決定・削除対象・I/O 境界）が実装され、impl_integration_design.rs の ConfigUnification と整合する。
- **Invariant**: 設定一本化は §10/§13 と矛盾しない — ClientConfig のフィールド集合が RFC §10（N0013）と一致し、IceConfig/StunServerConfig/TurnServerConfig が §13（N0015）と一致する。

### C080 — N0070→N0013

- **Precondition**: N0013 が §10 ClientConfig を定義する — src/config/client_config_spec.rs が user_agent/log_level/max_calls/event_bus_capacity/raw_sip_event_capacity/audio/transports/stun_servers/turn_servers/ice/raw_sip_events/timeouts を保持する。
- **Postcondition**: 62.1 が ClientConfig 一本化を規定する — lib.rs が client_config_spec::ClientConfig を唯一の公開型として再公開し、旧 config.rs の ClientConfig/ClientConfigBuilder が削除される。
- **Invariant**: RFC §10 型が唯一の公開型として残る — `siprs::ClientConfig` と `crate::config::ClientConfig` が同一型（client_config_spec::ClientConfig）であり、旧フィールド（sip_proxy_host 等）が公開 API に存在しない。

### C081 — N0070→N0015

- **Precondition**: N0015 が §12/§13 の transport/ICE/STUN/TURN を定義する — src/config/transport_ice_spec.rs が TransportConfig/IceConfig/StunServerConfig/TurnServerConfig/TurnTransport/TlsConfig を保持する。
- **Postcondition**: 62.1 が StunServerConfig/TurnServerConfig 一元化と Vec 化を規定する — 旧 config.rs:71 の StunServerConfig が削除され、client_config_spec::ClientConfig の stun_servers: Vec<StunServerConfig> / turn_servers: Vec<TurnServerConfig> に一元化される。
- **Invariant**: 二重定義が解消され §13 既定値と一致する — StunServerConfig がソースツリーに 1 箇所のみ存在し、IceConfig 既定値が enabled=true / aggressive_nomination=true / max_host_candidates=16 と一致する。

## Boy Scout Rule

### Translatability Improvement Plan

本チケットが触れるコードの翻訳可能性を向上させる（Boy Scout Rule）:

1. **旧 config.rs の untranslatable なコードを削除で解消**: `ClientConfigBuilder::build()` の `expect("sip_proxy_host is required")`（パニック API）と、複数責務を抱えた旧 validate() を、RFC §10 ClientConfig の型による構造化 + 明確な §42 validate() に置換。パニックに依存する `client_config_builder_build_panics_without_host` テストも撤去。
2. **String プレースホルダーを型に置換**: client_config_spec.rs L51 の `default_delivery_format: String // placeholder for AudioFormat (P4-2)` を `AudioFormat` 型へ置換。文字列 `"16000Hz/i16/stereo"` の暗黙的エンコーディングを、ドメイン型 AudioFormat{sample_rate:Hz16000, bit_depth:I16, channel_layout:StereoInOut, frame_ms:20} で明示化。
3. **reactor.rs の dtmf 参照を BootConfig パラメータ化**: `boot_config.config.dtmf.sent_timeout_ms` という config の深部参照を、`BootConfig { dtmf_sent_timeout_ms: u64 }` という明示的な reactor 起動パラメータに昇格。reactor が ClientConfig の形状に依存しなくなる。
4. **テストの魔法数を定数/フィールド参照へ**: reactor.rs L1607 の `config.dtmf.sent_timeout_ms = 50` のような直値テストを、BootConfig フィールド経由の設定に置換。
5. **lib.rs の複雑なコメントを単純化**: L145-147 の「StunServerConfig は衝突回避のため再エクスポートしない」という型重複を許容する言い訳コメントを、一元化後の単純な `pub use config::transport_ice_spec::StunServerConfig` に置換（No-Justification Rule）。
6. **config.rs の再エクスポートで内部参照を単純化**: client.rs/command.rs/backend.rs/reactor.rs の `crate::config::ClientConfig` が config.rs の再エクスポートを通じて RFC 型に解決され、各所の型注釈が実体と一致する。

スコープ外の既存コード（resampler_quality プレースホルダー等）は、ResamplerQuality 型が未実装のため今回変更しない。

## Test Plan

### Unit Tests

- UT: [Normal] ClientConfig::validate() が §42 規則（event_bus_capacity>=16, sample rate 8/16/24/48k, raw_sip 容量制約）を検査すること。対象: src/config/client_config_spec.rs::validate()。
- UT: [Normal] IceConfig 既定値が §13（enabled=true / aggressive_nomination=true / max_host_candidates=16）と一致すること。対象: src/config/transport_ice_spec.rs::IceConfig::default()。
- UT: [Normal] TurnServerConfig が StunServerConfig と別型であること（型バグ解消）。旧 config.rs の `turn_server: Option<StunServerConfig>` が消え、RFC ClientConfig の `turn_servers: Vec<TurnServerConfig>` に置換されることをコンパイル時に保証。
- **UT: [Normal] — RFC §10 型の昇格と既定値**: (1) `siprs::ClientConfig` が `client_config_spec::ClientConfig` に解決され、`ClientConfig::default()` の全フィールドが §10.1 と一致する（user_agent="tauri-siprs/0.1", log_level=LogLevel::Info, max_calls=32, event_bus_capacity=2048, raw_sip_event_capacity=4096, transports=[Udp(5060),Tcp(5060)], stun_servers=[], turn_servers=[], ice=IceConfig::default(), raw_sip_events=RawSipEventConfig::default(), timeouts=TimeoutConfig::default()）。検証: client_config_spec.rs の `client_config_default_constructs` / `client_config_default_transports_include_udp_and_tcp` を拡張。(2) `ClientAudioConfig.default_delivery_format` が `AudioFormat` 型（src/model/audio_format_chunkpair.rs, P4-2 実装済み）に解決され、既定値 `AudioFormat{sample_rate:SampleRate::Hz16000, bit_depth:BitDepth::I16, channel_layout:ChannelLayout::StereoInOut, frame_ms:20}` を持つ。検証: `client_audio_config_defaults` を拡張。(3) `IceConfig::default()` が §13 の enabled=true / aggressive_nomination=true / trickle_ice=false / renomination=false / max_host_candidates=16 に一致する。検証: transport_ice_spec.rs の `ice_config_default_values` を §13 値へ更新。(4) `StunServerConfig{uri}` が構築可能（例: `StunServerConfig{uri:"stun.example.com:3478".into()}`）。検証: `stun_server_config_constructs` を uri 形状へ更新。(5) `TurnServerConfig{uri, username:Option<String>, password:Option<SecretString>, transport}` が構築可能。検証: `turn_server_config_constructs` を §13 形状へ更新。
- **UT: [Error] — §42 検証の拒否経路**: (1) `event_bus_capacity<16` で validate() が `Err(SipErrorKind::InvalidConfig)` を返し message に "event_bus_capacity" を含む。検証: `client_config_rejects_small_event_bus_capacity`。(2) raw_sip enabled 時 `raw_sip_event_capacity < event_bus_capacity` で InvalidConfig。検証: `client_config_rejects_raw_sip_capacity_less_than_event_bus`。(3) `max_calls==0` で InvalidConfig。検証: `client_config_rejects_zero_max_calls`。(4) `transports` 空で InvalidConfig。検証: `client_config_rejects_empty_transports`。(5) 旧 config.rs の `sip_proxy_host` 空 / port 0 / user_agent>256B の検証規則は旧型削除に伴い撤去される（RFC §10 ClientConfig はこれらフィールドを持たない）。
- **UT: [Boundary] — 境界値**: (1) `event_bus_capacity==16`（最小境界）で validate() が Ok。検証: `client_config_accepts_minimum_event_bus_capacity`。(2) `raw_sip_event_capacity == event_bus_capacity`（等値境界）で Ok。検証: `client_config_accepts_raw_sip_capacity_equal_to_event_bus`。(3) §42 sample-rate: `default_delivery_format` の `SampleRate` 全バリアント（Hz8000/Hz16000/Hz24000/Hz48000）で validate() が Ok を返す。SampleRate は閉じた enum（src/model/audio_format_chunkpair.rs:43-52）のため不正値は型レベルで表現不能であり、§42 検査は防御的（到達不能）。(4) `AudioFormat::new` が `frame_ms∉{10,20,40,60}` を `Err(AudioFormatError)` で拒否する（§42 のフレーム保証の型レベル実装）。
- **UT: [Invariant] — 公開型単一性と §13 整合（C070/C080/C081 の検証）**: §62 親セクションが存在する（C070 precondition: impl_integration_design.rs に ConfigUnification 設計判断が定義される）ことを確認し、62.1 が §62 の一部として記述される（C070 postcondition: client_config_unify.rs に §62.1 設計レコードが実装される）こと、設定一本化は §10/§13 と矛盾しない（C070 invariant）ことを検証する。C080: N0013 が §10 ClientConfig を定義する（client_config_spec.rs）ことを確認し、62.1 が ClientConfig 一本化を規定する（lib.rs が RFC §10 型を唯一の公開型として再公開）こと、RFC §10 型が唯一の公開型として残る（旧 config.rs ClientConfig が削除される）ことを検証する。C081: N0015 が §12/§13 の transport/ICE/STUN/TURN を定義する（transport_ice_spec.rs）ことを確認し、62.1 が StunServerConfig/TurnServerConfig 一元化と Vec 化を規定する（二重定義を一元化）こと、二重定義が解消され §13 既定値と一致する（IceConfig enabled=true/aggressive_nomination=true/max_host_candidates=16）ことを検証する。具体的検証: (a) `siprs::ClientConfig`（lib.rs）と `crate::config::ClientConfig`（config.rs 再エクスポート）が同一型であることを compile-time 関数 `fn assert_same<T>(_: T, _: T){}` で確認。(b) `crate::config::StunServerConfig` が `transport_ice_spec::StunServerConfig` と同一型であり、旧 config.rs 定義がソースツリーに存在しないことを `find_identifier_outside_comments`（src/transport.rs の既存パターン）で検証。

### Integration Tests

- IT: [Integration point] client.rs(SipClient::new) → runtime/reactor.rs(CoreReactor::spawn) → EventBus publish の経路。SipClient::new(ClientConfig::default()) が Ok を返し、返却された broadcast receiver の最初の制御イベントが SipEventPayload::ClientInitialized であることを検証（src/client.rs::sip_client_constructs_with_valid_config を拡張）。
- IT: [Verification] 不正な ClientConfig（event_bus_capacity=8）で SipClient::new が Err(SipErrorKind::InvalidConfig) を返す（fail-fast）。config.validate() が reactor spawn 前に実行され、reactor が起動しないことを検証（client.rs に `client_config_invalid_fails_fast` を追加）。
- **IT: [Integration point] — 各インターフェースの接続**: (1) SipClient::new(config: RFC ClientConfig) → BootConfig{config, dtmf_sent_timeout_ms} → CoreReactor::spawn の経路で RFC 型が reactor へ渡る。(2) config.rs の `pub use client_config_spec::ClientConfig` 再エクスポートにより `crate::config::ClientConfig`（runtime/command.rs::RuntimeCommand::Initialize の config フィールド、backend.rs::SipBackend::initialize の引数）が RFC 型に解決されることをコンパイルで検証。(3) `siprs::ClientConfig` と `crate::config::ClientConfig` の型同一性を `fn assert_same<T>(_: T, _: T){}` で検証。
- **IT: [Verification] — 統合テストが検証する内容**: (1) 正常系: `SipClient::new(ClientConfig::default())` が Ok((SipClient, Receiver)) を返し、Receiver の最初のイベントが ClientInitialized である（C047 postcondition）。(2) 異常系: `event_bus_capacity<16` で `Err(SipErrorKind::InvalidConfig)`（fail-fast 検証）。(3) `ClientConfig::default()` が `siprs::config::transport_ice_spec::StunServerConfig` と `siprs::StunServerConfig` の同一性を介して §13 型に解決される。
- **IT: [Prerequisites] — 統合テストの前提条件**: (1) config.rs が RFC ClientConfig を再エクスポート済み。(2) runtime/reactor.rs::BootConfig が `dtmf_sent_timeout_ms: u64` フィールドを持ち、`boot_config.config.dtmf.sent_timeout_ms` 参照が除去済み。(3) client.rs::SipClient::new の `#[instrument]` フィールドが RFC フィールド（user_agent/log_level 等）へ更新済み。(4) reactor は MockBackend（62.2 未着）のまま。
- **IT: [Related tickets] — 関連チケット**: P15-1（§62 実装整合設計 — 型付き設計データと evidence テスト）、P7-2（O-002 DtmfSent タイムアウト — BootConfig への移行）、P3-1（RFC 設定型の原型）、P4-2（AudioFormat/SampleRate 実装）、62.2（バックエンド選択 — 本チケットでは MockBackend 維持）。

### Exceptions

- **Exception entry — STUN/TURN/ICE 設定の実 PJSIP 反映**:
  - [Item] 一元化された StunServerConfig/TurnServerConfig/IceConfig が実 PJSUA（pjsua）スタックに設定として反映され、ICE ネゴシエーションが実機で成立すること。実 PJSIP への設定反映は pjsua-native feature（Layer 3+）で検証する。
  - [Reason] 現在のバックエンドは MockBackend（src/runtime/reactor.rs:75 の `Box::new(MockBackend::new())`）であり、pjsua-native feature が未導入（62.2 バックエンド選択機構のスコープ）。PJSIP スタックを起動できない本ビルドでは実設定反映を実行できず、この項目はテスト不能（cannot test）である。ただしこれは「決定的だが原理的にテスト不能」な設計欠陥（architectural defect / design defect）ではなく、pjsua-native feature というテスト可能な経路が別チケット（62.2 / Layer 3+ SIP Integration）に存在するスコープ制約である（not a design defect）。
  - [Alternative verification] Layer 1/2 で代替検証: (a) §13 の型形状（StunServerConfig{uri} / TurnServerConfig{uri,username,password,transport} / IceConfig 既定値）を UT で検証。(b) §42 validate() の全拒否経路を UT で検証。(c) SipClient::new が RFC 型を受理し ClientInitialized を publish することを IT で検証。pjsua-native feature 実装後に Layer 3 統合テストで実設定反映を検証する。

### Plan Test Code (concrete code)

- UT: [C070-PRE] §62 親セクションが存在する（impl_integration_design.rs に ConfigUnification が定義される）
  ```rust
  use crate::architecture::impl_integration_design::{
      DesignDecisionId, breaking_change_order,
  };
  let order = breaking_change_order();
  assert!(
      order.contains(&DesignDecisionId::ConfigUnification),
      "62.1 ConfigUnification must be in the §62 breaking-change order"
  );
  ```
- UT: [C070-POST] 62.1 が §62 の一部として記述される（client_config_unify.rs に §62.1 設計レコードが実装される）
  ```rust
  use crate::config::client_config_unify::ConfigUnificationDecision;
  use crate::architecture::impl_integration_design::DesignDecisionId;
  let decision = ConfigUnificationDecision::record();
  assert_eq!(decision.section(), "62.1");
  assert_eq!(decision.decision(), DesignDecisionId::ConfigUnification);
  ```
- UT: [C070-INV] 設定一本化は §10/§13 と矛盾しない（ClientConfig が §10、ICE/STUN/TURN が §13 に一致）
  ```rust
  let config = ClientConfig::default();
  assert!(config.validate().is_ok());
  assert_eq!(config.transports.len(), 2);              // §10.1 transports=[udp(5060),tcp(5060)]
  assert_eq!(config.stun_servers.len(), 0);            // §10.1 stun_servers=[]
  assert_eq!(config.turn_servers.len(), 0);            // §10.1 turn_servers=[]
  assert_eq!(config.ice.max_host_candidates, 16usize); // §13
  ```
- UT: [C080-PRE] N0013 が §10 ClientConfig を定義する（client_config_spec.rs の全フィールドと既定値）
  ```rust
  let config = ClientConfig::default();
  assert_eq!(config.user_agent, "tauri-siprs/0.1");
  assert_eq!(config.log_level, LogLevel::Info);
  assert_eq!(config.max_calls, 32);
  assert_eq!(config.event_bus_capacity, 2048);
  assert_eq!(config.raw_sip_event_capacity, 4096);
  assert_eq!(config.transports.len(), 2);
  assert!(config.stun_servers.is_empty());
  assert!(config.turn_servers.is_empty());
  ```
- UT: [C080-POST] 62.1 が ClientConfig 一本化を規定する（lib.rs が RFC §10 型を唯一の公開型として再公開）
  ```rust
  fn assert_same_type<T>(_: T, _: T) {}
  let a: siprs::ClientConfig = crate::config::ClientConfig::default();
  let b: crate::config::client_config_spec::ClientConfig = crate::config::ClientConfig::default();
  assert_same_type(a, b);
  ```
- UT: [C080-INV] RFC §10 型が唯一の公開型として残る（旧 config.rs ClientConfig / sip_proxy_host が消滅）
  ```rust
  let needle = "sip_".to_string() + "proxy_host";
  let violations = find_identifier_outside_comments(&needle)?;
  assert!(violations.is_empty(), "legacy sip_proxy_host must be gone: {violations:?}");
  ```
- UT: [C081-PRE] N0015 が §12/§13 の transport/ICE/STUN/TURN を定義する（transport_ice_spec.rs の §13 形状）
  ```rust
  let stun = StunServerConfig { uri: "stun.example.com:3478".into() };
  let turn = TurnServerConfig {
      uri: "turn.example.com:3478".into(),
      username: Some("user".into()),
      password: Some(SecretString::new("pass")),
      transport: TurnTransport::Udp,
  };
  assert_eq!(stun.uri, "stun.example.com:3478");
  assert_eq!(turn.uri, "turn.example.com:3478");
  ```
- UT: [C081-POST] 62.1 が StunServerConfig/TurnServerConfig 一元化と Vec 化を規定する（turn_servers が STUN 型でない）
  ```rust
  let config = ClientConfig::default();
  let _stuns: &[StunServerConfig] = &config.stun_servers;
  let _turns: &[TurnServerConfig] = &config.turn_servers;
  // type-bug fix: TurnServerConfig and StunServerConfig are distinct types.
  // Compiling `let _wrong: &[StunServerConfig] = &config.turn_servers;` would FAIL.
  ```
- UT: [C081-INV] 二重定義が解消され §13 既定値と一致する（IceConfig 既定値 + 単一 StunServerConfig 定義）
  ```rust
  let ice = IceConfig::default();
  assert!(ice.enabled, "§13: enabled=true");
  assert!(ice.aggressive_nomination, "§13: aggressive_nomination=true");
  assert!(!ice.trickle_ice, "§13: trickle_ice=false");
  assert!(!ice.renomination, "§13: renomination=false");
  assert_eq!(ice.max_host_candidates, 16usize, "§13: max_host_candidates=16");
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| lib.rs は旧 config.rs 型を公開 | lib.rs が client_config_spec 版 ClientConfig を唯一の公開型として再公開 | 公開 ClientConfig の RFC §10 型への昇格 |
| src/config.rs に旧 ClientConfig / StunServerConfig が存在 | 旧型を完全削除 | 二重定義の排除 |
| client_config_spec.rs / transport_ice_spec.rs に単一型・旧既定値 | stun_servers/turn_servers の Vec 化と §13 ICE 既定値への一致 | STUN/TURN/ICE 型の一元化 |
| SipClient::new が旧 config 型を受理 | SipClient::new が RFC 型を受理し validate() を fail-fast 実行 | 公開 API のシグネチャ移行 |
| IceConfig{max_host_candidates:u8}, default enabled=false/aggressive=false/max_host_candidates=5 | IceConfig{max_host_candidates:usize}, default enabled=true/aggressive=true/max_host_candidates=16 (RFC §13) | §13 ICE defaults + usize |
| StunServerConfig{host,port} | StunServerConfig{uri:String} (RFC §13) | §13 STUN shape unification |
| TurnServerConfig{host,port,username:String,password:SecretString,transport} | TurnServerConfig{uri,username:Option<String>,password:Option<SecretString>,transport} (RFC §13) | §13 TURN shape unification |
| ClientAudioConfig.default_delivery_format:String placeholder | AudioFormat{Hz16000,I16,StereoInOut,20} (§10.1) | AudioFormat type resolution (P4-2 type) |
| validate() had no sample-rate check | validate() adds §42 sample-rate rule (defensive: closed SampleRate enum) | §42 validation |
| config.rs defined legacy ClientConfig/StunServerConfig/ClientConfigBuilder/LogLevel/consts | legacy definitions deleted; client_config_spec/transport_ice_spec types re-exported at crate::config::ClientConfig | ConfigUnification (RESIDUE R1 resolved) |
| lib.rs re-exported legacy config types (ClientConfig from config.rs, ClientConfigBuilder, old StunServerConfig) | lib.rs re-exports RFC §10 ClientConfig/LogLevel + §13 StunServerConfig/TurnServerConfig/IceConfig; collision comment removed | Public API migration to RFC types |
| reactor BootConfig{config}; dtmf_sent_timeout_ms read from config.dtmf.sent_timeout_ms | BootConfig{config, dtmf_sent_timeout_ms}; sourced from DtmfConfig::default().sent_timeout_ms at SipClient::new | DtmfSent timeout as reactor boot parameter |
| client_config_unify.rs: pub struct Config{} + [::STUB::] P15-2 | ConfigUnificationDecision record + C070 tests; STUB resolved and removed | N0070 §62.1 implementation |
| impl_integration_design evidence_matches_actual_source asserted legacy config presence | asserts R1 resolved (legacy gone, RFC re-export present), R2/R3 still open | evidence test updated for resolved R1 |
| examples/common/cli.rs built ClientConfig via builder (sip_proxy_host/port/stun_server) | builds ClientConfig::default() + stun_servers from --stun (RFC §13) | example CLI migrated to RFC type |
| tests/examples used ClientConfig::builder() and sip_proxy_host/port | use ClientConfig::default() (RFC type) | test adaptation across public_api_design, standalone_server_config, verify_spec_* |
| run_server relied on ClientConfig::default() failing validate (empty sip_proxy_host) | run_server validates ServerConfig.auth (Jwt requires secret) before startup; test updated | deterministic startup failure path after RFC ClientConfig became valid |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) transport_ice_spec.rs の IceConfig/StunServerConfig/TurnServerConfig を §13 形状・既定値へ変更し、既存テスト（ice_config_default_values / stun_server_config_constructs / turn_server_config_constructs）を §13 値へ更新。(2) client_config_spec.rs の default_delivery_format を String → AudioFormat へ変更（§10.1 既定値 AudioFormat{Hz16000,I16,StereoInOut,20}）し、validate() に §42 sample-rate 検査を追加。(3) config.rs から旧 ClientConfig / StunServerConfig / ClientConfigBuilder / LogLevel 定義と旧 validate / consts（MIN_SIP_PORT/MAX_SIP_PORT/MAX_USER_AGENT_BYTES）を削除し、client_config_spec / transport_ice_spec 型を config ルートに再エクスポート。DtmfConfig / AuthCredentials は維持。(4) lib.rs の公開再エクスポートを RFC 型へ変更（ClientConfig/LogLevel→client_config_spec、StunServerConfig→transport_ice_spec、ClientConfigBuilder/旧StunServerConfig 除去、衝突コメント解消）。(5) client.rs の SipClient::new #[instrument] フィールドと旧ビルダー依存テストを RFC 型構築（struct リテラル + ..Default::default()）へ移行。(6) reactor.rs の BootConfig に dtmf_sent_timeout_ms: u64 を追加し、boot_config.config.dtmf.sent_timeout_ms 参照を移行。テスト 1606-1607 を更新。(7) impl_integration_design.rs の evidence_matches_actual_source テストを R1 解決後の状態へ更新。(8) client_config_unify.rs の STUB を解決（§62.1 型付き設計レコード + 検証テスト）。(9) make test で全テスト green を確認。
- [Risks] RFC ClientConfig に dtmf フィールドが無いため reactor.rs:98 の dtmf_sent_timeout_ms 参照が壊れる（BootConfig への移動で解決するが、O-002 のユーザー設定可能な DtmfSent タイムアウトが一時的にデフォルト固定になる）。旧ビルダー API（ClientConfig::builder()）に依存するテスト（client.rs / standalone_server_config.rs / public_api_design.rs）が一斉に壊れる。impl_integration_design.rs の evidence テストが旧型存在を前提としており削除後に壊れる。
- [Caveats] §42 の sample-rate 検査は SampleRate が閉じた enum（4 バリアント全て有効）のため型レベルで保証済みで、検査自体は到達不能（防御的）。resampler_quality は String プレースホルダーのまま（ResamplerQuality 型が未実装）。StunServerConfig が host/port → uri へ変わるため既存コンシューマの移行が必要。StunServerConfig の uri 形式（"stun.example.com:3478"）のパースは実装時に検討。
- [Open items] O-002 DtmfSent タイムアウトのユーザー設定可能な再導入経路（DtmfPolicy 側か別設定か）。ResamplerQuality 型の実装チケット。client_config_unify.rs の具体的構成（型付き設計レコード vs 配線ロジック）は実装フェーズで確定。
- [Future improvements] 62.2 バックエンド選択で MockBackend が消えた後、pjsua-native feature で実設定反映を Layer 3+ で検証。62.3 イベントバス一元化で EventBus::new(config.event_bus_capacity, raw_sip_capacity) に移行。
Implementation summary (P15-2):
- Changed files: src/config/transport_ice_spec.rs, src/config/client_config_spec.rs, src/config.rs, src/lib.rs, src/client.rs, src/runtime/reactor.rs, src/config/client_config_unify.rs, src/architecture/impl_integration_design.rs, src/api/standalone_server_config.rs, src/api/public_api_design.rs, examples/common/cli.rs, tests/verify_spec_p8_2.rs, tests/verify_spec_p9_1.rs, tests/verify_spec_p9_2.rs, tests/verify_spec_p10_1.rs
- Key changes: RFC §10 ClientConfig promoted to sole public config type (lib.rs re-export); legacy config.rs ClientConfig/StunServerConfig/ClientConfigBuilder/LogLevel deleted; StunServerConfig{uri}/TurnServerConfig{uri,Option fields}/IceConfig{usize, §13 defaults} unified in transport_ice_spec; ClientAudioConfig.default_delivery_format resolved to AudioFormat; §42 sample-rate validation added; BootConfig carries dtmf_sent_timeout_ms (RFC ClientConfig has no dtmf); client_config_unify.rs implements §62.1 ConfigUnificationDecision (STUB resolved); impl_integration_design evidence test updated for resolved R1.
- Test results: make test exit 0, 1124 passed across 17 test binaries; cargo check --all-targets exit 0.
- Quality notes: run-quality-checks on changed files reports pre-existing findings (test-code unwrap/expect ~47, hardcoded server-test ports, commented-code false positives). Zero new findings introduced by P15-2; single-letter var in touched file fixed; 4 hardcoded port literals replaced with DEFAULT_SIPRS_PORT. Remaining findings predate P15-2 and are permitted-in-context or require API changes; left for user approval per Zero Tolerance exception.
Additional caveat (P15-2): `runtime::audio_worker::tests::spawn_log_carries_call_id_field` is a pre-existing flaky test — it fails intermittently in the full-suite run (test-ordering dependent on the global tracing subscriber state) but passes in isolation and on re-run. It is unrelated to P15-2 (audio_worker.rs was not modified). make test final state: 1124 passed, exit 0.
Review report (P15-2):
- Status gate: passed (done)
- Crimes: 0 open (1 false_positive: resampler_quality String placeholder, ResamplerQuality type unimplemented)
- STUBs: P15-2 TS-001 resolved; 7 remaining stubs owned by other tickets (P15-3..P15-9), correctly deferred
- No-excuse gate: 7/7 pass
- Annotation verify: 15 files, 0 missing, 0 AMBIGUOUS
- Compilation: cargo check --all-targets exit 0
- Tests: make test exit 0, 1124 passed (spawn_log_carries_call_id_field flaky — passes in isolation, unrelated to P15-2)
- Implementation completeness: C070/C080/C081 all satisfied (ConfigUnificationDecision record, RFC §10 ClientConfig sole public type, §13 STUN/TURN/ICE unified + defaults)
- Static quality: 55 findings, all pre-existing or intentional (test unwraps permitted by project rust rules; production .expect() on static parses technically unfixable without API change; port constant definition/clap default/override-test literals; commented-code false positives). P15-2 added 0 new findings. Per Zero Tolerance exception, these require user approval to skip.
- Translatability: no issues in new code; Boy Scout improvements applied (single-letter var fixed, hardcoded ports → DEFAULT_SIPRS_PORT constant, panic-based builder removed, AudioFormat placeholder resolved, lib.rs collision comment removed)
- Issues found and fixes applied: 1 single-letter var + 4 hardcoded ports fixed during start; 5 missing annotations re-injected during review.

## P15-2 — implemented at 49 locations

### examples/common/cli.rs

- Line 243
```rust
    fn build_client_config_maps_stun_to_stun_servers() {
```

### src/api/public_api_design.rs

- Line 220
```rust
        fn assert_clone<T: Clone>() {}
```

- Line 229
```rust
        fn assert_debug<T: std::fmt::Debug>() {}
```

- Line 238
```rust
        fn assert_send<T: Send>() {}
```

- Line 335
```rust
        fn assert_sync<T: Sync>() {}
```

- Line 350
```rust
        fn assert_result_type(_: &Result<RegistrationState, SipError>) {}
```

### src/api/standalone_server_config.rs

- Line 758
```rust
    fn test_server_config_struct_fields() {
```

- Line 774
```rust
    fn test_server_config_send_sync() {
```

- Line 778
```rust
        fn assert_sync<T: Sync>() {}
```

- Line 796
```rust
    fn test_server_config_default_values() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 910
```rust
        fn test_from_args_default_port() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 974
```rust
        fn test_from_args_rejects_invalid_bind_addr() {
```

- Line 1044
```rust
            fn assert_send<T: Send>() {}
```

- Line 1046
```rust
            fn assert_sync<T: Sync>() {}
```

### src/architecture/impl_integration_design.rs

- Line 387
```rust
    fn evidence_matches_actual_source() -> Result<(), std::io::Error> {
```

### src/client.rs

- Line 79
```rust
impl SipClient {
```

- Line 503
```rust
    async fn sip_client_rejects_invalid_config_fail_fast() {
```

- Line 539
```rust
        fn assert_send<T: Send>() {}
```

- Line 541
```rust
        fn assert_sync<T: Sync>() {}
```

- Line 670
```rust
        fn assert_call_state_result(_: &Result<Vec<crate::runtime::state::CallEntry>, SipError>) {}
```

- Line 677
```rust
        fn assert_shutdown_result(_: &Result<(), SipError>) {}
```

### src/config.rs

- Line 115
```rust
pub use client_config_spec::{
```

- Line 119
```rust
pub use transport_ice_spec::{
```

- Line 125
```rust
pub use transport_ice_spec::TlsConfig;
```

- Line 159
```rust
    fn dtmf_config_default_sent_timeout() {
```

### src/config/client_config_spec.rs

- Line 70
```rust
impl Default for ClientAudioConfig {
```

- Line 73
```rust
    fn default() -> Self {
```

- Line 197
```rust
impl ClientConfig {
```

- Line 311
```rust
    fn client_config_timeout_defaults() {
```

- Line 324
```rust
    fn client_audio_config_defaults() {
```

- Line 346
```rust
    fn client_config_accepts_all_sample_rates() {
```

### src/config/client_config_unify.rs

- Line 36
```rust
impl ConfigUnificationDecision {
```

- Line 63
```rust
    fn config_unification_decision_is_62_1() {
```

- Line 72
```rust
    fn config_unification_decision_id_is_known_to_parent() {
```

### src/config/transport_ice_spec.rs

- Line 131
```rust
impl Default for IceConfig {
```

- Line 134
```rust
    fn default() -> Self {
```

- Line 261
```rust
    fn ice_config_default_values() {
```

- Line 305
```rust
    fn stun_server_config_constructs() {
```

- Line 317
```rust
    fn turn_server_config_constructs() {
```

### src/lib.rs

- Line 103
```rust
// [::TICKET::] P15-2 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-2 --for-spec --no-implementation-order`.
```

- Line 104
```rust
// [::TICKET::] P15-2: RFC §10 ClientConfig promoted to the only public config type.
```

### src/runtime/reactor.rs

- Line 43
```rust
impl Default for BootConfig {
```

- Line 45
```rust
    fn default() -> Self {
```

- Line 74
```rust
impl CoreReactor {
```

### tests/verify_spec_p10_1.rs

- Line 22
```rust
fn test_config() -> ClientConfig {
```

### tests/verify_spec_p8_2.rs

- Line 27
```rust
// [::TICKET::] P15-2 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-2 --for-spec --no-implementation-order`.
```

- Line 28
```rust
    let config = ClientConfig::default();
```

### tests/verify_spec_p9_1.rs

- Line 47
```rust
fn client_config() -> ClientConfig {
```

### tests/verify_spec_p9_2.rs

- Line 21
```rust
fn client_config() -> ClientConfig {
```
