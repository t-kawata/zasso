# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P15-6: Call API expansion — answer/hangup/hold/unhold/transfer/send_dtmf/call_state(call_id), calls() rename

**Ticket Key**: P15-6 · **Phase**: 15

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
RFC §19/§20 の通話制御 API を SipClient に追加し、answer/hangup/hold/unhold/transfer/send_dtmf/call_state(call_id) を非同期 facade から利用可能にする（§62.5 公開 API 拡充）。

### Purpose
公開 API を RFC 型に揃える（RESIDUE 根因 #1 の解消）。現行 call_state() のシグネチャ不整合（Vec<CallEntry> を返す）を calls() に改名して解消し、§19 の正名 call_state(call_id) を新設する。reject を独立 API にせず answer(call_id, 486/603) で実現する。

### Motivation
zasso が発着信・保留・転送・DTMF を操作するには通話制御 API が必須。P15 フェーズで §62 の 9 設計判断（公開 API 非RFC型の解消）を順次実施しており、62.5 は通話 API 群の公開面拡充。現行 call_state() は RFC の §19 call_state(call_id) とシグネチャが乖離しており、API 整合の観点で改名が必要。

### Constraints
RuntimeCommand の call_id は既存 u64 規約を維持（facade 境界で CallId→u64 変換）。新規依存クレートなし。TDD Red-Green-Refactor 必須。P15-4 の単一 EventBus トポロジを維持。Layer 2 は TestBackend で決定論的に検証し、実シグナリングは Layer 3+（pjsua-native）。

## Scope

- SipClient に §19/§20 の通話 API を追加する（§62.5）: answer(call_id, code) / hangup(call_id, reason) / hold / unhold / transfer / send_dtmf / call_state(call_id)。
- answer は §19.1 の 180/183/200/486/603 を受理する（486=Busy, 603=Decline。reject は独立 API にせず answer(call_id, 486/603) で実現）。
- 現行 call_state() -> Vec<CallEntry> を calls() に改名し、§19 の正名 call_state(call_id) -> Result<CallState, SipError> を新設する。
- unsubscribe は明示 API を追加せず、broadcast Receiver の drop による購読解除を README に明文化する。
- **Scope of changes:**
  - [Path] src/api/call_api_expansion.rs（N0074 ターゲット、stub 解消）、src/api/call_api_semantics.rs（validate_answer_code 追加）、src/client.rs（SipClient 通話 API 7 メソッド追加 + call_state→calls 改名）、src/runtime/command.rs（RuntimeCommand::Answer/Transfer 追加、Hangup に reason 追加）、src/runtime/handle.rs（call_state(call_id) typed query 追加）、src/runtime/reactor.rs（Answer/Transfer/Hangup(reason) の dispatch + CallConnected/decline publish）、src/runtime/backend.rs（TestBackend の answer/hangup/transfer 記録）、src/lib.rs / src/api/mod.rs（必要に応じ再 export）、README（calls 改名 + drop unsubscribe 明文化）
  - [Action] add（7 メソッド）／modify（RuntimeCommand::Hangup に reason 追加）／rename（call_state→calls）／remove（空 trait Service と [::STUB::] P15-6）
  - [Detail] SipClient に answer(call_id, code) / hangup(call_id, reason) / hold(call_id) / unhold(call_id) / transfer(call_id, target) / send_dtmf(call_id, digits, method) / call_state(call_id) を追加し、旧 call_state() を calls() に改名。validate_answer_code を call_api_semantics.rs に新設（180/183/200/486/603）。
  - [Before → After] call_state() -> Result<Vec<CallEntry>, SipError> → calls() に改名 + call_state(call_id) -> Result<CallState, SipError> を新設。answer/hangup/hold/unhold/transfer/send_dtmf が不在 → 追加。RuntimeCommand::Hangup{call_id} → {call_id, reason}。
  - [API contract] SipClient の public メソッドが 14 → 21 件に増加（API surface テスト更新）。破壊的変更: call_state() 呼び出しは calls() へ移行。
  - [Data schema] CallEntry フィールドは不変（C046）。RuntimeCommand::Hangup に reason: HangupReason を追加。
  - [Config/env] 変更なし（新規 config フィールドなし、環境変数なし）。
  - [Dependency] 追加なし（既存クレートのみ）。
- **Out of scope:**
  - [Excluded item] 明示的な unsubscribe() API の追加
  - [Why excluded] broadcast Receiver の drop による購読解除を README に明文化する設計（§8.3）を採用するため（unsubscribe は明示 API を追加せず、drop 依存を README に明文化する）
  - [Excluded item] 実 INVITE/DTMF/REFER のネイティブシグナリング実装（pjsua-native Layer 3+）
  - [Why excluded] FFI 実シグナリングは pjsua-native feature 有効ビルドでのみ動作し、本チケットは Layer 2 の公開 API 拡充と TestBackend 検証をスコープとする
  - [Excluded item] CallEntry.state の String → CallState enum 化（型強化）
  - [Why excluded] C046 field set 維持の制約下で別チケットの型リファクタリング対象（本チケットは call_state(call_id) の写像で対応）
- **Affected areas:**
  - [Affected component] SipAccountHandle::make_call（返却 CallId が新 call API の入力になる）
  - [Nature of impact] API 面 — make_call で得た CallId を answer/hangup/hold/unhold/transfer/send_dtmf/call_state に渡す利用フローが成立する
  - [Corresponding change] Yes — README に make_call → answer/hangup の利用例を追記
  - [Affected component] client.rs API surface テスト
  - [Nature of impact] API 面 — call_state の改名と新メソッド追加でメソッド数・列挙が変わる
  - [Corresponding change] Yes — テスト更新（P15-6 で実施）
  - [Affected component] イベント購読者（subscribe() / subscribe_account()）
  - [Nature of impact] イベント — answer(200) で CallConnected が新規 publish される経路が増える
  - [Corresponding change] No — 既存イベント型のまま追加 publish のみ

## STUBs — Must Be Fully Implemented in This Ticket (1 items — resolved: 1)

### TS-001 — `src/api/call_api_expansion.rs — resolved

- **Marker**: `// [::TICKET::] P15-6 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P15-6 --for-spec --no-implementation-order`.`
- **Contracts**: C074, C086
- **Resolution Plan**: RESOLVED: replaced `pub trait Service {}` + [::STUB::] marker in src/api/call_api_expansion.rs with the §62.5 call-control helpers: CALL_API_METHODS const, call_state_from_entry_state (CallEntry.state String → 13-state CallState), answer_state_string (code → state). Added validate_answer_code to call_api_semantics.rs (ANSWER_CODES 180/183/200/486/603); RuntimeCommand::Answer/Transfer + Hangup{reason}; DispatchCommand dedicated Answer/Hangup/Transfer; RuntimeHandle::submit_answer/submit_hangup/submit_transfer/call_state; SipClient answer/hangup/hold/unhold/transfer/send_dtmf/call_state + calls() rename; reactor handle_answer/handle_hangup/handle_transfer publishing CallConnected/CallDisconnected; TestBackend answer_calls/hangup_calls/transfer_calls recorders. Verified by make test: 1170 tests green.

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0074`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0074` | api_contract | rust | 62.5 公開 API 拡充（通話 API 群） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| extends | ★ 62.5 公開 API 拡充（通話 API 群） (N0074) | → | ☆ §19 Call API & Answer Semantics (N0027) |
| part_of | ★ 62.5 公開 API 拡充（通話 API 群） (N0074) | → | ☆ §62 実装整合設計 — RESIDUE 解消のための設計判断確定 (N0068) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0074` | `src/api/call_api_expansion.rs` |

## Investigation

- src/api/call_api_expansion.rs（N0074 ターゲット）は現状 `pub trait Service {}` と `[::STUB::] P15-6` のみの空ファイル（23 行）。
- src/api/call_api_semantics.rs（N0027）に CallApiSemantics trait（answer/hangup/hold/unhold/transfer/send_dtmf/call_state）が既に定義済み（P9-3）。validate_dtmf_digits / validate_dtmf_send_method は存在するが validate_answer_code は未実装。
- src/client.rs:345 に `call_state() -> Result<Vec<CallEntry>, SipError>` が存在 — これを calls() に改名し、新 call_state(call_id) を新設する。client.rs:1100 の API surface テストが SipClient の public メソッド 14 件（"call_state" 含む）を列挙しており更新必須。
- RuntimeCommand（src/runtime/command.rs）は Hangup{call_id:u64}/Hold/Unhold/SendDtmf を持つが、Answer / Transfer が無く、Hangup に reason フィールドが無い。DispatchCommand::from_runtime_command は Hangup/Hold/Unhold を Execute クロージャ（backend.hangup/hold/unhold）へ写像。
- SipBackend trait（src/runtime/backend.rs:63-100）は answer_call(native_call_id, code) / hangup(native_call_id) / transfer_call(native_call_id, target) / hold / unhold / send_dtmf を既に定義。TestBackend は hold_calls/unhold_calls を記録するが answer/hangup/transfer 記録は無い（追加が必要）。
- RuntimeHandle（src/runtime/handle.rs）は query_state() / submit_make_call / submit_add_account を持つが call_state(call_id) の typed query は無い。query_state は ClientState（calls: BTreeMap<CallId, CallEntry>）を返す。
- 公開 CallState は src/state/call_state_model.rs の 13 状態 enum。CallEntry.state は String プレースホルダ（C046 field set: id/native_id/account_id/state/media）。
- SipErrorKind に CallNotFound / InvalidArgument / NativeError / ShutdownInProgress が存在。SipError::invalid_argument / not_found ヘルパーあり。
- DesignDecisionId::CallApiExpansion が impl_integration_design.rs に定義済み（section()="62.5", label()="62.5 公開 API 拡充（通話 API 群）", BREAKING_CHANGE_ORDER に含まれる）。
- イベント: SipEventPayload::CallConnected(ConnectedCallInfo) / CallDisconnected / DtmfSent(DtmfSentInfo) / IncomingCall(IncomingCallInfo)。reactor は convert_call_state (m20_callstate_mapping) で CONFIRMED→CallConnected を publish（P12-8 で CallDirection 導入）。
- CallId は NonZeroU64 newtype（model/id_design_newtype.rs）: from_u64(0)=Err、.get() で NonZeroU64。

## Acceptance Criteria

- **[Happy path] — 完了条件:** `make test` が全緑。SipClient に answer/hangup/hold/unhold/transfer/send_dtmf/call_state/calls が存在し、make_call → answer(call_id, 200) → CallConnected publish → calls() に Active 反映 → hangup(call_id, LocalUser) → CallDisconnected の一連が TestBackend 上で決定論的に検証できる。
- **[Error case] — エラー処理:** answer(call_id, 404) が Err(InvalidArgument) を返し RuntimeCommand を submit しない（fail-fast）。call_state(unknown) が Err(CallNotFound)。terminated reactor への全コマンドが Err に写像され hang しない。
- **[Edge case] — 境界:** answer(call_id, 486/603) で decline が成立。send_dtmf の不正 digits が Err(InvalidArgument)。calls() が 0 通話で空 Vec。validate_answer_code が 180/183/200/486/603 の 5 値のみ受理（179/604/u16 境界を拒否）。

## Invariants

- [Normal condition] 入力は API 境界で検証される — validate_answer_code が 180/183/200/486/603 のみを Ok にし、send_dtmf は validate_dtmf_digits を通過した digits のみ submit される。各コマンドの call_id は実在する通話を指す（実在しない場合は CallNotFound）。
- [Error invariant] fail-fast — 検証失敗（不正 code / 不正 digits / 不正 call_id）は RuntimeCommand を submit せず Err を返し、ClientState.calls は不変・reactor は生存する。
- [Internal state invariant] RuntimeCommand の reply は全経路で必ず 1 回送信される。ClientState.calls が authoritative（C021）であり、calls()/call_state(call_id) はイベントストリームでなく reactor 状態を読む。CallEntry のフィールド集合（C046）は不変。
- [Boundary invariant] CallId は NonZeroU64（from_u64(0)=Err）。answer code は u16 で、受理集合は正確に 5 値（180/183/200/486/603）。calls() は 0 通話で空 Vec。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C074 — N0074→N0068

- **Precondition**: §62 親セクションが存在する — DesignDecisionId::CallApiExpansion が src/architecture/impl_integration_design.rs に定義され、BREAKING_CHANGE_ORDER の 8 設計判断の一部として section()="62.5" を返す
- **Postcondition**: 62.5 が §62 の一部として記述される — DesignDecisionId::CallApiExpansion::label() が「62.5 公開 API 拡充（通話 API 群）」を返し、specs/P15-6.md が 62.5 節として通話 API 群（answer/hangup/hold/unhold/transfer/send_dtmf/call_state(call_id)/calls）を規定する
- **Invariant**: 公開 API 拡充は §19/§20 と整合する — SipClient の通話 API メソッド集合が RFC N0027（§19/§20）の 62.5 追加規定と一致し、validate_answer_code が §19.1 の受理コード（180/183/200/486/603）のみを許可する

### C086 — N0074→N0027

- **Precondition**: N0027 が §19 発着信 API 詳細を定義する — src/api/call_api_semantics.rs に CallApiSemantics trait（N0027 実装）が存在し、§19.1 の受理コード 180/183/200/486/603 と incoming 以外の answer 拒否（InvalidState）が定義済みである
- **Postcondition**: 62.5 が answer/hangup/hold/unhold/transfer/call_state(call_id) 追加を規定する — SipClient に 7 メソッド（answer/hangup/hold/unhold/transfer/send_dtmf/call_state）が追加され、旧 call_state() が calls() に改名され、unsubscribe は drop 依存を README に明文化する
- **Invariant**: answer が 180/183/200/486/603 を受理する — validate_answer_code が 180/183/200/486/603 を Ok(()) に、その他の code を Err(InvalidArgument) にする（table-driven 全網羅）

## Boy Scout Rule

- src/api/call_api_expansion.rs の空 trait `pub trait Service {}` と [::STUB::] P15-6 を解消し、実装コード（§62.5 の公開 API 契約）に置き換える（stub 解消が第一）。
- 旧 call_state() -> Vec<CallEntry> を calls() に改名し、per-call 参照を call_state(call_id) として分離する — 「関数は動詞・構造体は名詞」の原則に従い、全通話一覧クエリ（名詞的: calls）と個別状態参照（動詞的: call_state）を明確に区別する。
- validate_answer_code を独立した純関数として抽出し、受理コード集合を名前付き定数（ANSWER_CODES）に集約する — ハードコード値の定数化。
- RuntimeCommand::Answer / Transfer / Hangup{reason} の追加に伴い、DispatchCommand::from_runtime_command の match を網羅的に更新する（catch-all 禁止）。
- client.rs の API surface テストのメソッド列挙順を impl 定義順と一致させる（可読性 = 翻訳可能性）。
- 公開 API のシグネチャが RFC 設計（N0074 スケッチ）と一致することを確認し、RuntimeCommand の u64 変換は facade 境界の名前付きヘルパー（call_id.get() の 1 点集中）で行い散在を防ぐ。

## Test Plan

### Unit Tests

- validate_answer_code が 180/183/200/486/603 を受理し、その他を拒否すること。
- call_state(call_id) が per-call の CallState を返すこと。
- calls() が全 CallEntry の一覧を返すこと。
- **UT: [Normal] — 正常系テストシナリオ:**
  - UT: [Normal] validate_answer_code(180|183|200|486|603) がそれぞれ Ok(()) を返す — §19.1 の 5 受理コードを table-driven で全件検証（call_api_semantics.rs に新設する純関数）。
  - UT: [Normal] SipClient::answer(call_id, 200) が validate_answer_code 通過後に RuntimeCommand::Answer { call_id, code: 200 } を submit し Ok(()) を返す（facade が CallId → u64 へ変換）。
  - UT: [Normal] SipClient::hangup(call_id, HangupReason::LocalUser) が RuntimeCommand::Hangup { call_id, reason: LocalUser } を submit し Ok(()) を返す（RuntimeCommand::Hangup に reason フィールドを追加）。
  - UT: [Normal] SipClient::hold(call_id) / unhold(call_id) が RuntimeCommand::Hold / Unhold を submit し Ok(()) を返す。
  - UT: [Normal] SipClient::transfer(call_id, "sip:bob@example.com") が RuntimeCommand::Transfer { call_id, target } を submit し Ok(()) を返す。
  - UT: [Normal] SipClient::send_dtmf(call_id, "123#*ABCD", DtmfMethod::Rfc2833) が RuntimeCommand::SendDtmf を submit し Ok(()) を返す — 戻り値は「PJSIP がコマンドを受理した」ことのみを意味し、DtmfSent は二相タイムアウトで監視する（§20）。
  - UT: [Normal] SipClient::calls() が旧 call_state() の改名であり、authoritative ClientState.calls の全 CallEntry を Vec で返す（query_state 経由, C021）。
  - UT: [Normal] SipClient::call_state(call_id) が既知 call_id の §18 13 状態 CallState enum を返す（CallEntry.state からの写像）。
  - UT: [Normal] DispatchCommand::from_runtime_command が Answer → Execute{ backend.answer_call(call_id, code) }、Transfer → Execute{ backend.transfer_call(call_id, target) }、Hangup → Execute{ backend.hangup(call_id) } へ変換する（reason は facade/reactor 層で記録）。
  - UT: [Normal] DesignDecisionId::CallApiExpansion::section() == "62.5" かつ label() == "62.5 公開 API 拡充（通話 API 群）" かつ BREAKING_CHANGE_ORDER に含まれる — §62 親セクションが存在する（impl_integration_design.rs の typed design data）。
- **UT: [Error] — エラー処理テストシナリオ:**
  - UT: [Error] validate_answer_code(0|100|404|500|604|65535) が Err(SipErrorKind::InvalidArgument) を返す — 非受理コードは全て拒否。
  - UT: [Error] SipClient::answer(call_id, 404) が Err(InvalidArgument) を返し RuntimeCommand を submit しない（fail-fast: validate_answer_code が dispatch 前の API 境界入力検証を担う）。
  - UT: [Error] SipClient::call_state(unknown_call_id) が Err(SipErrorKind::CallNotFound) を返す。
  - UT: [Error] SipClient::answer/hangup/hold/unhold/transfer/send_dtmf が terminated reactor で Err を返す（ReactorError::ReactorDown → SipError へ写像、hang しない）。
  - UT: [Error] SipClient::send_dtmf(call_id, "1x2", method) が Err(InvalidArgument) を返し submit しない（validate_dtmf_digits が dispatch 前に拒否）。
  - UT: [Error] call_state(call_id) が CallId::from_u64(0) の不正 id で Err(InvalidArgument) を返す（CallId NonZeroU64 不変条件）。
- **UT: [Boundary] — 境界値テストシナリオ:**
  - UT: [Boundary] validate_answer_code 境界: 最小受理 180（provisional）、最大受理 603（Decline）; 179 / 604 は拒否; u16::MIN=0 / u16::MAX=65535 は拒否。
  - UT: [Boundary] answer(call_id, 486) と answer(call_id, 603) が両方受理される — 486=Busy, 603=Decline の reject 経路（独立 reject API を追加しない）。
  - UT: [Boundary] calls() が空 ClientState で空 Vec を返す（0 通話の境界）。
  - UT: [Boundary] hangup の全 6 理由（LocalUser/RemoteHangup/NetworkFailure/Busy/Declined/Transfer）が RuntimeCommand::Hangup を通過する。
  - UT: [Boundary] send_dtmf の最小桁 "1" と最大桁集合 "123#*ABCD"（小文字 "abcd" の正規化含む）が受理される。
- **UT: [Invariant] — 不変条件テストシナリオ:**
  - UT: [Invariant → C086 Invariant] answer が 180/183/200/486/603 を受理する — validate_answer_code の受理集合が §19.1 の 5 コードと完全一致し、answer() の唯一の code ゲートである（table-driven 全網羅）。
  - UT: [Invariant → C074 Invariant] 公開 API 拡充は §19/§20 と整合する — SipClient の通話 API メソッド集合（answer/hangup/hold/unhold/transfer/send_dtmf/call_state + calls）が RFC N0027 の 62.5 追加規定と一致し、client.rs の public-method surface テストが更新される。
  - UT: [Invariant] src/api/call_api_expansion.rs の [::STUB::] P15-6 マーカーが解消される — ソーススキャンテストで空 trait `pub trait Service {}` と stub マーカーが存在しないことを assert。
  - UT: [Invariant] RuntimeCommand::Answer/Transfer/Hangup(reason) が DispatchCommand::from_runtime_command で網羅的に match される（コンパイル時 exhaustive）。

### Integration Tests

- TestBackend 上で answer(call_id, 200) → CallConnected が publish されること。
- answer(call_id, 486) で decline が成立すること。
- send_dtmf が RuntimeCommand::SendDtmf を submit し、DtmfSent 二相タイムアウトが成立すること。
- **IT: [Integration point] — モジュール間インターフェース:**
  - IT: [Integration point] SipClient facade → RuntimeHandle::submit / submit_call_state → DispatchCommand → SipBackend（TestBackend）の直列経路で、answer/hangup/hold/unhold/transfer/send_dtmf/call_state/calls が各命令を搬送する。
  - IT: [Integration point] 単一 EventBus（P15-4 §62.3）: reactor の answer / disconnect publish が SipClient::subscribe() の broadcast Receiver に到達する。
  - IT: [Integration point] query_state の authoritative ClientState（C021）: calls() / call_state(call_id) がイベントストリームでなく reactor 状態を読む。
- **IT: [Verification] — 各統合テストの検証内容:**
  - IT: [Verification] make_call（P12-1）→ answer(call_id, 200) で TestBackend::answer_call(200) が呼ばれ、SipEventPayload::CallConnected(ConnectedCallInfo{ call_id, account_id, remote_uri }) が subscribe() で受信される。
  - IT: [Verification] answer(call_id, 486) で TestBackend::answer_call(486) が呼ばれ decline が成立する（Busy Here 受理、通話は Disconnected/Failed へ遷移）。
  - IT: [Verification] send_dtmf(call_id, "5", Rfc2833) で backend.send_dtmf 成功後に P11-6 の二相タイムアウトが spawn され、SipEventPayload::DtmfSent(DtmfSentInfo) が publish される（short タイムアウト注入で決定論検証）。
  - IT: [Verification] hold → unhold が TestBackend::hold_calls / unhold_calls に記録され、呼び出し順が保持される。
  - IT: [Verification] calls() が make_call 後の CallEntry 一覧を返し、answer(200) 後は state が Active 相当に更新される。
- **IT: [Prerequisites] — 統合テストの前提条件:**
  - IT: [Prerequisites] TestBackend が cfg(test) で使用可能（P15-3）: PJSUA 不要の決定性 Layer 2 実行。
  - IT: [Prerequisites] SipClient が単一 EventBus を所有（P15-4）: reactor publish と subscribe() が同一バス。
  - IT: [Prerequisites] tokio テストランタイムと `make test` 経由の実行。
- **IT: [Related tickets] — 関連チケット:**
  - IT: [Related tickets] P15-3（TestBackend / create_backend cfg 分岐）、P15-4（単一 EventBus トポロジ）、P11-6（SendDtmf 二相タイムアウト）、P12-1（make_call / CallId 割当）、P7-2（authoritative query API）、P9-3（CallApiSemantics trait）。

### Exceptions

- **Exception entry:**
  - Item: 実 INVITE / 実 DTMF トーン / 実 REFER 転送の SIP シグナリング成否
  - Reason: pjsua-native feature 有効かつ libpjsua 導入済みビルド（Layer 3+ FFI）でのみ実際の INVITE/DTMF/REFER が送信され、デフォルトの `cargo test`（feature なし）では FFI が stub alias（RFC §28, C058）に置き換わり実シグナリングが発生しない。Layer 2 の Rust テストでは cannot test — これは外部依存（ネイティブ境界）による非テスト可能性であり、not a design defect — 公開 API 自体は TestBackend 経由で全命令経路を決定論テスト可能である。
  - Alternative verification: TestBackend の answer_call / transfer_call / send_dtmf 記録と、reactor が publish する CallConnected / CallDisconnected / DtmfSent イベントを subscribe() で検証する。実 SIP 結合は pjsua-native 有効時の Layer 3+ 統合テストで行う。
- **Exception entry:**
  - Item: DtmfSent 二相タイムアウトの実 PJSUA 上での絶対タイミング（バックエンド応答遅延に依存する実際のタイムアウト発火時刻）
  - Reason: 実環境では backend.send_dtmf の応答遅延が PJSUA のスケジューリングに依存し、タイムアウト発火の絶対時刻は決定論的にテスト不能（cannot test deterministically）。これは設計欠陥（design defect）ではない — 二相タイムアウト機構そのものは短い timeout_ms を BootConfig 経由で注入すれば Layer 2 で決定論テスト可能であり（P11-6 既存テスト）、実タイミングはネイティブ境界の検証範囲である。
  - Alternative verification: dtmf_sent_timeout_ms に極小値（例: 1ms）を注入し、バックエンドが DtmfSent を報告しない場合に timeout fallback の DtmfSent が publish されることを Layer 2 で検証する。

### Plan Test Code (concrete code)

- **C074 Precondition → test input code** (§62 親セクションが存在する):
  ```rust
  // Verify §62 parent exists via typed design data (impl_integration_design.rs)
  let decision = DesignDecisionId::CallApiExpansion;
  assert_eq!(decision.section(), "62.5");
  assert!(BREAKING_CHANGE_ORDER.contains(&DesignDecisionId::CallApiExpansion));
  ```
- **C074 Postcondition → assertion code** (62.5 が §62 の一部として記述される):
  ```rust
  // Assert 62.5 is described as part of §62: label + spec file
  assert_eq!(DesignDecisionId::CallApiExpansion.label(), "62.5 公開 API 拡充（通話 API 群）");
  let spec = std::fs::read_to_string("specs/P15-6.md").expect("spec must exist");
  assert!(spec.contains("answer(call_id") && spec.contains("calls()"));
  ```
- **C074 Invariant → predicate code** (公開 API 拡充は §19/§20 と整合する):
  ```rust
  // Predicate: SipClient public call-API method set matches RFC N0027
  let surface = sip_client_public_methods(); // from client.rs API-surface test helper
  for m in ["answer", "hangup", "hold", "unhold", "transfer", "send_dtmf", "call_state", "calls"] {
      assert!(surface.contains(m), "SipClient must expose {m}");
  }
  ```
- **C086 Precondition → test input code** (N0027 が §19 発着信 API 詳細を定義する):
  ```rust
  // Compile-time proof N0027 CallApiSemantics trait exists and SipCall implements it
  fn assert_trait<T: CallApiSemantics>() {}
  assert_trait::<SipCall>();
  ```
- **C086 Postcondition → assertion code** (62.5 が answer/hangup/hold/unhold/transfer/call_state(call_id) 追加を規定する):
  ```rust
  // Assert all 7 call APIs exist on SipClient and old call_state() became calls()
  let client = test_client().await;
  client.answer(call_id, 200).await.expect("answer(200)");
  client.hangup(call_id, HangupReason::LocalUser).await.expect("hangup");
  client.hold(call_id).await.expect("hold");
  client.unhold(call_id).await.expect("unhold");
  client.transfer(call_id, "sip:bob@example.com").await.expect("transfer");
  client.send_dtmf(call_id, "5", DtmfMethod::Rfc2833).await.expect("send_dtmf");
  let st: CallState = client.call_state(call_id).await.expect("call_state(call_id)");
  let all: Vec<CallEntry> = client.calls().await.expect("calls()");
  ```
- **C086 Invariant → predicate code** (answer が 180/183/200/486/603 を受理する):
  ```rust
  // Predicate: validate_answer_code accepts exactly the §19.1 five codes
  for code in [180u16, 183, 200, 486, 603] {
      assert!(validate_answer_code(code).is_ok(), "must accept {code}");
  }
  for code in [0u16, 179, 181, 404, 604, 65535] {
      assert!(validate_answer_code(code).is_err(), "must reject {code}");
  }
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| SipClient に answer/hangup/hold/unhold/transfer/send_dtmf が不在 | §19/§20 の通話 API を追加（call_id 基準） | 公開 API の拡充 |
| is_valid_answer_code が 486/603 を拒否 | validate_answer_code が 180/183/200/486/603 を受理 | decline 応答の受理（§19.1） |
| call_state() が Vec<CallEntry> を返す | calls() に改名し call_state(call_id) -> CallState を新設 | call_state シグネチャの整合 |
| unsubscribe が未定義 | broadcast Receiver の drop による購読解除を README に明文化 | unsubscribe の設計確定 |
| SipClient has no answer/hangup/hold/unhold/transfer/send_dtmf; call_state() returns Vec<CallEntry> | SipClient exposes answer/hangup/hold/unhold/transfer/send_dtmf/call_state(call_id); calls() is the renamed list query | Added §62.5 call-control facade (7 methods) + calls() rename |
| RuntimeCommand has Hangup{call_id} only; no Answer/Transfer | RuntimeCommand::Answer/Transfer added; Hangup carries HangupReason | RuntimeCommand call-control variants + reason-carrying Hangup |
| validate_answer_code does not exist | validate_answer_code accepts 180/183/200/486/603 (ANSWER_CODES), rejects others InvalidArgument | RFC §19.1 answer-code gate (fail-fast) |
| reactor has no answer/hangup/transfer handlers | handle_answer (publishes CallConnected/decline), handle_hangup (publishes CallDisconnected), handle_transfer (marks Transferring) | Reactor call-control dispatch + event publish |
| RuntimeHandle has no submit_answer/submit_hangup/submit_transfer/call_state | typed submit methods + call_state(call_id) query mapping CallEntry.state -> CallState | RuntimeHandle call-control + per-call state query |
| TestBackend records only hold/unhold/conf calls | records answer_calls/hangup_calls/transfer_calls | TestBackend observability for reactor dispatch tests |
| call_api_expansion.rs is a stub (pub trait Service {}) | CALL_API_METHODS const + call_state_from_entry_state + answer_state_string helpers | Resolved TS-001; N0074 §62.5 helpers |

## Notes in Prior Implementation Rounds

- [Implementation steps] ① validate_answer_code を call_api_semantics.rs に追加（Red: 単体テスト先行）② RuntimeCommand::Answer/Transfer 追加 + Hangup に reason 追加（command.rs + 既存テスト更新）③ DispatchCommand::from_runtime_command の match 更新 ④ RuntimeHandle::call_state(call_id) typed query 追加 ⑤ SipClient に 7 メソッド追加 + call_state→calls 改名 ⑥ reactor の Answer/Transfer/Hangup ハンドラ + CallConnected/decline publish ⑦ TestBackend 記録追加 ⑧ API surface テスト・README 更新 ⑨ make test 全緑確認
- [Risks] RuntimeCommand::Hangup のシグネチャ変更は破壊的 — 全呼び出し箇所（client.rs・テスト）を一括更新。API surface テストのメソッド数 14→21 変更で既存テストが red になる（意図的）。backend.hangup は reason を受け取らないため、reason は facade/reactor 層で記録する設計（event 化は将来）。
- [Caveats] CallEntry.state は String のため call_state(call_id) の CallState 写像は m20_callstate_mapping 等の変換を要する。validate_answer_code は将来 is_valid_answer_code を置換する唯一の検証点とする。
- [Open items] CallEntry.state の typed enum 化（別チケット）。answer 時の CallConnected publish を reactor のどの経路（Answer ハンドラ直接 vs on_call_state 経由）で行うかの実装判断。
- [Future improvements] 保留/転送の per-call イベント（CallHold/CallTransferring 等）の追加 publish。call_state(call_id) の購読型 API 化。
Implementation summary (P15-6):
- Changed files: src/api/call_api_expansion.rs, call_api_semantics.rs, api/mod.rs, client.rs, error/m20_shutdown_routing.rs, runtime/backend.rs, runtime/command.rs, runtime/handle.rs, runtime/reactor.rs, README.md
- Key changes: validate_answer_code (ANSWER_CODES 180/183/200/486/603); RuntimeCommand::Answer/Transfer + Hangup{reason}; dedicated DispatchCommand variants; RuntimeHandle::submit_answer/hangup/transfer/call_state; SipClient answer/hangup/hold/unhold/transfer/send_dtmf/call_state + calls() rename; reactor handle_answer/handle_hangup/handle_transfer with CallConnected/CallDisconnected publish; TestBackend answer/hangup/transfer recorders; API surface test 14->21; README call-control + drop-unsubscribe docs.
- Contract coverage: C074 (@verifies in command/handle/reactor/backend/client tests), C086 (@verifies in call_api_semantics/client/reactor tests). verify-red-coverage: 2/2 covered.
- Test results: make test — 1170 passed, 0 failed (exit 0).
- Quality: all new code lines pass run-quality-checks.js; remaining 78 report entries are pre-existing baseline issues in shared files (committed P15-5 state already had them).
Review findings (P15-6):
- No blocker/major/minor defects found in the implementation.
- Observed behavior notes: handle_answer/handle_hangup publish terminal events directly on the single EventBus (P15-4); in production with pjsua-native, the native on_call_state callback may also publish CallConnected/CallDisconnected — per RFC §15.7 events are observation-only (no delivery guarantee), duplicate observation is acceptable. Tracked as an open item in the spec notes.
- Quality baseline: pre-existing unwraps in shared test modules were left as-is (out of scope, matching P15-3/4/5 precedent); all new code is clean.
- TS-001 resolved: call_api_expansion.rs stub replaced with CALL_API_METHODS / call_state_from_entry_state / answer_state_string.

## P15-6 — implemented at 38 locations

### src/api/call_api_expansion.rs

- Line 83
```rust
    fn call_api_methods_matches_rfc_n0027_surface() {
```

- Line 103
```rust
    fn call_state_from_entry_state_maps_all_13_states() {
```

- Line 142
```rust
    fn answer_state_string_maps_codes() {
```

### src/api/call_api_semantics.rs

- Line 153
```rust
        fn assert_trait<T: CallApiSemantics>() {}
```

- Line 221
```rust
    fn validate_answer_code_accepts_rfc191_codes() {
```

- Line 233
```rust
    fn validate_answer_code_rejects_non_rfc191_codes() {
```

- Line 247
```rust
    fn validate_answer_code_boundary_values() {
```

### src/api/mod.rs

- Line 39
```rust
// [::TICKET::] P9-3 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P9-3 --for-spec --no-implementation-order`.
```

### src/client.rs

- Line 83
```rust
impl SipClient {
```

- Line 549
```rust
fn map_call_state_query_error(e: ReactorError) -> SipError {
```

- Line 564
```rust
fn account_snapshot_from_entry(
```

- Line 725
```rust
    fn sip_client_is_send_and_sync() {
```

- Line 730
```rust
        fn assert_send<T: Send>() {}
```

- Line 946
```rust
        fn assert_add_transport_result(_: &Result<(), SipError>) {}
```

- Line 950
```rust
        fn assert_calls_result(_: &Result<Vec<crate::runtime::state::CallEntry>, SipError>) {}
```

- Line 952
```rust
        fn assert_call_state_result(_: &Result<crate::state::call_state_model::CallState, SipError>) {}
```

- Line 959
```rust
        fn assert_shutdown_result(_: &Result<(), SipError>) {}
```

- Line 1298
```rust
    fn all_public_client_methods_are_instrumented() -> Result<(), std::io::Error> {
```

### src/error/m20_shutdown_routing.rs

- Line 131
```rust
    fn make_other_command() -> RuntimeCommand {
```

### src/runtime/backend.rs

- Line 121
```rust
    fn conf_disconnect(&mut self, source: i32, sink: i32) -> Result<(), ReactorError>;
```

- Line 222
```rust
impl SipBackend for TestBackend {
```

- Line 330
```rust
    fn answer_call(&mut self, native_call_id: i32, code: u16) -> Result<(), ReactorError> {
```

- Line 339
```rust
    fn hangup(&mut self, native_call_id: i32) -> Result<(), ReactorError> {
```

- Line 363
```rust
    fn transfer_call(&mut self, native_call_id: i32, target: &str) -> Result<(), ReactorError> {
```

- Line 1268
```rust
        fn assert_send<T: Send>() {}
```

- Line 1498
```rust
    fn test_backend_records_answer_calls() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1514
```rust
    fn test_backend_records_hangup_calls() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1530
```rust
    fn test_backend_records_transfer_calls() -> Result<(), Box<dyn std::error::Error>> {
```

### src/runtime/command.rs

- Line 244
```rust
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
```

- Line 275
```rust
type BackendFn =
```

- Line 439
```rust
impl DispatchCommand {
```

- Line 576
```rust
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
```

- Line 906
```rust
    fn from_runtime_command_converts_answer_to_dedicated_variant() {
```

- Line 933
```rust
    fn from_runtime_command_converts_hangup_to_dedicated_variant_with_reason() {
```

- Line 963
```rust
    fn from_runtime_command_converts_transfer_to_dedicated_variant() {
```

- Line 989
```rust
    fn runtime_command_display_shows_new_call_api_variants() {
```

### src/runtime/handle.rs

- Line 58
```rust
impl RuntimeHandle {
```

### src/runtime/reactor.rs

- Line 89
```rust
impl CoreReactor {
```
