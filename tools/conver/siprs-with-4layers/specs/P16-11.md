# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P16-11: Round-2 I/O boundary reference (§62.20)

**Ticket Key**: P16-11 · **Phase**: 16

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
§62.20 の I/O 境界表（10 設計判断: 62.11–62.19）を Rust の型付き定数として符号化し、round-1 §62.9 の IoBoundaryRow パターンを round-2 に適用する。削除対象（RegistrationSucceeded/Failed dead code・CallRejected publish 経路・DtmfMethod 重複定義）を Round2DeleteTarget として整理する。

### Purpose
graphify/boundify が round-2 設計判断の分割・Prune 判断に使う I/O 境界参照情報（入力・出力・関連ファイル候補・関連グラフノード）を機械可読にする。round-1 §62.9（impl_integration_design::IO_BOUNDARY_TABLE / DELETE_TARGETS）と同型の round-2 参照を提供し、ツール群が round-2 の削除対象を追跡できるようにする。

### Motivation
round-2 実装（P16-2〜P16-10: 62.12–62.19）が完了しつつあり、§62.20 の参照情報が src/architecture/io_boundary_round2.rs の STUB として未符号化のまま残っている。round-1 では §62.9 の DELETE_TARGETS が boundify の Prune 判断に使用されており、round-2 でも同型の参照が graphify/boundify から要求される。また P16-1（§62.10）の ROUND2_BREAKING_CHANGES（3 件）と対応する削除対象を一元管理する必要がある。

### Constraints
- round-1 の `IoBoundaryRow.decision` は `DesignDecisionId`（62.1–62.8 専用）であり `Round2Section` を直接受け入れられない。round-2 専用型（Round2IoBoundaryRow / Round2DeleteTarget）を io_boundary_round2.rs に新設し、round-1 の実装ファイル（impl_integration_design.rs）は変更しない。
- 実ファイルの削除は行わない。本チケットは削除対象の参照符号化のみを担当し、実際の Prune は boundify（外部ツール）が行う。
- 新規依存クレート・設定・スキーマ変更なし。対象は src/architecture/io_boundary_round2.rs（add）と src/architecture/mod.rs（モジュール宣言 1 行）のみ。

## Scope

- **Scope of changes:**
  - src/architecture/io_boundary_round2.rs — add（新規実装）
  - src/architecture/mod.rs — modify（`pub mod io_boundary_round2;` 1 行追加）
- [Action: add/modify] io_boundary_round2.rs は add、mod.rs は modify（モジュール宣言のみ）。実ファイル削除なし。
- [What specifically changes] §62.20 の I/O 境界表を `Round2IoBoundaryRow`（decision: Round2Section, consumes, produces, file_candidates, graph_node_ids）10 行として符号化。削除対象を `Round2DeleteTarget`（path, description, resolved_by）3 件として整理。`known_round2_graph_node_ids()` で既知ノード集合を提供。
- [Before → After] io_boundary_round2.rs: `[::STUB::]` マーカーのみ（21 行）→ `ROUND2_IO_BOUNDARY_TABLE: [Round2IoBoundaryRow; 10]` と `ROUND2_DELETE_TARGETS: [Round2DeleteTarget; 3]` と `known_round2_graph_node_ids()` および #[cfg(test)] テスト群を実装。mod.rs: io_boundary_round2 未宣言 → 宣言追加。
- [API contract change] `crate::architecture::io_boundary_round2` 配下に新規 pub 型/定数（Round2IoBoundaryRow / Round2DeleteTarget / ROUND2_IO_BOUNDARY_TABLE / ROUND2_DELETE_TARGETS / known_round2_graph_node_ids）。round-1 の IoBoundaryRow / DeleteTarget / IO_BOUNDARY_TABLE は変更なし（互換維持）。
- [Data schema change] なし（Rust 型定義のみ。シリアライズ・DB・外部スキーマの変更なし）
- [Config/env change] なし（環境変数・設定ファイルの変更なし）
- [Dependency added/removed] なし（新規依存クレート追加なし）
- **Out of scope:**
  - round-1 §62.9 の実装（impl_integration_design.rs の IoBoundaryRow / DeleteTarget / IO_BOUNDARY_TABLE / DELETE_TARGETS / BREAKING_CHANGE_ORDER）
- **Why excluded:** round-1 の `IoBoundaryRow.decision` は `DesignDecisionId`（62.1–62.8 固定）で `Round2Section` を表現できない。また round-1/round-2 の責務分離を維持するため round-1 実装ファイルは変更しない。実ファイル削除（Prune）は boundify の責務であり本チケットでは行わない。
- **Affected areas:**
  - src/architecture/mod.rs — モジュール宣言追加
  - graphify/boundify ツール — round-2 の分割・Prune 判断に新表を参照可能
  - round-2 後続チケット（P16-12 以降）— file_candidates の更新元
- [Nature of impact] API surface（architecture モジュールに新規公開型・定数を追加、既存 API は不変）。参照データの一元化により graphify/boundify の round-2 対応が容易になる。実行時性能・セキュリティへの影響なし。
- [Corresponding change needed] mod.rs に `pub mod io_boundary_round2;` を追加（必須）。boundify 側の参照は追加のみで既存挙動に影響なし（Y, mod.rs のみ）。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0089`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0089` | architecture | rust | 62.20 I/O 境界参照情報 |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.20 I/O 境界参照情報 (N0089) | → | ☆ 62.9 I/O 境界参照情報（graphify / boundify 用） (N0078) |
| part_of | ★ 62.20 I/O 境界参照情報 (N0089) | → | ☆ §62 実装整合設計 — RESIDUE 解消のための設計判断確定 (N0068) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0089` | `src/architecture/io_boundary_round2.rs` |

## Investigation

- RFC §62.20（RFC-ROOT.md）の I/O 境界表は 10 行（62.11 が 2 行: トランスポート配線 + bindgen 整合方針、62.12–62.19 が各 1 行）。列は「設計判断 / 入力(consumes) / 出力(produces) / 関連 GRAPH ノード・ファイル候補」で、第 4 列はファイル候補（backend.rs, reactor.rs, dtmf_spec.rs 等）を列挙。
- round-1 参照モデル（§62.9, N0078）: src/architecture/impl_integration_design.rs:125-245 に `IoBoundaryRow{decision: DesignDecisionId, consumes, produces, graph_node_ids}` と `IO_BOUNDARY_TABLE: [IoBoundaryRow; 8]`、`DeleteTarget{path, description, resolved_by}` と `DELETE_TARGETS: [DeleteTarget; 3]`、`BREAKING_CHANGE_ORDER: [DesignDecisionId; 8]`、`known_graph_node_ids()` が定義されている。テストは io_boundary_table_covers_all_sections / io_boundary_rows_have_consumes_and_produces / io_boundary_graph_nodes_are_known / delete_targets_reference_real_files / breaking_change_order_is_complete_and_unique / evidence_matches_actual_source。
- round-2 設計判断型: src/architecture/round2_scope_rootcause.rs に `Round2Section`（10 変種, 62.11–62.20, derive Debug/Clone/Copy/PartialEq/Eq）と `section()`/`node_id()`/`label()` が定義済み。node_id は TransportWiring→N0080, RegistrationLifecycle→N0081, EventPathCompletion→N0082, IncomingCallEvents→N0083, DtmfUnification→N0084, MediaPathCompletion→N0085, StunTurnIceWiring→N0086, Examples→N0087, DockerAsteriskBase→N0088, IoBoundaryReference→N0089。
- グラフ調査（RFC-ROOT-GRAPH.json の N0080–N0088 参照関係）: N0080 extends N0015,N0037 / N0081 extends N0025, refines N0073 / N0082 extends N0018,N0037 / N0083 refs N0022, extends N0027, refines N0048 / N0084 extends N0028, refines N0029 / N0085 extends N0033,N0049 / N0086 extends N0015, refines N0070 / N0087 refs N0050 / N0088 extends N0052,N0054。これらを各行の graph_node_ids に使用。
- 削除対象の現状: (1) CallRejected は src/api/event_model_payload_bus.rs から variant 除去済み（grep で不在を確認、round2_scope_rootcause.rs:494 が解決済みを検証）。(2) DtmfMethod は src/model/dtmf_spec.rs:27 が単一定義（Rfc4733/Info/Inband）、account_config_spec.rs:40・observability_metrics.rs:253・m20_dtmfsent_twophase.rs:57 は re-export に統一済み。(3) RegistrationSucceeded/RegistrationFailed は payload enum に variant が無く、コメント参照（m20_registr_cmd_pat.rs:28, reg_account_lifecycle.rs:38,123）のみ。
- モジュール配線: src/architecture/mod.rs は crate_scope / impl_integration_design / policy_reference / round2_scope_rootcause / examples_e1e5 を宣言済み。io_boundary_round2 は未宣言（add 必要）。
- src/architecture/io_boundary_round2.rs は現在 21 行の STUB（Initial Design Artifact ヘッダ + `[::STUB::] P16-11: implement NODE_ID=N0089` のみ）。

## Acceptance Criteria

- **[Happy path] `make test` が全て green。`crate::architecture::io_boundary_round2::ROUND2_IO_BOUNDARY_TABLE` が 10 行（62.11: 2 行 + 62.12–62.19: 8 行）で、各 decision が Round2Section の 62.11–62.19 に属し、consumes / produces / file_candidates が非空。`ROUND2_DELETE_TARGETS` が 3 件で各 path が実在。`known_round2_graph_node_ids()` が N0067 / N0078 / N0068 / N0080–N0089 を含む。
- **[Error case] ノード ID の typo（例: N00115）、行数不一致（10 行以外）、削除対象 path の実在違反、resolved_by が表に存在しない — 全ての逸脱がテストで赤として検出され、TDD の赤→緑を経て修正される。const 定義の不整合はコンパイルエラーまたはテスト失敗として可視化される。
- **[Edge case] 62.11（TransportWiring）が 2 行（transport wiring / bindgen 整合方針）で同一 decision・同一 graph_node_ids（N0015, N0037）を持ちつつ、consumes / produces / file_candidates が異なる。62.20（IoBoundaryReference）自身は表に含まれない（表は 62.11–62.19 を対象）。未生成ファイル候補（registr_wiring.rs 等）は存在チェック対象から除外され description で明示される。

## Invariants

- 【Normal establishment】ROUND2_IO_BOUNDARY_TABLE が常に 10 行を保持し、各 decision が Round2Section の 62.11–62.19 に属する。62.11（TransportWiring）のみ 2 行（transport wiring と bindgen 整合方針）で、他節は 1 行。
- 【Invariant on error】本チケットは純粋な参照符号化（const データ）であり実行時エラーを生まない。const 定義の不整合（行数・ノード ID typo・パス誤り）はコンパイル時またはテスト時に検出され、表が不正な状態で出荷されることはない。
- 【Internal state invariant】ROUND2_DELETE_TARGETS の各 resolved_by（RegistrationLifecycle / IncomingCallEvents / DtmfUnification）が ROUND2_IO_BOUNDARY_TABLE の decision 集合に必ず含まれる。これにより graphify/boundify の Prune 判断が表を参照可能になる（C120 Invariant）。
- 【Boundary invariant】graph_node_ids の全参照が known_round2_graph_node_ids() に含まれ、§61（N0067）・§62.9（N0078）・§62 親（N0068）と整合する。62.20（IoBoundaryReference）自身は表の外側（参照情報の容器）であり、表に含まれない。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C119 — N0089→N0068

- **Precondition**: §62 親セクションが存在する — 実体: round-1 §62 の符号化（crate::architecture::impl_integration_design::IO_BOUNDARY_TABLE, N0068 配下の設計判断）が 8 行で定義されている。テスト翻訳: UT「[C120 Precondition] round-1 §62.9（N0078）の IO_BOUNDARY_TABLE が 8 行で定義されている」
- **Postcondition**: 62.20 がラウンド 2 の I/O 境界を定義する — 実体: ROUND2_IO_BOUNDARY_TABLE が 10 行（62.11 は 2 行、62.12–62.19 は各 1 行）で定義され、各行が非空の consumes/produces/file_candidates を持つ。テスト翻訳: UT「[Normal] ROUND2_IO_BOUNDARY_TABLE が §62.20 の 10 設計判断を保持する」/ UT「[Error] 各行の consumes / produces が非空である」
- **Invariant**: I/O 境界は §61/§62.9 と整合する — 実体: 各行の graph_node_ids が known_round2_graph_node_ids() に含まれ、既知集合が §61（N0067）・§62.9（N0078）・§62 親（N0068）を参照し、§61/§62.9 の境界情報と矛盾しない。テスト翻訳: UT「[Invariant] 各行の graph_node_ids が既知ノード集合に含まれる」/ UT「[C119 Invariant] round-2 既知ノード集合が §61（N0067）と §62.9（N0078）と §62 親（N0068）を含む」

### C120 — N0089→N0078

- **Precondition**: N0078 が 62.9 I/O 境界を定義する — 実体: round-1 §62.9 の符号化（impl_integration_design::IO_BOUNDARY_TABLE）が 8 行で存在する。テスト翻訳: UT「[C120 Precondition] round-1 §62.9（N0078）の IO_BOUNDARY_TABLE が 8 行で定義されている」
- **Postcondition**: 62.20 がラウンド 2 の I/O 境界を参照する — 実体: 表の decision が Round2Section の 62.11–62.19 に限定され、62.20（IoBoundaryReference）自体は表の外側（参照情報の容器）である。テスト翻訳: UT「[Boundary] 表が 62.11–62.19 の全設計判断節を網羅する」/ IT「[Integration point] round2_scope_rootcause::Round2Section ↔ io_boundary_round2::ROUND2_IO_BOUNDARY_TABLE」
- **Invariant**: graphify/boundify が分割判断に使用する — 実体: 各 Round2DeleteTarget.resolved_by（RegistrationLifecycle / IncomingCallEvents / DtmfUnification）が ROUND2_IO_BOUNDARY_TABLE の decision 集合に存在し、graphify/boundify が分割・Prune 判断に表を参照可能。テスト翻訳: UT「[C120 Invariant] 各削除対象の resolved_by が I/O 境界表の decision に存在する」

## Boy Scout Rule

- 関数名を動詞句に: `known_round2_graph_node_ids()`（既知ノード集合を返す）・`round2_io_boundary_rows()`（表へのアクセサ）は動詞句 + ドメイン名詞で命名。
- 構造体/定数をドメイン概念で命名: `Round2IoBoundaryRow`（1 設計判断の I/O 境界）・`Round2DeleteTarget`（boundify の削除対象）・`ROUND2_IO_BOUNDARY_TABLE`（§62.20 表）・`ROUND2_DELETE_TARGETS`（§62.20 削除対象）は round-1 の命名パターン（IoBoundaryRow / DeleteTarget / IO_BOUNDARY_TABLE / DELETE_TARGETS）を踏襲。
- ハードコード値を名前付き定数に: RFC 表の 10 行と 3 削除対象を `pub const` として符号化し、マジック値（行数 10 / 削除数 3 / ノード ID 文字列）をテストから定数参照で検証。
- エラー吞み込みなし: 純粋な const データ（実行時 I/O なし）のため Result 不要。テストは `Result<(), std::io::Error>` を返し、ファイル読取エラーを伝播。
- 既存コードの改善（Boy Scout）: (a) src/architecture/mod.rs のヘッダコメント「Sub-modules:」一覧に examples_e1e5 と io_boundary_round2 が欠落しているため追記（コメントの嘘を正す）。(b) io_boundary_round2.rs の STUB マーカーを実装で置換し、[::STUB::] を解消。(c) round-1 の `IO_BOUNDARY_TABLE` は変更しない（round-1 と round-2 の責務分離を維持）。

## Test Plan

### Unit Tests

- UT: [Normal] ROUND2_IO_BOUNDARY_TABLE が §62.20 の 10 設計判断を保持する — assert_eq!(ROUND2_IO_BOUNDARY_TABLE.len(), 10)。62.11 が 2 行（transport wiring / bindgen 整合方針）+ 62.12–62.19 が 8 行で、RFC §62.20 表の行数と一致すること。
- UT: [Error] 各行の consumes / produces が非空である — 空文字列の行があれば RFC 表からの逸脱として失敗。assert!(!row.consumes.is_empty()) / assert!(!row.produces.is_empty()) を全行に対して行う。decision の section()（例: "62.11"）を失敗メッセージに含める。
- UT: [Boundary] 表が 62.11–62.19 の全設計判断節を網羅する — 9 節が最低 1 回出現し、62.11 のみ 2 回（計 10 行）。62.20（IoBoundaryReference）が表に含まれないこと（62.20 自体は参照情報であり表の外側）。round2_scope_rootcause::Round2Section の section() を収集して検証。
- UT: [Invariant] 各行の graph_node_ids が既知ノード集合に含まれる — known_round2_graph_node_ids() に存在しないノード参照は typo として失敗。各 62.11 行は ["N0015","N0037"]、62.12 は ["N0025","N0073"] 等、グラフ調査で得た extends/refines/references 元ノードを使用。
- UT: [C119 Postcondition] ROUND2_DELETE_TARGETS が 3 件定義され、各 target.path がディスク上に実在する — assert_eq!(len, 3)。std::path::Path::new(target.path).exists() を全 target に適用（round-1 DELETE_TARGETS の delete_targets_reference_real_files と同型）。
- UT: [C120 Precondition] round-1 §62.9（N0078）の IO_BOUNDARY_TABLE が 8 行で定義されている — crate::architecture::impl_integration_design::IO_BOUNDARY_TABLE.len() == 8。62.20 の前提として round-1 の I/O 境界参照が存在することを検証。
- UT: [C119 Invariant] round-2 既知ノード集合が §61（N0067）と §62.9（N0078）と §62 親（N0068）を含む — known_round2_graph_node_ids() が N0067 / N0078 / N0068 / N0080–N0089 を含み、§61/§62.9 との整合を静的に保証。
- UT: [C120 Invariant] 各削除対象の resolved_by が I/O 境界表の decision に存在する — 全 Round2DeleteTarget の resolved_by（RegistrationLifecycle / IncomingCallEvents / DtmfUnification）が ROUND2_IO_BOUNDARY_TABLE の decision 集合に含まれる。graphify/boundify が分割・Prune 判断に利用可能であることの構造的証明。

### Integration Tests

- IT: [Integration point] round2_scope_rootcause::Round2Section ↔ io_boundary_round2::ROUND2_IO_BOUNDARY_TABLE — round2_sections()（62.11–62.20 の 10 節）の各設計判断節（62.11–62.19）が表の decision に最低 1 回出現し、62.20（IoBoundaryReference）自体は表に含まれないことを検証する（62.20 は参照情報の容器）。
- IT: [Verification] ROUND2_DELETE_TARGETS ↔ round2_scope_rootcause::ROUND2_BREAKING_CHANGES の対応 — 3 削除対象（RegistrationSucceeded/Failed dead code・CallRejected publish 経路・DtmfMethod 重複定義）が 3 breaking change（RegistrationEventsRemoval / CallRejectedRemoval / DtmfSipInfoRename）に一対一対応することを検証。resolved_by 節と breaking change の解決節が一致する。
- IT: [Prerequisites] round-1 §62.9（impl_integration_design::IO_BOUNDARY_TABLE / DELETE_TARGETS）が既に存在し、round-2 の既知ノード集合が §61（N0067）・§62.9（N0078）・§62 親（N0068）を参照できること。round-2 表が round-1 の境界情報と矛盾しない（C119 Invariant / C120 Precondition）。
- IT: [Related tickets] 各設計判断の file_candidates が実在ソース（src/runtime/backend.rs, src/runtime/reactor.rs, src/model/dtmf_spec.rs, src/ffi/backend_calls.rs 等）を指すことを確認。先行チケット P16-2（62.12）〜P16-10（62.19）で生成されたファイル候補との整合を静的に検証。

### Exceptions

- [Item] boundify/graphify による実際のファイル Prune（削除）処理 / [Reason] 外部ツール（Node.js 製 graphify/boundify）が行う処理であり、Rust 単体テストの実行対象外（外部依存）のため単体テスト不能（cannot test）。本チケットは削除対象の参照符号化のみを担当する / [Alternative verification] ROUND2_DELETE_TARGETS の path 実在チェックと、ソース整合性テスト（CallRejected variant 不在・DtmfMethod 単一定義）で削除対象が正しく解決済みであることを静的に検証する。これは設計欠陥ではない（not a design defect）——削除実行の委譲は設計判断であり、参照符号化自体は全てテスト可能。
- [Item] §62.20 表の file_candidates に含まれる未生成パス（registr_wiring.rs 等、将来チケットで生成予定のモジュール）/ [Reason] 実装が先行チケットで進行中であり、make 時点ではディスク上に存在しないパスがあるため、それらのパス実在検証はテスト不能（not testable）。既存パスの実在検証は可能 / [Alternative verification] 既存パスのみを std::path::Path::exists() で検証し、未生成パスは description に「boundify が Prune 対象とすべきファイル候補」として明記して存在チェックから除外する。これは設計欠陥ではない（not a design defect）——file_candidates は参照情報であり、実在検証を既存パスに限定する設計である。
- [Item] RegistrationSucceeded/Failed・CallRejected・DtmfMethod 重複の「削除済み」状態そのもの / [Reason] これらは先行チケット（P16-2〜P16-10）で既に削除・一元化済みであり、本チケットは削除対象を参照として再符号化するだけ（再削除はしない）ため、削除という動的挙動の再現テストは不可能（impossible to test） / [Alternative verification] ソース整合性テストで削除済み状態（payload enum に CallRejected variant が不在、src/model/dtmf_spec.rs のみ pub enum DtmfMethod を定義）を静的アサーションで検証する。これは設計欠陥ではない（not an architectural defect）——参照符号化としての設計は全てテスト可能であり、テスト不能なのは先行削除の履歴だけである。

### Plan Test Code (concrete code)

- UT: [C119 Precondition] §62 親セクションが存在する — round-1 §62.9（N0078）の I/O 境界表が 8 行で定義されていること
```rust
#[test]
// @verifies C119 -- precondition: §62 親セクションが存在する
fn round1_io_boundary_table_exists() {
    use crate::architecture::impl_integration_design::IO_BOUNDARY_TABLE;
    assert_eq!(IO_BOUNDARY_TABLE.len(), 8, "round-1 §62.9 (N0078) table must have 8 rows");
}
```
- UT: [C119 Postcondition] 62.20 がラウンド 2 の I/O 境界を定義する — 表が 10 行、各行の consumes/produces/file_candidates が非空、削除対象が 3 件
```rust
#[test]
// @verifies C119 -- postcondition: 62.20 がラウンド 2 の I/O 境界を定義する
fn round2_table_has_ten_rows_with_concrete_boundaries() {
    assert_eq!(ROUND2_IO_BOUNDARY_TABLE.len(), 10, "§62.20 table must have 10 rows");
    for row in &ROUND2_IO_BOUNDARY_TABLE {
        assert!(!row.consumes.is_empty(), "{} consumes must be non-empty", row.decision.section());
        assert!(!row.produces.is_empty(), "{} produces must be non-empty", row.decision.section());
        assert!(!row.file_candidates.is_empty(), "{} file_candidates must be non-empty", row.decision.section());
    }
}

#[test]
// @verifies C119 -- postcondition: 62.20 が削除対象を定義する
fn round2_delete_targets_are_defined_and_exist() {
    assert_eq!(ROUND2_DELETE_TARGETS.len(), 3, "§62.20 lists exactly 3 delete targets");
    for target in &ROUND2_DELETE_TARGETS {
        assert!(std::path::Path::new(target.path).exists(), "delete target {} must exist", target.path);
    }
}
```
- UT: [C119 Invariant] I/O 境界は §61/§62.9 と整合する — 既知ノード集合が N0067/N0078/N0068 を含み、全行の graph_node_ids が既知集合に含まれる
```rust
#[test]
// @verifies C119 -- invariant: I/O 境界は §61/§62.9 と整合する
fn round2_known_nodes_include_section61_and_62_9() {
    let known = known_round2_graph_node_ids();
    assert!(known.contains(&"N0067"), "§61 node N0067 must be known");
    assert!(known.contains(&"N0078"), "§62.9 node N0078 must be known");
    assert!(known.contains(&"N0068"), "§62 parent node N0068 must be known");
    for row in &ROUND2_IO_BOUNDARY_TABLE {
        for node in row.graph_node_ids {
            assert!(known.contains(node), "unknown graph node {node} in section {}", row.decision.section());
        }
    }
}
```
- UT: [C120 Precondition] N0078 が 62.9 I/O 境界を定義する — round-1 §62.9 の表が非空
```rust
#[test]
// @verifies C120 -- precondition: N0078 が 62.9 I/O 境界を定義する
fn round1_io_boundary_table_is_available() {
    use crate::architecture::impl_integration_design::IO_BOUNDARY_TABLE;
    assert!(!IO_BOUNDARY_TABLE.is_empty(), "round-1 §62.9 I/O boundary table must be defined");
}
```
- UT: [C120 Postcondition] 62.20 がラウンド 2 の I/O 境界を参照する — 表が 62.11–62.19 のみを対象にし、62.20 自体は含まない
```rust
#[test]
// @verifies C120 -- postcondition: 62.20 がラウンド 2 の I/O 境界を参照する
fn round2_table_covers_62_11_to_62_19_only() {
    let sections: Vec<&str> = ROUND2_IO_BOUNDARY_TABLE.iter().map(|row| row.decision.section()).collect();
    assert_eq!(sections.len(), 10, "table must have 10 rows");
    assert_eq!(sections.iter().filter(|s| **s == "62.11").count(), 2, "62.11 has 2 rows (transport + bindgen)");
    for n in 12..=19 {
        let sec = format!("62.{n}");
        assert_eq!(sections.iter().filter(|s| **s == sec).count(), 1, "{sec} must appear exactly once");
    }
    assert!(!sections.contains(&"62.20"), "62.20 itself must not be a row (container)");
}
```
- UT: [C120 Invariant] graphify/boundify が分割判断に使用する — 各削除対象の resolved_by が表の decision 集合に存在する
```rust
#[test]
// @verifies C120 -- invariant: graphify/boundify が分割判断に使用する
fn delete_targets_resolve_to_table_rows() {
    let decisions: Vec<Round2Section> = ROUND2_IO_BOUNDARY_TABLE.iter().map(|row| row.decision).collect();
    for target in &ROUND2_DELETE_TARGETS {
        assert!(decisions.contains(&target.resolved_by),
            "delete target {} resolved_by {} must appear in table",
            target.path, target.resolved_by.section());
    }
}
```
- UT: [C119+C120 source consistency] 削除済み状態の静的検証 — CallRejected 不在・DtmfMethod 単一定義
```rust
#[test]
// @verifies C119 @verifies C120 -- source consistency
fn round2_delete_targets_source_consistency() -> Result<(), std::io::Error> {
    let payload_bus = std::fs::read_to_string("src/api/event_model_payload_bus.rs")?;
    assert!(!payload_bus.contains("CallRejected"), "CallRejected must be removed from payload bus");
    let model_dtmf = std::fs::read_to_string("src/model/dtmf_spec.rs")?;
    assert!(model_dtmf.contains("pub enum DtmfMethod"), "dtmf_spec.rs must define DtmfMethod");
    let account_spec = std::fs::read_to_string("src/config/account_config_spec.rs")?;
    assert!(!account_spec.contains("pub enum DtmfMethod"), "account_config_spec.rs must not define DtmfMethod");
    let obs_metrics = std::fs::read_to_string("src/config/observability_metrics.rs")?;
    assert!(!obs_metrics.contains("pub enum DtmfMethod"), "observability_metrics.rs must not define DtmfMethod");
    Ok(())
}
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| src/architecture/io_boundary_round2.rs: [::STUB::] marker only (21 lines) | Round2IoBoundaryRow / Round2DeleteTarget / ROUND2_IO_BOUNDARY_TABLE (10 rows) / ROUND2_DELETE_TARGETS (3) / known_round2_graph_node_ids() + 8 tests | Encoded §62.20 I/O boundary table (62.11x2 + 62.12-62.19) and 3 delete targets as typed constants; resolved TS-001. |
| src/architecture/mod.rs: io_boundary_round2 not declared | pub mod io_boundary_round2; added with [::TICKET::] P16-11 annotation | Registered the new module in the architecture module tree. |

## Notes in Prior Implementation Rounds

Review report:
- Static quality check: passed (0 issues on io_boundary_round2.rs, mod.rs)
- Translatability: no issues — known_round2_graph_node_ids / ROUND2_IO_BOUNDARY_TABLE / Round2DeleteTarget follow round-1 naming; test variables (row/target/node/known/sections) are domain-meaningful
- STUB evaluation: 1 stub remains in runtime/backend.rs:1082 (PX-3, correctly deferred — out of this ticket's scope); no-excuse gate passed
- Incomplete implementations: none found in changed files (7 patterns scanned)
- Issues found and fixes applied: rustfmt reverted out-of-scope module-tree formatting via git checkout; TS-001 targetStub marked resolved (targetStubs=verified_empty); AMBIGUOUS marker in mod.rs resolved via inject-at line 36
- Contracts C119/C120: verified by verify-red-coverage (2/2) and verify-final-contracts

## P16-11 — implemented at 9 locations

### src/architecture/io_boundary_round2.rs

- Line 168
```rust
    fn round1_io_boundary_table_exists() {
```

- Line 179
```rust
    fn round2_table_has_ten_rows_with_concrete_boundaries() {
```

- Line 207
```rust
    fn round2_delete_targets_are_defined_and_exist() {
```

- Line 225
```rust
    fn round2_known_nodes_include_section61_and_62_9() {
```

- Line 247
```rust
    fn round1_io_boundary_table_is_available() {
```

- Line 257
```rust
    fn round2_table_covers_62_11_to_62_19_only() {
```

- Line 285
```rust
    fn delete_targets_resolve_to_table_rows() {
```

- Line 303
```rust
    fn round2_delete_targets_source_consistency() -> Result<(), std::io::Error> {
```

### src/architecture/mod.rs

- Line 36
```rust

```
