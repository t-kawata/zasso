# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P16-2: Transport wiring & bindgen alignment (§62.11)

**Ticket Key**: P16-2 · **Phase**: 16

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
本チケットは §62.11（N0080）を実装し、`PjsuaBackend::initialize` が `config.transports` を列挙して各 `TransportConfig` の種別（UDP/TCP/TLS）と bind_addr ポートを `pjsua_transport_create` へ反映する（null config を廃止）。生成した `pjsua_transport_id` を `PjsuaBackend.transport_ids` に保持し、シャットダウン（§32 手順5 InvokeDestroy）で `pjsua_transport_close` により破棄する。加えて bindgen allowlist とコード期待の整合方針（PJ_SUCCESS 等の定数 / `pjsua_acc_config.registrar_uri` / `SecretString::expose_secret` / constified enum モジュール）を §27/§28 追補として文書化する。

## Scope

- PjsuaBackend::initialize が config.transports を列挙し、TransportConfig の種別（UDP/TCP/TLS）と bind_addr ポートを pjsua_transport_create へ反映する（null config を廃止）。
- 生成した pjsua_transport_id を PjsuaBackend.transport_ids に保持し、シャットダウン（§32 手順5）で破棄する。
- bindgen allowlist とコード期待の整合方針（PJ_SUCCESS 等の定数、pjsua_acc_config.registrar_uri、SecretString::expose_secret、constified enum モジュール）を §27/§28 追補として文書化する。
- **Scope of changes:** src/ffi/transport_wiring.rs（add）/ src/ffi/mod.rs（add）/ src/runtime/backend.rs（modify: initialize で配線）/ src/ffi/backend_calls.rs（modify: null config 廃止 + close 追加）/ src/ffi/bindings.rs + src/build/build_script_bindgen.rs（modify: stub/allowlist 同期の最小変更）
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] `src/ffi/transport_wiring.rs`（N0080 の実装ファイル）
  - [Action: add/modify/remove/rename/refactor] modify — `[::STUB::] P16-2` を実装へ置換し、配線モジュールとして実装（Initial Design Artifact ヘッダは維持）
  - [What specifically changes] `TransportKind` enum（Udp/Tcp/`#[cfg(feature="tls")] Tls`）と `resolve_transport_kind_and_bind_addr(&TransportConfig) -> (TransportKind, SocketAddr)`（§12 の一対一対応、catch-all 無し）、`transport_kind_label(TransportKind) -> &str`（"udp"/"tcp"/"tls"）、`resolve_bound_addr_string(SocketAddr) -> String`（未指定→空文字列）、`wire_transports(&[TransportConfig], impl Fn(&TransportConfig) -> Result<i32, E>) -> Result<Vec<i32>, E>`（列挙 + fail-fast 収集）、`destroy_transports(&[i32], impl Fn(i32) -> Result<(), E>) -> Result<(), E>`、`#[cfg(feature="pjsua-native")] to_pjsua_transport_type(TransportKind) -> c_int`（PJSIP_TRANSPORT_* 定数参照）、`#[cfg(feature="pjsua-native")] native_transport_create(&TransportConfig) -> (i32, i32)`（backend_calls::transport_create へ委譲）、`#[cfg(feature="pjsua-native")] destroy_native_transport(i32) -> (i32, i32)`（backend_calls::close_transport へ委譲）
  - [Before → After (behavior/signature)] Before: ヘッダー + STUB マーカーのみでコンパイル対象外・テスト不能。After: 純粋ロジック（kind 変換 / label / bound_addr 解決 / 列挙・破棄オーケストレーション）がデフォルトビルドでテスト可能になり、FFI 委譲が pjsua-native で機能する
  - [API contract change (if any)] 公開 API の破壊的変更なし。`ffi::transport_wiring` に `pub(crate)` 配線関数が新規公開される。`SipBackend::create_transport` のシグネチャは不変（`Result<(), ReactorError>`）
  - [Data schema change (if any)] データスキーマ変更なし。`TransportConfig` / `ClientConfig` の型は不変
  - [Config/env change (if any)] 設定・環境変数の変更なし。`config.transports` の意味論のみ変更（initialize が実際に反映する）
  - [Dependency added/removed (if any)] 依存クレートの追加・削除なし
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] `src/runtime/backend.rs`（modify）+ `src/ffi/backend_calls.rs`（modify）
  - [Action: add/modify/remove/rename/refactor] modify + add
  - [What specifically changes] `PjsuaBackend` に `transport_ids: Vec<pjsua_transport_id>` フィールドを追加。`initialize` が `backend_calls::initialize()` 成功後に `config.transports` を列挙し、`transport_wiring::native_transport_create` 経由で各 id を生成して `transport_ids` に push。`shutdown` が `transport_wiring::destroy_transports(&self.transport_ids, destroy_native_transport)` で全 id を `pjsua_transport_close` してから `backend_calls::shutdown()` を実行。`backend_calls.rs` の `create_transport()`（null config, `std::ptr::null_mut()`）を `transport_create(kind: c_int, bind_addr: SocketAddr) -> (i32, i32)` に置換し、`pjsua_transport_config` を `std::mem::zeroed()` で初期化して port / bound_addr（PjOwnedStr）を反映。`close_transport(transport_id: i32) -> i32` を追加（`pjsua_transport_close` 呼び出し）
  - [Before → After (behavior/signature)] Before: `initialize(&mut self, _config)` が config 未使用で null-config の `create_transport()` を呼び id を破棄。After: `initialize(config)` が config.transports を列挙し実 config で生成、`transport_ids` に保持。`shutdown` が生成済み transport を明示破棄してから `pjsua_destroy`
  - [API contract change (if any)] `SipBackend::create_transport`（trait メソッド）のシグネチャは不変。`PjsuaBackend` 内部の `create_native_transport`（private）と `transport_ids`（private field）を追加
  - [Data schema change (if any)] なし
  - [Config/env change (if any)] なし
  - [Dependency added/removed (if any)] なし
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] `src/ffi/mod.rs`（add）+ `src/ffi/bindings.rs`（modify, stub）+ `src/build/build_script_bindgen.rs`（modify, allowlist）+ `src/security/security_platform_diffs.rs`（modify, expose_secret）
  - [Action: add/modify/remove/rename/refactor] add + modify
  - [What specifically changes] `ffi/mod.rs` に `pub mod transport_wiring;` と配線関数の re-export を追加。`bindings.rs` stub_aliases に `pjsua_transport_config`（port: u32 / bound_addr: pj_str_t）、`PJSIP_TRANSPORT_UDP/TCP/TLS` 定数、`pjsua_transport_close` を追加して pjsua-native の bindgen 出力と同期。`build_script_bindgen.rs` の allowlist に `pjsua_transport_close`（function）、`PJSIP_TRANSPORT_UDP/TCP/TLS`（vars）、`pjsip_transport_type_e`（type）を追加。`SecretString::expose_secret() -> &str` を `as_str()` の明示的別名として実装（RFC §28 追補 item 3: TURN パスワード等の機微値を PJSIP へ渡す経路。既存の backend_calls add_account/update_account が参照）
  - [Before → After (behavior/signature)] Before: stub に `pjsua_transport_config` / トランスポート定数 / `pjsua_transport_close` が無く、pjsua-native ビルドで allowlist 不足。`expose_secret` は呼び出しがあるが未定義（pjsua-native ビルドの 39 エラーの一部）。After: stub と bindgen 出力が同期し、デフォルトビルドで配線の純粋ロジックがテスト可能、pjsua-native ビルドが transport 配線をコンパイル可能
  - [API contract change (if any)] `SecretString::expose_secret()` が公開 API に追加（`as_str` のエイリアス）。破壊的変更なし
  - [Data schema change (if any)] なし
  - [Config/env change (if any)] なし
  - [Dependency added/removed (if any)] なし
- **Out of scope (items intentionally excluded, with justification):**
  - [Excluded item] STUN/TURN/ICE の `pjsua_config.stun_srv` / `turn_cfg` / ICE 設定反映（§62.17）
  - [Why excluded — separate ticket / future phase / not applicable] §62.17（P16-9）のスコープ。本チケットはトランスポート配線のみ
  - [Excluded item] 登録・アカウント経路の配線（§62.12 / P16-3）とイベント経路の完成（§62.13 / P16-4）
  - [Why excluded — separate ticket / future phase / not applicable] 別チケット。本チケットは initialize のトランスポート生成と shutdown の破棄に限定
  - [Excluded item] constified enum モジュール（pjsip_inv_state / pjsua_call_media_status）の bindgen 設定変更と pjsua_acc_config.registrar_uri の検証
  - [Why excluded — separate ticket / future phase / not applicable] §27/§28 追補として方針を文書化する（本チケット item 3）。実ビルド修復（39 エラー）は P16-4（§62.13）以降の実装チケットで解決
  - [Excluded item] reactor の `CreateTransport` コマンド経路の runtime state 変更（TransportRuntimeState.transport_id の実 id 化）
  - [Why excluded — separate ticket / future phase / not applicable] 本チケットは initialize/shutdown 配線に限定。reactor 側の実 id 反映は Boy Scout 対象として transport_kind_label の一元化に留め、構造変更は §62.13 以降で対応
- **Affected areas (components/systems impacted, even without direct modification):**
  - [Affected component] `src/runtime/reactor.rs`（CreateTransport ディスパッチ, 732-777 行）
  - [Nature of impact: performance / security / API surface / data format / …] API 表面: `SipBackend::create_transport` の動作が null-config 生成から実 config 生成に変わる。データ形式: TransportRuntimeState の transport_type/port は不変だが、将来実 id 反映の余地が生じる
  - [Corresponding change needed Y/N + details] N（本チケットでは変更不要）。reactor の重複マッピング（740-751 行）は transport_kind_label への置換を Boy Scout として推奨するが、必須変更ではない
  - [Affected component] `src/state/shutdown_specification.rs`（§32 5 フェーズ, N0043）
  - [Nature of impact: performance / security / API surface / data format / …] 動作: InvokeDestroy フェーズで pjsua_destroy の前に transport 明示破棄が入る
  - [Corresponding change needed Y/N + details] N。`PjsuaBackend::shutdown` の内部実装の変更であり、ShutdownPhase 列挙やオーケストレーションは不変
  - [Affected component] `src/ffi/bindings.rs` stub と bindgen 出力の同期
  - [Nature of impact: security / data format / …] 整合方針（§27/§28 追補）の文書化。stub に pjsua_transport_config 等を追加することでデフォルトビルドのテスト可能性が向上
  - [Corresponding change needed Y/N + details] Y — 本チケットで stub 追加を実施

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0080`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0080` | architecture | rust | 62.11 トランスポート生成配線と bindgen 整合方針 |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| extends | ★ 62.11 トランスポート生成配線と bindgen 整合方針 (N0080) | → | ☆ §27 PJSIP FFI Layer Architecture (N0037) |
| extends | ★ 62.11 トランスポート生成配線と bindgen 整合方針 (N0080) | → | ☆ §12 TransportConfig & §13 ICE/STUN/TURN Spec (N0015) |
| part_of | ★ 62.11 トランスポート生成配線と bindgen 整合方針 (N0080) | → | ☆ §62 実装整合設計 — RESIDUE 解消のための設計判断確定 (N0068) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0080` | `src/ffi/transport_wiring.rs` |

## Investigation

- 現状の未配線状態（P16-1 evidence と一致）:
  - `src/ffi/backend_calls.rs:55-62` — `create_transport()` が `pjsua_transport_create(std::ptr::null_mut(), &mut transport_id)` を呼び、種別・ポートを反映しない（null config）。`#[cfg(feature="pjsua-native")]` でゲートされ、`(status, transport_id)` を返す。
  - `src/runtime/backend.rs:576` — `PjsuaBackend::initialize(&mut self, _config: &ClientConfig)` が `_config` 未使用で `backend_calls::initialize()` を呼ぶだけ。
  - `src/runtime/backend.rs:590-605` — `SipBackend::create_transport` が `backend_calls::create_transport()`（null config）を呼び `_transport_id` を破棄。`#[cfg(not(pjsua-native))]` で「requires the pjsua-native feature」エラー。
  - `src/runtime/backend.rs:849-860` — `shutdown()` が `backend_calls::shutdown()`（pjsua_destroy）を呼ぶだけ。transport の明示破棄なし。
  - `src/runtime/reactor.rs:732-777` — `DispatchCommand::CreateTransport` が `backend.create_transport(&config)` を呼び、成功時に `match &config` で `(transport_type, port)` を導出（重複マッピング）。`transport_id: (client_state.transports.len() + 1) as i32` と実 id でない値を `TransportRuntimeState` に書く。
  - `src/config/client_config_spec.rs:163` — `ClientConfig.transports: Vec<TransportConfig>`。`validate()` が空配列を拒否（line 228）。
  - `src/config/transport_ice_spec.rs:28-33` — `TransportConfig` enum（Udp / Tcp / `#[cfg(feature="tls")] Tls`）、各バリアントが `bind_addr: SocketAddr`。
  - `src/runtime/state.rs:10-16` — `TransportRuntimeState { transport_id: i32, transport_type: String, port: u16 }`。
  - `src/ffi/bindings.rs` stub — `pjsua_transport_id`（i32 エイリアス）と `PJ_SUCCESS` は存在。`pjsua_transport_config` / `PJSIP_TRANSPORT_UDP/TCP/TLS` / `pjsua_transport_close` は stub に無い。
  - `src/build/build_script_bindgen.rs:25-99` — allowlist に `pjsua_transport_config`（type）・`pjsua_transport_create`（function）・`PJ_SUCCESS`（var）は存在。`pjsua_transport_close` / `PJSIP_TRANSPORT_UDP/TCP/TLS` / `pjsip_transport_type_e` は無い。
  - `src/security/security_platform_diffs.rs:58-75` — `SecretString::as_str()` のみ。`expose_secret()` は未定義だが `backend_calls.rs:87,135` が参照（pjsua-native ビルドの 39 エラーの一部）。
  - `src/ffi/pj_str.rs:24-66` — `PjOwnedStr::new(&str) -> Self` / `as_raw() -> bindings::pj_str_t`。bound_addr の pj_str_t 構築に使用可能。
- 設計判断: `transport_wiring.rs`（src/ffi/ 内）は純粋ロジック + pjsua-native ゲート付き FFI 委譲。unsafe は backend_calls に閉じる（P11-10 不変条件）。オーケストレーション（列挙・破棄）はクロージャ注入でデフォルトビルドでもテスト可能にする。
- エラー型: `ReactorError::NativeError { message, native_status }`（src/runtime/command.rs:16）。`map_pjsua_status`（backend.rs:513）が §62.8 の native_status 保持を担う。

## Acceptance Criteria

- **[Happy path]** `PjsuaBackend::initialize(config)` が `config.transports`（例: UDP 5060 + TCP 5061）を列挙し、各トランスポートを実 config（port / bound_addr）で `pjsua_transport_create` に渡して PJ_SUCCESS を返し、生成した native transport_id を `PjsuaBackend.transport_ids` に 2 件保持する。`shutdown()` が `pjsua_transport_close` で全 id を破棄（各 PJ_SUCCESS）してから `pjsua_destroy` を実行する。デフォルトビルドでは `wire_transports` / `destroy_transports` / `resolve_transport_kind_and_bind_addr` / `resolve_bound_addr_string` / `transport_kind_label` / `apply_transport_port` の全ユニットテストが green。
- **[Error case]** `pjsua_transport_create` が非 PJ_SUCCESS（例: ポート使用中）を返した場合、`initialize` は `ReactorError::NativeError { native_status }` を返し、`transport_ids` に部分生成した id を残さない（fail-fast）。`pjsua_transport_close` が失敗した場合、`shutdown` はそのエラーを伝播し、`pjsua_destroy` を実行しない（握りつぶし禁止, C111）。`wire_transports` は create クロージャの Err をそのまま伝播する。
- **[Edge case]** 未指定アドレス `0.0.0.0:5060` は `bound_addr` 空文字列（PJSIP 全インターフェース bind）に変換される。ポート 0 はそのまま反映される。`#[cfg(feature = "tls")]` が無効なビルドで `TransportConfig::Tls` はコンパイルされず、kind 変換の match は catch-all `_` を使わず全バリアントを網羅する。デフォルトビルド（pjsua-native off）でも crate 全体がコンパイル可能（C058）。

## Invariants

- 【Normal establishment】配線前提条件 — `config.transports` は非空（ClientConfig::validate が §42 で空配列を拒否、src/config/client_config_spec.rs:228）。各 `TransportConfig` の `bind_addr` は有効な `SocketAddr` であり、種別（Udp/Tcp/Tls）は §12 の enum に従う。`pjsua_transport_create` は PJ_SUCCESS（0）を返し、`transport_ids` は `config.transports` と同数・同順序で native id を保持する。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C092 — N0080→N0068

- **Precondition**: §62 親セクション（N0068）が存在し、round 2 section 表（P16-1 の Round2Section）に 62.11 → N0080 が登録されている。テスト可能形: `Round2Section::section()/node_id()`（src/architecture/round2_scope_rootcause.rs）が 62.11 を N0080 に解決する。
- **Postcondition**: 62.11 がトランスポート配線と bindgen 方針を定義する — `crate::ffi::transport_wiring` が配線 API（resolve_transport_kind_and_bind_addr / wire_transports / destroy_transports / build 系）を公開し、`PjsuaBackend::initialize` が config.transports を pjsua_transport_create へ反映する。テスト可能形: transport_wiring モジュールが ffi/mod.rs から到達可能で、配線関数が §12/§27 の想定シグネチャを持つ（UT: 列挙オーケストレーション / 種別変換）。
- **Invariant**: トランスポート配線は §12/§27 と矛盾しない — kind 変換は §12 TransportConfig enum に従い、unsafe FFI は src/ffi/ 内（backend_calls）に閉じ、デフォルトビルドの FFI 経路は `#[cfg(feature = "pjsua-native")]` で除外される。テスト可能形: match が全バリアント網羅 + catch-all 無し、FFI 境界テスト（UT: §12 enum 網羅 / FFI 境界）。

### C093 — N0080→N0037

- **Precondition**: N0037 が §27 PJSIP FFI 層を定義する — `src/ffi/mod.rs` が transport_wiring を宣言し、unsafe は backend_calls（P11-10）が担う。テスト可能形: ffi/mod.rs に `pub mod transport_wiring;` が存在し、backend_calls が unsafe FFI 境界を保持する（C038）。
- **Postcondition**: 62.11 が pjsua_transport_create への反映を規定する — `backend_calls::transport_create(kind, bind_addr)` が null config（std::ptr::null_mut）を廃止し、port と bound_addr を `pjsua_transport_config` に反映して `pjsua_transport_create` を呼ぶ。テスト可能形: `apply_transport_port` が port を反映、`resolve_bound_addr_string` が未指定アドレスを空文字列にする（UT: 実 config 反映）。
- **Invariant**: FFI 呼び出しは §62.8 の native_status 保持に従う — 非 PJ_SUCCESS の status は `ReactorError::NativeError { native_status }` として返り、status が握りつぶされない。テスト可能形: `map_pjsua_status(non_zero, "transport_create")` が native_status を保持した Err を返す（UT: §62.8 native_status 保持）。

### C094 — N0080→N0015

- **Precondition**: N0015 が §12 TransportConfig を定義する — `TransportConfig` enum が Udp / Tcp / `#[cfg(feature="tls")] Tls` の各バリアントを持ち、各バリアントが `bind_addr: SocketAddr` を持つ（src/config/transport_ice_spec.rs:28-33）。テスト可能形: バリアントと bind_addr フィールドの存在をコンパイル時に検証。
- **Postcondition**: 62.11 が config.transports の列挙と種別/ポート反映を規定する — `PjsuaBackend::initialize` が `config.transports`（ClientConfig.transports: Vec<TransportConfig>）を列挙し、各トランスポートの種別と bind_addr ポートを pjsua_transport_create に反映し、生成 id を `PjsuaBackend.transport_ids` に保持する。テスト可能形: `wire_transports` が N 件のトランスポートから N 個の id を順序どおり収集（UT: 列挙オーケストレーション / 内部状態）。
- **Invariant**: UDP/TCP/TLS の種別選択は §12 の enum に従う — Udp → TransportKind::Udp（PJSIP_TRANSPORT_UDP） / Tcp → Tcp（TCP） / Tls → Tls（TLS）の一対一対応で、catch-all `_` を使用しない。テスト可能形: 全バリアントの kind 変換が一対一で決まる（UT: §12 enum 網羅）。

## Boy Scout Rule

- 関数名を動詞句に — `resolve_transport_kind_and_bind_addr`（種別と bind アドレスを解決する）、`wire_transports`（トランスポート群を配線する）、`destroy_transports`（トランスポート群を破棄する）、`resolve_bound_addr_string`（bound_addr 文字列を解決する）、`apply_transport_port`（ポートを反映する）。処理の流れが名前だけで読める。
- 重複マッピングの一元化 — `src/runtime/reactor.rs:740-751` の `TransportConfig → ("udp"/"tcp"/"tls", port)` match を `transport_wiring::transport_kind_label` + `resolve_transport_kind_and_bind_addr` に置換し、§12 の種別知識を一箇所に集約する（DRY + 翻訳可能性）。
- 変数名のドメイン化 — `backend_calls::create_transport()` の `transport_id` を `native_transport_id` に、`cfg` を `transport_cfg` に改名し、FFI 構造体であることを明示する。`_config` / `_transport_id` のアンダースコア接頭辞（未使用マーカー）を廃止し、実際に使用する。
- ハードコード値の定数化 — PJSIP トランスポート種別（PJSIP_TRANSPORT_UDP=0 / TCP=1 / TLS=2）をマジックナンバーでなく `bindings::PJSIP_TRANSPORT_*` 定数参照にする。
- 一関数一責務 — `backend_calls::create_transport()` が「config 構築 + FFI 呼び出し」の 2 責務を持つのを、`apply_transport_port` / `resolve_bound_addr_string`（純粋）と `transport_create`（FFI）に分割する。
- エラー握りつぶしの排除 — 既存の `PjsuaBackend::create_transport` が `_transport_id` を捨てるのは握りつぶしに近い。新配線では id を `transport_ids` に保持し、status は `map_pjsua_status` 経由でエラー化する。
- スコープ外の改善（将来） — `TransportRuntimeState.transport_id` の実 id 化（§62.13）、reactor の CreateTransport 経路への実 id 反映。

## Test Plan

### Unit Tests

- UT: [Normal] §12 種別変換 — `TransportConfig::Udp(UdpTransportConfig { bind_addr: "0.0.0.0:5060" })` を `resolve_transport_kind_and_bind_addr` に渡すと `(TransportKind::Udp, SocketAddr "0.0.0.0:5060")` を返し、`Tcp` → `(TransportKind::Tcp, ...)`、`#[cfg(feature="tls")] Tls` → `(TransportKind::Tls, ...)` を返す。bind_addr は不変のまま、ポート値が保持される（C094 postcondition）。
- UT: [Normal] 実 config 反映 — `apply_transport_port(&mut cfg, 5060)` が `pjsua_transport_config.port` に 5060 を反映する。`resolve_bound_addr_string("0.0.0.0:5060")` が空文字列、`resolve_bound_addr_string("192.168.0.5:5060")` が "192.168.0.5" を返す（C093 postcondition: null config を廃止し port/bound_addr を実 config に反映）。
- UT: [Normal] ラベル変換 — `transport_kind_label(TransportKind::Udp)` が "udp"、`Tcp` → "tcp"、`Tls` → "tls" を返す（reactor.rs:740-751 の重複マッピングを一元化する先）。
- UT: [Normal] 列挙オーケストレーション — `wire_transports(&[udp_5060, tcp_5061], |_| Ok(42))` が `Ok(vec![42, 42])` を返す。トランスポート数 N に対して N 個の native id が順序どおり収集される（C094 postcondition: config.transports の列挙）。
- UT: [Normal] 破棄オーケストレーション — `destroy_transports(&[1, 2], |id| { destroyed.push(id); Ok(()) })` が全 id を破棄し `Ok(())` を返す（§32 手順5 InvokeDestroy: シャットダウンで破棄）。
- UT: [Error] fail-fast — `wire_transports` が 2 番目の create クロージャで Err を返すと、その Err を伝播し 3 番目の create を呼ばない（作成失敗時の部分状態の明確化、握りつぶし禁止）。
- UT: [Error] §62.8 native_status 保持 — `map_pjsua_status(non_zero_status, "transport_create")` が `Err(ReactorError::NativeError { native_status: non_zero_status, .. })` を返す（C093 invariant: FFI 呼び出しは §62.8 の native_status 保持に従う）。
- UT: [Error] 破棄失敗の伝播 — `destroy_transports` が destroy クロージャの Err を伝播する（シャットダウン失敗が無視されない）。
- UT: [Boundary] 未指定アドレス — `resolve_bound_addr_string("0.0.0.0:0")` が空文字列（PJSIP の全インターフェース bind 指定）を返し、ポート 0 が `apply_transport_port` でそのまま反映される。
- UT: [Boundary] IPv6 未指定 — `resolve_bound_addr_string("[::]:5060")` が空文字列を返す（IPv6 ワイルドカード bind）。
- UT: [Invariant] §12 enum 網羅 — kind 変換の match が全バリアントを網羅し、catch-all `_` を使用しない。`TransportKind` は Udp/Tcp/Tls の 3 値のみ（C094 invariant: UDP/TCP/TLS の種別選択は §12 の enum に従う）。
- UT: [Invariant] FFI 境界 — デフォルトビルド（pjsua-native off）で `native_transport_create` / `to_pjsua_transport_type` / `destroy_native_transport` は `#[cfg(feature = "pjsua-native")]` でコンパイル除外され、unsafe は src/ffi/（backend_calls）内に閉じる（C092 invariant: §12/§27 と矛盾しない / C038）。
- UT: [Invariant] 内部状態 — `PjsuaBackend.transport_ids` は initialize 成功後に config.transports と同数になり、shutdown（destroy_transports）後に空になる。`wire_transports` / `destroy_transports` の純粋ロジックに mock クロージャを注入して検証（実 FFI は pjsua-native 専用）。
- UT: [Normal] C092 precondition — round 2 section 表（`Round2Section`、src/architecture/round2_scope_rootcause.rs）が 62.11 を N0080 に解決し、`crate::ffi::transport_wiring` が `ffi/mod.rs`（N0068 §62 親セクション配下の配線モジュール）から到達可能である（配線 API: resolve_transport_kind_and_bind_addr / wire_transports / destroy_transports が公開される）。

### Integration Tests

- IT: [Integration point] ffi 層（transport_wiring + backend_calls）と runtime 層（PjsuaBackend::initialize / shutdown）の接続 — pjsua-native ビルドで `PjsuaBackend::initialize(config)` が `config.transports`（UDP 5060 + TCP 5061）を列挙し、各トランスポートを `pjsua_transport_create` に渡して `PjsuaBackend.transport_ids` に 2 件の native id を保持する。
- IT: [Verification] Asterisk（docker）上で UDP(5060) トランスポート生成の実機確認 — `pjsua_transport_create` が PJ_SUCCESS（0）を返し transport_id が 0 以上。生成後、SIP REGISTER が Asterisk に到達し 401/200 応答を受信できる。shutdown 時に `pjsua_transport_close` が各 id に対して PJ_SUCCESS を返す。
- IT: [Prerequisites] `pjsua-native` feature 有効 + システム PJSIP または `vendor/prebuilt/{target}/lib` の libpjsua*、docker で Asterisk コンテナ起動（P16-8 §62.19 の基盤）、UDP/TCP 5060 ポートが空いていること、`config.transports` に UDP(5060) を設定。
- IT: [Related tickets] P16-1（§62.10 blueprint: config.transports 未反映の根因 RC1）、P16-8（§62.19 Docker/Asterisk 統合テスト基盤）、P16-9（§62.17 STUN/TURN/ICE 配線と並行動作）。

### Exceptions

- Exception 1 — [Item] TLS トランスポート生成の実機確認。 [Reason] TLS は `#[cfg(feature = "tls")]` でゲートされ、デフォルトビルドでは `TransportConfig::Tls` バリアント自体がコンパイルされないためテスト不能（cannot test the TLS variant in the default build）。TLS 対応 PJSIP ビルドと証明書（ca_cert / client_cert / client_key）が必要で、CI のデフォルト環境では実行不能。これは機能ゲートによる意図的な分岐であり、設計欠陥ではない（not an architectural defect）— kind 変換（Tls → TransportKind::Tls）は `#[cfg(feature = "tls")]` 付きユニットテストで検証可能。 [Alternative verification] `#[cfg(feature = "tls")]` でゲートした `resolve_transport_kind_and_bind_addr` の Tls ケース単体テスト + TLS 統合は P16-9（§62.17）で実施。
- Exception 2 — [Item] `pjsua_transport_create` / `pjsua_transport_close` の実 FFI 呼び出し（デフォルトビルド）。 [Reason] 実 PJSIP ライブラリが無いと実行不能で、デフォルトビルドではテスト不能（cannot test the real FFI call without pjsua-native）。デフォルトビルドは stub（bindings.rs stub_aliases）であり、実 FFI は `pjsua-native` ビルド専用。これはプラットフォーム依存であり、設計欠陥ではない（not an architectural defect）— 純粋ロジック（kind 変換 / port 反映 / 列挙・破棄オーケストレーション）はデフォルトビルドでテスト可能なため、テスト不能なのは FFI 境界のみ。 [Alternative verification] 純粋関数（resolve_transport_kind_and_bind_addr / resolve_bound_addr_string / apply_transport_port / wire_transports / destroy_transports）のユニットテスト + pjsua-native ビルドでの Asterisk 統合テスト。
- Exception 3 — [Item] 実 Asterisk との SIP トランザクション（OPTIONS / REGISTER）。 [Reason] docker + 外部 SIP サーバ + ネットワーク疎通が必要で、単体テスト環境ではテスト不能（cannot test against a live Asterisk in unit tests）。CI に docker ゲート（P16-8 §62.19）が導入されるまでは自動化不可。これはインフラ依存であり、設計欠陥ではない（not an architectural defect）— トランスポート生成のオーケストレーションと config 反映はデフォルトビルドで検証可能。 [Alternative verification] 統合テストは docker 利用時に実行（P16-8 でゲート）。デフォルトでは config 反映の純粋関数テスト + map_pjsua_status の native_status 保持テストで代替検証。

### Plan Test Code (concrete code)

- C092-Precondition → test input code:
  ```rust
  use crate::architecture::round2_scope_rootcause::Round2Section;
  // Pre: §62 parent section exists; round-2 table resolves 62.11 → N0080
  assert_eq!(Round2Section::TransportWiring.section(), "62.11");
  assert_eq!(Round2Section::TransportWiring.node_id(), "N0080");
  // Pre: transport_wiring module is reachable from ffi/mod.rs (compile-time)
  let _ = crate::ffi::transport_wiring::resolve_transport_kind_and_bind_addr;
  ```
- C092-Postcondition → assertion code:
  ```rust
  use crate::ffi::transport_wiring::{wire_transports, TransportKind};
  use crate::config::transport_ice_spec::TransportConfig;
  // Post: 62.11 defines transport wiring — wire_transports enumerates & collects
  let transports = vec![TransportConfig::udp(5060), TransportConfig::tcp(5061)];
  let collected: Vec<i32> = wire_transports(&transports, |_t| Ok(42)).unwrap();
  assert_eq!(collected, vec![42, 42]);
  ```
- C092-Invariant → predicate code:
  ```rust
  use crate::ffi::transport_wiring::{resolve_transport_kind_and_bind_addr, TransportKind};
  use crate::config::transport_ice_spec::TransportConfig;
  // Inv: wiring consistent with §12/§27 — exhaustive match, no catch-all
  let udp = resolve_transport_kind_and_bind_addr(&TransportConfig::udp(5060));
  assert_eq!(udp.0, TransportKind::Udp);
  assert_eq!(udp.1.port(), 5060);
  let tcp = resolve_transport_kind_and_bind_addr(&TransportConfig::tcp(5061));
  assert_eq!(tcp.0, TransportKind::Tcp);
  assert_eq!(tcp.1.port(), 5061);
  ```
- C093-Precondition → test input code:
  ```rust
  // Pre: §27 FFI layer — ffi/mod.rs declares transport_wiring (compile-time);
  // unsafe stays in backend_calls. The pure helpers below must compile in the
  // default (stub) build, proving the FFI boundary holds.
  let mut cfg: crate::ffi::bindings::pjsua_transport_config = unsafe { std::mem::zeroed() };
  crate::ffi::transport_wiring::apply_transport_port(&mut cfg, 5060);
  ```
- C093-Postcondition → assertion code:
  ```rust
  use crate::ffi::transport_wiring::{apply_transport_port, resolve_bound_addr_string};
  // Post: 62.11 reflects kind/port into pjsua_transport_create (no null config)
  let mut cfg: crate::ffi::bindings::pjsua_transport_config = unsafe { std::mem::zeroed() };
  apply_transport_port(&mut cfg, 5060);
  assert_eq!(cfg.port, 5060);
  assert_eq!(resolve_bound_addr_string("0.0.0.0:5060".parse().unwrap()), "");
  assert_eq!(resolve_bound_addr_string("192.168.0.5:5060".parse().unwrap()), "192.168.0.5");
  ```
- C093-Invariant → predicate code:
  ```rust
  use crate::runtime::backend::map_pjsua_status;
  use crate::runtime::command::ReactorError;
  // Inv: FFI calls preserve native_status (§62.8)
  let err = map_pjsua_status(70013, "transport_create").unwrap_err();
  assert!(matches!(err, ReactorError::NativeError { native_status: 70013, .. }));
  assert!(map_pjsua_status(0, "transport_create").is_ok());
  ```
- C094-Precondition → test input code:
  ```rust
  use crate::config::transport_ice_spec::{TransportConfig, UdpTransportConfig, TcpTransportConfig};
  // Pre: §12 TransportConfig enum — each variant carries bind_addr: SocketAddr
  let udp = TransportConfig::Udp(UdpTransportConfig { bind_addr: "0.0.0.0:5060".parse().unwrap() });
  let tcp = TransportConfig::Tcp(TcpTransportConfig { bind_addr: "0.0.0.0:5061".parse().unwrap() });
  match &udp { TransportConfig::Udp(c) => assert_eq!(c.bind_addr.port(), 5060), _ => panic!("expected Udp") }
  match &tcp { TransportConfig::Tcp(c) => assert_eq!(c.bind_addr.port(), 5061), _ => panic!("expected Tcp") }
  ```
- C094-Postcondition → assertion code:
  ```rust
  use crate::ffi::transport_wiring::{wire_transports, destroy_transports};
  use crate::config::transport_ice_spec::TransportConfig;
  // Post: 62.11 enumerates config.transports, keeps ids, destroys at shutdown
  let transports = vec![TransportConfig::udp(5060), TransportConfig::tcp(5061)];
  let ids = wire_transports(&transports, |_t| Ok(10)).unwrap();
  assert_eq!(ids, vec![10, 10]);
  let mut destroyed = Vec::new();
  destroy_transports(&ids, |id| { destroyed.push(id); Ok(()) }).unwrap();
  assert_eq!(destroyed, vec![10, 10]);
  ```
- C094-Invariant → predicate code:
  ```rust
  use crate::ffi::transport_wiring::{resolve_transport_kind_and_bind_addr, TransportKind};
  use crate::config::transport_ice_spec::TransportConfig;
  // Inv: UDP/TCP/TLS selection follows §12 enum — one-to-one, no catch-all
  match resolve_transport_kind_and_bind_addr(&TransportConfig::udp(5060)).0 {
      TransportKind::Udp => {}
      _ => panic!("Udp must map to TransportKind::Udp"),
  }
  match resolve_transport_kind_and_bind_addr(&TransportConfig::tcp(5060)).0 {
      TransportKind::Tcp => {}
      _ => panic!("Tcp must map to TransportKind::Tcp"),
  }
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| src/ffi/transport_wiring.rs was a [::STUB::] placeholder (header only) | Implemented the transport wiring module: TransportKind enum, resolve_transport_kind_and_bind_addr, transport_kind_label, resolve_bound_addr_string, apply_transport_port, to_pjsua_transport_type (cfg-paired constants), wire_transports/destroy_transports orchestration, and pjsua-native-gated native_transport_create/destroy_native_transport delegating to backend_calls. 15 unit tests. | TS-001 stub resolved (marker replaced with real implementation) |
| backend_calls::create_transport() called pjsua_transport_create(std::ptr::null_mut(), ...) — null config | backend_calls::transport_create(kind, bind_addr) builds a real pjsua_transport_config (port via apply_transport_port, bound_addr via PjOwnedStr) and calls pjsua_transport_create; close_transport(id) added for pjsua_transport_close | Null config abolished (§62.11) |
| PjsuaBackend had only audio_taps field; initialize ignored config | PjsuaBackend.transport_ids: Vec<pjsua_transport_id> added; initialize enumerates config.transports via wire_transports and stores native ids; shutdown drains transport_ids via destroy_transports before pjsua_destroy; create_transport stores the native id | Transport lifecycle wired into PjsuaBackend |
| bindings.rs stub lacked pjsua_transport_config and PJSIP_TRANSPORT_* constants | Added pjsua_transport_config (port/bound_addr) and PJSIP_TRANSPORT_UDP=1/TCP=2/TLS=3 matching enum pjsip_transport_type_e; build_script_bindgen.rs allowlist extended with pjsip_transport_type_e, PJSIP_TRANSPORT_*, pjsua_transport_close | bindgen alignment (stub/allowlist sync) |
| SecretString had only as_str() | Added SecretString::expose_secret() alias for the FFI marshalling boundary (RFC §28 addendum item 3) | expose_secret implemented |
| round2_scope_rootcause.rs RC1 evidence asserted null config still present | Updated RC1 evidence assertion to verify null config is gone and apply_transport_port is wired (P16-1 blueprint anticipated this) | P16-1 source-consistency test updated for the §62.11 change |

## Notes in Prior Implementation Rounds

- 【Implementation steps】①`transport_wiring.rs` に純粋ロジック（TransportKind / resolve_transport_kind_and_bind_addr / transport_kind_label / resolve_bound_addr_string / wire_transports / destroy_transports）を実装し、デフォルトビルドでテスト（RED→GREEN）。②`bindings.rs` stub に `pjsua_transport_config` / `PJSIP_TRANSPORT_UDP/TCP/TLS` / `pjsua_transport_close` を追加し、`build_script_bindgen.rs` の allowlist に同期追加。③`backend_calls.rs` の `create_transport()` を `transport_create(kind, bind_addr)` + `close_transport(id)` に置換（null config 廃止）。④`ffi/mod.rs` に `pub mod transport_wiring;` + re-export。⑤`backend.rs` の `PjsuaBackend` に `transport_ids` 追加、`initialize` / `shutdown` を配線。⑥`SecretString::expose_secret` 実装。⑦`make test` で全テスト green 確認 → `make check-be` で clippy/fmt 確認。
- 【Risks】・`pjsua_transport_close` の強制クローズ（force=0）はアクティブな登録/通話があると失敗する — shutdown 順序（§32: 先に UnregisterAccounts → CancelCalls）が前提。・stub の `pjsua_transport_config` と bindgen 出力のフィールド型不一致（port: u32 vs c_uint）で型エラー — 実装時に `as _` キャストで吸収。・`PjOwnedStr` のライフタイム: `bound_addr` は FFI コール中のみ有効なため `transport_create` 関数内で生成・保持する（構造体に埋め込んで返さない）。
- 【Caveats】`SipBackend::create_transport` trait メソッド（reactor の CreateTransport コマンド経由）は現状 `Result<(), _>` で id を返さない。本チケットでは initialize 配線を主とし、trait シグネチャ変更は行わない（破壊的変更回避）。reactor の `TransportRuntimeState.transport_id` が実 id でなく `(len()+1)` を書く問題は §62.13 で対応。
- 【Open items】・`pjsua_transport_config` の stub フィールドを最小（port / bound_addr）にするか、bindgen と完全一致させるか。・`pjsua_transport_close` の force フラグをどう扱うか（デフォルト 0）。・TLS トランスポートの kind 定数（PJSIP_TRANSPORT_TLS）が pjsua-native の bindgen でどう現れるか（constified enum か生定数か）。
- 【Future improvements】・reactor の `CreateTransport` 経路にも実 native id を反映し、`TransportRuntimeState.transport_id` を実 id 化する（§62.13 / P16-4）。・STUN/TURN/ICE 設定を `pjsua_config` へ反映（§62.17 / P16-9）。・トランスポート状態変化イベント（TransportStateChanged）の配線（§62.13）。
Implementation summary (P16-2):
- Changed files: src/ffi/transport_wiring.rs (new impl), src/ffi/mod.rs (module decl), src/ffi/backend_calls.rs (transport_create/close_transport), src/ffi/bindings.rs (stub sync), src/build/build_script_bindgen.rs (allowlist), src/runtime/backend.rs (transport_ids + initialize/shutdown wiring), src/security/security_platform_diffs.rs (expose_secret), src/architecture/round2_scope_rootcause.rs (RC1 evidence update)
- TDD: 15 transport_wiring unit tests (RED on todo!() stubs → GREEN on implementation); full make test = 1151 lib + integration + doc tests pass
- cargo check (default) clean; pjsua-native build: 37 pre-existing bindgen-alignment errors remain (down from 39), all in pre-existing code (PJ_SUCCESS emission, constified enums, registrar_uri field) — deferred per RFC N0080 §27/§28 addendum to later implementation tickets; P16-2 adds zero new pjsua-native errors
- Verification: make test (all pass), cargo check (default), validate-no-external-excuses (pass), verify-red-coverage (3/3 contracts), validate-ticket-targets (verified_empty)
Review report (P16-2):
- Crime scan: 0 crimes
- STUB evaluation: TS-001 resolved (transport_wiring.rs implemented); 9 remaining stubs correctly deferred to P16-3..P16-11
- No-excuse gate: pass (0 fail)
- Annotation check: 8 files verified, 0 missing, 0 ambiguous
- Incomplete-implementation scan: clean (no todo!/unimplemented!/FIXME/#[allow] without marker)
- Compilation: cargo check clean; make test all pass
- Implementation completeness: RFC N0080 design fully satisfied — initialize enumerates config.transports, transport_ids lifecycle (create→store→destroy), §62.8 native_status preserved, bindgen alignment (stub constants + allowlist + expose_secret)
- Static quality: passed
- Translatability: passed
- Issue found and fixed during review: replaced #[cfg_attr(not(feature="pjsua-native"), allow(dead_code))] on transport_ids with #[cfg(feature="pjsua-native")] field gating (no suppression, cleaner)

## P16-2 — implemented at 38 locations

### src/architecture/round2_scope_rootcause.rs

- Line 452
```rust
    fn round2_evidence_matches_actual_source() -> Result<(), std::io::Error> {
```

### src/build/build_script_bindgen.rs

- Line 26
```rust
    "pjsua_acc_id",
```

- Line 70
```rust
    "pjsua_call_get_info",
```

- Line 107
```rust
    "PJ_SUCCESS",
```

### src/ffi/backend_calls.rs

- Line 60
```rust
    let mut cfg: bindings::pjsua_transport_config = unsafe { std::mem::zeroed() };
```

- Line 87
```rust
    // SAFETY: transport_id is a valid pjsua_transport_id; force = 0 (pj_bool_t)
```

### src/ffi/bindings.rs

- Line 75
```rust
    /// TCP transport (`PJSIP_TRANSPORT_TCP` = 2).
```

- Line 227
```rust
        /// Local port to bind (0 selects a PJSIP-assigned port).
```

### src/ffi/mod.rs

- Line 64
```rust

```

### src/ffi/transport_wiring.rs

- Line 115
```rust
fn transport_udp_value() -> i32 {
```

- Line 120
```rust
fn transport_udp_value() -> i32 {
```

- Line 126
```rust
fn transport_tcp_value() -> i32 {
```

- Line 131
```rust
fn transport_tcp_value() -> i32 {
```

- Line 138
```rust
fn transport_tls_value() -> i32 {
```

- Line 144
```rust
fn transport_tls_value() -> i32 {
```

- Line 207
```rust
    fn udp(port: u16) -> TransportConfig {
```

- Line 212
```rust
    fn tcp(port: u16) -> TransportConfig {
```

- Line 219
```rust
    fn round2_section_resolves_62_11_to_n0080() {
```

- Line 227
```rust
    fn resolve_transport_kind_and_bind_addr_maps_udp_to_udp_kind() {
```

- Line 235
```rust
    fn resolve_transport_kind_and_bind_addr_maps_tcp_to_tcp_kind() {
```

- Line 244
```rust
    fn resolve_transport_kind_and_bind_addr_maps_tls_to_tls_kind() {
```

- Line 257
```rust
    fn apply_transport_port_reflects_port_into_config() {
```

- Line 265
```rust
    fn resolve_bound_addr_string_unspecified_ipv4_is_empty() {
```

- Line 271
```rust
    fn resolve_bound_addr_string_specific_ip_returns_ip_string() {
```

- Line 280
```rust
    fn resolve_bound_addr_string_unspecified_ipv6_is_empty() {
```

- Line 286
```rust
    fn transport_kind_label_returns_kind_strings() {
```

- Line 296
```rust
    fn to_pjsua_transport_type_maps_kinds_to_constants() {
```

- Line 307
```rust
    fn wire_transports_collects_ids_for_each_transport() {
```

- Line 315
```rust
    fn wire_transports_stops_at_first_error() {
```

- Line 332
```rust
    fn destroy_transports_destroys_each_id() {
```

- Line 344
```rust
    fn destroy_transports_propagates_error() {
```

- Line 358
```rust
    fn map_pjsua_status_preserves_native_status() {
```

### src/runtime/backend.rs

- Line 133
```rust
    fn conf_disconnect(&mut self, source: i32, sink: i32) -> Result<(), ReactorError>;
```

- Line 551
```rust
impl PjsuaBackend {
```

- Line 589
```rust
    fn initialize(&mut self, _config: &crate::config::ClientConfig) -> Result<(), ReactorError> {
```

- Line 612
```rust
    fn create_transport(
```

- Line 874
```rust
    fn shutdown(&mut self) -> Result<(), ReactorError> {
```

### src/security/security_platform_diffs.rs

- Line 57
```rust
impl SecretString {
```
