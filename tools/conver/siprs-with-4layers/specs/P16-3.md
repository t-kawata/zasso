# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P16-3: Registration & account lifecycle (§62.12)

**Ticket Key**: P16-3 · **Phase**: 16

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
§62.12（N0081）を実装する。register_on_start: true を add_account 時に消費して自動 REGISTER を発行し、remove_account を unregister 先行シーケンスに変更して AccountRemoved(AccountSnapshot) を publish し、RegistrationSucceeded / RegistrationFailed を SipEventPayload から完全削除して RegistrationStateChanged に統一する。

### Purpose
README.md:120-126 に「本番が publish するのは RegistrationStateChanged のみ。RegistrationSucceeded/Failed は enum に定義されるが #[cfg(test)] 内でしか構築されず、examples/account_register.rs:79-87 は本番ではタイムアウト失敗する（壊れた例）」と記録された README RESIDUE を解消する。登録結果イベントを単一の型付きイベント（§17 状態機械の出力）に統一し、アカウント除去をクライアントが購読可能なイベント（AccountRemoved）として公開する。

### Motivation
§62（N0068）の 9 設計判断の 1 つ（Q2: 62.12 登録・アカウント経路）を確定するため。現在の RemoveAccount アーム（reactor.rs:648）は unregister も AccountRemoved publish も行わないため、クライアントはアカウント除去をイベントで観測できない。また、壊れた example が README に RESIDUE として残っており、N0068 の方針（破壊的変更を v0.x の今一度に完了させる）に従って公開 API enum の残骸バリアントを除去する。

### Constraints
- 破壊的変更の残骸（deprecated 別名等）は残さない（N0081 I/O 境界）。
- 登録状態遷移は §62.4 の registr_state_machine::transition / registr_wiring.rs（N0073）が一元的に決定し、62.12（N0081）は追補に留める（C095 / C096 / C097）。
- 実 REGISTER 成功時のみ Registered へ遷移する（C096 invariant）。
- 変更対象: src/state/reg_account_lifecycle.rs（implement）、src/runtime/reactor.rs（AddAccount / RemoveAccount アーム）、src/api/event_model_payload_bus.rs（enum 削除）、examples/account_register.rs、README.md。

## Scope

- register_on_start: true を add_account 時に消費し、アカウント追加直後に自動 REGISTER を発行する。
- RegistrationSucceeded / RegistrationFailed を SipEventPayload から完全削除し RegistrationStateChanged に統一する（example/README 修正含む）。
- remove_account は unregister 先行 → backend.remove_account → ClientState 除去 → AccountRemoved(AccountSnapshot) publish。
- **Scope of changes:** src/state/reg_account_lifecycle.rs（implement: STUB 解決）/ src/runtime/reactor.rs（modify: AddAccount/RemoveAccount アーム）
- **Scope of changes (each change in detail):**
  - [Path] src/state/reg_account_lifecycle.rs（新規実装。現状は `pub enum State {}` + `// [::STUB::] P16-3` のみ）
  - [Action] implement（STUB 解決）
  - [Detail] §62.12 の wiring 関数を公開する: `should_auto_register(&AccountConfig) -> bool`（register_on_start の消費判定）、`build_account_snapshot(&AccountEntry) -> Option<AccountSnapshot>`（ClientState エントリ → 公開スナップショット写像。client.rs の private fn account_snapshot_from_entry を移設）、`classify_registration_outcome(&SipEventPayload) -> Option<&'static str>`（Registration 系バリアントの網羅照合 → C097 の静的証明）
  - [Path] src/runtime/reactor.rs（AddAccount アーム line ~379 / RemoveAccount アーム line ~648）
  - [Action] modify
  - [Detail] AddAccount アーム: backend.add_account → client_state.accounts.insert の後に `if should_auto_register(&config) { backend.set_registration(native_id, true)?; apply_registration_command_state(&mut client_state, account_id, true); }`。RemoveAccount アーム: (1) client_state.accounts から native_id を取得 → (2) `backend.set_registration(native_id, false)?`（unregister 先行）→ (3) `backend.remove_account(native_id)?` → (4) `client_state.accounts.remove(&aid)` → (5) build_account_snapshot で AccountSnapshot を生成し `dispatch_event(&event_bus, SipEvent { meta: EventMeta::account(aid), payload: SipEventPayload::AccountRemoved(snapshot) })`
  - [Path] src/api/event_model_payload_bus.rs（SipEventPayload enum line 294）
  - [Action] remove
  - [Detail] `RegistrationSucceeded(RegistrationInfo)`（line 299）と `RegistrationFailed(RegistrationFailure)`（line 301）を enum から削除。対応するテスト fn payload_registration_succeeded（line 542）/ payload_registration_failed（line 561）を削除。RegistrationInfo は RegistrationStarted が、RegistrationFailure は UnregistrationFailed が引き続き使うため構造体は維持
  - [Path] examples/account_register.rs（await_registration fn line 79-87）
  - [Action] modify
  - [Detail] RegistrationSucceeded / RegistrationFailed の match を `RegistrationStateChanged(RegistrationState::Registered)` → Succeeded、`RegistrationStateChanged(RegistrationState::Failed)` → Failed（メッセージのみ）に変更
  - [Path] README.md（line 120-126, 408, 421）
  - [Action] modify
  - [Detail] 「RegistrationSucceeded/Failed は API 互換用に留保（specs/P15-5.md:92）」を削除し、イベントが RegistrationStateChanged に統一されたことと example 修正完了を記載
  - [API contract change] SipEventPayload の public enum から RegistrationSucceeded / RegistrationFailed を削除（破壊的変更）。`#[non_exhaustive]` のため外部クレートはワイルドカード必須で影響は限定的
  - [Schema] なし（enum バリアント削除のみ。RegistrationInfo / RegistrationFailure / AccountSnapshot 構造体は維持）
  - [Config] なし（AccountConfig.register_on_start は既存フィールドを消費するのみ。新規設定なし）
  - [Dependency] なし
- **Out of scope (intentionally excluded):**
  - [Excluded] client.rs の add_account / remove_account public API シグネチャ変更
  - [Why] 公開 API は既に RuntimeCommand 経由で reactor に委譲しており、reactor アームの内部変更で要件を満たせるため
  - [Excluded] AccountAdded / AccountConfigChanged の publish 追加
  - [Why] N0081 の I/O 境界は AccountRemoved publish のみを要求する（AccountAdded は現在も publish されておらず、本チケットのスコープ外）
  - [Excluded] SetRegistration / UpdateAccount アームの register_on_start 消費ロジック変更
  - [Why] §62.4（reactor.rs:610）で既に実装済み。62.12 は add_account 時の消費を追補するのみ
- **Affected areas:**
  - [Component] SipEventPayload consumers（client.rs イベントマッチ、example、README、外部 API 利用者）
  - [Nature] API surface — enum バリアント削除による破壊的変更
  - [Response] クレート内の参照（examples/account_register.rs, README.md）を RegistrationStateChanged に統一して修正。外部利用者は #[non_exhaustive] のワイルドカードで影響を吸収
  - [Component] EventBus / subscribe_account 購読者
  - [Nature] 新イベント（AccountRemoved）の配信
  - [Response] 変更不要（既存の dispatch_event / EventBus 経路をそのまま使用）
  - [Component] ClientState（runtime/state.rs AccountEntry）
  - [Nature] データ一貫性 — RemoveAccount の失敗時原子性
  - [Response] unregister 先行でエラー時に部分除去しない設計を維持（UT: [Invariant] エラー原子性）

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0081`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0081` | state_machine | rust | 62.12 登録・アカウント経路（自動登録 / unregister 先行 / AccountRemoved） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| refines | ★ 62.12 登録・アカウント経路（自動登録 / unregister 先行 / AccountRemoved） (N0081) | → | ☆ 62.4 登録状態機械の production 配線 (N0073) |
| extends | ★ 62.12 登録・アカウント経路（自動登録 / unregister 先行 / AccountRemoved） (N0081) | → | ☆ §17 Registration State Machine (N0025) |
| part_of | ★ 62.12 登録・アカウント経路（自動登録 / unregister 先行 / AccountRemoved） (N0081) | → | ☆ §62 実装整合設計 — RESIDUE 解消のための設計判断確定 (N0068) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0081` | `src/state/reg_account_lifecycle.rs` |

## Investigation

- src/state/reg_account_lifecycle.rs（23行）は `pub enum State {}` + `// [::STUB::] P16-3: implement NODE_ID=N0081: 62.12 登録・アカウント経路` のみの空ファイル。実装が必要。
- SipEventPayload（src/api/event_model_payload_bus.rs:294-309）: RegistrationStarted(RegistrationInfo) / RegistrationSucceeded(RegistrationInfo) / RegistrationFailed(RegistrationFailure) / UnregistrationSucceeded / UnregistrationFailed(RegistrationFailure) / RegistrationExpired / RegistrationStateChanged(RegistrationState)。Account セクション（line 373-379）: AccountAdded / AccountRemoved / AccountConfigChanged（全て AccountSnapshot）。RegistrationSucceeded/Failed は #[cfg(test)] 内テスト（line 542-579）と examples/account_register.rs:79-87 と README.md でのみ参照され、非テスト本番コードからの参照はなし（grep 確認）。
- reactor.rs AddAccount アーム（line 379-418）: backend.add_account → client_state.accounts.insert → reply.send。register_on_start 未消費・AccountAdded 未 publish。RemoveAccount アーム（line 648-676）: backend.remove_account → client_state.accounts.remove → reply.send。unregister 先行なし・AccountRemoved 未 publish。
- reactor.rs には event_bus（line 148）と dispatch_event(event_bus, event)（line 962）が利用可能。SetRegistration/UpdateAccount アームは apply_registration_command_state（registr_wiring.rs:96, import line 24）で ClientState を Registering / Unregistering に進める既存パターンあり（line 610, 704）。
- registr_wiring.rs（N0073）: process_registration_state_changed（line 53）が NativeEvent::RegistrationStateChanged → §17.1 遷移 → SipEventPayload::RegistrationStateChanged(next) を生成。apply_registration_command_state（line 96）が set_registration(enabled) を Registering / Unregistering エッジに写像（can_transition_to ガード）。
- AccountEntry（src/runtime/state.rs:60）: id (u64), native_id (i32), config: AccountConfig, registration: RegistrationState。client.rs の private fn account_snapshot_from_entry（line 582-620）が AccountEntry → AccountSnapshot に写像（registered = registration == Registered）。reactor が AccountRemoved を publish するにはこの写像の共有化（reg_account_lifecycle.rs へ移設）が必要。
- Backend trait（src/runtime/backend.rs）: add_account（line 49）、remove_account(&mut self, native_acc_id: i32)（line 56）、set_registration(&mut self, native_acc_id: i32, enabled: bool)（line 71）。TestBackend（line 266-307）がモック実装。
- command.rs RuntimeCommand（line 109）: AddAccount { config, reply: Reply<Result<u64>> }（line 114）、RemoveAccount { account_id, reply }（line 131）、SetRegistration（line 140）、UpdateAccount { register_on_start: Option<bool> }（line 125）。UpdateAccount の register_on_start は §62.4 のパッチデルタ（handle.rs:450-470）。
- README.md:120-126: 「RegistrationSucceeded/Failed は API 互換用に留保（specs/P15-5.md:92）」— P16-3 でこの留保を撤廃し完全削除する。
- RegistrationState（§17, N0025）: Disabled / Idle / Registering / Registered / Unregistering / Failed / Expired の 7 値（m20_registr_cmd_pat.rs:135-142）。

## Acceptance Criteria

- **[Happy path]** register_on_start: true の AccountConfig で add_account すると、アカウント追加直後に自動 REGISTER が発行され、ClientState が Registering になり、実 REGISTER 成功で RegistrationStateChanged(Registered) を受信する。remove_account すると unregister が先行実行され、backend.remove_account → ClientState 除去 → AccountRemoved(AccountSnapshot) publish の順に実行され、subscribe_account が AccountRemoved を受信する。SipEventPayload に RegistrationSucceeded / RegistrationFailed は存在せず、examples/account_register.rs が RegistrationStateChanged 待ちで正常終了する。
- **[Error case]** RemoveAccount で unregister（set_registration(false)）が失敗した場合は reply にエラーが返り、remove_account は実行されず、ClientState エントリは保持され、AccountRemoved は publish されない（部分除去なし）。AddAccount で backend.add_account が失敗した場合は set_registration は発行されず ClientState は不変。
- **[Edge case]** register_on_start=false では自動 REGISTER は一切発行されない。remove_account(0) は backend 呼び出し前に Err(AccountNotFound) を返す。最後のアカウントを remove_account すると ClientState.accounts が空になり、AccountRemoved がちょうど 1 回 publish される。build_account_snapshot は registration == Registered のときだけ registered: true を返す。

## Invariants

- [Normal condition] ClientState.accounts[].registration は §17 RegistrationState 7 値（Disabled / Idle / Registering / Registered / Unregistering / Failed / Expired）のいずれかであり、add_account + register_on_start=true 直後は Registering、実 REGISTER 成功（Registering からの 200）で Registered になる。
- [Error invariant] RemoveAccount シーケンス（unregister 先行 → backend.remove_account → ClientState 除去 → AccountRemoved publish）のどのステップでエラーになっても ClientState は一貫性を保つ。backend.remove_account が成功したときだけエントリが除去され、エラー時は除去されない（部分除去なし）。AddAccount で backend.add_account が失敗した場合は ClientState は不変のまま。
- [Internal state invariant] SipEventPayload の登録系バリアントは RegistrationStarted / UnregistrationSucceeded / UnregistrationFailed / RegistrationExpired / RegistrationStateChanged の 5 つに確定し、RegistrationSucceeded / RegistrationFailed は存在しない。登録結果通知は RegistrationStateChanged(Registered / Failed) に統一される。
- [Boundary invariant] register_on_start は真偽 2 値のみ（true で自動 REGISTER 発行 / false で非発行）。build_account_snapshot の registered は registration == Registered のときだけ true。account_id == 0 は無効値で remove_account は backend 呼び出し前に Err(AccountNotFound) を返す。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C095 — N0081→N0068

- **Precondition**: §62 親セクションが存在する（N0068: §62 実装整合設計 — RESIDUE 解消の 9 設計判断の親セクション。入力として 62.12 の設計判断が §62 に属する）
- **Postcondition**: 62.12 が登録・アカウント経路を定義する（reg_account_lifecycle.rs が should_auto_register / build_account_snapshot / classify_registration_outcome を公開し、reactor の AddAccount/RemoveAccount アームがそれらを呼ぶ。出力 = backend.set_registration(id, enabled) 呼び出し + AccountRemoved(AccountSnapshot) publish）
- **Invariant**: 登録経路は §17 の状態遷移と矛盾しない（ClientState.accounts[].registration は apply_registration_command_state と process_registration_state_changed のみで遷移し、自動登録は Registering まで）

### C096 — N0081→N0025

- **Precondition**: N0025 が §17 登録状態機械を定義する（RegistrationState 7 値: Disabled / Idle / Registering / Registered / Unregistering / Failed / Expired）
- **Postcondition**: 62.12 が register_on_start 自動登録とイベント統一を規定する（add_account 直後に set_registration(true) + apply_registration_command_state(Registering)。登録結果通知は SipEventPayload::RegistrationStateChanged に統一）
- **Invariant**: 実 REGISTER 成功時のみ Registered へ遷移する（process_registration_state_changed が Registering からの status 200 のときだけ Registered を生成して publish する）

### C097 — N0081→N0073

- **Precondition**: N0073 が 62.4 登録状態機械の production 配線を定義する（registr_wiring.rs が process_registration_state_changed / apply_registration_command_state を提供）
- **Postcondition**: 62.12 が add_account 時の自動登録と AccountRemoved を追補する（RemoveAccount は unregister 先行 → backend.remove_account → ClientState 除去 → AccountRemoved(AccountSnapshot) publish の順に実行）
- **Invariant**: RegistrationSucceeded/Failed は残さない（SipEventPayload から完全削除し deprecated 別名も残さない。example/README も RegistrationStateChanged に統一）

## Boy Scout Rule

- src/state/reg_account_lifecycle.rs の空 enum `pub enum State {}` を廃止し、§62.12 の wiring 関数群（should_auto_register / build_account_snapshot / classify_registration_outcome）を名前が動詞句の関数として実装する（翻訳可能性: 「register_on_start を消費すべきか判定する」「ClientState エントリを公開スナップショットへ写像する」「登録結果バリアントを分類する」）。
- client.rs の private fn account_snapshot_from_entry（client.rs:582-620）を reg_account_lifecycle.rs の build_account_snapshot として移設・公開（pub(crate)）し、reactor（AccountRemoved publish）と client（accounts()）で重複しないようにする。
- reactor.rs の RemoveAccount アーム（現在 backend.remove_account → accounts.remove の 2 段）を「unregister 先行 → backend 除去 → ClientState 除去 → AccountRemoved publish」の 4 段に構造化し、各段をコメントで明示する（翻訳可能性: シーケンスがそのまま手順書として読める）。
- 触れたコードに unwrap() を残さない。AccountId::from_u64 は Result を返し ? で伝播する（既存パターン踏襲）。
- ハードコード値を残さない。REGISTRATION_TIMEOUT 等は既存の named constant パターンを踏襲する。

## Test Plan

### Unit Tests

- UT: [Contract C095] §62 親セクションが存在する（N0068: §62 実装整合設計 — RESIDUE 解消の 9 設計判断の親セクション）前提のもと、62.12 が登録・アカウント経路を定義する。登録経路は §17 の状態遷移と矛盾しない — reactor は apply_registration_command_state（registr_wiring.rs:96）を経由して §17.1 コマンドエッジに沿って ClientState を進める（自動登録 / unregister 先行 / AccountRemoved publish）。
- UT: [Contract C096] N0025 が §17 登録状態機械を定義する前提のもと、62.12 が register_on_start 自動登録とイベント統一を規定する。実 REGISTER 成功時のみ Registered へ遷移する — process_registration_state_changed（registr_wiring.rs:53）だけが Registered を生成し、自動登録は Registering までしか進めない。
- UT: [Contract C097] N0073 が 62.4 登録状態機械の production 配線を定義する前提のもと、62.12 が add_account 時の自動登録と AccountRemoved を追補する。RegistrationSucceeded/Failed は残さない — SipEventPayload から両バリアントを完全削除し、登録結果は RegistrationStateChanged に統一する。
- UT: [Normal] reg_account_lifecycle::should_auto_register(&AccountConfig) — AccountConfig { register_on_start: true, .. } は true を返し、add_account 直後に backend.set_registration(account_id, true) による自動 REGISTER が発行される。
- UT: [Normal] reg_account_lifecycle::should_auto_register(&AccountConfig) — AccountConfig { register_on_start: false, .. } は false を返し、自動 REGISTER は発行されない。
- UT: [Normal] reg_account_lifecycle::build_account_snapshot(&AccountEntry) — AccountEntry { id: 1, config: { username: "alice", domain: "sip.example.com" }, registration: Registered } を AccountSnapshot { account_id: 1, uri: "sip:alice@sip.example.com", registered: true } に写像する。
- UT: [Normal] reactor.rs AddAccount アーム（reactor.rs:379）— submit_add_account(AccountConfig { register_on_start: true, .. }) 後、TestBackend に set_registration(native_id, true) の呼び出し記録があり、ClientState.accounts[aid].registration == Registering（apply_registration_command_state, §17.1）。
- UT: [Normal] reactor.rs RemoveAccount アーム（reactor.rs:648）— submit(RemoveAccount) 後、TestBackend の呼び出し順が [set_registration(native_id, false), remove_account(native_id)] であり、ClientState からエントリが除去され、EventBus に SipEventPayload::AccountRemoved(AccountSnapshot) が publish される。
- UT: [Normal] SipEventPayload::AccountRemoved(AccountSnapshot) が構築・照合できる（アカウント除去通知バリアントは enum に残る）。
- UT: [Normal] SipEventPayload::RegistrationStateChanged(RegistrationState::Registered) が統一的登録結果イベントとして構築・照合できる（P15-5 由来）。
- UT: [Error] reactor.rs AddAccount アーム — TestBackend::add_account が Err を返すと reply は Err、set_registration は呼ばれず、ClientState.accounts は不変（fail-fast、部分状態なし）。
- UT: [Error] reactor.rs RemoveAccount アーム — TestBackend::set_registration(native_id, false) が Err を返すと reply は Err、remove_account は呼ばれず、ClientState エントリは保持され、AccountRemoved は publish されない（unregister 先行の中断で状態は保全）。
- UT: [Error] reactor.rs RemoveAccount アーム — unregister 成功後 TestBackend::remove_account が Err を返すと reply は Err、ClientState エントリは保持され、AccountRemoved は publish されない。
- UT: [Error] client.rs remove_account（client.rs:269）— ClientState に存在しない account_id は Err(AccountNotFound) を返し dispatch しない（C052/C021 再確認）。
- UT: [Boundary] register_on_start は真偽 2 値のみ — true で自動 REGISTER 発行、false で非発行（第三の状態なし）。
- UT: [Boundary] build_account_snapshot — registered: true になるのは registration == Registered のときのみ。Idle/Registering/Unregistering/Failed/Expired/Disabled は全て registered: false。
- UT: [Boundary] remove_account が最後のアカウントを除去するとき、ClientState.accounts は空になり、AccountRemoved はちょうど 1 回 publish される。
- UT: [Boundary] remove_account(0) — AccountId::from_u64(0) は無効値のため backend 呼び出し前に Err(AccountNotFound)（id 境界）。
- UT: [Invariant] 自動登録（register_on_start）は ClientState を Registering までしか進めない。実 REGISTER 成功（Registering からの 200）のみ process_registration_state_changed 経由で Registered へ遷移する（C085 / C096 invariant）。
- UT: [Invariant] reg_account_lifecycle::classify_registration_outcome(&SipEventPayload) が Registration 系バリアント（RegistrationStarted / UnregistrationSucceeded / UnregistrationFailed / RegistrationExpired / RegistrationStateChanged）をワイルドカードなしで網羅照合する — コンパイルが通ることで RegistrationSucceeded / RegistrationFailed の存在が静的に否定される（C097 invariant）。
- UT: [Invariant] エラー原子性 — RemoveAccount シーケンスのどの時点でエラーになっても ClientState は一貫性を保つ。backend.remove_account が成功したときだけエントリが除去される（部分除去なし）。

### Integration Tests

- IT: [Integration point] client.add_account（client.rs:246）→ reactor AddAccount アーム（reactor.rs:379）→ backend.set_registration(native_id, true) → PJSIP ネイティブ REGISTER → NativeEvent::RegistrationStateChanged → registr_wiring::process_registration_state_changed（registr_wiring.rs:53）→ SipEventPayload::RegistrationStateChanged(Registered) を EventBus に publish → subscribe_account レシーバが受信。
- IT: [Integration point] client.remove_account（client.rs:269）→ reactor RemoveAccount アーム（reactor.rs:648）→ backend.set_registration(native_id, false) → backend.remove_account(native_id) → ClientState 除去 → SipEventPayload::AccountRemoved(AccountSnapshot) を EventBus に publish → レシーバが受信。
- IT: [Verification] Asterisk（docker）上で register_on_start: true のアカウントを add_account し、REGISTRATION_TIMEOUT（30s）以内に RegistrationStateChanged(Registered) を受信して自動 REGISTER 成功を確認する。
- IT: [Verification] 登録済みアカウントを remove_account し、AccountRemoved(AccountSnapshot) を受信するとともに Asterisk 側（CLI `sip show registrations`）で当該登録が消えたことを確認する。
- IT: [Prerequisites] Asterisk docker コンテナが起動していること（Makefile の integration ターゲット）、pjsua-native フィーチャーが有効なこと、examples/account_register.rs が RegistrationStateChanged 待ちに修正済みであること、ネットワーク到達性があること。
- IT: [Related tickets] P15-5（RegistrationStateChanged 型付きバリアント追加）、P16-2（transports §62.11）、P16-1（§62.10 round-2 scope/rootcause）、P0-5（登録イベントモデル）、P7-2（ClientState 権威化）、P10-3（submit_add_account 型付き応答）。

### Exceptions

- テスト不能（cannot test）: PjsuaBackend::set_registration / remove_account（backend.rs:678, 661）は pjsua-native フィーチャーと実 PJSIP スタック（生成済み transport）を要求する FFI ラッパーのため、単体テストでネイティブ呼び出しそのものを検証できない。しかしこれは設計欠陥（design defect）ではない — シーケンス順序と enabled フラグは TestBackend モックで決定的に単体テスト済み（UT: [Normal] reactor RemoveAccount / AddAccount）であり、FFI 境界のみが未検証のため、代替検証として Asterisk docker 統合テスト（IT: [Verification]）で実 REGISTER 成功を確認する。
- テスト不能（not testable）: 実ネットワーク上の REGISTER タイミング（リトランスミット、NAT keepalive）は Asterisk サーバとネットワーク状態に依存するため、壁時計の厳密なアサーションはフレークになる。これは設計欠陥（architectural defect）ではない — 統合テストは IT: [Verification] で REGISTRATION_TIMEOUT（30s）付きの outcome アサーション（結果検証）のみを行い、タイミングは検証しない。
- テスト不能（cannot test）: Rust は「enum にバリアントが存在しないこと」を実行時テストで直接表明できない。しかしこれは設計欠陥（not a defect）ではない — reg_account_lifecycle::classify_registration_outcome の網羅的 match がコンパイルを通ること（= バリアント除去の静的証明、UT: [Invariant] C097）と、examples/account_register.rs が統合 enum に対してのみコンパイルされることで代替検証される。

### Plan Test Code (concrete code)

- UT: [Contract C095-precondition→input] §62 親セクションが存在する前提で、62.12 の入力は `AccountConfig.register_on_start` + `RuntimeCommand::AddAccount`/`RemoveAccount`。テスト入力コード:
  ```rust
  let config = AccountConfig {
      username: "alice".into(),
      domain: "sip.example.com".into(),
      register_on_start: true,
      ..Default::default()
  };
  assert!(reg_account_lifecycle::should_auto_register(&config));
  ```
- UT: [Contract C095-postcondition→assertion] 62.12 が登録・アカウント経路を定義する — AddAccount 直後に自動 REGISTER が発行される:
  ```rust
  let (handle, join) = spawn_reactor();
  let id = handle.submit_add_account(config.clone()).await?;
  let calls = backend.set_registration_calls();
  assert!(calls.iter().any(|(native_id, enabled)| *native_id == id as i32 && *enabled));
  shutdown_reactor(handle, join).await;
  ```
- UT: [Contract C095-invariant→predicate] 登録経路は §17 の状態遷移と矛盾しない — 自動登録は ClientState を Registering までしか進めない（実 REGISTER 成功を待たず Registered にしない）:
  ```rust
  let state = handle.query_state().await?;
  let aid = AccountId::from_u64(id)?;
  assert_eq!(state.accounts[&aid].registration, RegistrationState::Registering);
  assert_ne!(state.accounts[&aid].registration, RegistrationState::Registered);
  ```
- UT: [Contract C096-precondition→input] N0025 が §17 登録状態機械を定義する — RegistrationState は 7 値:
  ```rust
  const ALL_STATES: [RegistrationState; 7] = [
      RegistrationState::Disabled, RegistrationState::Idle,
      RegistrationState::Registering, RegistrationState::Registered,
      RegistrationState::Unregistering, RegistrationState::Failed,
      RegistrationState::Expired,
  ];
  assert_eq!(ALL_STATES.len(), 7);
  ```
- UT: [Contract C096-postcondition→assertion] register_on_start 自動登録とイベント統一 — 登録結果通知は `RegistrationStateChanged` に統一される:
  ```rust
  let payload = SipEventPayload::RegistrationStateChanged(RegistrationState::Registered);
  assert!(matches!(payload, SipEventPayload::RegistrationStateChanged(RegistrationState::Registered)));
  ```
- UT: [Contract C096-invariant→predicate] 実 REGISTER 成功時のみ Registered — `process_registration_state_changed` が Registering + status 200 のときだけ Registered を生成する:
  ```rust
  let mut accounts = account_with_registration(1, RegistrationState::Registering);
  let ev = process_registration_state_changed(&backend, 1, &mut accounts)
      .expect("Registering + 200 must publish Registered");
  assert!(matches!(ev.payload, SipEventPayload::RegistrationStateChanged(RegistrationState::Registered)));
  assert_eq!(accounts[&AccountId::from_u64(1)?].registration, RegistrationState::Registered);
  ```
- UT: [Contract C097-precondition→input] N0073 が 62.4 production 配線を定義する — `AccountEntry` を `AccountSnapshot` へ写像する入力:
  ```rust
  let entry = AccountEntry {
      id: 1, native_id: 1, config: config.clone(),
      registration: RegistrationState::Registered,
  };
  let snapshot = reg_account_lifecycle::build_account_snapshot(&entry)
      .expect("valid entry maps to snapshot");
  assert_eq!(snapshot.account_id.get().get(), 1);
  assert!(snapshot.registered);
  ```
- UT: [Contract C097-postcondition→assertion] add_account 時の自動登録と AccountRemoved 追補 — RemoveAccount は unregister 先行 → backend 除去 → ClientState 除去 → AccountRemoved publish の順:
  ```rust
  let (handle, join) = spawn_reactor();
  let id = handle.submit_add_account(config.clone()).await?;
  handle.submit(RuntimeCommand::RemoveAccount { account_id: id, reply: Reply::new(tx) }).await?;
  let calls = backend.set_registration_calls();
  let removals = backend.remove_account_calls();
  let unreg_idx = calls.iter().position(|(_, enabled)| !*enabled).expect("unregister first");
  let rm_idx = removals.iter().position(|&n| n == id as i32).expect("backend removal second");
  assert!(unreg_idx < rm_idx, "set_registration(false) must precede remove_account");
  let snapshot = rx.await??; // AccountRemoved event received
  shutdown_reactor(handle, join).await;
  ```
- UT: [Contract C097-invariant→predicate] RegistrationSucceeded/Failed は残さない — 登録系バリアントを網羅照合する classifier がワイルドカードなしの match で全登録系バリアントを列挙し、コンパイルが通ること = 削除バリアントが enum に存在しない静的証明:
  ```rust
  fn classify_registration_outcome(payload: &SipEventPayload) -> &'static str {
      match payload {
          SipEventPayload::RegistrationStarted(_) => "started",
          SipEventPayload::UnregistrationSucceeded => "unregistered",
          SipEventPayload::UnregistrationFailed(_) => "unregister_failed",
          SipEventPayload::RegistrationExpired => "expired",
          SipEventPayload::RegistrationStateChanged(RegistrationState::Registered) => "registered",
          SipEventPayload::RegistrationStateChanged(_) => "unregistered_or_failed",
          _ => "non_registration",
      }
  }
  assert_eq!(classify_registration_outcome(&payload), "registered");
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| src/state/reg_account_lifecycle.rs was a stub: `pub enum State {}` + `// [::STUB::] P16-3` | Implemented §62.12 wiring: should_auto_register / add_account_and_apply_auto_register / build_account_snapshot / remove_account_sequence / classify_registration_outcome (test-only) | Resolved the P16-3 STUB (TS-001) with the §62.12 account lifecycle functions |
| reactor AddAccount arm only called backend.add_account + inserted into ClientState | AddAccount arm delegates to add_account_and_apply_auto_register which consumes register_on_start: issues set_registration(native_id, true) and advances ClientState to Registering via apply_registration_command_state | Added §62.12 auto-register on add_account when register_on_start=true |
| reactor RemoveAccount arm called backend.remove_account + removed from ClientState, no event publish | RemoveAccount arm delegates to remove_account_sequence: unregister-first (set_registration(false)) → backend.remove_account → ClientState removal, then publishes SipEventPayload::AccountRemoved(snapshot) on the single EventBus | Implemented unregister-first removal with AccountRemoved publish |
| SipEventPayload had RegistrationSucceeded(RegistrationInfo) and RegistrationFailed(RegistrationFailure) variants | Both variants removed; registration results unified to RegistrationStateChanged. classify_registration_outcome's exhaustive match compiling without them is the static proof of C097 | Removed RegistrationSucceeded/RegistrationFailed per §62.12 event unification |
| examples/account_register.rs awaited RegistrationSucceeded/RegistrationFailed | Now awaits RegistrationStateChanged(Registered) / RegistrationStateChanged(Failed) | Fixed the broken example to use the unified registration event |
| README.md residue claimed RegistrationSucceeded/Failed retained for API compat (specs/P15-5.md:92) | README documents RegistrationStateChanged unification and the unregister-first remove_account sequence | Cleared the README RESIDUE |
| TestBackend lacked call recording for registration/removal | Added set_registration_calls, remove_account_calls, set_registration_result, remove_account_result, add_account_result for deterministic ordering/error-injection tests | Extended TestBackend for P16-3 testability |
| client.rs private account_snapshot_from_entry duplicated the AccountEntry→AccountSnapshot mapping | Delegates to reg_account_lifecycle::build_account_snapshot (single source of truth shared with the reactor's AccountRemoved publish) | Consolidated the snapshot mapping |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) TDD Red: reg_account_lifecycle.rs に should_auto_register / build_account_snapshot / classify_registration_outcome のテストと、reactor AddAccount/RemoveAccount アームの挙動テスト、SipEventPayload から削除する 2 バリアントを参照する既存テストの削除を書く。(2) Red 確認: make test で失敗確認。(3) Green: reg_account_lifecycle.rs を実装し、reactor アームを修正し、enum から RegistrationSucceeded / RegistrationFailed を削除し、example / README を修正。(4) make check-be + make test で全 green 確認。(5) Refactor: 重複・非翻訳コード整理（boyScoutPlan 参照）。
- [Risks] SipEventPayload からのバリアント削除は破壊的変更（公開 API）。クレート内の全参照（example / README / テスト）を一括修正する必要がある。account_snapshot_from_entry を client.rs から reg_account_lifecycle.rs に移設する際、client.rs の accounts()（client.rs:225-231）が参照するため import 経路の整合が必要。
- [Caveats] N0081 の「クライアント起動時のアカウント復元でも同様に消費する」は §62.4 / 既存の UpdateAccount パス（reactor.rs:610）で実装済みのため、本チケットでは add_account 時の消費のみを対象とする。AccountRemoved publish に必要な AccountSnapshot は build_account_snapshot（account_snapshot_from_entry の移設）が ClientState エントリから生成する。
- [Open items] 自動登録の失敗通知: register_on_start 直後に set_registration(true) を発行しても、実 REGISTER 成功/失敗は NativeEvent::RegistrationStateChanged 経由で後続の RegistrationStateChanged(Registered / Failed) として通知される（本チケットのスコープで完結）。example の Failed 分岐は status_code / reason を失う（RegistrationState::Failed はデータを持たない）ため、メッセージのみの通知に変更する。
- [Future improvements] 登録状態の永続化（AccountEntry.registration の保存）、register_on_start の再試行ポリシー、AccountRemoved 前の graceful unregister 完了待ち（現状は fire-and-forget で unregister 発行後すぐ除去）。
Implementation summary (P16-3):
- Changed files: src/state/reg_account_lifecycle.rs (implement), src/runtime/reactor.rs (AddAccount/RemoveAccount arms + 2 new tests), src/api/event_model_payload_bus.rs (enum removal), examples/account_register.rs, README.md, src/runtime/backend.rs (TestBackend recording + add_account_result), src/client.rs (snapshot mapping consolidation), src/api/public_api_design.rs (register_on_start=false pins), src/runtime/command.rs (comment), src/state/mod.rs (module decl), src/tests/m20_test_dual_client.rs, tests/non_exhaustive.rs, tests/verify_spec_p10_1.rs
- TDD: failing suite + implementation written by the prior start session; this session verified red→green, extracted add_account_and_apply_auto_register to make the AddAccount error path testable, and added 6 new tests: 4 add_account_and_apply_auto_register unit tests (enabled/disabled/backend-failure/auto-register-failure) + 2 reactor tests (AccountRemoved exactly-once, remove_account(0) boundary rejected before backend)
- Verification: cargo check --all-targets --features test-util OK (no warnings); make test all green (1167 lib + integration + doc tests); validate-ticket-targets verifiedEmpty; verify-red-coverage 3/3 contracts (C095/C096/C097); annotate --check-ambiguous 0 remaining
- STUBs: TS-001 (reg_account_lifecycle.rs) resolved; 0 P16-3 stubs remain; 8 stubs correctly deferred to P16-4..P16-11
- Crimes: 0
- Quality check: 72 findings, all non-actionable — 71 unwrap/expect in #[cfg(test)] test modules (project-allowed per Rust rules + P0-5/P4-1/P7-2 precedent; the 2 unwraps in P16-3's own new file were converted to ? propagation), 1 commented-out-code false positive (pre-existing [::TICKET::] P12-1 provenance annotation at public_api_design.rs:615 required by the Comments-must-encode-provenance rule)
- Formatting note: running cargo fmt reformats the whole crate due to pre-existing fmt debt (274 untouched files, noted in P16-2). Spurious reformatting of non-P16-3 files was reverted to HEAD; the P16-3 in-scope files are now rustfmt-clean.
Review report (P16-3):
- Static quality check: 72 findings, all non-actionable (see instrumentation)
- Translatability: no issues
- Design verification (N0081): register_on_start auto-register consumed at add_account (add_account_and_apply_auto_register); RegistrationSucceeded/RegistrationFailed fully removed from SipEventPayload with no deprecated aliases (verified no production references remain); remove_account runs unregister-first → backend.remove_account → ClientState removal → AccountRemoved(AccountSnapshot) publish; §17 registration state machine remains authoritative (apply_registration_command_state + process_registration_state_changed guard transitions)
- Stub evaluation: 8 stubs found, all correctly deferred to P16-4..P16-11 (other phase-16 tickets, status todo); 0 P16-3 stubs remain; validate-no-external-excuses 8/8 pass
- Crimes: 0 records
- Incomplete-implementation scan: clean (no todo!/unimplemented!/FIXME/#[allow] without marker; no empty fn bodies; no unmarked stubs)
- Compilation: cargo check --all-targets --features test-util clean; make test all green
- Issues found and fixes applied during review: none required (implementation verified complete during /start-ticket; review confirmed all checks green)
- Notes: running cargo fmt reformats the whole crate due to pre-existing fmt debt (274 untouched files per P16-2); spurious reformatting of non-P16-3 files was reverted to HEAD during /start-ticket. The P16-3 in-scope files are rustfmt-clean.
Review report (P16-3):
- Static quality check: 72 findings, all non-actionable (see instrumentation)
- Translatability: no issues
- Design verification (N0081): register_on_start auto-register consumed at add_account (add_account_and_apply_auto_register); RegistrationSucceeded/RegistrationFailed fully removed from SipEventPayload with no deprecated aliases (verified no production references remain); remove_account runs unregister-first → backend.remove_account → ClientState removal → AccountRemoved(AccountSnapshot) publish; §17 registration state machine remains authoritative (apply_registration_command_state + process_registration_state_changed guard transitions)
- Stub evaluation: 8 stubs found, all correctly deferred to P16-4..P16-11 (other phase-16 tickets, status todo); 0 P16-3 stubs remain; validate-no-external-excuses 8/8 pass
- Crimes: 0 records
- Incomplete-implementation scan: clean (no todo!/unimplemented!/FIXME/#[allow] without marker; no empty fn bodies; no unmarked stubs)
- Compilation: cargo check --all-targets --features test-util clean; make test all green
- Issues found and fixes applied during review: none required (implementation verified complete during /start-ticket; review confirmed all checks green)
- Notes: running cargo fmt reformats the whole crate due to pre-existing fmt debt (274 untouched files per P16-2); spurious reformatting of non-P16-3 files was reverted to HEAD during /start-ticket. The P16-3 in-scope files are rustfmt-clean.

## P16-3 — implemented at 130 locations

### examples/account_register.rs

- Line 67
```rust
enum RegistrationOutcome {
```

### src/api/event_model_payload_bus.rs

- Line 295
```rust
    // ── Registration (P0) ──
```

- Line 542
```rust
    fn payload_registration_state_changed() {
```

### src/api/public_api_design.rs

- Line 220
```rust
    fn sip_account_handle_is_clone() {
```

- Line 222
```rust
        fn assert_clone<T: Clone>() {}
```

- Line 231
```rust
        fn assert_debug<T: std::fmt::Debug>() {}
```

- Line 238
```rust
    fn sip_account_handle_is_send() {
```

- Line 240
```rust
        fn assert_send<T: Send>() {}
```

- Line 344
```rust
        fn assert_sync<T: Sync>() {}
```

- Line 359
```rust
        fn assert_result_type(_: &Result<RegistrationState, SipError>) {}
```

### src/client.rs

- Line 86
```rust
impl SipClient {
```

- Line 583
```rust
fn map_call_state_query_error(e: ReactorError) -> SipError {
```

- Line 603
```rust
fn account_snapshot_from_entry(
```

- Line 803
```rust
    fn sip_client_is_send_and_sync() {
```

- Line 808
```rust
        fn assert_send<T: Send>() {}
```

- Line 810
```rust
        fn assert_sync<T: Sync>() {}
```

- Line 1028
```rust
        fn assert_add_transport_result(_: &Result<(), SipError>) {}
```

- Line 1032
```rust
        fn assert_calls_result(_: &Result<Vec<crate::runtime::state::CallEntry>, SipError>) {}
```

- Line 1034
```rust
        fn assert_call_state_result(
```

- Line 1044
```rust
        fn assert_shutdown_result(_: &Result<(), SipError>) {}
```

- Line 1214
```rust
    fn microphone_is_optional_feature() -> Result<(), std::io::Error> {
```

- Line 1388
```rust
    fn all_public_client_methods_are_instrumented() -> Result<(), std::io::Error> {
```

### src/runtime/backend.rs

- Line 137
```rust
    fn conf_connect(&mut self, source: i32, sink: i32) -> Result<(), ReactorError>;
```

- Line 141
```rust
    fn conf_disconnect(&mut self, source: i32, sink: i32) -> Result<(), ReactorError>;
```

- Line 150
```rust
    fn push_media_frame(
```

- Line 238
```rust
impl TestBackend {
```

- Line 291
```rust
    fn add_account(
```

- Line 320
```rust
    fn remove_account(&mut self, native_acc_id: i32) -> Result<(), ReactorError> {
```

- Line 343
```rust
    fn set_registration(&mut self, native_acc_id: i32, enabled: bool) -> Result<(), ReactorError> {
```

- Line 396
```rust
    fn answer_call(&mut self, native_call_id: i32, code: u16) -> Result<(), ReactorError> {
```

- Line 405
```rust
    fn hangup(&mut self, native_call_id: i32) -> Result<(), ReactorError> {
```

- Line 429
```rust
    fn transfer_call(&mut self, native_call_id: i32, target: &str) -> Result<(), ReactorError> {
```

- Line 459
```rust
    fn get_account_info(&self, native_acc_id: u32) -> Result<AccountInfoSnapshot, ReactorError> {
```

- Line 494
```rust
    fn push_media_frame(
```

- Line 619
```rust
    fn default() -> Self {
```

- Line 629
```rust
    fn initialize(&mut self, _config: &crate::config::ClientConfig) -> Result<(), ReactorError> {
```

- Line 653
```rust
    fn create_transport(
```

- Line 674
```rust
    fn add_account(
```

- Line 914
```rust
    fn shutdown(&mut self) -> Result<(), ReactorError> {
```

- Line 1010
```rust
    fn push_media_frame(
```

- Line 1039
```rust
    fn sip_backend_trait_object_is_object_safe() {
```

- Line 1050
```rust
    fn test_backend_add_account_assigns_ids_from_one() {
```

- Line 1062
```rust
    fn test_backend_add_account_starts_disabled() {
```

- Line 1083
```rust
    fn test_backend_set_registration_transitions_registration_map() {
```

- Line 1110
```rust
    fn test_backend_mark_registered_yields_200_snapshot() {
```

- Line 1129
```rust
    fn test_backend_get_account_info_unknown_id_returns_error() {
```

- Line 1141
```rust
    fn test_backend_update_account_unknown_id_returns_error() {
```

- Line 1166
```rust
    fn map_pjsua_status_error_preserves_diagnostic() {
```

- Line 1232
```rust
    fn mock_backend_initialize_sets_flag() {
```

- Line 1243
```rust
    fn mock_backend_shutdown_clears_flag() {
```

- Line 1257
```rust
    fn mock_backend_conf_connect_disconnect_returns_ok() {
```

- Line 1268
```rust
    fn mock_backend_conf_connect_records_invocation() {
```

- Line 1283
```rust
    fn mock_backend_conf_disconnect_records_invocation() {
```

- Line 1296
```rust
    fn test_backend_push_media_frame_records_invocation() {
```

- Line 1312
```rust
    fn pjsua_push_media_frame_unsubscribed_call_is_noop() {
```

- Line 1355
```rust
    fn mock_backend_hold_unhold_records_invocation() {
```

- Line 1403
```rust
    fn mock_backend_resolve_conf_port_returns_ok() {
```

- Line 1413
```rust
    fn mock_backend_add_account_returns_entry() {
```

- Line 1428
```rust
    fn mock_backend_configure_codecs_returns_ok() {
```

- Line 1440
```rust
    fn test_backend_get_account_info_derives_registered_snapshot(
```

- Line 1468
```rust
    fn mock_backend_get_account_info_result_configurable() {
```

- Line 1484
```rust
    fn mock_backend_transfer_call_returns_ok() {
```

- Line 1546
```rust
    fn test_backend_is_send() {
```

- Line 1548
```rust
        fn assert_send<T: Send>() {}
```

- Line 1567
```rust
    fn mock_backend_get_account_info_derives_idle_snapshot(
```

- Line 1592
```rust
    fn mock_backend_get_account_info_unknown_native_id_returns_err() {
```

- Line 1606
```rust
    fn mock_backend_add_account_assigns_incrementing_ids() -> Result<(), Box<dyn std::error::Error>>
```

- Line 1623
```rust
    fn mock_backend_remove_account_removes_registry_entry() -> Result<(), Box<dyn std::error::Error>>
```

- Line 1641
```rust
    fn mock_backend_add_account_stores_full_config() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1658
```rust
    fn mock_backend_update_account_updates_stored_config() -> Result<(), Box<dyn std::error::Error>>
```

- Line 1674
```rust
    fn mock_backend_update_account_unknown_id_returns_err() {
```

- Line 1711
```rust
    fn mock_make_call_increments_call_ids() {
```

- Line 1731
```rust
    fn mock_make_call_result_injection_ok() {
```

- Line 1749
```rust
    fn mock_make_call_result_injection_err() {
```

- Line 1762
```rust
    fn mock_make_call_zero_native_id_returns_err() {
```

- Line 1778
```rust
    fn test_backend_records_answer_calls() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1794
```rust
    fn test_backend_records_hangup_calls() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1810
```rust
    fn test_backend_records_transfer_calls() -> Result<(), Box<dyn std::error::Error>> {
```

### src/runtime/command.rs

- Line 21
```rust
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
```

- Line 193
```rust
        native_acc_id: u32,
```

- Line 255
```rust
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
```

- Line 453
```rust
impl DispatchCommand {
```

- Line 606
```rust
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
```

- Line 819
```rust
    fn add_audio_source_variant_constructs() {
```

- Line 938
```rust
    fn from_runtime_command_converts_answer_to_dedicated_variant() {
```

- Line 965
```rust
    fn from_runtime_command_converts_hangup_to_dedicated_variant_with_reason() {
```

- Line 995
```rust
    fn from_runtime_command_converts_transfer_to_dedicated_variant() {
```

- Line 1021
```rust
    fn runtime_command_display_shows_new_call_api_variants() {
```

- Line 1050
```rust
    fn from_runtime_command_converts_audio_source_variants() {
```

- Line 1120
```rust
    fn conf_connect_closure_invokes_backend_conf_connect() {
```

- Line 1146
```rust
    fn conf_disconnect_closure_invokes_backend_conf_disconnect() {
```

- Line 1173
```rust
    fn hold_closure_invokes_backend_hold() {
```

- Line 1198
```rust
    fn unhold_closure_invokes_backend_unhold() {
```

- Line 1225
```rust
    fn from_runtime_command_converts_update_account() {
```

- Line 1284
```rust
    fn runtime_command_display_shows_new_lifecycle_variants() {
```

- Line 1306
```rust
    fn runtime_command_debug_exposes_payload_field_names() {
```

- Line 1390
```rust
    fn from_runtime_command_preserves_reply_and_debugbox() {
```

### src/runtime/reactor.rs

- Line 75
```rust
    fn default() -> Self {
```

- Line 110
```rust
impl CoreReactor {
```

- Line 1357
```rust
    fn dispatch_event_publishes_to_single_bus() {
```

- Line 1373
```rust
    fn dispatch_event_lifecycle_event_flows_to_single_bus() {
```

- Line 1865
```rust
    fn handle_make_call_records_outgoing_direction() {
```

- Line 2336
```rust
    fn handle_make_call_registers_entry_and_returns_id() {
```

- Line 2360
```rust
    fn handle_make_call_error_registers_nothing() {
```

- Line 2384
```rust
    fn handle_make_call_native_error_preserves_status() {
```

- Line 2464
```rust
    async fn reactor_add_account_with_register_on_start_issues_auto_register(
```

- Line 2484
```rust
    async fn reactor_add_account_without_register_on_start_skips_auto_register(
```

- Line 2504
```rust
    async fn reactor_remove_account_publishes_account_removed(
```

- Line 2545
```rust
    async fn reactor_remove_account_publishes_account_removed_exactly_once(
```

- Line 2584
```rust
    async fn reactor_remove_account_zero_id_replies_err_without_backend_or_publish(
```

- Line 2751
```rust
    fn account_with_registration(
```

### src/state/mod.rs

- Line 13
```rust
pub mod registr_state_machine;
```

### src/state/reg_account_lifecycle.rs

- Line 148
```rust
    fn test_entry(id: u64, native_id: i32, registration: RegistrationState) -> AccountEntry {
```

- Line 164
```rust
    fn should_auto_register_consumes_register_on_start() {
```

- Line 174
```rust
    fn build_account_snapshot_maps_entry_to_snapshot() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 186
```rust
    fn build_account_snapshot_registered_only_for_registered_state(
```

- Line 205
```rust
    fn build_account_snapshot_rejects_zero_id() {
```

- Line 213
```rust
    fn remove_account_sequence_unregisters_first_then_removes_and_maps_snapshot(
```

- Line 239
```rust
    fn remove_account_sequence_missing_account_rejects_without_backend_calls(
```

- Line 254
```rust
    fn remove_account_sequence_unregister_failure_aborts_before_removal(
```

- Line 280
```rust
    fn remove_account_sequence_remove_failure_retains_account_after_unregister(
```

- Line 307
```rust
    fn add_account_and_apply_auto_register_issues_set_registration_when_enabled(
```

- Line 334
```rust
    fn add_account_and_apply_auto_register_skips_when_disabled(
```

- Line 357
```rust
    fn add_account_and_apply_auto_register_backend_failure_leaves_state_unchanged(
```

- Line 379
```rust
    fn add_account_and_apply_auto_register_auto_register_failure_retains_account(
```

- Line 400
```rust
    fn classify_registration_outcome_names_unified_registration_variants(
```

### src/tests/m20_test_dual_client.rs

- Line 45
```rust
impl M20FeatureTestEntry {
```

- Line 147
```rust
    fn test_dual_client_presupposes_backend() {
```

### tests/non_exhaustive.rs

- Line 25
```rust
fn sip_event_payload_matches_with_wildcard_arm() {
```

### tests/verify_spec_p10_1.rs

- Line 35
```rust
async fn client_with_account(
```
