# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P16-8: STUN/TURN/ICE wiring & coturn verification (§62.17)

**Ticket Key**: P16-8 · **Phase**: 16

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
`ClientConfig.stun_servers` / `turn_servers` / `ice`（§13）を PJSIP の pjsua_config / pjsua_media_config へ反映する配線（apply_stun_turn / apply_ice）を実装し、NAT 越えメディア転送（STUN / TURN / ICE）を実際に機能させる（N0086 / §62.17）。

### Purpose
RFC §13 は STUN / TURN / ICE の完全仕様を定義するが、PJSIP への実配線は未実装（`backend_calls::initialize` は media_cfg に null を渡し、stun_srv / turn_cfg を一切反映しない）。本チケットは ClientConfig の設定値を PJSIP の実構造体（stun_srv[8] 配列 / pjsua_turn_config / pjsua_media_config）へ変換・反映し、2 者間の NAT 越えメディア経路を確立するための基盤を提供する。

### Motivation
§62.17 は RESIDUE 解消として「STUN/TURN/ICE 配線」を定義する。Q7（配線実装）と Q7a（coturn 検証）が未解決で、RFC-ROOT の I/O 境界表（4542 行）に「backend_calls.rs / coturn 統合テスト」が必須項目として明記されている。加えて、P2P 完全分散型ボイス通信という zasso の使命上、NAT 越えは必須であり、STUN/TURN/ICE の実配線なしでは実ネットワークでの通話が成立しない。

### Constraints
- 実 PJSIP API に従う: RFC 擬似コード（stun_srv 単一 / turn_cfg_srv / ice_aggressive）は実 pjsua.h と乖離しており、stun_srv[8] 配列 + pjsua_turn_config + ice_opt.aggressive へ実装を合わせる（RFC 乖離として記録）。
- デフォルトビルドでテスト可能に分離する: 純粋マッピング（apply_stun_turn / apply_ice）をデフォルトビルドでユニットテストし、FFI 呼び出し（pjsua_init）は pjsua-native に委譲する（transport_wiring.rs パターン）。
- coturn プロトコルレベル検証（STUN binding / TURN allocate / relay 転送）は Docker 依存のため §62.19（P16-10）に委譲し、本チケットは構造検証 + デフォルトビルド統合テストを実施する。
- 新規依存クレートを追加しない。bindgen allowlist / stub aliases の追加のみで実装する。TURN パスワードは SecretString::expose_secret() 経由でのみ PJSIP へ渡し、機密を漏らさない。
- 本環境の pjsua-native ビルドは pre-broken（P16-7 記録、40+ bindgen エラー）のため、pjsua-native コンパイル検証は P16-10 に委譲する。

## Scope

- **Scope of changes:** src/config/stun_turn_ice_wiring.rs（add）/ src/ffi/backend_calls.rs（modify: initialize シグネチャ + STUN/TURN/ICE 配線）/ src/ffi/bindings.rs（modify: stub aliases 追加）/ src/build/build_script_bindgen.rs（modify: allowlist 追加）/ src/config.rs（modify: モジュール登録）
**Scope of changes (describe each change comprehensively):**
  - [File/module path] `src/config/stun_turn_ice_wiring.rs`（N0086 実装ファイル）
  - [Action: add/modify/remove/rename/refactor] add — `[::STUB::] P16-8` を実装へ置換（Initial Design Artifact ヘッダは維持）。`pub struct Config {}`（未使用プレースホルダ）を削除
  - [What specifically changes] `apply_stun_turn(cfg: &mut bindings::pjsua_config, config: &ClientConfig) -> Result<(), SipError>`（STUN: stun_srv_cnt / stun_srv[0..8] へ URI 反映、TURN: turn_cfg_use=PJSUA_TURN_CONFIG_USE_CUSTOM / turn_cfg.enable_turn / turn_server / turn_conn_type / turn_auth_cred.static_cred へ反映）、`apply_ice(cfg: &mut bindings::pjsua_media_config, ice: &IceConfig)`（enable_ice / ice_max_host_cands / ice_opt.aggressive へ反映）、および純粋ヘルパー（`resolve_stun_server_uris` / `resolve_turn_server` / `resolve_turn_conn_type` / `resolve_turn_auth_cred` / `resolve_ice_flags`）
  - [Before → After (behavior/signature)] Before: ヘッダー + STUB マーカーのみでコンパイル対象外・テスト不能。After: STUN/TURN/ICE 配線がデフォルトビルドでユニットテスト可能になり、pjsua-native 下の initialize から参照可能
  - [API contract change (if any)] 公開 API 追加: `apply_stun_turn` / `apply_ice` / `resolve_*`（`pub(crate)`）。破壊的変更なし。`backend_calls::initialize` のシグネチャ変更（後述）
  - [Data schema change (if any)] なし
  - [Config/env change (if any)] なし
  - [Dependency added/removed (if any)] 新規依存なし
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] `src/ffi/backend_calls.rs`（modify）+ `src/runtime/backend.rs`（modify）
  - [Action: add/modify/remove/rename/refactor] modify — `initialize()` を `initialize(config: &ClientConfig)` へ変更し、実 pjsua_config（callback 登録 + STUN/TURN）と実 pjsua_media_config（ICE）を構築して `pjsua_init(&mut cfg, log_cfg, &mut media_cfg)` を呼ぶ
  - [What specifically changes] `backend_calls::initialize` が `stun_turn_ice_wiring::apply_stun_turn` / `apply_ice` を呼び、media_cfg に null を渡す代わりに ICE 反映済みの `pjsua_media_config` を渡す。`PjsuaBackend::initialize` が config を `backend_calls::initialize(config)` へ渡す（未使用引数 `let config = _config;` を解消）
  - [Before → After (behavior/signature)] Before: `initialize()` は引数なし・media_cfg=null・STUN/TURN/ICE 未反映。After: `initialize(config)` が STUN/TURN/ICE を反映した config で PJSUA スタックを初期化
  - [API contract change (if any)] `backend_calls::initialize` のシグネチャ変更（引数追加）。`SipBackend::initialize` trait は `&ClientConfig` を既に受理しており変更不要
  - [Data schema change (if any)] なし
  - [Config/env change (if any)] なし
  - [Dependency added/removed (if any)] なし
**Scope of changes (describe each change comprehensively):**
  - [File/module path] `src/ffi/bindings.rs`（modify）+ `src/build/build_script_bindgen.rs`（modify）+ `src/config.rs`（modify）
  - [Action: add/modify/remove/rename/refactor] modify — stub aliases に STUN/TURN/ICE 型と定数を追加、bindgen allowlist に実 PJSIP シンボルを追加、config モジュールに `pub mod stun_turn_ice_wiring;` を宣言
  - [What specifically changes] stub_aliases: `pjsua_config` に `stun_srv_cnt: u32` / `stun_srv: [pj_str_t; 8]` / `turn_cfg_use: i32` / `turn_cfg: pjsua_turn_config` を追加。`pjsua_turn_config { enable_turn: i32, turn_server: pj_str_t, turn_conn_type: i32, turn_auth_cred: pj_stun_auth_cred, turn_tls_setting }`、`pjsua_media_config { enable_ice: i32, ice_max_host_cands: i32, ice_opt: pj_ice_sess_options }`、`pj_ice_sess_options { aggressive: i32 }`、`pj_stun_auth_cred`（type + static_cred）を追加。定数 `PJ_TURN_TP_UDP=17` / `PJ_TURN_TP_TCP=6` / `PJ_TURN_TP_TLS=56` / `PJSUA_TURN_CONFIG_USE_DEFAULT=0` / `PJSUA_TURN_CONFIG_USE_CUSTOM=1` / `PJ_STUN_AUTH_CRED_STATIC` / `PJ_STUN_PASSWD_PLAIN=0` を追加。BINDGEN_ALLOWLIST_TYPES に `pjsua_media_config` / `pjsua_turn_config` / `pjsua_turn_config_use` / `pj_ice_sess_options` / `pj_stun_auth_cred` / `pj_turn_tp_type` / `pj_stun_auth_cred_type` / `pj_stun_passwd_type`、BINDGEN_ALLOWLIST_VARS に `PJ_TURN_TP_UDP/TCP/TLS` / `PJSUA_TURN_CONFIG_USE_DEFAULT/CUSTOM` / `PJ_STUN_AUTH_CRED_STATIC` / `PJ_STUN_PASSWD_PLAIN` を追加
  - [Before → After (behavior/signature)] Before: スタブに STUN/TURN/ICE 型なし・allowlist 未登録・モジュール未宣言。After: デフォルトビルドで配線がコンパイル・テスト可能、pjsua-native で実シンボルが生成される
  - [API contract change (if any)] なし（bindings は内部 FFI 型。stun_turn_ice_wiring モジュールは pub(crate)）
  - [Data schema change (if any)] なし
  - [Config/env change (if any)] なし
  - [Dependency added/removed (if any)] なし
- **Out of scope (items intentionally excluded, with justification):**
  - [Excluded item] coturn 実サーバによるプロトコルレベル統合テスト（STUN binding / TURN allocate / relay candidate 経由のメディア転送）
  - [Why excluded — separate ticket / future phase / not applicable] P16-10（N0088 / §62.19 Asterisk/coturn docker IT 基盤）で対応予定。本チケットの IT はデフォルトビルドで apply_stun_turn / apply_ice の構造体反映を検証する
  - [Excluded item] pjsua-native ビルドの実 FFI シンボル動作検証（pjsua_media_config / pjsua_turn_config / PJ_TURN_TP_*）
  - [Why excluded — separate ticket / future phase / not applicable] 本環境の pjsua-native ビルドは pre-broken（P16-7 記録、40+ bindgen エラー）。CI / P16-10 docker 基盤で検証する
**Affected areas (components/systems impacted, even without direct modification):**
  - [Affected component] `SipBackend` / `PjsuaBackend`（src/runtime/backend.rs）
  - [Nature of impact: performance / security / API surface / data format / …] API サーフェス — `backend_calls::initialize` のシグネチャ変更（&ClientConfig 追加）。trait の `SipBackend::initialize(&ClientConfig)` は既存のまま
  - [Corresponding change needed Y/N + details] Y — `PjsuaBackend::initialize` が config を `backend_calls::initialize(config)` へ渡す
  - [Affected component] `ClientConfig`（src/config/client_config_spec.rs）
  - [Nature of impact: performance / security / API surface / data format / …] データフロー — stun_servers / turn_servers / ice フィールドが初めて消費される（配線で使用）。既存フィールド変更なし
  - [Corresponding change needed Y/N + details] N — 既存 API で対応可能
  - [Affected component] `config.validate()`（§42 fail-fast 検証）
  - [Nature of impact: performance / security / API surface / data format / …] 検証 — stun_srv[8] 上限超過の fail-fast を配線側でも防御的に行う。§42 検証との二重化
  - [Corresponding change needed Y/N + details] N — 配線の `apply_stun_turn` が SipError を返すことで防御

## STUBs — Must Be Fully Implemented in This Ticket (1 items — resolved: 1)

### TS-001 — `src/config/stun_turn_ice_wiring.rs — resolved

- **Marker**: `// [::TICKET::] P16-8: STUN/TURN/ICE wiring — §62.17 / N0086.`
- **Contracts**: C111, C112, C113

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0086`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0086` | config | rust | 62.17 STUN/TURN/ICE 配線と coturn 検証 |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| refines | ★ 62.17 STUN/TURN/ICE 配線と coturn 検証 (N0086) | → | ☆ 62.1 公開設定 API の一本化（ClientConfig / STUN/TURN/ICE） (N0070) |
| extends | ★ 62.17 STUN/TURN/ICE 配線と coturn 検証 (N0086) | → | ☆ §12 TransportConfig & §13 ICE/STUN/TURN Spec (N0015) |
| part_of | ★ 62.17 STUN/TURN/ICE 配線と coturn 検証 (N0086) | → | ☆ §62 実装整合設計 — RESIDUE 解消のための設計判断確定 (N0068) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0086` | `src/config/stun_turn_ice_wiring.rs` |

## Investigation

- src/config/stun_turn_ice_wiring.rs（N0086 実装ファイル）は `[::STUB::] P16-8` + `pub struct Config {}` のみで、ヘッダー以外の実装は無い。src/config.rs（config モジュールルート、src/config.rs が mod.rs 相当）には `pub mod stun_turn_ice_wiring;` の宣言が無い（モジュール未登録）。
- transport_ice_spec.rs（N0015 / §12-§13、384 行）に統合型が定義済み: `StunServerConfig { uri: String }`、`TurnServerConfig { uri: String, username: Option<String>, password: Option<SecretString>, transport: TurnTransport }`、`TurnTransport` enum（Udp / Tcp / Tls）、`IceConfig { enabled, aggressive_nomination, trickle_ice, renomination, max_host_candidates: usize }`。IceConfig::default は §13 既定値（enabled=true / aggressive_nomination=true / trickle_ice=false / renomination=false / max_host_candidates=16）。
- client_config_spec.rs（N0013 / §10）の `ClientConfig` は `stun_servers: Vec<StunServerConfig>` / `turn_servers: Vec<TurnServerConfig>` / `ice: IceConfig` を保持（N0070 一元化済み、二重定義は解消）。
- src/ffi/backend_calls.rs の `initialize()`（P11-10）は引数なしで、`pjsua_config` を zeroed → callback 登録 → `pjsua_init(&mut config, null_mut(), null_mut())` を呼ぶ。media_cfg は null のままで ICE 設定を反映できていない。STUN/TURN/ICE を反映するにはシグネチャ変更（&ClientConfig 受理）と実 pjsua_media_config の構築が必要。
- src/runtime/backend.rs の `PjsuaBackend::initialize(&mut self, _config: &ClientConfig)`（674 行）は `backend_calls::initialize()` を引数なしで呼んでおり、config を渡せていない（`let config = _config;` は存在するが未使用）。
- src/ffi/bindings.rs の stub_aliases は `pjsua_config { cb: pjsua_callback }` のみ（378 行）。`pjsua_media_config` / `pjsua_turn_config` / `pj_ice_sess_options` / `PJ_TURN_TP_*` / `PJSUA_TURN_CONFIG_USE_*` は未定義。デフォルトビルドで配線をテストするには stub 追加が必要。
- build_script_bindgen.rs の allowlist: BINDGEN_ALLOWLIST_TYPES に `pjsua_config` はあるが `pjsua_media_config` / `pjsua_turn_config` / `pjsua_ice_config` / `pjsua_turn_config_use` / `pj_ice_sess_options` / `pj_stun_auth_cred` / `pj_turn_tp_type` / `pj_stun_auth_cred_type` / `pj_stun_passwd_type` が無い。BINDGEN_ALLOWLIST_VARS に `PJ_TURN_TP_UDP/TCP/TLS` / `PJSUA_TURN_CONFIG_USE_DEFAULT/CUSTOM` / `PJ_STUN_AUTH_CRED_STATIC` / `PJ_STUN_PASSWD_PLAIN` が無い。
- 実 PJSIP API（vendor/pjsip/pjsip/include/pjsua-lib/pjsua.h）のフィールド構成: STUN は `stun_srv_cnt: unsigned` + `stun_srv: [pj_str_t; 8]`（単一 pj_str_t ではない）。TURN は `turn_cfg_use: pjsua_turn_config_use`（DEFAULT=0 / CUSTOM=1）+ `turn_cfg: pjsua_turn_config`（`enable_turn: pj_bool_t` / `turn_server: pj_str_t` / `turn_conn_type: pj_turn_tp_type` / `turn_auth_cred: pj_stun_auth_cred` / `turn_tls_setting`）。ICE は pjsua_media_config の `enable_ice: pj_bool_t` / `ice_max_host_cands: int` / `ice_opt: pj_ice_sess_options`（`aggressive: pj_bool_t`）。
- 定数値: `PJ_TURN_TP_UDP=17` / `PJ_TURN_TP_TCP=6` / `PJ_TURN_TP_TLS=56`（pj_turn_tp_type、IANA プロトコル番号）。`PJ_STUN_PASSWD_PLAIN=0`。`PJ_STUN_AUTH_CRED_STATIC`（pj_stun_auth_cred.type）。`pj_stun_auth_cred` は union で `static_cred { realm, username, data_type, data, nonce }` を持つ。
- RFC 乖離（重要）: §62.17 の擬似コードは `cfg.stun_srv = cstring(...)`（単一文字列）/ `cfg.turn_cfg_srv` / `cfg.turn_cfg_user` / `cfg.turn_cfg_pass` / `cfg.ice_aggressive` を想定するが、実 pjsua.h は stun_srv[8] 配列 + pjsua_turn_config 構造体 + ice_opt.aggressive であり、実 API に合わせた配線設計が必要。
- 実装パターン: transport_wiring.rs（P16-2）が「純粋マッピング（apply_transport_port 等、デフォルトビルドでテスト可能）+ pjsua-native 下の FFI 委譲（backend_calls）」。STUN/TURN/ICE も同様に純粋マッピングをデフォルトビルドでテスト可能に分離する。
- P16-7 の注記: 本環境の pjsua-native ビルドは 40+ 件の無関係 bindgen エラーで pre-broken であり、コンパイル検証は P16-10（docker 基盤）に委譲済み。
- SipError（error_design_siperror.rs:191）は `kind / message / native_status / account_id / call_id / retryable` の 6 フィールド。`SipErrorKind::InvalidConfig` が存在（stun_srv[8] 超過の fail-fast に使用可能）。SecretString::expose_secret()（security_platform_diffs.rs:83）で TURN パスワードを PJSIP へ受け渡し可能。
- Makefile: `make test` = `cargo test --features test-util`。テストは tests/ ディレクトリ（verify_spec_* 等）。

## Acceptance Criteria

- **[Happy path]** `ClientConfig` に STUN サーバ 1 個 + TURN サーバ 1 個（UDP, username/password 付き）+ 既定 IceConfig を設定して `SipClient::new` → initialize すると、pjsua_config.stun_srv_cnt=1 / stun_srv[0]=STUN URI、turn_cfg_use=PJSUA_TURN_CONFIG_USE_CUSTOM / turn_cfg.enable_turn=1 / turn_server=TURN URI / turn_conn_type=PJ_TURN_TP_UDP / turn_auth_cred.static_cred に username/password、pjsua_media_config.enable_ice=1 / ice_max_host_cands=16 / ice_opt.aggressive=1 が反映される（ユニットテスト + TestBackend 統合で検証）。
- **[Error case]** `stun_servers` が 9 個以上（stun_srv[8] 上限超過）の場合、`apply_stun_turn` は `SipError`（InvalidConfig）を返し、超過分を静かに切り捨てない（fail-fast）。エラーは ReactorError 経由で呼び出し元へ伝播する。TURN パスワードは SecretString::expose_secret() 経由でのみ PJSIP へ渡り、ログ / Display / Debug に漏れない。
- **[Edge case]** `stun_servers` / `turn_servers` が空（未設定）の場合、stun_srv_cnt=0 / turn_cfg_use=PJSUA_TURN_CONFIG_USE_DEFAULT のまま変更されない（no-op）。STUN サーバがちょうど 8 個の場合は全反映されエラーにならない。TurnTransport::Tls は PJ_TURN_TP_TLS=56 へマッピングされる。IceConfig { enabled: false, max_host_candidates: 0 } は enable_ice=0 / ice_max_host_cands=0 へ反映される。

## Invariants

- 【正常成立条件】STUN/TURN/ICE 配線は §13 と矛盾しない: `ClientConfig.stun_servers` / `turn_servers` / `ice`（§13 の型と既定値）が pjsua_config の `stun_srv_cnt` / `stun_srv[0..8]`、`turn_cfg_use` / `turn_cfg`（pjsua_turn_config）、pjsua_media_config の `enable_ice` / `ice_max_host_cands` / `ice_opt.aggressive` へ一貫して反映される。§13 既定値（enabled=true / aggressive_nomination=true / max_host_candidates=16）は変更されない。
- 【エラー時不変条件】STUN サーバが stun_srv[8] 上限を超える場合は SipError（InvalidConfig）を返し、部分反映（超過分の切り捨て）をしない。エラーは伝播し、pjsua_config / pjsua_media_config への反映は全 or 無（atomic）で行う。TURN パスワードは SecretString::expose_secret() 経由のみで PJSIP へ渡り、ログ / Display / Debug に漏れない（マスクは [REDACTED]）。
- 【内部状態不変条件】STUN/TURN/ICE の field 反映は src/config/stun_turn_ice_wiring.rs の単一モジュールに閉じる — backend_calls.rs / runtime/backend.rs に stun_srv / turn_cfg / enable_ice の直書きマッピングを新設しない（単一責任）。`apply_stun_turn` / `apply_ice` は transport_ice_spec の統合型（N0070 一元化）のみを受理し、旧 config.rs 二重定義に依存しない。
- 【境界値不変条件】stun_srv[8] 配列上限: STUN サーバちょうど 8 個は全反映（エラーなし）、9 個以上は SipError。STUN / TURN 未設定（空 Vec）は no-op（stun_srv_cnt=0 / turn_cfg_use=PJSUA_TURN_CONFIG_USE_DEFAULT）。TurnTransport は網羅マッチで PJ_TURN_TP_*（Udp=17 / Tcp=6 / Tls=56）へ変換され、未知値は存在しない。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C111 — N0086→N0068

- **Precondition**: §62 親セクションが存在する（N0068 実装整合設計）配下に 62.17（N0086）が解決され、STUN/TURN/ICE 配線の決定事項（ClientConfig.stun_servers → pjsua_config.stun_srv、turn_servers → pjsua_config.turn_cfg、ice → pjsua_media_config の ICE 設定）の依存が確定している。実装対象ファイル src/config/stun_turn_ice_wiring.rs は [::STUB::] P16-8 状態で、src/config.rs（config モジュールルート）にはモジュール宣言が未追加。
- **Postcondition**: 62.17 が STUN/TURN/ICE 配線を定義する — apply_stun_turn / apply_ice がデフォルトビルドでテスト可能な形で実装され、(a) STUN サーバが pjsua_config.stun_srv_cnt / stun_srv[0..8] へ反映、(b) TURN サーバが pjsua_config.turn_cfg_use / turn_cfg 構造体（enable_turn / turn_server / turn_conn_type / turn_auth_cred）へ反映、(c) ICE 設定が pjsua_media_config（enable_ice / ice_max_host_cands / ice_opt.aggressive）へ反映される。
- **Invariant**: STUN/TURN/ICE 配線は §13 と矛盾しない — IceConfig 既定値（enabled=true / aggressive_nomination=true / max_host_candidates=16）が PJSIP 設定へ一貫して反映され、STUN/TURN/ICE の field 反映が src/config/stun_turn_ice_wiring.rs の単一モジュールに閉じる。

### C112 — N0086→N0015

- **Precondition**: N0015 が §12/§13 Transport/ICE 仕様を定義する — transport_ice_spec.rs に StunServerConfig { uri } / TurnServerConfig { uri, username, password: Option<SecretString>, transport: TurnTransport } / IceConfig { enabled, aggressive_nomination, trickle_ice, renomination, max_host_candidates } が定義済み。ClientConfig は stun_servers / turn_servers / ice フィールドを保持する（N0070 一元化後）。
- **Postcondition**: 62.17 が pjsua_config への反映を規定する — 実 PJSIP API（vendor/pjsip/pjsip/include/pjsua-lib/pjsua.h）に合わせ、stun_srv[8] 配列 + stun_srv_cnt、turn_cfg_use（PJSUA_TURN_CONFIG_USE_CUSTOM）+ pjsua_turn_config（enable_turn / turn_server / turn_conn_type / turn_auth_cred）、pjsua_media_config（enable_ice / ice_max_host_cands / ice_opt.aggressive）へ反映される。RFC 擬似コード（stun_srv 単一 / turn_cfg_srv / ice_aggressive 直書き）との差分を実 API に合わせて解決する。
- **Invariant**: stun_srv/turn_cfg/ICE 設定は §13 の既定値に従う — STUN 未設定時は stun_srv_cnt=0、TURN 未設定時は turn_cfg_use=PJSUA_TURN_CONFIG_USE_DEFAULT、ICE 既定値は enable_ice=1 / ice_max_host_cands=16 / ice_opt.aggressive=1。stun_srv[8] 配列上限を超える STUN 設定は SipError（fail-fast）。

### C113 — N0086→N0070

- **Precondition**: N0070 が 62.1 設定一本化を定義する — ClientConfig（client_config_spec.rs）が唯一の公開型で、StunServerConfig / TurnServerConfig / IceConfig の二重定義（旧 config.rs と transport_ice_spec.rs）は一元化済み。stun_servers / turn_servers は Vec 化され、ice は §13 既定値に一致する。
- **Postcondition**: 62.17 が STUN/TURN/ICE の実配線を追補する — ClientConfig.stun_servers / turn_servers / ice を apply_stun_turn / apply_ice が消費し、SecretString::expose_secret() で TURN パスワードを pj_stun_auth_cred.static_cred.data へ渡す。backend_calls::initialize が実 pjsua_config + pjsua_media_config を構築して pjsua_init へ渡す。
- **Invariant**: 単一の StunServerConfig/TurnServerConfig/IceConfig を使用する — 配線は transport_ice_spec の統合型のみを受理し、旧 config.rs 二重定義やフィールド直書きマッピングが配線モジュール外に存在しない。

## Boy Scout Rule

- **stun_turn_ice_wiring.rs を翻訳可能に実装する**: 関数名は動詞句（`apply_stun_turn` / `apply_ice` / `resolve_stun_server_uris` / `resolve_turn_server` / `resolve_turn_conn_type` / `resolve_turn_auth_cred` / `resolve_ice_flags`）、変数名はドメイン概念（`stun_srv_cnt` / `turn_server_uri` / `turn_conn_type` / `ice_max_host_cands`）、1 関数 1 責務（STUN 反映・TURN 反映・ICE 反映・定数解決を分離）、マジックナンバー（stun_srv[8] 上限 / PJ_TURN_TP_UDP=17 / PJ_TURN_TP_TCP=6 / PJ_TURN_TP_TLS=56 / §13 既定値）を命名定数（`STUN_SRV_MAX` / `PJ_TURN_TP_UDP` 等、bindings 由来）として抽出。
- **既存コードの翻訳可能性を改善する（スコープ内外）**:
  - `src/ffi/backend_calls.rs` の `initialize()` は現在 media_cfg に null を渡し、STUN/TURN/ICE を一切反映していない — P16-8 で「pjsua_config 構築（callback 登録 + STUN/TURN）」と「pjsua_media_config 構築（ICE）」をそれぞれ名前付きの小さな関数へ分離し、`pjsua_init(cfg, log_cfg, media_cfg)` の引数が何を運ぶかをコード構造で表現する（transport_wiring.rs の apply_transport_port パターンを踏襲）。
  - `src/runtime/backend.rs` の `PjsuaBackend::initialize` は `_config` を受けて未使用（`let config = _config;` のみ）— P16-8 で config を `backend_calls::initialize(config)` へ渡し、未使用引数を解消する。
  - `src/ffi/bindings.rs` の stub_aliases は `pjsua_config { cb }` のみ — STUN/TURN フィールド追加時に、実 pjsua.h の構造（stun_srv 配列 / pjsua_turn_config / pj_ice_sess_options）をミラーするコメント付き宣言で追加し、C 型名（非 camelCase）の allow と整合させる。
- **エラー握りつぶしを排除する**: `apply_stun_turn` は STUN サーバが stun_srv[8] 上限を超える場合に SipError（InvalidConfig）を返し、超過分を静かに切り捨てない。`apply_ice` は全フィールドが有効値のため Result 不要だが、`resolve_turn_conn_type` は TurnTransport を網羅マッチ（catch-all 禁止）で PJ_TURN_TP_* へ変換する。TURN パスワードは `SecretString::expose_secret()` 経由でのみ PJSIP へ渡し、ログ / Display / Debug に漏らさない。
- **STUB 解消**: `src/config/stun_turn_ice_wiring.rs` の `[::STUB::] P16-8` + `pub struct Config {}`（未使用プレースホルダ）を実装へ置換し、`pub struct Config {}` を削除する（Initial Design Artifact ヘッダは維持）。

## Test Plan

### Unit Tests

- UT: [Normal] C111-pre: §62 親セクションが存在する — Round2Section::StunTurnIceWiring が section() == "62.17" かつ node_id() == "N0086" を返し、62.17 が §62 実装整合設計（N0068）配下に解決されることを検証する。
- UT: [Normal] C111-post: 62.17 が STUN/TURN/ICE 配線を定義する — src/config/stun_turn_ice_wiring.rs（N0086 実装ファイル）が apply_stun_turn / apply_ice を公開し、デフォルトビルドで到達・テスト可能であることを size_of / 関数ポインタで検証する。
- UT: [Normal] C112-pre: N0015 が §12/§13 Transport/ICE 仕様を定義する — StunServerConfig { uri } / TurnServerConfig { uri, username, password, transport } / IceConfig（§13 既定値）が transport_ice_spec から構築可能であることを検証する。
- UT: [Normal] C112-post: 62.17 が pjsua_config への反映を規定する — apply_stun_turn が STUN URI を pjsua_config.stun_srv_cnt / stun_srv[0]（pj_str_t 配列）へ反映し、stun_srv_cnt == 1 かつ stun_srv[0] の文字列が "stun:stun.example.com:3478" と一致することを検証する。@verifies C112-post
- UT: [Normal] C112-post: apply_stun_turn が TURN サーバを pjsua_config.turn_cfg 構造体へ反映する — turn_cfg_use == PJSUA_TURN_CONFIG_USE_CUSTOM、turn_cfg.enable_turn == 1、turn_cfg.turn_server == "turn:turn.example.com:3478"、turn_cfg.turn_conn_type が transport 対応の PJ_TURN_TP_* 値であることを検証する。@verifies C112-post
- UT: [Normal] C113-post: 62.17 が STUN/TURN/ICE の実配線を追補する — apply_stun_turn が TURN 認証情報（username / SecretString password）を turn_cfg.turn_auth_cred（type=PJ_STUN_AUTH_CRED_STATIC、static_cred.username / data / data_type=PJ_STUN_PASSWD_PLAIN）へ反映し、SecretString::expose_secret() 値が data へ渡ることを検証する。@verifies C113-post
- UT: [Normal] C112-post: apply_ice が ICE 設定を pjsua_media_config へ反映する — enable_ice == ice.enabled as c_int、ice_max_host_cands == ice.max_host_candidates、ice_opt.aggressive == ice.aggressive_nomination を検証する。@verifies C112-post
- UT: [Normal] C112-post: TurnTransport → PJ_TURN_TP 変換（Udp=17 / Tcp=6 / Tls=56）を検証する — resolve_turn_conn_type が pjsua.h の pj_turn_tp_type（IANA プロトコル番号）へ正確にマッピングする。@verifies C112-post
- UT: [Error] C112-post: STUN サーバが stun_srv[8] 配列上限（8 個）を超える場合、apply_stun_turn が SipError（InvalidConfig）を返し、超過分を静かに切り捨てない（fail-fast）ことを検証する。@verifies C112-post
- UT: [Error] C113-post: TURN パスワード（SecretString）の expose_secret() 値が turn_auth_cred.static_cred.data へ正しく渡り、Display / Debug では [REDACTED] にマスクされる（機密漏洩なし）ことを検証する。@verifies C113-post
- UT: [Boundary] C112-inv: STUN サーバがちょうど 8 個の場合、全 8 個が stun_srv[0..8] へ反映されエラーにならない（配列上限ちょうどの境界値）ことを検証する。@verifies C112-inv
- UT: [Boundary] C112-inv: STUN / TURN サーバ未設定（空 Vec）の場合、stun_srv_cnt は 0 のまま、turn_cfg_use は PJSUA_TURN_CONFIG_USE_DEFAULT のまま変更されない（no-op）ことを検証する。@verifies C112-inv
- UT: [Boundary] C112-inv: IceConfig { enabled: false, max_host_candidates: 0 } の境界値が enable_ice=0 / ice_max_host_cands=0 へ反映されることを検証する。@verifies C112-inv
- UT: [Invariant] C111-inv: STUN/TURN/ICE 配線は §13 と矛盾しない — IceConfig::default()（enabled=true / aggressive_nomination=true / max_host_candidates=16）が enable_ice=1 / ice_max_host_cands=16 / ice_opt.aggressive=1 へ一貫して反映され、§13 既定値（L584-593）と一致することを検証する。@verifies C111-inv + C112-inv
- UT: [Invariant] C113-inv: 単一の StunServerConfig/TurnServerConfig/IceConfig を使用する — apply_stun_turn / apply_ice が transport_ice_spec の統合型（N0070 一元化）を受理し、旧 config.rs 二重定義（StunServerConfig / TurnServerConfig）に依存しないことをコンパイル時 + 実行時で検証する。@verifies C113-inv
- UT: [Invariant] C113-pre: N0070 が 62.1 設定一本化を定義する — ClientConfig が Vec<StunServerConfig> / Vec<TurnServerConfig> / IceConfig（統合型）を保持し、stun_servers / turn_servers / ice フィールド経由で配線へ渡ることを検証する。@verifies C113-pre
- UT: [Invariant] C112-inv: stun_srv/turn_cfg/ICE 設定は §13 の既定値に従う — 配線モジュール（stun_turn_ice_wiring）以外に stun_srv / turn_cfg / enable_ice の直書きマッピングが存在せず、STUN/TURN/ICE の field 反映が単一モジュールに閉じることを検証する。@verifies C112-inv

### Integration Tests

- IT: [Integration point] ClientConfig → PjsuaBackend::initialize → backend_calls::initialize → pjsua_config + pjsua_media_config → pjsua_init。STUN/TURN/ICE 設定が ClientConfig（stun_servers / turn_servers / ice）から pjsua_config（stun_srv / turn_cfg）と pjsua_media_config（enable_ice / ice_max_host_cands / ice_opt.aggressive）へ反映され、pjsua_init へ渡る一連の配線を、stun_turn_ice_wiring::apply_stun_turn / apply_ice を組み合わせた統合テスト（デフォルトビルド）で検証する。
- IT: [Integration point] apply_stun_turn + apply_ice → pjsua_init の media_cfg 引数。backend_calls::initialize が実 pjsua_config（callback 登録 + STUN/TURN）と実 pjsua_media_config（ICE）を構築して pjsua_init(cfg, log_cfg, media_cfg) を呼ぶことを、pjsua-native feature 下のコンパイル検証（cargo check --features pjsua-native）で確認する。
- IT: [Verification] make test（cargo test --features test-util）で上記統合フローが green になる。pjsua-native feature 下では cargo check --features pjsua-native で新しい FFI シンボル（pjsua_media_config / pjsua_turn_config / PJ_TURN_TP_*）の解決を確認する（本環境では pre-broken のため P16-10 docker 基盤に委譲）。
- IT: [Prerequisites / Related tickets] N0070（§62.1 設定一本化、reviewed）、P16-2（transport & bindgen alignment、reviewed）、P15-2（transport_ice_spec 統合型）が前提。coturn プロトコルレベル検証（STUN binding / TURN allocate / relay 転送）は P16-10（§62.19 Asterisk/coturn docker IT 基盤）に依存し、本チケットでは構造検証 + デフォルトビルド統合テストを実施する。関連: P16-9（Examples E1-E5）

### Exceptions

- Exception entry 1 — [Item]: coturn 実サーバによるプロトコルレベル検証（STUN binding / TURN allocate / relay candidate 経由のメディア転送）はテスト不能（not testable）。[Reason]: 実 coturn（Docker）と 2 クライアントの実メディア転送が必要で、docker 可用性は §62.19（P16-10）でゲートされる外部依存である。これは「決定的だが本質的にテスト不能」な設計欠陥（architectural defect）ではなく not a defect — 配線ロジック（stun_srv 配列 / turn_cfg 構造体 / ICE フラグ反映）はデフォルトビルドで決定的にユニットテスト可能であり、実サーバ検証のみがインテグレーション範囲に属する。[Alternative verification]: apply_stun_turn / apply_ice の構造体反映をユニットテストで検証し、実 coturn 検証は P16-10（§62.19 docker IT 基盤）に委譲。
- Exception entry 2 — [Item]: pjsua-native ビルドでの実 FFI シンボル（pjsua_media_config / pjsua_turn_config / PJ_TURN_TP_*）の動作検証はテスト不能（cannot test）。[Reason]: 本環境の pjsua-native ビルドは P16-7 で確認された通り 40+ 件の無関係 bindgen エラーで pre-broken であり、コンパイル検証が通らない外部環境依存である。これは設計欠陥（design defect）ではなく not a defect — bindgen allowlist の整合性（allowlist_covers_stub_surface 等）は静的テストで検証可能であり、実シンボル検証は CI / P16-10 docker 基盤に委譲される。[Alternative verification]: BINDGEN_ALLOWLIST_TYPES / FUNCTIONS / VARS のテストでスタブサーフェスと allowlist の一致を検証。
- Exception entry 3 — [Item]: TURN 認証の実サーバ動的チャレンジ（realm / nonce を用いた long-term credential の往復）の応答検証はテスト不能（not testable）。[Reason]: pj_stun_auth_cred.static_cred への username / password 反映ロジックはユニットテスト可能だが、実 coturn とのチャレンジ-レスポンス往復はサーバが必須で、本チケットのデフォルトビルドでは検証できない外部依存である。これは設計欠陥（architectural defect）ではなく not a defect — 静的認証情報の反映は決定的にテスト可能。[Alternative verification]: static_cred の realm / username / data_type / data フィールドへの反映をユニットテストで検証し、実チャレンジ応答は P16-10 coturn IT に委譲。

### Plan Test Code (concrete code)

- UT: [Normal] C111-pre: §62 親セクションが存在する — Round2Section::StunTurnIceWiring が 62.17 / N0086 を解決する。
```rust
#[test]
fn round2_section_resolves_62_17_to_n0086() {
    assert_eq!(Round2Section::StunTurnIceWiring.section(), "62.17");
    assert_eq!(Round2Section::StunTurnIceWiring.node_id(), "N0086");
}
```
- UT: [Normal] C111-post: 62.17 が STUN/TURN/ICE 配線を定義する — apply_stun_turn / apply_ice がデフォルトビルドで到達・テスト可能。
```rust
#[test]
fn stun_turn_ice_wiring_surface_is_reachable() -> Result<(), SipError> {
    use crate::config::stun_turn_ice_wiring::{apply_stun_turn, apply_ice};
    let mut cfg: crate::ffi::bindings::pjsua_config = unsafe { std::mem::zeroed() };
    let config = crate::config::ClientConfig::default();
    apply_stun_turn(&mut cfg, &config)?;
    let mut media: crate::ffi::bindings::pjsua_media_config = unsafe { std::mem::zeroed() };
    apply_ice(&mut media, &config.ice);
    Ok(())
}
```
- UT: [Normal] C112-pre: N0015 が §12/§13 Transport/ICE 仕様を定義する — IceConfig::default が §13 既定値（enabled=true / aggressive_nomination=true / max_host_candidates=16）。
```rust
#[test]
fn ice_config_default_matches_section_13() {
    let ice = IceConfig::default();
    assert!(ice.enabled);
    assert!(ice.aggressive_nomination);
    assert!(!ice.trickle_ice);
    assert!(!ice.renomination);
    assert_eq!(ice.max_host_candidates, 16);
}
```
- UT: [Normal] C112-post: 62.17 が pjsua_config への反映を規定する — apply_stun_turn が STUN URI を stun_srv_cnt / stun_srv[0]（pj_str_t 配列）へ反映。
```rust
#[test]
fn apply_stun_turn_reflects_stun_uri_into_config() -> Result<(), SipError> {
    let mut config = ClientConfig::default();
    config.stun_servers = vec![StunServerConfig { uri: "stun:stun.example.com:3478".into() }];
    let mut cfg: bindings::pjsua_config = unsafe { std::mem::zeroed() };
    apply_stun_turn(&mut cfg, &config)?;
    assert_eq!(cfg.stun_srv_cnt, 1);
    assert_eq!(bindings::pj_str_to_string(&cfg.stun_srv[0]), "stun:stun.example.com:3478");
    Ok(())
}
```
- UT: [Normal] C112-post: apply_stun_turn が TURN サーバを pjsua_config.turn_cfg 構造体へ反映（turn_cfg_use / enable_turn / turn_server / turn_conn_type）。
```rust
#[test]
fn apply_stun_turn_reflects_turn_server_into_config() -> Result<(), SipError> {
    let mut config = ClientConfig::default();
    config.turn_servers = vec![TurnServerConfig {
        uri: "turn:turn.example.com:3478".into(),
        username: Some("user".into()),
        password: Some(SecretString::new("pass")),
        transport: TurnTransport::Udp,
    }];
    let mut cfg: bindings::pjsua_config = unsafe { std::mem::zeroed() };
    apply_stun_turn(&mut cfg, &config)?;
    assert_eq!(cfg.turn_cfg_use, bindings::PJSUA_TURN_CONFIG_USE_CUSTOM);
    assert_eq!(cfg.turn_cfg.enable_turn, 1);
    assert_eq!(bindings::pj_str_to_string(&cfg.turn_cfg.turn_server), "turn:turn.example.com:3478");
    assert_eq!(cfg.turn_cfg.turn_conn_type, bindings::PJ_TURN_TP_UDP);
    Ok(())
}
```
- UT: [Normal] C113-post: 62.17 が STUN/TURN/ICE の実配線を追補する — TURN 認証情報（username / SecretString password）が turn_auth_cred.static_cred へ反映（expose_secret）。
```rust
#[test]
fn apply_stun_turn_reflects_turn_auth_cred() -> Result<(), SipError> {
    let mut config = ClientConfig::default();
    config.turn_servers = vec![TurnServerConfig {
        uri: "turn:turn.example.com:3478".into(),
        username: Some("alice".into()),
        password: Some(SecretString::new("s3cret")),
        transport: TurnTransport::Tcp,
    }];
    let mut cfg: bindings::pjsua_config = unsafe { std::mem::zeroed() };
    apply_stun_turn(&mut cfg, &config)?;
    let cred = &cfg.turn_cfg.turn_auth_cred.cred.static_cred;
    assert_eq!(cred.data_type, bindings::PJ_STUN_PASSWD_PLAIN);
    assert_eq!(bindings::pj_str_to_string(&cred.username), "alice");
    assert_eq!(bindings::pj_str_to_string(&cred.data), "s3cret");
    Ok(())
}
```
- UT: [Normal] C112-post: apply_ice が ICE 設定を pjsua_media_config へ反映（enable_ice / ice_max_host_cands / ice_opt.aggressive）。
```rust
#[test]
fn apply_ice_reflects_ice_settings_into_media_config() {
    let ice = IceConfig { enabled: true, aggressive_nomination: true, trickle_ice: false, renomination: false, max_host_candidates: 10 };
    let mut media: bindings::pjsua_media_config = unsafe { std::mem::zeroed() };
    apply_ice(&mut media, &ice);
    assert_eq!(media.enable_ice, 1);
    assert_eq!(media.ice_max_host_cands, 10);
    assert_eq!(media.ice_opt.aggressive, 1);
}
```
- UT: [Normal] C112-post: TurnTransport → PJ_TURN_TP 変換（Udp=17 / Tcp=6 / Tls=56）。
```rust
#[test]
fn resolve_turn_conn_type_maps_transport_to_pj_turn_tp() {
    assert_eq!(resolve_turn_conn_type(TurnTransport::Udp), bindings::PJ_TURN_TP_UDP);
    assert_eq!(resolve_turn_conn_type(TurnTransport::Tcp), bindings::PJ_TURN_TP_TCP);
    assert_eq!(resolve_turn_conn_type(TurnTransport::Tls), bindings::PJ_TURN_TP_TLS);
}
```
- UT: [Error] C112-post: STUN サーバが stun_srv[8] 上限を超える場合 SipError（fail-fast、切り捨てない）。
```rust
#[test]
fn apply_stun_turn_rejects_more_than_eight_stun_servers() -> Result<(), SipError> {
    let mut config = ClientConfig::default();
    config.stun_servers = (0..9).map(|i| StunServerConfig { uri: format!("stun:s{i}.example.com:3478") }).collect();
    let mut cfg: bindings::pjsua_config = unsafe { std::mem::zeroed() };
    let result = apply_stun_turn(&mut cfg, &config);
    assert!(result.is_err());
    assert!(matches!(result, Err(SipError { kind: SipErrorKind::InvalidConfig, .. })));
    assert_eq!(cfg.stun_srv_cnt, 0, "no partial reflection on error");
    Ok(())
}
```
- UT: [Boundary] C112-inv: STUN / TURN 未設定（空 Vec）は no-op（stun_srv_cnt=0 / turn_cfg_use=PJSUA_TURN_CONFIG_USE_DEFAULT）。
```rust
#[test]
fn apply_stun_turn_with_no_servers_is_noop() -> Result<(), SipError> {
    let config = ClientConfig::default(); // stun_servers / turn_servers は空
    let mut cfg: bindings::pjsua_config = unsafe { std::mem::zeroed() };
    apply_stun_turn(&mut cfg, &config)?;
    assert_eq!(cfg.stun_srv_cnt, 0);
    assert_eq!(cfg.turn_cfg_use, bindings::PJSUA_TURN_CONFIG_USE_DEFAULT);
    assert_eq!(cfg.turn_cfg.enable_turn, 0);
    Ok(())
}
```
- UT: [Invariant] C111-inv + C112-inv: STUN/TURN/ICE 配線は §13 と矛盾しない — 既定 IceConfig が enable_ice=1 / ice_max_host_cands=16 / ice_opt.aggressive=1 へ一貫して反映。
```rust
#[test]
fn apply_ice_default_reflects_section_13_defaults() {
    let ice = IceConfig::default();
    let mut media: bindings::pjsua_media_config = unsafe { std::mem::zeroed() };
    apply_ice(&mut media, &ice);
    assert_eq!(media.enable_ice, 1);
    assert_eq!(media.ice_max_host_cands, 16);
    assert_eq!(media.ice_opt.aggressive, 1);
}
```
- UT: [Invariant] C113-inv: 単一の StunServerConfig/TurnServerConfig/IceConfig を使用する — apply_stun_turn / apply_ice が transport_ice_spec の統合型を受理し、旧 config.rs 二重定義に依存しない（コンパイル時検証）。
```rust
#[test]
fn wiring_accepts_unified_config_types() {
    fn assert_unified<T>() {}
    assert_unified::<StunServerConfig>();
    assert_unified::<TurnServerConfig>();
    assert_unified::<IceConfig>();
    // ClientConfig のフィールド型が統合型 Vec であることを検証
    let config = ClientConfig::default();
    let _stun: &Vec<StunServerConfig> = &config.stun_servers;
    let _turn: &Vec<TurnServerConfig> = &config.turn_servers;
    let _ice: &IceConfig = &config.ice;
}
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| src/config/stun_turn_ice_wiring.rs was [::STUB::] P16-8 + pub struct Config {} placeholder | Implemented apply_stun_turn(cfg: &mut pjsua_config, config: &ClientConfig) -> Result<StunTurnOwned, SipError> (STUN stun_srv_cnt/stun_srv[0..8], TURN turn_cfg_use/turn_cfg{enable_turn,turn_server,turn_conn_type,turn_auth_cred}), apply_ice(cfg: &mut pjsua_media_config, ice: &IceConfig) (enable_ice/ice_max_host_cands/ice_opt.aggressive), resolve_turn_conn_type (Udp=17/Tcp=6/Tls=56), and StunTurnOwned lifetime guard. 13 unit tests. | C111/C112/C113 — STUN/TURN/ICE wiring per §62.17, RFC discrepancy resolved to real pjsua.h API (stun_srv[8] array + pjsua_turn_config + ice_opt.aggressive) |
| src/ffi/bindings.rs stub pjsua_config had only cb field; no pjsua_media_config/pjsua_turn_config/PJ_TURN_TP_* constants | Added stub types pj_turn_tp_type/pjsua_turn_config_use/pj_stun_auth_cred_type/pj_stun_passwd_type + constants PJ_TURN_TP_UDP=17/TCP=6/TLS=56/PJSUA_TURN_CONFIG_USE_*/PJ_STUN_AUTH_CRED_STATIC/PJ_STUN_PASSWD_PLAIN + structs pjsua_turn_config/pjsua_media_config/pj_ice_sess_options/pj_stun_auth_cred(_static/_union); extended pjsua_config with stun_srv_cnt/stun_srv[8]/turn_cfg_use/turn_cfg | Stub surface mirrors vendored pjsua.h/pjnath headers so the wiring compiles in the default build |
| backend_calls::initialize() took no args, passed null media_cfg to pjsua_init | backend_calls::initialize(config: &ClientConfig) builds real pjsua_config (callbacks + STUN/TURN via apply_stun_turn, owned strings held in scope) and pjsua_media_config (ICE via apply_ice), passes both to pjsua_init; STUN>8 returns PJ_EINVALIDOP | PjsuaBackend now applies STUN/TURN/ICE at stack init (§62.17 Q7) |
| PjsuaBackend::initialize received config but did not pass it to backend_calls::initialize | PjsuaBackend::initialize passes config to backend_calls::initialize(config) (unused-argument resolved) | Config reachability — ClientConfig.stun_servers/turn_servers/ice consumed at init |
| config.rs had no stun_turn_ice_wiring module declaration | Added pub mod stun_turn_ice_wiring; with P16-8 provenance | Module registration for the N0086 implementation file |
| build_script_bindgen.rs allowlists lacked STUN/TURN/ICE symbols | Added pjsua_media_config/pjsua_turn_config/pjsua_turn_config_use/pj_ice_sess_options/pj_stun_auth_cred(_static/_type)/pj_stun_passwd_type/pj_turn_tp_type to BINDGEN_ALLOWLIST_TYPES and PJ_TURN_TP_*/PJSUA_TURN_CONFIG_USE_*/PJ_STUN_AUTH_CRED_STATIC/PJ_STUN_PASSWD_PLAIN to BINDGEN_ALLOWLIST_VARS; added allowlist_covers_stun_turn_ice_surface test | Bindgen allowlist alignment for pjsua-native (deferred compile verification to P16-10) |

## RFC Discrepancies found in Prior Implementation Rounds

- RFC §62.17 pseudocode (cfg.stun_srv single string / turn_cfg_srv / ice_aggressive) differs from the vendored pjsua.h — implemented against the real API: stun_srv[8] array + pjsua_turn_config struct + ice_opt.aggressive (documented in spec Investigation/C112-post)

## Notes in Prior Implementation Rounds

- [Implementation steps] 1) src/config.rs に `pub mod stun_turn_ice_wiring;` を追加。2) src/ffi/bindings.rs の stub_aliases に STUN/TURN/ICE 型（stun_srv_cnt / stun_srv[8] / pjsua_turn_config / pjsua_media_config / pj_ice_sess_options / pj_stun_auth_cred）と定数（PJ_TURN_TP_* / PJSUA_TURN_CONFIG_USE_* / PJ_STUN_AUTH_CRED_STATIC / PJ_STUN_PASSWD_PLAIN）を追加。3) src/config/stun_turn_ice_wiring.rs に apply_stun_turn / apply_ice + resolve_* ヘルパーを実装（デフォルトビルドでテスト可能）。4) backend_calls::initialize を `initialize(config: &ClientConfig)` へ変更し、pjsua_config + pjsua_media_config を構築して pjsua_init へ渡す。5) runtime/backend.rs の PjsuaBackend::initialize が config を渡す。6) build_script_bindgen.rs の allowlist に追加。7) テスト（unit + integration）を実装し make test で green 確認。
- [Risks] stun_srv[8] 配列上限: ClientConfig.stun_servers が 9 個以上の場合は SipError（fail-fast）。RFC 擬似コードと実 pjsua.h の乖離（stun_srv 単一 / turn_cfg_srv / ice_aggressive → stun_srv[8] / pjsua_turn_config / ice_opt.aggressive）により、実装時にフィールド名の誤マッピングリスクがある。
- [Caveats] pj_stun_auth_cred は union 型で、static_cred の realm / username / data_type / data / nonce を正しく初期化する必要がある（未初期化領域を PJSIP が読むと未定義動作）。turn_auth_cred 未使用時（username/password なし）はゼロ初期化のままにする。turn_tls_setting は TLS 時のみ使用。
- [Open items] turn_tls_setting（pj_turn_sock_tls_cfg）の TLS 設定反映は TurnTransport::Tls 時に必要だが、詳細な TLS パラメータ（CA 証明書等）は §12 TlsConfig との関連を要確認。ice_manual_host_cnt / ice_no_rtcp / ice_always_update / ice_opt の残りフィールドは §13 の既定値に従い、本チケットでは aggressive のみ明示設定する。
- [Future improvements] coturn 実サーバ統合テスト（P16-10）で STUN binding / TURN allocate / relay 転送を検証。Examples（P16-9）で STUN/TURN/ICE 設定済み ClientConfig の使用例を提供。trickle ICE / renomination（IceConfig に存在）の PJSIP 反映（pj_ice_sess_options）は将来拡張。


Implementation summary (P16-8 / start):
- Changed files: src/config/stun_turn_ice_wiring.rs, src/ffi/backend_calls.rs, src/ffi/bindings.rs, src/config.rs, src/build/build_script_bindgen.rs, src/runtime/backend.rs
- Key design decision: RFC §62.17 pseudocode (stun_srv single / turn_cfg_srv / ice_aggressive) differs from the vendored pjsua.h; implemented against the real API (stun_srv[8] array + pjsua_turn_config struct + ice_opt.aggressive). apply_stun_turn returns a StunTurnOwned guard so the caller keeps the reflected pj_str_t backing strings alive (use-after-free avoidance, mirroring transport_create's bound_owned).
- Test results: make test exit 0 (all green); new tests 13 (stun_turn_ice_wiring) + 1 (allowlist_covers_stun_turn_ice_surface).
- Quality: run-quality-checks reports 76 findings — all pre-existing test-code unwrap/expect in backend.rs (untouched by P16-8) and by-design unsafe in src/ffi/ + test zeroed() pattern. New production code is unwrap-free; one commented-code false positive (doc comment containing 'function') was fixed.
- Annotations: 4 files annotated with P16-8 provenance (stun_turn_ice_wiring.rs 20 sites, backend_calls.rs 1, bindings.rs 3, config.rs 1, backend.rs merged, build_script_bindgen.rs already annotated); 3 AMBIGUOUS markers resolved via resolve-ambiguous-markers.js.
- TS-001 resolved: [::STUB::] P16-8 in stun_turn_ice_wiring.rs replaced with real implementation.
- Deferred: pjsua-native compile verification (P16-10, pre-broken in this env); coturn protocol-level IT (P16-10).
Review report (P16-8):
- Step 3: crime scan — 0 open crimes (Condition A satisfied).
- Step 4: STUB evaluation — 4 stubs remain, all correctly deferred to future tickets (P16-9 Examples, P16-10 RustMediaPort FFI + docker_asterisk_it, P16-11 I/O boundary). TS-001 (P16-8 own stub) resolved. no-excuse gate 4/4 pass.
- Step 5: active incomplete scan — no todo!/unimplemented!/panic!/empty-fn/return-Ok stubs in changed code. Annotation verify 6/6 files annotated, 0 missing/ambiguous. check-ambiguous exit 0.
- Step 6: cargo check --all-targets 0 errors/0 warnings; make test exit 0 (all green).
- Step 7: completeness — spec Goal/Purpose/Motivation/Constraints/Scope/Acceptance/Invariants satisfied: apply_stun_turn (STUN stun_srv[8] + TURN turn_cfg struct), apply_ice (enable_ice/ice_max_host_cands/ice_opt.aggressive), StunTurnOwned lifetime guard, backend_calls::initialize(config) wiring, allowlist additions, module registration. Contracts C111/C112/C113 covered by 13 @verifies annotations.
- Step 8: static quality — 76 findings, all pre-existing/by-design (see instrumentation).
- Step 9: translatability — no issues.
- Step 10b: verify-final-contracts exit 0 (coverage 100%); validate-ticket-targets 11 checks pass.
- Issues found and fixes applied during review: (1) commented-code false positive in stun_turn_ice_wiring.rs doc ('function' keyword) — rephrased to 'helpers'; (2) TS-001 targetStub status resolved + markerText updated to resolved implementation location; (3) 3 AMBIGUOUS markers resolved via resolve-ambiguous-markers.js.

## P16-8 — implemented at 29 locations

### src/build/build_script_bindgen.rs

- Line 347
```rust
    fn allowlist_covers_stun_turn_ice_surface() {
```

### src/config.rs

- Line 46
```rust
pub mod stun_turn_ice_wiring;
```

### src/config/stun_turn_ice_wiring.rs

- Line 71
```rust
fn turn_tp_value(transport: TurnTransport) -> bindings::pj_turn_tp_type {
```

- Line 81
```rust
fn turn_tp_value(transport: TurnTransport) -> bindings::pj_turn_tp_type {
```

- Line 91
```rust
fn turn_config_custom() -> bindings::pjsua_turn_config_use {
```

- Line 97
```rust
fn turn_config_custom() -> bindings::pjsua_turn_config_use {
```

- Line 103
```rust
fn stun_auth_cred_static() -> bindings::pj_stun_auth_cred_type {
```

- Line 109
```rust
fn stun_auth_cred_static() -> bindings::pj_stun_auth_cred_type {
```

- Line 115
```rust
fn stun_passwd_plain() -> bindings::pj_stun_passwd_type {
```

- Line 121
```rust
fn stun_passwd_plain() -> bindings::pj_stun_passwd_type {
```

- Line 220
```rust
    fn round2_section_resolves_62_17_to_n0086() {
```

- Line 230
```rust
    fn stun_turn_ice_wiring_surface_is_reachable() -> Result<(), SipError> {
```

- Line 244
```rust
    fn ice_config_default_matches_section_13() {
```

- Line 258
```rust
    fn apply_stun_turn_reflects_stun_uri_into_config() -> Result<(), SipError> {
```

- Line 276
```rust
    fn apply_stun_turn_reflects_turn_server_into_config() -> Result<(), SipError> {
```

- Line 301
```rust
    fn apply_stun_turn_reflects_turn_auth_cred() -> Result<(), SipError> {
```

- Line 321
```rust
    fn apply_ice_reflects_ice_settings_into_media_config() {
```

- Line 339
```rust
    fn resolve_turn_conn_type_maps_transport_to_pj_turn_tp() {
```

- Line 359
```rust
    fn apply_stun_turn_rejects_more_than_eight_stun_servers() -> Result<(), SipError> {
```

- Line 383
```rust
    fn apply_stun_turn_with_no_servers_is_noop() -> Result<(), SipError> {
```

- Line 399
```rust
    fn apply_ice_default_reflects_section_13_defaults() {
```

- Line 411
```rust
    fn wiring_accepts_unified_config_types() {
```

- Line 413
```rust
        fn assert_unified<T>() {}
```

- Line 426
```rust
    fn client_config_exposes_unified_stun_turn_ice_fields() {
```

### src/ffi/backend_calls.rs

- Line 32
```rust
pub fn initialize(config: &crate::config::ClientConfig) -> i32 {
```

### src/ffi/bindings.rs

- Line 380
```rust
    pub struct pjsua_config {
```

- Line 469
```rust
    pub struct pjsua_turn_config {
```

- Line 495
```rust
    pub struct pjsua_media_config {
```

### src/runtime/backend.rs

- Line 673
```rust
    fn initialize(&mut self, _config: &crate::config::ClientConfig) -> Result<(), ReactorError> {
```
