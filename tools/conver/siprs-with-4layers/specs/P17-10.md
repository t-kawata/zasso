# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P17-10: Round-3 I/O boundary reference (§62.30)

**Ticket Key**: P17-10 · **Phase**: 17

**RFC Source**: `RFC-ROOT.md`

---

## Background

graphify / boundify の分割判断に使う round 3 の I/O 境界参照情報（§62.30）。

### Goal
§62.30 の I/O 境界表（8 設計判断: 62.22–62.29）を Rust の型付き定数として符号化し、round-2 §62.20（io_boundary_round2.rs）の Round2IoBoundaryRow パターンを round-3 に適用する。Prune / 更新対象（callback.rs の stale on_rx_msg コメント・m20_native_event_conv.rs の「P1/P2 returns None」doc・CallResumed unit variant・DtmfSent「コールバック優先」記述）を Round3DeleteTarget として整理する。

### Purpose
graphify/boundify が round-3 設計判断（62.22–62.29）の分割・Prune 判断に使う I/O 境界参照情報（入力・出力・関連ファイル候補・関連グラフノード）を機械可読にする。round-1 §62.9（impl_integration_design::IO_BOUNDARY_TABLE）→ round-2 §62.20（io_boundary_round2::ROUND2_IO_BOUNDARY_TABLE）→ round-3 §62.30 と連なる参照チェーンを完成させ、ツール群が round-3 の削除/更新対象を追跡できるようにする。

### Motivation
round-3 実装（P17-2〜P17-9: 62.22–62.29）が完了しつつあり、§62.30 の参照情報が src/architecture/io_boundary_round3.rs の STUB として未符号化のまま残っている。round-2 では §62.20 の ROUND2_DELETE_TARGETS が boundify の Prune 判断に使用されており、round-3 でも同型の参照が graphify/boundify から要求される。また P17-1（§62.21）の ROUND3_BREAKING_CHANGES（2 件: CallResumed ペイロード化 / subscribe 系 Subscription<T> 化）と対応する削除/更新対象を一元管理する必要がある。

### Constraints
- round-2 の `Round2IoBoundaryRow.decision` は `Round2Section`（62.11–62.20 専用）であり `Round3Section` を直接受け入れられない。round-3 専用型（Round3IoBoundaryRow / Round3DeleteTarget）を io_boundary_round3.rs に新設し、round-1（impl_integration_design.rs）と round-2（io_boundary_round2.rs）の実装ファイルは変更しない。
- 実ファイルの削除は行わない。本チケットは削除/更新対象の参照符号化のみを担当し、実際の Prune は boundify（外部ツール）が行う。
- 新規依存クレート・設定・スキーマ変更なし。対象は src/architecture/io_boundary_round3.rs（add）と src/architecture/mod.rs（モジュール宣言 1 行）のみ。

## Scope

- 【変更1-path】src/architecture/io_boundary_round3.rs（add）、src/architecture/mod.rs（modify）
- 【変更1-action】io_boundary_round3.rs は add（STUB → 実装）、mod.rs は modify（モジュール宣言 1 行追加）。実ファイル削除なし。
- 【変更1-detail】§62.30 の I/O 境界表を Round3IoBoundaryRow（decision: Round3Section, consumes, produces, file_candidates, graph_node_ids）8 行として符号化。Prune/更新対象を Round3DeleteTarget（path, description, resolved_by）4 件として整理。known_round3_graph_node_ids() で既知ノード集合を提供。
- 【変更1-before-after】io_boundary_round3.rs: [::STUB::] マーカーのみ（23 行）→ ROUND3_IO_BOUNDARY_TABLE: [Round3IoBoundaryRow; 8] と ROUND3_DELETE_TARGETS: [Round3DeleteTarget; 4] と known_round3_graph_node_ids() および #[cfg(test)] テスト群を実装。mod.rs: io_boundary_round3 未宣言 → 宣言追加。
- 【変更1-api】crate::architecture::io_boundary_round3 配下に新規 pub 型/定数（Round3IoBoundaryRow / Round3DeleteTarget / ROUND3_IO_BOUNDARY_TABLE / ROUND3_DELETE_TARGETS / known_round3_graph_node_ids）。round-1/round-2 の I/O 境界実装（impl_integration_design.rs / io_boundary_round2.rs）は変更なし（互換維持）。
- 【変更1-schema】データスキーマ変更なし（Rust の型付き定数のみ。シリアライズ・DB・外部スキーマの変更なし）。
- 【変更1-config】設定変更なし（環境変数・設定ファイルの変更なし）。
- 【変更1-dep】依存クレート追加なし。
- 【非変更1-item】round-2 §62.20 の実装（io_boundary_round2.rs の Round2IoBoundaryRow / Round2DeleteTarget / ROUND2_IO_BOUNDARY_TABLE / ROUND2_DELETE_TARGETS）と round-1 §62.9 の実装（impl_integration_design.rs の IoBoundaryRow / IO_BOUNDARY_TABLE）。
- 【非変更1-why】round-2 の Round2IoBoundaryRow.decision は Round2Section（62.11–62.20 固定）で Round3Section を表現できない。また round-1/round-2/round-3 の責務分離を維持するため先行の実装ファイルは変更しない。実ファイル削除（Prune）は boundify の責務であり本チケットでは行わない。
- 【影響1-component】src/architecture/mod.rs（pub mod io_boundary_round3 追加）、graphify/boundify ツール（round-3 の分割・Prune 判断に新表を参照可能）、round-3 後続チケット（P17-11 以降）の file_candidates 更新元。
- 【影響1-nature】API surface（architecture モジュールに新規公開型・定数を追加、既存 API は不変）。参照データの一元化により graphify/boundify の round-3 対応が容易になる。実行時性能・セキュリティ・データ形式への影響なし。
- 【影響1-response】mod.rs に pub mod io_boundary_round3; を追加（必須）。boundify 側の参照は追加のみで既存挙動に影響なし（Y, mod.rs のみ）。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0099`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0099` | architecture | rust | 62.30 I/O 境界参照情報（graphify / boundify 用）— round 3 |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.30 I/O 境界参照情報（graphify / boundify 用）— round 3 (N0099) | → | ☆ 62.20 I/O 境界参照情報 (N0089) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0099` | `src/architecture/io_boundary_round3.rs` |

## Investigation

- RFC §62.30（RFC-ROOT.md / N0099）の I/O 境界表は 8 行（62.22–62.29 が各 1 行）。列は「設計判断 / 入力(consumes) / 出力(produces) / 関連 GRAPH ノード・ファイル候補」。削除対象の整理として callback.rs stale on_rx_msg コメント・m20_native_event_conv.rs stale doc・CallResumed unit variant・DtmfSent コールバック優先記述が列挙される。
- round-1 参照モデル（§62.9, N0078）: src/architecture/impl_integration_design.rs に IoBoundaryRow{decision: DesignDecisionId, consumes, produces, graph_node_ids} / IO_BOUNDARY_TABLE: [IoBoundaryRow; 8] / DeleteTarget / DELETE_TARGETS / known_graph_node_ids。
- round-2 参照モデル（§62.20, N0089）: src/architecture/io_boundary_round2.rs に Round2IoBoundaryRow{decision: Round2Section, consumes, produces, file_candidates, graph_node_ids} / ROUND2_IO_BOUNDARY_TABLE: [Round2IoBoundaryRow; 10] / Round2DeleteTarget{path, description, resolved_by} / ROUND2_DELETE_TARGETS: [Round2DeleteTarget; 3] / known_round2_graph_node_ids()。テストは round1_io_boundary_table_exists / round2_table_has_ten_rows_with_concrete_boundaries / round2_delete_targets_are_defined_and_exist / round2_known_nodes_include_section61_and_62_9 / round1_io_boundary_table_is_available / round2_table_covers_62_11_to_62_19_only / delete_targets_resolve_to_table_rows / round2_delete_targets_source_consistency。
- round-3 設計判断型: src/architecture/round3_scope_rootcause.rs に Round3Section（10 変種, 62.21–62.30, derive Debug/Clone/Copy/PartialEq/Eq）と section()/node_id()/label() が定義済み。node_id は EvolutionScopeRootcause→N0090, RawSipProductionPath→N0091, FfiCallbackRegistration→N0092, TestBackendRegistrationEvents→N0093, CallEntryStateTransition→N0094, CallResumedMechanism→N0095, DtmfSentContract→N0096, TapProductionPath→N0097, DocumentationDecisions→N0098, IoBoundaryReference→N0099。known_graph_node_ids() は N0068/N0089/N0090..N0099 を返す。ROUND3_ROOT_CAUSES（RC1..RC6）・ROUND3_POLICY・ROUND3_BREAKING_CHANGES（CallResumedPayload / SubscriptionGenerics）も定義済み。
- グラフ調査（RFC-ROOT-GRAPH.json の N0091–N0098 references 関係）: N0091 refs N0038（§27 M20 Dual Client PJSIP Callback Routing）・N0080（62.11 トランスポート配線と bindgen）/ N0092 refs N0021（§15 M20 NativeEvent to SipEventPayload Conversion）・N0038 / N0093 refs N0071（62.2 バックエンド選択機構）/ N0094 refs N0045（§33 Runtime Internal State & Lock Rules）・N0083（62.14 着信・通話イベント）/ N0095 refs N0026（§18 Call State Model）/ N0096 refs N0029（§20 M20 DtmfSentInfo & Two-Phase Design）・N0084（62.15 DTMF 実装整合）/ N0097 refs N0049（§39 Media Bridge & PJSUA Conference Port）・N0085（62.16 メディア経路の完成）/ N0098 refs N0031（§22 Audio Subscribe API & Backpressure Policy）・N0074（62.5 公開 API 拡充）。これらを各行の graph_node_ids に使用。
- 削除/更新対象の現状（全て先行チケットで解決済み）: (1) src/ffi/callback.rs:147 は「pjsua_callback に on_rx_msg は存在せず、pjsip_module extension point 経由」と更新済み（P17-2 §62.22）。(2) src/ffi/raw_sip_module.rs は P17-2 で新規作成済み（12KB、on_rx_request/on_rx_response を pjsip_endpt_register_module で登録）。(3) src/state/m20_native_event_conv.rs:42-45 は「P1/P2 variants convert to Some(SipEventPayload) since P16-4 §62.13」と更新済み（「P1/P2 returns None」は既に不存在）。(4) src/api/event_model_payload_bus.rs:342 は CallResumed(CallResumedInfo) のペイロード variant（unit variant は不存在、P17-6 §62.26 解決済み）。(5) src/api/m20_dtmfsent_twophase.rs:32-33 は「backend accept + 500ms timeout」契約（コールバック優先記述は不存在、P17-7 §62.27 解決済み）。
- ファイル候補の実在性: src/ffi/raw_sip_module.rs, src/ffi/callback.rs, src/api/eventbus_receiver.rs, src/state/m20_native_event_conv.rs, src/runtime/backend.rs, src/runtime/reactor.rs, src/state/m20_callstate_mapping.rs, src/api/event_model_payload_bus.rs, src/api/m20_dtmfsent_twophase.rs, src/runtime/audio_worker.rs, src/api/audio_subscribe_bp.rs, src/client.rs は全て実在。src/runtime/registr_wiring.rs は未生成（例外として明示）。
- モジュール配線: src/architecture/mod.rs は crate_scope / impl_integration_design / policy_reference / round2_scope_rootcause / examples_e1e5 / io_boundary_round2 / round3_scope_rootcause を宣言済み。io_boundary_round3 は未宣言（add 必要）。
- src/architecture/io_boundary_round3.rs は現在 23 行の STUB（Initial Design Artifact ヘッダ + [::STUB::] P17-10 + [::TICKET::] P17-1 参照のみ）。

## Acceptance Criteria

- **[Happy path]** `make test` が全て green。`crate::architecture::io_boundary_round3::ROUND3_IO_BOUNDARY_TABLE` が 8 行（62.22–62.29 各 1 行）で、各 decision が Round3Section の 62.22–62.29 に属し、consumes / produces / file_candidates / graph_node_ids が非空。`ROUND3_DELETE_TARGETS` が 4 件で各 path が実在。`known_round3_graph_node_ids()` が N0068 / N0078 / N0089 / N0090–N0099 と各行の参照ノードを含む。
- **[Error case]** ノード ID の typo（例: N00115）、行数不一致（8 行以外）、削除対象 path の実在違反、resolved_by が表に存在しない — 全ての逸脱がテストで赤として検出され、TDD の赤→緑を経て修正される。const 定義の不整合はコンパイルエラーまたはテスト失敗として可視化される。
- **[Edge case]** 62.21（EvolutionScopeRootcause）と 62.30（IoBoundaryReference）が表に含まれない（62.21 は根因定義、62.30 は参照情報の容器）。RFC のファイル名表記揺れ（m20_native_event_conv.rs）が実ソースパス（src/state/）に正規化され、パス実在検証が実在パスに対して行われる。未生成パス（src/runtime/registr_wiring.rs）は存在チェック対象から除外され description で明示される。

## Invariants

round 3 の全設計判断が I/O 境界テーブルに網羅される。

- 【Normal establishment】ROUND3_IO_BOUNDARY_TABLE が常に 8 行を保持し、各 decision が round3_scope_rootcause::Round3Section の 62.22–62.29 に属する。各節がちょうど 1 行で、62.21（EvolutionScopeRootcause・根因定義）と 62.30（IoBoundaryReference・参照情報の容器）は表に含まれない。
- 【Invariant on error】本チケットは純粋な参照符号化（const データ）であり実行時エラーを生まない。const 定義の不整合（行数・ノード ID typo・パス誤り）はコンパイル時またはテスト時に検出され、表が不正な状態で出荷されることはない。
- 【Internal state invariant】ROUND3_DELETE_TARGETS の各 resolved_by（RawSipProductionPath / FfiCallbackRegistration / CallResumedMechanism / DtmfSentContract）が ROUND3_IO_BOUNDARY_TABLE の decision 集合に必ず含まれる。これにより graphify/boundify の Prune 判断が表を参照可能になる（C136 Invariant）。
- 【Boundary invariant】graph_node_ids の全参照が known_round3_graph_node_ids() に含まれ、§62 親（N0068）・round-1 §62.9（N0078）・round-2 §62.20（N0089）・round-3 節（N0090–N0099）と整合する。62.30（IoBoundaryReference）自身は表の外側（参照情報の容器）であり、表に含まれない。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C136 — N0099→N0089

- **Precondition**: Round 2 I/O boundary reference exists (§62.20) — 実体: crate::architecture::io_boundary_round2::ROUND2_IO_BOUNDARY_TABLE（10 行）と ROUND2_DELETE_TARGETS（3 件）が定義されている。テスト翻訳: UT「[C136 Precondition] ROUND2_IO_BOUNDARY_TABLE.len() == 10 かつ ROUND2_DELETE_TARGETS.len() == 3」
- **Postcondition**: Round 3 I/O boundaries are listed for graphify / boundify partitioning — 実体: ROUND3_IO_BOUNDARY_TABLE が 8 行（62.22–62.29 各 1 行）で定義され、各行が非空の consumes/produces/file_candidates/graph_node_ids を持ち、ROUND3_DELETE_TARGETS が 4 件定義される。テスト翻訳: UT「[C136 Postcondition] ROUND3_IO_BOUNDARY_TABLE が 8 行で各 decision が 62.22–62.29」/ UT「[C136 Postcondition] ROUND3_DELETE_TARGETS が 4 件で各 path が実在」
- **Invariant**: Every round-3 decision maps to consumes / produces / file hints — 実体: 全行が非空の consumes/produces/file_candidates を持ち、各 Round3DeleteTarget.resolved_by が表の decision 集合に存在する。テスト翻訳: UT「[C136 Invariant] 全行の consumes/produces/file_candidates が非空」/ UT「[C136 Invariant] 各削除対象の resolved_by が表の decision に存在する」

## Boy Scout Rule

- 関数名を動詞句に: known_round3_graph_node_ids()（既知ノード集合を返す）は round-2 の known_round2_graph_node_ids() と同型の動詞句 + ドメイン名詞で命名。
- 構造体/定数をドメイン概念で命名: Round3IoBoundaryRow（1 設計判断の I/O 境界）・Round3DeleteTarget（boundify の削除/更新対象）・ROUND3_IO_BOUNDARY_TABLE（§62.30 表）・ROUND3_DELETE_TARGETS（§62.30 削除/更新対象）は round-1/round-2 の命名パターン（IoBoundaryRow / Round2IoBoundaryRow 等）を踏襲。
- ハードコード値を名前付き定数に: RFC 表の 8 行と 4 削除/更新対象を pub const として符号化し、マジック値（行数 8 / 削除数 4 / ノード ID 文字列）をテストから定数参照で検証。
- エラー吞み込みなし: 純粋な const データ（実行時 I/O なし）のため Result 不要。テストは Result<(), std::io::Error> を返し、ファイル読取エラーを伝播。
- 既存コードの改善（Boy Scout）: (a) src/architecture/mod.rs のヘッダコメント「Sub-modules:」一覧に io_boundary_round3 を追記し、コメントが嘘にならないよう一覧を最新化する（P16-11 が io_boundary_round2 を追記したのと同様の修正）。(b) io_boundary_round3.rs の STUB マーカーを実装で置換し、[::STUB::] を解消。(c) round-1/round-2 の実装ファイル（impl_integration_design.rs / io_boundary_round2.rs）は変更しない（round-1/round-2/round-3 の責務分離を維持）。

## Test Plan

### Unit Tests

- UT: [Normal] ROUND3_IO_BOUNDARY_TABLE が §62.30 の 8 設計判断を保持する — assert_eq!(ROUND3_IO_BOUNDARY_TABLE.len(), 8)。62.22–62.29 が各 1 行で、RFC §62.30 表の行数（8 設計判断）と一致すること。各行の decision が round3_scope_rootcause::Round3Section の 62.22–62.29 に属し、consumes / produces / file_candidates / graph_node_ids が非空であることを全行で検証する。
- UT: [Error] 各行の consumes / produces / file_candidates / graph_node_ids が非空である — 空文字列・空配列の行があれば RFC 表からの逸脱として失敗。assert!(!row.consumes.is_empty()) / assert!(!row.produces.is_empty()) / assert!(!row.file_candidates.is_empty()) / assert!(!row.graph_node_ids.is_empty()) を全行に対して行い、decision の section()（例: "62.22"）を失敗メッセージに含める。
- UT: [Boundary] 表が 62.22–62.29 の全設計判断節を網羅する — 8 節が各 1 回出現し、62.21（EvolutionScopeRootcause）と 62.30（IoBoundaryReference）が表に含まれないこと（62.21 は根因定義、62.30 は参照情報の容器であり表の外側）。round3_scope_rootcause::Round3Section の section() を収集して検証する。
- UT: [Invariant] 各行の graph_node_ids が既知ノード集合に含まれる — known_round3_graph_node_ids() に存在しないノード参照は typo として失敗。62.22 は ["N0038","N0080"]、62.23 は ["N0021","N0038"]、62.24 は ["N0071"]、62.25 は ["N0045","N0083"]、62.26 は ["N0026"]、62.27 は ["N0029","N0084"]、62.28 は ["N0049","N0085"]、62.29 は ["N0031","N0074"] を使用（RFC-ROOT-GRAPH.json の references 関係から導出）。
- UT: [C136 Precondition] Round 2 I/O boundary reference exists (§62.20) — crate::architecture::io_boundary_round2::ROUND2_IO_BOUNDARY_TABLE.len() == 10 かつ ROUND2_DELETE_TARGETS.len() == 3 を検証し、§62.30 の前提として round-2 の I/O 境界参照が定義済みであることを確認する。
- UT: [C136 Postcondition] Round 3 I/O boundaries are listed for graphify / boundify partitioning — ROUND3_IO_BOUNDARY_TABLE が 8 行で定義され、ROUND3_DELETE_TARGETS が 4 件定義され、各 target.path がディスク上に実在する（std::path::Path::new(target.path).exists() を全 target に適用）。
- UT: [C136 Invariant] Every round-3 decision maps to consumes / produces / file hints — 全行の consumes / produces / file_candidates が非空で、各 Round3DeleteTarget.resolved_by が ROUND3_IO_BOUNDARY_TABLE の decision 集合に存在する。graphify/boundify が分割・Prune 判断に利用可能であることの構造的証明。
- UT: [C136 source consistency] 削除/更新対象の解決済み状態を静的検証する — src/ffi/raw_sip_module.rs が存在する（P17-2 §62.22 で新規作成）。src/ffi/callback.rs に「pjsip_module extension point」への言及があり stale な on_rx_msg 依存の記述がない。src/state/m20_native_event_conv.rs に「Some(SipEventPayload)」の記述があり「P1/P2 returns None」が不存在。src/api/event_model_payload_bus.rs に「CallResumed(CallResumedInfo)」が存在（ペイロード化済み）。src/api/m20_dtmfsent_twophase.rs に「backend accept + 500ms」契約が存在（コールバック優先記述は不存在）。

### Integration Tests

- IT: [Integration point] round3_scope_rootcause::Round3Section ↔ io_boundary_round3::ROUND3_IO_BOUNDARY_TABLE — round3_sections()（62.21–62.30 の 10 節）の各設計判断節（62.22–62.29）が表の decision に各 1 回出現し、62.21（EvolutionScopeRootcause）と 62.30（IoBoundaryReference）は表に含まれないことを検証する。
- IT: [Verification] ROUND3_DELETE_TARGETS ↔ round3_scope_rootcause::ROUND3_BREAKING_CHANGES の対応 — 4 削除/更新対象（callback.rs stale on_rx_msg コメント・m20_native_event_conv.rs stale doc・CallResumed unit variant・DtmfSent コールバック優先記述）が 2 breaking change（CallResumedPayload / SubscriptionGenerics）および round-3 実装チケット（P17-2/3/6/7）の解決節と整合することを検証。resolved_by 節と breaking change の解決節が一致する。
- IT: [Prerequisites] round-2 §62.20（io_boundary_round2::ROUND2_IO_BOUNDARY_TABLE / ROUND2_DELETE_TARGETS）が既に存在し、round-3 の既知ノード集合が §62 親（N0068）・round-1 §62.9（N0078）・round-2 §62.20（N0089）を参照できること。round-3 表が round-2 の境界情報と矛盾しない（C136 Precondition / Invariant）。
- IT: [Related tickets] 各設計判断の file_candidates が実在ソース（src/ffi/raw_sip_module.rs, src/ffi/callback.rs, src/state/m20_native_event_conv.rs, src/runtime/backend.rs, src/runtime/reactor.rs, src/api/m20_dtmfsent_twophase.rs, src/api/event_model_payload_bus.rs, src/api/eventbus_receiver.rs 等）を指すことを確認。先行チケット P17-2（62.22）〜P17-9（62.29）で生成されたファイル候補との整合を静的に検証。未生成候補（src/runtime/registr_wiring.rs）は例外として明示。

### Exceptions

- **[Exception 1]** [Item] boundify/graphify による実際のファイル Prune（削除）処理 / [Reason] 外部ツール（Node.js 製 graphify/boundify）が行う処理であり、Rust 単体テストの実行対象外（外部依存）のため単体テスト不能（cannot test）。本チケットは削除/更新対象の参照符号化のみを担当する / [Alternative verification] ROUND3_DELETE_TARGETS の path 実在チェックと、ソース整合性テスト（raw_sip_module.rs 存在・callback.rs の pjsip_module 言及・CallResumed ペイロード化・DtmfSent 契約更新）で削除/更新対象が正しく解決済みであることを静的に検証する。これは設計欠陥ではない（not a design defect）——削除実行の委譲は設計判断であり、参照符号化自体は全てテスト可能。
- **[Exception 2]** [Item] §62.30 表の file_candidates に含まれる未生成パス（src/runtime/registr_wiring.rs 等）と RFC の表記揺れパス（m20_native_event_conv.rs が RFC では src/api/ 配下と記載されるが実体は src/state/ 配下）/ [Reason] registr_wiring.rs は make 時点でディスク上に存在しない。RFC のファイル名が実ソースのパスと不一致であり、RFC 文字列のパス実在検証は不可能（not testable） / [Alternative verification] 既存パスのみを std::path::Path::exists() で検証し、未生成パスは description に「boundify が Prune 対象とすべきファイル候補」として明記して存在チェックから除外する。RFC の表記揺れは実在パス（src/state/m20_native_event_conv.rs）へ正規化して検証する。これは設計欠陥ではない（not an architectural defect）——file_candidates は実ソースの正規化パスを指す参照であり、実在検証を既存パスに限定する設計である。
- **[Exception 3]** [Item] 削除/更新対象が「既に解決済み」であること自体 / [Reason] これらは先行チケット（P17-2: raw_sip_module + callback.rs コメント更新、P17-3: m20_native_event_conv.rs の Some() 化 doc、P17-6: CallResumed ペイロード化、P17-7: DtmfSent 契約確定）で既に更新・ペイロード化済みであり、本チケットは対象を参照として再符号化するだけ（再更新はしない）ため、更新という動的挙動の再現テストは不可能（impossible to test） / [Alternative verification] ソース整合性テストで解決済み状態（callback.rs の「pjsip_module extension point」言及、src/ffi/raw_sip_module.rs の存在、m20_native_event_conv.rs の「Some(SipEventPayload)」記述、event_model_payload_bus.rs の CallResumed(CallResumedInfo)、m20_dtmfsent_twophase.rs の「backend accept + 500ms」契約）を静的アサーションで検証する。これは設計欠陥ではない（not an architectural defect）——参照符号化としての設計は全てテスト可能であり、テスト不能なのは先行更新の履歴だけである。

### Plan Test Code (concrete code)

- UT: [C136 Precondition] Round 2 I/O boundary reference exists (§62.20) — round-2 の表が 10 行・削除対象 3 件で定義済みであることを検証する。
  ```rust
  #[test]
  // @verifies C136 -- precondition: Round 2 I/O boundary reference exists (§62.20)
  fn round2_io_boundary_reference_exists() {
      use crate::architecture::io_boundary_round2::{
          ROUND2_DELETE_TARGETS, ROUND2_IO_BOUNDARY_TABLE,
      };
      assert_eq!(
          ROUND2_IO_BOUNDARY_TABLE.len(),
          10,
          "round-2 §62.20 table must have 10 rows"
      );
      assert_eq!(
          ROUND2_DELETE_TARGETS.len(),
          3,
          "round-2 §62.20 must list 3 delete targets"
      );
  }
  ```
- UT: [C136 Postcondition] Round 3 I/O boundaries are listed for graphify / boundify partitioning — 表が 8 行・削除/更新対象 4 件で、各行の consumes/produces/file_candidates/graph_node_ids が非空、各 path が実在することを検証する。
  ```rust
  #[test]
  // @verifies C136 -- postcondition: Round 3 I/O boundaries are listed
  fn round3_table_lists_eight_boundaries() {
      assert_eq!(
          ROUND3_IO_BOUNDARY_TABLE.len(),
          8,
          "§62.30 table must have 8 rows"
      );
      for row in &ROUND3_IO_BOUNDARY_TABLE {
          assert!(
              !row.consumes.is_empty(),
              "{} consumes must be non-empty",
              row.decision.section()
          );
          assert!(
              !row.produces.is_empty(),
              "{} produces must be non-empty",
              row.decision.section()
          );
          assert!(
              !row.file_candidates.is_empty(),
              "{} file_candidates must be non-empty",
              row.decision.section()
          );
          assert!(
              !row.graph_node_ids.is_empty(),
              "{} graph_node_ids must be non-empty",
              row.decision.section()
          );
      }
      assert_eq!(
          ROUND3_DELETE_TARGETS.len(),
          4,
          "§62.30 lists exactly 4 prune/update targets"
      );
      for target in &ROUND3_DELETE_TARGETS {
          assert!(
              std::path::Path::new(target.path).exists(),
              "delete target {} must exist",
              target.path
          );
      }
  }
  ```
- UT: [C136 Invariant] Every round-3 decision maps to consumes / produces / file hints — 全行の境界列が非空で、各削除対象の resolved_by が表の decision 集合に存在することを検証する。
  ```rust
  #[test]
  // @verifies C136 -- invariant: every round-3 decision maps to boundaries
  fn every_round3_decision_maps_to_boundaries() {
      let decisions: Vec<Round3Section> = ROUND3_IO_BOUNDARY_TABLE
          .iter()
          .map(|row| row.decision)
          .collect();
      for row in &ROUND3_IO_BOUNDARY_TABLE {
          assert!(
              !row.consumes.is_empty(),
              "{} consumes must be non-empty",
              row.decision.section()
          );
          assert!(
              !row.produces.is_empty(),
              "{} produces must be non-empty",
              row.decision.section()
          );
          assert!(
              !row.file_candidates.is_empty(),
              "{} file_candidates must be non-empty",
              row.decision.section()
          );
      }
      for target in &ROUND3_DELETE_TARGETS {
          assert!(
              decisions.contains(&target.resolved_by),
              "delete target {} resolved_by {} must appear in table",
              target.path,
              target.resolved_by.section()
          );
      }
  }
  ```
- UT: [C136 Boundary] 表が 62.22–62.29 の全設計判断節を網羅し、62.21 と 62.30 を含まない。
  ```rust
  #[test]
  // @verifies C136 -- boundary: table covers exactly 62.22..62.29
  fn round3_table_covers_62_22_to_62_29_only() {
      let sections: Vec<&str> = ROUND3_IO_BOUNDARY_TABLE
          .iter()
          .map(|row| row.decision.section())
          .collect();
      assert_eq!(sections.len(), 8, "table must have 8 rows");
      for n in 22..=29 {
          let sec = format!("62.{n}");
          assert_eq!(
              sections.iter().filter(|s| **s == sec).count(),
              1,
              "{sec} must appear exactly once"
          );
      }
      assert!(
          !sections.contains(&"62.21"),
          "62.21 must not be a row (root-cause container)"
      );
      assert!(
          !sections.contains(&"62.30"),
          "62.30 itself must not be a row (container)"
      );
  }
  ```
- UT: [C136 Invariant] graph_node_ids の全参照が known_round3_graph_node_ids() に含まれ、§62 親（N0068）・§62.9（N0078）・round-2（N0089）を含む。
  ```rust
  #[test]
  // @verifies C136 -- invariant: graph_node_ids all in known set
  fn round3_known_nodes_include_boundary_chain() {
      let known = known_round3_graph_node_ids();
      assert!(known.contains(&"N0068"), "§62 parent N0068 must be known");
      assert!(known.contains(&"N0078"), "§62.9 node N0078 must be known");
      assert!(known.contains(&"N0089"), "§62.20 round-2 node N0089 must be known");
      for row in &ROUND3_IO_BOUNDARY_TABLE {
          for node in row.graph_node_ids {
              assert!(
                  known.contains(node),
                  "unknown graph node {node} in section {}",
                  row.decision.section()
              );
          }
      }
  }
  ```
- UT: [C136 source consistency] 削除/更新対象の解決済み状態を静的検証する — raw_sip_module.rs 存在・callback.rs の pjsip_module 言及・m20_native_event_conv の Some() 化・CallResumed ペイロード化・DtmfSent timeout 契約。
  ```rust
  #[test]
  // @verifies C136 -- source consistency: prune/update targets resolved
  fn round3_delete_targets_source_consistency() -> Result<(), std::io::Error> {
      assert!(
          std::path::Path::new("src/ffi/raw_sip_module.rs").exists(),
          "raw_sip_module.rs must exist (P17-2 §62.22)"
      );
      let callback = std::fs::read_to_string("src/ffi/callback.rs")?;
      assert!(
          callback.contains("pjsip_module"),
          "callback.rs must reference pjsip_module approach"
      );
      let native_conv = std::fs::read_to_string("src/state/m20_native_event_conv.rs")?;
      assert!(
          native_conv.contains("Some(SipEventPayload)"),
          "m20_native_event_conv must reflect Some() behavior"
      );
      let payload_bus = std::fs::read_to_string("src/api/event_model_payload_bus.rs")?;
      assert!(
          payload_bus.contains("CallResumed(CallResumedInfo)"),
          "CallResumed must be payload-ized"
      );
      let dtmf = std::fs::read_to_string("src/api/m20_dtmfsent_twophase.rs")?;
      assert!(
          dtmf.contains("timeout"),
          "DtmfSent must document timeout contract"
      );
      Ok(())
  }
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| src/architecture/io_boundary_round3.rs: [::STUB::] marker only (22 lines) | Round3IoBoundaryRow / Round3DeleteTarget / ROUND3_IO_BOUNDARY_TABLE (8 rows) / ROUND3_DELETE_TARGETS (4) / known_round3_graph_node_ids() + 6 @verifies C136 tests | Encoded §62.30 I/O boundary table (62.22-62.29, one row each) and 4 prune/update targets as typed constants; resolved TS-001. |
| src/architecture/mod.rs: io_boundary_round3 not declared; stale Sub-modules header comment | pub mod io_boundary_round3; declared with [::TICKET::] P17-10 annotation; Sub-modules header comment updated | Registered the new module in the architecture module tree; fixed stale header comment (Boy Scout Rule). |

## Notes in Prior Implementation Rounds

- [Implementation steps] 1) Red: io_boundary_round3.rs に #[cfg(test)] テスト群を先行実装（ROUND3_IO_BOUNDARY_TABLE が 8 行・非空・62.22–62.29 網羅・既知ノード整合、ROUND3_DELETE_TARGETS が 4 件・path 実在、ソース整合性）。未実装のためコンパイル失敗/テスト失敗で赤を確認。2) Green: Round3IoBoundaryRow / Round3DeleteTarget / ROUND3_IO_BOUNDARY_TABLE（8 行）/ ROUND3_DELETE_TARGETS（4 件）/ known_round3_graph_node_ids() を実装し、mod.rs に pub mod io_boundary_round3; を追加。3) Refactor: コメントの正確性確認・テスト変数名のドメイン化。
- [Risks] RFC のファイル名表記揺れ（m20_native_event_conv.rs が実体は src/state/ 配下）により、RFC 文字列をそのまま path に使うと実在検証が失敗する。実在パスへ正規化して符号化する。registr_wiring.rs は未生成のため存在チェック対象から除外する。
- [Caveats] 実ファイルの削除は行わない（boundify の責務）。round-1/round-2 の実装ファイル（impl_integration_design.rs / io_boundary_round2.rs）は変更しない。Initial Design Artifact ヘッダは絶対に編集しない。
- [Open items] なし — 本チケットは参照符号化のみであり、ランタイム挙動の未決事項はない。
- [Future improvements] graphify/boundify が本表（ROUND3_IO_BOUNDARY_TABLE / ROUND3_DELETE_TARGETS）を参照して round-3 の分割・Prune 判断を行う。P17-11 以降のチケットが file_candidates の更新元として利用する。


Implementation summary:
- Changed files: src/architecture/io_boundary_round3.rs (add), src/architecture/mod.rs (modify)
- Key changes: ROUND3_IO_BOUNDARY_TABLE (8 rows, 62.22-62.29) with consumes/produces/file_candidates/graph_node_ids; ROUND3_DELETE_TARGETS (4 prune/update targets, all pre-resolved by P17-2/3/6/7); known_round3_graph_node_ids() covering the §61/§62 boundary chain (N0067/N0068/N0078/N0089/N0090-N0099 + row refs)
- Test results: make test all green (exit 0, 0 failures); 6 io_boundary_round3 tests; cargo check clean; rustfmt clean; clippy clean on changed files; quality check 0 issues
- Contracts: C136 covered by 6 @verifies-annotated tests (precondition/postcondition/invariant/boundary/source-consistency)
- Boy Scout: mod.rs Sub-modules header comment updated; rustfmt indentation fix in round3_scope_rootcause.rs


Review report:
- Static quality check: passed (0 issues on io_boundary_round3.rs, mod.rs)
- Translatability: no issues — known_round3_graph_node_ids / ROUND3_IO_BOUNDARY_TABLE / Round3IoBoundaryRow / Round3DeleteTarget follow round-1/round-2 naming; test variables (row/target/known/decisions/sections) are domain-meaningful
- Tests: make test exit 0 (0 failures); 6 io_boundary_round3 tests pass; cargo check clean; rustfmt clean on changed files; clippy clean
- STUB evaluation: 0 stubs remaining (TS-001 resolved; preflight classified as pendingObligation then resolved); no-excuse gate passed
- Crimes: 0 open
- Issues found and fixes applied: annotate --verify flagged round3_scope_rootcause.rs (out-of-scope rustfmt side effect); reverted via git checkout so the diff contains only P17-10-scope files; AMBIGUOUS marker in mod.rs resolved via resolve-ambiguous-markers.js (inject-at line 38)
- Contracts C136: verified by verify-red-coverage (6 @verifies) and verify-final-contracts

## P17-10 — implemented at 7 locations

### src/architecture/io_boundary_round3.rs

- Line 173
```rust
    fn round2_io_boundary_reference_exists() {
```

- Line 189
```rust
    fn round3_table_lists_eight_boundaries() {
```

- Line 234
```rust
    fn every_round3_decision_maps_to_boundaries() {
```

- Line 269
```rust
    fn round3_table_covers_62_22_to_62_29_only() {
```

- Line 296
```rust
    fn round3_known_nodes_include_boundary_chain() {
```

- Line 318
```rust
    fn round3_delete_targets_source_consistency() -> Result<(), std::io::Error> {
```

### src/architecture/mod.rs

- Line 42
```rust
// [::TICKET::] P17-1 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P17-1 --for-spec --no-implementation-order`.
```
