# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P17-3: P1/P2 FFI callback registration (§62.23)

**Ticket Key**: P17-3 · **Phase**: 17

**RFC Source**: `RFC-ROOT.md`

---

## Background

M20 変換器は Some() 化済みだが、register_callbacks が 4 コールバックを未登録のため P1/P2 イベントが発火しない（H8）。PJSIP 2.17 の pjsua_callback に全フィールド存在を確認済み（Q2）。

### Goal
register_callbacks に on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect の 4 コールバックを追加登録し、対応 NativeEvent を enqueue する。併せて m20_native_event_conv.rs の stale doc comment（P1/P2 returns None）を Some() 実態へ修正する。これにより P1/P2 イベント系列が FFI コールバックから publish される。

### Purpose
P0（Registration/Call/DTMF）は P11-11 で登録済みだが、P1（Transport/ICE）と P2（CallTsx 等）のコールバックが未登録のため、実機でトランスポート状態変化や NAT 検出などの運用観測イベントが決して発火しない。本実装は M20 変換器の Some() 化（P16-4 §62.13）で完成済みの変換経路に、FFI コールバックという入力側を接続し、P1/P2 イベントを EventBus 経由で購読可能にする。

### Motivation
round 2（P16-4 §62.13）で P1/P2 変換器が Some() 化済みだが、入力側（register_callbacks）が 8 コールバックのままで、変換器が呼ばれる機会がない（H8 が検出）。RFC N0092（§62.23）は PJSIP 2.17 の pjsua_callback に全 4 フィールドが存在することを実ヘッダで確認済み（pjsua.h L1345/L1606/L1998/L2077）。変換経路を活かすため入力側の登録を完了する。

### Constraints
- PJSIP RT スレッド制約: ハンドラは lock / 確保 / await / PJSUA 再入を禁止し、enqueue_native_event のみ許可（src/ffi/callback.rs L5-15）。
- キューはロックフリー ArrayQueue（容量 256）で、満杯時はドロップして損失カウンタを増やす（既存設計維持）。
- pjsua-native ビルド修復（H1）が前提の実機検証は本チケットのスコープ外。
- stub/native 二重ビルド整合（P11-9 パターン）: ハンドラは両ビルドで同一ソースがコンパイルされる必要がある。

## Scope

- src/ffi/callback.rs の register_callbacks に on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect を追加登録する。
- 各ハンドラを実装し、対応 NativeEvent（TransportStateChanged / CallTsxStateChanged / CallReplaced / NatDetected）を enqueue する。
- src/state/m20_native_event_conv.rs の「P1/P2 returns None」stale doc comment を Some() 実態へ修正する。
- **Scope of changes**
- [File/module path] src/ffi/bindings.rs（stub pjsua_callback に 4 フィールド + 3 struct 追加）、src/ffi/callback.rs（register_callbacks + 4 ハンドラ）、src/state/m20_native_event_conv.rs（stale doc comment）、src/build/build_script_bindgen.rs（native allowlist 追加）
- [Action] modify（pjsua_callback フィールド追加）/ add（4 ハンドラ関数 + 3 構造体）/ modify（register_callbacks 4 行追加）/ refactor（doc comment 修正）
- [What specifically changes] pjsua_callback に on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect を Option<unsafe extern "C" fn> 型で追加。stub に opaque struct pjsip_transport_state_info / pj_stun_nat_detect_result と mirror struct pjsip_transport { pub id: u32 } を追加。register_callbacks に 4 行の config.cb.<field> = Some(<handler>) を追加。4 ハンドラ（#[no_mangle] pub unsafe extern "C" fn）を実装し enqueue_native_event で対応 NativeEvent を enqueue。m20_native_event_conv.rs の L40-46 / L81 / L90 / L106-115 の stale doc comment を Some() 実態へ修正
- [Before → After] before = register_callbacks が 8 コールバックのみ設定、P1/P2 イベントが FFI 経路で一切 enqueue されない。after = 12 コールバック設定、4 ハンドラが TransportStateChanged / CallTsxStateChanged / CallReplaced / NatDetected を enqueue し、M20 変換器の Some() 経路で publish される
- [API contract change] register_callbacks のシグネチャは不変（&mut pjsua_config, ArrayQueue<NativeEvent>）。4 ハンドラが #[no_mangle] pub unsafe extern "C" fn として公開 FFI シンボルに追加（on_call_state 等と同パターン）。NativeEvent enum / SipEventPayload enum は変更なし
- [Data schema change] なし（NativeEvent の P1/P2 バリアントは P16-4 で定義済み）
- [Config/env change] なし
- [Dependency added/removed] なし（新規 crate 依存なし）
- **Out of scope**
- [Excluded item] resolve_transport_context の実トランスポート解決（pjsua_transport_get_info による transport_type / local_addr の実値化）
- [Why excluded] stub ビルドでは transport 情報が空文字列で十分（NativeEvent を運ぶだけ。m20_native_event_conv.rs L246-250）。実解決は native ビルド修復（H1）後に別チケットで対応
- [Excluded item] pjsua-native ビルド（H1）での実コールバック発火確認
- [Why excluded] pjsua-native ビルド修復（H1）が前提。デフォルトビルドでは FFI コールバックは実機から発火しない
- [Excluded item] bindgen 出力（build_script_bindgen.rs）のコンパイル検証
- [Why excluded] native ビルドが H1 で修復されるまで allowlist 型追加のみ行い、コンパイル検証は deferred
- **Affected areas**
- [Affected component] src/runtime/event_path_wiring.rs（test が register_callbacks を利用、L138-157）
- [Nature of impact] テストは config を std::mem::zeroed() で構築するため、pjsua_callback への Option フィールド追加は影響なし（zeroed Option は None）
- [Corresponding change needed] N（変更不要）
- [Affected component] src/ffi/backend_calls.rs（initialize が register_callbacks を呼ぶ、L44）
- [Nature of impact] native 専用コード（#[cfg(feature = "pjsua-native")]）。フィールド追加は zeroed 初期化に影響なし
- [Corresponding change needed] N（変更不要）
- [Affected component] src/ffi/m20_dual_client_routing.rs（N0038 実装、account_id ベースの EventBus 振り分け）
- [Nature of impact] 新規 P1/P2 イベントが既存の EventBus 経路へ流れる。dispatch は account_id ベースで既存設計のまま
- [Corresponding change needed] N（現状の dispatch で対応可能）

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0092`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0092` | architecture | rust | 62.23 P1/P2 FFI コールバック登録の完了（Q2） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.23 P1/P2 FFI コールバック登録の完了（Q2） (N0092) | → | ☆ §27 M20 Dual Client PJSIP Callback Routing (N0038) |
| references | ★ 62.23 P1/P2 FFI コールバック登録の完了（Q2） (N0092) | → | ☆ §15 M20 NativeEvent to SipEventPayload Conversion (N0021) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0092` | `src/ffi/callback.rs` |

## Investigation

## Evidence obtained from source-code investigation

### 1. src/ffi/callback.rs — register_callbacks は 8 コールバックのみ設定
- register_callbacks（L186-202）は on_incoming_call / on_reg_state / on_call_state / on_call_media_state / on_reg_started / on_call_redirected / on_dtmf_digit / on_call_transfer_status の 8 個のみ config.cb へ設定。P1/P2 の 4 コールバックは未登録。
- ハンドラの既存パターン: #[no_mangle] pub unsafe extern "C" fn が enqueue_native_event()（L86-97, 公開・ロックフリー・損失許容）で NativeEvent を enqueue。
- テスト register_callbacks_fills_all_callback_pointers（L384-396）は 8 件の .is_some() を assert。install_test_queue（L373-377）でキュー隔離。

### 2. src/ffi/bindings.rs — stub pjsua_callback に 4 フィールドが未定義
- pjsua_callback struct（L572-593）は 8 フィールドのみ。on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect が存在しない。
- pjsip_transport_state module（L549-563）は既存（IDLE=0 / CONNECTING=1 / CONNECTED=2 / DISCONNECTED=3 / SHUTDOWN=4 / DESTROYED=5, 全て u32 const）。
- pjsua_call_id = i32（L38）。pjsip_transaction（L485）と pjsip_event（L503）は opaque/minimal struct として既存。
- 未定義: pjsip_transport struct / pjsip_transport_state_info struct / pj_stun_nat_detect_result struct。

### 3. vendored pjsua.h の実シグネチャ（RFC N0092 の主張を確認）
- vendor/pjsip/pjsip/include/pjsua-lib/pjsua.h L1345: void (*on_call_tsx_state)(pjsua_call_id, pjsip_transaction *, pjsip_event *)
- L1606: void (*on_call_replaced)(pjsua_call_id old_call_id, pjsua_call_id new_call_id)
- L1998: void (*on_nat_detect)(const pj_stun_nat_detect_result *res)
- L2077: pjsip_tp_state_callback on_transport_state（typedef は sip_transport.h L1593: void(*)(pjsip_transport*, pjsip_transport_state, const pjsip_transport_state_info*)）

### 4. 重大な発見 — 実 pjsip_transport に id フィールドが存在しない
- pjsip_transport struct（sip_transport.h L837-935）の全メンバを精査した結果、id フィールドは存在しない。RFC N0092 のスニペット (*tp).id は native bindgen ビルドでコンパイル不能。
- 対応: stub に mirror struct pjsip_transport { pub id: u32 } を追加して (*tp).id as u32 をコンパイル可能にし、native 復旧（H1）時の id 解決は notes-open に記録。

### 5. src/state/m20_native_event_conv.rs — 変換器は P1/P2 全て Some() 化済み
- NativeEvent enum（L47-104）に P1/P2 バリアント全存在: TransportStateChanged{transport_id,state} / IceTransportError / CallTsxStateChanged{call_id} / CallRedirected / CallTransferStatus / CallReplaced{call_id} / NatDetected。
- convert_native_event_to_payload（L123-238）は P1/P2 全て Some(SipEventPayload) を返す（P16-4 §62.13 で Some() 化）。
- STALE doc comment: enum doc L40-46「P1/P2 variants are declared but convert_native_event_to_payload returns None」/ セクションコメント L81「P1: Transport/ICE (deferred)」・L90「P2: Supplemental (deferred)」/ 関数 doc L106-115「P1/P2 variants produce None with documented rationale」。これらを Some() 実態へ修正する。
- 既存テスト native_event_transport_state_changed_connected（L500-512）と native_event_p2_variants_return_some（L571-592）が Some 変換を assert 済み。

### 6. SipEventPayload バリアントは全て存在
- src/api/event_model_payload_bus.rs L355-359: TransportConnected/TransportDisconnected/TransportError。L384: CallTsxStateChanged。L390: CallReplaced。L392: NatDetected。

### 7. 関連呼び出し箇所（変更不要）
- src/runtime/event_path_wiring.rs L138-157: テストが register_callbacks + enqueue_native_event を利用。config は zeroed 構築のためフィールド追加の影響なし。
- src/ffi/backend_calls.rs L44: initialize が register_callbacks を呼ぶ（native 専用）。

### 8. build_script_bindgen.rs — native allowlist
- BINDGEN_ALLOWLIST_TYPES（L29-94）に pjsua_callback（L58）はあるが pjsip_transport / pjsip_transport_state / pjsip_transport_state_info / pj_stun_nat_detect_result は未登録。native ビルドで 4 コールバック型を bindgen 出力するため 4 型を追加する。

## Acceptance Criteria

- TransportStateChanged / CallTsxStateChanged / CallReplaced / NatDetected が実コールバックから publish される。
- [Happy path] register_callbacks 後、config.cb の 4 新規フィールドが全て Some。4 ハンドラを直接呼び出すと対応 NativeEvent がキューへ 1:1 で enqueue され、convert_native_event_to_payload が Some(SipEventPayload) を返す。make test が green。
- [Error case] on_transport_state が null tp を受け取っても panic せず transport_id=0 で enqueue される。キュー満杯時はドロップし損失カウンタが増加（ブロックしない）。
- [Edge case] pjsua_call_id の境界値（0 と i32::MAX）が u32 へ無劣化変換される。on_call_replaced は new_call_id を採用し old_call_id を捨てる。stale doc comment（P1/P2 returns None）が Some() 実態に修正されている。

## Invariants

各ハンドラは対応 NativeEvent を 1:1 で enqueue し、変換器の Some() 経路で publish される。

- 【Normal】register_callbacks 後、config.cb.on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect は全て Some。各ハンドラは 1 回の呼び出しで対応 NativeEvent をちょうど 1 個 enqueue する（既存 every_callback_enqueues_exactly_one_event と同じ不変条件を新規ハンドラにも適用）。
- 【Error】on_transport_state が null tp を受け取っても panic せず transport_id=0 で enqueue する。キュー満杯時は enqueue_native_event がドロップして NATIVE_EVENT_DROPPED を増加させる（RT スレッドはブロックしない — src/ffi/callback.rs L86-97）。
- 【Internal state】NATIVE_EVENT_QUEUE は install_native_event_queue で一度だけ差し替えられ、コールバックスレッドからは AcqRel publish ordering で常に有効なポインタが見える（src/ffi/callback.rs L71-80）。
- 【Boundary】pjsua_call_id (i32) は 0..=i32::MAX の全域で u32 へ無劣化変換される。on_call_replaced は new_call_id を採用し old_call_id は捨てる。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C124 — N0092→N0038

- **Precondition**: pjsua_callback exposes the four P1/P2 fields (on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect) in PJSIP 2.17. The stub mirror in src/ffi/bindings.rs declares the same fields with matching ABI types.
- **Postcondition**: register_callbacks wires all four fields into config.cb as Some, alongside the existing 8. After registration each new field is_some().
- **Invariant**: Each handler enqueues exactly one corresponding NativeEvent: on_transport_state→TransportStateChanged{transport_id,state}, on_call_tsx_state→CallTsxStateChanged{call_id}, on_call_replaced→CallReplaced{call_id:new_call_id}, on_nat_detect→NatDetected.

### C125 — N0092→N0021

- **Precondition**: M20 converter convert_native_event_to_payload returns Some for the four P1/P2 events (already Some()化 in P16-4 §62.13).
- **Postcondition**: P1/P2 events enqueued by the new callbacks convert to SipEventPayload and publish: TransportConnected/Disconnected/Error, CallTsxStateChanged, CallReplaced, NatDetected.
- **Invariant**: Converter doc comments match the Some() behavior (no stale P1/P2 returns None prose). The behavioral Some() is asserted by converter tests.

### C126 — N0092→N0038

- **Precondition**: on_transport_state may receive a null transport pointer (PJSIP abnormal path).
- **Postcondition**: A null tp yields TransportStateChanged{transport_id:0, state} without panicking; a valid tp yields the transport id read from (*tp).id.
- **Invariant**: The handler never dereferences a null transport pointer.

### C127 — N0092→N0038

- **Precondition**: pjsua_call_id is a signed i32 in the FFI ABI (bindings.rs L38).
- **Postcondition**: call_id (i32) converts to u32 without truncation across 0..=i32::MAX; on_call_replaced emits CallReplaced with new_call_id.
- **Invariant**: The i32→u32 conversion is total over the valid call-id range; PJSUA does not pass negative call ids to these callbacks.

## Boy Scout Rule

## Translatability improvement plan

### 1. m20_native_event_conv.rs — stale doc comment の修正（最重要）
- enum doc（L40-46）の「P1/P2 variants are declared but convert_native_event_to_payload returns None」を「P1/P2 variants convert to Some(SipEventPayload)（P16-4 §62.13）」へ修正。Priority levels の P1 (future) / P2 (future) 表記を (implemented) へ更新。
- セクションコメント L81「P1: Transport/ICE (deferred)」と L90「P2: Supplemental (deferred)」を既存の L185「P1 Transport/ICE (P16-4 §62.13 Some() 化)」・L219「P2 Supplemental (P16-4 §62.13 Some() 化)」の表記に統一。
- 関数 doc（L106-115）の「P0 variants produce Some. P1/P2 variants produce None」を「P0/P1/P2 全て Some。RegistrationStateChanged のみ RuntimeCommand 経由で None」へ書き換え。コメントが嘘を付かない状態にする。

### 2. callback.rs — 新規ハンドラは既存パターンと同一の翻訳可能性を維持
- 4 ハンドラは関数名を動詞句（on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect）、引数はドメイン概念（call_id, old_call_id, new_call_id, tp）で命名し、既存ハンドラ（on_call_state 等）と同型の doc comment（# Safety 含む）を付与する。
- on_call_replaced では old_call_id を _old_call_id と明示し「old は捨て、new を採用する」という意図をコードで表現する（RFC N0092 の CallReplaced{call_id:new_call_id} 仕様）。

### 3. bindings.rs — 新規 struct は既存の opaque/mirror パターンに準拠
- pjsip_transport_state_info / pj_stun_nat_detect_result は既存の pjsip_uri / pjsip_transaction と同じ _private: [u8; 0] opaque パターンで定義し、コールバックが参照しないことを doc comment で明示。
- pjsip_transport は on_transport_state が読む id パスのみを公開する minimal mirror とし、native との乖離（実 struct に id が無い）を doc comment に記録（コメントが嘘を付かないため）。

### 4. スコープ外だが触れた既存コードの改善
- src/ffi/callback.rs L182-185 の register_callbacks doc comment は「every callback slot P11-11 owns」と記述されているが、本チケットで P17-3 のスロットが追加されるため、記述を「P11-11 と P17-3 が所有する全スロット」へ更新（コメント腐敗の防止）。
- round3_scope_rootcause.rs L137 の「register_callbacks registers 8 callbacks only」は履歴記録のため変更しない（過去の round の証跡）。

## Test Plan

### Unit Tests

- UT: [Normal] register_callbacks が 12 コールバック（既存 8 + 新規 on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect）を config.cb へ Some で設定する。既存テスト register_callbacks_fills_all_callback_pointers（src/ffi/callback.rs L384-396）に 4 件の assert!(config.cb.<field>.is_some()) を追加する。C124 Precondition/Postcondition をカバー。
- UT: [Normal] on_transport_state が pjsip_transport.id から NativeEvent::TransportStateChanged { transport_id, state } を enqueue する。install_test_queue(2) でキューを差し替え、bindings::pjsip_transport { id: 7 } を構築し、on_transport_state(&mut tp, pjsip_transport_state::CONNECTED, std::ptr::null()) を直接呼び、pop したイベントが TransportStateChanged { transport_id: 7, state: pjsip_transport_state::CONNECTED } と一致することを assert する。C124 Invariant をカバー。
- UT: [Normal] on_call_tsx_state / on_call_replaced / on_nat_detect が対応 NativeEvent を enqueue する。on_call_tsx_state(3, null, null) で CallTsxStateChanged { call_id: 3 }、on_call_replaced(1, 9) で CallReplaced { call_id: 9 }（new_call_id を採用、old は捨てる）、on_nat_detect(null) で NatDetected をそれぞれ assert する。C124 Invariant をカバー。
- UT: [Error] on_transport_state が null ポインタ tp を受け取ったとき、transport_id=0 で NativeEvent::TransportStateChanged を enqueue し panic しない。on_transport_state(std::ptr::null_mut(), pjsip_transport_state::DISCONNECTED, std::ptr::null()) を呼び、pop したイベントの transport_id == 0 を assert する。既存 on_call_state の null ハンドリング（src/ffi/callback.rs L263-264）と同じ防御パターン。C126 をカバー。
- UT: [Boundary] pjsua_call_id (i32) から u32 への変換の境界値。call_id = 0 と call_id = i32::MAX (2147483647) を on_call_tsx_state に渡し、CallTsxStateChanged { call_id: 0 } / { call_id: 2147483647 } として正しく enqueue されることを assert する。on_call_replaced は old_call_id=0, new_call_id=i32::MAX で CallReplaced { call_id: 2147483647 } を assert する。C127 をカバー。
- UT: [Invariant] M20 変換器が P1/P2 4 イベント全てに Some(SipEventPayload) を返すことを維持する。既存テスト native_event_transport_state_changed_connected（src/state/m20_native_event_conv.rs L500-512）と native_event_p2_variants_return_some（L571-592）が TransportStateChanged を起点とする Some 変換と CallTsxStateChanged / CallReplaced / NatDetected の Some 変換を assert する。C125 Precondition をカバー。
- UT: [Invariant] コールバックから変換器へのチェーンで P1/P2 イベントが publish される。on_transport_state で enqueue した NativeEvent を try_pop_native_event() で取り出し、convert_native_event_to_payload(ev, None) が Some(SipEventPayload::TransportConnected(_)) を返すことを assert する。C125 Postcondition（実コールバック経路の stub 検証版）をカバー。

### Integration Tests

- IT: [Integration point] src/ffi/callback.rs（FFI コールバック bridge）と src/state/m20_native_event_conv.rs（NativeEvent → SipEventPayload 変換）の境界。コールバックは PJSIP RT スレッドから enqueue し、Reactor の drain（P16-4 §62.13）が try_pop_native_event() で取り出して convert_native_event_to_payload() へ渡す経路。
- IT: [Verification] callback.rs のテストで、4 ハンドラが enqueue した NativeEvent を try_pop_native_event() で回収し、convert_native_event_to_payload(ev, None) が Some(SipEventPayload) を返し、対応バリアント（TransportConnected / CallTsxStateChanged / CallReplaced / NatDetected）と一致することを検証する。
- IT: [Prerequisites] デフォルト stub ビルド（pjsua-native 非活性）で実行。install_test_queue / install_test_raw_sip_queue によるキュー隔離（src/ffi/callback.rs L373-377 / L692-698）。実 PJSIP 不要。
- IT: [Related tickets] P16-4（§62.13 FFI drain / raw SIP / P1/P2 Some()化）、P11-11（8 コールバック登録）、P17-2（§62.22 raw SIP 生産経路）、N0082（イベント経路の完成）、N0092（本チケットの RFC ノード）。

### Exceptions

- Exception 1 — [Item] 実 PJSIP コールバックの発火確認。 [Reason] pjsua-native ビルド修復（H1）が前提であり、デフォルトビルドでは FFI コールバックは実機から発火しないため実機検証はテスト不能（not testable）。これは設計欠陥（design defect）ではなく、外部依存（native ビルド）による制約である。 [Alternative verification] ハンドラを直接呼び出し、install_test_queue で enqueue 結果を assert する単体テストで代替検証する。
- Exception 2 — [Item] on_transport_state の transport_id を実 PJSIP から検証（(*tp).id 経路）。 [Reason] 実 pjsip_transport struct（vendor/pjsip/pjsip/include/pjsip/sip_transport.h L837-935）には id フィールドが存在せず、native ビルド（H1）修復前は実機で id 解決経路を検証できないためテスト不能（not testable）。これは設計欠陥（design defect）ではなく、外部依存（native ビルド）とヘッダ不整合に起因する。 [Alternative verification] stub の mirror struct pjsip_transport { pub id: u32 } に値を設定してハンドラ単体テストで検証する。native 復旧時の id 解決は notes-open に記録。
- Exception 3 — [Item] m20_native_event_conv.rs の stale doc comment 修正の自動検証。 [Reason] doc comment は実行時挙動ではなく prose であり、ユニットテストで直接 assert できないためテスト不能（not testable）。これは設計欠陥（design defect）ではなく、prose 検証が本質的にユニットテストの対象外であることによる。 [Alternative verification] 変換器の Some() 挙動は既存テスト（native_event_transport_state_changed_connected / native_event_p2_variants_return_some）で検証済み。doc comment とコードの一致はコードレビューで確認する。

### Plan Test Code (concrete code)

- UT: [C124 Postcondition] register_callbacks が 4 コールバックを config.cb へ Some で設定する（既存テスト register_callbacks_fills_all_callback_pointers を拡張）。
```rust
#[test]
fn register_callbacks_fills_all_callback_pointers() {
    let queue = crossbeam_queue::ArrayQueue::new(8);
    let mut config: bindings::pjsua_config = unsafe { std::mem::zeroed() };
    register_callbacks(&mut config, queue);
    // existing 8 (P11-11)
    assert!(config.cb.on_incoming_call.is_some());
    assert!(config.cb.on_reg_state.is_some());
    assert!(config.cb.on_call_state.is_some());
    assert!(config.cb.on_call_media_state.is_some());
    assert!(config.cb.on_reg_started.is_some());
    assert!(config.cb.on_call_redirected.is_some());
    assert!(config.cb.on_dtmf_digit.is_some());
    assert!(config.cb.on_call_transfer_status.is_some());
    // P17-3: P1/P2 callbacks
    assert!(config.cb.on_transport_state.is_some());
    assert!(config.cb.on_call_tsx_state.is_some());
    assert!(config.cb.on_call_replaced.is_some());
    assert!(config.cb.on_nat_detect.is_some());
}
```
- UT: [C124 Invariant / on_transport_state] 有効な pjsip_transport から transport_id を読み取り NativeEvent::TransportStateChanged を enqueue する。
```rust
#[test]
fn on_transport_state_enqueues_transport_state_changed() {
    let queue = install_test_queue(2);
    let mut tp = bindings::pjsip_transport { id: 7 };
    unsafe { on_transport_state(&mut tp, pjsip_transport_state::CONNECTED, std::ptr::null()) };
    assert_eq!(
        queue.pop(),
        Some(NativeEvent::TransportStateChanged {
            transport_id: 7,
            state: pjsip_transport_state::CONNECTED,
        })
    );
    assert_eq!(queue.pop(), None);
}
```
- UT: [C124 Invariant / tsx + replaced + nat] on_call_tsx_state / on_call_replaced / on_nat_detect が対応 NativeEvent を enqueue する。
```rust
#[test]
fn on_call_tsx_state_enqueues_call_tsx_state_changed() {
    let queue = install_test_queue(2);
    unsafe { on_call_tsx_state(3, std::ptr::null_mut(), std::ptr::null_mut()) };
    assert_eq!(queue.pop(), Some(NativeEvent::CallTsxStateChanged { call_id: 3 }));
}

#[test]
fn on_call_replaced_enqueues_new_call_id() {
    let queue = install_test_queue(2);
    unsafe { on_call_replaced(1, 9) };
    assert_eq!(queue.pop(), Some(NativeEvent::CallReplaced { call_id: 9 }));
}

#[test]
fn on_nat_detect_enqueues_nat_detected() {
    let queue = install_test_queue(2);
    unsafe { on_nat_detect(std::ptr::null()) };
    assert_eq!(queue.pop(), Some(NativeEvent::NatDetected));
}
```
- UT: [C126 Error] null tp を受け取ったとき transport_id=0 で enqueue し panic しない（null ポインタを決して dereference しない）。
```rust
#[test]
fn on_transport_state_null_transport_enqueues_id_zero() {
    let queue = install_test_queue(2);
    unsafe {
        on_transport_state(
            std::ptr::null_mut(),
            pjsip_transport_state::DISCONNECTED,
            std::ptr::null(),
        )
    };
    assert_eq!(
        queue.pop(),
        Some(NativeEvent::TransportStateChanged {
            transport_id: 0,
            state: pjsip_transport_state::DISCONNECTED,
        })
    );
}
```
- UT: [C127 Boundary] pjsua_call_id (i32) の境界値 0 と i32::MAX が u32 へ無劣化変換される。on_call_replaced は new_call_id を採用し old を捨てる。
```rust
#[test]
fn on_call_tsx_state_call_id_boundary_values() {
    let queue = install_test_queue(4);
    unsafe { on_call_tsx_state(0, std::ptr::null_mut(), std::ptr::null_mut()) };
    assert_eq!(queue.pop(), Some(NativeEvent::CallTsxStateChanged { call_id: 0 }));
    unsafe { on_call_tsx_state(i32::MAX, std::ptr::null_mut(), std::ptr::null_mut()) };
    assert_eq!(
        queue.pop(),
        Some(NativeEvent::CallTsxStateChanged {
            call_id: i32::MAX as u32,
        })
    );
}

#[test]
fn on_call_replaced_new_call_id_boundary() {
    let queue = install_test_queue(2);
    unsafe { on_call_replaced(0, i32::MAX) };
    assert_eq!(
        queue.pop(),
        Some(NativeEvent::CallReplaced { call_id: i32::MAX as u32 })
    );
}
```
- UT: [C125 Precondition] M20 変換器が P1/P2 イベントに Some(SipEventPayload) を返す（既存テストを維持）。
```rust
#[test]
fn native_event_transport_state_changed_connected() {
    let result = convert_native_event_to_payload(
        NativeEvent::TransportStateChanged {
            transport_id: 1,
            state: pjsip_transport_state::CONNECTED,
        },
        None,
    );
    assert!(matches!(result, Some(SipEventPayload::TransportConnected(_))));
}

#[test]
fn native_event_p2_variants_return_some() {
    assert!(matches!(
        convert_native_event_to_payload(NativeEvent::CallTsxStateChanged { call_id: 1 }, None),
        Some(SipEventPayload::CallTsxStateChanged)
    ));
    assert!(matches!(
        convert_native_event_to_payload(NativeEvent::CallReplaced { call_id: 1 }, None),
        Some(SipEventPayload::CallReplaced)
    ));
    assert!(matches!(
        convert_native_event_to_payload(NativeEvent::NatDetected, None),
        Some(SipEventPayload::NatDetected)
    ));
}
```
- UT: [C125 Postcondition] コールバックから変換器へのチェーンで P1/P2 イベントが publish される（on_transport_state → try_pop_native_event → convert_native_event_to_payload）。
```rust
#[test]
fn p1_event_publishes_from_callback_to_payload() {
    install_test_queue(2);
    let mut tp = bindings::pjsip_transport { id: 7 };
    unsafe { on_transport_state(&mut tp, pjsip_transport_state::CONNECTED, std::ptr::null()) };
    let ev = try_pop_native_event().expect("transport event enqueued");
    let payload = convert_native_event_to_payload(ev, None);
    assert!(matches!(payload, Some(SipEventPayload::TransportConnected(_))));
}
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| register_callbacks は 8 コールバックのみ | P1/P2 4 コールバックを追加登録し、P1/P2 イベントが実コールバックから発火 |  |
| register_callbacks は 8 コールバックのみ（P1/P2 未登録） | register_callbacks が 12 コールバックを登録（P1/P2 4 件追加） | on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect を config.cb へ追加登録（P17-3 §62.23） |
| stub pjsua_callback に P1/P2 4 フィールドが未定義 | pjsua_callback に 4 フィールド + pjsip_transport / pjsip_transport_state_info / pj_stun_nat_detect_result を追加 | P1/P2 コールバックの ABI 型を stub に追加（bindings.rs） |
| m20_native_event_conv.rs の doc comment が「P1/P2 returns None」と嘘を記述 | P1/P2 は Some() 化済みと正確に記述 | enum/fn/section doc comment を Some() 実態へ修正（L40-46/L81/L90/L106-115） |
| build_script_bindgen.rs の allowlist に P1/P2 型が未登録 | BINDGEN_ALLOWLIST_TYPES に 4 型を追加 | native ビルド用の P1/P2 コールバック型を allowlist へ追加 |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) bindings.rs stub に 4 フィールド + 3 struct（pjsip_transport mirror / pjsip_transport_state_info / pj_stun_nat_detect_result）を追加。(2) build_script_bindgen.rs の BINDGEN_ALLOWLIST_TYPES に 4 型を追加。(3) callback.rs に 4 ハンドラを実装し register_callbacks へ 4 行追加。(4) register_callbacks_fills_all_callback_pointers を 12 コールバック検証へ拡張。(5) m20_native_event_conv.rs の stale doc comment（L40-46/L81/L90/L106-115）を修正。(6) cargo fmt / make check-be / make test で検証。
- [Risks] 実 pjsip_transport struct に id フィールドが存在しない（vendor/pjsip/pjsip/include/pjsip/sip_transport.h L837-935）ため、RFC の (*tp).id は native ビルドでコンパイル不能。stub では mirror struct で対応するが、native 復旧（H1）時に id 解決を再設計する必要がある。
- [Caveats] pjsua_callback は #[repr(C)] でレイアウトが固定。stub のフィールド追加順序は vendored pjsua.h の宣言順（on_call_tsx_state → on_call_replaced → on_nat_detect → on_transport_state）に合わせ、native bindgen と整合させる。pjsip_transport_state は module consts（u32）であり、コールバックの state 引数は u32 型で統一する。
- [Open items] native ビルド復旧（H1）時に on_transport_state の transport_id を pjsua_transport 管理 API（pjsua_transport_get_id 相当）で解決する設計。bindgen enum style（rustified vs moduleconsts）の確認と state 引数型の統一。
- [Future improvements] resolve_transport_context（m20_native_event_conv.rs L247-250）で pjsua_transport_get_info により transport_type / local_addr を実値化する。on_nat_detect の pj_stun_nat_detect_result から NAT 種別・マッピング結果を SipEventPayload に載せる。
Implementation summary:
- Changed files: src/ffi/callback.rs, src/ffi/bindings.rs, src/state/m20_native_event_conv.rs, src/build/build_script_bindgen.rs
- Red: 8 新規テスト（on_transport_state / on_call_tsx_state / on_call_replaced / on_nat_detect enqueue + null tp + call_id 境界 + callback→converter チェーン）+ register_callbacks_fills_all_callback_pointers を 12 コールバック検証へ拡張。C124-C127 に @verifies 付与。
- Green: 4 ハンドラ（#[no_mangle] pub unsafe extern "C" fn）実装、register_callbacks に 4 行追加、stub に 4 フィールド + 3 struct、allowlist に 4 型、stale doc comment 修正。
- Refactor: p1_event_publishes_from_callback_to_payload の .expect() を .and_then() チェーンへ変更（unwrap/expect 排除）。cargo fmt 適用。
- Test results: make test 全 green（lib 1287 passed / 0 failed、integration 25 passed / doc-tests 含む）。警告なし。
- Quality: run-quality-checks 62 件（全て技術的に修正不能 — FFI 固有 unsafe ブロック / 既存テストの expect / doc comment の誤検知）。新規に修正可能な finding はゼロ。
Review report (P17-3):
- Static quality check: 62 issues reported by run-quality-checks.js — all technically unfixable and pre-existing/inherent: (1) 55 unsafe blocks in FFI callback/bindings code with SAFETY comments (FFI-inherent), (2) 2 .expect() in pre-existing test code (P11-11 queue test / P16-4 raw SIP test, permitted by project Rust testing rules), (3) 5 doc-comment false positives (/// lines flagged as commented-out code). Zero new fixable findings introduced.
- Translatability: no issues — new handlers use verb-phrase function names (on_transport_state etc.), domain-concept variables (call_id / new_call_id / transport_id), no single-char/generic vars, no debug output, no hardcoded magic numbers; comments explain why (SAFETY rationale, old-call discard intent).
- Contracts: C124-C127 all fulfilled and @verifies-annotated (verify-red-coverage 4/4).
- Annotations: P17-3 provenance present in all 4 changed files (callback.rs 8 / bindings.rs 2 / m20_native_event_conv.rs 2 / build_script_bindgen.rs 1). verify exit 0, AMBIGUOUS 0.
- Stubs: 1 pre-existing stub (io_boundary_round3.rs:21 P17-10) correctly deferred; no new stubs. Crimes: 0.
- Issues found and fixed: p1_event_publishes_from_callback_to_payload .expect() replaced with .and_then() chain during implementation (unwrap/expect elimination).
- no-excuse gate: pass 1 / fail 0.

## P17-3 — implemented at 14 locations

### src/build/build_script_bindgen.rs

- Line 25
```rust
pub const BINDGEN_ALLOWLIST_TYPES: &[&str] = &[
```

### src/ffi/bindings.rs

- Line 498
```rust
    pub struct pjsip_transport {
```

- Line 603
```rust
    pub struct pjsua_callback {
```

### src/ffi/callback.rs

- Line 470
```rust
    fn register_callbacks_fills_all_callback_pointers() {
```

- Line 763
```rust
    fn on_transport_state_enqueues_transport_state_changed() {
```

- Line 786
```rust
    fn on_transport_state_null_transport_enqueues_id_zero() {
```

- Line 807
```rust
    fn on_call_tsx_state_enqueues_call_tsx_state_changed() {
```

- Line 819
```rust
    fn on_call_replaced_enqueues_new_call_id() {
```

- Line 828
```rust
    fn on_nat_detect_enqueues_nat_detected() {
```

- Line 837
```rust
    fn on_call_tsx_state_call_id_boundary_values() {
```

- Line 856
```rust
    fn on_call_replaced_new_call_id_boundary() {
```

- Line 870
```rust
    fn p1_event_publishes_from_callback_to_payload() {
```

### src/state/m20_native_event_conv.rs

- Line 47
```rust
pub enum NativeEvent {
```

- Line 121
```rust
pub fn convert_native_event_to_payload(
```
