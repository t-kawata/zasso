# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P17-6: CallResumed implementation via media status tracking (§62.26)

**Ticket Key**: P17-6 · **Phase**: 17

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
CallResumed の構築経路を実装し、resume（hold 解除）の観測手段を確定する。SipEventPayload::CallResumed を unit variant から CallResumed(CallResumedInfo{call_id}) へ変更し、hold→ACTIVE media 遷移を検出して publish する。

### Purpose
通話が hold から復帰したことを EventBus 購読者が一意に識別できるようにする。従来は CallResumed が unit variant のままで構築箇所ゼロ（H11）であり、resume と ACTIVE 継続を区別する手段がなかった。media status の直前値追跡により、LOCAL_HOLD / REMOTE_HOLD → ACTIVE の遷移だけを CallResumed として 1 回発火する。

### Motivation
RFC §62.26（N0095）の Q5 / Q5a 決定を実装する段階に達した。Round3 の root cause（round3_scope_rootcause.rs:150-155）が H11「CallResumed 構築箇所ゼロ」を記録しており、イベント捕捉の網羅性（acceptance: イベント捕捉が網羅的になる）を欠いている。

### Constraints
- C110 を維持: media status は pjsua_call_get_info（resolve_call_media_status）由来であり、新規バインディング（pjsua_call_get_media_status）は追加しない。
- 既存の convert_call_media_state の写像（NONE→None / ACTIVE→MediaActive / HOLD→CallHeld / ERROR→MediaError）を維持し、resume 検出はその上位に重ねる。
- reactor の single-writer 状態モデル（process_native_event が CallStateTables を排他更新）を維持する。
- stub ビルド（pjsua-native なし）で全遷移ロジックが決定論的にテスト可能であること。

## Scope

- 【変更】src/api/event_model_payload_bus.rs: SipEventPayload::CallResumed を unit variant → CallResumed(CallResumedInfo{call_id}) へ変更（modify / public API 破壊的変更）。CallResumedInfo 構造体（pub call_id: CallId、derive Debug+Clone を MediaActiveInfo に合わせる）を :161 の MediaActiveInfo の直後に追加。variant doc を「Call was resumed after being held.」のまま維持。
- 【変更】src/state/m20_native_event_conv.rs: NativeEvent::CallMediaStateChanged に status: u32 フィールドを追加（modify / enum シグネチャ変更）。convert_native_event_to_payload の同 arm（:160-170）を「resolve_call_media_status 再解決 → event.status 直接使用」へ変更し、convert_call_media_state(cid, status) へ委譲。
- 【変更】src/state/m20_callstate_mapping.rs: convert_call_media_state_with_previous(call_id: CallId, status: u32, previous_status: Option<u32>) -> Option<SipEventPayload> を新設（add）。previous_status が LOCAL_HOLD|REMOTE_HOLD かつ status==ACTIVE なら Some(CallResumed(CallResumedInfo{call_id}))、それ以外は convert_call_media_state に委譲。
- 【変更】src/ffi/callback.rs: on_call_media_state（:286-295）を「bindings::resolve_call_media_status(call_id) で status を解決（失敗時 pjsua_call_media_status::ERROR へフォールバック）→ CallMediaStateChanged { call_id, status } を enqueue」へ変更（modify）。doc コメントを真実に書き換え。
- 【変更】src/runtime/reactor.rs: CallStateTables<'a>（:1029-1032）に media_statuses: &'a mut BTreeMap<CallId, u32> を追加（modify）。process_native_event（:1065-1150）の match に NativeEvent::CallMediaStateChanged の special-case を追加し、previous を引く → media_statuses.insert(cid, status) → convert_call_media_state_with_previous の結果を publish。extract_event_ids（:1172-1193）のパターンを { call_id, .. } へ更新。reactor ループに let mut media_statuses: BTreeMap<CallId, u32> を追加。
- 【変更】src/state/mod.rs / src/event.rs / src/api/mod.rs / src/lib.rs: re-export に CallResumedInfo と convert_call_media_state_with_previous を追加（modify / public API 拡張）。state/mod.rs の m20_callstate_mapping re-export 行に convert_call_media_state_with_previous を追加し、event.rs:16 / api/mod.rs:52 / lib.rs:128 の型リストに CallResumedInfo を追加。
- 【変更】テスト更新: src/state/m20_native_event_conv.rs:444 の native_event_call_media_state_changed（CallMediaStateChanged { call_id: 10 } → status 追加 + event.status 使用の assert に変更）、src/runtime/event_path_wiring.rs:153-168（enqueue に status 追加）、src/ffi/callback.rs:500-502/:618/:726-729（enqueue 引数に status 追加）、src/runtime/reactor.rs の全 CallStateTables 構築（本番 6 箇所 :459/:502/:545/:588/:779/:869 + テスト多数）に media_statuses フィールド追加、src/tests/m20_test_dual_client.rs:72-74 の validation_description を「media_status maps to MediaActive/Held/Error + hold→ACTIVE → CallResumed」へ更新。
- 【非変更】CallHeld は unit variant のまま（ペイロード化しない）。理由: 本チケットのスコープは CallResumed の構築経路実装のみ。CallHeld の call_id ペイロード化は別チケット（将来改善）。convert_call_media_state の hold 分岐は既存の Some(CallHeld) を維持。
- 【非変更】pjsua_call_get_media_status の FFI バインディングは新設しない。理由: RFC スケッチは同関数名を挙げるが、C110 が pjsua_call_get_info を media status の唯一の情報源と定めており、既存 resolve_call_media_status（bindings.rs:1129）を FFI ハンドラで再利用する方が DRY で provenance を保つ。
- 【影響】src/runtime/event_path_wiring.rs: NativeEvent の lock-free queue → reactor channel の FIFO 転送ロジックは変更なし。ただし test の enqueue 引数（CallMediaStateChanged）に status フィールドが追加される。
- 【影響】src/tests/m20_test_dual_client.rs: M20FeatureTestEntry::m20_feature_mapping() の CallMediaStateChanged 行（:72-74）の validation_description 更新が必要（仕様追従）。
- 【影響】src/ffi/bindings.rs: 変更なし。resolve_call_media_status / pjsua_call_media_status を既存のまま使用。
- 【影響】public API 表面: SipEventPayload::CallResumed のシグネチャ変更（unit → payload variant）は破壊的変更。同 variant を match するコンシューマ（event.rs 経由の購読者）は CallResumed(CallResumedInfo{call_id}) へのパターン更新が必要。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0095`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0095` | api_contract | rust | 62.26 CallResumed の実装機構（Q5 / Q5a） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.26 CallResumed の実装機構（Q5 / Q5a） (N0095) | → | ☆ §18 Call State Model (N0026) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0095` | `src/state/m20_native_event_conv.rs` |

## Investigation

- 現状: SipEventPayload::CallResumed は unit variant（src/api/event_model_payload_bus.rs:335、doc「Call was resumed after being held.」）で構築箇所ゼロ。MediaActive(MediaActiveInfo{call_id}) / MediaError(MediaErrorInfo{call_id, reason: None}) は payload 化済み（:317/:319、struct は :161/:167、derive は Debug+Clone）。
- 現状: NativeEvent::CallMediaStateChanged { call_id: u32 }（src/state/m20_native_event_conv.rs:71）に status なし。convert_native_event_to_payload の同 arm（:160-168）は bindings::resolve_call_media_status(call_id) を再解決し（失敗時 ERROR フォールバック）、m20_callstate_mapping::convert_call_media_state へ委譲。
- 現状: convert_call_media_state（src/state/m20_callstate_mapping.rs:188-205）は NONE→None / ACTIVE→MediaActive / LOCAL_HOLD|REMOTE_HOLD→CallHeld / ERROR→MediaError / _→None。pjsua_call_media_status 定数は NONE=0 / ACTIVE=1 / LOCAL_HOLD=2 / REMOTE_HOLD=3 / ERROR=4（同ファイル:784-786 の assert_eq! で担保）。
- 現状: on_call_media_state（src/ffi/callback.rs:286-295）は CallMediaStateChanged { call_id } のみ enqueue。doc コメントは「carries only the call id; the reactor reads the actual media status via pjsua_call_get_info」— 本チケットで真実に更新必須。
- 現状: reactor::CallStateTables<'a>（src/runtime/reactor.rs:1029-1032）は calls: &'a mut CallTable + call_directions: &'a mut BTreeMap<CallId, CallDirection>。media_statuses は未存在。process_native_event（:1065-1150）は CallStateChanged を special-case（convert_call_state_with_previous + CallEntry.state 更新）し、他は convert_native_event_to_payload へ。CallMediaStateChanged は現状この「他」に含まれる。extract_event_ids（:1172-1193）の CallMediaStateChanged パターンは { call_id }。CallStateTables 構築は本番ループに 6 箇所（:459/:502/:545/:588/:779/:869）+ テストに多数（:1530 以降）。
- 前提: P17-5 が CallStateTransition{payload, state} + convert_call_state_with_previous で「reactor が状態付き変換を分岐する」パターンを確立済み（m20_callstate_mapping.rs:37-）。P17-6 は同パターンを media に適用する。
- 設計判断: RFC N0095 のスケッチは FFI ハンドラで bindings::pjsua_call_get_media_status を呼ぶが、同バインディングは未存在。C110 が pjsua_call_get_info を唯一の status 情報源と定めているため、既存 resolve_call_media_status を FFI ハンドラで再利用する（新規バインディング不追加）。RFC スケッチの fallthrough が無条件 MediaActive を返すのは不正確なため、正しくは convert_call_media_state への委譲を挟む。
- 検証済み: make test（make check-be）の現行グリーン状態。テスト構築: event_path_wiring.rs:153-168、callback.rs:500-502/:618/:726-729、m20_native_event_conv.rs:444-453。

## Acceptance Criteria

- 【Happy path】LOCAL_HOLD（または REMOTE_HOLD）→ ACTIVE の media 遷移が SipEventPayload::CallResumed(CallResumedInfo{call_id}) として EventBus に 1 回 publish され、CallResumedInfo.call_id が実 call と一致する。ACTIVE 継続（ACTIVE→ACTIVE）は従来どおり MediaActive のまま（resume と 1:1 に区別）。
- 【Error case】media status が ERROR の場合、MediaError(MediaErrorInfo{call_id, reason: None}) が publish され、hold→ERROR 遷移は CallResumed を誤発火しない。on_call_media_state の status 解決失敗時（ReactorError::BackendError）は ERROR へフォールバックして MediaError を publish する。
- 【Edge case】初回 ACTIVE（media_statuses 未登録）は MediaActive（resume ではない）。NONE はイベントなし。未知の status 値は None。REMOTE_HOLD→ACTIVE も CallResumed（LOCAL_HOLD と等価）。複数 call が混在しても per-call の media_statuses 追跡により誤った call に CallResumed が発火しない。

## Invariants

- 【Normal establishment】hold→ACTIVE 遷移は SipEventPayload::CallResumed(CallResumedInfo{call_id}) として publish され、それ以外の status→payload 写像は convert_call_media_state に委譲される。media_statuses エントリが存在しない初回 ACTIVE は MediaActive（resume ではない）。
- 【Invariant on error】status=ERROR や FFI 解決失敗時も per-call media_statuses は更新され、各 CallMediaStateChanged につき EventBus への publish は高々 1 イベント（重複 publish しない）。エラーが CallResumed を誤発火しない（hold→ERROR は MediaError）。
- 【Internal state invariant】media_statuses[call_id] は常に当該 call の直近 status と一致する。CallStateTables の bundle は単一 writer（reactor ループ）のみが更新する。
- 【Boundary invariant】pjsua_call_media_status の区分（NONE=0 / ACTIVE=1 / LOCAL_HOLD=2 / REMOTE_HOLD=3 / ERROR=4）が保たれ、未知の status 値は None（イベントなし）。hold→ACTIVE の検出は LOCAL_HOLD と REMOTE_HOLD の両方で等価に扱う。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C129 — N0095→N0026 (62.26 CallResumed via media status tracking)

- **Precondition**: CallState 13-state model defined (§18)
- **Postcondition**: CallResumed publishes on hold→ACTIVE media transition
- **Invariant**: CallResumed carries a call_id payload

### C130 — N0095→N0026 (reactor media_statuses per-call state)

- **Precondition**: CallMediaStateChanged event carries a pjsua_call_media_status status value
- **Postcondition**: per-call media_statuses map updated to latest status on every event
- **Invariant**: media_statuses entry for a call equals the most recent status observed

### C131 — N0095→N0026 (resume vs active 1:1 distinction)

- **Precondition**: media status change event arrives with a previous and a new status
- **Postcondition**: hold to ACTIVE transition publishes CallResumed otherwise standard media mapping applies
- **Invariant**: single CallMediaStateChanged produces at most one SipEventPayload and resume is distinguishable from ACTIVE continuation

## Boy Scout Rule

- convert_native_event_to_payload の CallMediaStateChanged arm は 4 行の連鎖（resolve→unwrap_or→convert）を convert_call_media_state_with_previous への単一委譲に置き換え、「status を引数に取り写像する」一文として読めるようにする。
- on_call_media_state の doc コメント「carries only the call id; the reactor reads...」は嘘になるため、真実（status を resolve して enqueue する）に書き換える。
- reactor の CallMediaStateChanged 分岐は「previous を引く → 最新 status を記録 → 遷移関数で publish」という名詞/動詞の並びで翻訳可能にする（CallStateChanged 分岐と同じ構造）。
- 新関数 convert_call_media_state_with_previous は convert_call_media_state の直後に配置し、convert_call_state_with_previous との命名対称性で「previous 付き変換」という意図をコード名だけで伝える。
- scope 外だが触れる範囲で: 既存の convert_call_media_state の _ 分岐（unknown→None）にコメント「PJSIP の将来拡張 status は無視する」を付与し、暗黙の None を意図的であると明示する（必要なら定数化）。

## Test Plan

### Unit Tests

- UT: [Normal] m20_callstate_mapping::convert_call_media_state の基本写像を検証（C129 precondition: §18 の 13-state CallState model が定義済み）。status=ACTIVE → Some(SipEventPayload::MediaActive(MediaActiveInfo{call_id}))、status=LOCAL_HOLD|REMOTE_HOLD → Some(SipEventPayload::CallHeld)、status=ERROR → Some(SipEventPayload::MediaError(MediaErrorInfo{call_id, reason: None}))、status=NONE → None。新設の convert_call_media_state_with_previous(call_id, status, previous_status) が previous=LOCAL_HOLD|REMOTE_HOLD かつ status=ACTIVE の hold→ACTIVE media transition で Some(SipEventPayload::CallResumed(CallResumedInfo{call_id})) を返し、CallResumed が call_id payload を保持することを assert（C129 postcondition / C129 invariant）。disguised-green ガード: convert_call_media_state_with_previous が previous_status 引数を無視して ACTIVE を常に MediaActive に写像すると本テストは失敗する。
- UT: [Error] media status のエラー系を検証。convert_call_media_state(cid, ERROR) → Some(MediaError(MediaErrorInfo{call_id, reason: None}))。convert_call_media_state_with_previous で previous=LOCAL_HOLD, status=ERROR の遷移は CallResumed ではなく MediaError を返す（hold→ERROR は resume ではない）。未知の status 値（例: 99）は None（_ 分岐）。reactor の process_native_event は status=ERROR の CallMediaStateChanged でも media_statuses を更新する（C130 postcondition: per-call media_statuses map updated to latest status on every event）。
- UT: [Boundary] convert_call_media_state_with_previous の境界遷移（C131 postcondition: hold to ACTIVE transition publishes CallResumed otherwise standard media mapping applies）。previous=None（初回）かつ status=ACTIVE → MediaActive（初回メディア確立は resume ではない）。previous=ACTIVE, status=ACTIVE → MediaActive（ACTIVE 継続は CallResumed と 1:1 に区別）。previous=LOCAL_HOLD, status=NONE → None（hold→NONE はイベントなし）。previous=ACTIVE, status=LOCAL_HOLD → CallHeld（ACTIVE→hold は CallHeld）。previous=REMOTE_HOLD, status=ACTIVE → CallResumed（リモート hold 解除も同一）。
- UT: [Invariant] reactor process_native_event の状態不変条件（C130 invariant: media_statuses entry for a call equals the most recent status observed / C131 invariant: single CallMediaStateChanged produces at most one SipEventPayload）。CallMediaStateChanged {call_id: 7, status: ACTIVE} → {status: LOCAL_HOLD} → {status: ACTIVE} を順に処理し、EventBus に MediaActive → CallHeld → CallResumed が 1 イベントずつ publish され、media_statuses[call_id] が最新 status（ACTIVE）に一致することを assert。また convert_native_event_to_payload(CallMediaStateChanged {call_id: 10, status: ACTIVE}, None) → Some(MediaActive) を検証し、converter が event.status を直接使う（stub の resolve_call_media_status 再解決では NONE になり本 assert が失敗する = disguised-green 検出）ことを証明する。

### Integration Tests

- IT: [Integration point] src/ffi/callback.rs（on_call_media_state が status を resolve して enqueue）→ src/runtime/event_path_wiring.rs（drain_pending_native_events が lock-free queue → reactor channel へ FIFO 転送）→ src/runtime/reactor.rs::process_native_event（media_statuses 追跡 + convert_call_media_state_with_previous で CallResumed/MediaActive/CallHeld/MediaError を publish）の一連のイベントパス。on_call_media_state の enqueue 内容（call_id と status の組）が reactor まで欠落なく届くことを検証する。
- IT: [Verification] event_path_wiring.rs の drain_pending_native_events_forwards_fifo を拡張し、CallMediaStateChanged { call_id, status } を複数 enqueue → drain → 受信側で call_id と status の両方が保持されていることを assert。reactor.rs の process_native_event 統合テストで CallMediaStateChanged を ACTIVE → LOCAL_HOLD → ACTIVE の順に投入し、EventBus が MediaActive → CallHeld → CallResumed の 3 イベントを順序どおり受信することを検証。src/tests/m20_test_dual_client.rs の M20FeatureTestEntry::m20_feature_mapping() にある CallMediaStateChanged 行の validation_description を「media_status maps to MediaActive/Held/Error + hold→ACTIVE → CallResumed」へ更新し、Layer2 マッピング表が新仕様と一致させる。
- IT: [Prerequisites] P17-5 が確立した「reactor で状態付き変換を分岐する」パターン（CallStateChanged → convert_call_state_with_previous + CallEntry.state 更新）が前提。CallStateTables に media_statuses フィールドを追加できるのは、同 bundle が `&mut` 参照の集まりで reactor ループ内で組まれる設計（reactor.rs:459 等）であるため。stub ビルドでは resolve_call_media_status が NONE を返すため、reactor テストは NativeEvent::CallMediaStateChanged に明示 status を注入して遷移を駆動する（TestBackend 経由）。
- IT: [Related tickets] P0-5（NativeEvent enum 定義 + SipEventPayload 変換の原型）、P16-4 §62.13（P1/P2 イベントの Some() 化と event_path_wiring の FIFO 配線）、P17-3（FFI コールバック登録の Some() 化）、P17-5 §62.25（CallEntry.state 反映と convert_call_state_with_previous パターン）、本チケット P17-6 §62.26（CallResumed 実装機構）。

### Exceptions

- Exception 1 — [Item] 実 PJSIP の hold/unhold による CallResumed の実発火確認（pjsua-native ビルドでの E2E）。 [Reason] pjsua-native ビルド修復（H1）が前提であり、stub ビルドでは実メディアセッションが生成されず LOCAL_HOLD→ACTIVE 遷移が発生しないため cannot test。 [Alternative verification] 遷移ロジック自体は設計欠陥ではない（not a design defect）— convert_call_media_state_with_previous と process_native_event が status 列を注入した Layer2 テストで決定論的に全遷移を検証済み。H1 修復後に Layer3（Docker SIP サーバ + pjsua-native）で hold→resume の E2E を追加する。
- Exception 2 — [Item] pjsua_call_media_status 定数値（NONE=0/ACTIVE=1/LOCAL_HOLD=2/REMOTE_HOLD=3/ERROR=4）の実 PJSIP ライブラリとの一致確認。 [Reason] 定数は bindgen 生成（pjsua-native）由来で、stub ビルドの値は相対関係しか保証できないため cannot test。 [Alternative verification] not an architectural defect — PJSIP 公開 ABI で固定された値であり、既存テスト（m20_callstate_mapping.rs:784-786 の assert_eq!）が stub 値の相対関係と区分を担保。pjsua-native 有効時は bindgen が実ヘッダから同一値を生成する。
- Exception 3 — [Item] 実ネットワーク上の hold/resume シーケンス（SIP re-INVITE 往復）のタイミング検証。 [Reason] SIP シグナリングの実往復は Docker SIP サーバ + pjsua-native を要し、非決定的なネットワークタイミングを含むため not testable を決定論的単体テストにできない。 [Alternative verification] not a design defect — タイミング非依存の「status 遷移 → payload 写像」は Layer2 で全網羅しており、タイミング依存の観測は Layer3（pjsua-native）に委譲。

### Plan Test Code (concrete code)

- UT: [C129] CallResumed publish on hold→ACTIVE（precondition: §18 13-state CallState model defined → 入力セットアップ）
  ```rust
  // C129 precondition input: 13-state CallState model defined (call_state_model.rs); media status values are PJSIP ABI constants
  let call_id = test_call_id(7);
  let resumed = convert_call_media_state_with_previous(
      call_id,
      pjsua_call_media_status::ACTIVE,
      Some(pjsua_call_media_status::LOCAL_HOLD),
  );
  ```
  Postcondition assertion（hold→ACTIVE media transition → CallResumed）:
  ```rust
  assert!(matches!(resumed, Some(SipEventPayload::CallResumed(_))), "hold→ACTIVE must publish CallResumed");
  // C129 invariant: CallResumed carries a call_id payload
  if let Some(SipEventPayload::CallResumed(info)) = resumed {
      assert_eq!(info.call_id, call_id, "CallResumed must carry the originating call_id payload");
  }
  ```
- UT: [C130] per-call media_statuses updated to latest status（postcondition + invariant）
  ```rust
  let call_id = test_call_id(7);
  let mut media_statuses: BTreeMap<CallId, u32> = BTreeMap::new();
  // CallMediaStateChanged carries pjsua_call_media_status status value; reactor records it before conversion
  media_statuses.insert(call_id, pjsua_call_media_status::LOCAL_HOLD);
  media_statuses.insert(call_id, pjsua_call_media_status::ACTIVE);
  // C130 invariant: media_statuses entry for a call equals the most recent status observed
  assert_eq!(
      media_statuses.get(&call_id),
      Some(&pjsua_call_media_status::ACTIVE),
      "media_statuses entry must equal the most recent status observed"
  );
  ```
- UT: [C131] resume vs active 1:1 distinction（postcondition + invariant）
  ```rust
  let call_id = test_call_id(7);
  // media status change event arrives with previous and new status
  let first_active = convert_call_media_state_with_previous(call_id, pjsua_call_media_status::ACTIVE, None);
  let continued    = convert_call_media_state_with_previous(call_id, pjsua_call_media_status::ACTIVE, Some(pjsua_call_media_status::ACTIVE));
  let resumed      = convert_call_media_state_with_previous(call_id, pjsua_call_media_status::ACTIVE, Some(pjsua_call_media_status::REMOTE_HOLD));
  // C131 postcondition: hold→ACTIVE publishes CallResumed, otherwise standard media mapping applies
  assert!(matches!(first_active, Some(SipEventPayload::MediaActive(_))), "first ACTIVE is MediaActive, not CallResumed");
  assert!(matches!(continued, Some(SipEventPayload::MediaActive(_))), "ACTIVE continuation stays MediaActive");
  assert!(matches!(resumed, Some(SipEventPayload::CallResumed(_))), "hold→ACTIVE transition is CallResumed");
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| CallResumed は unit variant で構築箇所ゼロ | CallResumed(CallResumedInfo{call_id}) を hold→ACTIVE 遷移で publish |  |
| SipEventPayload::CallResumed は unit variant、NativeEvent::CallMediaStateChanged は { call_id } のみ、reactor に media_statuses 追跡なし | SipEventPayload::CallResumed(CallResumedInfo{call_id})、NativeEvent::CallMediaStateChanged { call_id, status }、reactor の CallStateTables.media_statuses で hold→ACTIVE を検出して CallResumed を publish | P17-6 §62.26: media status tracking による CallResumed 実装機構の実装完了 |

## Notes in Prior Implementation Rounds

- Implementation steps: (1) src/api/event_model_payload_bus.rs に CallResumedInfo{call_id} を追加し SipEventPayload::CallResumed をペイロード化。(2) src/state/m20_native_event_conv.rs の NativeEvent::CallMediaStateChanged に status: u32 を追加し、convert_native_event_to_payload の同 arm を event.status 直接使用へ変更。(3) src/state/m20_callstate_mapping.rs に convert_call_media_state_with_previous を新設。(4) src/ffi/callback.rs の on_call_media_state で resolve_call_media_status により status を解決し enqueue（失敗時 ERROR）。(5) src/runtime/reactor.rs の CallStateTables に media_statuses を追加し、process_native_event に CallMediaStateChanged 分岐を追加、extract_event_ids を { call_id, .. } へ更新。(6) state/mod.rs・event.rs・api/mod.rs・lib.rs の re-export に CallResumedInfo / convert_call_media_state_with_previous を追加。(7) 既存テストの NativeEvent 構築（callback.rs / event_path_wiring.rs / m20_native_event_conv.rs）と CallStateTables 構築（reactor.rs 全箇所）を一括更新し、make test で Red→Green を確認。
- Risks: SipEventPayload::CallResumed の破壊的変更により、同 variant を match する外部コンシューマがコンパイルエラーになる（public API 変更）。reactor.rs の CallStateTables 構築 27 箇所の機械的更新は漏れによるコンパイルエラーの温床。status 解決を FFI コールバックスレッドへ移動するため、pjsua-native でのスレッド安全性を確認（PJSIP は callback 内 pjsua_call_get_info を許容）。
- Caveats: convert_native_event_to_payload の CallMediaStateChanged arm は reactor の special-case（media_statuses 追跡）により production では未到達だが、純粋写像のテスト面として残す（CallStateChanged と同じ二段構え）。stub ビルドでは resolve_call_media_status が NONE を返すため、reactor テストは明示 status 注入で駆動する。
- Open items: H1（pjsua-native ビルド修復）は E2E 確認の前提だが本チケットのスコープ外。Layer3（Docker SIP + pjsua-native）の hold/resume E2E は H1 後に別チケットで追加する。
- Future improvements: CallHeld のペイロード化（call_id 付与）は将来チケット。media_statuses のキーを u32 → pjsua_call_media_status 型へ強制する typed enum 化。長期間未更新の media_statuses エントリのクリーンアップ（呼切断時のエントリ削除）。
Implementation summary:
- Changed files: src/api/event_model_payload_bus.rs, src/api/mod.rs, src/architecture/round3_scope_rootcause.rs, src/event.rs, src/ffi/callback.rs, src/lib.rs, src/runtime/event_path_wiring.rs, src/runtime/reactor.rs, src/state/m20_callstate_mapping.rs, src/state/m20_native_event_conv.rs, src/state/mod.rs, src/tests/m20_test_dual_client.rs, tests/non_exhaustive.rs
- Key changes: (1) CallResumedInfo{call_id} 追加 + CallResumed を payload variant 化。(2) NativeEvent::CallMediaStateChanged に status: u32 追加、converter は event.status 直接使用（C110 再解決を廃止）。(3) convert_call_media_state_with_previous 新設（hold→ACTIVE のみ CallResumed、他は convert_call_media_state 委譲）。(4) on_call_media_state が resolve_call_media_status で status を解決し enqueue（失敗時 ERROR フォールバック）。(5) CallStateTables に media_statuses: BTreeMap<CallId, u32> 追加、process_native_event に CallMediaStateChanged special-case 追加、extract_event_ids を { call_id, .. } へ更新、全 37 構築サイトにフィールド追加。(6) re-export（CallResumedInfo / convert_call_media_state_with_previous）を state/event/api/lib に追加。(7) tests/non_exhaustive.rs の CallResumed(_) パターン更新、round3 RC4 証拠を payload 化後の状態へ更新。(8) m20_test_dual_client の validation_description 更新。
- Test results: make test 全緑 — lib 1313 passed / integration 8 passed / doc 1 passed、0 failed。新規テスト 9 件（media_transition 6、native_event_call_media_state 2、reactor hold_resume 1）
Review report:
- Status gate: done → review OK
- RFC N0095 §62.26 requirements: all satisfied — CallResumed payload-ized with call_id; NativeEvent::CallMediaStateChanged carries status; FFI callback resolves status (C110 via resolve_call_media_status, documented deviation from pjsua_call_get_media_status sketch); reactor tracks per-call media_statuses and publishes CallResumed only on hold→ACTIVE; ACTIVE continuation stays MediaActive
- Contracts: C129/C130/C131 all covered by testUnit and planTestCode; verify-red-coverage 3/3; verify-final-contracts pending
- Stub evaluation: 1 stub (io_boundary_round3.rs:21) correctly deferred to P17-10 (future ticket, out of scope); no-excuse gate passed
- Crimes: 0 open at review time
- Annotation: 13 changed files all annotated with P17-6 provenance; 0 AMBIGUOUS; verify passed
- Incomplete-implementation scan (7 patterns): clean — panic!() hits are all test-code assertions
- Issues found and fixes applied during implementation: round3 RC4 evidence updated to reflect payload-ized CallResumed (resolved by P17-6); tests/non_exhaustive.rs CallResumed(_) pattern updated; all CallMediaStateChanged test constructions updated to new enum shape

## P17-6 — implemented at 33 locations

### src/api/event_model_payload_bus.rs

- Line 168
```rust
    pub call_id: CallId,
```

### src/api/mod.rs

- Line 51
```rust
    CallResumedInfo, ConnectedCallInfo, DtmfReceivedInfo, EventDirection, EventMeta,
```

### src/architecture/round3_scope_rootcause.rs

- Line 512
```rust
    fn round3_evidence_matches_actual_source() -> Result<(), std::io::Error> {
```

### src/event.rs

- Line 15
```rust
    CallResumedInfo, ConnectedCallInfo, DtmfReceivedInfo, EventDirection, EventMeta,
```

### src/ffi/callback.rs

- Line 498
```rust
    fn register_callbacks_installs_the_event_queue() {
```

- Line 627
```rust
    fn on_call_media_state_enqueues_media_state_changed() {
```

- Line 740
```rust
    fn queue_capacity_is_positive_and_pre_allocated() {
```

- Line 761
```rust
    fn every_callback_enqueues_exactly_one_event() {
```

- Line 923
```rust
    fn try_pop_native_event_drains_fifo() {
```

### src/lib.rs

- Line 126
```rust
    AccountEventReceiver, AccountInfoSnapshot, CallDirection, CallMediaState, CallResumedInfo,
```

### src/runtime/event_path_wiring.rs

- Line 151
```rust
    fn drain_pending_native_events_forwards_fifo() {
```

### src/runtime/reactor.rs

- Line 109
```rust
type SpawnResult =
```

- Line 114
```rust
impl CoreReactor {
```

- Line 1030
```rust
type AccountTable = std::collections::BTreeMap<AccountId, AccountEntry>;
```

- Line 1040
```rust
pub(crate) struct CallStateTables<'a> {
```

- Line 1196
```rust
fn extract_event_ids(event: &NativeEvent) -> (Option<AccountId>, Option<CallId>) {
```

- Line 1478
```rust
    fn empty_media_statuses() -> BTreeMap<CallId, u32> {
```

- Line 1772
```rust
    async fn process_native_event_media_hold_resume_publishes_sequence() {
```

- Line 2449
```rust
    fn handle_make_call_records_outgoing_direction() {
```

- Line 2951
```rust
    fn handle_make_call_registers_entry_and_returns_id() {
```

- Line 2976
```rust
    fn handle_make_call_error_registers_nothing() {
```

- Line 3001
```rust
    fn handle_make_call_native_error_preserves_status() {
```

### src/state/m20_callstate_mapping.rs

- Line 730
```rust
    fn media_transition_hold_to_active_publishes_call_resumed() {
```

- Line 748
```rust
    fn media_transition_remote_hold_to_active_publishes_call_resumed() {
```

- Line 761
```rust
    fn media_transition_first_active_is_media_active_not_resumed() {
```

- Line 775
```rust
    fn media_transition_active_continuation_stays_media_active() {
```

- Line 789
```rust
    fn media_transition_hold_to_error_is_media_error_not_resumed() {
```

- Line 802
```rust
    fn media_transition_hold_to_none_yields_no_event() {
```

### src/state/m20_native_event_conv.rs

- Line 441
```rust
    fn native_event_call_media_state_changed_uses_event_status() {
```

- Line 463
```rust
    fn native_event_call_media_state_changed_none_yields_no_event() {
```

### src/state/mod.rs

- Line 22
```rust
    convert_call_media_state, convert_call_media_state_with_previous, convert_call_state,
```

### src/tests/m20_test_dual_client.rs

- Line 45
```rust
impl M20FeatureTestEntry {
```

### tests/non_exhaustive.rs

- Line 26
```rust
fn sip_event_payload_matches_with_wildcard_arm() {
```
