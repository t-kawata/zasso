# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P17-7: DtmfSent timeout contract formalization (§62.27)

**Ticket Key**: P17-7 · **Phase**: 17

**RFC Source**: `RFC-ROOT.md`

---

## Background

【Goal】§62.27 の決定（N0096）を実装へ昇格する: DtmfSent { status: Ok(()) } の意味論を「backend 受理 + 500ms タイムアウト経過で送出完了とみなす」として正式契約に確定し、README / RFC に明記する。
【Purpose】H12（DtmfSent の契約未確定 — 実装は 500ms タイムアウト由来だが §62.15 は「コールバック優先」と記し契約が曖昧）を解消し、呼び出し側に決定論的な送出完了シグナルを提供する。
【Motivation】PJSIP に DTMF 送信完了コールバックは存在しない（vendored ヘッダ実測: pjsua_call_send_dtmf / pjsua_call_dial_dtmf は同期 pj_status_t を返すのみ、on_dtmf_digit は受信専用 src/ffi/callback.rs:312-319）。§62.15 の「コールバック優先」は成立しないため撤回する。
【Constraints】公開型 DtmfSentInfo.status の 3 値（Ok / Timeout / PjsipError）を維持（破壊的変更なし）。DEFAULT_DTMF_SENT_TIMEOUT_MS = 500 を維持。新規 PJSIP コールバック依存を追加しない。

## Scope

- [Changes] path: src/api/m20_dtmfsent_twophase.rs — DtmfSentInfo / SentDtmfError / spawn_dtmf_sent_timeout の doc comment を §62.27 契約へ形式化。
- [Changes] action: doc comment 更新（契約表明 + ticket provenance 注釈 [::TICKET::] P17-7 追加）。spawn_dtmf_sent_timeout の「future callback path」記述を撤回。
- [Changes] detail: DtmfSentInfo に契約 doc comment を追加 — Ok = backend 受理 + 500ms 経過（送出完了とみなす）、PjsipError = backend 同期エラー、Timeout = 型レベル状態。SentDtmfError の各バリアント doc comment を実態に合わせる（Timeout は「callback 不発」でなく「送出完了としての代替」でない旨）。
- [Changes] before/after: Before: §62.15 は「PJSIP 送信完了コールバック優先、未確認時は 500ms フォールバック」と記し契約が曖昧。spawn_dtmf_sent_timeout の doc comment は「Err(Timeout) remains a type-level state for the future callback path」と記す。After: タイムアウトが唯一の送出完了契約（§62.27）。doc comment から「future callback path」への言及を除去。
- [Changes] api: 公開 API 変更なし（DtmfSentInfo フィールド / SentDtmfError バリアント / DtmfMethod 不変）。RFC §62.27 のコード例表記（DtmfSentError / SipErrorKind / call_id フィールド）を実コードの SentDtmfError / EventMeta.call_id に整合。
- [Changes] schema: 該当なし（DtmfSentInfo は serde 非導出のまま）。
- [Changes] config: 該当なし（DEFAULT_DTMF_SENT_TIMEOUT_MS = 500 は変更しない。ClientConfig.sent_timeout_ms の上書きは既存のまま）。
- [Changes] dep: 該当なし（依存追加・削除なし）。
- [Non-change] item: handle_send_dtmf の実行時挙動（backend 受理 → 500ms タイマー → DtmfSent{Ok}）は変更しない。
- [Non-change] why: 当該挙動は P11-6 / P16-6 で実装・テスト済み（C030）。P17-7 は契約形式化と文書化のみ。
- [Impact] component: src/api/m20_dtmfsent_twophase.rs（doc comment）、README.md（DTMF 節 RESIDUE 解消）、RFC-ROOT.md（§62.15 撤回 / §62.27 整合）。src/runtime/reactor.rs は影響なし（挙動不変）。
- [Impact] nature: 文書化 + doc comment のみ。実行時挙動・バイナリ形状の変更なし。
- [Impact] response: 既存テスト（m20_dtmfsent_twophase / reactor handle_send_dtmf / m20_test_dual_client）が全て green のまま維持されること。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0096`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0096` | api_contract | rust | 62.27 DtmfSent 送出完了契約の確定（Q6） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.27 DtmfSent 送出完了契約の確定（Q6） (N0096) | → | ☆ 62.15 DTMF 実装整合（DtmfMethod 一元化 / method / DtmfSent） (N0084) |
| references | ★ 62.27 DtmfSent 送出完了契約の確定（Q6） (N0096) | → | ☆ §20 M20 DtmfSentInfo & Two-Phase Design (N0029) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0096` | `src/api/m20_dtmfsent_twophase.rs` |

## Investigation

実装コード調査の証拠:
1. src/api/m20_dtmfsent_twophase.rs:34-53 — DtmfSentInfo { method, digit, status: Result<(), SentDtmfError>, pjsip_status: Option<u32> } / SentDtmfError { PjsipError(u32), Timeout }。公開型は既に Ok/Timeout/PjsipError の 3 値を持つ。call_id は DtmfSentInfo に含まれず EventMeta が保持。
2. src/api/m20_dtmfsent_twophase.rs:63-113 — DEFAULT_DTMF_SENT_TIMEOUT_MS = 500。spawn_dtmf_sent_timeout は tokio::time::sleep(timeout_ms) 後に DtmfSent { status: Ok(()), pjsip_status: None } を publish。doc comment に「Err(Timeout) remains a type-level state for the future callback path」= 撤回対象の記述（偽コメント）。
3. src/runtime/reactor.rs:1394-1423 — handle_send_dtmf は backend.send_dtmf（同期受理）→ Ok 時 account_id 解決 → 桁ごとに spawn_dtmf_sent_timeout。Err 時は ? で伝播しタイマー起動なし。doc comment は「(two-phase C030 preserved, §62.15)」— §62.27 参照へ更新対象。
4. src/ffi/backend_calls.rs:256-289 — send_dtmf(native_call_id, method, digits) -> i32。Rfc4733/Info → pjsua_call_send_dtmf（param 付き）、Inband → pjsua_call_dial_dtmf。同期 pj_status_t 返却のみ。
5. src/model/dtmf_spec.rs:26-34 — DtmfMethod { Rfc4733, Info, Inband } 単一定義（§62.15 Q5 実装済み、SipInfo 改名済み）。
6. src/ffi/callback.rs:312-319 — on_dtmf_digit は受信 DTMF 専用コールバック。送信完了コールバックは存在しない。
7. README.md:330-338 — DTMF 節 RESIDUE。項目 330「SipInfo というバリアントは存在しない」誤記、項目 338「DtmfSent を『PJSIP 送信完了コールバック由来』に置き換えるか、『500ms タイムアウトで送出完了とみなす』設計を README に明記する」= P17-7 の直接作業。
8. RFC-ROOT.md:4320 — §62.15 決定文に「PJSIP の送信完了コールバックが確認できる場合はそれを優先し」= 撤回対象。
9. RFC-ROOT.md:4830-4866 — §62.27 決定文は既に「backend 受理 + 500ms タイムアウト」を契約と記すが、コード例は DtmfSentError / SipErrorKind / DtmfSentInfo{call_id} 表記で実コードと不一致。DtmfSentError はコードに存在しない。call_id は EventMeta が保持。Ok(Ok(()))/Ok(Err(Timeout))/Err(PjsipError) のネスト表記も実コードの status: Result<(), SentDtmfError> と不整合。
10. RFC-ROOT.md:4562 — RESIDUE item 6 (H12)「DtmfSent の契約未確定」→ §62.27。
11. src/error.rs:49-94 — SipErrorKind は DtmfFailed / Timeout / NativeError 等を持つ。PjsipError バリアントは存在しない（RFC の SipErrorKind::PjsipError 表記は実コードの SentDtmfError::PjsipError と対応）。
12. 既存テスト — dtmf_sent_timeout_fallback_publishes_ok（C030/C107）、dtmf_sent_timeout_not_before_deadline、aborting_timeout_handle_suppresses_publication、dtmf_sent_timeout_event_carries_identifiers、dtmf_sent_info_ok_status / _pjsip_error / _timeout、default_dtmf_timeout_is_500ms（src/api/m20_dtmfsent_twophase.rs）、handle_send_dtmf_spawns_timeout_on_backend_ok / _returns_err_without_timer_on_backend_err / _passes_unified_method_to_backend / _resolves_account_id_from_call_table（src/runtime/reactor.rs）、dtmf_method_matches_section20_three_variants（src/model/dtmf_spec.rs）が契約の大部分を既にカバー。

## Acceptance Criteria

- Happy path: README.md DTMF 節が「PJSIP に送信完了コールバックは存在しないため、500ms タイムアウトを送出完了とみなす」契約を明記する（H12 解消）。RFC-ROOT.md §62.15 の「コールバック優先」が撤回され、§62.27 のコード例が実コード（SentDtmfError 型、call_id は EventMeta）と一致する。make test が全 green。
- Error case: handle_send_dtmf が backend Err 時にタイマーを起動しない（handle_send_dtmf_returns_err_without_timer_on_backend_err が green 維持）。DtmfSent は publish されない（at-most-once）。
- Edge case: DtmfSentInfo.status の 3 値（Ok / Timeout / PjsipError）テストが green。DEFAULT_DTMF_SENT_TIMEOUT_MS == 500。タイマー abort（at-most-once 抑制）テストが green。

## Invariants

- 【正常成立】handle_send_dtmf が backend 受理（send_dtmf Ok）後、桁ごとに 500ms タイマーを起動し DtmfSent { status: Ok(()), pjsip_status: None } を publish する。DtmfSent の送出完了は 500ms タイムアウト経過のみで観測される（§62.27 唯一の契約、H12 解消）。
- 【エラー時不変】backend send_dtmf が Err を返した場合、handle_send_dtmf はエラーを伝播しタイマーは起動されない。DtmfSent イベントは一切 publish されない（at-most-once）。
- 【内部状態】DtmfSentInfo.status は Result<(), SentDtmfError> で Ok(()) / Err(SentDtmfError::Timeout) / Err(SentDtmfError::PjsipError(u32)) の 3 値を保持し、pjsip_status は PjsipError 時のみ Some。PJSIP 送信完了コールバックへの依存ゼロ。
- 【境界値】DEFAULT_DTMF_SENT_TIMEOUT_MS == 500。500ms 経過前に発火せず、500ms 経過で発火する。タイマー abort 時は publish されない（at-most-once の境界）。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C130 — N0096→N0084 (references)

- **Precondition**: DtmfMethod 一元化がラウンド 2（§62.15）で確定済み: src/model/dtmf_spec.rs に Inband/Info/Rfc4733 の単一定義が存在し（重複定義なし）、src/ffi/backend_calls.rs::send_dtmf が method を pjsua_call_send_dtmf（Rfc4733/Info）/ pjsua_call_dial_dtmf（Inband）へ反映する。
- **Postcondition**: DtmfSent 意味論が「backend 受理 + 500ms タイムアウト」として形式化される: src/runtime/reactor.rs::handle_send_dtmf が backend.send_dtmf（同期受理）後に桁ごとに spawn_dtmf_sent_timeout（500ms）を起動し DtmfSent{status: Ok(()), pjsip_status: None} を publish。backend Err 時はエラーを伝播しタイマー起動なし（DtmfSent 未 publish）。
- **Invariant**: PJSIP 送信完了コールバック非依存: DtmfSent{Ok} は spawn_dtmf_sent_timeout（tokio::time::sleep + EventBus publish）のみで生成される。on_dtmf_digit は受信専用（src/ffi/callback.rs:312-319）、pjsua_call_send_dtmf / pjsua_call_dial_dtmf は同期 pj_status_t を返すのみ。

### C131 — N0096→N0029 (references)

- **Precondition**: 二相 DtmfSent 設計（§20 N0029）がフォールバックタイマーを定義: src/api/m20_dtmfsent_twophase.rs::spawn_dtmf_sent_timeout が DEFAULT_DTMF_SENT_TIMEOUT_MS=500 で存在し、DtmfSentInfo が method / digit / status / pjsip_status を保持する。
- **Postcondition**: タイムアウト契約が唯一の文書化された完了シグナルになる: README.md DTMF 節が「PJSIP には送信完了コールバックが存在しないため 500ms タイムアウトを送出完了とみなす」を明記し、RFC-ROOT.md §62.15 の「コールバック優先」が撤回され、§62.27 のコード例が実コード（SentDtmfError 型、call_id は EventMeta）と一致する。
- **Invariant**: DtmfSentInfo.status が Ok/Timeout/PjsipError の 3 値を保持する: status: Result<(), SentDtmfError>（SentDtmfError::{PjsipError(u32), Timeout}）で、pjsip_status は PjsipError 時のみ Some。

## Boy Scout Rule

対象ファイル src/api/m20_dtmfsent_twophase.rs の翻訳可能性向上:
1. 偽コメント修正 — spawn_dtmf_sent_timeout の doc comment「Err(Timeout) remains a type-level state for the future callback path」は §62.27 確定後に偽（PJSIP に送信完了コールバック経路は存在しない）→「タイムアウトが唯一の送出完了シグナル」へ書き換え。
2. 偽コメント修正 — SentDtmfError::Timeout の doc comment「The PJSIP callback did not fire within the timeout window」も偽 →「型レベルで保持されるタイムアウト結果（publish 経路は Ok のみ）」へ修正。
3. 関数名は動詞句 — spawn_dtmf_sent_timeout（動詞句）は維持。DtmfSentTimeoutRequest（名詞）は 6 フィールドが bundle として意味を語る。
4. 変数名はドメイン概念 — target_bus / call_id_typed / account_id は既にドメイン概念。ハードコード値 500 は DEFAULT_DTMF_SENT_TIMEOUT_MS 定数として既に抽出済み。
5. エラー吞み込みなし — 既存コードは ? で伝播し、エラー吞み込みなし（維持確認）。
スコープ外の改善:
6. README.md DTMF 節の「SipInfo」誤記（RESIDUE 項目 330）を P17-7 の文書化作業で同時修正（Info へ）。
7. RFC-ROOT.md §62.15 のコード例コメント「SipInfo → Info」整合と §62.27 の実コード整合。
8. src/runtime/reactor.rs::handle_send_dtmf の doc comment「§62.15」参照を §62.27 へ更新（コメント腐敗防止）。

## Test Plan

### Unit Tests

- UT: [Normal] handle_send_dtmf が backend 受理（send_dtmf Ok）後に桁ごとに 500ms タイマーを起動し DtmfSent{Ok} を publish する（C130 postcondition）。src/runtime/reactor.rs::handle_send_dtmf_spawns_timeout_on_backend_ok が該当 — TestBackend::send_dtmf Ok → spawn_dtmf_sent_timeout 起動 → tokio::time::advance(500ms) → SipEventPayload::DtmfSent(info) の info.status.is_ok() かつ pjsip_status.is_none() を検証。
- UT: [Error] backend send_dtmf が Err を返した場合、handle_send_dtmf はエラーを伝播しタイマーを起動しない（DtmfSent は publish されない）。src/runtime/reactor.rs::handle_send_dtmf_returns_err_without_timer_on_backend_err が該当。
- UT: [Boundary] タイマーは 500ms 経過前には発火せず、500ms 経過ちょうどで発火する。src/api/m20_dtmfsent_twophase.rs::dtmf_sent_timeout_not_before_deadline（100ms 時点で Empty 検証）と dtmf_sent_timeout_fallback_publishes_ok（500ms 時点で受信検証）が該当。
- UT: [Invariant] DtmfSentInfo.status が Ok(()) / Err(SentDtmfError::Timeout) / Err(SentDtmfError::PjsipError(code)) の 3 値を保持できる（C131 invariant）。src/api/m20_dtmfsent_twophase.rs::dtmf_sent_info_ok_status / dtmf_sent_info_timeout / dtmf_sent_info_pjsip_error が該当。pjsip_status は PjsipError 時のみ Some。
- UT: [Contract C130 precondition] DtmfMethod が単一定義（Inband/Info/Rfc4733）で重複定義なし。src/model/dtmf_spec.rs::dtmf_method_matches_section20_three_variants が該当（exhaustive match がコンパイル時不変条件として Rfc2833 排除を証明）。
- UT: [Contract C130 invariant] DtmfSent{Ok} は PJSIP 送信完了コールバック非依存 — コールバック未登録のまま spawn_dtmf_sent_timeout 単体で 500ms 後に DtmfSent{status: Ok(())} が受信される。src/api/m20_dtmfsent_twophase.rs::dtmf_sent_timeout_fallback_publishes_ok が該当（callback 登録なしで発火することを契約表明として明示）。
- UT: [Contract C131 precondition] DEFAULT_DTMF_SENT_TIMEOUT_MS == 500。src/api/m20_dtmfsent_twophase.rs::default_dtmf_timeout_is_500ms が該当。

### Integration Tests

- IT: [Integration point] src/ffi/backend_calls.rs::send_dtmf — DtmfMethod 別ディスパッチ（Rfc4733/Info → pjsua_call_send_dtmf param 付き、Inband → pjsua_call_dial_dtmf）が backend 受理（同期 pj_status_t 返却）を実現する。
- IT: [Verification] 二相分離 — send_dtmf() の戻り値 Ok = コマンド受理、DtmfSent イベント = 500ms タイムアウト後の送出完了。src/tests/m20_test_dual_client.rs の「DtmfSent dual layer (return + event)」「DtmfSent timeout fallback」検証項目（M20TestLayer::Layer2）が該当。
- IT: [Prerequisites] make test（= cargo test --features test-util）で TestBackend により実行。実 PJSIP は不要（pjsua-native はビルド不能 H1、RFC §62.2）。
- IT: [Related tickets] P11-6（Inband variant + at-most-once abort 抑制）／P16-6（§62.15 DtmfMethod 一元化 + Ok publish 経路）／P17-7（§62.27 契約確定）。m20 dual-client 統合テストで担保。

### Exceptions

- Item: PJSIP 送信完了コールバックの実在観測。Reason: vendored PJSIP 2.17 に DTMF 送信完了コールバックは存在しない（テスト不能）— pjsua_call_send_dtmf / pjsua_call_dial_dtmf は同期 pj_status_t を返すのみ、on_dtmf_digit は受信専用（src/ffi/callback.rs:312-319、実ヘッダ検証済み）。これは「決定論的だが本質的にテスト不能」な設計欠陥ではなく、不在そのものが C130 invariant（コールバック非依存）の設計前提である。DtmfSent{Ok} の決定論的部分（500ms タイマー → EventBus publish）は tokio::time::pause()/advance() で完全にテスト可能。Alternative: タイマー単体テスト + TestBackend で送出完了経路を検証。
- Item: README / RFC の文書内容（§62.15 撤回、§62.27 整合）の機械的検証。Reason: 文書テキストの内容一致はユニットテストではテスト不能（assert 対象にできない）。コード内 doc comment + コードレビューで担保する。これは設計欠陥ではなく、契約の実行時挙動（500ms タイムアウト publish、status 3 値）は全てユニットテストで検証済み（not testable）。Alternative: コード内 doc comment で契約を明文化し、レビューで README/RFC との一致を確認。
- Item: 実 pjsua-native による Layer-3 DTMF 送受信の統合テスト。Reason: pjsua-native はビルド不能（H1）のためテスト不能。Docker Asterisk 統合（make test-integration）は pjsua-native ゲート（P16-10）。これは設計欠陥ではなく、外部依存（PJSIP ネイティブビルド）の不在による。Alternative: TestBackend + tokio::time で決定論的検証（m20_test_dual_client の Layer2 検証項目）。

### Plan Test Code (concrete code)

- UT: [C130 Precondition] DtmfMethod 単一定義（Inband/Info/Rfc4733 のみ、重複定義なし）— src/model/dtmf_spec.rs
```rust
#[test]
fn dtmf_method_matches_section20_three_variants() {
    fn variant(method: DtmfMethod) -> &'static str {
        match method {
            DtmfMethod::Rfc4733 => "rfc4733",
            DtmfMethod::Info => "info",
            DtmfMethod::Inband => "inband",
        }
    }
    assert_eq!(variant(DtmfMethod::Rfc4733), "rfc4733");
    assert_eq!(variant(DtmfMethod::Info), "info");
    assert_eq!(variant(DtmfMethod::Inband), "inband");
}
```
- UT: [C130 Postcondition] backend 受理 + 500ms タイムアウト経過 → DtmfSent{Ok} publish — src/runtime/reactor.rs
```rust
#[tokio::test]
async fn handle_send_dtmf_spawns_timeout_on_backend_ok() {
    tokio::time::pause();
    let bus = EventBus::new(16, None);
    let mut rx = bus.subscribe_control();
    let mut backend = TestBackend::new();
    let client_state = ClientState::default();
    let mut ctx = SendDtmfContext {
        backend: &mut backend,
        client_state: &client_state,
        event_bus: &bus,
        sent_timeout_ms: DEFAULT_DTMF_SENT_TIMEOUT_MS,
    };
    let result = handle_send_dtmf(&mut ctx, 1, DtmfMethod::Rfc4733, "5");
    assert!(result.is_ok(), "backend success must return Ok(())");
    tokio::time::advance(std::time::Duration::from_millis(500)).await;
    let ev = rx.recv().await.unwrap();
    if let SipEventPayload::DtmfSent(info) = ev.payload {
        assert!(info.status.is_ok(), "backend-accept + 500ms => Ok");
        assert_eq!(info.digit, '5');
    } else {
        panic!("expected DtmfSent, got {:?}", ev.payload);
    }
}
```
- UT: [C130 Invariant] PJSIP 送信完了コールバック非依存 — コールバック未登録でタイマー単体が DtmfSent{Ok} を生成 — src/api/m20_dtmfsent_twophase.rs
```rust
#[tokio::test]
async fn dtmf_sent_timeout_fallback_publishes_ok() -> Result<(), Box<dyn std::error::Error>> {
    tokio::time::pause();
    let bus = EventBus::new(16, None);
    let mut rx = bus.subscribe_control();
    // No PJSIP callback is registered: the 500ms timer alone must produce DtmfSent{Ok}.
    spawn_dtmf_sent_timeout(DtmfSentTimeoutRequest {
        call_id: CallId::from_u64(1)?,
        account_id: Some(AccountId::from_u64(1)?),
        method: DtmfMethod::Rfc4733,
        digit: '5',
        timeout_ms: DEFAULT_DTMF_SENT_TIMEOUT_MS,
        event_bus: bus,
    });
    tokio::time::advance(std::time::Duration::from_millis(500)).await;
    let ev = rx.recv().await?;
    if let SipEventPayload::DtmfSent(info) = ev.payload {
        assert!(info.status.is_ok(), "timer alone produces completion");
        assert!(info.pjsip_status.is_none());
    } else {
        panic!("expected DtmfSent, got {:?}", ev.payload);
    }
    Ok(())
}
```
- UT: [C131 Precondition] 二相フォールバックタイマー + DEFAULT_DTMF_SENT_TIMEOUT_MS == 500 — src/api/m20_dtmfsent_twophase.rs
```rust
#[test]
fn default_dtmf_timeout_is_500ms() {
    assert_eq!(DEFAULT_DTMF_SENT_TIMEOUT_MS, 500);
}
```
- UT: [C131 Postcondition] タイムアウトが唯一の完了シグナル — 500ms 前には発火せず、500ms 後に Ok を publish — src/api/m20_dtmfsent_twophase.rs
```rust
#[tokio::test]
async fn dtmf_sent_timeout_not_before_deadline() -> Result<(), Box<dyn std::error::Error>> {
    tokio::time::pause();
    let bus = EventBus::new(16, None);
    let mut rx = bus.subscribe_control();
    spawn_dtmf_sent_timeout(DtmfSentTimeoutRequest {
        call_id: CallId::from_u64(1)?,
        account_id: None,
        method: DtmfMethod::Info,
        digit: '#',
        timeout_ms: DEFAULT_DTMF_SENT_TIMEOUT_MS,
        event_bus: bus,
    });
    tokio::time::advance(std::time::Duration::from_millis(100)).await;
    let result = rx.try_recv();
    assert!(matches!(result, Err(tokio::sync::broadcast::error::TryRecvError::Empty)),
        "DtmfSent must not fire before the deadline, got {:?}", result);
    Ok(())
}
```
- UT: [C131 Invariant] DtmfSentInfo.status が Ok / Timeout / PjsipError の 3 値を保持（pjsip_status は PjsipError 時のみ Some）— src/api/m20_dtmfsent_twophase.rs
```rust
#[test]
fn dtmf_sent_info_three_outcomes() {
    let ok = DtmfSentInfo {
        method: DtmfMethod::Rfc4733, digit: '1',
        status: Ok(()), pjsip_status: None,
    };
    let timeout = DtmfSentInfo {
        method: DtmfMethod::Info, digit: '2',
        status: Err(SentDtmfError::Timeout), pjsip_status: None,
    };
    let pjsip = DtmfSentInfo {
        method: DtmfMethod::Inband, digit: '3',
        status: Err(SentDtmfError::PjsipError(12345)), pjsip_status: Some(12345),
    };
    assert!(ok.status.is_ok());
    assert!(matches!(timeout.status, Err(SentDtmfError::Timeout)));
    assert!(matches!(pjsip.status, Err(SentDtmfError::PjsipError(12345))));
    assert_eq!(pjsip.pjsip_status, Some(12345));
}
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| §62.15 は「コールバック優先」と記し契約が曖昧 | 500ms タイムアウトを唯一の送出完了契約として確定 |  |
| DtmfSent contract undefined: §62.15 said callback-priority; spawn_dtmf_sent_timeout doc mentioned future callback path; README had RESIDUE | §62.27 contract formalized: 500ms timeout is the sole completion signal; docs aligned with SentDtmfError type; README/RFC updated | Doc-comment contract formalization on DtmfSentInfo/SentDtmfError/DEFAULT const/spawn_dtmf_sent_timeout; handle_send_dtmf §62.27 reference; README DTMF section RESIDUE resolved; RFC §62.15 callback-priority withdrawn and §62.27 code example aligned |

## Notes in Prior Implementation Rounds

【Implementation steps】(1) src/api/m20_dtmfsent_twophase.rs の doc comment を契約形式化（DtmfSentInfo / SentDtmfError / spawn_dtmf_sent_timeout、[::TICKET::] P17-7 注釈）→ (2) RFC-ROOT.md §62.15 の「コールバック優先」撤回 + §62.27 コード例を実コードへ整合 → (3) README.md DTMF 節の RESIDUE（特に項目 338）を契約記述で置換 → (4) make test で全テスト green 確認。
【Risks】RFC §62.27 のコード例と実コードの乖離（DtmfSentError vs SentDtmfError、SipErrorKind vs SentDtmfError::PjsipError、call_id フィールド有無）を整合させる際、RFC の設計意図（Ok/Timeout/PjsipError の 3 値）を保ちながら実コードの型に合わせて記述し直す点に注意。
【Caveats】スコープ記載の「src/state/m20_dtmfsent_twophase.rs」は実在しない — DtmfSentInfo は src/api/m20_dtmfsent_twophase.rs にある（パス修正が必要）。
【Open items】Err(SentDtmfError::Timeout) は現状 publish 経路で生成されない（publish は Ok のみ）。型レベルで残る理由を doc comment に明記するか、削除するかを実装時に判断する。
【Future improvements】PJSIP 側に送信完了コールバックが将来追加された場合の追補方針（spawn_dtmf_sent_timeout の JoinHandle キャンセル）。DtmfSendMethod（§48 既定値）との統合。
Implementation summary (P17-7):
- Changed files: src/api/m20_dtmfsent_twophase.rs, src/runtime/reactor.rs, src/model/dtmf_spec.rs, README.md, RFC-ROOT.md, specs/P17-7.md, Tickets.json
- Key changes: (1) Formalized DtmfSentInfo.status §62.27 contract in doc comments (Ok = backend accept + 500ms timeout as sole completion signal; PjsipError = backend sync error; Timeout = type-level state); (2) Removed false comments (future callback path, The PJSIP callback did not fire); (3) handle_send_dtmf doc references §62.27; (4) README DTMF section RESIDUE replaced with the §62.27 contract (H12 resolved), SipInfo→Info fixed; (5) RFC-ROOT.md §62.15 callback-priority withdrawn, §62.27 code example aligned to real SentDtmfError type (call_id in EventMeta); (6) Added @verifies C130/C131 annotations to 8 contract-pinning tests; (7) Added P17-7 provenance annotations via annotate-ticket-context-by-git-diff.
- Test results: make test all green (1313 lib tests + integration suites).
- Quality check: 1 findCommentedCode false positive fixed (doc-comment wording); 19 unwrap/expect findings are pre-existing test-code patterns permitted by project rust rules (all within #[cfg(test)] modules).
Review report (P17-7):
- Crime scan: 0 crimes. Stub scan: 1 stub in src/architecture/io_boundary_round3.rs (P17-10, correctly deferred, outside this ticket scope).
- No-excuse gate: pass.
- Annotation verify: 3 source files annotated with P17-7, 0 missing, 0 ambiguous.
- Changed-line incomplete-implementation scan: none found.
- Completeness inspection: all 10 spec items verified — DtmfSentInfo/SentDtmfError/DEFAULT const/spawn_dtmf_sent_timeout doc comments formalize §62.27; handle_send_dtmf doc references §62.27; README DTMF RESIDUE resolved (H12); RFC §62.15 callback-priority withdrawn; RFC §62.27 code example aligned to real code; @verifies C130/C131 present; provenance annotations present.
- Annotation placement fix applied during review: 6 annotations repositioned to precede their definitions (were inside struct/enum/fn bodies due to 0-indexed line interpretation).
- Final contracts: verify-final-contracts 100% (fulfilled), validate-ticket-targets verifiedEmpty.
- Issues found and fixes applied: annotation placement (cosmetic) fixed.

## P17-7 — implemented at 6 locations

### src/api/m20_dtmfsent_twophase.rs

- Line 46
```rust
pub struct DtmfSentInfo {
```

- Line 64
```rust
pub enum SentDtmfError {
```

- Line 81
```rust
pub const DEFAULT_DTMF_SENT_TIMEOUT_MS: u64 = 500;
```

- Line 116
```rust
pub(crate) fn spawn_dtmf_sent_timeout(
```

### src/model/dtmf_spec.rs

- Line 46
```rust
    fn dtmf_method_matches_section20_three_variants() {
```

### src/runtime/reactor.rs

- Line 1395
```rust
pub(crate) fn handle_send_dtmf(
```
