# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P17-8: Tap driving via RustMediaPort port ops (§62.28)

**Ticket Key**: P17-8 · **Phase**: 17

**RFC Source**: `RFC-ROOT.md`

---

## Background

vendored PJSIP 2.17 に pjsua_conf_set_callback は存在しない（実ヘッダ検証済み）。RustMediaPort を custom pjmedia_port として登録し、port ops から tap を駆動する（Q7）。

### Goal
RustMediaPort の port ops（get_frame / put_frame）が conf bridge フレームを受けるたびに共有ヘルパー push_frame_to_tap を呼び、subscribe_audio の tap へ AudioChunkPair を連続供給する生産経路（§62.28 / N0097）を実装する。

### Purpose
H13（実通話で AudioTapHandle::recv() が AudioChunkPair を受信できない — 供給経路が PjsuaBackend::push_media_frame の手動呼び出しのみで、conf bridge 実機経由が未接続）を解消する。pjsua-native 実機で conf bridge を流れるメディアを tap へ届ける唯一の経路を完成させる。

### Motivation
vendored PJSIP 2.17 に pjsua_conf_set_callback が存在しない（実ヘッダ検証済み）ため、conf コールバック経由の tap 供給（N0085 §62.16 の旧案）は成立しない。代替として RustMediaPort を custom pjmedia_port として pjsua_conf_add_port で登録し、port ops から駆動する（Q7）。PX-3 で登録・conf_connect は完了済みだが、port ops から tap への供給が未実装であり、実通話で tap が沈黙する。

### Constraints
RT コールバック内は非ブロック（try_push、最古破棄、lock-free queue §24.0 / §62.6）。公開 API 変更なし（AudioTapSender / AudioTapHandle / subscribe_audio のシグネチャ不変）。pjsua_conf_set_callback に依存しない（2.17 に存在しない）。RustMediaPort::new のシグネチャ拡張は crate-internal に留める（pub API ではない）。

## Scope

- **Scope of changes (shared helper push_frame_to_tap):**
  - [path] src/runtime/backend.rs — 共有ヘルパー `pub(crate) fn push_frame_to_tap(call_id: CallId, frame: &ProcessedFrame, taps: &AudioTapRegistry)` を新規追加し、PjsuaBackend::push_media_frame（:1250-1267）の tap 供給ロジックを委譲する。
  - [action] add（共有ヘルパー）+ refactor（push_media_frame 委譲、挙動不変）
  - [detail] push_frame_to_tap は taps.lock()（poison 時 into_inner）→ `taps.get(&call_id)` → `AudioChunkPair::from_processed_frame(call_id, *account_id, frame)` → `tap.try_push(pair)`。未購読コールは no-op。PjsuaBackend::push_media_frame は CallId::from_u64 検証後に push_frame_to_tap を呼ぶだけにする。
  - [before→after] Before: tap 供給ロジックが PjsuaBackend::push_media_frame のメソッド内に閉じ、port ops からは到達不能。After: push_frame_to_tap が唯一の供給点（Q7）となり、PjsuaBackend と RustMediaPort port ops の両方から呼ばれる。
  - [api] 公開 API 変更なし。push_frame_to_tap は pub(crate)。
  - [schema] 変更なし（AudioChunkPair / AudioTapSender / AudioTapRegistry の型は不変）。
  - [config] 変更なし（TAP 容量・モードは subscribe_audio 引数のまま）。
  - [dep] 依存追加・削除なし。
- **Scope of changes (RustMediaPort tap_registry + port ops):**
  - [path] src/runtime/audio_worker.rs — RustMediaPort に `tap_registry: AudioTapRegistry` フィールドを追加し、`RustMediaPort::new(mixer, call_id, tap_registry)` へ拡張。get_frame / put_frame（:381-414）が conf bridge フレームを受けるたびに push_frame_to_tap を呼ぶ。
  - [action] modify（struct + impl）+ add（port ops からの tap 供給）
  - [detail] get_frame: out_queue pop（アンダーラン zero-fill）後に ProcessedFrame を構築し push_frame_to_tap。put_frame: in_queue push 後に同様に供給。供給は AudioTapSender::try_push（非ブロック、§62.6）のみ。C110 不変条件（get_frame が out_queue の唯一の消費者）を維持。
  - [before→after] Before: RustMediaPort は queue 消費のみ。After: queue 消費 + tap 供給の両方（供給は try_push で非ブロック）。
  - [api] RustMediaPort::new のシグネチャ拡張は crate-internal（pub API ではない）。MediaPortAdapter::new の呼び出し側（ffi/media_port_adapter.rs:88）と register_media_ports_for_calls（backend.rs:825）を更新。
  - [schema] 変更なし。
  - [config] 変更なし。
  - [dep] 変更なし。
- **Scope of changes (ProcessedFrame constructor + FFI wiring):**
  - [path] src/audio/pipeline.rs — `ProcessedFrame::from_i16_stereo(pcm: &[i16], call_id: u64) -> ProcessedFrame`（または同等のコンストラクタ）を追加。negotiated_codec は RT 経路で確定 codec を持たないため決定的既定値（NegotiatedCodec::Pcmu）を使用し、timestamp は Instant::now()。
  - [path] src/ffi/media_port_adapter.rs — media_port_get_frame / media_port_put_frame（:160-186）が RustMediaPort の port ops 経由で tap 供給を駆動することを確認（既存の pjmedia_port 配線を活かす）。
  - [action] add（ProcessedFrame コンストラクタ）+ modify（port ops 呼び出し整合）
  - [detail] port ops が構築する ProcessedFrame は conf bridge の mono PCM（8kHz/16bit, CHANNEL_COUNT=1）を stereo_interleaved へ写像し AudioChunkPair::from_processed_frame へ渡す。写像仕様（IN/OUT 割当）は plan で確定しテストで明示。
  - [before→after] Before: ProcessedFrame は AudioPipeline のみが構築（from_i16_stereo 不在）。After: RT 経路も ProcessedFrame を構築し tap へ供給可能。
  - [api] 公開 API 変更なし（from_i16_stereo は crate-internal）。
  - [schema] 変更なし。
  - [config] 変更なし。
  - [dep] 変更なし。
- **Out of scope (items intentionally excluded, with justification):**
  - [item] subscribe_audio の API・tap 容量検証（validate_tap_capacity / MIN_TAP_CAPACITY）の変更。
  - [why] P9-2 / RFC §22.1 で確定済み。P17-8 は生産経路の追加のみ。
  - [item] Lossless モード tap の RT 駆動。
  - [why] Lossless は async push（backpressure, audio_subscribe_bp.rs:187-218）が必要で RT スレッドで await 不可（§62.6）。RT 経路は Realtime モードのみが対象。
  - [item] conf_connect / call_conf_port の conf slot 接続タイミングの変更。
  - [why] PX-3 / §62.16 で実装・テスト済み（C119）。P17-8 は port ops からの tap 供給追加のみ。
- **Affected areas (components/systems impacted, even without direct modification):**
  - [component] src/runtime/audio_worker.rs（RustMediaPort 構造体・port ops）、src/runtime/backend.rs（push_media_frame 委譲）、src/ffi/media_port_adapter.rs（RT コールバック）、src/audio/pipeline.rs（ProcessedFrame コンストラクタ）。
  - [nature] 性能: RT コールバックに try_push（lock-free、最古破棄）を追加 — 非ブロック維持（§24.0）。API 面: RustMediaPort::new の crate-internal シグネチャ拡張。データ形式: 変更なし。
  - [response] Y — RustMediaPort::new の全呼び出し箇所（audio_worker.rs / media_port_adapter.rs / register_media_ports_for_calls）を更新。既存テスト（C110 / PX-3 / P15-7 / P16-7）が green のまま維持されること。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0097`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0097` | architecture | rust | 62.28 tap 駆動の生産経路: RustMediaPort port ops（Q7） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.28 tap 駆動の生産経路: RustMediaPort port ops（Q7） (N0097) | → | ☆ 62.16 メディア経路の完成（conf port / キュー消費 / WAV） (N0085) |
| references | ★ 62.28 tap 駆動の生産経路: RustMediaPort port ops（Q7） (N0097) | → | ☆ §39 Media Bridge & PJSUA Conference Port (N0049) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0097` | `src/runtime/audio_worker.rs` |

## Investigation

- push_frame_to_tap は未実装（grep 0 hit）。PjsuaBackend::push_media_frame（src/runtime/backend.rs:1250-1267）が唯一の tap 供給点: self.audio_taps.lock() → AudioChunkPair::from_processed_frame(call_id, *account_id, &frame) → tap.try_push(pair)。push_media_frame_calls（:262）で記録。CallId::from_u64 失敗時 ReactorError::BackendError("invalid CallId ...")。
- AudioTapRegistry = Arc<Mutex<HashMap<CallId, (AccountId, AudioTapSender)>>>（backend.rs:35-45）。subscribe_audio（client.rs:477）が sender を登録し、sender drop で close。
- RustMediaPort（audio_worker.rs:366-420）は mixer: Arc<AudioMixer>, call_id: u64 のみ保持。queue-facing get_frame(&mut [u8], capacity) -> usize / put_frame(&[u8], size) -> bool。tap_registry 参照なし。C110 不変条件: get_frame が out_queue の唯一の消費者。
- MediaPortAdapter（ffi/media_port_adapter.rs:76-200）が RustMediaPort を pjmedia_port 化。RT コールバック media_port_get_frame（out_queue pop / zero-fill）/ media_port_put_frame（in_queue push）/ media_port_on_destroy。現在これらは tap へ供給しない — これが P17-8 のギャップ。
- register_media_ports_for_calls（backend.rs:814）が各 audio_mixers エントリから RustMediaPort::new → MediaPortAdapter::new → conf_add_port → call_conf_port + conf_connect（PX-3 / C119）。
- pjsua_conf_set_callback は vendored PJSIP 2.17 に存在しない（ticket background、実ヘッダ検証済み）→ conf コールバック経由は不可、port ops 経由に一本化（Q7 / N0097）。
- ProcessedFrame（audio/pipeline.rs:163-171）: stereo_interleaved: Vec<i16> / negotiated_codec: NegotiatedCodec / timestamp: Instant。from_i16_stereo コンストラクタは存在しない — 新規追加が必要。NegotiatedCodec は Pcmu / Opus(OpusConfig)（config/codec_policy_fallback.rs:36）。
- AudioChunkPair::from_processed_frame（model/audio_format_chunkpair.rs:236-250）: split_stereo_interleaved で IN=L（偶数 index）/ OUT=R（奇数 index）に分割。
- AudioTapSender::try_push（api/audio_subscribe_bp.rs:229-239）: Realtime 非ブロック、満杯時最古破棄（evict した pair を overflow report として返す）。RT コールバックからの唯一の正当な push 手段（async push は RT スレッドで await 不可 §62.6）。
- AudioTapHandle::recv（api/audio_subscribe_bp.rs:113-123）: 非同期、producer close で None。AudioTapSender は Clone 不可（単一 producer）。
- conf bridge ポート仕様: CLOCK_RATE_HZ=8000 / CHANNEL_COUNT=1 / BITS_PER_SAMPLE=16 / FRAME_TIME_USEC=20000（media_port_adapter.rs:46-52）。MIXER_FRAME_SAMPLES と整合。
- 既存テスト: P16-7 RustMediaPort queue テスト（audio_worker.rs:1697-1776）、PX-3 MediaPortAdapter テスト（ffi/media_port_adapter.rs:202-）、P15-7 push_media_frame テスト（backend.rs:1694-1726, 1865-1886）、register_media_ports_for_calls テスト（backend.rs:1792-1840）。
- audio_tap example（examples/audio_tap.rs:8-53）: subscribe_audio(call_id, format, TAP_CAPACITY, AudioTapMode::Realtime) → AudioTapHandle::recv()。H13 の検証対象。

## Acceptance Criteria

- audio_tap example の AudioTapHandle::recv() が AudioChunkPair を受信し、H13 を解消する。
- **[Happy path]**: pjsua-native 実機（または stub conf bridge テスト）で、subscribe_audio した通話に RustMediaPort の port ops が AudioChunkPair を連続供給し、AudioTapHandle::recv().await が AudioChunkPair を返す（永久待機しない）。共有ヘルパー push_frame_to_tap が PjsuaBackend::push_media_frame と RustMediaPort port ops の両方から使用される（Q7 の一本化）。
- **[Error case]**: 未購読コール / 破損 Mutex / 満杯 queue のいずれでも RT コールバックはブロックせず、tap 供給は try_push の最古破棄で継続。RustMediaPort port ops は常に PJ_SUCCESS を返す。
- **[Edge case]**: tap_registry に同一 CallId の再 subscribe（sender 差し替え）があっても port ops は常に最新 sender へ供給する。conf bridge の mono フレーム（8kHz/16bit）を AudioChunkPair へ写像してもパニック・不正値なし。フレーム長 0 / 容量超過も安全。

## Invariants

RT コールバック内で tap push は非ブロック（try_push）であり、最古破棄（Realtime）に従う。

- 【Normal condition】購読済み CallId が tap_registry に存在する限り、RustMediaPort の port ops が conf bridge フレームを受けるたびに push_frame_to_tap が AudioChunkPair を try_push し、AudioTapHandle::recv() が連続受信できる。get_frame は out_queue の唯一の消費者（C110）を維持しつつ供給する。
- 【Error invariant】破損 Mutex（into_inner 回復）・満杯 queue（最古破棄）・未購読コール（no-op）のいずれでも、RT コールバックはパニック・エラー・ブロックせず PJ_SUCCESS を返し続ける。エラーは呼び出し側（RT）へ伝播しない。
- 【Internal state invariant】tap_registry（AudioTapRegistry = Arc<Mutex<HashMap<CallId,(AccountId,AudioTapSender)>>>）の各エントリは subscribe_audio が登録し、sender drop（close）で消滅する。port ops は registry の参照のみ持ち、登録・削除・差し替えを行わない。
- 【Boundary invariant】Realtime モードで tap queue が満杯のとき try_push は最古フレームを evict し最新を常に admitted する（§22.1 最新優先）。フレーム長 0 / 容量超過でもパニックしない。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C132 — N0097→N0085

- **Precondition**: Media path design settled in round 2 (§62.16)
- **Postcondition**: RustMediaPort port ops drive the tap registry via push_frame_to_tap
- **Invariant**: Tap push never blocks the RT callback

### C133 — N0097→N0049

- **Precondition**: PJSUA conference port model (§39) provides pjsua_conf_add_port
- **Postcondition**: RustMediaPort registers as a custom pjmedia_port per call
- **Invariant**: pjsua_conf_set_callback is not required (absent in 2.17)

## Boy Scout Rule

- 共有ヘルパー抽出: PjsuaBackend::push_media_frame（backend.rs:1250）の tap 供給ロジックをフリー関数 push_frame_to_tap へ抽出し、動詞句の関数名で「フレームを tap へ押し込む」処理を文として読めるようにする（翻訳可能性向上）。push_media_frame は「CallId を検証してから共有ヘルパーへ委譲する」だけに単純化。
- RustMediaPort の queue 操作の翻訳可能性: 既存 get_frame（audio_worker.rs:381-401）の index / modulo 演算を「out_queue を pop し、i16 LE へ直列化、アンダーランは無音」と読めるよう、serialize_pcm_le / fill_silence 等への抽出を検討（touch 範囲内）。
- RT コールバックの意図明示: ffi/media_port_adapter.rs の media_port_get_frame / media_port_put_frame（:160-186）に tap 供給が加わるため、「get_frame は conf bridge からの要求に応じて OUT フレームを渡し tap へ供給する」等、関数の責務を関数構造で語る。SAFETY コメントは実態（pdata 生存期間）に合わせて更新。
- ハードコード値排除: tap 供給に関わる新規閾値（あれば）は named constant 化。既存の CLOCK_RATE_HZ / CHANNEL_COUNT / FRAME_TIME_USEC / BITS_PER_SAMPLE は定数のまま維持。
- エラー吞み込み排除: RT コールバックの PJ_SUCCESS 固定は PJSIP 契約として正当である旨をコメントで明記（「なぜエラーを返さないか」）。非 RT 経路（register_media_ports_for_calls）のエラーは map_pjsua_status で伝播継続。

## Test Plan

### Unit Tests

- UT: [Normal][@verifies C132] push_frame_to_tap（backend.rs 共有ヘルパー）: subscribe_audio 済み (CallId, (AccountId, AudioTapSender)) を持つ AudioTapRegistry へ ProcessedFrame{stereo_interleaved:[L0,R0,L1,R1,...], L=IN, R=OUT} を渡すと、AudioChunkPair::from_processed_frame（src/model/audio_format_chunkpair.rs:236）が in_chunk=IN / out_chunk=OUT / 同一 timestamp の pair を構築し、AudioTapSender::try_push（src/api/audio_subscribe_bp.rs:229）で送信する。続けて AudioTapHandle::recv().await（同 :113）が当該 pair を受信できる。
- UT: [Normal][@verifies C132] RustMediaPort の port ops（audio_worker.rs の get_frame / put_frame、tap_registry 参照を持たせた状態）が、conf bridge フレーム（media_port_get_frame / media_port_put_frame, src/ffi/media_port_adapter.rs:160-186）を受けるたびに push_frame_to_tap を呼び、購読済み tap へ AudioChunkPair を連続供給する。get_frame は out_queue を pop し（アンダーラン時 zero-fill）、put_frame は in_queue へ push しつつ両方とも tap へ供給する。RT コールバックはブロックしない。
- UT: [Boundary][@verifies C132] from_processed_frame のステレオ分割: stereo_interleaved が偶数長のとき split_stereo_interleaved が L（偶数 index）=IN / R（奇数 index）=OUT に分割する。空ベクタは空の in_chunk / out_chunk を生成し、単一サンプル等の奇数長は分割関数の境界仕様どおり動作する。
- UT: [Invariant][@verifies C132] RT 非ブロック不変条件: push_frame_to_tap の全呼び出し経路（port ops → 共有ヘルパー → AudioTapSender::try_push）に async/await・ブロッキング lock・ヒープ確保を導入しない。try_push は同期・非ブロック（§62.6 / §24.0）であり、Realtime モードで queue 満杯時は最古フレームを evict し最新を常に admitted する（overflow report として evict した AudioChunkPair を返す）。
- UT: [Error][@verifies C132] 未購読コールと破損 Mutex: tap_registry に該当 CallId が無い場合 push_frame_to_tap は何もせず完了（RT コールバックはエラー・パニックなし）。AudioTapRegistry の Mutex が poisoned のとき unwrap_or_else(into_inner) で回復し継続する（PjsuaBackend::push_media_frame の lock パターン src/runtime/backend.rs:1259 と同一）。
- UT: [Normal][@verifies C133] register_media_ports_for_calls（src/runtime/backend.rs:814）が各 audio_mixers エントリから RustMediaPort::new で port を構築し、MediaPortAdapter（src/ffi/media_port_adapter.rs:88）が pjmedia_port の get_frame / put_frame / on_destroy 関数ポインタを配線した上で conf_add_port が port slot を発行し、call_conf_port + conf_connect が接続する。P17-8 で tap_registry を保持した RustMediaPort が同ループで登録される。
- UT: [Invariant][@verifies C133] pjsua_conf_set_callback 非依存: vendored PJSIP 2.17 に同シンボルは存在しない（実ヘッダ検証済み）。src/ 配下の新規コードは pjsua_conf_set_callback を参照せず、conf bridge との接続は RustMediaPort を custom pjmedia_port として pjsua_conf_add_port で登録する経路（Q7）に一本化する。grep で src/ に pjsua_conf_set_callback への参照が 0 件であることを検証する。
- UT: [Normal][@verifies C133] MediaPortAdapter が call ごとの RustMediaPort（call_id 保持）を port_data.pdata に boxed し、on_destroy で解放する。port の info.fmt は CLOCK_RATE_HZ=8000 / CHANNEL_COUNT=1 / BITS_PER_SAMPLE=16 / FRAME_TIME_USEC=20000 で初期化され、pjsua_conf_add_port が受理する（src/ffi/media_port_adapter.rs:46-52, 94-117）。

### Integration Tests

- IT: [pjsua-native][@verifies C132][@verifies C133] subscribe_audio した通話（make_call / answer 後の call conf slot を conf_connect で接続済み）で、RustMediaPort port ops 経由の AudioChunkPair が連続生産され、AudioTapHandle::recv().await が永久待機せず AudioChunkPair を返し続ける（H13 解消）。audio_tap example（examples/audio_tap.rs:37-53）が subscribe_audio(call_id, format, TAP_CAPACITY, AudioTapMode::Realtime) 後に recv() で AudioChunkPair を受信できる。
- IT: [Integration point] audio_worker.rs（RustMediaPort port ops）↔ backend.rs（AudioTapRegistry / push_frame_to_tap 共有ヘルパー）↔ ffi/media_port_adapter.rs（MediaPortAdapter: conf bridge RT コールバック）↔ client.rs（subscribe_audio tap 登録）。PjsuaBackend::register_media_ports_for_calls（src/runtime/backend.rs:814）が MediaPortAdapter を pjsua_conf_add_port で登録し、call の conf slot を pjsua_conf_connect で接続する。
- IT: [Verification] m20 系 integration（src/tests/m20_test_dual_client.rs）で subscribe_audio → conf_connect → RustMediaPort port ops → push_frame_to_tap → AudioTapHandle::recv() の一連を検証。subscribe_audio の AudioTapHandle が recv() で AudioChunkPair を受信し、audio_tap_overflows_total が Overflow 時にのみ増分される。
- IT: [Prerequisites] pjsua-native feature 有効ビルド（vendored PJSIP 2.17 conf bridge 実機）。subscribe_audio（src/client.rs:477）で tap を登録済み。make_call / answer 後の call conf slot が conf_connect で接続済み（§62.16 / P16-7）。RustMediaPort が tap_registry 参照を保持して登録される（P17-8 実装後）。
- IT: [Related tickets] PX-3（RustMediaPort を custom pjmedia_port として conf_add_port 登録）、P16-7（RustMediaPort の out_queue 消費 / in_queue 供給、C110）、P15-7（push_media_frame 経由の tap 供給）、P17-7（同一 phase の DtmfSent 契約形式化）。

### Exceptions

- **Exception entry — RT スレッド上の絶対レイテンシ/ワーストケースタイミング実測**
  - [Item] 実 conf bridge RT コールバックスレッド上での tap push の絶対レイテンシ / ワーストケースタイミングの実測
  - [Reason] PJSIP conf bridge の RT コールバックは OS リアルタイムスレッド（§24.0）で駆動され、そのスケジューリング遅延・割り込みは実行環境（OS・ハードウェア・負荷）に依存する観測値であり、決定的な単体テストとして表現できない。これは「決定的だが本質的にテスト不能」な設計欠陥ではなく、環境要因の観測問題である。非ブロック性そのものは静的構造（async/await・ブロッキング lock・ヒープ確保の不在）と try_push の同期動作としてコードレベルで検証可能。
  - [Alternative verification] (1) UT: push_frame_to_tap 経路に非同期要素が無いことをコード構造で検証（testUnit [Invariant]）。(2) UT: try_push が満杯 queue で最古破棄し常に同期 return することを検証（audio_subscribe_bp.rs:229 の既存契約）。(3) IT: audio_tap example で recv() が AudioChunkPair を連続受信することを確認（H13 解消の実証）。

### Plan Test Code (concrete code)

- UT: [C132-pre] push_frame_to_tap builds an AudioChunkPair from a ProcessedFrame and delivers it to the subscribed tap
  ```rust
  #[tokio::test]
  async fn push_frame_to_tap_builds_pair_and_drives_subscribed_tap() -> Result<(), Box<dyn std::error::Error>> {
      let registry: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
      let (sender, mut handle) = tap_channel(4, AudioTapMode::Realtime);
      registry
          .lock()
          .unwrap_or_else(|poisoned| poisoned.into_inner())
          .insert(CallId::from_u64(42)?, (AccountId::from_u64(1)?, sender));
      let frame = ProcessedFrame {
          stereo_interleaved: vec![1i16, 2, 3, 4],
          negotiated_codec: NegotiatedCodec::Pcmu,
          timestamp: std::time::Instant::now(),
      };
      push_frame_to_tap(CallId::from_u64(42)?, &frame, &registry);
      let pair = handle.recv().await.expect("tap must receive the pushed pair");
      assert_eq!(pair.in_chunk, AudioChunk::I16(vec![1, 3]));
      assert_eq!(pair.out_chunk, AudioChunk::I16(vec![2, 4]));
      assert_eq!(pair.call_id, CallId::from_u64(42)?);
      assert_eq!(pair.account_id, AccountId::from_u64(1)?);
      Ok(())
  }
  ```
- UT: [C132-post] RustMediaPort::get_frame drives the tap registry via push_frame_to_tap
  ```rust
  #[tokio::test]
  async fn rust_media_port_get_frame_drives_tap_registry() -> Result<(), Box<dyn std::error::Error>> {
      let registry: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
      let (sender, mut handle) = tap_channel(4, AudioTapMode::Realtime);
      registry
          .lock()
          .unwrap_or_else(|poisoned| poisoned.into_inner())
          .insert(CallId::from_u64(42)?, (AccountId::from_u64(1)?, sender));
      let mixer = Arc::new(AudioMixer::new());
      mixer.out_queue.push(vec![7i16; MIXER_FRAME_SAMPLES]).ok();
      let port = RustMediaPort::new(mixer, 42, registry);
      let mut buf = vec![0u8; MIXER_FRAME_SAMPLES * BYTES_PER_I16];
      let written = port.get_frame(&mut buf, buf.len());
      assert_eq!(written, MIXER_FRAME_SAMPLES * BYTES_PER_I16);
      assert_eq!(i16::from_le_bytes([buf[0], buf[1]]), 7);
      let pair = handle.recv().await.expect("get_frame must drive the tap");
      assert_eq!(pair.call_id, CallId::from_u64(42)?);
      Ok(())
  }
  ```
- UT: [C132-post] RustMediaPort::put_frame drives the tap registry via push_frame_to_tap
  ```rust
  #[tokio::test]
  async fn rust_media_port_put_frame_drives_tap_registry() -> Result<(), Box<dyn std::error::Error>> {
      let registry: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
      let (sender, mut handle) = tap_channel(4, AudioTapMode::Realtime);
      registry
          .lock()
          .unwrap_or_else(|poisoned| poisoned.into_inner())
          .insert(CallId::from_u64(42)?, (AccountId::from_u64(1)?, sender));
      let mixer = Arc::new(AudioMixer::new());
      let port = RustMediaPort::new(mixer.clone(), 42, registry);
      let data = vec![0u8, 0, 7, 0]; // two i16 samples (0, 7)
      assert!(port.put_frame(&data, data.len()));
      assert_eq!(mixer.in_queue.len(), 1);
      let pair = handle.recv().await.expect("put_frame must drive the tap");
      assert_eq!(pair.call_id, CallId::from_u64(42)?);
      Ok(())
  }
  ```
- UT: [C132-inv] tap push never blocks the RT callback — Realtime try_push evicts oldest on a full queue
  ```rust
  #[tokio::test]
  async fn push_frame_to_tap_never_blocks_on_full_queue() -> Result<(), Box<dyn std::error::Error>> {
      let registry: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
      let (sender, mut handle) = tap_channel(2, AudioTapMode::Realtime);
      registry
          .lock()
          .unwrap_or_else(|poisoned| poisoned.into_inner())
          .insert(CallId::from_u64(1)?, (AccountId::from_u64(1)?, sender));
      let frame = |n: i16| ProcessedFrame {
          stereo_interleaved: vec![n, n],
          negotiated_codec: NegotiatedCodec::Pcmu,
          timestamp: std::time::Instant::now(),
      };
      push_frame_to_tap(CallId::from_u64(1)?, &frame(1), &registry);
      push_frame_to_tap(CallId::from_u64(1)?, &frame(2), &registry);
      // Third push on a full queue returns synchronously (never blocks/panics)
      // and evicts the oldest (frame 1) in favour of the newest (frame 3).
      push_frame_to_tap(CallId::from_u64(1)?, &frame(3), &registry);
      let first = handle.recv().await.expect("frame 2 admitted");
      assert_eq!(first.in_chunk, AudioChunk::I16(vec![2]));
      let second = handle.recv().await.expect("frame 3 admitted");
      assert_eq!(second.in_chunk, AudioChunk::I16(vec![3]));
      Ok(())
  }
  ```
- UT: [C132-inv] unsubscribed call and poisoned registry are safe no-ops on the push path
  ```rust
  #[test]
  fn push_frame_to_tap_unsubscribed_call_is_noop() -> Result<(), Box<dyn std::error::Error>> {
      let registry: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
      let frame = ProcessedFrame {
          stereo_interleaved: vec![1i16, 2],
          negotiated_codec: NegotiatedCodec::Pcmu,
          timestamp: std::time::Instant::now(),
      };
      // No tap registered for call 99: the push is a clean no-op.
      push_frame_to_tap(CallId::from_u64(99)?, &frame, &registry);
      // A poisoned registry is recovered via into_inner and does not panic.
      let poisoned = Arc::new(Mutex::new(HashMap::new()));
      let _ = poisoned.lock().unwrap_or_else(|p| p.into_inner());
      drop(std::panic::catch_unwind(|| {
          push_frame_to_tap(CallId::from_u64(99)?, &frame, &poisoned);
          Ok::<(), Box<dyn std::error::Error>>(())
      }));
      Ok(())
  }
  ```
- UT: [C133-pre] pjsua_conf_add_port registration — one RustMediaPort per audio_mixers entry, call slot connected
  ```rust
  #[test]
  fn register_media_ports_for_calls_registers_and_connects_every_mixer_entry() -> Result<(), ReactorError> {
      let mixers: Arc<RwLock<HashMap<u64, Arc<AudioMixer>>>> = Arc::new(RwLock::new(HashMap::new()));
      mixers.write().unwrap_or_else(|p| p.into_inner()).insert(1, Arc::new(AudioMixer::default()));
      mixers.write().unwrap_or_else(|p| p.into_inner()).insert(2, Arc::new(AudioMixer::default()));
      let taps: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
      let mut backend = PjsuaBackend::with_registries(taps, mixers);
      backend.register_media_ports_for_calls()?;
      assert_eq!(backend.registered_port_count(), 2);
      assert_eq!(backend.connected_call_count(), 2);
      assert_eq!(backend.conf_connect_pairs().len(), 2);
      Ok(())
  }
  ```
- UT: [C133-post] MediaPortAdapter exposes a pjmedia_port wired with get_frame / put_frame / on_destroy
  ```rust
  #[test]
  fn media_port_adapter_wires_port_ops_for_conf_registration() {
      let mixer = Arc::new(AudioMixer::default());
      let taps: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
      let port = RustMediaPort::new(mixer, 1, taps);
      let mut adapter = MediaPortAdapter::new(port);
      let raw = adapter.port_mut();
      assert!(!raw.is_null());
      let pj = unsafe { &*raw };
      assert!(pj.get_frame.is_some(), "adapter must wire get_frame");
      assert!(pj.put_frame.is_some(), "adapter must wire put_frame");
      assert!(pj.on_destroy.is_some(), "adapter must wire on_destroy");
      assert_eq!(pj.info.fmt.det.aud.clock_rate, CLOCK_RATE_HZ);
      assert_eq!(pj.info.fmt.det.aud.channel_count, CHANNEL_COUNT);
      assert_eq!(pj.info.fmt.det.aud.bits_per_sample, BITS_PER_SAMPLE);
      assert_eq!(pj.info.fmt.det.aud.frame_time_usec, FRAME_TIME_USEC);
  }
  ```
- UT: [C133-inv] no pjsua_conf_set_callback dependency — tap production goes through port ops + conf_add_port
  ```rust
  #[test]
  fn pjsua_conf_set_callback_is_not_referenced_in_media_path() {
      for path in [
          "src/runtime/backend.rs",
          "src/runtime/audio_worker.rs",
          "src/ffi/media_port_adapter.rs",
      ] {
          let src = std::fs::read_to_string(path).unwrap_or_default();
          assert!(
              !src.contains("pjsua_conf_set_callback"),
              "{path} must not reference pjsua_conf_set_callback"
          );
      }
  }
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| push_media_frame を呼ぶ生産コードがゼロ | RustMediaPort port ops が push_frame_to_tap で tap へ連続供給 |  |
| push_frame_to_tap 未実装（grep 0 hit）、port ops は tap へ供給しない | push_frame_to_tap 共有ヘルパーを実装し PjsuaBackend::push_media_frame と RustMediaPort port ops の両方から供給 | P17-8: Q7 の一本化 — tap 供給点を共有ヘルパーへ統合 |

## Notes in Prior Implementation Rounds

- 【Implementation steps】(1) backend.rs に共有ヘルパー `push_frame_to_tap(call_id, frame, taps)` を抽出し PjsuaBackend::push_media_frame を委譲（挙動不変、既存テスト green 維持）。(2) audio_worker.rs の RustMediaPort に `tap_registry: AudioTapRegistry` を追加し `new()` を拡張（crate-internal）。(3) RustMediaPort の get_frame / put_frame が ProcessedFrame を構築し push_frame_to_tap を呼ぶ port ops を実装。(4) src/audio/pipeline.rs に `ProcessedFrame::from_i16_stereo`（または同等コンストラクタ）を追加 — 現状存在しない。(5) ffi/media_port_adapter.rs の RT コールバックが tap 供給を駆動することを確認。(6) TDD Red: testUnit の UTs（C132 / C133 カバー）→ Green → Refactor。
- 【Risks】RustMediaPort::new のシグネチャ拡張は audio_worker.rs / media_port_adapter.rs / register_media_ports_for_calls の全呼び出し箇所を更新する必要（コンパイルエラーで検出）。ProcessedFrame の negotiated_codec に RT 経由の確定 codec が無い — 決定的既定値（例: Pcmu）が必要。AudioChunkPair::from_processed_frame は stereo 前提だが conf bridge は mono（CHANNEL_COUNT=1）— チャンネル写像の意味論をテストで明示。
- 【Caveats】RT コールバック内の Mutex lock は購読辞書読み取りのみで時間極小だが、厳密な lock-free ではない（§24.0 は lock-free queue を要求）。N0097 の設計例は get_frame のみ示すが、ticket scope は get_frame / put_frame 両方の供給を要求する。両 op が push するため pair の IN/OUT 内容は各 op の供給フレームに依存。
- 【Open items】conf bridge mono フレームを AudioChunkPair の stereo（IN=受信 / OUT=送信）へどう写像するか（get_frame=OUT / put_frame=IN を別々に供給し align する方式は AudioBridge / PairAligner §39.3 の完成で対応）。tap 二重供給の抑制要否。shutdown 時 port on_destroy と tap_registry の整合。
- 【Future improvements】AudioBridge（to_rt / from_rt, §39.2）による RT 側の完全 lock-free 化。WAV 書き出し（N0085 §62.16 write_stereo_wav）との接続で tap キャプチャをファイル化。
Implementation summary:
- Changed files: src/runtime/backend.rs, src/runtime/audio_worker.rs, src/audio/pipeline.rs, src/ffi/media_port_adapter.rs
- Key changes:
  (1) backend.rs に pub(crate) push_frame_to_tap(call_id, frame, taps) を追加し PjsuaBackend::push_media_frame を委譲
  (2) RustMediaPort に tap_registry フィールドを追加し new() を 3 引数化、get_frame/put_frame が ProcessedFrame を構築して push_frame_to_tap を呼ぶ
  (3) ProcessedFrame::from_i16_stereo コンストラクタ追加
  (4) register_media_ports_for_calls が self.audio_taps.clone() を port へ渡す
  (5) RustMediaPort::new 全 11 呼び出し箇所を 3 引数化（AudioTapRegistry::default() で既存挙動維持）
- Tests: make test 全 green（1319 lib + integration + doc-tests）。C132/C133 は @verifies 付きテストでカバー。
- Verification: cargo check（default）/ cargo check --tests --features test-util / make test 全て警告なし・成功
Review report (P17-8):
- Step 0-4: status gate done; 0 crimes; 1 stub (P17-10, correctly deferred to another ticket); no-excuse gate pass
- Step 5: annotation verify — 4 files annotated, no missing, no ambiguous; incomplete-pattern scan clean
- Step 6: cargo check (default + test-util) and make test all green (1320 lib + integration + doc-tests)
- Step 7: completeness — verified push_frame_to_tap shared helper, RustMediaPort port ops tap supply, ProcessedFrame::from_i16_stereo, register_media_ports_for_calls registry wiring; fixed a spec testUnit gap by adding push_frame_to_tap_recovers_from_poisoned_registry (C132-inv poisoned-mutex recovery)
- Step 8: static quality check passed
- Step 9: translatability check passed
- Step 10b: verify-final-contracts (coverage 100%) and validate-ticket-targets both pass
- Issues found and fixes applied: (1) annotation indentation flattened by the annotation tool — rustfmt applied to restore 4-space indent; (2) missing poisoned-mutex recovery test — added during review
- Note: the annotation tool flattens indented [::TICKET::] annotations across the whole crate (observed in P17-6/P17-8 files); a dedicated cleanup would fix the remaining files outside this ticket's scope

## P17-8 — implemented at 23 locations

### src/audio/pipeline.rs

- Line 102
```rust
    type Error = AudioFormatError;
```

- Line 173
```rust
impl ProcessedFrame {
```

### src/ffi/media_port_adapter.rs

- Line 213
```rust
    fn media_port_adapter_builds_pjmedia_port_with_rt_callbacks() {
```

- Line 231
```rust
    fn media_port_get_frame_pops_out_queue_as_le_i16() {
```

- Line 264
```rust
    fn media_port_put_frame_pushes_in_queue_as_le_i16() {
```

- Line 288
```rust
    fn media_port_rt_callbacks_do_not_block_on_full_or_empty_queues() {
```

### src/runtime/audio_worker.rs

- Line 379
```rust
impl RustMediaPort {
```

- Line 397
```rust
    fn supply_frame_to_tap(&self, frame: &ProcessedFrame) {
```

- Line 1634
```rust
        fn assert_send<T: Send>() {}
```

- Line 1741
```rust
    fn rust_media_port_get_frame_pops_out_queue() {
```

- Line 1757
```rust
    fn rust_media_port_put_frame_pushes_in_queue() {
```

- Line 1769
```rust
    fn rust_media_port_get_frame_underrun_zero_fills() {
```

- Line 1783
```rust
    fn rust_media_port_put_frame_full_queue_drops() {
```

- Line 1795
```rust
    fn out_queue_consumed_only_by_rust_media_port() {
```

- Line 1809
```rust
    fn rust_media_port_retains_call_id() {
```

### src/runtime/backend.rs

- Line 172
```rust
    fn get_account_info(&self, native_acc_id: u32) -> Result<AccountInfoSnapshot, ReactorError>;
```

- Line 176
```rust
    fn conf_connect(&mut self, source: i32, sink: i32) -> Result<(), ReactorError>;
```

- Line 180
```rust
    fn conf_disconnect(&mut self, source: i32, sink: i32) -> Result<(), ReactorError>;
```

- Line 740
```rust
impl PjsuaBackend {
```

- Line 886
```rust
impl SipBackend for PjsuaBackend {
```

- Line 1273
```rust
    fn push_media_frame(
```

- Line 1836
```rust
    fn push_frame_to_tap_unsubscribed_call_is_noop() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 1878
```rust
    fn pjsua_conf_set_callback_is_not_referenced_in_media_path() {
```
