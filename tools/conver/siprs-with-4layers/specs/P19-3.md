# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P19-3: H13: push_media_frame 生産経路の配線（§62.40）

**Ticket Key**: P19-3 · **Phase**: 19

**RFC Source**: `RFC-ROOT.md`

---

## Background

【Goal】実 PJSIP conf bridge（pjsua_conf_add_port）が RustMediaPort port ops（get_frame/put_frame）を駆動し、push_frame_to_tap を経て subscribe_audio の AudioTapHandle へ AudioChunkPair が届く production 経路を統合テストで固定する（§62.40 / N0109 / Q19）。src/runtime/push_media_frame.rs の [::STUB::] を解決する。
【Purpose】H13 RESIDUE「TestBackend では tap が永久待機」を解消する。P17-8（N0097 / §62.28）で共有ヘルパー push_frame_to_tap と RustMediaPort port ops の tap 駆動は完成済みだが、実 conf bridge がそれらを駆動する証拠がゼロだった。本チケットで実通話の統合テストを追加し、production 経路の実在を証明する。
【Motivation】residue 監査で「push_media_frame の production 呼び出し元が実証されていない」と検出。RFC N0109 が Q19 として実 conf bridge 駆動の固定を要求。PX-3 は MediaActive（conf bridge 経由メディア確立）まで検証済みだが、tap への AudioChunkPair 供給は未検証のまま残っている（H13）。
【Constraints】(1) RT コールバックスレッド制約: push_frame_to_tap は lock / alloc / await せず AudioTapSender::try_push（Realtime）のみ（§62.6 / C052 拡張）。(2) 実挙動の証明は pjsua-native 統合テスト（docker Asterisk）のみ — TestBackend フックは共有ヘルパーのユニット検証に限定（Q17 ダミー禁止原則）。(3) 公開 API 変更なし（subscribe_audio / AudioTapHandle / AudioChunkPair のシグネチャ不変）。(4) pjsua_conf_set_callback は vendored PJSIP 2.17 に存在しないため RustMediaPort 登録方式（pjsua_conf_add_port）に一本化（N0097 決定）。

## Scope

- 【変更1-path】src/runtime/push_media_frame.rs
- 【変更1-action】implement（[::STUB::] P19-3 解決）: N0109 実装ターゲット。production 配線 on_conf_frame(call_id: CallId, pcm: &[i16], taps: &AudioTapRegistry) を実装し、i16 PCM → ProcessedFrame::from_i16_stereo → push_frame_to_tap（唯一の供給点）へ委譲。ユニットテストを同ファイルに配置。
- 【変更2-path】src/runtime/mod.rs
- 【変更2-action】modify: `pub mod push_media_frame;` を宣言し、N0109 モジュールをコンパイル対象に追加。
- 【変更3-path】src/runtime/backend.rs
- 【変更3-action】modify: PjsuaBackend に conf port の put_frame/get_frame 経路から呼ばれる on_conf_frame（push_frame_to_tap 呼び出し、RFC N0109 スニペット）を実装。既存の push_media_frame（backend.rs:1282）は同一ヘルパーへ委譲する構造を維持し、production 呼び出し元を確立。
- 【変更4-path】tests/sip_integration.rs
- 【変更4-action】add: 実通話で AudioTapHandle::recv() が AudioChunkPair を受信できる統合テスト conf_bridge_drives_audio_tap（conf bridge 駆動）。TestBackend フックは共有ヘルパーのユニット検証に限定（Q17 原則）。
- **Scope of changes (describe each change comprehensively):**
- [File/module path] src/runtime/push_media_frame.rs
- [Action: add/modify/remove/rename/refactor] implement（[::STUB::] P19-3 解決）
- [What specifically changes] on_conf_frame production 配線 + ユニットテスト。conf-port i16 PCM を受け ProcessedFrame::from_i16_stereo へ変換し push_frame_to_tap へ委譲。
- [Before → After (behavior/signature)] Before: Initial Design Artifact header + `[::STUB::] P19-3` のみ。After: on_conf_frame(call_id, pcm, taps) が tap 供給を行い、mod.rs から pub mod 宣言される。
- [API contract change (if any)] crate::runtime::push_media_frame モジュールが pub(crate) 関数 on_conf_frame を公開（内部 API）。公開クレート API は不変。
- [Data schema change (if any)] なし
- [Config/env change (if any)] なし
- [Dependency added/removed (if any)] なし
- [File/module path] src/runtime/backend.rs
- [Action: add/modify/remove/rename/refactor] modify
- [What specifically changes] PjsuaBackend::on_conf_frame メソッド（RFC N0109 スニペット: conf port put_frame/get_frame 経路から呼ばれ tap へ AudioChunkPair 供給）。既存 push_media_frame メソッド（backend.rs:1282）はヘルパー委譲のまま維持。
- [Before → After (behavior/signature)] Before: production 呼び出し元は RustMediaPort port ops のみ。After: PjsuaBackend::on_conf_frame が production 呼び出し元として明示され、push_frame_to_tap へ委譲。
- [API contract change (if any)] なし（SipBackend トレイトは不変。内部メソッド追加）
- [Data schema change (if any)] なし
- [Config/env change (if any)] なし
- [Dependency added/removed (if any)] なし
- [File/module path] tests/sip_integration.rs
- [Action: add/modify/remove/rename/refactor] add
- [What specifically changes] conf_bridge_drives_audio_tap 統合テスト（docker Asterisk echo 実通話 → subscribe_audio → AudioTapHandle::recv() が AudioChunkPair を受信）。
- [Before → After (behavior/signature)] Before: MediaActive まで検証（PX-3）。After: tap への AudioChunkPair 供給まで検証（H13 解消）。
- [API contract change (if any)] なし（テストのみ）
- [Data schema change (if any)] なし
- [Config/env change (if any)] なし
- [Dependency added/removed (if any)] なし
- **Out of scope (items intentionally excluded, with justification):**
- [Excluded item] TestBackend の tap 駆動実装（AudioTapHandle::recv() が TestBackend 経由で AudioChunkPair を返すようにすること）
- [Why excluded] TestBackend は決定論的シミュレータであり実 conf bridge を持たない。実挙動の証明にダミーを使うのは Q17 ダミー禁止原則に反する。実挙動は pjsua-native 統合テストで固定する。
- [Excluded item] subscribe_audio / AudioTapHandle / AudioChunkPair の公開 API 変更
- [Why excluded] 本チケットは production 配線の固定が目的であり、公開 API は既に P15-7 / P9-2 で完成している。API 変更が必要な要件が出た場合は別チケット。
- [Excluded item] RustMediaPort の port ops 構造変更（get_frame / put_frame のシグネチャ変更等）
- [Why excluded] P17-8 / N0097 で完成済み。N0109 は既存構造の実駆動を固定するのが目的であり、再設計しない。
- **Affected areas (components/systems impacted, even without direct modification):**
- [Affected component] src/runtime/audio_worker.rs（RustMediaPort port ops）
- [Nature of impact] get_frame/put_frame → supply_frame_to_tap → push_frame_to_tap の既存経路が統合テストの検証対象になる。実装変更は必須ではない（P17-8 完成済み）が、on_conf_frame への委譲集約（任意）で supply_frame_to_tap の呼び先が変わる可能性あり。
- [Corresponding change needed] N（必須ではない。任意リファクタとして boyScoutPlan に記載）
- [Affected component] src/runtime/backend.rs push_media_frame（SipBackend トレイト実装）
- [Nature of impact] on_conf_frame 追加により production 呼び出し元が増える。push_media_frame との整合（同一ヘルパー委譲）を保つ。
- [Corresponding change needed] Y — on_conf_frame メソッド追加 + doc 整合
- [Affected component] tests/sip_integration.rs 既存テスト群
- [Nature of impact] 新テスト追加は既存テスト（outgoing_call_to_asterisk 等）と並行。docker 有無のゲート（docker_available）を踏襲し、ローカル CI を壊さない。
- [Corresponding change needed] Y — 新テスト追加のみ（既存テスト変更なし）
- [Affected component] specs/P19-3.md 生成
- [Nature of impact] 本 spec が /start-ticket 時の実装計画の入力になる。
- [Corresponding change needed] Y — 本 spec

## STUBs — Must Be Fully Implemented in This Ticket (1 items — resolved: 1)

### TS-001 — `src/runtime/push_media_frame.rs — resolved

- **Marker**: `// [::TICKET::] P19-3 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P19-3 --for-spec --no-implementation-order`.`
- **Contracts**: C148, C149, C152
- **Resolution Plan**: Implemented on_conf_frame(call_id: CallId, pcm: &[i16], taps: &AudioTapRegistry) in src/runtime/push_media_frame.rs — converts i16 PCM via ProcessedFrame::from_i16_stereo and delegates to push_frame_to_tap (single supply point); declared `pub mod push_media_frame;` in src/runtime/mod.rs; RustMediaPort::supply_frame_to_tap (audio_worker.rs) delegates to on_conf_frame; added SipBackend::ensure_conf_port_for_call + PjsuaBackend per-call registration so the real PJSIP conf bridge drives the port ops; added conf_bridge_drives_audio_tap integration test (§62.40 / N0109).

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0109`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0109` | architecture | rust | 62.40 push_media_frame 生産経路配線（Q19） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.40 push_media_frame 生産経路配線（Q19） (N0109) | → | ☆ 62.28 tap 駆動の生産経路: RustMediaPort port ops（Q7） (N0097) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0109` | `src/runtime/push_media_frame.rs` |

## Investigation

- **RFC N0109（§62.40 push_media_frame 生産経路配線 / Q19）**: 決定 = RustMediaPort port ops（get_frame/put_frame）内で push_frame_to_tap を呼ぶ既存構造（P17-8 / §62.28）を、実 PJSIP conf bridge（pjsua_conf_add_port）が駆動することを統合テストで固定。TestBackend フックは共有ヘルパーのユニット検証に限定（Q17 ダミー禁止原則）。RFC スニペットは PjsuaBackend::on_conf_frame(call_id, pcm) → ProcessedFrame::from_i16_stereo → push_frame_to_tap。I/O 境界: 入力 = conf bridge フレーム（RustMediaPort port ops）、出力 = AudioTapSender::try_push（AudioChunkPair）。関連: audio_worker.rs / backend.rs / tests/sip_integration.rs。
- **既存 production 実装（P17-8 / N0097 / §62.28 完成済み）**: (a) push_frame_to_tap（backend.rs:73）が唯一の供給点。lock + get(call_id) + AudioChunkPair::from_processed_frame + tap.try_push(pair)（Realtime 非ブロック）。(b) RustMediaPort（audio_worker.rs:371）が get_frame（audio_worker.rs:408、out_queue pop + underrun ゼロフィル + supply_frame_to_tap）と put_frame（audio_worker.rs:437、in_queue push + supply_frame_to_tap）を実装。supply_frame_to_tap（audio_worker.rs:398）が CallId 変換の失敗を no-op にする。(c) PjsuaBackend::register_conf_callback（backend.rs:1294）→ register_media_ports_for_calls（backend.rs:844）が pjsua_conf_add_port + pjsua_call_get_conf_port + pjsua_conf_connect を実施（PX-3 / C119-post）。
- **既存ユニットテスト（Q17: TestBackend フックは共有ヘルパーの検証のみ）**: push_frame_to_tap_builds_pair_and_drives_subscribed_tap（backend.rs:1782）、push_frame_to_tap_never_blocks_on_full_queue（backend.rs:1814）、unsubscribed no-op（backend.rs:1841）、rust_media_port_get_frame/put_frame_drives_tap_registry（audio_worker.rs:1822/1850）、from_processed_frame_splits_lr_into_in_out（model/audio_format_chunkpair.rs:390）。
- **H13 ギャップ（本チケットの対象）**: (a) src/runtime/push_media_frame.rs は Initial Design Artifact header + `[::STUB::] P19-3` のみ（N0109 実装ターゲット、mod.rs 未宣言）。(b) 実 conf bridge が port ops → tap を駆動する統合テストがゼロ。PX-3 の outgoing_call_to_asterisk（tests/sip_integration.rs:189）は MediaActive まで検証するが、AudioTapHandle::recv() での AudioChunkPair 受信は未検証。(c) TestBackend::push_media_frame（backend.rs:620）は (call_id, frame) 記録のみで実 tap を駆動しない — TestBackend ベースのテストでは recv() が永久待機（H13 断言の根拠）。
- **統合テスト基盤**: tests/sip_integration.rs は `#![cfg(feature = "pjsua-native")]` + docker_available() ゲート（P16-10 / §62.19）。make test-integration = docker compose up -d + cargo test --features pjsua-native --test sip_integration（Makefile:26）。subscribe_audio（client.rs:479）は call 検証 → tap_channel → registry 登録 → AudioTapHandle。AudioTapHandle::recv（audio_subscribe_bp.rs:113）は producer クローズで None。

## Acceptance Criteria

- 実通話で AudioTapHandle::recv() が AudioChunkPair を受信する
- make test-integration で本テストが green
- - **[Happy path]** docker Asterisk echo アプリへの実通話（sip:1001@127.0.0.1:5060）で CallConnected + MediaActive を確認後、subscribe_audio(call_id, AudioFormat, 16, AudioTapMode::Realtime) で AudioTapHandle を取得し、EVENT_TIMEOUT（30s）以内に handle.recv() が Some(AudioChunkPair) を返す。pair.call_id == 当該 call_id を assert。make test-integration が exit 0。
- - **[Error case]** docker 非存在時は docker_available() により [SKIPPED: docker unavailable] で早期 return（ローカル CI 非破壊）。未購読 call への on_conf_frame / push_frame_to_tap は silent no-op でエラーにしない（RT パスは決して失敗を返さない）。
- - **[Edge case]** メディア未確立（call slot なし）の conf_connect は skip + warn（backend.rs:868）。get_frame underrun はゼロフィル（無音）でも tap 供給を継続。full Realtime queue は最古 evict で最新を保持。recv() が 30s 無応答なら fail（経路非駆動の検出）。

## Invariants

push_frame_to_tap は RT コールバックをブロックしない。
実挙動の証明は実 pjsua-native 統合テストのみ（TestBackend フックはユニット検証に限定）。
【Normal establishment】register_conf_callback（backend.rs:1294）→ register_media_ports_for_calls（backend.rs:844）が pjsua_conf_add_port で RustMediaPort を登録し、pjsua_call_get_conf_port + pjsua_conf_connect で call slot と接続する。メディア確立後、conf bridge が port ops（get_frame/put_frame）を駆動し、supply_frame_to_tap → push_frame_to_tap → AudioTapSender::try_push（Realtime）→ AudioTapHandle::recv() へ AudioChunkPair が届く。
【Invariant on error】未購読 call / invalid call_id では push_frame_to_tap が silent no-op（RT コールバックをブロック・パニックしない）。full Realtime queue では最古フレームを同期的に破棄して新フレームを受理（try_push 非ブロック）。conf_connect 時に call slot 未確立なら skip して warn ログ（backend.rs:868）。
【Internal state invariant】AudioTapRegistry（backend.rs:46）は CallId → (AccountId, AudioTapSender) の共有マップで、push_frame_to_tap が唯一の消費者。on_conf_frame と push_media_frame は同一ヘルパーへ委譲し、フレーム変換・tap ルックアップを一元化する（複製禁止）。
【Boundary invariant】空 i16 PCM フレームは空 AudioChunkPair を供給。i16::MIN / i16::MAX サンプルは無劣化で AudioChunk へ運ばれる。EVENT_TIMEOUT=30s を超えて recv() が無応答なら統合テストは fail（実 conf bridge 非駆動を検出）。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C148 — N0109→N0097

- **Precondition**: N0097 defines tap driving via port ops — RustMediaPort::get_frame/put_frame (src/runtime/audio_worker.rs:408/437) → supply_frame_to_tap (audio_worker.rs:398) → push_frame_to_tap (backend.rs:73), sharing the AudioTapRegistry
- **Postcondition**: Real PJSIP conf bridge (pjsua_conf_add_port, wired in register_media_ports_for_calls backend.rs:844) drives the port ops, so push_frame_to_tap supplies AudioChunkPair to the subscribed tap in a real call
- **Invariant**: push_frame_to_tap never blocks the RT callback — AudioTapSender::try_push (Realtime) evicts the oldest frame on a full queue and returns synchronously (§62.6)

### C149 — N0109→production wiring

- **Precondition**: src/runtime/push_media_frame.rs (N0109 implementation target) hosts the production wiring on_conf_frame(call_id: CallId, pcm: &[i16], taps: &AudioTapRegistry) that converts conf-port i16 PCM → ProcessedFrame::from_i16_stereo → push_frame_to_tap, and the module is declared in src/runtime/mod.rs
- **Postcondition**: on_conf_frame feeds exactly one AudioChunkPair to the subscribed tap for a valid call; an unsubscribed/invalid call is a silent no-op (never panics on the RT path)
- **Invariant**: The wiring delegates to the single supply point push_frame_to_tap — no duplicated frame-building or tap-lookup logic; the [::STUB::] P19-3 marker in push_media_frame.rs is resolved

### C150 — N0109→subscribe_audio integration

- **Precondition**: A real call against docker Asterisk echo reaches MediaActive; a tap is subscribed via SipClient::subscribe_audio(call_id, format, capacity, AudioTapMode::Realtime) (client.rs:479)
- **Postcondition**: AudioTapHandle::recv() (audio_subscribe_bp.rs:113) yields an AudioChunkPair carrying the call's call_id/account_id within EVENT_TIMEOUT (30s) — proof the conf bridge drives the port ops
- **Invariant**: Only the pjsua-native real path (docker Asterisk, §62.19) proves real behavior; TestBackend hooks are used solely for shared-helper unit verification (Q17 dummy-prohibition)

### C151 — N0109→N0097 RT callback boundary

- **Precondition**: The conf bridge invokes the port ops on the PJSUA RT callback thread
- **Postcondition**: get_frame returns the bytes written (underrun zero-fills) and put_frame returns the queue-admit bool, both supplying the tap synchronously without blocking
- **Invariant**: No lock/alloc/await beyond try_push on the RT thread; a full Realtime queue evicts the oldest frame (never blocks — C052 extension)

### C152 — N0109→H13 RESIDUE

- **Precondition**: H13 asserts 'TestBackend では tap が永久待機' — TestBackend::push_media_frame only records (call_id, frame) (backend.rs:620) and never drives a real tap channel
- **Postcondition**: make test-integration (docker compose + cargo test --features pjsua-native --test sip_integration) is green and proves the real path delivers AudioChunkPair — the residue is resolved with real evidence
- **Invariant**: make test-integration is the sole acceptance gate for the real conf-bridge behavior; no TestBackend-based test claims to prove real media delivery

## Boy Scout Rule

- RustMediaPort::get_frame / put_frame（audio_worker.rs:408/437）は「conf bridge フレームを tap へ供給する」同一の関心を各メソッド内で処理している。N0109 の on_conf_frame へ委譲を集約し、「i16 PCM を受け取り tap へ供給する」処理が一箇所（push_frame_to_tap への委譲）で散文として読めるようにする。
- on_conf_frame は「conf フレーム到着 → 変換 → tap 供給」の動詞句（verb phrase）として命名し、関数本体は変換と委譲のみ（一関数一責務）。コメントで「RT スレッドで lock / alloc / await しない」制約を記述。
- push_frame_to_tap の doc コメントは既に「唯一の供給点」を明示しており、on_conf_frame 追加後も二重実装（フレーム変換や tap ルックアップの複製）が発生しないよう整合させる。
- 統合テストは「実通話を張り、tap を購読し、AudioChunkPair を受信する」手順を関数名（conf_bridge_drives_audio_tap 等）で語る。マジックナンバー（capacity=16 等）はハードコードせず、EVENT_TIMEOUT（sip_integration.rs:25）等の既存定数を再利用する。
- 既存の関連 doc（backend.rs register_conf_callback の「routes captured media into push_media_frame」）が実装と整合することを確認し、ずれがあれば修正（コメント腐敗防止）。

## Test Plan

### Unit Tests

- UT: [Normal] on_conf_frame production 配線が i16 PCM → ProcessedFrame → push_frame_to_tap で tap を駆動する（C149-Post / C148-Post unit-level）: src/runtime/push_media_frame.rs の on_conf_frame(call_id, pcm, taps) に stereo_interleaved vec![1i16,2,3,4] を渡し、AudioTapHandle::recv() が pair.in_chunk == I16(vec![1,3]) / pair.out_chunk == I16(vec![2,4]) / pair.call_id == CallId::from_u64(42) / pair.account_id == AccountId::from_u64(1) の AudioChunkPair をちょうど 1 個受信する。push_frame_to_tap_builds_pair_and_drives_subscribed_tap（backend.rs:1782）と同型の検証で、「i16 変換 + tap 供給」の production 配線を固定。
- UT: [Error] 未購読 call / invalid call_id の on_conf_frame は silent no-op（C149-Inv）: 空 AudioTapRegistry で on_conf_frame(42, &[0i16,0], &registry) を呼んでも正常終了し、recv() 側に何も届かない。CallId::from_u64 が Err を返す値（0 / u64::MAX 相当）でもパニックせず no-op（RT コールバックは決して失敗を返さない）。push_frame_to_tap_unsubscribed_call_is_noop（backend.rs:1841）と同型。
- UT: [Boundary] 空 PCM（0 サンプル）は空 AudioChunkPair を供給し、full Realtime queue では最古フレームが同期的に evict される（C148-Inv / C151-Inv）: on_conf_frame(1, &[], &registry) → pair.in_chunk == I16(vec![])。capacity=2 の tap に 3 回 push し、recv() 順で 2 個目・3 個目を受信して 1 個目破棄を検証（push_frame_to_tap_never_blocks_on_full_queue backend.rs:1814 と同型）。i16::MIN / i16::MAX サンプルが無劣化で AudioChunk へ運ばれる。
- UT: [Invariant] push_frame_to_tap が唯一の供給点であり、on_conf_frame は直接 tap を操作せず委譲する（C148-Inv / C149-Inv）: on_conf_frame の本体が ProcessedFrame 変換 + push_frame_to_tap 呼び出しのみで、tap レジストリ直操作・フレーム変換の複製を持たない（コード構造 + 上記 UT で実証）。ProcessedFrame → AudioChunkPair 変換（IN=L / OUT=R split）と tap ルックアップが一元化されている。
- UT: [C148-Pre] RustMediaPort port ops が共有ヘルパー経由で tap registry を駆動する（既存グリーン維持）: rust_media_port_get_frame_drives_tap_registry（audio_worker.rs:1822）が get_frame → push_frame_to_tap → recv() で pair.call_id == CallId::from_u64(42) を検証。rust_media_port_put_frame_drives_tap_registry（audio_worker.rs:1850）が put_frame の同経路を検証。N0097 の「port ops が tap 駆動を定義する」前提を維持。
- UT: [C148-Post] push_frame_to_tap が実 AudioChunkPair を組み立て subscribed tap を駆動する（既存グリーン維持）: push_frame_to_tap_builds_pair_and_drives_subscribed_tap（backend.rs:1782）が IN=L / OUT=R 分割と call_id/account_id 保持を検証。実 conf bridge 駆動そのもの（C148-Post の実環境側）は IT: conf_bridge_drives_audio_tap が固定する。
- UT: [C148-Inv] full Realtime queue で push がブロックせず最古を破棄する（既存グリーン維持）: push_frame_to_tap_never_blocks_on_full_queue（backend.rs:1814）が同期的復帰と evict を検証（RT スレッド無ブロック §62.6 / C052 拡張）。
- UT: [C149-Pre] push_media_frame.rs が N0109 実装ターゲットとして mod.rs に宣言され、on_conf_frame(call_id: CallId, pcm: &[i16], taps: &AudioTapRegistry) を公開する: `pub mod push_media_frame;`（src/runtime/mod.rs）の存在と関数シグネチャをコンパイル + 直接呼び出しで検証。`[::STUB::] P19-3` マーカーが解決されている。
- UT: [C150-Pre] subscribe_audio が既存 call に AudioTapHandle を返し、未存在 call には SipError::not_found を返す（既存グリーン維持 + 必要なら追加）: 既存 client.rs テスト（facade → runtime → reactor → TestBackend 経路）で subscribe_audio の call 検証を確認。AudioTapHandle::recv() が producer クローズで None を返す（audio_subscribe_bp.rs:113 / P9-2 テスト）も担保。
- UT: [C151-Post] get_frame underrun（無音）でも tap 供給を継続し、put_frame は in_queue へ push しつつ tap 供給する（既存グリーン維持）: rust_media_port_get_frame_underrun_zero_fills（audio_worker.rs:1770）+ rust_media_port_get_frame_drives_tap_registry（audio_worker.rs:1822）+ rust_media_port_put_frame_drives_tap_registry（audio_worker.rs:1850）が「フレームの有無に関わらず供給は継続・非ブロック」を検証。
- UT: [C150-Inv / C152-Pre] TestBackend::push_media_frame は記録のみで実 tap チャンネルを駆動しない（H13「永久待機」の設計境界を文書化）: test_backend_push_media_frame_records_invocation（backend.rs:1720）が (call_id, frame) 記録を検証。subscribe_audio で取得した AudioTapHandle に TestBackend 経由では AudioChunkPair が届かず、tap が閉じない（=永久待機）ことを確認し、Q17 のダミー禁止原則が実装に反映されていることを固定。
- UT: [C152-Inv] 統合テストが sip_integration.rs に存在し docker_available() ゲートを持つ（実挙動の証明は pjsua-native のみ）: 新テスト conf_bridge_drives_audio_tap が `#![cfg(feature = "pjsua-native")]` ファイル内で `if !docker_available() { return Ok(()); }` パターン（P16-10 §62.19 Q9c）を踏襲し、docker 非存在で [SKIPPED] になる構造をコンパイル + 構造検証。

### Integration Tests

- IT: [pjsua-native] 実通話で subscribe_audio の tap が AudioChunkPair を連続受信する（conf bridge → RustMediaPort → push_frame_to_tap）: docker Asterisk echo アプリへの実通話（sip:1001@127.0.0.1:5060）で CallConnected + MediaActive を待ち、subscribe_audio(call_id, AudioFormat::new(SampleRate::Hz8000, BitDepth::I16, ChannelLayout::Stereo, 20)?, 16, AudioTapMode::Realtime) で AudioTapHandle を取得。tokio::time::timeout(EVENT_TIMEOUT=30s, handle.recv()) が Some(AudioChunkPair) を返し、pair.call_id == 当該 call_id を assert。§62.40 / N0109 の production 経路固定（H13 解消）。
- IT: [Integration point] register_conf_callback（backend.rs:1294）→ register_media_ports_for_calls（backend.rs:844、pjsua_conf_add_port + pjsua_call_get_conf_port + pjsua_conf_connect）→ MediaPortAdapter port ops → RustMediaPort::get_frame/put_frame（audio_worker.rs:408/437）→ supply_frame_to_tap（audio_worker.rs:398）→ push_frame_to_tap（backend.rs:73）→ AudioTapSender::try_push（Realtime）→ AudioTapHandle::recv（audio_subscribe_bp.rs:113）。PjsuaBackend::push_media_frame（backend.rs:1282）が同一ヘルパーへ委譲することもコード上で確認。
- IT: [Verification] make test-integration（docker compose up -d + cargo test --features pjsua-native --test sip_integration、Makefile:26）が exit 0。docker 非存在時は docker_available() により新テストが [SKIPPED: docker unavailable] で早期 return（P16-10 §62.19 Q9c）し、ローカル CI を壊さない。
- IT: [Prerequisites] docker + docker compose（Asterisk / coturn、P16-10 / §62.19 Q9b）; pjsua-native feature（P18-2 prebuilt 3-OS パイプライン、またはローカル PJSIP 2.17 ビルド）; sip_integration.rs 既存ヘルパー（registered_account / split_sip_uri / wait_for_event / client_against_asterisk）。
- IT: [Related tickets] P17-8（N0097 / C132: push_frame_to_tap 共有ヘルパー + port ops の tap 駆動 — 本チケットは実 conf bridge 駆動を統合テストで固定）; PX-3（C119: register_conf_callback の RustMediaPort conf-bridge 登録 — MediaActive まで検証済み、tap 供給は未検証）; P16-10（docker Asterisk/coturn 統合テスト基盤 §62.19）; P15-7（tap push §62.6）; P18-1/P18-2（pjsua-native build 基盤）; P19-1（実 PJSIP raw SIP 検証経路の先例）; P19-4（N0110 AddAudioSource conf-bridge 再登録 — 本チケットと独立）。

### Exceptions

- - **Exception 1 — TestBackend は実 conf bridge を駆動しない（H13: TestBackend では tap が永久待機）**
  - [Item] TestBackend::push_media_frame（backend.rs:620）は (call_id, frame) を記録するだけで実 tap チャンネルへ供給せず、TestBackend を subscribe_audio の実挙動検証に使うと AudioTapHandle::recv() が永久待機する。実 conf bridge → port ops → tap の経路は TestBackend ではテスト不能（cannot test）。
  - [Reason] TestBackend は決定論的シミュレータであり実 conf bridge / メディアエンジンを持たない（Q17 ダミー禁止原則: 実挙動の証明にダミーを使わない）。テスト不能なのは「実 PJSIP が port ops を駆動する」という外部トリガーのみで、内部ロジックに untestable な欠陥は存在しない — not a design defect。再設計すべきコードは存在しない。
  - [Alternative verification] 実挙動の証明は pjsua-native 統合テスト（docker Asterisk）のみ。共有ヘルパー（push_frame_to_tap）のユニット検証は backend.rs:1782-1844 の既存テストが担い、TestBackend が tap を駆動しない設計（記録のみ）は UT: [C150-Inv / C152-Pre] が文書化する。
- - **Exception 2 — 実 conf bridge のフレームタイミング/フレーム数の決定論的アサート不能**
  - [Item] 実 conf bridge が port ops を呼ぶ正確な回数・間隔・順序（通常 20ms/フレームだが OS スケジューラ・ネットワークジッタ・Asterisk 挙動で変動）の決定論的アサートは不能（cannot test）。
  - [Reason] これは外部トリガーの非決定性であり、内部ロジックの欠陥ではない — not a design defect。再設計で解決すべき untestable なコードは存在しない。
  - [Alternative verification] 「少なくとも 1 個の AudioChunkPair が EVENT_TIMEOUT（30s）以内に届く」ことを時間枠ベースでアサートし、経路が実駆動されることを固定する。フレームレート・変換の厳密検証は shared-helper のユニットテストが担う。
- - **Exception 3 — 実メディア内容（PCM サンプル値）の決定論的アサート不能**
  - [Item] Asterisk echo アプリが返す実際の PCM サンプル値はコーデック・無音区間・エコー遅延で変動し、特定値をアサートできない。要件は「AudioChunkPair が配信される」であり「特定コンテンツが配信される」ではない（cannot test）。
  - [Reason] これは意図された検証粒度（配信の実在性）によるものであり、設計欠陥ではない — not a design defect。コンテンツ値の変換検証は合成フレームのユニットテストが担う。
  - [Alternative verification] pair.call_id / pair.account_id が購読した call と一致し、in_chunk / out_chunk が構造的に妥当（AudioChunk::I16、非空）であることをアサート。コンテンツ値の厳密検証は from_processed_frame_splits_lr_into_in_out（model/audio_format_chunkpair.rs:390）等の合成フレームユニットテストで実施。

### Plan Test Code (concrete code)

- UT: [C148-Pre] RustMediaPort port ops drive the subscribed tap via on_conf_frame (existing green, refactored to route through on_conf_frame)
```rust
let registry: AudioTapRegistry = Arc::new(StdMutex::new(HashMap::new()));
let (sender, mut handle) = tap_channel(4, AudioTapMode::Realtime);
registry.lock().unwrap_or_else(|p| p.into_inner())
    .insert(CallId::from_u64(42)?, (AccountId::from_u64(1)?, sender));
let mixer = Arc::new(AudioMixer::new());
mixer.out_queue.push(vec![7i16; MIXER_FRAME_SAMPLES]).ok();
let port = RustMediaPort::new(mixer, 42, registry);
let mut buf = vec![0u8; MIXER_FRAME_SAMPLES * BYTES_PER_I16];
let written = port.get_frame(&mut buf, buf.len());
assert_eq!(written, MIXER_FRAME_SAMPLES * BYTES_PER_I16);
let pair = handle.recv().await.expect("get_frame must drive the tap");
assert_eq!(pair.call_id, CallId::from_u64(42)?);
```
- IT: [C148-Post] Real conf bridge drives push_frame_to_tap in a real call (new: tests/sip_integration.rs conf_bridge_drives_audio_tap)
```rust
let raw_call_id = account.make_call(OutgoingCallRequest {
    target_uri: "sip:1001@127.0.0.1:5060".into(),
    headers: Vec::new(), auth_override: None, preferred_transport: None,
    media: CallMediaPreferences::default(), auto_answer_refer: false,
}).await?;
let call_id = CallId::from_u64(raw_call_id)?;
let mut tap = client.subscribe_audio(call_id,
    AudioFormat::new(SampleRate::Hz8000, BitDepth::I16, ChannelLayout::StereoInOut, 20)?,
    16, AudioTapMode::Realtime).await?;
wait_for_event(&mut events, |p| matches!(p, SipEventPayload::MediaActive(_))).await;
let pair = tokio::time::timeout(EVENT_TIMEOUT, tap.recv()).await
    .map_err(|_| "timed out waiting for audio chunk")?
    .ok_or_else(|| "tap closed before delivering a chunk")?;
assert_eq!(pair.call_id, call_id);
```
- UT: [C148-Inv] Full Realtime queue evicts oldest; push_frame_to_tap never blocks (existing green: backend.rs:1814)
```rust
let registry: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
let (sender, mut handle) = tap_channel(2, AudioTapMode::Realtime);
registry.lock().unwrap_or_else(|p| p.into_inner())
    .insert(CallId::from_u64(1)?, (AccountId::from_u64(1)?, sender));
let frame = |n: i16| ProcessedFrame {
    stereo_interleaved: vec![n, n],
    negotiated_codec: NegotiatedCodec::Pcmu,
    timestamp: Instant::now(),
};
push_frame_to_tap(CallId::from_u64(1)?, &frame(1), &registry);
push_frame_to_tap(CallId::from_u64(1)?, &frame(2), &registry);
push_frame_to_tap(CallId::from_u64(1)?, &frame(3), &registry); // full → evict 1, no block
let first = handle.recv().await.expect("frame 2 admitted");
assert_eq!(first.in_chunk, AudioChunk::I16(vec![2]));
```
- UT: [C149-Pre] push_media_frame.rs hosts on_conf_frame production wiring (new: replaces [::STUB::] P19-3, mod.rs declaration)
```rust
// src/runtime/push_media_frame.rs
use crate::audio::pipeline::ProcessedFrame;
use crate::model::CallId;
use crate::runtime::backend::{push_frame_to_tap, AudioTapRegistry};

pub(crate) fn on_conf_frame(call_id: CallId, pcm: &[i16], taps: &AudioTapRegistry) {
    let processed = ProcessedFrame::from_i16_stereo(pcm);
    push_frame_to_tap(call_id, &processed, taps);
}
// src/runtime/mod.rs: pub mod push_media_frame;
```
- UT: [C149-Post] on_conf_frame converts i16 PCM → ProcessedFrame → push_frame_to_tap, exactly one pair (new)
```rust
let registry: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
let (sender, mut handle) = tap_channel(4, AudioTapMode::Realtime);
registry.lock().unwrap_or_else(|p| p.into_inner())
    .insert(CallId::from_u64(42)?, (AccountId::from_u64(1)?, sender));
on_conf_frame(CallId::from_u64(42)?, &[1i16, 2, 3, 4], &registry);
let pair = handle.recv().await.expect("on_conf_frame must supply the tap");
assert_eq!(pair.in_chunk, AudioChunk::I16(vec![1, 3]));
assert_eq!(pair.out_chunk, AudioChunk::I16(vec![2, 4]));
assert_eq!(pair.call_id, CallId::from_u64(42)?);
assert_eq!(pair.account_id, AccountId::from_u64(1)?);
```
- UT: [C149-Inv] Unsubscribed call is a silent no-op (never panics on the RT path) (new)
```rust
let registry: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
on_conf_frame(CallId::from_u64(42)?, &[0i16, 0], &registry); // no tap → no-op
// The call must return without blocking/panicking; nothing to assert.
```
- UT: [C150-Pre] subscribe_audio returns AudioTapHandle for an existing call (existing green)
```rust
let raw_call_id = account.make_call(OutgoingCallRequest {
    target_uri: "sip:1001@127.0.0.1:5060".into(), headers: Vec::new(),
    auth_override: None, preferred_transport: None,
    media: CallMediaPreferences::default(), auto_answer_refer: false,
}).await?;
let call_id = CallId::from_u64(raw_call_id)?;
let tap = client.subscribe_audio(call_id,
    AudioFormat::new(SampleRate::Hz8000, BitDepth::I16, ChannelLayout::StereoInOut, 20)?,
    16, AudioTapMode::Realtime).await?;
let _ = tap; // Ok(handle) proves the call exists in the registry and a tap was created
```
- UT: [C150-Inv / C152-Pre] TestBackend::push_media_frame records only; never drives a real tap (existing green: backend.rs:1720)
```rust
let mut backend = TestBackend::new();
let frame = ProcessedFrame {
    stereo_interleaved: vec![1i16, 2],
    negotiated_codec: NegotiatedCodec::Pcmu,
    timestamp: Instant::now(),
};
backend.push_media_frame(42, frame.clone())?;
assert_eq!(backend.push_media_frame_calls.len(), 1);
assert_eq!(backend.push_media_frame_calls[0].0, 42);
assert_eq!(backend.push_media_frame_calls[0].1, frame);
// H13 boundary: a subscribed AudioTapHandle receives nothing via TestBackend
// (recv() waits forever) — real behavior is proven only by the pjsua-native IT.
```
- UT: [C151-Post] get_frame underrun zero-fills but still supplies; put_frame pushes in_queue and supplies (existing green: audio_worker.rs:1770/1850)
```rust
let mixer = Arc::new(AudioMixer::new());
let port = RustMediaPort::new(mixer.clone(), 1, AudioTapRegistry::default());
let mut buf = vec![0xFFu8; MIXER_FRAME_SAMPLES * BYTES_PER_I16];
let bytes = port.get_frame(&mut buf, buf.len());   // underrun → silence
assert_eq!(bytes, MIXER_FRAME_SAMPLES * BYTES_PER_I16);
assert!(buf.iter().all(|&b| b == 0));
assert!(port.put_frame(&[0u8, 0, 7, 0], 4));       // pushes in_queue
assert_eq!(mixer.in_queue.len(), 1);
```
- IT: [C152-Post] make test-integration is green and proves the real path (new)
```bash
make test-integration
# docker compose up -d && cargo test --features pjsua-native --test sip_integration
# conf_bridge_drives_audio_tap passes → H13 residue resolved with real evidence.
```
- IT: [C152-Inv] conf_bridge_drives_audio_tap is gated by docker_available() (new)
```rust
#[tokio::test]
async fn conf_bridge_drives_audio_tap() -> Result<(), Box<dyn std::error::Error>> {
    if !docker_available() {
        return Ok(()); // [SKIPPED: docker unavailable] — local CI not broken
    }
    // ... real call + subscribe_audio + recv() ... (see C148-Post code)
}
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| push_media_frame の production 呼び出し元がゼロ（H13 ギャップ） | conf bridge → RustMediaPort port ops → push_frame_to_tap の経路を実装し統合テストで固定 | push_media_frame 配線（§62.40） |

## RFC Discrepancies found in Prior Implementation Rounds

- RFC N0109 snippet sketches PjsuaBackend::on_conf_frame(&self, call_id, pcm) method using from_i16_stereo(pcm, call_id) and self.tap_registry. Implementation uses a free function on_conf_frame(call_id, pcm, taps) in src/runtime/push_media_frame.rs (the N0109 implementation-target file per spec plan code), calling the actual API from_i16_stereo(pcm), and wires the production path through PjsuaBackend::ensure_conf_port_for_call + RustMediaPort port ops -> supply_frame_to_tap -> on_conf_frame. This satisfies all contracts and the spec's plan-test code; the RFC snippet was a conceptual sketch.

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) src/runtime/push_media_frame.rs: on_conf_frame(call_id, pcm, taps) 実装 + ユニットテスト（[::STUB::] P19-3 解決）。(2) src/runtime/mod.rs: `pub mod push_media_frame;` 追加。(3) src/runtime/backend.rs: PjsuaBackend::on_conf_frame メソッド追加（RFC スニペット、push_frame_to_tap 委譲）。(4) tests/sip_integration.rs: conf_bridge_drives_audio_tap 統合テスト追加。(5) make test-integration で実証。
- [Risks] (a) 統合テストが docker 非存在環境で SKIPPED になるため、CI とローカルの両方で実行確認が必要。(b) subscribe_audio は call が reactor registry に存在する必要があるため、make_call 後の適切なタイミングで呼ぶ（MediaActive 前でも call entry は存在する）。(c) echo アプリは呼び出し直後に無音区間がある可能性があり、recv() のタイムアウトは十分（30s）に取る。
- [Caveats] on_conf_frame は RFC N0109 スニペットの概念実装。実際の conf bridge 駆動は RustMediaPort port ops（audio_worker.rs:408/437）が担い、on_conf_frame は production 呼び出し元の明示と共有ヘルパー委譲の一元化を目的とする。port ops と二重実装しないこと（No-Justification Rule）。
- [Open items] TestBackend が tap を駆動しない設計（H13 の永久待機）は Q17 原則に基づく意図的決定として維持。アプリ要件として TestBackend 上で tap を受信したい場合は、決定論的シミュレーション tap 供給の別設計が必要（現スコープ外）。
- [Future improvements] pjsua_conf_set_callback が将来の PJSIP で利用可能になれば、conf bridge 全体のフレームを単一コールバックで tap へ供給できる（RustMediaPort 登録方式の代替）。N0110（P19-4）の AddAudioSource 時 RustMediaPort 再登録が実装されれば、動的ソース追加時の tap 供給も統合テストで固定可能。
Implementation summary (P19-3 / §62.40 / N0109 / H13):
- src/runtime/push_media_frame.rs: implemented on_conf_frame(call_id, pcm, taps) production wiring — i16 PCM -> ProcessedFrame::from_i16_stereo -> push_frame_to_tap (single supply point). Resolved [::STUB::] P19-3. Added 4 unit tests (C149-post/C149-inv/C148-inv/C151-inv).
- src/runtime/mod.rs: declared `pub mod push_media_frame;`.
- src/runtime/audio_worker.rs: RustMediaPort::supply_frame_to_tap now routes through on_conf_frame (Boy Scout consolidation).
- src/runtime/backend.rs: added SipBackend::ensure_conf_port_for_call (default no-op) + PjsuaBackend impl that registers the call port via pjsua_conf_add_port, resolves the call conf slot, and connects (idempotent via registered_port_ids). Added 3 unit tests (registers+connects, missing-mixer no-op, conf_add_port error surfacing). Existing push_media_frame delegates to push_frame_to_tap.
- src/runtime/reactor.rs: connect_media_for_call now calls backend.ensure_conf_port_for_call before conf_connect (production call path for post-boot calls).
- tests/sip_integration.rs: added conf_bridge_drives_audio_tap integration test (docker Asterisk echo call -> subscribe_audio -> AudioTapHandle::recv() yields AudioChunkPair within EVENT_TIMEOUT).
- Verification: make test -> exit 0, 17 test binaries green (4 on_conf_frame + 3 ensure_conf_port tests among them). cargo check --features pjsua-native --test sip_integration -> clean. verify-red-coverage: 5/5 contracts (C148-C152). Quality checks: new-code findings resolved (0 remaining in P19-3-added code).
- NOTE (environmental): make test-integration could NOT be executed in this session — Docker Desktop VM cannot pull images from Docker Hub (docker pull hello-world hangs; host curl to registry works). The integration test compiles and follows the P16-10 pattern; it must be run in a docker-network-enabled environment (CI).
Review report (P19-3 / §62.40 / N0109 / H13):
- Static quality check: P19-3 new code clean (0 findings). 50 remaining unwrap/expect findings are pre-existing test code from prior tickets (P15-3/P17-8/PX-3 etc.), explicitly permitted by zasso Rust rules (test code + unreachable). Not a P19-3 regression.
- Translatability: on_conf_frame / ensure_conf_port_for_call / conf_bridge_drives_audio_tap are verb-phrase names; supply_frame_to_tap now routes through on_conf_frame (Boy Scout consolidation); no debug output, no new magic numbers.
- Annotation verification: initially 4 missing annotations in backend.rs (verify script's single-line lookback); fixed by restoring standard-format [::TICKET::] P19-3 changes. annotations immediately above the 4 fn definitions. Now 9/9 verified, 0 ambiguous.
- Tests: make test all green (17 binaries). pjsua-native integration test compiles. verify-red-coverage 5/5 contracts.
- ENVIRONMENTAL LIMITATION (not a code defect): make test-integration could NOT be executed in this session — Docker Desktop VM cannot pull images from Docker Hub (docker pull hello-world hangs while host curl reaches registry). conf_bridge_drives_audio_tap follows the P16-10 docker_available gate pattern and must be executed in a docker-network-enabled CI environment.
- Issues found and fixed during review: restored 4 standard annotation lines above backend.rs definitions; cleaned up annotate-script over-application (duplicates + spurious P19-3 merges in non-P19-3 files raw_sip_real_test.rs / docker_asterisk_it.rs / ffi/bindings.rs).

## P19-3 — implemented at 24 locations

### src/build/prebuilt_ci_commit.rs

- Line 73
```rust
    fn repo_root() -> std::path::PathBuf {
```

- Line 81
```rust
    fn workflow_constants_match_design() {
```

- Line 96
```rust
    fn committed_workflow_satisfies_s62_37() -> Result<(), String> {
```

### src/build/prebuilt_producer.rs

- Line 127
```rust
    fn target_set_follows_design_brief_s5_6() -> Result<(), &'static str> {
```

- Line 149
```rust
    fn file_and_nm_predicates_detect_expected_formats() {
```

- Line 170
```rust
    fn staged_layout_invariant_rejects_incomplete_stage() -> Result<(), std::io::Error> {
```

### src/runtime/audio_worker.rs

- Line 179
```rust
impl AudioMixer {
```

- Line 380
```rust
impl RustMediaPort {
```

- Line 399
```rust
    fn supply_frame_to_tap(&self, frame: &ProcessedFrame) {
```

- Line 1636
```rust
        fn assert_send<T: Send>() {}
```

### src/runtime/backend.rs

- Line 178
```rust
    fn get_account_info(&self, native_acc_id: u32) -> Result<AccountInfoSnapshot, ReactorError>;
```

- Line 195
```rust
    fn push_media_frame(
```

- Line 211
```rust
    fn register_conf_callback(&mut self) -> Result<(), ReactorError>;
```

- Line 223
```rust
    fn ensure_conf_port_for_call(&mut self, _call_id: u64) -> Result<(), ReactorError> {
```

- Line 769
```rust
impl PjsuaBackend {
```

- Line 920
```rust
impl SipBackend for PjsuaBackend {
```

- Line 1343
```rust
    fn ensure_conf_port_for_call(&mut self, call_id: u64) -> Result<(), ReactorError> {
```

- Line 2082
```rust
    fn ensure_conf_port_for_call_registers_and_connects_a_call() -> Result<(), ReactorError> {
```

- Line 2114
```rust
    fn ensure_conf_port_for_call_missing_mixer_is_noop() -> Result<(), ReactorError> {
```

- Line 2133
```rust
    fn ensure_conf_port_for_call_surfaces_conf_add_error() {
```

### src/runtime/mod.rs

- Line 39
```rust
pub mod push_media_frame;
```

### src/runtime/push_media_frame.rs

- Line 20
```rust
use crate::audio::pipeline::ProcessedFrame;
```

### src/runtime/reactor.rs

- Line 114
```rust
impl CoreReactor {
```

### tests/sip_integration.rs

- Line 385
```rust
    if !docker_available() {
```
