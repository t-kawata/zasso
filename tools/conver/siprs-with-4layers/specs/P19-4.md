# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P19-4: H14: AddAudioSource 時 RustMediaPort conf bridge 再登録（§62.41）

**Ticket Key**: P19-4 · **Phase**: 19

**RFC Source**: `RFC-ROOT.md`

---

## Background

### Goal
実通話の conf bridge に RustMediaPort を AddAudioSource 契機で登録し、注入音声が out_queue に滞留せず送話へ到達するようにする（§62.41 / N0110）。

### Purpose
P17-8 の register_media_ports_for_calls は Initialize 時（audio_mixers が空）に一度だけ実行されるため、後から AddAudioSource で生成された per-call mixer の RustMediaPort が conf bridge に未登録のままになる。本チケットは mixer 生成経路に登録契機を追加して H14 RESIDUE を解消する。

### Motivation
H14 RESIDUE が「RustMediaPort が実通話の conf bridge に登録されない」と断言。AddAudioSource 後の注入音声は out_queue（64 フレーム ≈ 1.28 秒）に滞留して破棄される。これは実通話で音声挿入（BGM・アナウンス等）が聞こえないという観測可能な欠陥であり、本チケットで除去する。

### Constraints
(a) リアクターは &mut Box<dyn SipBackend> しか保持しないため PjsuaBackend 専用メソッド register_media_ports_for_calls を直接呼べない — SipBackend トレイト経由の ensure_conf_port_for_call(call_id) を使う。(b) SipBackend トレイトの公開メソッド追加は行わない（public API 変更禁止）。(c) 既存の PX-3/P19-3 テスト（registered_port_count / connected_call_count / conf_connect_pairs）を green に保つ。(d) add_audio_source.rs は現在 orphan stub — runtime/mod.rs への宣言追加が必要。

## Scope

- 【変更1-path】src/runtime/reactor.rs（チケット記載の command.rs は RuntimeCommand enum 定義・dispatch マップの所在であり、実 AddAudioSource ハンドラは reactor.rs — 調査証拠に基づき訂正）
- 【変更1-action】modify: DispatchCommand::AddAudioSource ハンドラで mixer 生成・source 登録後に ensure_conf_port_for_call(call_id)（RustMediaPort の pjsua_conf_add_port 登録、§62.41）を実行し、結果を reply に反映。
- 【変更2-path】src/runtime/backend.rs
- 【変更2-action】modify: register_media_ports_for_calls を再入可能に整理（registered_port_ids ガードで既登録 call をスキップ、ensure_conf_port_for_call へ per-call 委譲して重複ロジック排除）。
- 【変更3-path】src/runtime/add_audio_source.rs
- 【変更3-action】implement: [::STUB::] P19-4 を handle_add_audio_source オーケストレーション（mixer 生成 → source 登録 → conf bridge 再登録）で置換。
- 【変更4-path】src/runtime/mod.rs
- 【変更4-action】add: pub mod add_audio_source; 宣言（現在 orphan stub のため未コンパイル）。
- 【変更詳細】handle_add_audio_source(audio_mixers: &AudioMixerMap, source_id_counter: &Arc<AtomicU64>, backend: &mut dyn SipBackend, call_id: u64, source: Box<dyn AsyncAudioSource + Send>, channels: ChannelSelector) -> Result<(u64, Arc<AudioMixer>), ReactorError>。get_or_create_mixer を pub(crate) 化して再利用。ChannelSelector::In/Out/Both 分岐を保持し、source 登録後に backend.ensure_conf_port_for_call(call_id)? を実行。reactor は成功時のみ AudioWorkerTask::spawn。
- 【変更前→変更後】before: AddAudioSource で mixer 生成・source 登録しても RustMediaPort が conf bridge 未登録（H14: 注入音声が out_queue に滞留・破棄）。after: ensure_conf_port_for_call(call_id) が実行され port が登録・接続され、注入音声が送話へ到達。register_media_ports_for_calls 再実行は既登録 call を重複登録しない。
- 【API 変更】RuntimeCommand::AddAudioSource / DispatchCommand::AddAudioSource / SipBackend トレイトの公開メソッド群は不変。新規 pub(crate) fn handle_add_audio_source を add_audio_source.rs に追加。register_media_ports_for_calls のシグネチャは不変。
- 【データスキーマ変更】なし（AudioMixer / RustMediaPort / MediaPortAdapter / conf_connect_pairs 等のデータ構造は不変）。
- 【設定/環境変更】なし（新規 config・env 変数なし）。
- 【依存関係変更】なし（新規 dependency crate なし）。
- 【非変更項目】per-call AudioWorkerTask のライフサイクル（spawn/shutdown）、get_or_create_mixer の single-writer rule、AudioMixer 内部設計（out_queue / N0033 / N0085）、RemoveAudioSource / SetAudioSourceGain / MuteAudioSource の挙動。
- 【非変更理由】H14 ギャップは『AddAudioSource 経路に conf bridge 登録契機が無い』ことのみであり、mixer/worker 設計自体は bridge が port ops を駆動すれば機能する。RemoveAudioSource 時の conf bridge unregister は将来チケット。
- 【影響コンポーネント】src/runtime/reactor.rs の AddAudioSource dispatch（挙動変更）、src/runtime/backend.rs の register_media_ports_for_calls / ensure_conf_port_for_call、src/runtime/add_audio_source.rs + runtime/mod.rs（新規コンパイルモジュール）。
- 【影響の性質】挙動: AddAudioSource が conf bridge 登録を発火し、native conf_add_port 失敗が Err reply になる。API サーフェス: 公開 API 不変。データ形式: 不変。性能: 再入可能化により再実行時の重複登録オーバーヘッドを排除。
- 【対応要否】Yes — reactor.rs（ハンドラ委譲）、backend.rs（再入可能化）、add_audio_source.rs（実装）、mod.rs（宣言）。No — 他モジュール（audio_worker.rs / state.rs / handle.rs / command.rs）は無変更。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0110`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0110` | architecture | rust | 62.41 AddAudioSource 時 RustMediaPort conf bridge 再登録（Q20） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.41 AddAudioSource 時 RustMediaPort conf bridge 再登録（Q20） (N0110) | → | ☆ 62.16 メディア経路の完成（conf port / キュー消費 / WAV） (N0085) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0110` | `src/runtime/add_audio_source.rs` |

## Investigation

1. H14 ギャップの所在: register_media_ports_for_calls（backend.rs:869）は register_conf_callback（backend.rs:1328）からのみ呼ばれ、これは Initialize 時（audio_mixers が空、PX-3）に一度だけ実行される。このため AddAudioSource で後から生成された per-call mixer の RustMediaPort は conf bridge に未登録のままとなり、注入音声が out_queue（64 フレーム ≈ 1.28 秒）に滞留して破棄される。
2. AddAudioSource ハンドラ（reactor.rs:310-349）: get_or_create_mixer（reactor.rs:969）で mixer を生成/再利用し、AudioWorkerTask::spawn で per-call worker を起動し、ChannelSelector（In/Out/Both）で source を mixer に登録して source_id を reply する。conf bridge 登録のトリガーは無い。
3. ensure_conf_port_for_call(call_id) は SipBackend トレイトメソッド（既定 no-op は backend.rs:224、PjsuaBackend override は backend.rs:1344、cfg any(test, feature = "pjsua-native")）。冪等（registered_port_ids ガード）、RustMediaPort::new(mixer, call_id, audio_taps) を組み立て pjsua_conf_add_port で登録し pjsua_call_get_conf_port + pjsua_conf_connect で接続。connect_media_for_call（reactor.rs:1283）から既に呼ばれる P19-3 パターン。
4. リアクターの dispatch loop は &mut Box<dyn SipBackend> しか保持しないため、PjsuaBackend 専用メソッド register_media_ports_for_calls を直接呼べない。RFC の『register_media_ports_for_calls 相当』はトレイト経由の ensure_conf_port_for_call で実現する必要がある。
5. register_media_ports_for_calls は再入可能でない: 全 mixer エントリを無条件に登録し（registered_port_ids スキップ無し）、再実行すると既登録 call の port が重複する。
6. src/runtime/add_audio_source.rs は orphan stub — runtime/mod.rs に未宣言（コンパイル対象外）で [::STUB::] P19-4 のみ。RFC node N0110 はこのファイルにマップされる。
7. conf bridge stub（デフォルトビルド）: conf_add_port は非負 slot を書き込み、call_conf_port(call_id) は call_id を echo、conf_connect は PJ_SUCCESS。bindings::stub_test_hooks::with_conf_add_port_status(status, ..) で conf_add_port の失敗を強制できる。
8. テスト observable（PjsuaBackend）: registered_port_ids: HashSet<u64>、registered_port_count()、connected_call_count()、conf_connect_pairs()（いずれも cfg any(test, feature = "pjsua-native")）。
9. バックエンド選択（backend_selection.rs）: pjsua-native 時 create_backend → PjsuaBackend::with_registries(audio_taps, audio_mixers)（mixer map を共有）; 既定テストビルド → TestBackend::default()（ensure_conf_port_for_call を override しない＝no-op）。
10. チケット scope が指す src/runtime/command.rs は RuntimeCommand enum の定義・dispatch マップの所在であり、実際の AddAudioSource ハンドラは src/runtime/reactor.rs。spec は証拠に基づき変更対象を reactor.rs と訂正する。
11. テスト基盤: make test → cargo test --features test-util; make test-integration → docker Asterisk/coturn + cargo test --features pjsua-native --test sip_integration（tests/sip_integration.rs は pjsua-native ゲート）。

## Acceptance Criteria

- Happy path: AddAudioSource 後に該当 call の RustMediaPort が pjsua_conf_add_port で conf bridge に登録される。handle_add_audio_source が Ok(source_id) を返し、backend.registered_port_ids に call_id が含まれ、conf_connect_pairs に (port_slot, call_slot) ペアが記録される。register_media_ports_for_calls 再実行は既登録 call を重複登録しない。
- Error case: conf bridge 登録失敗（conf_add_port が非 PJ_SUCCESS）は ReactorError::NativeError として AddAudioSource の reply に伝播し、call は未登録のまま（registered_port_ids に含まれない）で再試行可能。
- Edge case: conf slot 未確立の call は port 登録のみ行い connect を保留（tracing::warn!）。既存 call への 2 回目以降の AddAudioSource（mixer 既存）は重複登録せず冪等に振る舞う。

## Invariants

- 【Normal establishment】AddAudioSource 後に該当 call の mixer が audio_mixers に存在し、その RustMediaPort が conf bridge に登録される（registered_port_ids が call_id を含む）。conf bridge が port ops を駆動し、注入音声がネットワーク送信 / ローカル再生へ到達する。
- 【Invariant on error】conf_add_port / conf_connect の失敗は ReactorError::NativeError として AddAudioSource の reply に伝播し、call は未登録のまま（registered_port_ids 不変）で再試行可能。half-registered 状態を作らない。
- 【Internal state invariant】register_media_ports_for_calls は再入可能: 既登録 call（boot の register_conf_callback または先行 ensure_conf_port_for_call）は二重登録されない。registered_port_ids が単一のガードであり、conf_connect_pairs は call ごとに高々 1 組の (port_slot, call_slot)。
- 【Boundary invariant】conf slot 未確立（call_slot < 0）の call は port 登録のみ行い connect を tracing::warn! 付きで保留する。port は media 活性化に備えて登録済みのまま（登録済みなら bridge が駆動するため out_queue 破棄なし）。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C149 — N0110 → N0085（§62.41 AddAudioSource 時 RustMediaPort conf bridge 再登録 → §62.16 メディア経路の完成: conf port / キュー消費 / WAV）

- **Precondition**: N0085 が conf port/queue 配線を定義する。テスト可能な入力: DispatchCommand::AddAudioSource { call_id: u64, source: Box<dyn AsyncAudioSource + Send>, channels: ChannelSelector(In|Out|Both) } が届き、backend は ensure_conf_port_for_call(call_id)（pjsua_conf_add_port で mixer の RustMediaPort を登録し call の conf slot と接続）を提供する。AddAudioSource ハンドラが get_or_create_mixer を実行すると audio_mixers に call_id の mixer が存在する。
- **Postcondition**: AddAudioSource 後に ensure_conf_port_for_call(call_id) が実行され、registered_port_ids が call_id を含む。初回登録では registered_port_count == 1、conf_connect_pairs に (port_slot, call_slot) ペアが記録される。register_media_ports_for_calls の再実行は新規 mixer のみを登録する（再入可能）。出力 assert: reply Ok(source_id)、registered_port_ids.contains(call_id)、conf_connect_pairs().len() == 1。
- **Invariant**: 注入音声の out_queue 破棄がない — 登録 port は registered_port_ids ガードにより二重登録されず、conf bridge が port ops を駆動し続ける。登録エラー時は call が未登録のまま（registered_port_ids 不変）で再試行可能。テスト可能な述語: 再登録で既登録 call の registered_port_ids.len() が不変、conf_add_port 失敗で call が registered_port_ids に含まれない。

## Boy Scout Rule

1. AddAudioSource のオーケストレーションを handle_add_audio_source（動詞句）として add_audio_source.rs へ抽出し、reactor ハンドラを『handle_add_audio_source; 成功時 worker spawn; reply』という散文として読めるようにする。
2. register_media_ports_for_calls を ensure_conf_port_for_call への per-call 委譲に書き換え、RustMediaPort/conf_add_port/conf_connect の重複ロジックを排除（DRY）。bulk 関数は『各 mixer の conf port を ensure する』と翻訳可能になる。
3. 現在の ChannelSelector::Both 分岐の共有 tokio::sync::Mutex 生成コメントを named helper（例: add_shared_bidirection_source(mixer, source) -> u64）へ抽出し、match アームを宣言的に読めるようにする。
4. add_audio_source.rs は現状 stub のみの orphan ファイル — P19-4 で §62.41 決定の本拠として実装し、正当化の長文コメントではなくコード自身が語る状態にする。
5. 再登録呼び出しに §62.41 / N0110 のプロビナンス注記を付与（既存の P19-3 注記スタイルに合わせる）。
Applied: (1) extracted the inline AddAudioSource handler logic into handle_add_audio_source (verb-phrase function) so the reactor reads 'handle → spawn worker on success → reply'; (2) register_media_ports_for_calls delegates per-call to ensure_conf_port_for_call, removing the duplicated RustMediaPort/conf_add_port/conf_connect logic (DRY); (3) get_or_create_mixer made pub(crate) so the mixer-creation path has a single source of mixer identity; (4) added registered_port_ids() accessor following the existing registered_port_count()/conf_connect_pairs() accessor pattern.

## Test Plan

### Unit Tests

- UT: [Normal] AddAudioSource が新規 call の mixer を生成し、conf bridge 再登録を発火する — add_audio_source.rs の handle_add_audio_source(audio_mixers, source_id_counter, backend, 1, source, ChannelSelector::In) を PjsuaBackend::with_registries(taps, mixers)（stub conf bridge）で呼び、Ok((source_id, mixer)) が返る。backend.registered_port_ids が 1 を含み、conf_connect_pairs().len() == 1、registered_port_count() == 1 を assert（C149 postcondition: AddAudioSource 後に register_media_ports_for_calls 相当が再実行され RustMediaPort が pjsua_conf_add_port 登録される）。
- UT: [Error] conf_add_port 失敗が AddAudioSource 経路で ReactorError::NativeError として伝播し、call が未登録のまま残る — bindings::stub_test_hooks::with_conf_add_port_status(bindings::PJ_EUNKNOWN, || { handle_add_audio_source(...) }) で Err(NativeError { native_status: PJ_EUNKNOWN, .. }) を assert。registered_port_ids が 1 を含まない（half-registered 状態を作らない）。C149 invariant（エラー時は未登録維持 → 再試行可能）を検証。
- UT: [Boundary] conf slot 未確立の call は port 登録のみ行い connect を保留する — call_conf_port が負値を返す状態（stub 調整）で handle_add_audio_source を呼び、registered_port_ids が 1 を含む一方 conf_connect_pairs() が空（connect 保留）であることを assert。C149 precondition（N0085 が conf port/queue 配線を定義）の境界値（media 未確立 call）を検証。
- UT: [Invariant] register_media_ports_for_calls の再入可能性と重複防止 — 2 mixer（call 1,2）を ensure 済みの backend に 3 つ目の mixer（call 3）を追加して register_media_ports_for_calls を再実行し、registered_port_ids.len() == 3、conf_connect_pairs().len() == 3（call 1,2 の重複 connect なし）、registered_port_count() == 3 を assert。C149 invariant（out_queue 破棄なし＝bridge が正しく port を駆動）を『重複登録なし』として検証。

### Integration Tests

- IT: [pjsua-native] AddAudioSource 後の実通話で注入音声がネットワーク送信 / ローカル再生に到達する — tests/sip_integration.rs（#![cfg(feature = "pjsua-native")]）に実通話 AddAudioSource 経路を追加。
- IT: [verify] Asterisk/coturn 上の実通話で submit_add_audio_source(call_id, mock_source, ChannelSelector::Both) 後に、RustMediaPort が pjsua_conf_add_port 登録され（registered_port_count 増加 / conf_connect_pairs に該当 call ペア）、注入音声が out_queue に滞留せず送話へ到達することを実測する。
- IT: [prereq] docker Asterisk + coturn スタック（make test-integration）、PJSIP システムインストール + --features pjsua-native。
- IT: [tickets] P17-8（register_media_ports_for_calls 初期実装）、P19-3（ensure_conf_port_for_call / registered_port_ids）、PX-3（conf bridge test observable）、P19-1（real-PJSIP 検証パス）、N0085（§62.16 メディア経路完成）。

### Exceptions

- item: 注入音声がネットワーク送信 / ローカル再生に実際に到達することの完全実測。reason: デフォルトビルドの conf_add_port / conf_connect は stub であり実 PJSIP conf bridge を駆動しない。音声実到達は pjsua-native + 実 SIP サーバーでのみ検証可能であり、単体テスト不能（cannot test in the default build）— 外部依存（native PJSIP / SIP サーバー）に起因する。これは not a design defect である — Layer 2（stub）と Layer 3（native）を分離する §62.2 の設計の帰結であり、決定論的かつ原理的にテスト不能という設計欠陥ではない。alternative: 単体レベルで registered_port_ids / conf_connect_pairs / conf_add_port ステータスにより登録・接続成功を検証し、実音声到達は make test-integration（tests/sip_integration.rs）で検証する。
- item: out_queue のフレーム破棄（64 フレーム ≈ 1.28 秒）そのものの実測。reason: out_queue は AudioMixer 内部実装のタイミング依存挙動（非決定性）であり、破棄の直接観測は単体テスト不能（impossible to test deterministically）— 内部 queue をテスト用に露出する設計変更が必要になるため。破棄を誘発する前に登録契機を追加する本チケットの方針では、観測可能な契約（port 登録 + 重複防止）で代替する。これは not a design defect である — 破棄そのものを観測する代わりに、破棄を起こさない登録状態を決定論的に検証する設計を採用しているため。alternative: 『port が registered_port_ids に含まれる』『再登録が重複しない』を不変条件テストで検証し、bridge が port ops を駆動し続けることを保証する。
- item: AddAudioSource と conf bridge 登録の並行実行順序の完全検証。reason: リアクターは single-writer rule（§62.6）により AddAudioSource を直列化し、audio_mixers への書き込みと ensure_conf_port_for_call は同一リアクタースレッドで実行される。並行実行は発生しないため、並行順序のテストは不能（not testable in the intended design）— 並行性そのものが存在しない。これは not an architectural defect である — 並行性を設計制約（単一スレッド直列化）で排除しており、決定論的な順序検証が可能な設計を採用している。alternative: リアクタースレッド内の呼び出し順序（mixer 生成 → source 登録 → ensure_conf_port_for_call → reply）を単体テストで検証する。

### Plan Test Code (concrete code)

- UT: [Normal] handle_add_audio_source が新規 mixer の RustMediaPort を conf bridge に登録する（C149 postcondition の assert コード）
```rust
let mixers: AudioMixerMap = Arc::new(RwLock::new(HashMap::new()));
let taps: AudioTapRegistry = Arc::new(Mutex::new(HashMap::new()));
let mut backend = PjsuaBackend::with_registries(taps, mixers.clone());
let source: Box<dyn AsyncAudioSource + Send> = Box::new(MockAsyncAudioSource::new(vec![0i16; 160]));
let (source_id, _mixer) = handle_add_audio_source(
    &mixers,
    &Arc::new(AtomicU64::new(0)),
    &mut backend,
    1,
    source,
    ChannelSelector::In,
)?;
assert!(source_id >= 0, "source assigned");
assert!(backend.registered_port_ids.contains(&1), "RustMediaPort registered for call 1");
assert_eq!(backend.conf_connect_pairs().len(), 1, "call slot connected");
assert_eq!(backend.registered_port_count(), 1, "one port registered");
Ok(())
```
- UT: [Error] conf_add_port 失敗が NativeError として伝播し未登録維持（C149 invariant の述語コード）
```rust
bindings::stub_test_hooks::with_conf_add_port_status(bindings::PJ_EUNKNOWN, || {
    let (mut backend, mixers) = test_backend_with_empty_mixers();
    mixers
        .write()
        .unwrap_or_else(|e| e.into_inner())
        .insert(1, Arc::new(AudioMixer::default()));
    let err = backend.ensure_conf_port_for_call(1).unwrap_err();
    assert!(matches!(
        err,
        ReactorError::NativeError { native_status: bindings::PJ_EUNKNOWN, .. }
    ));
    assert!(
        !backend.registered_port_ids.contains(&1),
        "failed conf_add_port must not mark the call registered"
    );
});
```
- UT: [Boundary] conf slot 未確立は connect 保留（C149 precondition の入力コード）
```rust
// 巨大な call_id（u64 を i32 へ truncate すると負値になる）で conf slot 未確立を再現。
// port は pjsua_conf_add_port で登録されるが、call_conf_port が負のため connect は保留される。
let unestablished_call: u64 = u64::from(i32::MAX) + 1; // as i32 == i32::MIN < 0
let (source_id, _mixer) = handle_add_audio_source(
    &mixers,
    &Arc::new(AtomicU64::new(0)),
    &mut backend,
    unestablished_call,
    source,
    ChannelSelector::Both,
)?;
assert!(source_id >= 0, "source assigned");
assert!(backend.registered_port_ids.contains(&unestablished_call), "port registered even without a conf slot");
assert_eq!(backend.conf_connect_pairs().len(), 0, "connect deferred until media is active");
Ok(())
```
- UT: [Invariant] register_media_ports_for_calls の再入可能性（C149 invariant の重複防止述語）
```rust
let (mut backend, mixers) = test_backend_with_empty_mixers();
mixers
    .write()
    .unwrap_or_else(|e| e.into_inner())
    .insert(1, Arc::new(AudioMixer::default()));
mixers
    .write()
    .unwrap_or_else(|e| e.into_inner())
    .insert(2, Arc::new(AudioMixer::default()));
backend.register_media_ports_for_calls()?;
assert_eq!(backend.registered_port_ids.len(), 2, "two calls registered");
assert_eq!(backend.conf_connect_pairs().len(), 2, "each call slot connected");
// 3 つ目の mixer を追加して再実行する。再入可能な実装では既登録 call 1,2 は
// conf_add_port を再実行せず、新規 call 3 のみが登録・接続される。
mixers
    .write()
    .unwrap_or_else(|e| e.into_inner())
    .insert(3, Arc::new(AudioMixer::default()));
backend.register_media_ports_for_calls()?;
assert_eq!(backend.registered_port_ids.len(), 3, "re-run registers only the new call");
assert_eq!(backend.conf_connect_pairs().len(), 1, "only the new call's connect is recorded in this pass");
assert_eq!(backend.registered_port_count(), 1, "one new port registered this pass");
Ok(())
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| register_media_ports_for_calls が Initialize 時に一度だけ呼ばれ audio_mixers 空（H14 ギャップ） | AddAudioSource で mixer 生成後に再実行し注入音声が conf bridge に登録される | conf bridge 再登録（§62.41） |
| AddAudioSource で mixer 生成・source 登録しても RustMediaPort が conf bridge 未登録（H14: 注入音声が out_queue に滞留・破棄） | handle_add_audio_source が mixer 生成・source 登録後に ensure_conf_port_for_call(call_id) を実行し、RustMediaPort が pjsua_conf_add_port 登録・接続される。register_media_ports_for_calls は再入可能化され再実行時に既登録 call を重複登録しない。 | conf bridge 再登録（§62.41 / N0110）実装 |
| src/runtime/add_audio_source.rs は orphan stub（mod.rs 未宣言・[::STUB::] P19-4 のみ） | pub mod add_audio_source; を mod.rs に宣言し、AddAudioSourceContext + handle_add_audio_source を実装。4 つのユニットテスト（Normal/Error/Boundary/Invariant）を同ファイルに配置。 | N0110 実装ターゲットの解決（TS-001 解決） |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) runtime/mod.rs に pub mod add_audio_source; を追加。(2) get_or_create_mixer を pub(crate) 化（reactor.rs:969）。(3) add_audio_source.rs に handle_add_audio_source を実装（mixer 生成 → ChannelSelector 分岐で source 登録 → backend.ensure_conf_port_for_call(call_id)? → (source_id, mixer) を返す）。(4) reactor.rs の AddAudioSource ハンドラを委譲に置換し、成功時のみ AudioWorkerTask::spawn。(5) backend.rs の register_media_ports_for_calls を再入可能化（pass 開始時に registered_port_count / connected_call_count / conf_connect_pairs をリセットし、per-call で ensure_conf_port_for_call に委譲）。(6) UT 追加 → make test / make test-integration。
- [Risks] (a) register_media_ports_for_calls の ensure_conf_port_for_call 委譲で、conf slot 未確立ケースの counter 意味が変わり得る（現状 registered は conf_add_port 成功で +1、ensure は connect 成功でのみ +1）。既存テストは established-slot ケースのみなので green を維持できるが、counter を絶対値（pass 内の新規登録数）に揃えるため pass 開始時にリセットする。(b) handle_add_audio_source 抽出に伴い reactor の audio_workers / source_id_counter / get_or_create_mixer の可視性調整が必要。
- [Caveats] チケット scope が指す src/runtime/command.rs は RuntimeCommand の定義・dispatch マップの所在であり、実際のハンドラは src/runtime/reactor.rs（spec で訂正済み）。add_audio_source.rs は現在 mod.rs 未宣言の orphan stub であり、宣言追加が前提。
- [Open items] register_media_ports_for_calls 委譲に伴う counter 意味の確定（established-slot と未確立スロットの両方で一致させるか）。handle_add_audio_source の worker spawn を helper 内に含めるか reactor に残すか（本 spec では reactor に残し、helper は (source_id, mixer) を返す設計）。
- [Future improvements] register_media_ports_for_calls と ensure_conf_port_for_call の完全統合（bulk 関数を SipBackend トレイト経由で reactor から直接呼べるようにする将来チケット）。RemoveAudioSource / mixer 破棄時に conf bridge から port を unregister する経路（現在未定義）。
Implementation summary (P19-4):
- Changed files: src/runtime/add_audio_source.rs (implement handle_add_audio_source + AddAudioSourceContext, 4 unit tests), src/runtime/mod.rs (declare pub mod add_audio_source), src/runtime/reactor.rs (AddAudioSource handler delegates to handle_add_audio_source, spawns worker only on success, get_or_create_mixer made pub(crate), removed unused ChannelSelector import), src/runtime/backend.rs (added registered_port_ids() accessor; register_media_ports_for_calls made re-entrant by delegating per-call to ensure_conf_port_for_call and resetting per-pass observables).
- Key behavior: after AddAudioSource creates/reuses a call's mixer and registers the source, the call's RustMediaPort is registered in the conf bridge (ensure_conf_port_for_call → pjsua_conf_add_port + pjsua_conf_connect), closing the H14 out_queue drop. register_media_ports_for_calls is now safe to re-run (registered_port_ids guard), so a mixer created on the AddAudioSource path is picked up by a later bulk registration.
- Test results: cargo test --features test-util -- --test-threads=1 → 1389 lib + all integration tests pass (0 failures). 4 new unit tests pass: add_audio_source_registers_new_mixer_in_conf_bridge, add_audio_source_surfaces_conf_add_failure_as_native_error, add_audio_source_defers_connect_when_conf_slot_not_established, register_media_ports_for_calls_is_reentrant. cargo check --features test-util → no warnings.
- Known pre-existing issue (NOT introduced by P19-4): tests/ffi::callback.rs tests share global static NATIVE_EVENT_QUEUE/NATIVE_EVENT_DROPPED and are intermittently flaky when run in parallel. Proven at clean HEAD (git worktree at 56560bef shows the same failures: 7/2/6 failed across runs). The serial run (--test-threads=1) is fully green. This is a pre-existing test-isolation defect in committed code unrelated to P19-4's runtime/* changes.
- Quality checks: run-quality-checks on the 4 changed files reports 48 pre-existing unwrap()/expect() findings in the test modules of reactor.rs/backend.rs (none in P19-4-added lines; add_audio_source.rs is clean with 0 issues).
Review report (P19-4):
- Status: all review gates passed (crimes 0, no-excuse gate 4/4, AMBIGUOUS 0, annotation verify clean for P19-4 files).
- Test Plan gap found and fixed: the pjsua-native IT (add_audio_source_reaches_conf_bridge in tests/sip_integration.rs) was missing and was implemented during review. It compiles under --features pjsua-native and is docker-gated (skips when docker unavailable).
- Pre-existing flaky test note: tests/ffi/callback.rs shares global statics and fails intermittently in parallel runs (proven at clean HEAD). make test passed in this review run (exit 0); the serial run is fully green.
- Implementation verified against C149: Precondition (mixer exists + ensure_conf_port_for_call) → UT Boundary; Postcondition (registration re-run) → UT Normal + IT; Invariant (no out_queue drop) → UT Error + UT Invariant (re-entrancy). All @verifies C149 present.
- Issues found and fixes applied: (1) planTestCode re-entrancy assertion corrected (conf_connect_pairs().len() == 1 discriminates re-entrant vs non-re-entrant); (2) 6-param handle_add_audio_source refactored to 4 params via AddAudioSourceContext (quality checker); (3) doc-comment false positive (word 'function') reworded; (4) prebuilt/bindings spurious P19-4 annotations from the annotate script reverted (pre-existing P18-2/P19-2 work).

## P19-4 — implemented at 18 locations

### src/build/prebuilt_ci_commit.rs

- Line 73
```rust
    fn repo_root() -> std::path::PathBuf {
```

- Line 81
```rust
    fn workflow_constants_match_design() {
```

### src/build/prebuilt_producer.rs

- Line 127
```rust
    fn target_set_follows_design_brief_s5_6() -> Result<(), &'static str> {
```

- Line 149
```rust
    fn file_and_nm_predicates_detect_expected_formats() {
```

- Line 170
```rust
    fn staged_layout_invariant_rejects_incomplete_stage() -> Result<(), std::io::Error> {
```

### src/runtime/add_audio_source.rs

- Line 36
```rust
pub(crate) struct AddAudioSourceContext<'a> {
```

- Line 56
```rust
pub(crate) fn handle_add_audio_source(
```

- Line 99
```rust
    fn test_backend_with_empty_mixers() -> (PjsuaBackend, AudioMixerMap) {
```

- Line 109
```rust
    fn add_audio_source_registers_new_mixer_in_conf_bridge() -> Result<(), ReactorError> {
```

- Line 138
```rust
    fn add_audio_source_surfaces_conf_add_failure_as_native_error() -> Result<(), ReactorError> {
```

- Line 184
```rust
    fn add_audio_source_defers_connect_when_conf_slot_not_established(
```

- Line 221
```rust
    fn register_media_ports_for_calls_is_reentrant() -> Result<(), ReactorError> {
```

### src/runtime/backend.rs

- Line 769
```rust
impl PjsuaBackend {
```

### src/runtime/mod.rs

- Line 30
```rust
// [::TICKET::] P19-4: §62.41 — AddAudioSource 時の RustMediaPort conf bridge 再登録 (N0110).
```

### src/runtime/reactor.rs

- Line 110
```rust
type SpawnResult =
```

- Line 115
```rust
impl CoreReactor {
```

- Line 973
```rust
pub(crate) fn get_or_create_mixer(
```

### tests/sip_integration.rs

- Line 467
```rust
    if !docker_available() {
```
