# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P19-5: H15/EXAMPLES: 実 PJSIP プロトコル + RTP 統合テスト（§62.42）

**Ticket Key**: P19-5 · **Phase**: 19

**RFC Source**: `RFC-ROOT.md`

---

## Background

§4.3 はプロトコルレベルと通信レベルの両方を保証することを要求。§43.4（Layer 4）/ §62.19 / P16-10 のテスト基盤が存在。ビルド修復（Ticket A）後に通す。

### Goal
pjsua-native ビルド修復後、tests/sip_integration.rs と src/tests/docker_asterisk_it.rs（+ N0111 実装ターゲット src/tests/real_pjsip_itest.rs）を通して、実 PJSIP のプロトコルレベル（REGISTER→200 / INVITE→180/200 / BYE / SIP INFO DTMF / RFC 4733 DTMF / STUN binding / TURN allocate / relay candidates via coturn）と通信レベル（2 エンドポイント間 RTP メディア送受信）の両方を make test-integration で CI 保証する（§62.42 / Q21）。

### Purpose
H15 / EXAMPLES RESIDUE が『実 SIP 統合テストが存在しない』と断言。既存の 7 テスト（register_against_asterisk / raw_sip_rx_reaches_subscriber / outgoing_call_to_asterisk / incoming_call_via_originate / coturn_stun_turn_ice / conf_bridge_drives_audio_tap / add_audio_source_reaches_conf_bridge）は REGISTER・INVITE・BYE・STUN/TURN/ICE・raw SIP・audio tap をカバーするが、SIP INFO / RFC 4733 DTMF と 2 エンドポイント間 RTP が未カバー。本チケットで不足テストを追加し、§62.42 の全スコープを実 PJSIP で固定する。

### Motivation
TestBackend green は実 SIP 検証の証拠にならない（Q17 / C151-inv）。DTMF 送出と RTP メディア送受信は実 PJSIP + 実 SIP サーバーでのみ検証可能であり、H15 RESIDUE の解消には docker Asterisk/coturn 上の実統合テストが必須。また §62.19（N0088）の統合テスト基盤は既に存在し（P16-10 実装済み）、ビルド修復（Ticket A / phase 18）後に §62.42 の全スコープを通すことが README RESIDUE（H15）と EXAMPLES-RESIDUE の前提条件である。

### Constraints
(a) テストは #![cfg(feature = "pjsua-native")] ゲート + docker_available() ゲートの二段構え（Q9a/Q9c）。(b) デフォルトビルド / TestBackend では subscribe_raw_sip() は無音チャネル、subscribe_audio tap は駆動されない（Q17 / §62.40）— 実 SIP 検証は pjsua-native + docker のみ。(c) make test-integration の teardown（docker compose down）は trap で常時保証。(d) DockerItPolicy.integration_tests の既存単体テスト（c116_post が len==5 を assert）を green に保つため、マトリクス拡張と同時にアサートを更新する。(e) 2 エンドポイント RTP テストの RFC 例は同一プロセス内 2 SipClient（N0052/N0053 Dual Client）を前提とするが、現実装に PjsuaBackend singleton 共有が無い（pjsua_create 二重呼び出し問題）— 実装方式の判断を要する（1 SipClient + 2 アカウント代替 または backend 共有実装）。

## Scope

- 【変更1-path】tests/sip_integration.rs
- 【変更1-action】modify: プロトコルレベル（REGISTER→200 / INVITE→180/200 / BYE / SIP INFO / RFC 4733 DTMF / STUN binding / TURN allocate via coturn）と通信レベル（2 エンドポイント間 RTP メディア送受信）のテストを実装。
- 【変更2-path】src/tests/docker_asterisk_it.rs
- 【変更2-action】modify: Docker（Asterisk/coturn）環境での実 SIP フローをビルド修復後に通す。
- **Scope of changes (describe each change comprehensively):**
  - [path] src/tests/real_pjsip_itest.rs（N0111 実装ターゲット、現在 [::STUB::] P19-5）/ tests/sip_integration.rs / src/tests/docker_asterisk_it.rs / src/ffi/callback.rs（TS-001 resolve_transport_id）/ src/tests/mod.rs
  - [action] implement: real_pjsip_itest.rs に §62.42 スコープのポリシー RealPjsipItestPolicy を実装（docker_asterisk_it.rs / raw_sip_real_test.rs と同パターン）。modify: tests/sip_integration.rs に dtmf_sip_info / dtmf_rfc4733 / register_invite_bye_rtp_flow を追加。modify: docker_asterisk_it.rs の DockerItPolicy.integration_tests を 5→8 に拡張。implement: callback.rs の pjsua-native resolve_transport_id（[::STUB::] P19-5）を pjsua_enum_transports + pjsua_transport_get_info による実 transport-id マッチへ置換。modify: src/tests/mod.rs に pub mod real_pjsip_itest; を追加。
  - [detail] RealPjsipItestPolicy は test_file / feature_gate / make_target / docker_gate / skip_message / protocol_level_tests / rtp_level_tests を保持し、C150/C151 のユニットテスト（Normal/Error/Boundary/Invariant）を同ファイルに配置。dtmf_sip_info: Asterisk echo 通話確立後 send_dtmf(call_id, "1", DtmfMethod::Info) → DtmfSent{method: Info, digit: '1'} を検証。dtmf_rfc4733: send_dtmf(call_id, "5", DtmfMethod::Rfc4733) → DtmfSent{method: Rfc4733, digit: '5'} を検証。register_invite_bye_rtp_flow: alice→bob INVITE、bob answer(200)、両者 CallConnected、alice add_audio_source 注入、bob subscribe_audio で AudioChunkPair 受信、alice hangup → CallDisconnected。resolve_transport_id: 純粋述語 match_transport_id(transports: &[(u32, &str)], target: &str) -> u32 を抽出し、pjsua-native ラッパーは enum_transports/get_info で (id, local_name) を収集して委譲（on_transport_state が実 pjsua_transport_id を運ぶ）。
  - [before → after] before: SIP INFO / RFC 4733 DTMF と 2 エンドポイント間 RTP の実統合テストが存在しない（H15 RESIDUE）。resolve_transport_id（pjsua-native）は常に 0 を返す（on_transport_state の transport_id が不正確）。after: 3 テストが追加され、§62.42 のプロトコル + 通信レベル全スコープが make test-integration で CI 実行される。DockerItPolicy.integration_tests が 5 エントリ（P16-10）から 8 エントリへ拡張される。resolve_transport_id が実 transport id を返し、on_transport_state が正確な NativeEvent::TransportStateChanged を発火する。
  - [API contract change] なし — SipClient::send_dtmf(call_id, digits, DtmfMethod) / add_audio_source / subscribe_audio / hangup は既存公開 API のまま。RealPjsipItestPolicy は pub(crate) モジュール内の新規データ型（公開 API に非追加）。match_transport_id は callback.rs 内の private 純粋関数。
  - [Data schema change] なし — DtmfSentInfo / DtmfMethod / AudioChunkPair / CallId / NativeEvent::TransportStateChanged 等のデータ構造は不変（transport_id の値が正確になるのみ）。
  - [Config/env change] なし — 新規 config / env 変数なし。docker compose（asterisk:20.6.0 / coturn/coturn:4.6）と Makefile test-integration は既存のまま。
  - [Dependency added/removed] なし — 新規 dependency crate なし（tokio は process 機能含め P19-1 で導入済み）。
- **Out of scope (items intentionally excluded, with justification):**
  - [item] 真の 2 SipClient プロセス並走のための PjsuaBackend singleton 共有実装（N0052/N0053 Dual Client のバックエンド側）。
  - [why] 本チケットの RFC 実装ターゲットは src/tests/real_pjsip_itest.rs（テストポリシー）であり、backend 変更はスコープ外。register_invite_bye_rtp_flow は 1 SipClient + 2 アカウント（alice_account → bob_account）で RTP 送受信を検証する代替を基本とし、Dual Client 共有は将来チケット。
  - [item] EXAMPLES（E1-E5）の実 Asterisk での動作検証（README EXAMPLES-RESIDUE の解消）。
  - [why] EXAMPLES-RESIDUE は『単一実装例に全セクション統合』を要求する別セクションであり、本チケットは §62.42 の統合テストスコープに限定。examples の実機検証は将来チケット。
- **Affected areas (components/systems impacted, even without direct modification):**
  - [component] tests/sip_integration.rs（テスト追加）、src/tests/docker_asterisk_it.rs（DockerItPolicy マトリクス拡張 + c116_post アサート更新）、src/tests/real_pjsip_itest.rs（N0111 ポリシー新規実装）、src/tests/mod.rs（pub mod real_pjsip_itest; 宣言追加）、src/ffi/callback.rs（resolve_transport_id 実装 + on_transport_state テスト）。
  - [nature] テストカバレッジ拡張（プロトコル + 通信レベル）+ on_transport_state の transport_id 精度改善。public API サーフェス・データ形式・性能・設定に影響なし。
  - [response] Yes — DockerItPolicy.integration_tests のエントリ数 5→8 と c116_post_policy_encodes_q9_decisions の len アサート更新、tests モジュールの mod 宣言追加、callback.rs の resolve_transport_id + そのユニットテスト追加。No — runtime / backend / client / config / FFI の他部分は無変更。

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0111`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0111` | test_policy | rust | 62.42 実 PJSIP 統合テストスコープ（Q21） |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.42 実 PJSIP 統合テストスコープ（Q21） (N0111) | → | ☆ 62.19 Docker/Asterisk 実 SIP 統合テスト基盤 (N0088) |
| references | ★ 62.42 実 PJSIP 統合テストスコープ（Q21） (N0111) | → | ☆ §43 Test Strategy — 4-Layer Architecture (N0052) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0111` | `src/tests/real_pjsip_itest.rs` |

## Investigation

- [N0111 / §62.42] RFC-ROOT.md:5244-5272 が統合テストスコープを定義: tests/sip_integration.rs と src/tests/docker_asterisk_it.rs を通し、プロトコルレベル（REGISTER→200 / INVITE→180/200 / BYE / SIP INFO / RFC 4733 DTMF / STUN binding / TURN allocate / relay candidates via coturn）と通信レベル（2 エンドポイント間 RTP）をカバー。make test-integration（docker compose + --features pjsua-native + sip_integration、guaranteed teardown）を CI 実行。RFC コード例 register_invite_bye_rtp_flow は SipClient::new(cfg_a) / SipClient::new(cfg_b) の 2 エンドポイント構成。N0111 実装ファイルパス: src/tests/real_pjsip_itest.rs。
- [既存カバレッジ] tests/sip_integration.rs（7 テスト、全て #![cfg(feature = "pjsua-native")] + docker_available() ゲート）: register_against_asterisk（REGISTER→Registered）/ raw_sip_rx_reaches_subscriber（P19-1 raw SIP）/ outgoing_call_to_asterisk（INVITE→CallConnected→MediaActive→hangup→CallDisconnected）/ incoming_call_via_originate（channel originate→IncomingCall→answer 200）/ coturn_stun_turn_ice（STUN/TURN/ICE）/ conf_bridge_drives_audio_tap（P19-3）/ add_audio_source_reaches_conf_bridge（P19-4）。ヘルパー: registered_account / split_sip_uri / wait_for_event / wait_for_raw_sip / client_against_asterisk / originate_call_to、EVENT_TIMEOUT=30s。
- [不足ギャップ] SIP INFO DTMF（DtmfMethod::Info）と RFC 4733 DTMF（DtmfMethod::Rfc4733）の実統合テストが無い。2 エンドポイント間 RTP（register_invite_bye_rtp_flow）が無い（全テストが Asterisk echo 相手）。
- [TS-001 / callback.rs:469] enumerate-ticket-targets が [::STUB::] P19-5 を検出: resolve_transport_id（pjsua-native ブランチ）が常に 0 を返す（on_transport_state の transport_id が不正確）。コメント: 『match tp to a pjsua_transport_id via pjsua_enum_transports/pjsua_transport_get_info — the vendored pjsip_transport has no id field』。非 native ブランチは stub mirror の (*tp).id を返し、既存テスト on_transport_state_enqueues_transport_state_changed（callback.rs:882、id: 7 → TransportStateChanged{transport_id: 7}）は非 native 側で green。vendored pjsip_transport 構造体（bindings.rs:563）は id: u32 のみ。pjsua_transport_id は i32。pjsua_enum_transports / pjsua_transport_get_info は bindings に存在。解決策: 純粋述語 match_transport_id(transports: &[(u32, &str)], target: &str) -> u32 を抽出し（デフォルトビルドでテスト可能）、pjsua-native ラッパーは enum_transports/get_info で (id, local_name) を収集して委譲。
- [src/tests/docker_asterisk_it.rs] DockerItPolicy（N0088 §62.19）: integration_tests は 5 エントリ（register_against_asterisk / outgoing_call_to_asterisk / incoming_call_via_originate / coturn_stun_turn_ice / raw_sip_rx_reaches_subscriber）。docker_available() / SKIP_MESSAGE / ASTERISK_IMAGE=asterisk:20.6.0 / COTURN_IMAGE=coturn/coturn:4.6 / ASTERISK_SIP_PORT=5060 / ASTERISK_SIP_TCP_PORT=5061 / COTURN_STUN_PORT=3478 / IT_EVENT_TIMEOUT=30s。単体テスト C116/C117/C118（c116_post_policy_encodes_q9_decisions が integration_tests.len()==5 を assert — P19-5 で 8 へ更新必須）。
- [src/tests/real_pjsip_itest.rs] 現在は `// [::STUB::] P19-5: implement NODE_ID=N0111 real-PJSIP protocol + RTP integration test (§62.42)` のみ。Initial Design Artifact ヘッダ（N0111）あり、src/tests/mod.rs に未宣言（orphan stub）。
- [公開 API] SipClient::new(ClientConfig) -> (SipClient, Receiver<SipEvent>)（client.rs:104）; add_account(AccountConfig) -> SipAccountHandle; SipAccountHandle::make_call(OutgoingCallRequest) -> u64; hangup(call_id, HangupReason); answer(call_id, u16); send_dtmf(call_id, digits, DtmfMethod)（client.rs:438、validate_dtmf_digits で fail-fast）; subscribe_audio(call_id, AudioFormat, capacity, AudioTapMode) -> AudioTapHandle; add_audio_source(call_id, Box<dyn AsyncAudioSource+Send>, ChannelSelector) -> u64（client.rs:511）; subscribe_raw_sip() -> Option<Subscription<RawSipMessage>>（client.rs:211）。DtmfMethod: Rfc4733 | Info | Inband（model/dtmf_spec.rs:27）。DtmfSentInfo { method, digit: char, status: Result<(), SentDtmfError>, pjsip_status: Option<u32> }（api/m20_dtmfsent_twophase.rs:47、二段構え 500ms）。SipEventPayload に DtmfSent / DtmfReceived / CallConnected / CallDisconnected / MediaActive / IncomingCall / RegistrationStateChanged / TransportStateChanged が存在（api/event_model_payload_bus.rs:294）。MockAsyncAudioSource::new(vec![0i16; 160])（runtime::audio_worker、sip_integration.rs で使用実績）。
- [Dual-client ギャップ（最重要リスク）] RFC N0111 の register_invite_bye_rtp_flow は同一プロセス 2 SipClient 前提（§43 N0052/N0053 Dual Client）。しかし backend_selection.rs の create_backend は SipClient ごとに新規 PjsuaBackend を生成し、backend_calls.rs:43 initialize() は pjsua_create() を無条件に呼ぶ。PJSUA はプロセス単位 singleton のため 2 つ目の SipClient::new は pjsua_create 失敗（PJ_EEXISTS 相当）が予想される。grep で OnceLock / static backend / singleton 共有機構は未検出。m20_test_dual_client.rs の DualClientContext は空フィールドの構造スタブ。→ RTP テストは 1 SipClient + 2 アカウント（alice_account → bob_account）での代替を基本設計とする。
- [Makefile / Cargo.toml / docker-compose] Makefile test-integration（P16-10）: docker compose up -d → trap 'docker compose down' EXIT → cargo test --features pjsua-native --test sip_integration。Cargo.toml [[test]] name=sip_integration required-features=["pjsua-native"]。docker-compose.yml: asterisk（5060/5061、pjsip.conf / extensions.conf マウント、host.docker.internal エイリアス）+ coturn（3478、turnserver.conf マウント）。
- [TestLayer] src/tests/test_strategy_4layer.rs: Layer3SipIntegration（is_pjsip_free()==false / is_ci_runnable()==true）、Layer4Interop（is_ci_runnable()==false）— C151-pre の根拠。
- [C151-inv 根拠] raw_sip_real_test.rs（P19-1 N0107）C145-inv: observation_only=PJ_FALSE、production path steps=[pjsip_module hook → enqueue_raw_sip_bytes → drain_and_publish_raw_sip → subscribe_raw_sip]。pjsip_module フックは pjsua-native 配下のみ登録、TestBackend の subscribe_raw_sip() は無音チャネル（Q17 仕様）。

## Acceptance Criteria

- make test-integration が CI で green
- プロトコルレベルと通信（RTP）レベルの両方をカバーする
- **[Happy path]** make test-integration（docker compose up -d → cargo test --features pjsua-native --test sip_integration → trap で docker compose down）が CI で green。新テスト dtmf_sip_info（DtmfSent{method: Info, digit: '1', status: Ok(())}）/ dtmf_rfc4733（DtmfSent{method: Rfc4733, digit: '5', status: Ok(())}）/ register_invite_bye_rtp_flow（bob の AudioChunkPair{call_id: bob_call_id} 受信 → BYE）が実 PJSIP + docker Asterisk/coturn 上で成功。src/tests/real_pjsip_itest.rs の RealPjsipItestPolicy + C150/C151 ユニットテストが make test（--features test-util）で green。プロトコルレベル（REGISTER→200 / INVITE→180/200 / BYE / SIP INFO / RFC 4733 DTMF / STUN binding / TURN allocate / relay candidates via coturn）と通信レベル（2 エンドポイント間 RTP）の全スコープがカバーされる。
- **[Error case]** docker 不可時は各統合テストが docker_available() により [SKIPPED: docker unavailable] を明示ログ出力して早期 return し、実 SIP 状態に触れない（Q9c）。DTMF 送信失敗は DtmfSent.status == Err または pjsip_status.is_some() としてテスト失敗に検出される。make test-integration の teardown（docker compose down）はテスト失敗時も trap 'docker compose down' EXIT で保証される（teardown 未達なし）。
- **[Edge case]** DockerItPolicy.integration_tests のエントリ数が 5→8 に拡張され、方向カバレッジ不変条件（IntegrationDirection::Outbound≥2 / Inbound≥1 / Ice≥1）が保持される。各新テストは EVENT_TIMEOUT（30s、IT_EVENT_TIMEOUT と一致）内にイベントを受信できない場合 panic し、ハングしない。DtmfSent の 500ms タイムアウトフォールバック（P17-7）により Ok が必ずしも送信成功を意味しない境界値を、status/pjsip_status の assert で判別する。

## Invariants

TestBackend green を実 SIP 検証の証拠としない。
make test-integration の teardown（docker compose down）は常に保証する。

- 【Normal establishment】実 SIP 統合テスト（tests/sip_integration.rs）は #![cfg(feature = "pjsua-native")] のコンパイル時ゲート + docker_available() の実行時ゲートの二段構えで、docker compose（asterisk/coturn）up 状態でのみ実行される。プロトコルレベル（REGISTER→200 / INVITE→180/200 / BYE / SIP INFO / RFC 4733 DTMF / STUN binding / TURN allocate / relay candidates via coturn）と通信レベル（2 エンドポイント間 RTP）の両方が RealPjsipItestPolicy（src/tests/real_pjsip_itest.rs）にデータ化され、各テスト名が #[tokio::test] として実在する。
- 【Invariant on error】docker 不可時は各テストが [SKIPPED: docker unavailable] を明示ログ出力して早期 return し、実 SIP 状態に触れない（Q9c）。DTMF 送信失敗は DtmfSent.status == Err または pjsip_status.is_some() としてテスト失敗に検出される。make test-integration は trap 'docker compose down' EXIT によりテスト失敗時も teardown を保証する。
- 【Internal state invariant】DockerItPolicy.integration_tests が §62.19/§62.42 統合テストの単一情報源であり、P19-5 でエントリ数が 5 から 8 へ拡張される（追加: dtmf_sip_info / dtmf_rfc4733 / register_invite_bye_rtp_flow）。方向カバレッジ（IntegrationDirection::Outbound≥2 / Inbound≥1 / Ice≥1）が常に満たされる。RealPjsipItestPolicy の列挙テスト名（protocol_level_tests / rtp_level_tests）は tests/sip_integration.rs の実在 #[tokio::test] と一致し続ける。
- 【Boundary invariant】各イベント待ちは EVENT_TIMEOUT（30s、docker_asterisk_it::IT_EVENT_TIMEOUT と一致）で打ち切られ、ハングしない。DtmfSent の 500ms タイムアウトフォールバック（P17-7）により、実 PJSIP の send_dtmf が同期失敗しても DtmfSent は発火し、status/pjsip_status で失敗が判別できる（Ok が必ずしも送信成功を意味しない境界値）。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C150 — N0111→N0088

- **Precondition**: N0088（§62.19 docker Asterisk/coturn 基盤）が統合テスト基盤を定義している。テスト可能な入力: src/tests/docker_asterisk_it.rs の DockerItPolicy::policy() が compose_file="docker-compose.yml" / services=["asterisk","coturn"] / feature_gate="pjsua-native" / make_target="test-integration" / skip_message="[SKIPPED: docker unavailable]" を提供し、docker_available() 実行時ゲートが存在する。docker-compose.yml が asterisk（5060/5061）+ coturn（3478）サービスを定義する。→ UT [Boundary] がこのポリシー入力を検証。
- **Postcondition**: プロトコルレベル（REGISTER→200 / INVITE→180/200 / BYE / SIP INFO / RFC 4733 DTMF / STUN binding / TURN allocate / relay candidates via coturn）と通信レベル（2 エンドポイント間 RTP メディア送受信）が CI（make test-integration）でカバーされる。テスト可能な出力: RealPjsipItestPolicy（src/tests/real_pjsip_itest.rs）が protocol_level_tests=[register_against_asterisk, outgoing_call_to_asterisk, incoming_call_via_originate, dtmf_sip_info, dtmf_rfc4733, coturn_stun_turn_ice] と rtp_level_tests=[register_invite_bye_rtp_flow] を列挙し、各テスト名が tests/sip_integration.rs の #[tokio::test] fn として実在する。DockerItPolicy.integration_tests.len()==8。→ UT [Normal] が出力 assert。
- **Invariant**: make test-integration が green であり、teardown（docker compose down）が常に保証される。テスト可能な述語: Makefile の test-integration ターゲットが `docker compose up -d`・`trap 'docker compose down' EXIT`・`cargo test --features pjsua-native --test sip_integration` の 3 要素を持ち、Cargo.toml [[test]] name="sip_integration" required-features=["pjsua-native"] と一致する。docker 不可時は各テストが [SKIPPED: docker unavailable] を出力して早期 return する（Q9c）。→ UT [Error] が不変条件述語を検証。

### C151 — N0111→N0052

- **Precondition**: §43 4-Layer Test Strategy がテスト階層を定義している。テスト可能な入力: src/tests/test_strategy_4layer.rs の TestLayer enum が Layer3SipIntegration（is_pjsip_free()==false && is_ci_runnable()==true）と Layer4Interop（is_ci_runnable()==false）を定義し、§43.3（Docker SIP server / 実 PJSUA）と §43.4（実 PBX interop）の層分離を表現する。→ UT [Normal] がこの入力を検証。
- **Postcondition**: Layer 4（Interop）統合テストが実 PJSIP で実行される。テスト可能な出力: RealPjsipItestPolicy.feature_gate=="pjsua-native"、test_file=="tests/sip_integration.rs" であり、同ファイル先頭が `#![cfg(feature = "pjsua-native")]` を含む（コンパイル時ゲート）。実 SIP 検証（REGISTER/INVITE/DTMF/RTP）は pjsua-native ビルドでのみ実行され、TestBackend を経由しない。→ UT [Normal] / UT [Invariant] が出力 assert。
- **Invariant**: TestBackend green を実 SIP 検証の証拠としない。テスト可能な述語: (a) tests/sip_integration.rs の全テストが docker_available() ゲートを持ち、pjsua-native なしではコンパイルされない。(b) raw_sip_real_test.rs C145-inv が observation_only="PJ_FALSE" と production path steps=[pjsip_module hook → enqueue_raw_sip_bytes → drain_and_publish_raw_sip → subscribe_raw_sip] を assert 済み — pjsip_module フックは pjsua-native 配下のみで登録され、TestBackend の subscribe_raw_sip() は無音チャネルのまま（Q17 仕様）。(c) 新規 DTMF/RTP テストも同じ二段構え（pjsua-native + docker）を満たす。→ UT [Invariant] が不変条件述語を検証。

## Boy Scout Rule

- src/tests/real_pjsip_itest.rs は現状 [::STUB::] P19-5 のみの orphan ファイル — §62.42（N0111）決定の本拠として RealPjsipItestPolicy（名詞）を実装し、const fn policy() でデータを返す docker_asterisk_it.rs / raw_sip_real_test.rs パターンに揃える。policy() は『§62.42 スコープを返す』と翻訳可能な単一責務に留める。
- tests/sip_integration.rs の新テストは関数名を動詞句に統一: dtmf_sip_info（sends SIP INFO DTMF and observes DtmfSent）/ dtmf_rfc4733（sends RFC 4733 DTMF and observes DtmfSent）/ register_invite_bye_rtp_flow（registers both endpoints, INVITEs, exchanges RTP, BYEs）。Asterisk echo 通話を確立する共通手順（register → make_call → CallConnected → MediaActive）を繰り返し書かず、既存ヘルパー（registered_account / wait_for_event / client_against_asterisk）を再利用し、必要なら establish_asterisk_echo_call ヘルパー（動詞句）へ抽出する。
- マジックナンバーを定数化: テストで使う DTMF 桁 '1' / '5'、注入フレーム長 160、Asterisk ポート 5060、STUN/TURN ポート 3478 は docker_asterisk_it.rs の既存定数（ASTERISK_SIP_PORT / COTURN_STUN_PORT / IT_EVENT_TIMEOUT）またはテスト内 named constant を参照し、直書きを避ける。
- DockerItPolicy.integration_tests 拡張時、c116_post_policy_encodes_q9_decisions の『len()==5』ハードコードを『== 8』に更新し、マジックナンバーでなく policy 自体を情報源とする意図をコメントで補完する。
- エラー伝播は Result<(), Box<dyn std::error::Error>> と ? に統一し、unwrap() を新規追加しない（既存テストもこの規約に従う）。イベント待ちは wait_for_event / wait_for_raw_sip を再利用してタイムアウト panic を一元化する。
- コメントはプロビナンス注記（[::TICKET::] P19-5 changes. Details: ...）を既存 P19 シリーズのスタイルに合わせて付与し、§62.42 / N0111 の決定がコードから追跡可能にする。Applied: real_pjsip_itest.rs の STUB を RealPjsipItestPolicy（名詞）+ const fn policy()（動詞句）で置換。dtmf_sip_info / dtmf_rfc4733 / register_invite_bye_rtp_flow を動詞句関数として追加し、Asterisk echo 通話確立を共通ヘルパーに抽出。マジックナンバー（DTMF 桁・ポート・タイムアウト）を既存定数参照へ置換。

## Test Plan

### Unit Tests

- UT: [Normal] §62.42 スコープが RealPjsipItestPolicy にデータ化され、実在テスト名と一致する（C150-post / C151-pre / C151-post）— src/tests/real_pjsip_itest.rs に RealPjsipItestPolicy を実装し、policy() が test_file="tests/sip_integration.rs" / feature_gate="pjsua-native" / make_target="test-integration" / docker_gate="docker_available" / skip_message=SKIP_MESSAGE を返すことを assert。protocol_level_tests が [register_against_asterisk（REGISTER→200）、outgoing_call_to_asterisk（INVITE→180/200）、incoming_call_via_originate（Asterisk→siprs 着信 answer 200）、dtmf_sip_info（SIP INFO DTMF）、dtmf_rfc4733（RFC 4733 DTMF）、coturn_stun_turn_ice（STUN binding / TURN allocate / relay）] を列挙し、rtp_level_tests が [register_invite_bye_rtp_flow（2 エンドポイント RTP）] を列挙することを assert。各テスト名が tests/sip_integration.rs の #[tokio::test] fn として実在することを Path 存在 + ファイル内容の contains で assert（C150-post: protocol + RTP levels covered in CI）。src/tests/test_strategy_4layer.rs の TestLayer::Layer3SipIntegration が is_pjsip_free()==false && is_ci_runnable()==true、Layer4Interop が is_ci_runnable()==false であることを assert（C151-pre: §43 4-layer test strategy）。
- UT: [Error] docker 不可時スキップ + make test-integration の guaranteed teardown（C150-inv）— docker_asterisk_it::docker_info_succeeds が None / non-zero exit で false、Some(0) で true を返すことを assert（C117-post 再利用）。std::fs::read_to_string("Makefile") で test-integration ターゲットを読み、`docker compose up -d`・`trap 'docker compose down' EXIT`・`cargo test --features pjsua-native --test sip_integration` の 3 要素を contains で assert。trap の存在が『テスト失敗時も docker compose down を保証する』ことの根拠（C150-inv: make test-integration green + teardown 保証）。Makefile の --test 引数 sip_integration が Cargo.toml [[test]] name="sip_integration" required-features=["pjsua-native"] に一致することを assert。
- UT: [Boundary] DockerItPolicy マトリクス拡張と方向カバレッジ不変条件（C150-pre）— DockerItPolicy::policy().integration_tests.len()==8 を assert（既存 c116_post_policy_encodes_q9_decisions の 5 を 8 へ更新）。追加エントリ dtmf_sip_info（IntegrationDirection::Outbound）/ dtmf_rfc4733（Outbound）/ register_invite_bye_rtp_flow（Outbound）を含み、IntegrationDirection ごとの outbound≥2・inbound≥1・Ice≥1 を assert（C117-inv 拡張）。docker-compose.yml が services asterisk/coturn とポート 5060/5061/3478 を定義することを assert（C150-pre: N0088 が docker Asterisk/coturn base を定義）。
- UT: [Invariant] 実 SIP 検証は pjsua-native + docker ゲートの二段構えで、TestBackend green は検証の証拠にならない（C151-post / C151-inv）— tests/sip_integration.rs 先頭が `#![cfg(feature = "pjsua-native")]` を含むことを assert（C151-post: Layer 4 interop tests run real PJSIP）。新テスト（dtmf_sip_info / dtmf_rfc4733 / register_invite_bye_rtp_flow）の関数冒頭が docker_available() ガードを持つことをファイル内容の contains で assert。raw_sip_real_test.rs の C145-inv（observation_only="PJ_FALSE"、production path steps=[pjsip_module hook → enqueue_raw_sip_bytes → drain_and_publish_raw_sip → subscribe_raw_sip]）を参照し、pjsip_module フックは pjsua-native 配下のみで登録され TestBackend の subscribe_raw_sip() は無音チャネルのまま（Q17 仕様）であることを assert。これにより『TestBackend green ≠ real SIP verified』を不変条件として固定（C151-inv）。
- UT: [Normal] resolve_transport_id（TS-001）の実 transport-id 解決が純粋述語として検証可能（C150-post / C151-post）— src/ffi/callback.rs:469 の [::STUB::] P19-5 を解決する。pjsua-native ブランチから純粋述語 match_transport_id(transports: &[(u32, &str)], target: &str) -> u32 を抽出し、デフォルトビルドで単体テスト: (1) 一致する (id, local_name) が存在すれば該当 id を返す、(2) 一致なしは 0、(3) 空リストは 0、(4) 複数候補は最初の一致（昇順）を返す。pjsua-native ラッパーは pjsua_enum_transports + pjsua_transport_get_info で (id, local_name) を収集し、実 pjsip_transport の local_name/key から target を導出して純粋述語へ委譲する。既存の非 native ブランチ（(*tp).id）と on_transport_state テスト（id: 7 → TransportStateChanged{transport_id: 7}）は green を維持。on_transport_state が実 pjsua_transport_id を運ぶことで STUN/TURN/ICE のプロトコルレベル検証（C150）と Layer 4 real-PJSIP 経路（C151）に寄与。

### Integration Tests

- IT: [pjsua-native] REGISTER→200 / INVITE→180/200 / BYE が実 PJSIP で成立する — tests/sip_integration.rs の register_against_asterisk（REGISTER→RegistrationState::Registered）/ outgoing_call_to_asterisk（INVITE→CallConnected→MediaActive→hangup→CallDisconnected）/ incoming_call_via_originate（Asterisk channel originate→IncomingCall→answer(200)→CallConnected）
- IT: [pjsua-native] SIP INFO / RFC 4733 DTMF が実 PJSIP で成立する — 本チケットで追加する dtmf_sip_info（DtmfMethod::Info）と dtmf_rfc4733（DtmfMethod::Rfc4733）。Asterisk echo 通話を確立後、client.send_dtmf(call_id, digits, method) を実 PJSIP 経由で送信し、SipEventPayload::DtmfSent(DtmfSentInfo{ method, digit, status: Ok(()), .. }) を受信することを EVENT_TIMEOUT 内で検証。pjsip_status が Some なら送信失敗としてテスト失敗。
- IT: [pjsua-native] STUN binding / TURN allocate（coturn）が成立する — coturn_stun_turn_ice（STUN/TURN/ICE 設定で接続し CallConnected + MediaActive を検証、relay candidate 経由のメディア確立）
- IT: [pjsua-native] 2 エンドポイント間で RTP メディアが送受信される — 本チケットで追加する register_invite_bye_rtp_flow。alice が bob へ INVITE（bob_uri は sip:1002@127.0.0.1:5060）、bob が answer(200)、両者 CallConnected。alice 側で add_audio_source(call_id, MockAsyncAudioSource::new(vec![0i16; 160]), ChannelSelector::Both) により PCM バーストを注入し、bob 側で subscribe_audio(call_id, AudioFormat::new(Hz8000, I16, StereoInOut, 20), 16, Realtime) した AudioTapHandle::recv() が AudioChunkPair{ call_id: bob_call_id } を EVENT_TIMEOUT 内に返すことを検証 — RTP メディアが alice→bob のネットワーク経路で送受信された証明。最後に alice.hangup(call_id, HangupReason::LocalUser) で BYE し CallDisconnected を確認。
- IT: [Integration point] tests/sip_integration.rs（pjsua-native ゲートの統合テストバイナリ）↔ src/tests/docker_asterisk_it.rs（docker_available() 実行時ゲート + DockerItPolicy マトリクス）↔ src/tests/real_pjsip_itest.rs（§62.42 スコープのポリシー、N0111）。DTMF は client.send_dtmf → reactor SendDtmf ディスパッチ → backend.send_dtmf → pjsua_call_send_dtmf（backend_calls.rs:309）→ DtmfSent 二段構え（P17-7）の実 PJSIP 経路。RTP は add_audio_source → handle_add_audio_source（P19-4）→ RustMediaPort conf bridge 登録 → subscribe_audio tap（P19-3）の実経路。
- IT: [Verification] (1) dtmf_sip_info / dtmf_rfc4733: DtmfSent イベントの method/digit/status を assert（Info は "1"、Rfc4733 は "5"）。実 PJSIP が DTMF を RTP/SIP シグナリングとして送出したことの証拠。(2) register_invite_bye_rtp_flow: bob の AudioChunkPair.call_id == bob_call_id を assert — RTP メディアが alice から bob へ conf bridge + ネットワーク経由で到達した証明。(3) 全新テストは docker_available() が false なら [SKIPPED: docker unavailable] を出力して早期 return（Q9c）。
- IT: [Prerequisites] docker compose up -d で asterisk（pjsip.conf / extensions.conf マウント、5060/5061）+ coturn（turnserver.conf マウント、3478）が起動していること（make test-integration が保証）。PJSIP システムインストール + --features pjsua-native。P19-3（conf bridge 駆動の audio tap）、P19-4（AddAudioSource の conf bridge 再登録）、P17-7（DtmfSent 二段構え 500ms タイムアウト）、P16-10（docker 統合基盤）。register_invite_bye_rtp_flow は同一プロセス内 2 SipClient（N0052/N0053 Dual Client パターン）を前提。
- IT: [Related tickets] P16-10（§62.19 docker Asterisk/coturn 基盤、Q9）、P19-1（§62.38 raw SIP 実検証、Q17）、P19-2（§62.39 on_ice_transport_error、Q18）、P19-3（§62.40 push_media_frame 配線、Q19）、P19-4（§62.41 AddAudioSource conf bridge 再登録、Q20）、P17-7（§62.27 DtmfSent 二段構え）、PX-3（conf bridge テスト observable / registered_port_count / conf_connect_pairs）。N0088（62.19）/ N0052（43）/ N0107（62.38）/ N0111（62.42）。

### Exceptions

- item: docker デーモンが利用可能でない環境での make test-integration の実行（実 SIP サーバーとの結合検証）。reason: 統合テストは実 SIP サーバー（docker Asterisk/coturn）と native PJSIP（--features pjsua-native）を必要とし、docker が無い環境では compose サービスを起動できない（外部依存: docker デーモン + コンテナイメージ）。これは not a design defect である — テスト環境の外部条件（docker 可用性）に起因するものであり、決定論的かつ原理的にテスト不能という設計欠陥ではない。テスト自体は docker_available() 実行時ゲートにより [SKIPPED: docker unavailable] を明示出力して早期 return する（Q9c）ため、スキップが可視化される。alternative: CI ランナー（docker 常設）で make test-integration を実行して全テストを通し、ローカルで docker 不可時は明示スキップをログで確認する。
- item: 2 エンドポイント間 RTP で送受信される実音声ペイロードのバイト単位完全一致（alice が注入した PCM サンプル列が bob の受信 RTP で同一内容になることの完全実測）。reason: 実 RTP ペイロードの内容検証は pjsua-native + 実 SIP サーバー + 実 conf bridge でのみ可能。デフォルトビルド（stub conf bridge）では subscribe_audio の tap は無音のまま（§62.40 / H13）であり、単体テストでは tap が駆動されない（cannot test in the default build）— 外部依存（native PJSIP / SIP サーバー）に起因する。これは not a design defect である — §62.2 のレイヤ分離（Layer 2 stub / Layer 3 native）の帰結であり、決定論的かつ原理的にテスト不能という設計欠陥ではない。alternative: AudioChunkPair の到着 + call_id 一致を subscribe_audio で検証し、『media が conf bridge を経由して RTP で送受信された（経路確立）』ことを証明する。実ペイロード内容は make test-integration（tests/sip_integration.rs::register_invite_bye_rtp_flow）の範囲とする。
- item: SIP INFO / RFC 4733 DTMF の受信側（DtmfReceived）完全双方向検証。reason: Asterisk echo アプリは DTMF を自動送出しないため、テスト単独では受信側の DtmfReceived を実 SIP で誘発できない。受信側検証には DTMF を送出する相手エンドポイント（SIPp スクリプト等）が必要であり、docker 環境に存在しない（impossible to test with the current docker stack）。これは not a design defect である — 送信側（DtmfSent）は実 PJSIP で検証可能であり、受信側は将来チケットの SIPp 導入で解決可能な外部依存である。alternative: 送信側 DtmfSent(DtmfSentInfo{ method, digit, status }) の method/digit 検証を本チケットで行い、受信側は M20 Layer 2（MockBackend による NativeEvent→DtmfReceived 変換）で検証済み（N0053 マッピング表）ことを根拠とする。
- item: register_invite_bye_rtp_flow が同一プロセス内 2 SipClient（alice/bob）を同時稼働させる完全実証。reason: 現実装の backend_calls::initialize は pjsua_create() を無条件に呼び、PJSUA はプロセス単位 singleton のため、2 つ目の SipClient::new が pjsua_create 失敗（PJ_EEXISTS 相当）を返す可能性が高い。N0052/N0053 の Dual Client 設計（client_b が PjsuaBackend singleton を共有）は m20_test_dual_client.rs に構造のみ存在し、実装されていない（not yet implemented）— これは本チケットのスコープ（tests/ のみ）にないバックエンド変更を要する。これは not a design defect である — 設計（N0052）は存在し、実装順序の問題であり、テスト不能な設計ではない。alternative: 実装フェーズで (a) 1 SipClient + 2 アカウント（alice_account が bob_account へ INVITE）で RTP メディア送受信を検証する、または (b) PjsuaBackend singleton 共有を実装して RFC 例どおり 2 SipClient で検証する。両案とも make test-integration で実測可能。

### Plan Test Code (concrete code)

- UT: [C150-pre → input schema] N0088 docker base が統合テスト基盤を提供することを検証する入力コード（src/tests/real_pjsip_itest.rs の c150_pre_docker_base_defines_infrastructure()）
  ```rust
  use crate::tests::docker_asterisk_it::{docker_available, DockerItPolicy, SKIP_MESSAGE};
  use std::path::Path;

  // C150-pre: N0088 の DockerItPolicy が compose/services/feature/skip を提供し、
  // docker-compose.yml が asterisk + coturn を定義していること（入力前提）。
  let policy = DockerItPolicy::policy();
  assert_eq!(policy.compose_file, "docker-compose.yml");
  assert_eq!(policy.services, &["asterisk", "coturn"]);
  assert_eq!(policy.feature_gate, "pjsua-native");
  assert_eq!(policy.make_target, "test-integration");
  assert_eq!(policy.skip_message, SKIP_MESSAGE);
  assert!(Path::new("docker-compose.yml").exists(), "compose file must exist");
  assert!(!docker_available() || Path::new("docker-compose.yml").exists());
  ```
- UT: [C150-post → output assertion] protocol + RTP レベルの全テストが列挙され実在することを検証するアサーションコード（c150_post_protocol_and_rtp_levels_listed_and_exist()）
  ```rust
  use crate::tests::real_pjsip_itest::RealPjsipItestPolicy;
  use crate::tests::docker_asterisk_it::DockerItPolicy;

  // C150-post: RealPjsipItestPolicy が protocol_level_tests と rtp_level_tests を列挙し、
  // 各テスト名が tests/sip_integration.rs の #[tokio::test] fn として実在する。
  let policy = RealPjsipItestPolicy::policy();
  assert_eq!(policy.test_file, "tests/sip_integration.rs");
  let src = std::fs::read_to_string("tests/sip_integration.rs").unwrap_or_default();
  for name in policy.protocol_level_tests() {
      assert!(src.contains(name), "{name} must exist in tests/sip_integration.rs");
  }
  for name in policy.rtp_level_tests() {
      assert!(src.contains(name), "{name} must exist in tests/sip_integration.rs");
  }
  // DockerItPolicy マトリクスは 5→8 エントリに拡張済み。
  assert_eq!(DockerItPolicy::policy().integration_tests.len(), 8);
  ```
- UT: [C150-inv → predicate] make test-integration green と guaranteed teardown を検証する不変条件コード（c150_inv_make_test_integration_guarantees_teardown()）
  ```rust
  // C150-inv: Makefile の test-integration ターゲットが compose up → trap で down →
  // pjsua-native テストを実行し、teardown が失敗時も保証される。
  let makefile = std::fs::read_to_string("Makefile").unwrap_or_default();
  assert!(makefile.contains("docker compose up -d"), "compose up required");
  assert!(makefile.contains("trap 'docker compose down' EXIT"), "trap teardown required");
  assert!(makefile.contains("cargo test --features pjsua-native --test sip_integration"));
  // Cargo.toml [[test]] name=sip_integration required-features=["pjsua-native"] と整合。
  let cargo_toml = std::fs::read_to_string("Cargo.toml").unwrap_or_default();
  assert!(cargo_toml.contains("name = \"sip_integration\""));
  assert!(cargo_toml.contains("required-features = [\"pjsua-native\"]"));
  ```
- UT: [C151-pre → input schema] §43 4-layer test strategy の層分離を検証する入力コード（c151_pre_four_layer_strategy_defines_layers()）
  ```rust
  use crate::tests::test_strategy_4layer::TestLayer;

  // C151-pre: §43 の Layer 3（docker SIP / 実 PJSUA）と Layer 4（実 PBX interop）が
  // TestLayer enum に定義されている。
  assert!(!TestLayer::Layer3SipIntegration.is_pjsip_free());
  assert!(TestLayer::Layer3SipIntegration.is_ci_runnable());
  assert!(!TestLayer::Layer4Interop.is_ci_runnable());
  assert!(TestLayer::Layer3SipIntegration.description().contains("Docker Asterisk"));
  ```
- UT: [C151-post → output assertion] Layer 4 interop テストが実 PJSIP で実行されることを検証するアサーションコード（c151_post_layer4_runs_real_pjsip()）
  ```rust
  use crate::tests::real_pjsip_itest::RealPjsipItestPolicy;

  // C151-post: 統合テストは pjsua-native コンパイル時ゲート + docker 実行時ゲートで
  // 実 PJSIP 経路のみを実行する。
  let policy = RealPjsipItestPolicy::policy();
  assert_eq!(policy.feature_gate, "pjsua-native");
  assert_eq!(policy.docker_gate, "docker_available");
  let src = std::fs::read_to_string("tests/sip_integration.rs").unwrap_or_default();
  assert!(src.contains("#![cfg(feature = \"pjsua-native\")]"), "compile-time gate required");
  assert!(src.contains("docker_available()"), "runtime docker gate required");
  ```
- UT: [C151-inv → predicate] TestBackend green ≠ real SIP verified を検証する不変条件コード（c151_inv_testbackend_green_not_real_sip_verification()）
  ```rust
  use crate::tests::raw_sip_real_test::RawSipRealTestPolicy;
  use crate::tests::docker_asterisk_it::SKIP_MESSAGE;

  // C151-inv: pjsip_module フックは pjsua-native 配下のみ登録され（Q17）、
  // TestBackend の subscribe_raw_sip() は無音チャネルのまま。TestBackend green は
  // 実 SIP 検証の証拠にならない。
  let raw = RawSipRealTestPolicy::policy();
  assert_eq!(raw.observation_only, "PJ_FALSE");
  assert_eq!(raw.production_path_steps(), &[
      "pjsip_module hook",
      "enqueue_raw_sip_bytes",
      "drain_and_publish_raw_sip",
      "subscribe_raw_sip",
  ]);
  let src = std::fs::read_to_string("tests/sip_integration.rs").unwrap_or_default();
  for new_test in ["dtmf_sip_info", "dtmf_rfc4733", "register_invite_bye_rtp_flow"] {
      assert!(src.contains(new_test), "{new_test} must be docker-gated in sip_integration.rs");
  }
  assert_eq!(SKIP_MESSAGE, "[SKIPPED: docker unavailable]");
  ```
- UT: [TS-001 → predicate] resolve_transport_id の実 transport-id マッチを検証する純粋述語コード（src/ffi/callback.rs の match_transport_id ユニットテスト）
  ```rust
  // TS-001: 純粋述語 match_transport_id は (id, local_name) リストから target と一致する
  // transport id を返す（デフォルトビルドでテスト可能、FFI 非依存）。
  // pjsua-native ラッパーは pjsua_enum_transports + pjsua_transport_get_info で
  // このリストを収集して委譲する。
  assert_eq!(match_transport_id(
      &[(1, "udp:127.0.0.1:5060"), (2, "udp:127.0.0.1:5061")],
      "udp:127.0.0.1:5061",
  ), 2, "matching transport id returned");
  assert_eq!(match_transport_id(&[], "udp:127.0.0.1:5060"), 0, "empty list -> 0");
  assert_eq!(match_transport_id(
      &[(3, "udp:127.0.0.1:5060")],
      "udp:127.0.0.1:9999",
  ), 0, "no match -> 0 (pre-resolution fallback)");
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| sip_integration.rs / docker_asterisk_it.rs は pjsua-native ビルド不能で実行不可 | プロトコル + 通信（RTP）レベルの実統合テストを実装し make test-integration で CI 実行 | 実 PJSIP 統合テスト（§62.42） |
| src/tests/real_pjsip_itest.rs は [::STUB::] P19-5 のみの orphan ファイル | RealPjsipItestPolicy（N0111 §62.42 スコープのデータ化）+ C150/C151 単体テスト 6 本を実装し src/tests/mod.rs に宣言 | TS-002 解決（N0111 実装ターゲット） |
| src/ffi/callback.rs の pjsua-native resolve_transport_id は常に 0 を返す（[::STUB::] P19-5） | match_transport_id 純粋述語 + pjsua_enum_transports/pjsua_transport_get_info による実 transport-id 解決を実装。allowlist に pjsua_transport_info / pjsua_enum_transports / pjsua_transport_get_info を追加 | TS-001 解決（on_transport_state の実 transport id） |
| DockerItPolicy.integration_tests は 5 エントリ | dtmf_sip_info / dtmf_rfc4733 / register_invite_bye_rtp_flow を追加し 8 エントリに拡張、c116_post の len アサートを更新 | §62.42 テストのマトリクス登録 |
| SIP INFO / RFC 4733 DTMF と 2 エンドポイント間 RTP の実統合テストが存在しない | tests/sip_integration.rs に dtmf_sip_info / dtmf_rfc4733 / register_invite_bye_rtp_flow を追加（pjsua-native + docker ゲート、DtmfSent / AudioChunkPair 検証） | H15 RESIDUE 解消（§62.42 の 3 ギャップ） |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) src/tests/real_pjsip_itest.rs: RealPjsipItestPolicy を実装（test_file / feature_gate / make_target / docker_gate / skip_message / protocol_level_tests / rtp_level_tests を const fn policy() で提供）+ C150/C151 のユニットテスト 5 本（Normal×2 / Error / Boundary / Invariant）を同ファイルに配置。(2) src/tests/mod.rs に pub mod real_pjsip_itest; を追加。(3) src/tests/docker_asterisk_it.rs: DockerItPolicy.integration_tests に dtmf_sip_info / dtmf_rfc4733 / register_invite_bye_rtp_flow を追加し、c116_post_policy_encodes_q9_decisions の len アサートを 5→8 に更新。(4) src/ffi/callback.rs: resolve_transport_id（pjsua-native）の [::STUB::] P19-5 を解決 — 純粋述語 match_transport_id(transports: &[(u32, &str)], target: &str) -> u32 を抽出してユニットテスト（一致 / 不一致→0 / 空→0 / 複数→最初）を配置し、pjsua-native ラッパーは pjsua_enum_transports + pjsua_transport_get_info で (id, local_name) を収集し target を実 pjsip_transport から導出して委譲。非 native ブランチと既存 on_transport_state テスト（id: 7）は green 維持。(5) tests/sip_integration.rs: dtmf_sip_info（DtmfMethod::Info、digit '1'）を追加 — registered_account で Asterisk 登録 → outgoing_call → CallConnected/MediaActive → send_dtmf → DtmfSent{method: Info, digit: '1', status: Ok(())} を wait_for_event で検証 → hangup。(6) tests/sip_integration.rs: dtmf_rfc4733（DtmfMethod::Rfc4733、digit '5'）を同様に追加。(7) tests/sip_integration.rs: register_invite_bye_rtp_flow を追加 — alice/bob 2 アカウント（1 SipClient または DualClient）で REGISTER → alice make_call(bob_uri) → bob answer(200) → 両者 CallConnected → alice add_audio_source(MockAsyncAudioSource) → bob subscribe_audio → AudioChunkPair{call_id: bob_call_id} 受信 → alice hangup → 両者 CallDisconnected。(8) make test（--features test-util）で policy + match_transport_id 単体テスト green、make test-integration（--features pjsua-native）で新 IT green を確認。
- [Risks] (a)【最重要】2 エンドポイント RTP の dual-client 未対応: RFC 例は同一プロセス 2 SipClient 前提だが、backend_calls::initialize（src/ffi/backend_calls.rs:43）が pjsua_create() を無条件に呼び、PJSUA はプロセス単位 singleton のため 2 つ目の SipClient::new が失敗する可能性が高い。register_invite_bye_rtp_flow は 1 SipClient + 2 アカウントで RTP 送受信を検証する代替を基本とし、2 SipClient での検証は Dual Client 実装（将来チケット）後に追加する。(b) DtmfSent の 500ms タイムアウトフォールバック（P17-7）により、実 PJSIP の send_dtmf が失敗しても DtmfSent は Ok で発火し得る。テストは digit / method に加え status / pjsip_status を assert して送信失敗を検出する。(c) DockerItPolicy マトリクス拡張で c116_post の len アサート（現 5）を更新しないと既存テストが red になる。(d) resolve_transport_id の target 導出は実 pjsip_transport のフィールド（local_name/key）依存のため pjsua-native でのみコンパイル — 純粋述語 match_transport_id に分離しないとデフォルトビルドでテスト不能になる。
- [Caveats] チケット scope の src/tests/docker_asterisk_it.rs 変更は DockerItPolicy マトリクス拡張が実体。src/tests/real_pjsip_itest.rs が N0111 の実装ターゲット（現在 [::STUB::] P19-5、src/tests/mod.rs に未宣言）。make test-integration は --test sip_integration のみ実行するため、新テストは必ず tests/sip_integration.rs に追加する（別バイナリに追加しても実行されない）。DockerItPolicy マトリクスは現在 5 エントリで P19-3/P19-4 の conf_bridge_drives_audio_tap / add_audio_source_reaches_conf_bridge を含まない — 本チケットでは 3 テストを追加し 8 エントリにする（P19-3/P19-4 テストのマトリクス登録は範囲外とし spec に記録）。callback.rs の resolve_transport_id は enumerate-ticket-targets が TS-001 として検出した [::STUB::] P19-5 — チケット本来のスコープに無いが、make gate（Step 8b）が contracts 紐付け + 解決計画を要求するため spec に含める。
- [Open items] dual-client 対応の要否（register_invite_bye_rtp_flow を RFC 例の 2 SipClient で実現するか、1 SipClient + 2 アカウントで代替するか）。DtmfReceived 双方向検証（Asterisk 側から DTMF を送出する SIPp シナリオ、将来チケット）。EXAMPLES（E1-E5）の実 Asterisk 動作検証は本チケット対象外。resolve_transport_id の target 導出に使う実 pjsip_transport フィールド名（local_name vs key）は pjsua-native 実ヘッダ確認後確定。
- [Future improvements] DualClientContext（m20_test_dual_client.rs）の実装を伴う真の 2 クライアント RTP テスト。RTP ペイロード内容（PCM バイト列）の完全一致検証。SIP INFO / RFC 4733 の受信側（DtmfReceived）実 SIP 検証。P19-3/P19-4 追加テストの DockerItPolicy マトリクスへのバックフィル登録。on_transport_state の transport_id を利用したトランスポート状態検証テスト。
Implementation summary (P19-5):
- Changed files: src/tests/real_pjsip_itest.rs (implemented RealPjsipItestPolicy + 6 C150/C151 unit tests), src/tests/mod.rs (pub mod real_pjsip_itest), src/tests/docker_asterisk_it.rs (DockerItPolicy.integration_tests 5→8 + c116_post len assert), src/ffi/callback.rs (resolved TS-001: match_transport_id pure predicate + pjsua-native resolve_transport_id via pjsua_enum_transports/pjsua_transport_get_info + SAFETY comments + 3 match_transport_id unit tests), src/build/build_script_bindgen.rs (allowlist: pjsua_transport_info / pjsua_enum_transports / pjsua_transport_get_info), tests/sip_integration.rs (dtmf_sip_info / dtmf_rfc4733 / register_invite_bye_rtp_flow + establish_asterisk_echo_call helper).
- Key behavior: §62.42 (N0111) real-PJSIP protocol + RTP integration scope is now encoded as policy data and covered by 3 new docker-gated integration tests; on_transport_state now carries a real pjsua_transport_id instead of 0.
- Test results: cargo test --features test-util -- --test-threads=1 → 1398 lib tests + all integration binaries green (0 failures). 6 new real_pjsip_itest tests + 3 new match_transport_id tests + updated docker_asterisk_it (9) + build_script_bindgen (25) all pass. cargo check --features pjsua-native --lib and --test sip_integration compile green.
- ENV LIMITATION: make test-integration could NOT be completed in this session because docker pull for asterisk:20.6.0 / coturn/coturn:4.6 hangs (registry unreachable in this environment). The integration tests compile under pjsua-native and are docker-gated (docker_available()); runtime verification against the compose stack must run where image pulls succeed (CI).
- Pre-existing flaky note: tests/ffi/callback.rs share global statics and fail intermittently in parallel runs (proven pre-existing at clean HEAD, documented in P19-4); serial run (--test-threads=1) is fully green.
- Quality checks: 59 pre-existing findings (unwrap/expect in test modules of build_script_bindgen.rs/callback.rs, unsafe blocks in the FFI callback bridge) — none introduced by P19-5 lines; all new unsafe blocks carry SAFETY comments.
Review report (P19-5):
- Static quality check: passed (59 pre-existing findings, 0 introduced by P19-5).
- Translatability: passed (all new functions verb-phrase, no magic numbers, no debug output, comments explain why).
- Annotation verify: 6 changed source files all carry P19-5 [::TICKET::] provenance; 0 missing, 0 AMBIGUOUS.
- Crimes: 0. Stubs: P19-5 has 0 remaining (TS-001/TS-002 resolved); 2 P19-6 stubs correctly deferred to P19-6. No-excuse gate: 2/2 pass.
- Tests: 1398 lib + all integration binaries green (serial). cargo check default / pjsua-native lib / pjsua-native sip_integration all green.
- Issues found and fixes applied: (1) register_invite_bye_rtp_flow injected add_audio_source into bob's call instead of alice's — fixed to inject into alice_call_id and subscribe bob's tap, so the test genuinely verifies the alice→bob RTP network path (injected source → alice conf port → RTP → bob conf port → tap). (2) Confirmed the DTMF tests assert method/digit/status on DtmfSent and are docker-gated.
- Verification method: N0111 (§62.42) requirements checked against RealPjsipItestPolicy (protocol_level_tests / rtp_level_tests), the 3 new docker-gated integration tests, and the pjsua-native transport-id resolution (match_transport_id pure predicate + pjsua_enum_transports/pjsua_transport_get_info wrapper). C150/C151 pre/post/inv all @verifies-annotated.
- ENV LIMITATION: make test-integration not runnable here (docker registry unreachable; image pull hangs). The docker-gated ITs compile and are correctly gated; they must run in CI.

## P19-5 — implemented at 30 locations

### src/build/build_script_bindgen.rs

- Line 27
```rust
    // [::TICKET::] P16-7 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P16-7 --for-spec --no-implementation-order`.
```

- Line 130
```rust
    // [::TICKET::] P16-7 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P16-7 --for-spec --no-implementation-order`.
```

### src/ffi/callback.rs

- Line 470
```rust
fn resolve_transport_id(tp: *mut bindings::pjsip_transport) -> u32 {
```

- Line 491
```rust
fn match_transport_id(transports: &[(u32, &str)], target: &str) -> u32 {
```

- Line 506
```rust
unsafe fn host_port_of(name: &bindings::pjsip_host_port) -> String {
```

- Line 517
```rust
unsafe fn read_pj_str(text: &bindings::pj_str_t) -> String {
```

- Line 532
```rust
fn enumerate_pjsua_transports() -> Vec<(u32, String)> {
```

- Line 1013
```rust
    fn match_transport_id_returns_matching_id() {
```

- Line 1022
```rust
    fn match_transport_id_empty_list_falls_back_to_zero() {
```

- Line 1029
```rust
    fn match_transport_id_no_match_falls_back_to_zero() {
```

### src/tests/docker_asterisk_it.rs

- Line 106
```rust
impl DockerItPolicy {
```

- Line 220
```rust
    fn c116_post_policy_encodes_q9_decisions() {
```

### src/tests/mod.rs

- Line 16
```rust
pub mod real_pjsip_itest;
```

- Line 18
```rust
// [::TICKET::] P1-3 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P1-3 --for-spec --no-implementation-order`.
```

### src/tests/real_pjsip_itest.rs

- Line 80
```rust
impl RealPjsipItestPolicy {
```

- Line 106
```rust
    fn sip_integration_src() -> String {
```

- Line 116
```rust
    fn c150_pre_docker_base_defines_infrastructure() {
```

- Line 130
```rust
    fn c150_post_protocol_and_rtp_levels_listed_and_exist() {
```

- Line 154
```rust
    fn c150_inv_make_test_integration_guarantees_teardown() {
```

- Line 172
```rust
    fn c151_pre_four_layer_strategy_defines_layers() {
```

- Line 185
```rust
    fn c151_post_layer4_runs_real_pjsip() {
```

- Line 198
```rust
    fn c151_inv_testbackend_green_not_real_sip_verification() {
```

### tests/sip_integration.rs

- Line 546
```rust
async fn establish_asterisk_echo_call(
```

- Line 548
```rust
    client: &SipClient,
```

- Line 583
```rust
async fn dtmf_sip_info() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 585
```rust
    if !docker_available() {
```

- Line 625
```rust
async fn dtmf_rfc4733() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 627
```rust
    if !docker_available() {
```

- Line 671
```rust
async fn register_invite_bye_rtp_flow() -> Result<(), Box<dyn std::error::Error>> {
```

- Line 673
```rust
    if !docker_available() {
```
