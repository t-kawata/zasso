# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P19-6: Round 4 チケット構造 & I/O 境界（§62.43–62.44）

**Ticket Key**: P19-6 · **Phase**: 19

**RFC Source**: `RFC-ROOT.md`

---

## Background

設計ブリーフ §5.5 の 2 チケット構造とユーザー指示（A/B 最優先、ギャップは後に連ねる）を round 4 チケット配置として確定。I/O 境界は graphify/boundify の分割判断用。

### Goal
§62.43（N0112）のチケット構造（phase 18: Ticket A/B、phase 19 以降: H8/H13/H14/H15/EXAMPLES）を Round4TicketSpec enum と round4_ticket_ordering() として Rust の型付き定数に符号化し、§62.44（N0113）の I/O 境界表（13 設計判断: 62.31–62.43）を Round4IoBoundaryRow / ROUND4_IO_BOUNDARY_TABLE として round-3 の io_boundary_round3.rs パターンで round-4 に適用する。producer（crates/pjsip-prebuilt / Dockerfile / prebuilt.yml workflow）と consumer（build.rs / build_script_bindgen / bindings 等の 9 ファイル修復）の分割を型付き定数として確定する。

### Purpose
graphify/boundify が round-4 のチケット配置（A/B 最優先 + ギャップ）と各設計判断（62.31–62.43）の分割・Prune 判断に使う参照情報（入力・出力・関連ファイル候補・関連グラフノード・削除/更新対象）を機械可読にする。round-2 §62.20（io_boundary_round2）→ round-3 §62.30（io_boundary_round3）→ round-4 §62.44（io_boundary_round4）と連なる参照チェーンを完成させ、チケット順序（A/B 並列可・ギャップは後に連ねる）を Tickets.json の phase 18–19 配置と整合する形で符号化する。

### Motivation
round-4 実装（P18-1 Ticket A §62.31–62.35、P18-2 Ticket B §62.36–62.37、P19-1..P19-5 §62.38–62.42）が完了・reviewed 済みであり、§62.43–62.44 の参照情報が src/architecture/round4_tickets.rs と io_boundary_round4.rs の STUB（[::STUB::] P19-6 マーカーのみ、各 21 行）として未符号化のまま残っている。P18-2（producer）では crates/ ディレクトリと .github/workflows/prebuilt.yml が未生成のため、round-4 の I/O 境界参照には「未生成パス」の扱いが round-3 と異なる新規要素として含まれる。

### Constraints
- round-3 の io_boundary_round3.rs は decision に round3_scope_rootcause::Round3Section を使用するが、round4_scope_rootcause.rs には Round4Section enum が存在しない（P18-1 は ROUND4_SCOPE_TARGETS / is_round4_scope_target のみ定義）。round-3 実装（io_boundary_round3.rs）と round-1/round-2 実装（impl_integration_design.rs / io_boundary_round2.rs）は変更せず、round-4 用の節 enum を io_boundary_round4.rs 内に自己完結で定義する。
- 実ファイルの削除は行わない。本チケットは削除/更新対象の参照符号化のみを担当し、実際の Prune は boundify（外部ツール）が行う。
- 新規依存クレート・設定・スキーマ変更なし。対象は src/architecture/round4_tickets.rs（add）・src/architecture/io_boundary_round4.rs（add）・src/architecture/mod.rs（モジュール宣言 2 行 + Sub-modules コメント更新）のみ。
- Initial Design Artifact ヘッダ（冒頭の `// =====...` ブロック）は絶対に編集しない。

## Scope

- 【変更1-path】src/architecture/round4_tickets.rs（add）、src/architecture/io_boundary_round4.rs（add）、src/architecture/mod.rs（modify）
- 【変更1-action】round4_tickets.rs は add（STUB → 実装）、io_boundary_round4.rs は add（STUB → 実装）、mod.rs は modify（モジュール宣言 2 行 + Sub-modules コメント更新）。実ファイル削除なし。
- 【変更2-path】src/architecture/io_boundary_round4.rs
- 【変更2-action】add: round 4 の I/O 境界参照情報（新規 crates/pjsip-prebuilt / Dockerfile / workflow と更新ファイル群の列挙）。
- **Scope of changes:**
  - [1-path] src/architecture/round4_tickets.rs（add）
  - [1-action] add: Round4TicketSpec enum（phase 18: Ticket A/B、phase 19: H8/H13/H14/H15/EXAMPLES）+ round4_ticket_ordering()（A/B 最優先、ギャップは後に連ねる）+ ticket_key()/phase()/section() 各メソッド + #[cfg(test)] テスト群
  - [1-detail] §62.43 のチケット構造を 7 変種の enum（TicketAConsumerBindgen→P18-1 §62.31–62.35 / TicketBProducerPrebuilt→P18-2 §62.36–62.37 / H8RawSipIntegration→P19-1 §62.38 / H8IceTransportError→P19-2 §62.39 / H13PushMediaFrame→P19-3 §62.40 / H14ConfBridgeReregister→P19-4 §62.41 / H15ExamplesRealPjsip→P19-5 §62.42）として符号化。round4_ticket_ordering() が [Ticket A, Ticket B, H8-rawSIP, H8-ICE, H13, H14, H15/EXAMPLES] の順（A/B 最優先、ギャップは後に連ねる）を返す。
  - [1-before-after] round4_tickets.rs: [::STUB::] マーカーのみ（21 行）→ Round4TicketSpec enum + round4_ticket_ordering(): [Round4TicketSpec; 7] + ticket_key()/phase()/section() + テスト群。mod.rs: round4_tickets / io_boundary_round4 未宣言 → 宣言追加。
  - [1-api] crate::architecture::round4_tickets 配下に新規 pub 型/関数（Round4TicketSpec / round4_ticket_ordering / ticket_key / phase / section）。round-1/2/3 の実装ファイルは変更なし（互換維持）。
  - [1-schema] データスキーマ変更なし（Rust の型付き定数のみ。シリアライズ・DB・外部スキーマ変更なし）。
  - [1-config] 設定変更なし（環境変数・設定ファイル変更なし）。
  - [1-dep] 依存クレート追加なし。
  - [2-path] src/architecture/io_boundary_round4.rs（add）
  - [2-action] add: Round4Section enum（62.31–62.44 の 14 変種, section()/node_id()/label() 付き）/ Round4IoBoundaryRow{decision, consumes, produces, file_candidates, graph_node_ids} / Round4DeleteTarget{path, description, resolved_by} / ROUND4_IO_BOUNDARY_TABLE: [Round4IoBoundaryRow; 13] / ROUND4_DELETE_TARGETS: [Round4DeleteTarget; 4] / ROUND4_PRODUCER_SECTIONS / ROUND4_CONSUMER_SECTIONS / ROUND4_NOT_YET_CREATED_PATHS / known_round4_graph_node_ids() + #[cfg(test)] テスト群
  - [2-detail] §62.44 の I/O 境界表（13 設計判断 62.31–62.43）を Round4IoBoundaryRow として符号化。producer 節（62.36 producer ツール / 62.37 CI コミット）と consumer 節（62.31–62.35）を ROUND4_PRODUCER_SECTIONS / ROUND4_CONSUMER_SECTIONS で分離。削除/更新対象を Round4DeleteTarget（4 件: build.rs static=pjsua2 単独リンク / PJSUA_CALL_NULL bindings 依存 / PJ_CRED_DATA_PLAIN_PASSWD allowlist / pjsua_codec_info 直接参照）として整理。未生成パス（crates/pjsip-prebuilt/・crates/pjsip-prebuilt/Dockerfile・.github/workflows/prebuilt.yml）を ROUND4_NOT_YET_CREATED_PATHS で明示。
  - [2-before-after] io_boundary_round4.rs: [::STUB::] マーカーのみ（21 行）→ ROUND4_IO_BOUNDARY_TABLE: [Round4IoBoundaryRow; 13] と ROUND4_DELETE_TARGETS: [Round4DeleteTarget; 4] と producer/consumer 分離定数と ROUND4_NOT_YET_CREATED_PATHS と known_round4_graph_node_ids() とテスト群。
  - [2-api] crate::architecture::io_boundary_round4 配下に新規 pub 型/定数。round-1/2/3 の I/O 境界実装（impl_integration_design.rs / io_boundary_round2.rs / io_boundary_round3.rs）は変更なし（互換維持）。
  - [2-schema] データスキーマ変更なし。
  - [2-config] 設定変更なし。
  - [2-dep] 依存クレート追加なし。
  - [3-path] src/architecture/mod.rs（modify）
  - [3-action] modify: pub mod round4_tickets; と pub mod io_boundary_round4; の宣言追加 + [::TICKET::] P19-6 アノテーション + Sub-modules コメント更新。
  - [3-detail] mod.rs の「Sub-modules:」ヘッダ一覧に round4_tickets と io_boundary_round4 を追記し、コメントが嘘にならないよう最新化（P17-10 が io_boundary_round3 を追記したのと同様の修正）。
- **Out of scope:**
  - [Excluded] round-4 の実装チケット本体（P18-1 Ticket A / P18-2 Ticket B / P19-1..P19-5）の実装コード変更、crates/pjsip-prebuilt の実体生成、prebuilt.yml workflow の実体作成、vendor/prebuilt/<target> の実体生成、round4_scope_rootcause.rs（P18-1 成果物）の変更
  - [Why] これらは各実装チケット（P18-1/P18-2/P19-1..P19-5）の責務であり、本チケットはそれらの参照符号化（チケット構造・I/O 境界）のみを担当する。round4_scope_rootcause.rs は P18-1 の所有物であり、Round4Section enum を追加せず io_boundary_round4.rs 内に自己完結で定義する（責務分離維持）。
- **Affected areas:**
  - [Component] src/architecture/mod.rs（pub mod round4_tickets; / pub mod io_boundary_round4; 追加）、graphify/boundify ツール（round-4 の分割・Prune 判断に新表・新チケット順序を参照可能）、Tickets.json の phase 18/19 チケット（P18-1/P18-2/P19-1..P19-5 のキー整合の検証元）
  - [Nature] API surface（architecture モジュールに新規公開型・定数を追加、既存 API は不変）。参照データの一元化により graphify/boundify の round-4 対応が容易になる。実行時性能・セキュリティ・データ形式への影響なし。
  - [Response] mod.rs に pub mod round4_tickets; と pub mod io_boundary_round4; を追加（必須）。boundify 側の参照は追加のみで既存挙動に影響なし（Y, mod.rs のみ）。

## STUBs — Must Be Fully Implemented in This Ticket (2 items — resolved: 2)

### TS-001 — `src/architecture/io_boundary_round4.rs — resolved

- **Marker**: `// [::TICKET::] P19-6 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P19-6 --for-spec --no-implementation-order`.`
- **Contracts**: C153
- **Resolution Plan**: Resolved: implemented ROUND4_IO_BOUNDARY_TABLE (13 rows §62.31-62.43) with consumes/produces/file_candidates/graph_node_ids, ROUND4_DELETE_TARGETS (4 prune targets pre-resolved by P18-1), ROUND4_PRODUCER_SECTIONS/ROUND4_CONSUMER_SECTIONS split, ROUND4_NOT_YET_CREATED_PATHS, known_round4_graph_node_ids(); registered pub mod io_boundary_round4 in mod.rs

### TS-002 — `src/architecture/round4_tickets.rs — resolved

- **Marker**: `// [::TICKET::] P19-6 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=P19-6 --for-spec --no-implementation-order`.`
- **Contracts**: C152
- **Resolution Plan**: Resolved: implemented Round4TicketSpec enum (7 variants P18-1/A, P18-2/B, P19-1..P19-5) with ticket_key()/phase()/section(), round4_ticket_ordering() returning [Round4TicketSpec; 7] with A/B first then gaps; registered pub mod round4_tickets in mod.rs

## To show related RFC graph details

### Usage of query.js

```
node .claude/scripts/rfc-graph/query.js --graph="RFC-ROOT-GRAPH.json" --source="RFC-ROOT.md" --dirs-tree="RFC-ROOT-Dirs-Tree.json" --id=Nxxxx (NODE-ID, e.g. N0001) --hops=<N> (hop count: 1=direct edges only, 2+=includes grandchildren, etc.)
```

### Related RFC graph NODE-IDs to check

`N0112` `N0113`

### Related Nodes

| Node ID | Kind | Language | Title |
|----|------|----------|-------|
| `N0112` | architecture | rust | 62.43 チケット構造とフェーズ割当（Q22） |
| `N0113` | architecture | rust | 62.44 I/O 境界参照情報（graphify / boundify 用）— round 4 |

### Edge Relationships

| Type | From | → | To |
|------|------|----|-----|
| references | ★ 62.43 チケット構造とフェーズ割当（Q22） (N0112) | → | ☆ 62.31 ラウンド 4 進化スコープと根因（Q9–Q22） (N0100) |
| references | ★ 62.44 I/O 境界参照情報（graphify / boundify 用）— round 4 (N0113) | → | ☆ 62.30 I/O 境界参照情報（graphify / boundify 用）— round 3 (N0099) |

### Implementation Target File Paths

| Node ID | File Path |
|---------|-----------|
| `N0112` | `src/architecture/round4_tickets.rs` |
| `N0113` | `src/architecture/io_boundary_round4.rs` |

## Investigation

- RFC §62.43（RFC-ROOT.md / N0112）: round 4 として phase 18 に Ticket A（消費者/bindgen 整合 §62.31–62.35）と Ticket B（生産者/prebuilt + CI + コミット §62.36–62.37）を最優先配置（並列可）、phase 19 以降に H8（raw SIP 実統合テスト + on_ice_transport_error §62.38–62.39）/ H13（push_media_frame §62.40）/ H14（AddAudioSource 再登録 §62.41）/ H15 / EXAMPLES（実 PJSIP 統合テスト §62.42）のギャップチケットを順に連ねる（ユーザー指示）。
- RFC §62.44（RFC-ROOT.md / N0113）: round-4 の各設計判断（62.31–62.43）が graphify / boundify の分割判断に使う I/O 境界表（設計判断 / 入力(consumes) / 出力(produces) / 関連 GRAPH ノード・ファイル候補）。新規追加ファイル: crates/pjsip-prebuilt/（producer ツール + Cargo.toml + main.rs）、crates/pjsip-prebuilt/Dockerfile、.github/workflows/prebuilt.yml。更新ファイル: build.rs / src/build/build_script_bindgen.rs / src/ffi/bindings.rs / src/ffi/backend_calls.rs / src/ffi/callback.rs / src/config/observability_metrics.rs / src/config/stun_turn_ice_wiring.rs / src/state/m20_callstate_mapping.rs / src/runtime/backend.rs / src/runtime/command.rs / tests/sip_integration.rs / src/tests/docker_asterisk_it.rs / vendor/prebuilt/*/。削除/整理対象（boundify が Prune / 更新する箇所）: build.rs の static=pjsua2 単独リンク、PJSUA_CALL_NULL の bindings 依存、PJ_CRED_DATA_PLAIN_PASSWD の allowlist エントリ、pjsua_codec_info の encoding_name / clock_rate 直接参照。
- round-4 ノード対応（RFC-ROOT-GRAPH.json）: N0100=62.31 / N0101=62.32 / N0102=62.33 / N0103=62.34 / N0104=62.35 / N0105=62.36 / N0106=62.37 / N0107=62.38 / N0108=62.39 / N0109=62.40 / N0110=62.41 / N0111=62.42 / N0112=62.43 / N0113=62.44。references エッジ: N0100→N0090,N0039 / N0101→N0039 / N0102→N0080 / N0103→N0039 / N0104→N0039 / N0105→N0054 / N0106→N0054 / N0107→N0091,N0088 / N0108→N0092 / N0109→N0097 / N0110→N0085 / N0111→N0088,N0052 / N0112→N0100 / N0113→N0099。契約: C152（N0112→N0100, references）/ C153（N0113→N0099, references）。
- Tickets.json の phase 配置: phase 18 = P18-1（Ticket A・consumer/bindgen alignment §62.31–62.35, reviewed）、P18-2（Ticket B・producer crates/pjsip-prebuilt + CI commit §62.36–62.37, reviewed）。phase 19 = P19-1（H8 raw SIP §62.38, reviewed）/ P19-2（H8 on_ice_transport_error §62.39, reviewed）/ P19-3（H13 push_media_frame §62.40, reviewed）/ P19-4（H14 AddAudioSource conf 再登録 §62.41, reviewed）/ P19-5（H15/EXAMPLES §62.42, reviewed）/ P19-6（本チケット, todo）。
- round-3 参照モデル（§62.30, N0099）: src/architecture/io_boundary_round3.rs に Round3IoBoundaryRow{decision: Round3Section, consumes, produces, file_candidates, graph_node_ids} / ROUND3_IO_BOUNDARY_TABLE: [Round3IoBoundaryRow; 8] / Round3DeleteTarget{path, description, resolved_by} / ROUND3_DELETE_TARGETS: [Round3DeleteTarget; 4] / known_round3_graph_node_ids()。decision は round3_scope_rootcause::Round3Section（10 変種, 62.21–62.30, section()/node_id()/label() 付き）を使用。
- round-2 参照モデル（§62.20, N0089）: src/architecture/io_boundary_round2.rs に Round2IoBoundaryRow / ROUND2_IO_BOUNDARY_TABLE: [Round2IoBoundaryRow; 10] / Round2DeleteTarget / ROUND2_DELETE_TARGETS: [Round2DeleteTarget; 3]。
- round-4 scope 実装（P18-1 §62.31）: src/architecture/round4_scope_rootcause.rs に ROUND4_SCOPE_TARGETS（9 ファイル: build.rs / src/build/build_script_bindgen.rs / src/ffi/constants.rs / src/ffi/backend_calls.rs / src/ffi/callback.rs / src/config/observability_metrics.rs / src/state/m20_callstate_mapping.rs / src/runtime/backend.rs / src/config/stun_turn_ice_wiring.rs）/ OBSERVED_ERROR_CATEGORY_COUNT（15）/ is_round4_scope_target()。Round4Section enum は未定義（io_boundary_round4.rs 内に自己完結で定義する必要あり）。
- 対象ファイルの現状: src/architecture/round4_tickets.rs は 21 行の STUB（Initial Design Artifact ヘッダ + [::STUB::] P19-6 のみ）。src/architecture/io_boundary_round4.rs も同様（21 行 STUB）。src/architecture/mod.rs は crate_scope / impl_integration_design / policy_reference / round2_scope_rootcause / examples_e1e5 / io_boundary_round2 / io_boundary_round3 / round3_scope_rootcause / round4_scope_rootcause を宣言済みで、round4_tickets / io_boundary_round4 は未宣言（add 必要）。
- 実在ファイル（make 時点）: build.rs / src/build/build_script_bindgen.rs / src/ffi/constants.rs / src/ffi/backend_calls.rs / src/ffi/callback.rs / src/ffi/bindings.rs / src/config/observability_metrics.rs / src/config/stun_turn_ice_wiring.rs / src/state/m20_callstate_mapping.rs / src/runtime/backend.rs / src/runtime/command.rs / tests/sip_integration.rs / src/tests/docker_asterisk_it.rs / src/tests/raw_sip_real_test.rs / src/tests/real_pjsip_itest.rs / vendor/prebuilt/aarch64-apple-darwin / vendor/prebuilt/BUILD.md / .github/workflows/{ci,integration,prebuilt-refresh}.yml。未生成: crates/（ディレクトリ自体なし）・crates/pjsip-prebuilt/・crates/pjsip-prebuilt/Dockerfile・.github/workflows/prebuilt.yml・vendor/prebuilt/{x86_64-unknown-linux-gnu,x86_64-pc-windows-msvc}。
- 設計ブリーフ §5.5（docs/PJSUA-NATIVE-PREBUILT-DESIGN-BRIEF.md）: Ticket A（消費者/bindgen 整合、DoD = cargo build --features pjsua-native が通る + make test-integration が CI green）と Ticket B（生産者/prebuilt 生成 + CI + コミット、Ticket A のコンパイル成功に非依存、DoD = git clone 各 OS → prebuilt 存在 →（A 完了後）cargo build が通る）。§5.3: producer は専用の最小 Rust ツール（workspace member crates/pjsip-prebuilt、siprs 非依存、CMake ビルド・Docker で Linux lib 生成・vendor/prebuilt/<target>/{include,lib} へ staging・失敗時は非ゼロ終了）。§3.2: 7 エラーカテゴリ（PJ_SUCCESS enum 列挙子 / PJSUA_CALL_NULL・PJ_CRED_DATA_PLAIN_PASSWD 不在 / pjsua_config.turn_cfg フィールド / pjsua_codec_info.encoding_name・clock_rate 不在 / pjsip_inv_state enum / AccountId import / FFI シグネチャ不整合）。

## Acceptance Criteria

- チケット構造が設計ブリーフ §5.5 とユーザー指示に整合する
- I/O 境界情報が全 round-4 ノードをカバーする
- **[Happy path]** `make test` が全て green。`crate::architecture::round4_tickets::round4_ticket_ordering()` が 7 チケット（P18-1/A・P18-2/B・P19-1/H8-rawSIP・P19-2/H8-ICE・P19-3/H13・P19-4/H14・P19-5/H15-EXAMPLES）を A/B 最優先・ギャップ後に連ねる順で返し、`crate::architecture::io_boundary_round4::ROUND4_IO_BOUNDARY_TABLE` が 13 行（62.31–62.43 各 1 行）で、各行の consumes / produces / file_candidates / graph_node_ids が非空。`ROUND4_DELETE_TARGETS` が 4 件で各既存 path が実在。`known_round4_graph_node_ids()` が N0099 / N0100–N0113 と各行の参照ノードを含む。producer/consumer 分離定数と未生成パス列挙が定義済み。
- **[Error case]** ノード ID の typo（例: N00114）、行数不一致（13 行以外）、チケットキーの typo（例: P19-6 の P19-7）、削除対象 path の実在違反、resolved_by が表に存在しない — 全ての逸脱がテストで赤として検出され、TDD の赤→緑を経て修正される。const 定義の不整合はコンパイルエラーまたはテスト失敗として可視化される。
- **[Edge case]** 62.44（IoBoundaryReference・容器）と 62.30（round-3 容器）が表に含まれない。producer 節（62.36/62.37）の未生成パス（crates/pjsip-prebuilt/・crates/pjsip-prebuilt/Dockerfile・.github/workflows/prebuilt.yml）は存在チェック対象から除外され、ROUND4_NOT_YET_CREATED_PATHS で明示される。.github/workflows/ には既存の prebuilt-refresh.yml が存在し、RFC 記載の prebuilt.yml（未生成）と区別して扱われる。

## Invariants

Ticket A/B は phase 18、RESIDUE ギャップは phase 19 以降。

- 【Normal establishment】round4_ticket_ordering() が常に 7 チケット（Round4TicketSpec）を返し、先頭 2 件が phase 18（P18-1 Ticket A / P18-2 Ticket B）、残り 5 件が phase 19（P19-1..P19-5）で、phase 値が単調非減少（18 → 19）。ROUND4_IO_BOUNDARY_TABLE が常に 13 行を保持し、各 decision が Round4Section の 62.31–62.43 に属する。
- 【Invariant on error】本チケットは純粋な参照符号化（const データ）であり実行時エラーを生まない。const 定義の不整合（行数・ノード ID typo・パス誤り・チケットキー誤り）はコンパイル時またはテスト時に検出され、不正な表・順序が出荷されることはない。
- 【Internal state invariant】ROUND4_DELETE_TARGETS の各 resolved_by（62.34 リンク戦略 / 62.32 vendored 戦略 / 62.33 bindgen enum / 62.32 vendored 戦略）が ROUND4_IO_BOUNDARY_TABLE の decision 集合に必ず含まれる。round4_ticket_ordering() の各変種の ticket_key() が Tickets.json の実在チケットキー（P18-1/P18-2/P19-1..P19-5）に 1:1 対応する。
- 【Boundary invariant】graph_node_ids の全参照が known_round4_graph_node_ids() に含まれ、round-3 §62.30（N0099）・round-4 節（N0100–N0113）と整合する。62.44（IoBoundaryReference・参照情報の容器）自身は表の外側であり、表に含まれない。producer 節（62.36/62.37）の未生成パス（crates/pjsip-prebuilt/ 等）は ROUND4_NOT_YET_CREATED_PATHS に列挙され、実在パスのみ exists() 検証される。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C152 — N0112→N0100

- **Precondition**: Round 4 scope settled — 実体: crate::architecture::round4_scope_rootcause::ROUND4_SCOPE_TARGETS（9 ファイル: build.rs / src/build/build_script_bindgen.rs / src/ffi/constants.rs / src/ffi/backend_calls.rs / src/ffi/callback.rs / src/config/observability_metrics.rs / src/state/m20_callstate_mapping.rs / src/runtime/backend.rs / src/config/stun_turn_ice_wiring.rs）と is_round4_scope_target() が定義済み（P18-1 §62.31 完了）。テスト翻訳: UT「[C152 Precondition] ROUND4_SCOPE_TARGETS.len() == 9 かつ is_round4_scope_target() が 9 対象を true・round-3 settled ファイルを false」
- **Postcondition**: Tickets mapped to §62.31–62.44 — 実体: round4_ticket_ordering() が 7 チケット（P18-1/A・P18-2/B・P19-1/H8-rawSIP・P19-2/H8-ICE・P19-3/H13・P19-4/H14・P19-5/H15-EXAMPLES）を §62.31–62.42 の節対応で列挙し、各節が 62.31–62.44 の範囲内。テスト翻訳: UT「[C152 Postcondition] round4_ticket_ordering() が 7 件で ticket_key() が P18-1..P19-5・section() が 62.31–62.42」
- **Invariant**: A/B tickets first, gaps after — 実体: round4_ticket_ordering() の先頭 2 件が phase 18（P18-1/P18-2）、残り 5 件が phase 19（P19-1..P19-5）。テスト翻訳: UT「[C152 Invariant] ordering[0..2] が phase 18・ordering[2..] が phase 19 で phase() が単調非減少」

### C153 — N0113→N0099

- **Precondition**: N0099 provides round-3 I/O boundary — 実体: crate::architecture::io_boundary_round3::ROUND3_IO_BOUNDARY_TABLE（8 行, 62.22–62.29）と ROUND3_DELETE_TARGETS（4 件）が定義済み（P17-10 §62.30 完了）。テスト翻訳: UT「[C153 Precondition] ROUND3_IO_BOUNDARY_TABLE.len() == 8 かつ ROUND3_DELETE_TARGETS.len() == 4」
- **Postcondition**: Round-4 boundary adds producer/consumer split — 実体: ROUND4_IO_BOUNDARY_TABLE が 13 行（62.31–62.43 各 1 行）で、producer 節（62.36 producer ツール / 62.37 CI コミット）と consumer 節（62.31–62.35）が ROUND4_PRODUCER_SECTIONS / ROUND4_CONSUMER_SECTIONS として分離定義される。テスト翻訳: UT「[C153 Postcondition] ROUND4_IO_BOUNDARY_TABLE.len() == 13 かつ producer/consumer 分離定数が定義」
- **Invariant**: All round-4 nodes mapped to files — 実体: 各行の graph_node_ids が known_round4_graph_node_ids() に含まれ、file_candidates が非空で既存パス（未生成パスは ROUND4_NOT_YET_CREATED_PATHS で明示）を指す。テスト翻訳: UT「[C153 Invariant] 全行の graph_node_ids が既知集合・file_candidates が非空」

## Boy Scout Rule

- 関数名を動詞句に: round4_ticket_ordering()（チケットを A/B 最優先順に並べる）・known_round4_graph_node_ids()（既知ノード集合を返す）は round-2/round-3 の known_round*_graph_node_ids() と同型の動詞句 + ドメイン名詞で命名。
- 構造体/定数をドメイン概念で命名: Round4TicketSpec（1 チケットの仕様）・Round4IoBoundaryRow（1 設計判断の I/O 境界）・Round4DeleteTarget（boundify の削除/更新対象）・ROUND4_IO_BOUNDARY_TABLE（§62.44 表）・ROUND4_PRODUCER_SECTIONS / ROUND4_CONSUMER_SECTIONS（producer/consumer 分離）・ROUND4_NOT_YET_CREATED_PATHS（未生成パス明示）は round-1/round-2/round-3 の命名パターン（IoBoundaryRow / Round2IoBoundaryRow / Round3IoBoundaryRow）を踏襲。
- ハードコード値を名前付き定数に: RFC 表の 13 行と 4 削除/更新対象・7 チケットキー・producer/consumer 節集合・未生成パスを pub const / 関数として符号化し、マジック値（行数 13 / 削除数 4 / チケット数 7 / ノード ID 文字列 / チケットキー文字列）をテストから定数参照で検証。
- エラー吞み込みなし: 純粋な const データ（実行時 I/O なし）のため Result 不要。テストは Result<(), std::io::Error> を返し、ファイル読取エラーを伝播。
- 既存コードの改善（Boy Scout）: (a) src/architecture/mod.rs のヘッダコメント「Sub-modules:」一覧に round4_tickets と io_boundary_round4 を追記し、コメントが嘘にならないよう一覧を最新化する。(b) round4_tickets.rs / io_boundary_round4.rs の STUB マーカー（[::STUB::] P19-6）を実装で置換し解消。(c) round-1/round-2/round-3 の実装ファイル（impl_integration_design.rs / io_boundary_round2.rs / io_boundary_round3.rs / round3_scope_rootcause.rs）は変更しない（責務分離を維持）。

## Test Plan

### Unit Tests

- UT: [C152 Precondition] Round 4 scope settled (§62.31) — crate::architecture::round4_scope_rootcause::ROUND4_SCOPE_TARGETS.len() == 9 かつ is_round4_scope_target() が 9 対象全件（build.rs / src/build/build_script_bindgen.rs / src/ffi/constants.rs / src/ffi/backend_calls.rs / src/ffi/callback.rs / src/config/observability_metrics.rs / src/state/m20_callstate_mapping.rs / src/runtime/backend.rs / src/config/stun_turn_ice_wiring.rs）を true、round-3 settled ファイル（src/architecture/io_boundary_round3.rs / src/architecture/round3_scope_rootcause.rs）を false とする。
- UT: [C152 Postcondition] Tickets mapped to §62.31–62.44 — round4_ticket_ordering() が 7 件の Round4TicketSpec を返し、各 ticket_key() が P18-1 / P18-2 / P19-1 / P19-2 / P19-3 / P19-4 / P19-5 で、各 section() が 62.31–62.42 の範囲内（62.31..62.35 / 62.36..62.37 / 62.38 / 62.39 / 62.40 / 62.41 / 62.42）に割当済み。
- UT: [C152 Invariant] A/B tickets first, gaps after — round4_ticket_ordering()[0..2] が phase 18（P18-1 Ticket A / P18-2 Ticket B）、round4_ticket_ordering()[2..] が phase 19（P19-1..P19-5）で、phase() が 18 → 19 と単調非減少。
- UT: [C153 Precondition] Round-3 I/O boundary reference exists (§62.30) — crate::architecture::io_boundary_round3::ROUND3_IO_BOUNDARY_TABLE.len() == 8 かつ ROUND3_DELETE_TARGETS.len() == 4。
- UT: [C153 Postcondition] Round-4 boundary adds producer/consumer split — ROUND4_IO_BOUNDARY_TABLE.len() == 13（62.31–62.43 各 1 行）で、ROUND4_PRODUCER_SECTIONS（62.36 producer ツール / 62.37 CI コミット）と ROUND4_CONSUMER_SECTIONS（62.31–62.35）が分離定義される。
- UT: [C153 Invariant] All round-4 nodes mapped to files — 全行の graph_node_ids が known_round4_graph_node_ids() に含まれ、file_candidates が非空で、既存パスは exists() が true、未生成パス（crates/pjsip-prebuilt/ 等）は ROUND4_NOT_YET_CREATED_PATHS に列挙される。
- UT: [Normal] ROUND4_IO_BOUNDARY_TABLE が §62.44 の 13 設計判断を保持する — assert_eq!(ROUND4_IO_BOUNDARY_TABLE.len(), 13)。62.31–62.43 が各 1 行で、各行の decision が Round4Section の 62.31–62.43 に属し、consumes / produces / file_candidates / graph_node_ids が非空。
- UT: [Error] 空文字列・空配列の行がない — 全行に対して assert!(!row.consumes.is_empty()) / assert!(!row.produces.is_empty()) / assert!(!row.file_candidates.is_empty()) / assert!(!row.graph_node_ids.is_empty()) を行い、decision.section() を失敗メッセージに含める。
- UT: [Boundary] 表が 62.31–62.43 を網羅し、62.44（IoBoundaryReference・容器）と 62.30（round-3 容器）を含まない — 13 節が各 1 回出現することを検証。
- UT: [Invariant] 各 delete/prune target の resolved_by が表の decision 集合に存在し、既存 path が実在 — ROUND4_DELETE_TARGETS の各 resolved_by が ROUND4_IO_BOUNDARY_TABLE の decision に含まれ、各 target.path が既存パスなら exists() が true。

### Integration Tests

- IT: [Integration point] round4_tickets::Round4TicketSpec ↔ Tickets.json phase 18/19 チケットキー — 7 変種の ticket_key()（P18-1/P18-2/P19-1/P19-2/P19-3/P19-4/P19-5）が Tickets.json の phase 18（P18-1, P18-2）・phase 19（P19-1..P19-5）の実在キーと一致することを検証。
- IT: [Verification] io_boundary_round4::ROUND4_IO_BOUNDARY_TABLE ↔ round4_scope_rootcause::ROUND4_SCOPE_TARGETS — 表の consumer 節（62.31–62.35）の file_candidates が ROUND4_SCOPE_TARGETS の 9 ファイルをカバーし、round-3 settled ファイル（io_boundary_round3.rs / round3_scope_rootcause.rs）を触らない（C137 invariant）ことを検証。
- IT: [Prerequisites] P18-1（§62.31–62.35）と P18-2（§62.36–62.37）が reviewed 済みで、round4_scope_rootcause::ROUND4_SCOPE_TARGETS と io_boundary_round3::ROUND3_IO_BOUNDARY_TABLE が実装済みであること。producer 節（62.36/62.37）の file_candidates は crates/pjsip-prebuilt 未生成のため ROUND4_NOT_YET_CREATED_PATHS として明示。
- IT: [Related tickets] P18-1（Ticket A・consumer/bindgen 整合 §62.31–62.35）、P18-2（Ticket B・producer/prebuilt+CI+commit §62.36–62.37）、P19-1（H8 raw SIP §62.38）、P19-2（H8 on_ice_transport_error §62.39）、P19-3（H13 push_media_frame §62.40）、P19-4（H14 AddAudioSource conf 再登録 §62.41）、P19-5（H15/EXAMPLES §62.42）— round4_ticket_ordering() の各変種がこれら実チケットキーに 1:1 対応することを検証。

### Exceptions

- **[Exception 1]** [Item] producer ツール（crates/pjsip-prebuilt）・Dockerfile・prebuilt.yml workflow による実際の prebuilt 生成とコミット / [Reason] 外部ビルドツール（P18-2 §62.36–62.37 の Rust producer と GitHub Actions CI）が行う処理であり、crates/ ディレクトリと .github/workflows/prebuilt.yml は本チケットの対象ファイルではない（未生成パス）。Rust 単体テストの実行対象外（外部依存・未生成パス）のため単体テスト不能（cannot test） / [Alternative verification] ROUND4_NOT_YET_CREATED_PATHS に crates/pjsip-prebuilt/ と .github/workflows/prebuilt.yml を列挙し、io_boundary_round4 の参照符号化（producer 節 62.36/62.37 の consumes/produces/file_candidates）と実在ワークフロー（.github/workflows/prebuilt-refresh.yml）の存在確認で代替検証する。これは設計欠陥ではない（not a design defect）——producer 生成の委譲は設計判断であり、参照符号化自体は全てテスト可能。
- **[Exception 2]** [Item] vendor/prebuilt/<target>/{include,lib} のバイナリ成果物の実在検証 / [Reason] バイナリ lib は各 OS の CI で生成・コミットされるものであり、make 時点では aarch64-apple-darwin のみ存在する。他ターゲット（x86_64-unknown-linux-gnu / x86_64-pc-windows-msvc）の成果物は未生成（not present）のためパス実在検証不能（cannot test） / [Alternative verification] vendor/prebuilt/BUILD.md と aarch64-apple-darwin ディレクトリの実在を検証し、未生成ターゲットは ROUND4_NOT_YET_CREATED_PATHS として明示して存在チェックから除外する。これは設計欠陥ではない（not an architectural defect）——成果物は CI 生成物であり、参照符号化は対象パスの型付き列挙として完結する。
- **[Exception 3]** [Item] boundify/graphify による実際の Prune（build.rs の static=pjsua2 単独リンク置換・PJSUA_CALL_NULL bindings 依存解消・PJ_CRED_DATA_PLAIN_PASSWD allowlist 修正・pjsua_codec_info 直接参照置換）/ [Reason] 外部ツール（Node.js 製 graphify/boundify）が行う削除処理であり、Rust 単体テストの実行対象外（外部依存）のため単体テスト不能（cannot test）。本チケットは削除/更新対象の参照符号化のみを担当する / [Alternative verification] ROUND4_DELETE_TARGETS の各 path 実在チェックと、ソース整合性テスト（build.rs の static=pjsua2 言及・backend_calls.rs の PJSUA_CALL_NULL 依存・build_script_bindgen.rs の allowlist・observability_metrics.rs の codec_id パース）で削除/更新対象が正しく解決済みであることを静的に検証する。これは設計欠陥ではない（not a design defect）——削除実行の委譲は設計判断であり、参照符号化自体は全てテスト可能。

### Plan Test Code (concrete code)

- UT: [C152 Precondition] Round 4 scope settled (§62.31) — P18-1 成果物 ROUND4_SCOPE_TARGETS が 9 ファイルで is_round4_scope_target() が全対象を true、round-3 settled ファイルを false とする（入力スキーマ検証コード）。
  ```rust
  #[test]
  // @verifies C152 -- precondition: Round 4 scope settled (§62.31)
  fn round4_scope_targets_are_settled() {
      use crate::architecture::round4_scope_rootcause::{
          ROUND4_SCOPE_TARGETS, is_round4_scope_target,
      };
      assert_eq!(ROUND4_SCOPE_TARGETS.len(), 9);
      for target in ROUND4_SCOPE_TARGETS {
          assert!(is_round4_scope_target(target), "missing scope target {target}");
      }
      // C137 invariant: round-3-settled files must never be rewritten
      assert!(!is_round4_scope_target("src/architecture/io_boundary_round3.rs"));
      assert!(!is_round4_scope_target("src/architecture/round3_scope_rootcause.rs"));
  }
  ```
- UT: [C152 Postcondition] Tickets mapped to §62.31–62.44 — round4_ticket_ordering() が 7 チケットを返し、ticket_key() が P18-1..P19-5、section() が 62.x（出力アサーションコード）。
  ```rust
  #[test]
  // @verifies C152 -- postcondition: Tickets mapped to §62.31–62.44
  fn round4_tickets_map_to_sections() {
      use crate::architecture::round4_tickets::round4_ticket_ordering;
      let ordering = round4_ticket_ordering();
      assert_eq!(ordering.len(), 7);
      let keys: Vec<&str> = ordering.iter().map(|t| t.ticket_key()).collect();
      assert_eq!(keys, vec!["P18-1", "P18-2", "P19-1", "P19-2", "P19-3", "P19-4", "P19-5"]);
      for ticket in ordering {
          let sec = ticket.section();
          assert!(sec.starts_with("62."), "section must be 62.x: {sec}");
      }
  }
  ```
- UT: [C152 Invariant] A/B tickets first, gaps after — 先頭 2 件が phase 18（Ticket A/B）、残りが phase 19 で phase() が単調非減少（不変条件述語コード）。
  ```rust
  #[test]
  // @verifies C152 -- invariant: A/B tickets first, gaps after
  fn ab_tickets_first_then_gaps() {
      use crate::architecture::round4_tickets::round4_ticket_ordering;
      let ordering = round4_ticket_ordering();
      assert_eq!(ordering.len(), 7);
      assert_eq!(ordering[0].phase(), 18);
      assert_eq!(ordering[1].phase(), 18);
      assert_eq!(ordering[0].ticket_key(), "P18-1");
      assert_eq!(ordering[1].ticket_key(), "P18-2");
      for ticket in &ordering[2..] {
          assert_eq!(ticket.phase(), 19);
      }
      let phases: Vec<u16> = ordering.iter().map(|t| t.phase()).collect();
      assert!(phases.windows(2).all(|w| w[0] <= w[1]));
  }
  ```
- UT: [C153 Precondition] Round-3 I/O boundary reference exists (§62.30) — P17-10 成果物 ROUND3_IO_BOUNDARY_TABLE が 8 行・ROUND3_DELETE_TARGETS が 4 件（入力検証コード）。
  ```rust
  #[test]
  // @verifies C153 -- precondition: Round-3 I/O boundary reference exists (§62.30)
  fn round3_io_boundary_reference_exists() {
      use crate::architecture::io_boundary_round3::{
          ROUND3_DELETE_TARGETS, ROUND3_IO_BOUNDARY_TABLE,
      };
      assert_eq!(ROUND3_IO_BOUNDARY_TABLE.len(), 8);
      assert_eq!(ROUND3_DELETE_TARGETS.len(), 4);
  }
  ```
- UT: [C153 Postcondition] Round-4 boundary adds producer/consumer split — ROUND4_IO_BOUNDARY_TABLE が 13 行で、producer 節（62.36/62.37）と consumer 節（62.31–62.35）が分離定義（出力アサーションコード）。
  ```rust
  #[test]
  // @verifies C153 -- postcondition: Round-4 boundary adds producer/consumer split
  fn round4_boundary_splits_producer_consumer() {
      use crate::architecture::io_boundary_round4::{
          ROUND4_CONSUMER_SECTIONS, ROUND4_IO_BOUNDARY_TABLE, ROUND4_PRODUCER_SECTIONS,
      };
      assert_eq!(ROUND4_IO_BOUNDARY_TABLE.len(), 13);
      assert_eq!(ROUND4_CONSUMER_SECTIONS.len(), 5);
      assert_eq!(ROUND4_PRODUCER_SECTIONS.len(), 2);
      assert_eq!(ROUND4_PRODUCER_SECTIONS[0].section(), "62.36");
      assert_eq!(ROUND4_PRODUCER_SECTIONS[1].section(), "62.37");
      for sec in ROUND4_CONSUMER_SECTIONS {
          assert!(sec.section() >= "62.31" && sec.section() <= "62.35");
      }
  }
  ```
- UT: [C153 Invariant] All round-4 nodes mapped to files — 全行の graph_node_ids が known_round4_graph_node_ids() に含まれ、file_candidates が非空、未生成パスが ROUND4_NOT_YET_CREATED_PATHS に列挙（不変条件述語コード）。
  ```rust
  #[test]
  // @verifies C153 -- invariant: All round-4 nodes mapped to files
  fn all_round4_nodes_map_to_files() {
      use crate::architecture::io_boundary_round4::{
          ROUND4_IO_BOUNDARY_TABLE, ROUND4_NOT_YET_CREATED_PATHS, known_round4_graph_node_ids,
      };
      let known = known_round4_graph_node_ids();
      assert!(known.contains(&"N0099"), "round-3 §62.30 node N0099 must be known");
      for row in &ROUND4_IO_BOUNDARY_TABLE {
          assert!(!row.file_candidates.is_empty(), "{} no file candidates", row.decision.section());
          for node in row.graph_node_ids {
              assert!(known.contains(node), "unknown graph node {node} in {}", row.decision.section());
          }
      }
      assert!(!ROUND4_NOT_YET_CREATED_PATHS.is_empty());
  }
  ```
- UT: [Normal] ROUND4_IO_BOUNDARY_TABLE が §62.44 の 13 設計判断を保持し、各行の consumes/produces/file_candidates/graph_node_ids が非空（正規ケーステスト）。
  ```rust
  #[test]
  // @verifies C153 -- normal: table lists thirteen boundaries with non-empty fields
  fn round4_table_lists_thirteen_boundaries() {
      use crate::architecture::io_boundary_round4::ROUND4_IO_BOUNDARY_TABLE;
      assert_eq!(ROUND4_IO_BOUNDARY_TABLE.len(), 13);
      for row in &ROUND4_IO_BOUNDARY_TABLE {
          assert!(!row.consumes.is_empty(), "{} consumes empty", row.decision.section());
          assert!(!row.produces.is_empty(), "{} produces empty", row.decision.section());
          assert!(!row.file_candidates.is_empty(), "{} file_candidates empty", row.decision.section());
          assert!(!row.graph_node_ids.is_empty(), "{} graph_node_ids empty", row.decision.section());
      }
  }
  ```
- UT: [Error] 空文字列・空配列の行が無い（エラーケース検証コード）— 全行の境界列が非空であることを decision.section() を失敗メッセージに含めて検証。
  ```rust
  #[test]
  // @verifies C153 -- error: no empty consumes/produces/file_candidates/graph_node_ids
  fn round4_rows_are_non_empty() {
      use crate::architecture::io_boundary_round4::ROUND4_IO_BOUNDARY_TABLE;
      for row in &ROUND4_IO_BOUNDARY_TABLE {
          let section = row.decision.section();
          assert!(!row.consumes.is_empty(), "{section} consumes empty");
          assert!(!row.produces.is_empty(), "{section} produces empty");
          assert!(!row.file_candidates.is_empty(), "{section} file_candidates empty");
          assert!(!row.graph_node_ids.is_empty(), "{section} graph_node_ids empty");
      }
  }
  ```
- UT: [Boundary] 表が 62.31–62.43 を網羅し、62.44（容器）と 62.30（round-3 容器）を含まない（境界値テスト）。
  ```rust
  #[test]
  // @verifies C153 -- boundary: table covers exactly 62.31..62.43
  fn round4_table_covers_62_31_to_62_43_only() {
      use crate::architecture::io_boundary_round4::ROUND4_IO_BOUNDARY_TABLE;
      let sections: Vec<&str> = ROUND4_IO_BOUNDARY_TABLE.iter().map(|r| r.decision.section()).collect();
      assert_eq!(sections.len(), 13);
      for n in 31..=43 {
          let sec = format!("62.{n}");
          assert_eq!(sections.iter().filter(|s| **s == sec).count(), 1, "{sec} must appear once");
      }
      assert!(!sections.contains(&"62.44"), "62.44 (container) must not be a row");
      assert!(!sections.contains(&"62.30"), "62.30 (round-3 container) must not be a row");
  }
  ```
- UT: [Invariant] ROUND4_DELETE_TARGETS の各 resolved_by が表の decision 集合に存在し、既存 path が実在、削除対象が P18-1 で解決済みの状態にある（不変条件 + ソース整合性テスト）。
  ```rust
  #[test]
  // @verifies C153 -- invariant: delete targets resolve to table rows; all pre-resolved by P18-1
  fn round4_delete_targets_resolve_to_table_rows() -> Result<(), std::io::Error> {
      use crate::architecture::io_boundary_round4::{
          ROUND4_DELETE_TARGETS, ROUND4_IO_BOUNDARY_TABLE,
      };
      let decisions: Vec<_> = ROUND4_IO_BOUNDARY_TABLE.iter().map(|r| r.decision).collect();
      assert_eq!(ROUND4_DELETE_TARGETS.len(), 4);
      for target in &ROUND4_DELETE_TARGETS {
          assert!(decisions.contains(&target.resolved_by), "resolved_by {} not in table", target.resolved_by.section());
          assert!(std::path::Path::new(target.path).exists(), "delete target path must exist: {}", target.path);
      }
      // All 4 prune targets are pre-resolved by P18-1 (Ticket A):
      // 1. build.rs static=pjsua2 single link -> 4-stage link derivation (no static=pjsua2)
      let build = std::fs::read_to_string("build.rs")?;
      assert!(!build.contains("static=pjsua2"), "build.rs must not keep the single static=pjsua2 link");
      // 2. PJSUA_CALL_NULL moved into crate constants.rs
      let constants = std::fs::read_to_string("src/ffi/constants.rs")?;
      assert!(constants.contains("PJSUA_CALL_NULL"), "PJSUA_CALL_NULL must live in constants.rs");
      // 3. bindgen allowlist uses the corrected PJSIP_CRED_DATA_PLAIN_PASSWD symbol
      let bindgen = std::fs::read_to_string("src/build/build_script_bindgen.rs")?;
      assert!(bindgen.contains("PJSIP_CRED_DATA_PLAIN_PASSWD"), "allowlist must use PJSIP_CRED_DATA_PLAIN_PASSWD");
      // 4. codec name/rate derived from codec_id, not direct pjsua_codec_info fields
      let obs = std::fs::read_to_string("src/config/observability_metrics.rs")?;
      assert!(obs.contains("codec_id_to_name_rate"), "observability_metrics must derive name/rate from codec_id");
      Ok(())
  }
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| round 4 のチケット構造と I/O 境界が未整備 | チケット順序（A/B 最優先 + ギャップ）と I/O 境界参照を型付き定数として実装 | チケット構造 + I/O 境界（§62.43–62.44） |
| src/architecture/round4_tickets.rs: [::STUB::] marker only (21 lines) | Round4TicketSpec enum (7 variants: TicketA/B + H8/H13/H14/H15/EXAMPLES) + round4_ticket_ordering(): [Round4TicketSpec; 7] + ticket_key()/phase()/section() + 3 @verifies C152 tests | Encoded §62.43 ticket structure as typed constants (P19-6) |
| src/architecture/io_boundary_round4.rs: [::STUB::] marker only (21 lines) | Round4Section enum (14 variants 62.31-62.44) + Round4IoBoundaryRow + ROUND4_IO_BOUNDARY_TABLE: [Round4IoBoundaryRow; 13] + Round4DeleteTarget + ROUND4_DELETE_TARGETS: [Round4DeleteTarget; 4] + ROUND4_PRODUCER_SECTIONS/ROUND4_CONSUMER_SECTIONS + ROUND4_NOT_YET_CREATED_PATHS + known_round4_graph_node_ids() + 8 @verifies C153 tests | Encoded §62.44 I/O boundary table with producer/consumer split as typed constants (P19-6) |
| src/architecture/mod.rs: round4_tickets/io_boundary_round4 not declared; stale Sub-modules comment | pub mod round4_tickets; + pub mod io_boundary_round4; declared with [::TICKET::] P19-6 annotations; Sub-modules comment updated | Registered the new round-4 modules (P19-6) |

## Notes in Prior Implementation Rounds

- [Implementation steps] 1) Red: round4_tickets.rs / io_boundary_round4.rs に #[cfg(test)] テスト群を先行実装（round4_ticket_ordering が 7 チケット・A/B 先頭・phase 単調、ROUND4_IO_BOUNDARY_TABLE が 13 行・非空・62.31–62.43 網羅・既知ノード整合、ROUND4_DELETE_TARGETS が 4 件・path 実在、producer/consumer 分離、未生成パス列挙、ソース整合性）。未実装のためコンパイル失敗/テスト失敗で赤を確認。2) Green: Round4TicketSpec / round4_ticket_ordering / Round4Section / Round4IoBoundaryRow / ROUND4_IO_BOUNDARY_TABLE（13 行）/ ROUND4_DELETE_TARGETS（4 件）/ ROUND4_PRODUCER_SECTIONS / ROUND4_CONSUMER_SECTIONS / ROUND4_NOT_YET_CREATED_PATHS / known_round4_graph_node_ids() を実装し、mod.rs に pub mod round4_tickets; と pub mod io_boundary_round4; を追加。3) Refactor: コメントの正確性確認・テスト変数名のドメイン化。
- [Risks] crates/pjsip-prebuilt と .github/workflows/prebuilt.yml が未生成のため、RFC §62.44 の新規ファイルを file_candidates に含めるとパス実在検証が失敗する。ROUND4_NOT_YET_CREATED_PATHS で明示的に分離する。.github/workflows/ には既存の prebuilt-refresh.yml があり、RFC 記載の prebuilt.yml（未生成）とファイル名が紛らわしいため区別して扱う。round4_scope_rootcause.rs に Round4Section enum が無いため、round-3 と異なり io_boundary_round4.rs 内に自己完結で節 enum を定義する必要がある。
- [Caveats] 実ファイルの削除は行わない（boundify の責務）。round-1/round-2/round-3 の実装ファイル（impl_integration_design.rs / io_boundary_round2.rs / io_boundary_round3.rs / round3_scope_rootcause.rs）は変更しない。round4_scope_rootcause.rs（P18-1 成果物）も変更しない。Initial Design Artifact ヘッダは絶対に編集しない。
- [Open items] なし — 本チケットは参照符号化のみであり、ランタイム挙動の未決事項はない。
- [Future improvements] graphify/boundify が本表（ROUND4_IO_BOUNDARY_TABLE / ROUND4_DELETE_TARGETS）とチケット順序（round4_ticket_ordering()）を参照して round-4 の分割・Prune 判断を行う。P19-7 以降のチケットが file_candidates の更新元として利用する。


Implementation summary (P19-6):
- Changed files: src/architecture/round4_tickets.rs (add), src/architecture/io_boundary_round4.rs (add), src/architecture/mod.rs (modify)
- Key changes: Round4TicketSpec enum (7 variants P18-1/A, P18-2/B, P19-1..P19-5) with ticket_key()/phase()/section(); round4_ticket_ordering() returns [Round4TicketSpec; 7] with A/B first (phase 18) then gaps (phase 19). Round4Section enum (62.31-62.44, section()/node_id()/label()); ROUND4_IO_BOUNDARY_TABLE (13 rows 62.31-62.43) with consumes/produces/file_candidates/graph_node_ids; ROUND4_DELETE_TARGETS (4 prune targets all pre-resolved by P18-1); ROUND4_PRODUCER_SECTIONS [62.36,62.37] / ROUND4_CONSUMER_SECTIONS [62.31-62.35]; ROUND4_NOT_YET_CREATED_PATHS (crates/pjsip-prebuilt, Dockerfile, prebuilt.yml, other-target prebuilts); known_round4_graph_node_ids()
- Contracts: C152 covered by 3 @verifies tests in round4_tickets.rs (precondition/postcondition/invariant); C153 covered by 8 @verifies tests in io_boundary_round4.rs (precondition/postcondition/invariant/normal/error/boundary/source-consistency/scope-boundary)
- Test results: make test exit 0 (all pass); 11 new tests in the two round-4 modules; cargo fmt clean on changed files; clippy clean on changed files; quality check 0 issues


Review report (P19-6):
- Static quality check: passed (0 issues on round4_tickets.rs, io_boundary_round4.rs, mod.rs)
- Translatability: no issues — round4_ticket_ordering/known_round4_graph_node_ids/ticket_key/phase/section are verb-phrase functions; Round4TicketSpec/Round4IoBoundaryRow/Round4DeleteTarget/ROUND4_* constants follow round-1/2/3 naming patterns; test variables (ordering/keys/phases/sections/row/target) are domain-meaningful
- Tests: make test exit 0 (17 suites ok); 11 new @verifies tests (C152: 3, C153: 8); cargo fmt clean on changed files; clippy clean on changed files
- STUB evaluation: 0 stubs remaining (TS-001/TS-002 resolved); no-excuse gate passed (0 fail)
- Annotation check: 3/3 files annotated with [::TICKET::] P19-6; 0 AMBIGUOUS; 0 missing
- Contracts: verify-final-contracts 100% fulfilled; validate-ticket-targets 22 checks passed
- Completeness: ROUND4_IO_BOUNDARY_TABLE (13 rows 62.31-62.43), ROUND4_DELETE_TARGETS (4, all pre-resolved by P18-1), producer/consumer split, ROUND4_NOT_YET_CREATED_PATHS, known_round4_graph_node_ids, Round4TicketSpec (7 variants) all verified against §62.43-62.44
- Boy Scout: mod.rs Sub-modules comment updated (round4_scope_rootcause/round4_tickets/io_boundary_round4); resolved 2 STUB markers; kept round-1/2/3 files untouched (responsibility separation)
- Issues found and fixes applied during start: planTestCode delete-targets test corrected to assert the P18-1-resolved state (build.rs no longer has static=pjsua2); doc_lazy_continuation clippy warning fixed by rewording doc comments; enum/const quality-check false positive fixed by rephrasing to 'enum and constant'; rustfmt applied to changed files
- Pre-existing (out of scope): clippy unnecessary_cast warnings in m20_callstate_mapping.rs etc. (earlier-ticket files, not touched)

## P19-6 — implemented at 16 locations

### src/architecture/io_boundary_round4.rs

- Line 60
```rust
impl Round4Section {
```

- Line 354
```rust
    fn round3_io_boundary_reference_exists() {
```

- Line 362
```rust
    fn round4_boundary_splits_producer_consumer() {
```

- Line 376
```rust
    fn all_round4_nodes_map_to_files() {
```

- Line 402
```rust
    fn round4_table_lists_thirteen_boundaries() {
```

- Line 431
```rust
    fn round4_rows_are_non_empty() {
```

- Line 450
```rust
    fn round4_table_covers_62_31_to_62_43_only() {
```

- Line 477
```rust
    fn round4_delete_targets_resolve_to_table_rows() -> Result<(), std::io::Error> {
```

- Line 526
```rust
    fn round4_consumer_file_candidates_stay_in_scope() {
```

### src/architecture/mod.rs

- Line 52
```rust
pub mod round4_tickets;
```

- Line 54
```rust
pub mod io_boundary_round4;
```

- Line 56
```rust

```

### src/architecture/round4_tickets.rs

- Line 50
```rust
impl Round4TicketSpec {
```

- Line 114
```rust
    fn round4_scope_targets_are_settled() {
```

- Line 134
```rust
    fn round4_tickets_map_to_sections() {
```

- Line 151
```rust
    fn ab_tickets_first_then_gaps() {
```
