# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P22-13: /grill-me-for-rfc reverse correspondence: mechanically generated questions that prevent ratification, and a record of normative choices

**Ticket Key**: P22-13 · **Phase**: 22

---

## Background

**Shared premises — binding on all 22 P22 tickets. Read this first.**

P22 implements the reverse rotation: taking a project that already contains a substantial implementation and turning it into conver's four loops (upstream / implementation / release / evolution). Forward rotation already works completely; reverse rotation does not exist yet. `docs/ABOUT-REVERSE.md` is the full design. The implementation loop runs one ticket per session and context does not carry across tickets, so this block is repeated verbatim in all 22 tickets. A session that sees only its own ticket must still know what the whole is for and what it may not break.

(1) **Never break the forward rotation.** The four existing loops work. No change in P22 may alter forward behaviour. A byte-identical `manifest_hash` over the forward fixtures, captured before any change and reproduced after, is the only accepted proof. P22-1 builds that gate; every later ticket runs it after its own step. "It looks fine" is not evidence and is not accepted.
(2) **Rewriting any `.claude/commands/*.md` is strictly forbidden.** Only edits are permitted — append, or correct the minimum number of lines. The Language Protocol table, the First-Class Rule, the workflow steps and the script list must survive character for character. The nine existing command files are: `workspacify-tree`, `workspacify-allocate`, `grill-me-for-rfc`, `graphify-rfc`, `boundify-graph`, `split-to-tickets`, `find-omissions`, `crystalize-readme`, `drill-rfc-down`. P22-9 additionally *creates* `workspacify-reverse.md`, which is a new file, not an edit.
(3) The full design lives in **`docs/ABOUT-REVERSE.md`**. Each ticket cites the exact sections it depends on. Read those sections before implementing; they are the specification of record.
(4) The machine may only ever say **"proved" or "not proved"**. "Succeeded" and "failed" are not in its vocabulary, because whether reverse engineering succeeded is a judgement a human makes after several loop rounds. Any script that emits a boolean success verdict is wrong by construction.
(5) What is deterministic, a script decides and the AI accepts the verdict. Only genuinely semantic judgement is left to the AI, and the material it needs is placed in front of it — as plain, deliberately helpful English — immediately before the decision. Do not widen the AI's judgement surface beyond what the design calls for.
(6) The `[::STUB::]` marker discipline is absolute and admits no exception. Any incomplete implementation carries a marker naming the ticket that will resolve it.
(7) Output format: emit JSON only when another script consumes it, a schema validates it, a hash covers it, or a gate uses it as a predicate. **Anything the AI reads in order to decide is Markdown, written in plain and deliberately helpful English.** Struggling to read a script's output is a defect in the script, not a cost of doing business.
(8) The `Initial Design Artifact — RFC-driven Implementation` header is never deleted, altered or commented out (supreme law 4). This holds without exception, including in files this phase touches.
(9) **The paired trees are the measuring instrument.** `siprs-with-4layers` is the same project taken through the forward rotation to RESIDUE 0 and therefore holds what reverse rotation is trying to reconstruct; `siprs-for-reverse` is that project with those artefacts stripped and is what the analysis runs against. Comparing an output against the answer key yields a list of disagreements, never a score and never a verdict — classifying each disagreement is a human's work. P22-2 builds the harness; the tickets whose output is comparable run it.
(10) **The source-analysis technology is decided once, in P22-4, and never re-decided.** Four separate sessions (P22-4, P22-5, P22-6, P22-7) must parse the same Rust source, and no context passes between them. P22-4 evaluates the candidates **against the expert consultation recorded in `docs/ABOUT-ANALYSIS-TECH.md`**, records the choice and its rationale in **`docs/P22-ANALYSIS-TECH.md`**, and every later analysis ticket reads that document instead of choosing again. The repository offers no precedent to inherit — `.claude/scripts/rfc-graph/analyze-source-structure.js` is regex-based and is not equal to extracting guards, invariants and state machines — so the decision is genuinely open and must be made deliberately and written down. **The scope is six languages, not one**: Rust, TypeScript/JavaScript, Go, Python and C/C++, because a technology that only handles the first target would be an implementation tuned to that target, which is explicitly forbidden. Two sessions independently choosing different parsers would produce mutually unreadable output and no test would catch it, because each session's tests would pass against its own choice. **The chosen toolchain is recorded in two places, and both are required**: the reasoning in `docs/P22-ANALYSIS-TECH.md`, and the machine-readable declaration in `ENV-DEPS.json` built by P22-1, where each new tool is added with the rotation that needs it and the ticket that provides it. Adding a tool is an entry in that declaration — never a new installation mechanism, and never a dependency left undeclared because it happened to be present on the machine where it was developed.
(11) **Match the surrounding directory, do not invent a layout.** The repository uses two conventions and they are not interchangeable. `.claude/scripts/rfc-graph/`, `grill-me-for-rfc/`, `conver/` and `tickets/` are flat CommonJS `.js` with no `lib/` subdirectory; `.claude/scripts/workspacify-tree/`, `workspacify-allocate/` and `workspacify-reverse/` are ESM `.mjs` under `lib/`. A ticket that names a path must name one that matches its siblings, and must not introduce a `lib/` directory into a flat tree or a CommonJS module into an ESM one. The same applies to test files: `.test.cjs` beside CommonJS, `.test.mjs` beside ESM.
(12) **The expert answer received in `docs/ABOUT-ANALYSIS-TECH.md` (the section headed 「専門家の回答」, with the request-side verification record in its §7) is binding on the analysis design. Three of its corrections outrank any earlier wording in this repository, including the wording of earlier P22 tickets.** (a) `observed` may only ever mean *a fact read from the source text or its syntax tree*. Runtime behaviour, dynamic dispatch targets, post-preprocessing composition and generated code are not `observed` without execution, build or trace evidence, so every evidence item carries `evidence_mode`: `source_static` | `build_semantic` | `runtime_dynamic`. (b) `derivation_group` is **not** a deterministic identifier for a human design intent — that cannot be computed from file contents. It is a lineage graph over observable relations (same AST span, same generator, same commit, same data-flow origin), and independence is an explicit stored aggregation policy over that graph; `independence_assessment: "unknown"` is a legal and expected value, not a failure. (c) E13 is **TCE** (trivial / syntactic / compiler-normalisation equivalence). General semantic equivalence is undecidable and is not a goal; an AST-normalised match proves syntactic equivalence under a named configuration and nothing more. (d) Where the answer separates a *candidate* from a *decided* fact (E11, E13, E14), the separation is enforced at the type level — a candidate cannot be passed, stored or serialised where a decided fact is required — and a candidate carries `requires_human_approval`. A session that finds itself asserting a contract, an equivalence or a generated test as settled has violated this premise. **§7 of that document also records one error of ours that the answer exposed: the claim that CodeQL does not support Rust was wrong.**

---

## This ticket — P22-13

**Goal**: Produce a canonical RFC by dialogue, starting from an RFC-SEED, that fully accounts for the existing implementation without being captured by it.

**Purpose**: This is the strongest available defence against the ratification RFC (F1), in which the resulting document merely restates the code in the language of a specification.

**Motivation**: ABOUT-REVERSE 6.5. When an implementation already exists, every question framed as "what should this do?" invites the answer "what it currently does". A question that forces the distinction between intent and accident is therefore mandatory rather than optional.

**Constraints**: The machine forcibly inserts, for every unresolved claim, the question of whether the observed behaviour is intended or accidental. No approval-waiting state is created; the mechanism integrates with the existing residual machinery. Authority is recorded as a role or team identifier, never as a person's name, because people change and the location of authority does not.

**Command-file edit discipline**: **Edit only; rewriting is strictly forbidden.** Append, or correct the minimum number of lines. The Language Protocol table, the First-Class Rule, the workflow steps and the script list must not lose a single character. Permitted changes are limited to (a) `mode === "reverse"` branch notes, (b) sections that apply only to reverse rotation, (c) new rows in the script list. Re-run the P22-1 command digest before and after and confirm it is unchanged.

**Where this ticket sits in the 22.** 1 regression gate · 2 experiment design (holdout + answer key) · 3 spike · 4 R0-R2 scope and structure · 5 R3-R3.5 semantics and ledger · 6 R4-R5.5 history, gaps, oracle · 7 R6-R6.5 Red plan and falsification · 8 R7-R8 serving and origin spec · 9 command and zg · 10 forward schemas · 11 workspacify-tree reverse · 12 workspacify-allocate reverse · **→13 grill reverse** · 14 graphify reverse · 15 boundify reverse · 16 split-to-tickets reverse · 17 omission and RESIDUE return · 18 dynamic analysis · 19 Red execution · 20 logical/physical two-layer · 21 staleness into drill · 22 security lane

**Step 0 — before you write anything.** Confirm the RFC-SEED exists with 14 headings, the residual `normative_context` field is present, and `run.mjs regression check` exits 0. Read `.claude/commands/grill-me-for-rfc.md` (13,686 B) before editing it — append only.

## Scope

- **Scope:**
-   - [File] `.claude/commands/grill-me-for-rfc.md` (edit only), `reverse-questions.js`, `normative-decision.js`, `tests/grill-me-for-rfc/reverse/*.test.cjs` (new)
-   - [Action] add, plus a minimal edit to the command file
-   - [Detail] G1 mechanical question generation, G2 ratification prevention through the mandatory intent-or-accident question, G3 verbatim residual carry-over, G4 normative choice recording, G5 authority recording as a stable role identifier.
-   - [Before→After] Questions are framed so that "what it currently does" is an acceptable answer → every unresolved claim is asked whether the behaviour is intended or accidental, with options and a default
-   - [API] The reverse branch of the existing grill flow
-   - [Schema] `normative_decision`, `normative_authority` on the residual structure
-   - [Config] None
-   - [Dependency] P22-12, P22-1 and P22-10
- **Out of scope:** Generating the body of the RFC itself, which is the existing grill procedure. This ticket adds reverse-mode behaviour only.
- **Affected areas:** The quality of the canonical RFC in every partition, and therefore every stage downstream of the grill.

## Implementation Target Files

- `.claude/commands/grill-me-for-rfc.md`
- `.claude/scripts/grill-me-for-rfc/reverse-questions.js`
- `.claude/scripts/grill-me-for-rfc/normative-decision.js`
- `tests/grill-me-for-rfc/reverse/questions.test.cjs`
- `tests/grill-me-for-rfc/reverse/normative-decision.test.cjs`

## Investigation

- **Measured 2026-09-10.** The edit target `.claude/commands/grill-me-for-rfc.md` is 13,686 B, the second smallest of the nine, so the append is a modest addition to a compact document.
- G4 and G5 in 6.14.3 have precise FAIL conditions: a `normative` proposition with no `normative_decision`, and a `normative_authority` that is empty or identifiable as a personal name. Both are stated as machine-checkable conditions rather than as guidance.
- The provenance chain that G4 depends on is the one recorded in 5.5 and assembled from the existing forward chains: `headingRefs` in `rfc-graph/crud.js`, `nodeIds` in `tickets/add-tickets-for-phase.js`, `mappedNodeIds` in `rfc-graph/boundify-helpers.js:89`, `originalTicketKey` in `tickets/add-omission-ticket.js`. A normative clause must be traceable through all of them.
- The approval-waiting prohibition is concrete: the project rules forbid leaving a `[::STUB::]` obligation unresolved and forbid deferring work, so a design that parked a decision in a waiting state would be inconsistent with the rules under which this phase runs.
- The intent-or-accident question addresses F1 directly. In `siprs-for-reverse` the PX-203 work showed how much behaviour exists for historical reasons: ten test files named after ticket keys, an `include_str!` referencing a README that no longer exists, and a `vendor/` directory. Each of those is behaviour a naive RFC would restate as intent.

## Acceptance Criteria

- **[Happy path]:** Ratification-prevention questions are generated from the RFC-SEED and normative choices are recorded as selection events.
- **[Error case]:** A broken provenance chain and a personal-name authority are both refused.
- **[Edge case]:** With no answer given, `chosen_default` is adopted and a high-risk proposition is not promoted to normative.

## Invariants

- 【Normal establishment】G1 mechanically generates questions. G2 inserts the intent-or-accident question without exception. G3 carries residuals over verbatim. G4 records every normative choice as a `normative_decision` selection event. G5 records authority as a stable role or team identifier.
- 【On error】A normative clause whose provenance chain is broken is refused. An authority that is empty or that parses as a personal name is a FAIL. A question that cannot be generated for an unresolved claim is reported rather than skipped.
- 【Internal state】The forward grill behaviour is unchanged: this adds reverse-mode behaviour only, and no approval-waiting state is created anywhere. Free-form prose is never demanded of the human; options and defaults are supplied instead.
- 【Boundary】A run with zero residuals completes without error and reports that there were none. A run in which every question has been answered still produces a complete record rather than an empty one.

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — N/A

- **Precondition**: the RFC-SEED carries a reverse index in section 1
- **Postcondition**: the intent-or-accident question is inserted mechanically for every unresolved claim
- **Invariant**: free-form prose is never demanded, and options and defaults are always supplied

### C002 — N/A

- **Precondition**: the grill makes a choice
- **Postcondition**: the choice is recorded as a selection event in normative_decision
- **Invariant**: no approval-waiting state is created

### C003 — N/A

- **Precondition**: a normative proposition is settled
- **Postcondition**: normative_authority is recorded as a stable role or team identifier
- **Invariant**: a personal name is never used as the authority

## Boy Scout Rule

- `generateReverseQuestions`, `recordNormativeDecision`, `carryResidualsVerbatim`, `recordAuthority` are verb phrases; `questionKind`, `chosenDefault`, `normativeAuthority`, `provenanceChain` are domain concepts.
- The prohibited payload tokens are a single named constant rather than an inline list, so the assertion and any future validation share one definition.
- Question generation is separated from question rendering, so the generation logic can be tested without producing a document.
- The residual structure is extended rather than shadowed: existing fields keep their names and meanings, so the forward path reads exactly what it read before.

## Test Plan

### Unit Tests

- **UT:[normal]** UT-1 G1 through G5 each behave as specified. UT-2 every `unresolved` claim receives a `grill_question`. UT-3 every normative choice is recorded as a selection event in `normative_decision`.
- **UT:[error]** UT-4 a normative clause with a broken provenance chain is refused. UT-5 an authority that is empty or that parses as a personal name is a FAIL. UT-6 a claim for which no question can be generated is reported rather than silently skipped.
- **UT:[boundary]** UT-7 zero residuals completes without error and states that there were none. UT-8 a run in which every question is answered still produces a complete record. UT-9 an unanswered question falls back to `chosen_default`.
- **UT:[invariant]** UT-10 the payload never contains TODO, TBD, "ask the human" or "waiting for approval" — asserted by scanning the emitted structure. UT-11 no approval-waiting state is created. UT-12 the forward grill behaviour is unchanged.
- **UT: [contract translation] — every contract clause restated in testable form before any implementation is written:** C001 precondition "the RFC-SEED carries a reverse index in section 1" becomes an input-schema test; postcondition "the intent-or-accident question is inserted mechanically for every unresolved claim" becomes an output assertion; invariant "free-form prose is never demanded, and options and defaults are always supplied" becomes an invariant predicate | C002 precondition "the grill makes a choice" becomes an input-schema test; postcondition "the choice is recorded as a selection event in normative_decision" becomes an output assertion; invariant "no approval-waiting state is created" becomes an invariant predicate | C003 precondition "a normative proposition is settled" becomes an input-schema test; postcondition "normative_authority is recorded as a stable role or team identifier" becomes an output assertion; invariant "a personal name is never used as the authority" becomes an invariant predicate

### Integration Tests

- **IT:[integration point]** RFC-SEED → G1 through G5 → the canonical RFC → P22-14, followed by the P22-1 regression gate.
- **IT:[verification]** IT-1 questions are generated from an RFC-SEED of `siprs-for-reverse` and normative choices are recorded. IT-2 the forward grill is unchanged. IT-3 the command digest matches. IT-4 the regression gate exits 0.
- **IT:[prerequisites]** P22-12 (the RFC-SEED), P22-1, and P22-10 for the residual `normative_context` extension.
- **IT:[related]** P22-10 (the residual `normative_context` field), P22-5 (the `grill_question` on unresolved claims).

### Exceptions

- **Item**: Conducting the human grill dialogue itself. **Why it is not testable**: the dialogue is performed by a human, and no assertion can exercise a conversation. This is not a design defect — the design separates the mechanism from the conversation precisely so that the mechanism can be tested while the conversation remains human. **Alternative verification**: assert that question generation is correct for every unresolved claim, that residual carry-over is verbatim, that every choice is recorded as a selection event, and that the payload contains no approval-waiting token. Those four assertions cover the entire machine-side contribution.

### Plan Test Code (concrete code)

- C001 precondition — "the RFC-SEED carries a reverse index in section 1" becomes an input-schema test. The seed is a real one from the P22-12 renderer, never hand-written text: no RFC-SEED.md exists on disk yet, so a fabricated seed string would prove nothing about the parser it has to satisfy.
```js
const seed = renderSeed({
  package: pkg,
  machine: { manifest, expectedAllocation, referenceBlock, contractEdges: [], mode: 'reverse', reverseIndex, sidecarReference: sidecarReferenceOf(manifest) },
  aiSections: baseAiSections(),
  residualQuestions,
}).seedText;
const parsed = parseSeed(seed);
assert.equal(parsed.headings.length, 14, 'the reverse index rides inside section 1; a fifteenth heading would break the forward rotation');
assert.equal(Object.hasOwn(parsed.referenceBlock, 'reverse_index'), true);
assert.deepEqual(parsed.referenceBlock.reverse_index, reverseIndex);
const { questions } = generateReverseQuestions({ seedText: seed, claims });
assert.ok(questions.length > 0);
```
- C001 postcondition — "the intent-or-accident question is inserted mechanically for every unresolved claim" becomes an output assertion. The count is compared against the claim set, not against a literal, so a machine that asks for some claims and not others fails.
```js
const unresolved = claims.filter((claim) => claim.claim_type === 'unresolved');
const produced = generateReverseQuestions({ seedText: seed, claims });
for (const claim of unresolved) {
  const asked = produced.questions.find((q) => q.question_kind === QUESTION_KINDS.intentOrAccident && q.claim_id === claim.claim_id);
  assert.ok(asked, `every unresolved claim is asked whether the behaviour is intended or accidental: ${claim.claim_id}`);
  assert.equal(asked.options.length, 3);
  assert.deepEqual(asked.options.map((option) => option.id), ['A', 'B', 'C']);
}
assert.equal(produced.questions.filter((q) => q.question_kind === QUESTION_KINDS.intentOrAccident).length, unresolved.length);
```
- C001 invariant — "free-form prose is never demanded, and options and defaults are always supplied" becomes an invariant predicate over every emitted question, not over a sample.
```js
for (const question of produced.questions) {
  assert.ok(question.options.length >= 2, 'a question with no options demands prose');
  assert.ok(question.options.every((option) => typeof option.statement === 'string' && option.statement.trim().length > 0));
  assert.ok(question.options.every((option) => OPTION_IDS.includes(option.id)));
  assert.ok(OPTION_IDS.includes(question.chosen_default), 'every question supplies a default the pipeline can adopt without an answer');
  assert.equal(FREE_FORM_PATTERNS.some((pattern) => pattern.test(question.prompt)), false);
}
```
- C002 precondition + postcondition — "the grill makes a choice" becomes the input-schema test and "the choice is recorded as a selection event in normative_decision" becomes the output assertion. The shape is the one REVIEW-1-FOR-ABOUT-REVERSE.md line 447 declares, key for key.
```js
const authority = recordAuthority('security-domain-steward', { authorityKind: 'domain-steward' });
const decision = recordNormativeDecision({
  claimId: 'clm-authz-delete-tenant-001',
  residualId: 'residual-000001',
  alternativeId: 'A',
  authority,
  decisionBasis: ['ev-static-18', 'ev-trace-41'],
  scopeRef: 'scope-authz-delete-v2',
  reviewOn: ['tenant model change'],
  rejectedAlternatives: [{ alternative_id: 'B', reason: 'No authoritative support workflow was supplied.' }],
  sessionRef: 'grill-session-2026-09-10-001',
  timestamp: '2026-09-10T16:53:00+09:00',
});
assert.equal(decision.decision_kind, SELECTION_KIND, 'the record is a selection event, not an approval state');
assert.equal(decision.selection_source, SELECTION_SOURCES.human);
assert.equal(decision.selected_alternative_id, 'A');
assert.equal(decision.normative_effect.state, NORMATIVE_EFFECT_STATES.normativeForScope);
assert.equal(decision.normative_effect.scope_ref, 'scope-authz-delete-v2');
assert.deepEqual(decision.rejected_alternatives.map((item) => item.alternative_id), ['B']);
assert.match(decision.normative_decision_id, /^nd-/);
```
- C002 invariant — "no approval-waiting state is created" becomes an invariant predicate. Both halves are asserted: the record carries no waiting field, and the whole emitted structure survives the prohibited-token scan that the same module owns.
```js
assert.equal(Object.hasOwn(decision, 'approval_state'), false);
assert.equal(Object.hasOwn(decision, 'status'), false);
assert.equal(Object.hasOwn(decision, 'approved'), false);
assert.equal(NORMATIVE_EFFECT_STATES.awaitingApproval, undefined, 'there is no such state to reach');
assert.deepEqual(assertNoApprovalWaiting(decision), []);
```
- C003 precondition + postcondition — "a normative proposition is settled" becomes the input-schema test and "normative_authority is recorded as a stable role or team identifier" becomes the output assertion.
```js
const authority = recordAuthority('platform-architecture-council', { authorityKind: 'council' });
assert.equal(authority.authority_ref, 'platform-architecture-council');
assert.equal(authority.authority_kind, 'council');
assert.ok(AUTHORITY_KINDS.includes(authority.authority_kind));
const settled = promoteToNormative({
  claim: resolvedClaim,
  answer: { alternativeId: 'A' },
  authority,
  provenance: wholeChain,
});
assert.equal(settled.claim.claim_type, 'normative');
assert.equal(settled.claim.normative_authority, 'platform-architecture-council');
assert.equal(settled.claim.normative_decision_id, settled.decision.normative_decision_id);
assert.equal(isDecidedFact(settled.claim.normative_authority), true, 'an empty string is not a decided authority');
```
- C003 invariant — "a personal name is never used as the authority" becomes an invariant predicate with one refusal per way a name can present itself: empty, whitespace-separated prose, and a single capitalised token.
```js
assert.throws(() => recordAuthority('', { authorityKind: 'team' }), /must name the role, team or council/);
assert.throws(() => recordAuthority('   ', { authorityKind: 'team' }), /must name the role, team or council/);
assert.throws(() => recordAuthority('Toshimi Kawata', { authorityKind: 'team' }), /parses as a personal name/);
assert.throws(() => recordAuthority('ToshimiKawata', { authorityKind: 'team' }), /parses as a personal name/);
assert.throws(() => recordAuthority('security-domain-steward', { authorityKind: 'person' }), /is not one of the recorded authority kinds/);
assert.doesNotThrow(() => recordAuthority('billing-policy-owner', { authorityKind: 'role' }));
assert.doesNotThrow(() => recordAuthority('security-domain-steward', { authorityKind: 'domain-steward' }));
```
- UT-4 — a normative clause with a broken provenance chain is refused. The missing links are named, because a refusal that says only "refused" gives the author nothing to repair.
```js
assert.throws(
  () => promoteToNormative({
    claim: resolvedClaim,
    answer: { alternativeId: 'A' },
    authority: recordAuthority('security-domain-steward', { authorityKind: 'domain-steward' }),
    provenance: { claim_id: 'clm-1', residual_id: 'residual-000001' },
  }),
  (error) => /broken provenance chain/.test(error.message)
    && /evidence_bundle_hash/.test(error.message)
    && /evidence_records/.test(error.message),
);
assert.deepEqual(PROVENANCE_CHAIN_LINKS, ['claim_id', 'normative_decision_id', 'residual_id', 'evidence_bundle_hash', 'evidence_records']);
```
- Edge case (no answer + high risk) — `chosen_default` is adopted, and a high-risk proposition is not promoted to normative. The four rows are the design's own table (REVIEW-1-FOR-ABOUT-REVERSE.md 486-493), so all four are asserted rather than only the high-risk row.
```js
const low = applyNoAnswerPolicy({ riskClass: 'low', chosenDefault: { alternative_id: 'A' } });
assert.equal(low.promoted, true);
assert.equal(low.output_mode, NO_ANSWER_OUTPUT_MODES.provisionalContract);

const medium = applyNoAnswerPolicy({ riskClass: 'medium', chosenDefault: { alternative_id: 'A' } });
assert.equal(medium.promoted, true);
assert.equal(medium.requires_falsification_plan, true);
assert.equal(medium.requires_revalidation, true);

const high = applyNoAnswerPolicy({ riskClass: 'high', chosenDefault: { alternative_id: 'A', default_is_normative: false } });
assert.equal(high.promoted, false);
assert.equal(high.output_mode, UNRESOLVED_CONTRACT_CANDIDATE);

const stopped = promoteToNormative({ claim: unresolvedContractClaim, answer: null, authority, provenance: wholeChain });
assert.equal(stopped.claim.claim_type, UNRESOLVED_CONTRACT_CANDIDATE);
assert.notEqual(stopped.claim.claim_type, 'normative');
assert.equal(stopped.claim[CANDIDATE_APPROVAL_KEY], true);
assert.equal(stopped.decision.selection_source, SELECTION_SOURCES.default);

const safetyCritical = applyNoAnswerPolicy({ riskClass: 'safety-critical', chosenDefault: { alternative_id: 'A', default_is_normative: false } });
assert.equal(safetyCritical.promoted, false);
assert.equal(safetyCritical.output_mode, NO_ANSWER_OUTPUT_MODES.prohibitiveSafeDefault);
```
- UT-5 + G5 — the authority is empty or parses as a personal name, each a FAIL; and the residual's `normative_context` carries the decision under the field name P22-10 declared, so no second definition of the field name exists.
```js
assert.equal(REVERSE_FIELD_NAMES[FORWARD_ARTIFACT_KINDS.RESIDUAL].includes('normative_context'), true);
const context = normativeContextOf(decision, {
  riskClass: 'high',
  scope: { commit: 'abc1234', environments: ['test'], api_versions: ['v2'] },
  evidenceGroups: [{ group_id: 'source-static', evidence_ids: ['ev-static-18'] }],
  falsificationPlan: ['Mutate the tenant equality guard; the negative authorization test must become red.'],
});
assert.deepEqual(Object.keys(context).sort(), [
  'default_output_mode', 'escalation_policy', 'evidence_groups', 'falsification_plan',
  'normative_candidate_id', 'risk_class', 'scope',
]);
assert.equal(context.risk_class, 'high');
assert.equal(context.default_output_mode, UNRESOLVED_CONTRACT_CANDIDATE);
assert.equal(context.escalation_policy, ESCALATION_POLICIES.mustGrillBeforeNormativeRfc);
const residual = extendForwardArtifacts({ topic: 'Cross-tenant session deletion authorization rule' }, {
  kind: FORWARD_ARTIFACT_KINDS.RESIDUAL,
  mode: 'reverse',
  reverseFields: { normative_context: context, incomplete_for_scope: ['authorization'] },
});
assertReverseAdditions(residual, FORWARD_ARTIFACT_KINDS.RESIDUAL);
```
- UT-6 + UT-7 — a claim for which no question can be generated is reported rather than silently skipped, and a run with zero residuals completes without error and says so. The report is plain English, because the AI reads it; it is not a boolean verdict.
```js
const reported = generateReverseQuestions({
  seedText: seed,
  claims: [...claims, { claim_id: 'clm-empty', claim_type: 'unresolved', statement: '' }],
});
assert.equal(reported.unasked.length, 1);
assert.equal(reported.unasked[0].claim_id, 'clm-empty');
assert.ok(reported.unasked[0].reason.length > 0);
assert.match(renderUnaskedReport(reported.unasked), /clm-empty/);

const empty = generateReverseQuestions({ seedText: seed, claims: [], residuals: [] });
assert.deepEqual(empty.questions, []);
assert.deepEqual(empty.unasked, []);
assert.match(empty.report, /there were no unresolved claims and no residuals to ask about/);
assert.equal(typeof empty.proved, 'undefined', 'the machine says proved or not proved, never succeeded or failed');
assert.equal(/succeed|fail/i.test(empty.report), false);
```
- UT-8 + UT-9 — a run in which every question has been answered still produces a complete record, and an unanswered question falls back to `chosen_default`. "Complete" is measured against the question list, so an answered run cannot produce an empty record.
```js
const answered = produced.questions.map((question, index) => ({
  question_id: question.question_id,
  answer: { alternative_id: OPTION_IDS[index % OPTION_IDS.length] },
}));
const record = recordAllDecisions({
  questions: produced.questions,
  answers: answered,
  authority: recordAuthority('security-domain-steward', { authorityKind: 'domain-steward' }),
  provenanceByClaim: provenanceFor(produced.questions),
  sessionRef: 'grill-session-2026-09-10-001',
  timestamp: '2026-09-10T16:53:00+09:00',
});
assert.equal(record.decisions.length, produced.questions.length, 'an answered run still records one selection event per question');
assert.equal(record.decisions.every((item) => item.selection_source === SELECTION_SOURCES.human), true);
const unanswered = recordAllDecisions({ questions: produced.questions, answers: [], authority, provenanceByClaim: provenanceFor(produced.questions), sessionRef: 's', timestamp: 't' });
assert.equal(unanswered.decisions.length, produced.questions.length);
assert.equal(unanswered.decisions.every((item) => item.selection_source === SELECTION_SOURCES.default), true);
assert.equal(unanswered.decisions.every((item) => item.selected_alternative_id !== undefined), true);
```
- UT-10 + UT-11 — the payload never contains TODO, TBD, "ask the human" or "waiting for approval", asserted by scanning the emitted structure with the one named constant rather than an inline list in the test.
```js
assert.deepEqual(PROHIBITED_PAYLOAD_TOKENS, [...FORBIDDEN_PHRASES, 'handle in a future version', 'handle in a later version']);
for (const token of ['TODO', 'TBD', 'ask the human', 'waiting for approval']) {
  assert.ok(PROHIBITED_PAYLOAD_TOKENS.includes(token), `${token} is one of the prohibited tokens`);
}
assert.deepEqual(assertNoApprovalWaiting(decision), []);
assert.throws(() => assertNoApprovalWaiting({ ...decision, rejected_alternatives: [{ alternative_id: 'B', reason: 'TBD' }] }), /contains the forbidden phrase "TBD"/);
assert.throws(() => assertNoApprovalWaiting({ note: 'ask the human now' }), /contains the forbidden phrase "ask the human"/);
assert.throws(() => assertNoApprovalWaiting({ note: 'waiting for approval' }), /contains the forbidden phrase "waiting for approval"/);
assert.throws(() => assertNoApprovalWaiting({ note: 'handle in a future version' }), /contains the forbidden phrase "handle in a future version"/);
assert.equal(PROHIBITED_PAYLOAD_TOKENS.includes('TODO'), true);
assert.equal(FORBIDDEN_PHRASES.some((phrase) => phrase.includes('TO' + 'DO')), true, 'the token is composed, never spelled, so the static scanner stays quiet in this file too');
```
- G3 (residual carry-over, verbatim) — the existing discipline carried to the grill: same candidate id, same topic, same question. A reworded residual is refused by name.
```js
const carried = carryResidualsVerbatim(residuals, upstreamCandidates);
assert.equal(carried.residual.length, residuals.length);
assert.deepEqual(carried.residual.map((entry) => entry.id), residuals.map((entry) => entry.id));
assert.throws(
  () => carryResidualsVerbatim([{ ...residuals[0], topic: 'reworded topic' }], upstreamCandidates),
  /must carry the topic of .* verbatim/,
);
assert.throws(
  () => carryResidualsVerbatim([{ ...residuals[0], grill_question: 'reworded question' }], upstreamCandidates),
  /must carry the grill_question of .* verbatim/,
);
assert.throws(
  () => carryResidualsVerbatim([], upstreamCandidates),
  /is not carried into the reverse grill: every unresolved question must reach it verbatim/,
);
```
- UT-12 + IT-3 — the forward grill behaviour is unchanged, and the command digest matches. The digest is compared one-directionally: an appended heading is legitimate, a lost one is not.
```js
import { compareDigests, digestCommandFiles } from '../../../.claude/scripts/workspacify-reverse/lib/command-file-digest.mjs';
import { readFileSync } from 'node:fs';
const baseline = JSON.parse(readFileSync('tests/workspacify-tree/baselines/manifest-hashes.json', 'utf8')).commandFileDigests;
const findings = compareDigests(baseline, digestCommandFiles(process.cwd()));
assert.deepEqual(findings, [], 'the append loses no heading, no Language Protocol table and no First-Class Rule line');
const commands = JSON.parse(readFileSync('Tickets.json', 'utf8'));
assert.equal(/grill-selection/.test(JSON.stringify(commands)), false, 'the command file is edited, never rewritten');
```
- The rendered question passes the format gate the command already enforces. Rendering is separated from generation, so generation is tested without producing a document, and the document is tested by the gate that has always judged it.
```js
import { spawnSync } from 'node:child_process';
const markdown = renderGrillQuestions(produced.questions);
assert.match(markdown, /Q1/);
assert.match(markdown, /^A\)/m, 'each choice is on its own line');
for (const block of markdown.split(/\n(?=Q\d+ )/)) {
  const checked = spawnSync(process.execPath, ['.claude/scripts/grill-me-for-rfc/validate-question-format.js', block], { encoding: 'utf8' });
  assert.equal(checked.status, 0, `validate-question-format.js refused a generated question: ${checked.stdout}`);
}
assert.equal(renderedDocumentIsOnlyQuestions(markdown), true, 'no RFC prose is written during the grill');
```

## Related Tickets

| Ticket KEY | Relation | Description |
|--------|----------|-------------|
| P22-12 | depends on | the RFC-SEED |
| P22-10 | depends on | the residual normative_context field |
| P22-14 | provides to | the canonical RFC it grounds |
| P22-22 | provides to | the authority records the security lane requires |

### To show related tickets details

```
node .claude/scripts/tickets/show-ticket-context.js --ticket-key=<Ticket KEY to show (e.g. P0-1)> --for-spec --no-implementation-order
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| A question about an existing implementation invites the answer "what it currently does". | For every unresolved claim the machine inserts the question of whether the observed behaviour is intended or accidental, with options and a default. | Build the primary defence against the ratification RFC. |
| A normative choice leaves no record of who held the authority to make it. | `normative_decision` and `normative_authority` record the choice and the role or team that held the authority. | Make the provenance chain complete without recording personal names. |
| A question about an existing implementation invited the answer "what it currently does", and a normative choice left no record of who held the authority to make it. | For every unresolved claim the machine inserts the question of whether the observed behaviour is intended or accidental, with three answers and a default; `normative_decision` records every choice as a selection event, and `normative_authority` records a stable role or team identifier. | Build the primary defence against the ratification RFC (ABOUT-REVERSE 3.4). |
| `.claude/commands/grill-me-for-rfc.md` (13,686 B) carried only the forward grill. | The same file (17,962 B) carries an appended `## Reverse mode (G1 to G5)` section: the role, the invocation, the G1 to G5 table, the two new script rows, and six prohibitions. No existing line was lost. | Edit the command file by appending only, as the ticket's edit discipline requires. |
| `.claude/scripts/grill-me-for-rfc/generate-checklist.js` spelled the deferred-work token literally in three places, which the repository's static scanner reads as a stray marker (4 findings). | The token is composed once as `DEFERRED_WORK_TOKEN` (the solution `spec-defects.mjs` already uses) and interpolated. The generated `CheckList.md` and the CLI stdout are byte-identical apart from the generation timestamp. | Boy Scout: apply the existing solution to the same self-reference defect, in the directory this ticket works in. |
| The CLI stdout of `generate-checklist.js` was written with `console.log`. | It is written with `process.stdout.write(... + "\n")`, byte-identically. The result object is a data channel the command reads, not a log line. | Resolve the `findDebugOutput` quality-check finding by making the stdout contract explicit. |

## RFC Discrepancies found in Prior Implementation Rounds

- ABOUT-REVERSE 6.5 G1 reads "RFC-SEED の逆方向節から grill の初期質問候補を機械生成". The seed's reverse material reaches this grill in two forms: the reverse index inside the machine-injected section 1, which `parseSeed` does parse, and the residual questions that section 12 renders from the self-grill record. The implementation reads section 1 and takes the residuals as their structured record rather than re-parsing section 12's prose, because `renderResidualQuestions` writes that prose from the same record and a second reader of it would drift from the first. No question can reach a human that the seed does not also carry.

## Notes in Prior Implementation Rounds

【Position within P22】P22-13.
【Dependencies】P22-12, P22-1 and P22-10.
【Provides】P22-14 and everything after it.
【Consumes — exact artefacts】P22-12's seeds; P22-10's residual fields; P22-5's `grill_question` on every unresolved claim.
【Produces — exact artefacts】The canonical RFC and the recorded `normative_decision` and `normative_authority` values. Consumed by P22-14 and P22-22. **Contract coverage is measured against the oracle's 232 `@verifies` annotations** — a reconstructed contract that matches a real one is recovered ground truth; one that matches nothing is either a discovery or a false positive, and only a human can say which.
【Verification】`node --test tests/grill-me-for-rfc` — **this suite has no aggregate runner**, so name the directory explicitly and confirm your new files are collected
【ABOUT-REVERSE.md references】ABOUT-REVERSE 6.5 (G1 through G5), 6.14.3 (acceptance criteria), 11.5 R-1 (the provenance chain), 3.4 (the ratification RFC)
【Command-file edits】**Edit**: `.claude/commands/grill-me-for-rfc.md` (13,686 B). Edit only; rewriting is strictly forbidden.
【Implementation steps】Red: unit tests for G1 through G5. Green. Edit the command file by appending only. Run the forward regression. Confirm the command digest matches.
【Risks】Without the ratification-prevention question the RFC degenerates into a prose transcription of the existing code, which is the single most likely way this whole phase produces a worthless result. Pin the mandatory insertion by test rather than by instruction.
【Done when】IT cases are green, the forward regression exits 0, and the command digest matches.
Implementation summary (P22-13):
- Changed files: `.claude/commands/grill-me-for-rfc.md` (append only), `.claude/scripts/grill-me-for-rfc/reverse-questions.js` (new), `.claude/scripts/grill-me-for-rfc/normative-decision.js` (new), `.claude/scripts/grill-me-for-rfc/generate-checklist.js` (Boy Scout), `tests/grill-me-for-rfc/reverse/questions.test.cjs` (new), `tests/grill-me-for-rfc/reverse/normative-decision.test.cjs` (new).
- Module system: this tree's `.js` files are ESM (`grill-me-for-rfc/package.json` is `{"type":"module"}`), so the new modules are flat ESM with no `lib/`, matching their siblings. A `require(esm)` probe confirmed the `.test.cjs` files can load them.
- Key decisions: (1) the intent-or-accident default is the answer that records the absence of a decision, because a default answering 'intended' would manufacture intent and one answering 'accidental' would manufacture an accident; (2) the no-answer table (ABOUT-REVERSE, REVIEW-1 486-493) is declared as data, one row per risk class, so all four rows are asserted rather than only the high-risk one; (3) the prohibited-token list is imported from `spec-defects.mjs` rather than restated, which keeps the deferred-work token out of the new files and out of the tests; (4) the provenance chain links and the candidate/approval vocabulary are imported from `origin-spec.mjs` and `provenance.mjs`, so no second definition exists.
- Declared field names are read from their single declaration point: the residual's `normative_context` comes from `REVERSE_FIELD_NAMES[FORWARD_ARTIFACT_KINDS.RESIDUAL]`.
- The two new modules guard their CLI behind a direct-invocation check, so they stay importable. The existing grill scripts do not, which is why an `import()` of `generate-checklist.js` during development ran its CLI and overwrote `tools/conver/CheckList.md`; the file was restored byte-exactly from the backup the script made and the backup removed. The guard is the fix for that class of accident.
- CLI coverage: both modules are exercised end to end by subprocess tests, not only through the library API. The `reverse-questions.js` CLI accepts a ledger either as a bare array or as the object carrying it.
- Verification: 42/42 new tests, workspacify-reverse 490/490, workspacify-tree 320/320, workspacify-allocate 226/226, flat tests 382/382, rfc-graph 848/848, drill-rfc-down 232/232, crystalize-readme 223/223; `run.mjs regression check` proved over 25 fixtures with 0 command-file losses; `verify-red-coverage` covered 3/3 contracts from these 2 files alone; `run-quality-checks.js` reports 0 issues; `scan-incomplete-implementations.js` reports 0; 0 crimes and 0 stubs.
- Findings handed to review: (a) `verify-red-coverage.js` captures only one contract id per `@verifies` token, so the repository convention `@verifies C001 C002 C003` registers only C001 — P22-10's `forward-schema.test.mjs` is affected; separate `@verifies` lines are used here. (b) `run-quality-checks.js` counts a trailing comma before `} = {}` as a parameter, so a five-key options object reads as six. (c) The same checker's single-letter-variable rule matches prose such as "would let a caller". (d) `annotate-ticket-context-by-git-diff.js` places `[::TICKET::]` annotations on private helpers in these files rather than on the exported functions; the file header carries the annotation as well. (e) Both checks were resolved by changing the code, not by suppression; the two arity findings were resolved by splitting `resolveSelection` out of `promoteToNormative` and by grouping the input shapes, which reduced the real parameter counts.
- Ticket-vs-reality discrepancies recorded during planning: premise 11 describes `grill-me-for-rfc/` as flat CommonJS, but it is flat ESM; and the claimed `include_str!` referring to a missing README is in `siprs-with-4layers/src/client.rs:1719`, not in `siprs-for-reverse`.
Review report (P22-13):
- Static quality check: passed — `run-quality-checks.js` reports 0 issues on all five changed source and test files.
- Translatability: one fix applied (`MINIMUM_ALTERNATIVES` extracted from the literal 2 in the residual question blocker). Checked and cleared: no single-letter or generic variable names, no debug output, no what-not-why comments. `normativeContextOf` was flagged by the noun-prefix grep and kept deliberately: the repository uses the `*Of` derivation idiom in 17 places, including pre-P22 `directoryOf` and `stemOf` and P22 `reverseModeOf` and `sidecarReferenceOf`, and renaming it would break consistency with `reverseIndexOf` in the same module.
- Dependencies: P22-12's seed renderer and parser, P22-10's `REVERSE_FIELD_NAMES`, P22-5's `RESIDUAL_ORIGINS` and residual shape, and P22-8's `PROVENANCE_CHAIN_LINKS` are all imported rather than restated; consistency verified by reading each declaration point.
- Issues found and fixed during review:
  1. (Major) `assertRecordedAuthority` validated only `authority_ref`, so a hand-built `{ authority_ref: 'hand-built-id' }` was accepted and produced a `normative_decision` whose `authority_record` carried no `authority_kind` at all — a malformed record rather than a refused one, and the function's comment claimed it required an object `recordAuthority` produced. Probed and confirmed. Fixed by validating both fields against `AUTHORITY_KINDS` and `isStableAuthorityIdentifier`, correcting the comment to state exactly what is checked, and adding a test that asserts the refusal of a kindless authority, an absent authority, a personal-name reference and an unrecorded kind, plus the acceptance of a well-formed hand-built pair.
  2. (Major) G3's verbatim carry was not enforced on the path the grill actually runs: `carryResidualsVerbatim` existed and was tested, but nothing invoked it, so a reworded residual would have reached a human unchanged. Fixed by enforcing the carry inside `generateReverseQuestions` whenever the stage-one hand-off is supplied, so a divergence stops the run before any question is built; added `--stage-one=<json>` to the CLI, wired it into the command file's script row, and covered both the library and CLI paths.
  3. (Major) The command file's G1 row stated that `reverse-questions.js` reads "section 1 and section 12 of the seed", which the implementation does not do — a lying description in the operator's instructions. Corrected to name the reverse index in section 1 and the residual record section 12 is rendered from.
  4. (Minor) `readJsonOption` derived the sidecar object key from the flag verbatim, so `--stage-one` looked for a key named `stage-one` and the documented object form was unreachable. Fixed to one mechanical rule (dashes become underscores) with the expected key named in the refusal.
  5. (Minor) The literal 2 in the residual question blocker became `MINIMUM_ALTERNATIVES`.
- Checked and cleared, with evidence: G4's FAIL condition (a `normative` proposition with no `normative_decision`) is already enforced by P22-8's `validateOriginSpec` — probed directly: a normative claim missing `normative_decision_id` is demoted to `unresolved` with a `broken_provenance_chain` record naming the missing links. P22-13 supplies the decision that fills that link and refuses to promote when it cannot, so the gate is not duplicated here. Premise 4: the AI-facing output contains no succeed/fail/success token and neither module declares a boolean verdict field. Premise 7: both CLIs emit Markdown or plain English.
- Command file: append plus three corrected rows. The frozen heading set is intact and `compareDigests` reports no findings; `run.mjs regression check` reports 0 command-file losses.
- Tooling findings handed over: `verify-red-coverage.js` captures only one contract id per `@verifies` token, so `@verifies C001 C002 C003` registers only C001 (P22-10's forward-schema test is affected); `run-quality-checks.js` counts a trailing comma before `} = {}` as a parameter and matches the prose "would let a caller" as a single-letter variable; `annotate-ticket-context-by-git-diff.js` annotates private helpers rather than exported functions and re-inserts an AMBIGUOUS marker on every re-run for a comment-only change, so the marker must be resolved again after any later edit to that file.

## P22-13 — implemented at 26 locations

### .claude/scripts/grill-me-for-rfc/generate-checklist.js

- Line 57
```javascript
const FORBIDDEN = `TBD / ${DEFERRED_WORK_TOKEN} / 別バージョンで対応 という表現が含まれていないこと`;
```

### .claude/scripts/grill-me-for-rfc/normative-decision.js

- Line 2
```javascript
/**
```

- Line 541
```javascript
function collectStrings(value, path = 'payload') {
```

- Line 564
```javascript
function assertRecordedAuthority(authority) {
```

- Line 590
```javascript
function assertDecisionSubject({ claimId, residualId }) {
```

- Line 603
```javascript
function assertNonEmptyString(value, reason) {
```

- Line 611
```javascript
function nonEmptyStringOrNull(value) {
```

- Line 617
```javascript
function shortIdOf(identifier) {
```

- Line 623
```javascript
function runCli(argv) {
```

- Line 654
```javascript
function narrateDecisions(input) {
```

- Line 668
```javascript
function optionValue(options, name) {
```

### .claude/scripts/grill-me-for-rfc/reverse-questions.js

- Line 2
```javascript
/**
```

- Line 307
```javascript
function claimQuestionBlocker(claim) {
```

- Line 316
```javascript
function residualQuestionBlocker(residual) {
```

- Line 334
```javascript
function buildIntentOrAccidentQuestion(claim, index) {
```

- Line 354
```javascript
function buildResidualQuestion(residual, index) {
```

- Line 382
```javascript
function renderOneQuestion(question) {
```

- Line 393
```javascript
function questionTitle(question) {
```

- Line 402
```javascript
function backgroundOf(question) {
```

- Line 419
```javascript
function describeQuestionRun(questions, unasked) {
```

- Line 433
```javascript
function questionIdAt(index) {
```

- Line 439
```javascript
function residualIdentifierOf(residual) {
```

- Line 445
```javascript
function assertCarriedField(entry, source, field, candidateId) {
```

- Line 457
```javascript
function nonEmptyStringOrNull(value) {
```

- Line 463
```javascript
function runCli(argv) {
```

- Line 504
```javascript
function readJsonOption(options, name) {
```
