# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P22-17: /find-omissions and /crystalize-readme: references that return an omission to its originating uncertainty

**Ticket Key**: P22-17 · **Phase**: 22

---

## Background

**Shared premises — binding on all 22 P22 tickets. Read this first.**

P22 implements the reverse rotation: taking a project that already contains a substantial implementation and turning it into conver's four loops (upstream / implementation / release / evolution). Forward rotation already works completely; reverse rotation does not exist yet. `docs/ABOUT-REVERSE.md` is the full design. The implementation loop runs one ticket per session and context does not carry across tickets, so this block is repeated verbatim in all 22 tickets. A session that sees only its own ticket must still know what the whole is for and what it may not break.

(1) **Never break the forward rotation.** The four existing loops work. No change in P22 may alter forward behaviour. A byte-identical `manifest_hash` over the forward fixtures, captured before any change and reproduced after, is the only accepted proof. P22-1 builds that gate; every later ticket runs it after its own step. "It looks fine" is not evidence and is not accepted.
(2) **Rewriting any `.claude/commands/*.md` is strictly forbidden.** Only edits are permitted — append, or correct the minimum number of lines. The Language Protocol table, the First-Class Rule, the workflow steps and the script list must survive character for character. The nine existing command files are: `workspacify-tree`, `workspacify-allocate`, `grill-me-for-rfc`, `graphify-rfc`, `boundify-graph`, `split-to-tickets`, `find-omissions`, `crystalize-readme`, `drill-rfc-down`. P22-9 additionally *creates* `workspacify-reverse.md`, which is a new file, not an edit.
(3) The full design lives in **`docs/ABOUT-REVERSE.md`**. Each ticket cites the exact sections it depends on. Read those sections before implementing; they are the specification of record.
(4) The machine may only ever say **"proved" or "not proved"**. "Succeeded" and "failed" are not in its vocabulary, because whether reverse engineering succeeded is a judgement a human makes after several loop rounds. Any script that emits a boolean success verdict is wrong by construction.
(5) What is deterministic, a script decides and the AI accepts the verdict. Only genuinely semantic judgement is left to the AI, and the material it needs is placed in front of it — as plain, deliberately helpful English — immediately before the decision. Do not widen the AI's judgement surface beyond what the design calls for.
(6) The `[::STUB::]` marker discipline is absolute and admits no exception. Any incomplete implementation carries a marker naming the ticket that will resolve it.
(7) Output format: emit JSON only when another script consumes it, a schema validates it, a hash covers it, or a gate uses it as a predicate. **Anything the AI reads in order to decide is Markdown, written in plain and deliberately helpful English.** Struggling to read a script's output is a defect in the script, not a cost of doing business.
(8) The `Initial Design Artifact — RFC-driven Implementation` header is never deleted, altered or commented out (supreme law 4). This holds without exception, including in files this phase touches.
(9) **The paired trees are the measuring instrument.** `siprs-with-4layers` is the same project taken through the forward rotation to RESIDUE 0 and therefore holds what reverse rotation is trying to reconstruct; `siprs-for-reverse` is that project with those artefacts stripped and is what the analysis runs against. Comparing an output against the answer key yields a list of disagreements, never a score and never a verdict — classifying each disagreement is a human's work. P22-2 builds the harness; the tickets whose output is comparable run it.
(10) **The source-analysis technology is decided once, in P22-4, and never re-decided.** Four separate sessions (P22-4, P22-5, P22-6, P22-7) must parse the same Rust source, and no context passes between them. P22-4 evaluates the candidates **against the expert consultation recorded in `docs/ABOUT-ANALYSIS-TECH.md`**, records the choice and its rationale in **`docs/P22-ANALYSIS-TECH.md`**, and every later analysis ticket reads that document instead of choosing again. The repository offers no precedent to inherit — `.claude/scripts/rfc-graph/analyze-source-structure.js` is regex-based and is not equal to extracting guards, invariants and state machines — so the decision is genuinely open and must be made deliberately and written down. **The scope is six languages, not one**: Rust, TypeScript/JavaScript, Go, Python and C/C++, because a technology that only handles the first target would be an implementation tuned to that target, which is explicitly forbidden. Two sessions independently choosing different parsers would produce mutually unreadable output and no test would catch it, because each session's tests would pass against its own choice. **The chosen toolchain is recorded in two places, and both are required**: the reasoning in `docs/P22-ANALYSIS-TECH.md`, and the machine-readable declaration in `ENV-DEPS.json` built by P22-1, where each new tool is added with the rotation that needs it and the ticket that provides it. Adding a tool is an entry in that declaration — never a new installation mechanism, and never a dependency left undeclared because it happened to be present on the machine where it was developed.
(11) **Match the surrounding directory, do not invent a layout.** The repository uses two conventions and they are not interchangeable. `.claude/scripts/rfc-graph/`, `grill-me-for-rfc/`, `conver/` and `tickets/` are flat CommonJS `.js` with no `lib/` subdirectory; `.claude/scripts/workspacify-tree/`, `workspacify-allocate/` and `workspacify-reverse/` are ESM `.mjs` under `lib/`. A ticket that names a path must name one that matches its siblings, and must not introduce a `lib/` directory into a flat tree or a CommonJS module into an ESM one. The same applies to test files: `.test.cjs` beside CommonJS, `.test.mjs` beside ESM.
(12) **The expert answer received in `docs/ABOUT-ANALYSIS-TECH.md` (the section headed 「専門家の回答」, with the request-side verification record in its §7) is binding on the analysis design. Three of its corrections outrank any earlier wording in this repository, including the wording of earlier P22 tickets.** (a) `observed` may only ever mean *a fact read from the source text or its syntax tree*. Runtime behaviour, dynamic dispatch targets, post-preprocessing composition and generated code are not `observed` without execution, build or trace evidence, so every evidence item carries `evidence_mode`: `source_static` | `build_semantic` | `runtime_dynamic`. (b) `derivation_group` is **not** a deterministic identifier for a human design intent — that cannot be computed from file contents. It is a lineage graph over observable relations (same AST span, same generator, same commit, same data-flow origin), and independence is an explicit stored aggregation policy over that graph; `independence_assessment: "unknown"` is a legal and expected value, not a failure. (c) E13 is **TCE** (trivial / syntactic / compiler-normalisation equivalence). General semantic equivalence is undecidable and is not a goal; an AST-normalised match proves syntactic equivalence under a named configuration and nothing more. (d) Where the answer separates a *candidate* from a *decided* fact (E11, E13, E14), the separation is enforced at the type level — a candidate cannot be passed, stored or serialised where a decided fact is required — and a candidate carries `requires_human_approval`. A session that finds itself asserting a contract, an equivalence or a generated test as settled has violated this premise. **§7 of that document also records one error of ours that the answer exposed: the claim that CodeQL does not support Rust was wrong.**

---

## This ticket — P22-17

**Goal**: Make it possible to trace an omission or a RESIDUE back to the uncertainty that produced it.

**Purpose**: A shortfall is recorded as a shortfall of the original hypothesis, norm or scope, not as "not enough tests". Without the return chain, recurrence and circularity cannot be followed.

**Motivation**: ABOUT-REVERSE 6.12.3 layer B. The return chain is what makes an omission actionable rather than merely countable, and it is the edge described in 6.10.1 read in the opposite direction.

**Constraints**: The change is limited to additive optional fields. The forward output of both commands must not change: an omission produced during forward rotation carries exactly the fields it carried before.

**Command-file edit discipline**: **Edit only; rewriting is strictly forbidden.** Append, or correct the minimum number of lines. The Language Protocol table, the First-Class Rule, the workflow steps and the script list must not lose a single character. Permitted changes are limited to (a) `mode === "reverse"` branch notes, (b) sections that apply only to reverse rotation, (c) new rows in the script list. Re-run the P22-1 command digest before and after and confirm it is unchanged.

**Where this ticket sits in the 22.** 1 regression gate · 2 experiment design (holdout + answer key) · 3 spike · 4 R0-R2 scope and structure · 5 R3-R3.5 semantics and ledger · 6 R4-R5.5 history, gaps, oracle · 7 R6-R6.5 Red plan and falsification · 8 R7-R8 serving and origin spec · 9 command and zg · 10 forward schemas · 11 workspacify-tree reverse · 12 workspacify-allocate reverse · 13 grill reverse · 14 graphify reverse · 15 boundify reverse · 16 split-to-tickets reverse · **→17 omission and RESIDUE return** · 18 dynamic analysis · 19 Red execution · 20 logical/physical two-layer · 21 staleness into drill · 22 security lane

**Step 0 — before you write anything.** Confirm P22-10 is complete and `run.mjs regression check` exits 0. Read both `.claude/commands/find-omissions.md` (21,407 B) and `.claude/commands/crystalize-readme.md` (17,408 B) before editing — append only, and this is the only ticket that edits two command files.

## Scope

- **Scope:**
-   - [File] `.claude/commands/find-omissions.md` and `.claude/commands/crystalize-readme.md` (edit only), `tickets/lib/return-refs.js`, `tests/tickets/reverse/return-refs.test.cjs` (new)
-   - [Action] add, plus a minimal edit to both command files
-   - [Detail] Add `affected_claim_ids` and `origin_residual_ids` to omissions, and `scenario_ref` and `next_route` to RESIDUE, as additive optional fields, realising the layer B return path.
-   - [Before→After] An omission is a count with no ancestry → an omission names the claims and residuals it descends from, and a RESIDUE names the scenario and the next route
-   - [API] The reverse branch of the existing omission and residue flows
-   - [Schema] Optional `affected_claim_ids`, `origin_residual_ids` on omissions; optional `scenario_ref`, `next_route` on RESIDUE
-   - [Config] None
-   - [Dependency] P22-10 and P22-1
- **Out of scope:** Changing the forward behaviour of either command in any way other than adding optional fields that are absent in forward mode.
- **Affected areas:** P22-21, which consumes the return chain when propagating staleness.

## Implementation Target Files

- `.claude/commands/find-omissions.md`
- `.claude/commands/crystalize-readme.md`
- `.claude/scripts/tickets/lib/return-refs.js`
- `tests/tickets/reverse/return-refs.test.cjs`

## Investigation

- **Measured 2026-09-10.** Two edit targets: `.claude/commands/find-omissions.md` at 21,407 B and `.claude/commands/crystalize-readme.md` at 17,408 B. This is the only ticket in P22 that edits two command files, which is why the digest check is run for both.
- The return edge has a forward precedent in the existing machinery: `originalTicketKey` in `tickets/add-omission-ticket.js` already links an omission ticket back to the ticket that produced it, so the concept of an omission carrying an ancestry reference is established rather than new.
- The three kinds of gap recorded in 1.3 are the reason the return chain must reach the originating uncertainty rather than merely the producing ticket: an omission can descend from a missing hypothesis, a missing norm or a missing scope, and those three are distinguished only by where the chain terminates.
- The forward-compatibility requirement is strict because `crystalize-readme` produces RESIDUE, and RESIDUE 0 is the stated success condition for the whole reverse rotation. A change that altered the forward RESIDUE shape would corrupt the very measurement the phase is judged by.
- The optionality discipline matches P22-10: additions appear only where the value is known, so a forward consumer never observes a new key.

## Acceptance Criteria

- **[Happy path]:** An omission names its originating claims and residuals, and a RESIDUE names its scenario and next route.
- **[Error case]:** An unresolvable reference is reported rather than emitted as a dangling identifier.
- **[Edge case]:** An omission with no originating uncertainty carries no new fields and remains valid.

## Invariants

- 【Normal establishment】An omission gains `affected_claim_ids` and `origin_residual_ids`. A RESIDUE gains `scenario_ref` and `next_route`. Both are optional and appear only when the values are known.
- 【On error】A reference that cannot be resolved is reported rather than emitted as a dangling identifier. A forward omission with no reverse references keeps its original shape exactly.
- 【Internal state】The forward output of both commands is byte-identical. No required field is added, so a forward consumer that does not know about the new fields continues to work unchanged.
- 【Boundary】An omission with no originating uncertainty carries no new fields and is valid. A run producing zero omissions exits normally and reports that there were none.

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — N/A

- **Precondition**: an omission is detected
- **Postcondition**: affected_claim_ids and origin_residual_ids are added
- **Invariant**: the forward omission output is not changed

### C002 — N/A

- **Precondition**: a RESIDUE is recorded
- **Postcondition**: scenario_ref and next_route are added
- **Invariant**: the forward RESIDUE output is not changed

## Boy Scout Rule

- `attachReturnRefs`, `resolveReturnRef`, `assertForwardShapeUnchanged` are verb phrases; `affectedClaimIds`, `originResidualIds`, `scenarioRef`, `nextRoute` are domain concepts.
- The identifier namespaces are shared constants with P22-5 and P22-10, so a reference written here resolves there without a translation step.
- Resolution is separated from emission, so a reference can be checked before it is written and a dangling one never reaches the output.
- The forward-shape assertion is written once and applied to both commands, rather than duplicated with slightly different wording in each.

## Test Plan

### Unit Tests

- **UT:[normal]** UT-1 an omission gains `affected_claim_ids` and `origin_residual_ids`. UT-2 a RESIDUE gains `scenario_ref` and `next_route`. UT-3 a forward omission carries exactly its original fields.
- **UT:[error]** UT-4 an unresolvable reference is reported rather than emitted as a dangling identifier. UT-5 a malformed claim identifier is reported with the omission it was attached to.
- **UT:[boundary]** UT-6 an omission with no originating uncertainty carries no new fields and is valid. UT-7 a run producing zero omissions exits normally and states that there were none. UT-8 a single omission is valid.
- **UT:[invariant]** UT-9 the forward output of both commands is byte-identical to its pre-change form. UT-10 every added field is optional, so a forward consumer that is unaware of them still works. UT-11 the RESIDUE structure gains fields without losing any existing one.
- **UT: [contract translation] — every contract clause restated in testable form before any implementation is written:** C001 precondition "an omission is detected" becomes an input-schema test; postcondition "affected_claim_ids and origin_residual_ids are added" becomes an output assertion; invariant "the forward omission output is not changed" becomes an invariant predicate | C002 precondition "a RESIDUE is recorded" becomes an input-schema test; postcondition "scenario_ref and next_route are added" becomes an output assertion; invariant "the forward RESIDUE output is not changed" becomes an invariant predicate

### Integration Tests

- **IT:[integration point]** The claim ledger and the residuals → the return references → the P22-1 regression gate.
- **IT:[verification]** IT-1 an omission produced in reverse mode names the claims and residuals it descends from. IT-2 the forward outputs of both commands are unchanged. IT-3 both command digests match. IT-4 the regression gate exits 0.
- **IT:[prerequisites]** P22-10 has added the reverse fields, and P22-1 has frozen the baselines.
- **IT:[related]** P22-21 consumes the return chain to propagate staleness into the drill flow.

### Exceptions

- **Item**: Verifying that the recorded return chain reaches the correct originating uncertainty. **Why it is not testable**: correctness of the ancestry is a semantic claim about the relationship between two artefacts, and the source records identifiers rather than relationships. This is not a design defect — the design records the reference so that a human can follow it during the evolution loop, which is where the judgement belongs. **Alternative verification**: assert that every emitted reference resolves to an identifier that exists, that unresolvable references are reported rather than emitted, and that the forward output is byte-identical.

### Plan Test Code (concrete code)

- UT-1 [Normal] — C001 precondition, as an input-schema test: a reverse omission is the forward omission plus a declared reverse origin, and both references resolve.
```js
// tests/tickets/reverse/return-refs.test.cjs — const { describe, it } = require("node:test");
const assert = require("node:assert/strict");
const path = require("node:path");
const PROJECT_ROOT = path.resolve(__dirname, "../../..");
const {
  RETURN_REFERENCE_FIELDS,
  NEXT_ROUTES,
  CLAIM_ID_PATTERN,
  ReturnReferenceRefused,
  resolveReturnRef,
  attachReturnRefs,
  renderReturnRefFindings,
  assertForwardShapeUnchanged,
} = require("../../../.claude/scripts/tickets/lib/return-refs.js");

const KNOWN = {
  claims: new Set(["clm-lib-rs-invariant-42"]),
  residuals: new Set(["res-0007"]),
  scenarios: new Set(["H1-1"]),
};

const OMISSION = {
  type: "missing_implementation",
  contract_id: "C001",
  statement: "the guard is absent",
};
const RESIDUE = { candidate_id: "N0042", topic: "unexplained boundary" };

it("accepts a reverse omission carrying both references", () => {
  const result = attachReturnRefs(OMISSION, {
    kind: "omission",
    mode: "reverse",
    references: {
      affected_claim_ids: ["clm-lib-rs-invariant-42"],
      origin_residual_ids: ["res-0007"],
    },
    known: KNOWN,
  });
  assert.deepEqual(result.findings, []);
  assert.deepEqual(result.attached, ["affected_claim_ids", "origin_residual_ids"]);
});
```
- UT-2 [Normal] — C001 postcondition as an output assertion: affected_claim_ids and origin_residual_ids are added.
```js
it("adds affected_claim_ids and origin_residual_ids in reverse mode", () => {
  const { artefact } = attachReturnRefs(OMISSION, {
    kind: "omission",
    mode: "reverse",
    references: {
      affected_claim_ids: ["clm-lib-rs-invariant-42"],
      origin_residual_ids: ["res-0007"],
    },
    known: KNOWN,
  });
  assert.deepEqual(artefact.affected_claim_ids, ["clm-lib-rs-invariant-42"]);
  assert.deepEqual(artefact.origin_residual_ids, ["res-0007"]);
  assert.equal(artefact.contract_id, "C001");
});
```
- UT-3 [Invariant] — C001 invariant as an invariant predicate: in forward mode the artefact is returned by reference and its key set is untouched, so the forward omission output is not changed.
```js
it("returns the forward omission itself, with no key added", () => {
  const { artefact, findings, attached } = attachReturnRefs(OMISSION, {
    kind: "omission",
    mode: "forward",
  });
  assert.strictEqual(artefact, OMISSION);
  assert.deepEqual(Object.keys(artefact), ["type", "contract_id", "statement"]);
  assert.deepEqual(findings, []);
  assert.deepEqual(attached, []);
});

it("refuses a return reference sitting on a forward omission", () => {
  const contaminated = { ...OMISSION, affected_claim_ids: ["clm-lib-rs-invariant-42"] };
  assert.throws(
    () => attachReturnRefs(contaminated, { kind: "omission", mode: "forward" }),
    (error) => error instanceof ReturnReferenceRefused
      && /must not carry return reference/.test(error.message),
  );
});
```
- UT-4 [Normal] — C002 precondition, as an input-schema test: a RESIDUE is recorded with the scenario it came from and the route it should travel next.
```js
it("accepts a reverse RESIDUE carrying scenario_ref and next_route", () => {
  const result = attachReturnRefs(RESIDUE, {
    kind: "residue",
    mode: "reverse",
    references: { scenario_ref: "H1-1", next_route: "grill" },
    known: KNOWN,
  });
  assert.deepEqual(result.findings, []);
  assert.deepEqual(result.attached, ["scenario_ref", "next_route"]);
});
```
- UT-5 [Normal] — C002 postcondition as an output assertion: scenario_ref and next_route are added; UT-11 in the same assertion that no existing RESIDUE field is lost.
```js
it("adds scenario_ref and next_route without losing an existing field", () => {
  const { artefact } = attachReturnRefs(RESIDUE, {
    kind: "residue",
    mode: "reverse",
    references: { scenario_ref: "H1-1", next_route: "grill" },
    known: KNOWN,
  });
  assert.equal(artefact.scenario_ref, "H1-1");
  assert.equal(artefact.next_route, "grill");
  assert.equal(artefact.candidate_id, "N0042");
  assert.equal(artefact.topic, "unexplained boundary");
});
```
- UT-6 [Invariant] — C002 invariant as an invariant predicate: the forward RESIDUE output is not changed.
```js
it("returns the forward RESIDUE itself, with neither new field present", () => {
  const { artefact } = attachReturnRefs(RESIDUE, { kind: "residue", mode: "forward" });
  assert.strictEqual(artefact, RESIDUE);
  assert.equal(Object.hasOwn(artefact, "scenario_ref"), false);
  assert.equal(Object.hasOwn(artefact, "next_route"), false);
});
```
- UT-7 [Error] — UT-4 and UT-5: an unresolvable reference and a malformed claim identifier are both reported, each carrying the field it belongs to.
```js
it("reports a well-formed but unknown claim identifier rather than resolving it", () => {
  const { identifier, finding } = resolveReturnRef("clm-lib-rs-invariant-999", {
    kind: "claim",
    known: KNOWN.claims,
  });
  assert.equal(identifier, null);
  assert.equal(finding.reference, "affected_claim_ids");
  assert.match(finding.reason, /not found in the claim ledger/);
});

it("reports a malformed claim identifier", () => {
  const { identifier, finding } = resolveReturnRef("C001", { kind: "claim", known: KNOWN.claims });
  assert.equal(identifier, null);
  assert.match(finding.reason, /is not a claim identifier/);
  assert.ok(CLAIM_ID_PATTERN.test("clm-lib-rs-invariant-42"));
  assert.ok(!CLAIM_ID_PATTERN.test("clm-lib-rs-bogus-42"));
});

it("reports a route the design does not declare", () => {
  const { identifier, finding } = resolveReturnRef("R99", { kind: "route" });
  assert.equal(identifier, null);
  assert.equal(finding.reference, "next_route");
  assert.deepEqual(NEXT_ROUTES, ["R3.5", "R5", "R6", "grill"]);
});
```
- UT-8 [Error] — an unresolvable reference is reported rather than emitted as a dangling identifier, and the report names the omission it was attached to.
```js
it("never emits a dangling identifier and names the omission in the report", () => {
  const result = attachReturnRefs(OMISSION, {
    kind: "omission",
    mode: "reverse",
    references: {
      affected_claim_ids: ["clm-lib-rs-invariant-999"],
      origin_residual_ids: [],
    },
    known: KNOWN,
  });
  assert.equal(Object.hasOwn(result.artefact, "affected_claim_ids"), false);
  assert.equal(result.findings.length, 1);
  assert.equal(result.findings[0].artefact.contract_id, "C001");
  assert.match(renderReturnRefFindings(result.findings), /clm-lib-rs-invariant-999/);
});
```
- UT-9 [Boundary] — UT-6 and UT-8: an omission with no originating uncertainty carries no new fields and is still valid; a run with zero omissions states that there were none.
```js
it("adds nothing when no originating uncertainty is known", () => {
  const { artefact, attached, findings } = attachReturnRefs(OMISSION, {
    kind: "omission",
    mode: "reverse",
  });
  assert.strictEqual(artefact, OMISSION);
  assert.deepEqual(attached, []);
  assert.deepEqual(findings, []);
});

it("states that a run producing no omissions produced none", () => {
  assert.match(
    renderReturnRefFindings([], { subject: "omissions", considered: 0 }),
    /No omissions were detected/,
  );
});
```
- UT-10 [Invariant] — UT-9: the forward output of both commands is byte-identical to its pre-change form, asserted once and applied to both artefacts.
```js
it("proves the forward shape of the omission and of the RESIDUE unchanged", () => {
  const omissionAfter = attachReturnRefs(OMISSION, { kind: "omission", mode: "forward" }).artefact;
  const residueAfter = attachReturnRefs(RESIDUE, { kind: "residue", mode: "forward" }).artefact;

  assert.equal(
    assertForwardShapeUnchanged(OMISSION, omissionAfter, { label: ".claude/commands/find-omissions.md omission" }),
    true,
  );
  assert.equal(JSON.stringify(OMISSION), JSON.stringify(omissionAfter));
  assert.equal(
    assertForwardShapeUnchanged(RESIDUE, residueAfter, { label: ".claude/commands/crystalize-readme.md RESIDUE" }),
    true,
  );
  assert.equal(JSON.stringify(RESIDUE), JSON.stringify(residueAfter));
});

it("reports a forward shape that gained a key", () => {
  assert.throws(
    () => assertForwardShapeUnchanged(OMISSION, { ...OMISSION, scenario_ref: "H1-1" }),
    (error) => error instanceof ReturnReferenceRefused && /gained/.test(error.message),
  );
});
```
- UT-11 [Invariant] — UT-10: every added field is optional, so a forward consumer unaware of them still works; an omitted field is absent rather than null.
```js
it("leaves a field the caller did not supply absent, not null", () => {
  const { artefact } = attachReturnRefs(OMISSION, {
    kind: "omission",
    mode: "reverse",
    references: { affected_claim_ids: ["clm-lib-rs-invariant-42"] },
    known: KNOWN,
  });
  assert.deepEqual(artefact.affected_claim_ids, ["clm-lib-rs-invariant-42"]);
  assert.equal(Object.hasOwn(artefact, "origin_residual_ids"), false);
  assert.equal(artefact.origin_residual_ids, undefined);
});
```
- UT-12 [Invariant] — the return-reference vocabulary is a checked projection of the single declaration in forward-extensions.mjs, so a reference written here resolves there without a translation step.
```js
it("agrees with the reverse field names P22-10 declared", async () => {
  const { REVERSE_FIELD_NAMES, FORWARD_ARTIFACT_KINDS } = await import(
    "../../../.claude/scripts/workspacify-reverse/lib/forward-extensions.mjs"
  );
  const declaredOmission = REVERSE_FIELD_NAMES[FORWARD_ARTIFACT_KINDS.OMISSION];
  const declaredResidue = REVERSE_FIELD_NAMES[FORWARD_ARTIFACT_KINDS.RESIDUE];

  assert.deepEqual(
    [...RETURN_REFERENCE_FIELDS.omission].sort(),
    ["affected_claim_ids", "origin_residual_ids"],
  );
  for (const field of RETURN_REFERENCE_FIELDS.omission) {
    assert.ok(declaredOmission.includes(field), field + " is not declared for an omission");
  }
  for (const field of RETURN_REFERENCE_FIELDS.residue) {
    assert.ok(declaredResidue.includes(field), field + " is not declared for a RESIDUE");
  }
});
```

## Related Tickets

| Ticket KEY | Relation | Description |
|--------|----------|-------------|
| P22-10 | depends on | the reverse fields |
| P22-21 | provides to | the return chain it propagates staleness along |

### To show related tickets details

```
node .claude/scripts/tickets/show-ticket-context.js --ticket-key=<Ticket KEY to show (e.g. P0-1)> --for-spec --no-implementation-order
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| An omission is a count with no ancestry, so its cause cannot be followed. | An omission names the claims and residuals it descends from, and a RESIDUE names its scenario and next route. | Realise the layer B return path so omissions are actionable. |
| Two command files would each need their own change to carry the new references. | One resolution helper serves both, and the forward shape of each is asserted unchanged. | Keep one definition of the return reference across both commands. |
| An omission was a count with no ancestry, so its cause could not be followed. | An omission names the claims and residuals it descends from, and a RESIDUE names its scenario and next route, via return-refs.js. | Realised the layer B return path (ABOUT-REVERSE 6.12.3) for both commands. |
| Each command would have needed its own change to carry the new references. | One resolution helper serves both, and both command files hold the forward shape with the same words. | Kept one definition of the return reference across both commands (the two command file notes are appends only). |

## RFC Discrepancies found in Prior Implementation Rounds

- Ticket Notes name return-refs.mjs while Scope names return-refs.js. Evidence settles it for return-refs.js: .claude/package.json declares type commonjs, the siblings in .claude/scripts/tickets/lib/ use module.exports, and premise 11 forbids introducing an ESM module into a CommonJS tree.
- Ticket Notes give the verification command as node --test tests/tickets, which fails with MODULE_NOT_FOUND (measured). The working form, and the one the sibling reverse tests document, is node --test "tests/tickets/**/*.test.cjs".
- ABOUT-REVERSE 6.12.3 names RESIDUAL-REGISTRY.json, but no script in this repository produces it. Rather than guess a shape that would appear to resolve while resolving nothing, origin_residual_ids is resolved against a declared dot path (--residual-id-path, default residuals.residual_id), so an override is one argument.
- ABOUT-REVERSE 6.12.3 declares four layer B fields for an omission (affected_claim_ids, origin_residual_ids, scope_ref, oracle_gap_ref). P22-17 scope covers two of them, and no ticket among P22-18..P22-22 covers scope_ref or oracle_gap_ref. rfc-graph/contract-diff.js already emits both, unresolved. Recorded as a phase-level coverage observation, not a P22-17 obligation.
- .cjs is absent from SOURCE_EXTENSIONS in .claude/scripts/tickets/lib/scope-detection-constants.js, so reverse test files cannot receive a [::TICKET::] annotation and carry the // P22-NN @verifies header instead. Pre-existing and consistent with P22-16; not changed here.
- No Makefile target covers tests/tickets/, matching the ticket own note that the suite has no aggregate runner.

## Notes in Prior Implementation Rounds

【Position within P22】P22-17.
【Dependencies】P22-10 and P22-1.
【Provides】P22-21.
【Consumes — exact artefacts】P22-10's reverse fields; the existing omission and RESIDUE structures.
【Produces — exact artefacts】`affected_claim_ids` and `origin_residual_ids` on omissions, `scenario_ref` and `next_route` on RESIDUE, via `return-refs.mjs`. Consumed by P22-21. **Omission recall is measured against the oracle's 8 omission files.**
【Verification】`node --test tests/tickets` — **this suite has no aggregate runner**, so name the directory explicitly and confirm your new files are collected
【ABOUT-REVERSE.md references】ABOUT-REVERSE 6.12.3 layer B, 6.10.1 (the return edge), 1.3 (the three kinds of gap)
【Command-file edits】**Edit**: `.claude/commands/find-omissions.md` (21,407 B) and `.claude/commands/crystalize-readme.md` (17,408 B). Edit only; rewriting is strictly forbidden in both.
【Implementation steps】Red: return-reference tests. Green. Edit both command files by appending only. Run the forward regression for each. Confirm both command digests match.
【Risks】Changing the forward output breaks the forward rotation, and this ticket edits two command files rather than one, doubling the exposure. Pin the optionality of every added field by test.
【Done when】IT cases are green, the forward regression exits 0, and both command digests match.
Implementation summary for P22-17.

Changed files:
- .claude/scripts/tickets/lib/return-refs.js (new, CommonJS, also a CLI entry point)
- tests/tickets/reverse/return-refs.test.cjs (new; 33 tests, 7 suites)
- .claude/commands/find-omissions.md (append only: +2858 B, 370 -> 396 lines)
- .claude/commands/crystalize-readme.md (append only: +2802 B, 208 -> 233 lines)

Key changes:
- resolveReturnRef() resolves one reference against the registry that holds it, or returns a finding. A malformed claim identifier is distinguished from an unknown one because the two need different fixes.
- attachReturnRefs() attaches only the resolved references; an unresolvable one is reported, never emitted. In forward mode the artefact is returned by reference, and any return reference on it (or offered as an argument) is refused rather than dropped.
- withoutReturnRefs() strips the references an artefact proposes before resolution, so a reference the ledger did not confirm cannot survive into the output by being left where it was found. This defect was found by the CLI integration test and fixed.
- assertForwardShapeUnchanged() states the forward obligation once; both command files are held to it.
- RETURN_REFERENCE_FIELDS is a projection of P22-10s REVERSE_FIELD_NAMES in workspacify-reverse/lib/forward-extensions.mjs. It is a projection rather than an import because .claude/scripts/tickets/ is the forward tree and must not load the reverse tree (6.12.2 reads the dependency the other way). UT-12 proves the two agree by dynamic import.
- The CLI takes the artefact on stdin with its proposed references and prints the resolved artefact plus a plain-English report, exiting 0 either way: the machine states facts, the human judges.

Evidence:
- node --test "tests/tickets/**/*.test.cjs": 69 tests, 15 suites, 69 pass, 0 fail.
- make test-crystalize-readme: 223 pass, 0 fail. make test-rfc-graph: 915 pass, 0 fail.
- P22-1 forward regression gate (node .claude/scripts/workspacify-reverse/run.mjs regression check): proved - 25 fixtures compared, 0 disagreed, 0 command-file loss.
- Falsification: three independent mutations of return-refs.js each turned the suite red (forward copy instead of identity: 2 failures; dangling identifier emitted: 2; vocabulary drifted from P22-10: 5), then restored to green. The suite is not green by accident.
- Both documented command-file invocations were run against the real sidecars: tests/workspacify-reverse/analysis/CLAIM-LEDGER.json (4460 claims) and siprs-with-4layers/CRYSTALIZE-Status.json (18 sections). A real claim id resolved; a fabricated one was withheld and reported.
- Quality checks: 0 issues. scan-crimes.sh: 0. find-all-stubs.js: 0.

Notes and handover:
- The ticket Notes name return-refs.mjs while the Scope names return-refs.js. return-refs.js is correct: .claude/package.json declares type commonjs and the siblings in tickets/lib/ are CommonJS .js. The .mjs spelling would have introduced an ESM module into a CommonJS tree (premise 11).
- The ticket Notes give the verification command as node --test tests/tickets, which fails with MODULE_NOT_FOUND (measured). The working form, and the one the sibling tests document, is node --test "tests/tickets/**/*.test.cjs".
- RESIDUAL-REGISTRY.json is named by 6.12.3 but no script in this repository writes it yet, so origin_residual_ids resolves against a declared dot path (--residual-id-path, default residuals.residual_id) rather than a guessed fixed shape. An override is one argument.
- No Makefile target covers tests/tickets/, matching the tickets own note that the suite has no aggregate runner.
- Boy Scout: left out of scope deliberately - rfc-graph/contract-diff.js identifierList() is P22-14s settled artefact and is under this tickets verification gate.
Review report for P22-17.

Verdict: the implementation satisfies the design in full. No blocker, no major.

Design coverage verified against Step 1:
- Acceptance Criteria: happy path (omission names claims and residuals; RESIDUE names scenario and next route), error case (an unresolvable reference is reported, not emitted as a dangling identifier), and edge case (an omission with no originating uncertainty carries no new fields and remains valid) are each covered by named tests.
- Invariants: all four (normal establishment, on error, internal state, boundary) are covered.
- Unit Tests UT-1..UT-11 and Integration Tests IT-1..IT-4 from the Test Plan are all implemented, each labelled with its plan identifier in the test names.
- The declared Exception is honoured exactly: ancestry correctness is not asserted, and the three named alternatives (every emitted reference resolves; unresolvable references are reported rather than emitted; the forward output is byte-identical) are all tested.
- Contracts C001 and C002 are fully covered (Gate P ok, verify-final-contracts coverage 100).

Material evidence:
- Node ids: P22-17 carries no nodeIds and no referenceSection, so the query.js exploration of Step 2 is not applicable to this ticket; the design evidence is the ticket spec plus ABOUT-REVERSE 6.12.3, 6.10.1 and 1.3, read directly.
- Consumers: P22-21 (staleness into drill) consumes the return references as fields, not as a function signature, so the layer B field names are the whole interface and it is satisfied.
- The two documented command-file invocations were executed against the real sidecars (CLAIM-LEDGER.json with 4460 claims; siprs-with-4layers/CRYSTALIZE-Status.json with 18 sections). A real identifier resolved; a fabricated one was withheld from the output and named in the plain-English report; exit 0 either way.
- Protected sections of both edited command files are byte-identical before and after (verified against HEAD and against the frozen P22-1 baseline: find-omissions had Language Protocol and First-Class Rule absent at freeze time, crystalize-readme had Language Protocol present and its digest still matches).

Issues found in review and fixed:
1. Minor, real: a reference supplied in the wrong shape was emitted silently. A list given to a singular field (scenario_ref, next_route) would be written as a list, and a single identifier given to a plural field (affected_claim_ids, origin_residual_ids) would be written as a scalar - undeclared shapes, in a module whose purpose is to prevent exactly that drift. Fixed by declaring each field arity and reporting a mismatch rather than emitting it. Two tests added; falsifying the guard turns 2 tests red.
2. Minor: the route row of the identifier table declared a noun and a registry that were never read, duplicating the user-facing sentence in two places. The message now reads from the table, so the table is load-bearing and the sentence cannot rot.

Boy Scout: the review-time fixes are inside the file this ticket authored. Out-of-scope items were deliberately left: rfc-graph/contract-diff.js identifierList() (P22-14 settled artefact, under this ticket verification gate) and scope-detection-constants.js SOURCE_EXTENSIONS (shared by the reverse test convention, recorded as a discrepancy instead).

## P22-17 — implemented at 27 locations

### .claude/scripts/tickets/lib/return-refs.js

- Line 140
```javascript
class ReturnReferenceRefused extends Error {
```

- Line 142
```javascript
  constructor(message) {
```

- Line 150
```javascript
function isUsableName(value) {
```

- Line 156
```javascript
function display(value) {
```

- Line 162
```javascript
function knownFor(kind, known) {
```

- Line 180
```javascript
function declaredFields(kind) {
```

- Line 193
```javascript
function assertFieldsAreDeclared(references, fields, kind) {
```

- Line 216
```javascript
function resolveReturnRef(value, { kind, known = null, reference = null } = {}) {
```

- Line 270
```javascript
function suppliedValues(supplied) {
```

- Line 282
```javascript
function describeArity(isList) {
```

- Line 288
```javascript
function arityMatches(declared, isList) {
```

- Line 311
```javascript
function attachReturnRefs(artefact, { kind, mode, references = {}, known = null } = {}) {
```

- Line 398
```javascript
function withoutReturnRefs(artefact, kind) {
```

- Line 416
```javascript
function renderReturnRefFindings(findings, { subject = 'omissions', considered = null } = {}) {
```

- Line 445
```javascript
function describeArtefact(artefact) {
```

- Line 470
```javascript
function assertForwardShapeUnchanged(before, after, { label = 'forward artefact' } = {}) {
```

- Line 528
```javascript
function readJsonFile(filePath) {
```

- Line 538
```javascript
function readClaimIds(filePath) {
```

- Line 551
```javascript
function readScenarioIds(filePath) {
```

- Line 566
```javascript
function readIdsAtPath(document, dotPath) {
```

- Line 581
```javascript
function optionValue(argument, name) {
```

- Line 588
```javascript
function readArtefactFromStdin() {
```

- Line 604
```javascript
function parseCliOptions(argv) {
```

- Line 644
```javascript
function buildKnown(options) {
```

- Line 660
```javascript
function referencesOf(artefact, kind) {
```

- Line 667
```javascript
function subjectFor(kind) {
```

- Line 673
```javascript
function runCli(argv) {
```
