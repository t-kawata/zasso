# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P23-1: Rewrite /workspacify-reverse as a procedure, and hold its eight structural assertions

**Ticket Key**: P23-1 · **Phase**: 23

---

## Background

### Goal
`.claude/commands/workspacify-reverse.md` becomes a **procedure an operator follows** from an arbitrary project root to the origin spec and the hand-over: Step 0 identifies the input pattern, Steps 1-2 fix what is already there and the boundary, Step 3 reaches the exit, Step 4 reads the material in the order it is needed, Step 5 takes the one load-bearing decision, Step 6 records the seam, Step 7 hands over, Step 8 reports. The four principles of `ABOUT-REVERSE` §4.6 are named; the AI judgement surface is closed to exactly six items; the terminal state is stated; every formulation §5.7 forbids is absent.

### Purpose
The design's §5.2 records five defects in the current 159-line file. It is a *description of the entrance*, not a *procedure for the work*: D1 it describes rather than instructs and never says which instrument to run when; D2 it states no preconditions and no gates between the seven subcommands; D3 `--through` appears as a flag definition and never as what it is — the only instrument for seeing a prefix, because publishing is atomic; D4 the four principles, which are the spine of the whole design, do not appear; D5 "loop rounds" is mentioned once and undefined, and neither the round nor the terminal state is stated.

### Motivation
§7.1 measures that the instrument is not broken. `node .claude/scripts/workspacify-reverse/run.mjs analyze siprs-for-reverse --out=/tmp/wsp-r8-probe --through=r8` exits 0 in about three minutes and publishes 21 documents; `git status` is clean afterwards. What was missing was a procedure that told anyone to run it to the exit and what to do with what comes out. §5.1 measures the permission: the file is not in `COMMAND_FILE_NAMES` (`.claude/scripts/workspacify-reverse/lib/command-file-digest.mjs`), and `tests/workspacify-reverse/integration/command.test.mjs:159` asserts "the tenth file is deliberately outside the frozen digest: it is a creation, not an edit". P22-9 created it, so the append-only discipline that binds the nine does not bind it, and a rewrite is permitted. Simultaneously eight structural assertions at `command.test.mjs:162-183` do bind it. A rewrite is therefore permitted **and bounded**, and today the bound covers only those eight: nothing asserts that the new sections exist, that the six judgement items are enumerated, or that the forbidden formulations are absent. A rewrite without that guard would trade a weak document for an unguarded one.

### Constraints
- The frontmatter is kept **byte-identical**: `description`, `argument-hint: <path-to-the-project-root>`, `disable-model-invocation: true`. `/workspacify-reverse <root>` is P22-9's API contract.
- The Language Protocol table stays byte-identical to the nine's, asserted through `sharedLanguageProtocolDigest()` in `command.test.mjs`.
- The First-Class Rule line keeps matching `/First-Class Rule\s*—\s*\[::STUB::\]/`.
- At least one heading matches `/Step \d/` — satisfied by `## Step 0` … `## Step 8`.
- The file keeps `## Scripts used`, `## Arguments`, and the text `run.mjs analyze`.
- **No gate may stop the run because the subject is not a conver project** (design §1.2). Incompleteness is the input.
- `holdout isolation`, `scrub` / `detect` / `verify`, `oracle compare` and `run.mjs regression check` may appear **only** inside the experiment-mode section, never as a workflow step or a precondition (§5.7, §5.8).
- The nine protected command files are not touched; the PX-210 baseline digest over them must be reproduced.

## Scope

- **Scope of changes (describe each change comprehensively):**
  - [File/module path] `.claude/commands/workspacify-reverse.md` — the tenth command file, outside the frozen digest of nine
  - [Action] rewrite (permitted: §5.1 measures that the file is a creation, not an edit; the append-only discipline that binds the nine does not bind it)
  - [What specifically changes] Frontmatter, Role, Language Protocol table and First-Class Rule line are kept byte-for-byte. The body is replaced with the structure §5.3 declares: `## Arguments` (kept) → `## The four principles` (new) → `## What the machine decides, and what you decide` (rewritten, six items) → `## The terminal state this command serves` (new, design §2) → `## The canonical output and its constraints` (kept) → `## Scripts used` (kept, assertion 8) → `## Statuses and gates` (kept, exit codes made explicit) → `## Step 0: identify the input` … `## Step 8: report` → `## What this command cannot yet reach` (new, the six named absences) → `## A round, and what success is` (new, L0-L3 and RESIDUE 0) → `## Error recovery` (rewritten) → `## Prohibitions` (rewritten) → `## Definition of success` (rewritten)
  - [Before → After] A 159-line description whose five steps are an invocation sequence (probe zg, run analyze, read the report, serve candidates, report) becomes a nine-step procedure (Step 0 through Step 8) that names which instrument to run when, states the terminal state, and closes the judgement surface to six items. The defect list D1-D5 in design §5.2 is closed item by item.
  - [API contract change] None. `/workspacify-reverse <path-to-the-project-root> [--out=<dir>] [--through=<stage>] [--query=<text>]` is unchanged, and the frontmatter is byte-identical.
  - [Data schema change] None. The documented canonical artefacts are unchanged: `ORIGIN-LONG-SPEC.json` and `ORIGIN-LONG-SPEC.md`, published outside the target.
  - [Config/env change] None. No environment variable, hook or external fetch is introduced or removed.
  - [Dependency added/removed] None. The command file is prose; no script is added or removed by this ticket.
  - [Test added] `tests/workspacify-reverse/integration/command-procedure.test.mjs` — a new guard that asserts the sections exist, the six items are exactly six, and the six forbidden formulations are absent, with each assertion exercised against a throwaway fixture so that it is seen to fail rather than only seen to pass.
- **Out of scope (items intentionally excluded, with justification):**
  - [Excluded item] The mechanics the new procedure names as unreachable (the six absences N1-N6) — why excluded: they are tickets 3 through 8 of this same phase. This ticket writes the procedure that *names* them; it does not build them. Naming an absent entrance is the same discipline the instruments apply to unobserved regions (F12), and instructing an operator to run an entrance that does not exist is worse than silence.
  - [Excluded item] Any change to the nine protected command files — why excluded: they are frozen against `tests/workspacify-tree/baselines/manifest-hashes.json`, and `rotation-gate.test.mjs` C004 fails on any lost heading, changed Language Protocol table or changed First-Class Rule line.
  - [Excluded item] The `regression capture` that would freeze the rotation-gate sentence as a fourth digest key — why excluded: `command-files.test.mjs` records that capture re-derives the five forward surfaces, and a key the baseline carries no value for is a stored-but-uncompared field, the shape this repository has repaired twice.
- **Affected areas (components/systems impacted, even without direct modification):**
  - [Affected component] `tests/workspacify-reverse/integration/command.test.mjs` — the eight assertions at lines 162-183 run over the rewritten file on every test run
  - [Nature of impact] Test surface: the eight assertions must stay green, and `COMMAND_FILE_NAMES.includes('workspacify-reverse')` must stay false
  - [Corresponding change needed] N — no edit to that file. The rewrite is designed to satisfy it unchanged; if it does not, the rewrite is wrong, not the test.
  - [Affected component] `tests/workspacify-reverse/regression/rotation-gate.test.mjs` C004 — the PX-210 baseline digest over the nine
  - [Nature of impact] Regression gate: the baseline freezes exactly the nine, and this ticket touches none of them
  - [Corresponding change needed] N — the assertion `Object.keys(baseline.commandFileDigests).sort()` equals the nine sorted must still hold
  - [Affected component] Any operator reading the file as the entrance to the reverse rotation
  - [Nature of impact] Behaviour of the reverse rotation's first command: today it describes, after this ticket it instructs
  - [Corresponding change needed] Y — this is the change

## Investigation

Evidence gathered by reading the file, its tests and the design's measurements on 2026-09-11, in `/Users/kawata/shyme/zasso/tools/conver`.

- **The file as it stands**: `.claude/commands/workspacify-reverse.md`, 159 lines. Sections in order: frontmatter, `# /workspacify-reverse`, Role, `## Language Protocol`, the First-Class Rule paragraph, `## Arguments`, `## The canonical output and its constraints`, `## Scripts used`, `## Statuses and gates`, `## The boundary between design judgement and mechanisation`, `## Step 1: probe the environment (zg)`, `## Step 2: run R0 through R8`, `## Step 3: read the report`, `## Step 4: serve the candidates (zg)`, `## Step 5: report`, `## Error recovery`, `## Prohibitions`, `## Definition of success`. It already carries correct material — the atomic-publishing sentence, the `proved` / `not proved` vocabulary, the evidence-mode rule — but as description.
- **The permission, measured**: `.claude/scripts/workspacify-reverse/lib/command-file-digest.mjs` exports `COMMAND_FILE_NAMES` = `['workspacify-tree','workspacify-allocate','grill-me-for-rfc','graphify-rfc','boundify-graph','split-to-tickets','find-omissions','crystalize-readme','drill-rfc-down']` — nine names, and `workspacify-reverse` is not among them. `.claude/commands/` holds 21 files.
- **The eight assertions, verbatim from `tests/workspacify-reverse/integration/command.test.mjs`**: `assert.match(text, /^---\n/)`; `/^description: /m`; `/^argument-hint: /m`; `/^disable-model-invocation: true$/m`; `sha256(sections.languageProtocolTable) === sharedLanguageProtocolDigest()`; `assert.match(sections.firstClassRuleLine, /First-Class Rule\s*—\s*\[::STUB::\]/)`; `sections.headings.some((h) => /Step \d/.test(h))`; `/^## Scripts used$/m` and `/run\.mjs analyze/` and `/^## Arguments$/m`.
- **The protected nine are separately frozen**: `tests/workspacify-tree/baselines/manifest-hashes.json` carries `commandFileDigests`, and `tests/workspacify-reverse/regression/rotation-gate.test.mjs` C004 asserts `compareDigests(BASELINE.commandFileDigests, digestCommandFiles(PROJECT_ROOT))` is empty and that each file's `languageProtocolDigest` and `firstClassRuleDigest` are unchanged.
- **The material the new sections must carry**: the four principles are `docs/ABOUT-REVERSE.md` §4.6 — 決定論的解析の極大化 / AI の判断面積の極小化 / 残った判断面積への給仕の極大化 / 既存ツリーの絶対不可侵. The six judgement items are §6.2: the package boundary, owner assignment, layer estimation, what a contract means, the over-splitting decision, and the `observed` / `inferred` / `normative` / `unresolved` classification.
- **The atomic-publishing measurement**: `lib/scope.mjs` calls `publishDocuments(out, documents)` exactly once, after every stage in the prefix ran and the target was re-digested; the digest comparison that throws is at `scope.mjs:1128`. `ANALYSIS_EVALUATION_ORDER` at `scope.mjs:437` is `['r0','r0.5','r2.5','r1','r2','r3','r3.5','r4','r5','r5.5','r6','r6.5','r7','r8']` — R2.5 before R1 and R2.
- **The forbidden formulations and why**, each measured: `holdout isolation siprs-with-4layers` exits 1 naming 9 ground-truth artefacts, and on a pattern-2 project those artefacts are the project's legitimate prior work; `scrub` removes forward traces, which on a pattern-2 project are its 143 `Initial Design Artifact` headers and its `@verifies` annotations; `oracle compare` needs an answer key, which no real project has; `run.mjs regression check` takes no root — `run.mjs:81` declares `ROOT_TAKING_SUBCOMMANDS = ['detect','scrub','verify','analyze']`.

## Acceptance Criteria

- **[Happy path] — Describe the scenario that confirms this ticket is complete:**
  - The rewritten `.claude/commands/workspacify-reverse.md` satisfies all eight assertions of `command.test.mjs:162-183` and every guard of the new `command-procedure.test.mjs`: nine Step headings, the four principles named, the six judgement items counted at six, the terminal state stated, the two modes named, the six absences listed, and zero forbidden formulations. `compareDigests` over the PX-210 baseline returns `[]`.
- **[Error case] — Describe an error scenario the feature must handle:**
  - A fixture carrying any one of the six forbidden formulations — an incompleteness gate, `holdout isolation` as a precondition, `scrub`/`detect`/`verify` as steps, `oracle compare` as a step, `run.mjs regression check` as a precondition, or a statement that the project must already be complete — is reported by name and line. The run is not silently accepted because the document is otherwise well-formed.
- **[Edge case] — Describe any boundary or exception to verify:**
  - The judgement-item list carries seven items: the length assertion fails, so widening the AI's judgement surface beyond the six §5.2 licenses is a test failure. The complementary edge — five items — fails too, so narrowing it is caught as well.

## Invariants

- [Normal condition] The rewritten file satisfies all eight structural assertions of `command.test.mjs:162-183` unchanged: frontmatter present, `description`, `argument-hint`, `disable-model-invocation: true`, the shared Language Protocol digest, the First-Class Rule line matching `/First-Class Rule\s*—\s*\[::STUB::\]/`, at least one heading matching `/Step \d/`, and `## Scripts used` + `## Arguments` + the text `run.mjs analyze`.
- [Error invariant] No gate, precondition or early exit in the file can stop the run because the subject is not a complete conver project. This is design §1.2, the load-bearing sentence of the whole design, and it must survive every later edit — so the test asserts the *absence* of the six forbidden formulations rather than only the presence of the new sections.
- [Internal state invariant] The frontmatter is byte-identical to the P22-9 creation. The command API `/workspacify-reverse <path-to-the-project-root>` does not move, and neither does the set of four arguments (positional root, `--out`, `--through`, `--query`).
- [Boundary invariant] The judgement surface holds exactly six items. Six is asserted as a count, not as prose: a later edit that adds a seventh — widening the AI's surface beyond what the design licenses — fails the test rather than passing silently.

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — workspacify-reverse.md → tests/workspacify-reverse/integration/command.test.mjs:162-183 (the eight structural assertions)

- **Precondition**: The rewritten file exists at .claude/commands/workspacify-reverse.md and opens with the unchanged frontmatter block (description, argument-hint, disable-model-invocation: true).
- **Postcondition**: extractCommandFileSections(text) yields a languageProtocolTable whose sha256 equals sharedLanguageProtocolDigest(), a firstClassRuleLine matching /First-Class Rule\s*—\s*\[::STUB::\]/, and at least one heading matching /Step \d/; and the text matches /^## Scripts used$/m, /^## Arguments$/m and /run\.mjs analyze/.
- **Invariant**: The frontmatter is byte-identical to the P22-9 creation: the three keys and their values are unchanged, so the command API /workspacify-reverse <root> does not move.

### C002 — docs/WORKSPACIFY-4-PATTERNS-COMPLETE-DESIGN.md §5.7 (the forbidden formulations table)

- **Precondition**: The rewritten file has been split into its lines, and each line has been classified as belonging to a step section, a precondition, or prose.
- **Postcondition**: None of the six forbidden formulations appears as a workflow step or as a precondition: no gate stops the run for incompleteness; holdout isolation is not a precondition; scrub, detect and verify are not steps; oracle compare is not a step; run.mjs regression check is not a precondition; and no sentence states that the project must already be a complete conver project.
- **Invariant**: No sentence anywhere in the file makes the subject project completeness a condition of running: the string-level audit finds zero occurrences outside the experiment-mode section, which is explicitly labelled as not gating an operational run.

### C003 — docs/ABOUT-REVERSE.md §6.2 (the AI judgement surface) and §4.6 (the four principles)

- **Precondition**: The rewritten file carries the sections named in design §5.3, including "## The four principles" and "## What the machine decides, and what you decide".
- **Postcondition**: All four principles are named in one place, all six judgement items are enumerated in one place, and the file states explicitly that the machine verdicts on T1-T6, A1-A6, G4-G5, GF1-GF2, B1-B3, S1-S6 and §6.14.7 are accepted rather than re-opened.
- **Invariant**: The judgement surface holds exactly six items and never a seventh: the enumerated list is asserted to have length six by the test, so a later edit that widens the surface fails rather than passes quietly.

## Boy Scout Rule

- **Translatability of the file itself.** The rewritten command file is read top-to-bottom by an operator and by an AI, so it is held to the same standard the rules set for source: `## Step 0` … `## Step 8` are verb phrases in order (identify, record, fix, reach, read, decide, record, hand over, report), and the sequence reads as a procedure rather than as a list of headings. The one existing block that violates this — the current `## Step 1: probe the environment (zg)` / `## Step 2: run R0 through R8` pair, whose titles name an *activity* while their bodies describe a *sequence* — is rewritten so the title and the body say the same thing.
- **Boy Scout improvements outside this ticket's scope, in code this ticket's tests read.** `tests/workspacify-reverse/integration/command.test.mjs` carries the eight structural assertions inline in a single test body. This ticket extracts them into one named predicate function (`assertCommandFileStructure(text)`) used by both that test and the new guard, so the eight assertions are defined once. This is a test-only refactor inside a file the ticket must touch to avoid duplicating the assertions; it changes no assertion's meaning and is verified by the existing test staying green.
- **Hardcoded values.** The expected heading count (9), the judgement-item count (6) and the forbidden-formulation count (0) are extracted into named constants in the new test file (`EXPECTED_STEP_HEADINGS`, `JUDGEMENT_SURFACE_SIZE`, `FORBIDDEN_FORMULATION_COUNT`) so the number in the assertion and the number in its explanation cannot drift.
- **No error swallowing.** A file that cannot be read is reported by path, never skipped: the new guard follows `lintRotationPlacement`'s pattern of returning a finding of kind `unreadable-file` rather than throwing or passing.

## Test Plan

### Unit Tests

- **UT: [Normal] — Describe each normal-case test scenario:**
  - UT: [Normal] The rewritten file is parsed by `extractCommandFileSections` and yields a non-empty heading set containing `## Step 0` through `## Step 8`, so assertion 7 is satisfied by nine headings rather than by one.
  - UT: [Normal] `sha256(sections.languageProtocolTable)` equals `sharedLanguageProtocolDigest()` — the same value the nine carrying command files share — proving the rewritten table was copied rather than retyped.
  - UT: [Normal] `sections.firstClassRuleLine` matches `/First-Class Rule\s*—\s*\[::STUB::\]/`, and the matched text is identical to the text the nine carry.
  - UT: [Normal] The text matches `/^## Scripts used$/m`, `/^## Arguments$/m` and `/run\.mjs analyze/`, so the script list still names the entrance (assertion 8).
  - UT: [Normal] The file carries `## The four principles` and names all four: maximising the deterministic analysis, minimising the area left to AI judgement, maximising the serving to that area, and the absolute inviolability of the existing tree.
  - UT: [Normal] The file carries the six judgement items and the test counts them: the package boundary, owner assignment, layer estimation, what a contract means, the over-splitting decision, and the observed/inferred/normative/unresolved classification. The count is asserted to equal six.
  - UT: [Normal] The file states the terminal state of design §2: every package holds its complete four-layer set, the partition is explicit, and every inconsistency is recorded.
  - UT: [Normal] The file states the machine's vocabulary as `proved` / `not proved` and states that "succeeded" and "failed" are a human's, and that `omission 0` is not the success condition.
  - UT: [Normal] The file names all four patterns (independent, already four-layer, partially conver, empty-plus-spec) and states that patterns 1, 2 and 3 enter through the reverse rotation while pattern 4 does not.
  - UT: [Normal] The file names the two modes of §5.8 and says which one an operational run is in, and states that `holdout isolation`, `scrub` / `detect` / `verify` and `oracle compare` are never used in the operational mode.
  - UT: [Normal] The file names the six absences of §6 (N1-N6) as things this command cannot yet reach, rather than omitting them.
  - UT: [Normal] The file states `--through` as the only prefix instrument, and states the evaluation order `r0, r0.5, r2.5, r1, r2, r3, r3.5, r4, r5, r5.5, r6, r6.5, r7, r8` with the reason R2.5 runs before R1 and R2.
  - UT: [Normal] The file states that publishing is atomic: a run that stops publishes nothing, so there is no partial-document state to clean up.
  - UT: [Normal] The file states that the target is digested before and after and that a single byte moved refuses publication.
  - UT: [Normal] Contract C001: the frontmatter is read back and the three keys — `description`, `argument-hint`, `disable-model-invocation` — carry exactly the values the P22-9 creation gave them, proving the postcondition that the command API did not move.
  - UT: [Normal] Contract C003: the six items are extracted from the file and the extracted list has length six and contains each expected item, proving the postcondition that the surface is enumerated in one place and the invariant that it is never seven.
- **UT: [Error] — Describe each error-handling test scenario:**
  - UT: [Error] Contract C002: a throwaway fixture carrying "Step 3 is skipped when the project is not a conver project" is reported by the audit that scans for step-removing sentences, naming the line. The rule is exercised against a file that violates it, so the check is seen to fail rather than only seen to pass.
  - UT: [Error] Contract C002: a throwaway fixture containing "run.mjs regression check" as a step before the forward procedure ends is reported, because that subcommand takes no root and measures the conver repository rather than the subject.
  - UT: [Error] Contract C002: a throwaway fixture containing `holdout isolation` as a precondition is reported by name, and the engine says why — on a pattern-2 project the artefacts it calls contamination are the project's legitimate prior work, and the check exists to manufacture the experiment input, not to qualify a real project.
  - UT: [Error] Contract C002: a throwaway fixture containing "the project must already be a complete conver project" is reported, because that sentence is design §1.2's exact prohibition.
  - UT: [Error] Contract C002: a throwaway fixture containing `scrub`, `detect` or `verify` as a workflow step is reported, because those subcommands remove or re-detect forward traces, and on a pattern-2 project the traces are what must be carried forward.
  - UT: [Error] Contract C002: a throwaway fixture containing `oracle compare` as a workflow step is reported, because an answer key exists only in the paired-tree experiment.
  - UT: [Error] A fixture whose Language Protocol table differs from the nine's by a single space is reported by `compareDigests` with kind `changed-language-protocol` and the file path, rather than passing as "close enough".
  - UT: [Error] A fixture whose First-Class Rule sentence has been deleted is reported with kind `changed-first-class-rule`, named by file.
- **UT: [Boundary] — Describe each boundary-value test scenario:**
  - UT: [Boundary] A fixture with no heading matching `/Step \d/` fails assertion 7 and is reported, rather than passing because there was nothing to violate.
  - UT: [Boundary] A fixture whose six-item list carries seven items fails the length assertion, so widening the AI judgement surface is a test failure rather than a silent change.
  - UT: [Boundary] A fixture whose six-item list carries five items fails the length assertion, so narrowing the surface is caught too — a surface that shrank would leave judgement the design assigns to the AI unmade.
  - UT: [Boundary] A file that cannot be read is reported by path rather than throwing, so a missing file never reads like a file that passed.
  - UT: [Boundary] The gate sentence is on the final line of a fixture and is still found, so a section that ends the file is not treated as absent.
  - UT: [Boundary] The rewritten file has at least one heading matching `/Step \d/` and the match is asserted at the first occurrence and the last, so a single Step heading is not mistaken for the nine the procedure declares.
  - UT: [Boundary] Contract C001: the frontmatter check reads the first three lines only, so a `---` appearing later in the document as a horizontal rule is not mistaken for frontmatter.
- **UT: [Invariant] — Describe each invariant test scenario:**
  - UT: [Invariant] Contract C001 invariant: `compareDigests(BASELINE.commandFileDigests, digestCommandFiles(PROJECT_ROOT))` returns `[]` — none of the nine lost a heading, changed its Language Protocol table or changed its First-Class Rule line while the tenth was rewritten.
  - UT: [Invariant] Contract C001 invariant: `COMMAND_FILE_NAMES.includes('workspacify-reverse')` is `false` after the rewrite, so the tenth file did not quietly join the frozen digest.
  - UT: [Invariant] Contract C002 invariant: over the whole rewritten file, the count of lines matching any of the six forbidden formulations is zero, and the audit is run over the real file rather than over a fixture.
  - UT: [Invariant] Contract C003 invariant: the extracted judgement-item list has length exactly six on the real file and on a fixture with a seventh item added; the first passes and the second fails, so the assertion is shown to discriminate.
  - UT: [Invariant] Property: over every prefix of the rewritten file's lines, the file never states that the subject must be complete; the property is checked by scanning each line and is expressed as a predicate over the line set rather than as a spot check.

### Integration Tests

- **IT: [Integration point] — Specify each interface between modules:**
  - IT: `command-file-digest.mjs` `extractCommandFileSections` + `digestCommandFiles` + `compareDigests` reading `.claude/commands/workspacify-reverse.md` and the nine.
  - IT: `command.test.mjs`'s eight assertions and the new `command-procedure.test.mjs`'s guards over the same file, so the file is held by both the P22-9 contract and this ticket's structure.
  - IT: the design document as the source of the four principles, the six judgement items, the terminal state, the L0-L3 ladder and the forbidden formulations — the new test asserts the file against the document's own words rather than against a second copy of them.
- **IT: [Verification] — Describe what each integration test verifies:**
  - IT: Verifies that the eighth assertion group and the new structural guards agree: a file that satisfies one and not the other is a failure, so the two guards cannot drift into covering different documents.
  - IT: Verifies that the nine protected files are byte-stable across the rewrite, by reproducing the PX-210 baseline digest and asserting `[]` from `compareDigests`.
  - IT: Verifies that the file's declared workflow and the actual instrument agree: the step that runs the analysis names `run.mjs analyze` with the same four arguments `run.mjs` parses (`parseArguments`), so the procedure cannot instruct an invocation the entrance does not accept.
  - IT: Verifies that the evaluation order stated in the file equals `ANALYSIS_EVALUATION_ORDER` exported by `lib/scope.mjs`, so the document and the code name the same order.
- **IT: [Prerequisites] — State each prerequisite for integration tests:**
  - IT: `Tickets.json` present at the project root and readable.
  - IT: `tests/workspacify-tree/baselines/manifest-hashes.json` present with `commandFileDigests` carrying exactly the nine keys.
  - IT: `.claude/commands/` carrying the ten files (nine frozen plus the rewritten tenth).
  - IT: no network, no daemon and no build step: every input is a file in the working tree.
- **IT: [Related tickets] — List each related ticket:**
  - IT: P22-1 — froze the command-file digest and the baseline this ticket must reproduce.
  - IT: P22-9 — created the tenth command file; this ticket rewrites it inside the eight assertions P22-9's test established.
  - IT: PX-210 — added the rotation-gate lints; this ticket adds no key to that digest, for the reason `command-files.test.mjs` records.
  - IT: P23-3 through P23-8 — the absences this ticket names in `## What this command cannot yet reach`; each of those tickets removes one entry from that section.

### Exceptions

- **Exception entry (copy this block for each non-testable item):**
  - [Item] Whether the rewritten procedure actually helps an operator reach the terminal state.
  - [Reason] It is impossible to test this: it is a claim about whether a human reader, following the file, arrives at a correct partition and a published origin spec. There is no machine-checkable predicate over helpfulness, and the design says so — §7.3 records that "the four patterns genuinely converge to an identical terminal structure" is a design claim nobody has observed. This is not a design defect and not an architectural defect: the file's *structure* is fully testable and is tested by C001-C003, and what remains is a judgement the design deliberately assigns to a human (ABOUT-REVERSE §4.6).
  - [Alternative verification] The mechanical half is verified: the eight structural assertions, the six-item count, the four principles present, the terminal state stated, the two modes named, and the six forbidden formulations absent are all asserted. The human half is verified by one operational run of the procedure over `siprs-for-reverse` with the observed step list recorded, which P23-12's audit consumes.

## Notes in Prior Implementation Rounds

- [Implementation steps] 1) Write the failing guard first (`command-procedure.test.mjs`): assert the nine Step headings, the four principles, the six-item count, the terminal-state section, the two modes, the six absences, and the absence of the six forbidden formulations — each against the current 159-line file, so every assertion fails red. 2) Rewrite the command file section by section in the order §5.3 gives, keeping the frontmatter, Role, Language Protocol table, First-Class Rule line, `## Arguments`, `## Scripts used` and `## Statuses and gates` byte-identical where the design says "kept". 3) Re-run the new guard and `command.test.mjs` together until both are green. 4) Re-run `rotation-gate.test.mjs` to confirm the nine are untouched.
- [Risks] The highest risk is a rewrite that satisfies the new guards by writing the section headings and not their content — a document that lists "the four principles" without naming them, or that names them in a form a reader cannot act on. The guard against that is that the test asserts the *content* of each: the four principles by their four distinct claims, the six items by their six distinct names, the absences by their six distinct identifiers. A heading-only rewrite fails.
- [Caveats] The file must not become the design document. §5.7's general rule applies to this rewrite as much as to the instrument: the file says what to run and what to decide; the *why* lives in `ABOUT-REVERSE` and in the design document, which the file cites by section. A file that restates the whole design would rot against it.
- [Open items] The six absences named in `## What this command cannot yet reach` are removed one per ticket by P23-3 (N5), P23-4 (N6), P23-5 and P23-6 (N1), P23-7 (N2) and P23-8 (N3, N4). Until then the section is correct and must not be deleted.
- [Future improvements] Once P23-12's §1.2 audit exists, the file can cite it by name in Step 0, so the invariant is stated once and measured mechanically rather than asserted in prose in each command file.

## P23-1 — 0 locations, not implemented

Not found
