# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is P26-1: The audit's findings are closed: the routine gate defers what it says it defers, the frozen record can be falsified, and the design document is held to the code

**Ticket Key**: P26-1 · **Phase**: 26

---

## Background

- [Goal] Close every finding of the P23-P25 audit: the coverage gate that ran the deferred suite, the terminal-state record that silently rewrote itself, the scratch digest taken over bytes that were never written, the drifted citations and stale numbers in the design document, and the provenance annotations this work was done without.
- [Purpose] Each finding is a place where a record and the thing it records had parted, and where nothing was measuring the gap. The fixes are therefore not only corrections but guards, so the next parting fails by name instead of being read past.
- [Motivation] A design document that asks to be re-derived rather than trusted, and a frozen observation that §7.3 rests on, are worth exactly what their verification is worth. The audit found `make test-coverage` running the twenty files `make test` defers, and a record that corrected itself rather than reporting that it had stopped describing the chain.
- [Constraints] No unexcluded heavy test run (the standing instruction). No test weakened to make an implementation pass. The installed copies are not hand-synced: the measurement shows a uniform release-snapshot lag, and a partial sync would leave a head library inside an old tree.

## Scope

- **Scope of changes:**
  - [File] `Makefile` — [Action] modify — [What] `DEFERRED_TEST_PATH` becomes the one declaration of the deferred directory; `TEST_EXCLUDES` derives from it; `HEAVY_TEST_TARGETS` declares the targets that run it on purpose; `test-coverage` filters the deferred path out of its collection. The coverage comment records the re-measured basis. — [Why] `test-coverage` passed no exclusion while `test` beside it did, so the coverage gate ran the twenty deferred files on every invocation and nothing reported the disagreement.
- **Scope of changes:**
  - [File] `.claude/scripts/workspacify-reverse/lib/terminal-state.mjs` — [Action] modify — [What] `decisionsInputText` and `decisionsInputDigest` are added, so one function produces the bytes a decisions input is written as and the digest a record names it by. — [Why] The digest was taken over `JSON.stringify(skeleton)` while the file held the indented form, so the record named bytes that were never on disk.
- **Scope of changes:**
  - [File] `tests/workspacify-reverse/integration/terminal-state.test.mjs` — [Action] modify — [What] the record is asserted rather than rewritten; regeneration moves behind `WSP_TERMINAL_STATE_REGENERATE=1`; the scratch digest uses the shared functions. — [Why] Writing on change made the record unfalsifiable: a record that had stopped describing the chain was corrected by the run that exists to notice, and nothing was ever reported.
- **Scope of changes:**
  - [File] `tests/workspacify-reverse/helpers/decisions-authoring.mjs` — [Action] modify — [What] `DECISIONS_INPUT_SKELETON` is declared beside the path that locates an authored input. — [Why] The bytes a run reads and the digest it records must come from one declaration, and the unit surface — the only surface that runs on every routine run — must be able to reach it.
- **Scope of changes:**
  - [File] `docs/WORKSPACIFY-4-PATTERNS-COMPLETE-DESIGN.md` — [Action] modify — [What] nine drifted citations re-anchored; the A4 attribution corrected; `DesignTree.json` added to §2.3; §5.3's structure gains `## Two modes, never conflated`; §A.1's count corrected to the directory's 30 documents and its `--through=r5.5` sentence replaced by the measured fact; §7.1's reachability row re-evidenced; A6's fold into A3 and the `out_of_scope` mechanism described as implemented. — [Why] The document's own Appendix A.5 re-dates itself so a reader can re-derive rather than trust; nothing applied that discipline to the citations or the numbers.
- **Scope of changes:**
  - [File] `tests/conventions/makefile-test-runs.test.mjs` — [Action] add — [What] holds every test-running target to the deferral declaration. — [Why] The decision is data, not a comment a reader has to notice.
- **Scope of changes:**
  - [File] `tests/conventions/design-citations.test.mjs` — [Action] add — [What] sixteen anchored citations with their tokens, plus a scan of every `file:line` citation in the document for single-file resolution and in-range lines. — [Why] Nine of sixteen had drifted, and two more were ambiguous about which file they named.
- **Scope of changes:**
  - [File] `tests/conventions/design-measurements.test.mjs` — [Action] add — [What] holds §2.3's inventory to `TERMINAL_ARTEFACTS`, §A.1's count to the analysis directory, and §5.3's structure block to the command file's sections. — [Why] Both disagreements were invisible to reading and visible to comparison.
- **Scope of changes:**
  - [File] `tests/workspacify-reverse/unit/terminal-state-record.test.mjs` — [Action] add — [What] checks the committed observation's decisions digests against the committed files and the fallback skeleton, from the routine surface. — [Why] The file that produced the record is deferred from `make test`, so nothing on the routine path was holding it.
- **Scope of changes:**
  - [File] `tests/conventions/installed-copy-drift.test.mjs` — [Action] modify — [What] the record gains `claim-ledger.mjs`, and the comment records the measured decision not to advance the copies. — [Why] P25-6 moved the source of record and did not re-measure, and the shipped record was red from `8c289f45`.
- **Scope of changes:**
  - [File] `tests/workspacify-reverse/analysis/TERMINAL-STATE.json` — [Action] modify — [What] regenerated deliberately; the two `<scratch>` digests move from the compact form to the bytes the file holds. — [Why] The only difference the regeneration produces is that one field, which is what makes the defect legible.

## Implementation Target Files

- `Makefile`
- `.claude/scripts/workspacify-reverse/lib/terminal-state.mjs`
- `docs/WORKSPACIFY-4-PATTERNS-COMPLETE-DESIGN.md`
- `tests/workspacify-reverse/helpers/decisions-authoring.mjs`
- `tests/workspacify-reverse/integration/terminal-state.test.mjs`
- `tests/workspacify-reverse/unit/terminal-state.test.mjs`
- `tests/workspacify-reverse/unit/terminal-state-record.test.mjs`
- `tests/conventions/makefile-test-runs.test.mjs`
- `tests/conventions/design-citations.test.mjs`
- `tests/conventions/design-measurements.test.mjs`
- `tests/conventions/installed-copy-drift.test.mjs`
- `tests/workspacify-reverse/analysis/TERMINAL-STATE.json`

## Investigation

The audit measured the design document's claims against the repository: 51 modules under `workspacify-reverse/lib` with exactly five unreachable (`invariant-audit`, `language-representatives`, `staleness`, `terminal-state`, `two-pass`), the command definition at 289 lines carrying all eight structural assertions, the frozen command set at fifteen, the seed at fourteen headings, the isolation check reproducing A.2 exactly (exit 1 naming nine artefacts; exit 0 over 2791 files), and both representative trees uncontaminated and clean. Against that, sixteen line-anchored citations were checked: six resolved, eight had drifted, one named an unrelated test, and one attribution was imprecise. `make test-coverage` was found to collect `tests/workspacify-reverse` without the exclusion `make test` carries, so it ran the twenty deferred files. `terminal-state.test.mjs` rewrote the record it exists to check. The scratch digest was `sha256(JSON.stringify(skeleton))` while the file held `JSON.stringify(skeleton, null, 2) + '\n'`. And `installed-copy-drift.test.mjs` had been red since `8c289f45`, because P25-6 moved `claim-ledger.mjs` without re-measuring.

## Acceptance Criteria

- **[Happy path]** `make test-coverage` exits 0 at branch 84.5%, having collected 59 of the 79 files under `tests/workspacify-reverse`; `tests/conventions` 47 of 47; `tests/workspacify-reverse/unit` + `regression` 927 of 927.
- **[Error case]** A record that no longer describes the chain fails the observation test and is not rewritten; a target that runs the deferred suite without declaring it fails `makefile-test-runs.test.mjs` by name.
- **[Edge case]** §2.3 with `DesignTree.json` removed fails `design-measurements.test.mjs` naming that artefact; a citation naming two files fails `design-citations.test.mjs` naming both.

## Invariants

- 【Normal establishment】`make test-coverage` collects the deferred path out of its file list and exits 0 at branch 84.5%
- 【Invariant on error】A record that no longer describes the chain fails the observation test; the run does not rewrite it
- 【Internal state invariant】The bytes a decisions input is written as and the digest the record names it by are produced by one function
- 【Boundary invariant】Every `file:line` citation in the design document names exactly one file, and its line is inside that file

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — P26-1 — the routine gate and its declaration

- **Precondition**: A Makefile target whose recipe reaches `tests/workspacify-reverse`
- **Postcondition**: The recipe references the deferral (`$(TEST_EXCLUDES)` or `$(DEFERRED_TEST_PATH)`) or the target is named in `HEAVY_TEST_TARGETS`
- **Invariant**: `TEST_EXCLUDES` is derived from `DEFERRED_TEST_PATH`, so no target can defer a different directory from the one the declaration names

### C002 — P26-1 — the decisions input and its digest

- **Precondition**: A decisions input object the chain falls back to
- **Postcondition**: `decisionsInputDigest(skeleton)` is the sha256 of the bytes `decisionsInputText(skeleton)` produces
- **Invariant**: The bytes the writer writes and the bytes the digest names come from one function, so they cannot disagree

### C003 — P26-1 — the committed observation

- **Precondition**: A committed `TERMINAL-STATE.json` and the chain that produced it
- **Postcondition**: The observation test asserts the committed bytes against a fresh measurement, and writes only when `WSP_TERMINAL_STATE_REGENERATE=1`
- **Invariant**: A stale record fails rather than being corrected, and the file is byte-identical afterwards

### C004 — P26-1 — the design document as a measurement

- **Precondition**: The design document and the repository it describes
- **Postcondition**: Every anchored citation resolves to a line carrying its token; every `file:line` citation names one file and an in-range line
- **Invariant**: §2.3's inventory, §A.1's count and §5.3's structure block each equal the artefact, directory and file they describe

## Boy Scout Rule

`decisionsDigestFor` and `decisionsFor` each built the skeleton's serialization independently, which is how they came to disagree; one function now produces it for both. The skeleton moved from the integration test to the helper that already owns the decisions path, so the routine surface can reach it. `REVERSE_GATE_IDS`'s A6 was documented in code but absent from the design's acceptance table, so the table gained the row rather than the code gaining a redundant gate.

## Test Plan

### Unit Tests

- UT: [Normal] every test-running target that reaches `tests/workspacify-reverse` either passes the deferral or is named in `HEAVY_TEST_TARGETS`; a target that does neither is reported by name
- UT: [Error] the guard fails when `test-coverage` is edited back to collecting without the deferral, naming the target
- UT: [Boundary] `test` still passes `$(TEST_EXCLUDES)` and `TEST_EXCLUDES` still derives from `DEFERRED_TEST_PATH`, so the two cannot drift apart
- UT: [Invariant] the recorded decisions digest equals the sha256 of the bytes the file holds, not of a differently-serialized copy of the same object
- UT: [Invariant] a compact serialization's digest is not the input digest, so a revert to the old form fails
- UT: [Normal] a committed decisions input's recorded digest is the sha256 of the committed file it names
- UT: [Error] a `<scratch>` digest that is not the skeleton's written bytes fails, naming the representative
- UT: [Invariant] §2.3 names every artefact `TERMINAL_ARTEFACTS` declares, with `<PKG>` normalised and the brace shorthands expanded
- UT: [Invariant] §5.3's structure block names every `##` section the command file carries
- UT: [Boundary] §A.1's stated count equals the number of documents in the analysis directory, and the appendix and the correcting sentence agree
- UT: [Normal] every anchored citation resolves to a line carrying its token
- UT: [Error] a citation naming more than one file is reported rather than guessed at
- UT: [Boundary] every cited line number is inside its file

### Integration Tests

- IT: [Integration point] `tests/workspacify-reverse/integration/terminal-state.test.mjs` over the four pattern representatives, with `WSP_TERMINAL_STATE=1`
- IT: [Verification] the committed record reproduces byte-for-byte from a fresh run (11 of 11 pass), and a perturbed record fails rather than being rewritten — the file still carrying the perturbation afterwards
- IT: [Prerequisites] a git checkout with the four representatives present; the run costs about 141 s
- IT: [Related tickets] P24-8, P24-12 (the record), P25-3, P25-6 (the regeneration), P25-1 (the drift guard), P25-4 (the coverage target)

### Exceptions

- The installed copies are not asserted to have been advanced. Advancing them is an install at a released version, and the measurement is recorded in the guard's comment rather than turned into an assertion this ticket cannot honestly make.
- The design document's prose is not asserted for meaning. The guards hold its citations to resolving lines and its numbers to measured ones; whether a sentence says what it means is a reader's judgement.

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| `test-coverage` collected every test under `tests/workspacify-reverse` and passed no exclusion, so the coverage gate ran the twenty files `make test` deliberately defers. Nothing reported the disagreement, because the two targets shared no declaration. The recorded measurement (88.19% branch) was therefore of a basis the routine run does not use. | `DEFERRED_TEST_PATH` is the one declaration; `TEST_EXCLUDES` derives from it; `HEAVY_TEST_TARGETS := test-all test-workspacify-reverse` names the targets that run it on purpose; `test-coverage` filters the deferred path out of its collection. Measured: 79 files collected, 59 run, 20 deferred; exit 0 at branch 84.5% (84.55 and 84.54 across two runs), 33 s. `tests/conventions/makefile-test-runs.test.mjs` holds every test-running target to the declaration in both directions. | Close the coverage gate's undeclared deferral, and make the decision data |
| `decisionsDigestFor` hashed `JSON.stringify(DECISIONS_SKELETON)` while `decisionsFor` wrote `${JSON.stringify(DECISIONS_SKELETON, null, 2)}\n`. The record named bytes that were never on disk, its own comment claimed it named what the run read, and the two representatives that read the same skeleton could not be told apart by the field that exists to say what each run read. | `terminal-state.mjs` exports `decisionsInputText` and `decisionsInputDigest`; the writer and the digest both use them. `DECISIONS_INPUT_SKELETON` moved to `helpers/decisions-authoring.mjs`. Regenerating the committed record changes exactly the two `<scratch>` digests: `51579904…` becomes `631f102d…`, which is the hash of the bytes the file holds. `unit/terminal-state-record.test.mjs` checks this from the routine surface. | Make the decisions digest name the bytes the run actually reads |
| The observation test wrote the record whenever it differed from the measurement, so a record that had stopped describing the chain was corrected by the run that exists to notice it. The write also raced `verification-surface.test.mjs`, which digests the same tree around its own nested run. | The test asserts the committed bytes. Regeneration moved behind `WSP_TERMINAL_STATE_REGENERATE=1`, and the race is gone at its source because the measured case no longer touches the file. Verified both ways: the committed record reproduces byte-for-byte (11 of 11 pass), and a perturbed `runs[0].started_from` fails the run while the file still carries the perturbation afterwards. | Make the frozen terminal-state observation falsifiable |
| Sixteen line-anchored citations: six resolved, eight had drifted, one named an unrelated C002 stage-ordering test instead of the eight structural assertions, and the A4 attribution named `seed-parse.mjs` for a comparison that lives in `seed-local-checks.mjs`. `scope.mjs:1222` was 707 lines from the single `publishDocuments` call it named. | All sixteen re-anchored, and two more disambiguated (`run.mjs:81` and `reverse-mode.mjs:206` each named more than one file). `tests/conventions/design-citations.test.mjs` holds the sixteen to the line carrying their claim, and scans every `file:line` citation in the document for single-file resolution and in-range lines. | Re-anchor the design document's evidence pointers and guard them |
| §A.1 said the analysis publishes 21 documents and that the directory beside it holds the sidecars of a `--through=r5.5` run; the directory held 30 documents of a run that went to the exit. §2.3's inventory omitted `DesignTree.json`, so the page described an inventory one element short of the one every observation is measured against. §5.3's structure block omitted `## Two modes, never conflated`, which §5.8 requires the file to carry. §7.1's reachability row carried a date and no in-tree evidence. A6 was declared in code and absent from the acceptance table; the `out_of_scope` rule was described as a check rather than as a state that makes the substitution unrepresentable. | §A.1 states the 30 documents and says what evidences reachability (`ORIGIN-LONG-SPEC.json` is published only when r8 ran, `scope.mjs:1923`); §2.3 lists `DesignTree.json` and states the eleven-and-eight the instrument declares; §5.3 lists the modes section; §7.1 carries both the 2026-09-11 measurement and the in-tree re-evidence; the acceptance table gains A6 with its fold into A3; the `out_of_scope` row describes the three coverage states. `tests/conventions/design-measurements.test.mjs` holds §2.3, §A.1 and §5.3 to their subjects in both directions. | Hold the design document's inventories and counts to the repository |
| `tests/conventions/installed-copy-drift.test.mjs` recorded eleven differing files. P25-6 moved `claim-ledger.mjs` when it fixed `owningDirectoryOf` and did not re-measure, so the assertion was red from `8c289f45` while that ticket's review reported the conventions surface as passing. | Re-measured to twelve and recorded by name. The same measurement settles the open question of advancing the copies: all twelve differing files in both copies are historical revisions of the source of record, so none carries a local edit, but the lag is not this library's — the whole installed `.claude` is one older snapshot, down to a command file the source no longer has. A hand-sync would leave a head library inside an old tree, so the copies stay gated and the record is the finding. | Record the drift P25-6 left unrecorded, and settle the question of advancing the copies |

## Notes in Prior Implementation Rounds

- [Implementation steps] 1) Red for each finding, run and confirm it fails for the stated reason. 2) Green. 3) Re-run the surface and confirm no regression. 4) Re-measure the frozen numbers the change moved. 5) Record the decisions the measurement settled.
- [Risks] The citation guard resolves a citation by path tail, so a citation naming two files is reported rather than guessed at; two were ambiguous and the document was disambiguated instead.
- [Caveats] `make test` was not run, by the standing instruction against it. The surfaces the changes reach were run directly.
- [Open items] Whether the four generic markers in `red-reconstruction.mjs` are the right generic set, and whether `two-pass.mjs` and `staleness.mjs` should be wired into the run. Both are recorded in the design's §6 and are unchanged by this ticket.
- [Future improvements] A guard that measures the exemption rather than listing it, so an exempted command definition is a decision with a reason rather than a default.

## P26-1 — implemented at 20 locations

### .claude/scripts/workspacify-reverse/lib/terminal-state.mjs

- Line 52
```
  return `${JSON.stringify(skeleton, null, 2)}\n`;
```

- Line 63
```
  return createHash('sha256').update(decisionsInputText(skeleton), 'utf8').digest('hex');
```

### tests/conventions/design-citations.test.mjs

- Line 72
```
function lineOf(relativePath, line) {
```

- Line 110
```
function projectFiles(directory = PROJECT_ROOT, prefix = '') {
```

- Line 133
```
function resolveCitation(citedPath) {
```

### tests/conventions/design-measurements.test.mjs

- Line 36
```
function sectionBetween(fromHeading, toHeading) {
```

- Line 53
```
function expandBraceShorthands(text) {
```

- Line 74
```
function declaredInventories() {
```

- Line 137
```
function declaredStructureBlock() {
```

### tests/conventions/installed-copy-drift.test.mjs

- Line 76
```
const DIFFERING_IN_COPIES = Object.freeze([
```

### tests/conventions/makefile-test-runs.test.mjs

- Line 35
```
function nearestMakefileFrom(startDirectory) {
```

- Line 82
```
function recipesByTarget(makefileText) {
```

- Line 106
```
function variableValue(makefileText, name) {
```

- Line 113
```
function declaredHeavyTargets(makefileText) {
```

- Line 123
```
function targetsReachingDeferredTree() {
```

### tests/workspacify-reverse/helpers/decisions-authoring.mjs

- Line 95
```
  workspace: [], ownership: [], dependencies: [], adapters: [], approvals: [],
```

### tests/workspacify-reverse/integration/terminal-state.test.mjs

- Line 192
```
function decisionsFor(representative, scratchRoot) {
```

- Line 212
```
function decisionsDigestFor(representative) {
```

### tests/workspacify-reverse/unit/terminal-state-record.test.mjs

- Line 54
```
function digestOfCommittedFile(relativePath) {
```

### tests/workspacify-reverse/unit/terminal-state.test.mjs

- Line 412
```
  tree: [{ path: 'src', owned: true }],
```
