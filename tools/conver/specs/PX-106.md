# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-106: Inspection Sentinel Idempotency — Prevent prefix accumulation across find-omissions cycles

**Ticket Key**: PX-106 · **Phase**: -1

---

## Background

### Goal
Provide idempotent prefix prepending across create-tmp-omissions.js and add-omission-ticket.js so that running /find-omissions any number of times never accumulates duplicate REJECTION_WARNING or ABC_INSPECTION_PREFIX text in ticket backgrounds.

### Purpose
The find-omissions pipeline runs repeatedly (one cycle per implementation pass). Without idempotency, each cycle appends a new copy of the inspection warning text, causing backgrounds to grow unboundedly. This fix is prerequisite infrastructure for multi-pass find-omissions workflows.

### Motivation
The OMISSIONS to phasify to merge to re-implement to re-inspect loop is the core re-implementation pipeline. Without sentinel-based idempotency, this loop is limited to a single iteration before backgrounds become corrupted. The need is immediate: the second find-omissions cycle on siprs will trigger this bug.

### Constraints
- Must use startsWith() for detection (no regex, no substring search). Must not modify the signature or behavior of any existing public API. Must be backward compatible with OMISSIONS-*.json files generated before this fix. Must not require changes to Tickets.json schema.

## Scope

- modify .claude/scripts/tickets/create-tmp-omissions.js: enrichTickets() at line 194 — add INSPECTION_SENTINEL to REJECTION_WARNING start, add startsWith guard before prepend, add repair logic for duplicate sentinels
- modify .claude/scripts/tickets/add-omission-ticket.js: appendTicket() and --ticket-key mode clone path — add INSPECTION_SENTINEL to ABC_INSPECTION_PREFIX start, add startsWith guard, export sentinel for create-tmp-omissions.js to require
- define INSPECTION_SENTINEL = "[::INSPECTION_FLAGGED::]" in add-omission-ticket.js, export via module.exports
- add repairDuplicateSentinels(background) pure function — strips all but last occurrence of INSPECTION_SENTINEL
- add countInspectionSentinels(background) pure function — for diagnostics
- Changes: create-tmp-omissions.js enrichTickets() — prepend sentinel to REJECTION_WARNING, wrap prepend in startsWith guard. add-omission-ticket.js appendTicket() — prepend sentinel to ABC_INSPECTION_PREFIX, wrap prepend in startsWith guard. Same guard in --ticket-key mode new-clone path.
- Out of scope: Modifying REJECTION_WARNING or ABC_INSPECTION_PREFIX text beyond adding sentinel. Modifying any other script in the find-omissions pipeline. Modifying Tickets.json schema.
- Affected: All future OMISSIONS-*.json files generated by /find-omissions. All tickets passing through the re-implementation pipeline. No impact on fields other than background.
- Impact nature: Textual/idempotency only — no API surface change, no schema change, no performance impact. startsWith guard is O(1).

## Investigation

Investigation confirmed accumulation paths via source code reading:

1. create-tmp-omissions.js line 194: unconditional prepend in enrichTickets().
2. add-omission-ticket.js line 92: unconditional prepend in appendTicket().
3. --ticket-key mode clone path: deep clone via lookupTicket() from Tickets.json, then through appendTicket(). Clone inherits background from Tickets.json — may already contain prefixes from previous cycle.
4. REJECTION_WARNING and ABC_INSPECTION_PREFIX are separate constants with different text, both lacking sentinel or idempotency guard.
5. validate-tickets.js ALLOWED status list missing "remanded" — fixed as prerequisite.

## Acceptance Criteria

- After any sequence of create-tmp-omissions + add-omission-ticket operations, no background contains more than 1 INSPECTION_SENTINEL
- Existing data with duplicate sentinels is repaired with warning logged
- Pre-sentinel data (0 sentinels) gets one sentinel prepended — backward compatible
- Empty background strings handled gracefully — repairDuplicateSentinels returns empty string
- Happy path: run create-tmp-omissions.js twice on same Tickets.json → all backgrounds byte-identical between runs
- Error case: background with 3 stacked sentinels → repairDuplicateSentinels reduces to exactly 1 sentinel, warning logged
- Edge case: background is exactly sentinel string with no content after → repairDuplicateSentinels returns it unchanged, count returns 1

## Invariants

- [Normal condition] Every ticket background must contain exactly 0 or 1 occurrences of the sentinel "[::INSPECTION_FLAGGED::]".
- [Error invariant] Even if repairDuplicateSentinels encounters malformed input (null, undefined), it must return empty string rather than throw.
- [Internal state invariant] The INSPECTION_SENTINEL constant value must never change after definition.
- [Boundary invariant] repairDuplicateSentinels must handle strings up to 1MB.

## Contracts — mandatory 100% test coverage in TDD Red phase

### C106 — PX-106→create-tmp-omissions.js + add-omission-ticket.js

- **Precondition**: Ticket background string exists (may be empty, may already contain INSPECTION_SENTINEL)
- **Postcondition**: Background contains exactly 1 INSPECTION_SENTINEL at position 0 with appropriate prefix text
- **Invariant**: After any sequence of create-tmp-omissions + add-omission-ticket operations, no ticket background contains more than 1 occurrence of INSPECTION_SENTINEL

## Boy Scout Rule

Target files are already well-structured with clear function boundaries and descriptive names:

1. Extract prepend-with-guard logic into shared pure function to eliminate duplicated startsWith check across two files.
2. INSPECTION_SENTINEL exported from add-omission-ticket.js and required by create-tmp-omissions.js, establishing clear dependency direction.
3. No existing translatability violations — function names are verb phrases; variable names express domain concepts.

## Test Plan

### Unit Tests

- UT: repairDuplicateSentinels returns unchanged string when sentinel count is 0 — input: "plain text", output: "plain text"
- UT: repairDuplicateSentinels returns unchanged string when sentinel count is 1 — input: "[::INSPECTION_FLAGGED::]\nprefix\n\ntext", output unchanged
- UT: repairDuplicateSentinels strips first sentinel when count is 2, keeping only the last occurrence and everything after it
- UT: repairDuplicateSentinels strips N-1 sentinels when count is N (property: output always has count ≤ 1)
- UT: repairDuplicateSentinels handles empty string gracefully — input: "", output: ""
- UT: repairDuplicateSentinels handles string with sentinel only (no content after) — returns sentinel-only string without modification
- UT: countInspectionSentinels returns 0 for string without sentinel
- UT: countInspectionSentinels returns 1 for string with one sentinel
- UT: countInspectionSentinels returns N for string with N sentinels — inputs with 2,3,5 sentinels verified
- UT: enrichTickets does not prepend REJECTION_WARNING when background already starts with INSPECTION_SENTINEL
- UT: enrichTickets prepends REJECTION_WARNING when background does not start with sentinel
- UT: appendTicket does not prepend ABC_INSPECTION_PREFIX when background already starts with INSPECTION_SENTINEL
- UT: appendTicket prepends ABC_INSPECTION_PREFIX when background does not start with sentinel

### Integration Tests

- IT: create-tmp-omissions.js idempotency — run twice on same Tickets.json, verify all ticket backgrounds byte-identical
- IT: add-omission-ticket.js idempotency — run --ticket-key mode twice for same ticket, verify clone backgrounds identical
- IT: Two-cycle simulation — create-tmp → add-omission → simulate merge → create-tmp again → verify exactly 1 INSPECTION_SENTINEL per background
- IT: Cross-prefix repair — REJECTION_WARNING stacked on ABC_INSPECTION_PREFIX reduced to single sentinel

### Exceptions

- Cannot test infinite cycle accumulation deterministically — two-cycle simulation with real Tickets.json provides induction-style confidence. This is not an architectural defect; idempotence is guaranteed by startsWith check, fully unit-tested.

### Plan Test Code (concrete code)

- UT: [Precondition] repairDuplicateSentinels handles all input forms (empty, no sentinel, one sentinel, multiple sentinels)
  ```javascript
  const { repairDuplicateSentinels, countInspectionSentinels } = require("./add-omission-ticket.js");
  assert.strictEqual(repairDuplicateSentinels(""), "");  // empty
  assert.strictEqual(repairDuplicateSentinels("plain"), "plain");  // no sentinel
  const one = "[::INSPECTION_FLAGGED::]\nprefix\n\ntext";
  assert.strictEqual(repairDuplicateSentinels(one), one);  // one sentinel
  const two = "[::INSPECTION_FLAGGED::]\na\n\n[::INSPECTION_FLAGGED::]\nb\n\ntext";
  assert.strictEqual(countInspectionSentinels(repairDuplicateSentinels(two)), 1);  // multiple
  ```
- UT: [Postcondition] After repair, background has exactly 1 sentinel at position 0
  ```javascript
  const { countInspectionSentinels } = require("./add-omission-ticket.js");
  const repaired = repairDuplicateSentinels(two);
  assert.strictEqual(countInspectionSentinels(repaired), 1);
  assert.ok(repaired.startsWith("[::INSPECTION_FLAGGED::]"));
  ```
- UT: [Invariant] No background exceeds 1 sentinel after N simulated cycles (induction property)
  ```javascript
  const { countInspectionSentinels, repairDuplicateSentinels } = require("./add-omission-ticket.js");
  for (let n = 0; n < 5; n++) {
    let bg = "original";
    for (let c = 0; c <= n; c++) bg = repairDuplicateSentinels("[::INSPECTION_FLAGGED::]\nprefix\n\n" + bg);
    assert.ok(countInspectionSentinels(bg) <= 1);
  }
  ```

## Changes

| Before | After | Description |
|--------|-------|-------------|
| REJECTION_WARNING lacking sentinel; unconditional prepend | REJECTION_WARNING starts with [::INSPECTION_FLAGGED::]; startsWith guard before prepend | PX-106: Added sentinel prefix to create-tmp-omissions.js enrichTickets() |
| ABC_INSPECTION_PREFIX lacking sentinel; unconditional prepend | ABC_INSPECTION_PREFIX starts with [::INSPECTION_FLAGGED::]; startsWith guard before prepend | PX-106: Added sentinel prefix to add-omission-ticket.js appendTicket() and --ticket-key path |
| No idempotent sentinel infrastructure | INSPECTION_SENTINEL, repairDuplicateSentinels(), countInspectionSentinels() exported | PX-106: Added pure functions for sentinel count and repair |
| No test coverage for sentinel idempotency | 24 new tests covering count, repair, appendTicket guard, enrichTickets guard, N-cycle property | PX-106: Added test cases to scripts.test.js |

## Notes

### Implementation steps
1. Define INSPECTION_SENTINEL in add-omission-ticket.js, export via module.exports. 2. Prepend sentinel to ABC_INSPECTION_PREFIX constant. 3. Add repairDuplicateSentinels() and countInspectionSentinels() pure functions. 4. Modify appendTicket(): wrap background prepend in startsWith guard. 5. Modify --ticket-key mode new-clone path: same guard. 6. In create-tmp-omissions.js: require INSPECTION_SENTINEL. 7. Prepend sentinel to REJECTION_WARNING. 8. Modify enrichTickets(): wrap background prepend in startsWith guard. 9. Run test suite. 10. Run two-cycle integration test.

### Risks
- If any code path strips leading bracket characters from backgrounds, sentinel could be damaged.
- If sentinel text conflicts with legitimate ticket content. Mitigation: [::...::] format is exclusive to project metadata markers.

### Caveats
- Fix is idempotent going forward but does NOT retroactively clean existing OMISSIONS-*.json files.
- Sentinel makes REJECTION_WARNING and ABC_INSPECTION_PREFIX mutually exclusive per ticket.

### Open items
- Should sentinel be extracted to shared constants file? Deferred to separate refactoring ticket.

### Future improvements
- General-purpose prefix registry tracking all prepended metadata markers.

### Implementation summary
- Changed files: add-omission-ticket.js, create-tmp-omissions.js, scripts.test.js
- Key changes: INSPECTION_SENTINEL + startsWith guard in both scripts
- Test results: 122 passed (24 new), 0 failed
- Contracts: C106 covered in scripts.test.js
- AMBIGUOUS markers: 2 resolved

### Review report
- Static quality check: OK (CLI-intentional console.log only)
- Translatability: all verb-phrase function names, no issues
- Dependencies: consistency verified (add-omission-ticket.js + create-tmp-omissions.js)
- Issues found and fixes applied: validate-tickets.js ALLOWED remanded added; schema status enum expanded; 2 AMBIGUOUS resolved

## PX-106 — implemented at 10 locations

### .claude/scripts/lib/validate-tickets.js

- Line 3
```javascript

```

### .claude/scripts/tickets/add-omission-ticket.js

- Line 31
```javascript
function countInspectionSentinels(background) {
```

- Line 38
```javascript
function repairDuplicateSentinels(background) {
```

- Line 97
```javascript
function appendTicket(data, ticket) {
```

### .claude/scripts/tickets/create-tmp-omissions.js

- Line 43
```javascript
function countSentinels(bg) {
```

- Line 48
```javascript
function repairSentinels(bg) {
```

- Line 59
```javascript
function _resolveSentinels() {
```

- Line 228
```javascript
function enrichTickets(mergedEntries, ticketsData, ticketLookup) {
```

### .claude/tests/tickets/scripts.test.js

- Line 21
```javascript
function assertOk(value, message) {
```

- Line 27
```javascript
function runScript(scriptName, args, stdin) {
```
