# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-109: phasify-omissions --rollback: deterministic phase rollback from Tickets.json

**Ticket Key**: PX-109 · **Phase**: -1

---

## Background

Goal: phasify-omissions.js --rollback フラグで、直前にマージしたフェーズを Tickets.json から決定論的に除去できること。

Purpose: 誤マージ時の安全な復元手段を提供する。バックアップファイルからの手動復元に依存しない。

Motivation: PX-108 で自動マージが可能になったが、ロールバック手段がない。決定論的アルゴリズムなら機械的ロールバックが可能。

Constraints:
- ロールバックは追加されたフェーズのみを除去。既存フェーズは一切変更しない
- マージ情報（offset）は Tickets.json metadata に永続化する
- --dry-run と組み合わせて削除プレビュー可能
- ロールバック後も再マージ可能

## Scope

- Tickets.json metadata 拡張: phasify-omissions がマージ時に metadata.phasifyMerge に {offset, mergedPhaseIds, mergedAt} を書き込む
- --rollback 解析: parseArguments() に --rollback フラグを追加
- rollbackPhasifyMerge() 実装: Tickets.json を読み込み metadata.phasifyMerge.offset 以上の phase.id を phases から除去、該当 phaseId のチケットを全チケットから除去、metadata.phasifyMerge を削除
- --rollback --dry-run 対応: 削除対象のフェーズ一覧を表示して終了（ファイル書き込みなし）
- エラーハンドリング: metadata.phasifyMerge 不在時はエラー終了、既存フェーズとの整合性検証
- 全テスト追加
- Scope of changes:
  - Path: .claude/scripts/rfc-graph/phasify-omissions.js
  - Action: modify (add rollbackPhasifyMerge(), extend runPhasifyOmissions(), extend parseArguments())
  - Detail: Add 1 new function + --rollback CLI flag + merge metadata injection
  - Before: merge pipeline writes Tickets.json with metadata unchanged. After: metadata.phasifyMerge written on merge; --rollback removes merged phases
  - API: No breaking changes. New optional --rollback flag. New exported function rollbackPhasifyMerge()
  - Schema: Tickets.json.metadata.phasifyMerge added (optional, only present after merge)
  - Config: No change
  - Dep: No new dependencies
- Out of scope:
  - Item: --rollback-to=<offset> partial rollback. Not needed now.
  - Why: Separate ticket if needed
- Affected areas:
  - Component: Tickets.json.metadata
  - Impact: Data format — metadata extended with phasifyMerge object
  - Response: N/A (backward compatible, phasifyMerge is optional)

## Investigation

Key code patterns identified:

1. Current merge pipeline (runPhasifyOmissions Step P):
   atomicWrite(opts.ticketsPath, JSON.stringify(mergedResult.data, null, 2) + '\n');
   → Before this line, inject: mergedResult.data.metadata.phasifyMerge = { offset, mergedPhaseIds, mergedAt }

2. parseArguments() currently handles: --omissions, --graph, --tickets, --min-nodes, --output, --dry-run, --verbose
   → Add: } else if (arg === '--rollback') { opts.rollback = true; }
   → Add: rollback: false to CliOptions typedef and default object

3. atomicWrite() function (PX-108) already provides safe tmp+rename write pattern

4. validateTickets() in lib/validate-tickets.js validates full structure — should pass after rollback since existing phases unchanged

## Acceptance Criteria

- Happy path: phasify-omissions --graph=GRAPH.json でマージ後、--rollback で完全に元の状態に戻る。git diff Tickets.json で差分ゼロ。
- Error case: ロールバック対象の metadata がない Tickets.json に --rollback を実行するとエラー終了する。
- Edge case: --rollback --dry-run で削除予定のフェーズ一覧が表示され、ファイルは変更されない。

## Invariants

- [Normal condition] ticketsData.metadata.phasifyMerge must have {offset: number, mergedPhaseIds: number[], mergedAt: string}.
- [Error invariant] If phasifyMerge is missing, rollback must fail with clear error — never silently skip.
- [Internal state invariant] rollbackPhasifyMerge is a pure function (no I/O). Only the caller (runPhasifyOmissions) performs atomic write.
- [Boundary invariant] offset must be > all existing pre-merge phase IDs. If offset overlaps with existing IDs, rollback is rejected.

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — parseArguments() --rollback flag → opts.rollback

- **Precondition**: CLI argv contains '--rollback'. parseArguments reads it and sets opts.rollback = true.
- **Postcondition**: opts.rollback === true. Other opts fields (dryRun, graphPath, etc.) unaffected.
- **Invariant**: --rollback and --dry-run are independent flags; both can be true simultaneously.

### C002 — rollbackPhasifyMerge() → Tickets.json without merged phases

- **Precondition**: Accepts (ticketsData: object). ticketsData.metadata.phasifyMerge must exist with {offset: number, mergedPhaseIds: number[], mergedAt: string}.
- **Postcondition**: Returns deep-cloned object with: (1) all phases where id >= offset removed, (2) tickets with phaseId >= offset removed from remaining phases, (3) metadata.phasifyMerge deleted. Throws Error if phasifyMerge missing or offset invalid.
- **Invariant**: All phases with id < offset are deep-equal before and after rollback. No mutation of input.

### C003 — --rollback --dry-run → stdout preview only

- **Precondition**: opts.rollback === true && opts.dryRun === true. metadata.phasifyMerge exists.
- **Postcondition**: stdout lists phases to be removed with their ticket count. No fs.writeFileSync/renameSync/unlinkSync calls. Exit 0.
- **Invariant**: Tickets.json on disk is byte-identical before and after dry-run rollback.

## Boy Scout Rule

1. rollbackPhasifyMerge() as a pure function (no I/O) following the same pattern as mergePhasifyToTickets().
2. Verb-prefix naming: rollbackPhasifyMerge (verb: 'rollback').
3. Deep clone inputs to avoid mutation, matching existing mergePhasifyToTickets pattern.
4. Extract offset/mergedPhaseIds constants from metadata at function entry for readability.

## Test Plan

### Unit Tests

- UT: [Normal] parseArguments() parses --rollback flag as opts.rollback === true
- UT: [Normal] rollbackPhasifyMerge() removes phases with id >= offset from Tickets.json clone
- UT: [Normal] rollbackPhasifyMerge() removes metadata.phasifyMerge from clone
- UT: [Normal] rollbackPhasifyMerge() does not mutate original input (deep clone test)
- UT: [Error] rollbackPhasifyMerge() throws Error when metadata.phasifyMerge is missing
- UT: [Error] rollbackPhasifyMerge() throws Error when offset removes ALL phases (integrity check: offset=-2 removes PX and everything)
- UT: [Normal] rollbackPhasifyMerge() preserves phases with id < offset (offset=0 keeps PX(id=-1))
- UT: [Normal] --rollback --dry-run prints rollback target phases and exits without writing files
- UT: [Edge] --rollback on Tickets.json with no merged phases (empty mergedPhaseIds) exits cleanly with message
- UT: [Invariant] After rollback, remaining phases list is sorted by id ascending
- UT: [Invariant] After rollback, remaining tickets have phaseId values matching existing phases only

### Integration Tests

- IT: Full round-trip: phasify-omissions (merge) -> --rollback. Before/after SHA of Tickets.json must match. Use temp copy of Tickets.json.
- IT: --rollback --dry-run: verify stdout shows target phases, file SHA unchanged

### Exceptions

- Cannot test: Atomic rename race during rollback. Same as PX-108 — fs.renameSync is POSIX atomic. Not a design defect.
- Cannot test: Multiple concurrent --rollback calls. Single-process design; second call will fail on missing metadata. Not a design defect.

### Plan Test Code (concrete code)

- // C001-PRE: parseArguments parses --rollback
- const args1 = ['--graph=RFC-ROOT-GRAPH.json', '--rollback'];
- const opts1 = parseArguments(args1);
- assert.strictEqual(opts1.rollback, true, 'rollback flag set');
- 
- // C001-PRE: --rollback coexists with --dry-run
- const args2 = ['--graph=g.json', '--rollback', '--dry-run'];
- const opts2 = parseArguments(args2);
- assert.strictEqual(opts2.rollback, true, 'rollback with dry-run');
- assert.strictEqual(opts2.dryRun, true, 'dry-run with rollback');
- 
- // C001-INV: no --rollback means opts.rollback is false
- const args3 = ['--graph=g.json'];
- const opts3 = parseArguments(args3);
- assert.strictEqual(opts3.rollback, false, 'default rollback is false');
- 
- // C002-PRE: rollbackPhasifyMerge removes offset phases
- const inputData = {
-   title: 'test',
-   metadata: { source: 's', generatedAt: '2026-07-30', phasifyMerge: { offset: 6, mergedPhaseIds: [6,7], mergedAt: '2026-07-30' } },
-   phases: [
-     { id: -1, name: 'PX', tickets: [{ id: 1, phaseId: -1, title: 'X', status: 'todo' }] },
-     { id: 0, name: 'P0', tickets: [{ id: 1, phaseId: 0, title: 'A', status: 'done' }] },
-     { id: 6, name: 'P6', tickets: [{ id: 1, phaseId: 6, title: 'Merged', status: 'todo' }] },
-     { id: 7, name: 'P7', tickets: [{ id: 1, phaseId: 7, title: 'Merged2', status: 'todo' }] }
-   ]
- };
- const result = rollbackPhasifyMerge(inputData);
- assert.strictEqual(result.phases.length, 2, '2 remaining phases');
- assert.strictEqual(result.phases[0].id, -1, 'PX preserved');
- assert.strictEqual(result.phases[1].id, 0, 'P0 preserved');
- 
- // C002-POST: metadata.phasifyMerge removed
- assert.strictEqual(result.metadata.phasifyMerge, undefined, 'phasifyMerge removed from metadata');
- 
- // C002-INV: original not mutated
- assert.strictEqual(inputData.phases.length, 4, 'original input unchanged');
- 
- // C002-ERR: throws on missing metadata
- const noMeta = { title: 't', metadata: { source: 's', generatedAt: '2026-07-30' }, phases: [] };
- assert.throws(() => rollbackPhasifyMerge(noMeta), /phasifyMerge/, 'throws on missing metadata');
- 
- // C002-ERR: throws on invalid offset (overlaps existing phases < offset)
- // This test: offset=0 overlaps with existing phase id=0
- const badOffset = {
-   title: 't',
-   metadata: { source: 's', generatedAt: '2026-07-30', phasifyMerge: { offset: 0, mergedPhaseIds: [0,1], mergedAt: '2026-07-30' } },
-   phases: [{ id: -1, name: 'PX', tickets: [] }, { id: 0, name: 'P0', tickets: [{ id: 1, phaseId: 0, title: 'A', status: 'todo' }] }]
- };
- assert.throws(() => rollbackPhasifyMerge(badOffset), /offset/, 'throws on overlapping offset');
- 
- // C003-PRE: --rollback --dry-run prints preview (test via stdout capture)
- // Tested by running runPhasifyOmissions with rollback+dryRun opts
- // In UT: verify rollbackPhasifyMerge result has expected removed phases list
- // The dry-run printing is handled by the caller

## Changes

| Before | After | Description |
|--------|-------|-------------|
| runPhasifyOmissions() had no rollback capability | --rollback removes merged phases by offset from metadata.phasifyMerge | Added rollbackPhasifyMerge() pure function, --rollback CLI flag, merge metadata injection |
| Tickets.json metadata had no merge tracking | metadata.phasifyMerge written on every merge with offset/mergedPhaseIds/mergedAt | Enables deterministic rollback via stored metadata |

## Notes

- Implementation steps: (1) metadata injection in merge pipeline (2) --rollback CLI parse (3) rollbackPhasifyMerge() pure function (4) runPhasifyOmissions rollback path (5) dry-run support (6) tests
- Risks: None — pure function + atomic write guarantees safety
- Caveats: Must NOT remove backup file (tmp-Tickets-*.json) as part of rollback; backup is user's safety net
- Open items: None
- Future: Consider --rollback-to=<offset> for partial rollback (not needed now)
Implementation summary:
- Changed files: phasify-omissions.js (+rollbackPhasifyMerge, +CLI, +metadata injection, ~+100 lines), scripts.test.js (PX-109 tests, +55 lines)
- Key changes: rollbackPhasifyMerge() pure function; --rollback flag; merge metadata injection; rollback path
- Test results: 188/188 passed (12 PX-109-specific tests)
Review report:
- Static quality check: 53 issues (all pre-existing false positives)
- Translatability: no issues
- Crimes: 0
- Stubs: 3 (all pre-existing, correctly deferred)
- Annotations: verified OK (0 missing, 0 ambiguous)
- Contracts: 100% fulfilled (3/3)
- Tests: 188/188 PASS
- Acceptance Criteria: merge metadata injection, --rollback removal, dry-run preview all implemented

## PX-109 — implemented at 4 locations

### .claude/scripts/rfc-graph/phasify-omissions.js

- Line 97
```javascript
function parseArguments(argv) {
```

- Line 815
```javascript
function rollbackPhasifyMerge(ticketsData) {
```

- Line 863
```javascript
function runPhasifyOmissions(opts) {
```

### .claude/tests/tickets/scripts.test.js

- Line 26
```javascript
  if (value) { passed++; process.stdout.write(`  ✓ ${message}\n`); }
```
