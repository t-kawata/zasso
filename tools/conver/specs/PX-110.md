# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-110: rename-phases.js + phasify-omissions phase info output

**Ticket Key**: PX-110 · **Phase**: -1

---

## Background

Goal: phasify-omissions.js の出力に各フェーズのグラフノード情報を含め、AIが意味のあるフェーズ名を決定できるようにする。同時にTickets.jsonのphase.nameを安全に変更する専用スクリプトrename-phases.jsを提供する。

Purpose: 現在のフェーズ名（P6, P7等）は内容を一切表さない。人間にもAIにも理解できない。フェーズ内容を伝える情報を出力し、安全な改名経路を確立する。

Motivation: PX-108/PX-109でマージとロールバックが自動化された。次のステップとして、生成されたフェーズに意味のある名前を付ける必要がある。直接編集はJSON破損のリスクがあり許可しない。

Constraints:
- rename-phases.js がフェーズ名変更の唯一の変更経路
- phasify-omissions.js は名前を自動生成せず、情報提示のみ
- --dry-run, --rollback と矛盾しない

## Scope

- rename-phases.js 新規作成: --tickets=<PATH> --phase=<id> --name=<new-name> 形式のCLI。複数phaseの一括指定。atomicWrite。
- phasify-omissions.js 出力拡張: マージ後、各フェーズの nodeIds に対応するグラフノードタイトル一覧をstdoutに出力。
- --dry-run 時もフェーズ情報を表示（ファイル書き込みなしのため安全）。
- --rollback 非対応: rename-phases はマージ後のフェーズ名変更のみ。rollback とは独立。
- 全テスト追加: rename-phases.js のUT + phasify-omissions.js 出力フォーマットのUT
- Scope:
  - Path: .claude/scripts/tickets/rename-phases.js (NEW)
  - Action: add (new script)
  - Detail: CLI tool for renaming phases in Tickets.json
  - Before: nonexistent. After: rename-phases.js created with renamePhases() + atomicWrite
  - API: CLI: --tickets, --phase, --name (repeatable)
  - Schema: No change
  - Config: No change
  - Dep: No new dependencies
- Out of scope:
  - Item: phasify-omissions.js auto-naming of phases
  - Why: AI/人間がrename-phases.jsで命名する設計。自動生成は不正確。
- Affected:
  - Component: Tickets.json.phases[].name
  - Impact: Data content — phase name changes
  - Response: N/A

## Investigation

Key code patterns:

1. Current merge report output (runPhasifyOmissions):
   console.log('Schema validation: ...');
   console.log('Tickets.json: merged N phase(s), M ticket(s)');
   → Insert phase detail block before schema validation

2. graphData.nodes[] is already loaded and available at merge time. Each node has {id, title, kind, ...}.

3. output.phases[] has {id, name, nodeIds[], tickets[{id, title, phaseId}]}.
   → Build nodeTitleMap from graphData.nodes.
   → For each phase: iterate nodeIds, look up title from map, print.
   → For rename command: `node .claude/scripts/tickets/rename-phases.js --tickets=<path> --phase=<id> --name="<name>"`

4. rename-phases.js: new script in .claude/scripts/tickets/. Reuses atomicWrite pattern from merge-contracts-to-tickets.js (lines 38-42).
   - parse arguments: pairs of --phase=N --name="..."
   - read Tickets.json
   - clone
   - update names
   - clone and validate no other fields changed
   - atomicWrite

## Acceptance Criteria

- Happy path: phasify-omissions 実行後、stdoutに各フェーズのノード一覧（nodeId, title）とrename-phases.jsのコマンド例が出力される。コマンドを実行するとTickets.jsonのphase.nameのみが変わる。
- Error case: rename-phases.jsに存在しないphase IDを渡すとエラー終了。Tickets.jsonは変更されない。空文字のnameもエラー。
- Edge case: --dry-runでもフェーズ情報が出力される（ファイル書き込みなし）。--rollback後でもrename-phases.jsは正常動作する（rollbackと独立）。

## Invariants

- [Normal condition] ticketsData must be a valid Tickets.json with phases array. Each rename entry must have phaseId (integer) and newName (non-empty string).
- [Error invariant] rename-phases.js never writes to Tickets.json if any phaseId is invalid. Tickets.json is never partially modified.
- [Internal state invariant] renamePhases() is a pure function (no I/O). Only atomicWrite (caller) touches disk.
- [Boundary invariant] Phase id=-1 (PX) is always rejected for renaming.

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — rename-phases.js CLI args → Tickets.json phase rename

- **Precondition**: renamePhases() accepts (ticketsData, renames: {phaseId, newName}[]). Each phaseId must exist in ticketsData.phases. Phase with id=-1 (PX) is rejected.
- **Postcondition**: Deep-clones ticketsData, updates each matching phase.name, returns clone. Throws Error on invalid phaseId or invalid name (empty string).
- **Invariant**: Only phases[].name is modified. All other fields (id, characteristics, tickets, nodeIds, summary) are deep-equal before/after.

### C002 — phasify-omissions merge report → stdout with node titles

- **Precondition**: runPhasifyOmissions() has graphData.nodes[] with {id, title}. output.phases[] has {id, nodeIds[], tickets[]}.
- **Postcondition**: stdout contains each phase's nodeId-title mapping and ticketId-title list, plus the rename-phases.js command example. Non-dry-run and dry-run both output this info.
- **Invariant**: Output is informational only — no files written or modified by the output itself.

## Boy Scout Rule

1. renamePhases() as a pure function (no I/O) following mergePhasifyToTickets pattern.
2. Verb-prefix naming: renamePhases (verb: 'rename').
3. Deep clone inputs to avoid mutation.
4. Command example output is constructed from runtime values (not hardcoded).

## Test Plan

### Unit Tests

- UT: [Normal] renamePhases() updates phase.name for valid phaseId
- UT: [Normal] renamePhases() updates multiple phases in one call
- UT: [Normal] renamePhases() deep-clones input — no mutation of original
- UT: [Normal] renamePhases() with name identical to existing name does not change anything
- UT: [Error] renamePhases() throws on nonexistent phaseId
- UT: [Error] renamePhases() throws on empty name string
- UT: [Error] renamePhases() throws on phaseId=-1 (PX phase)
- UT: [Invariant] renamePhases() only modifies phases[].name — deep-equal for all other fields
- UT: [Normal] phasify-omissions merge report contains node title mapping for each phase
- UT: [Normal] phasify-omissions merge report ends with rename-phases.js command example
- UT: [Normal] --dry-run also outputs phase info and rename command example

### Integration Tests

- IT: phasify-omissions (merge) → capture stdout → parse rename command → execute → verify Tickets.json phase.name changed
- IT: rename-phases.js with 2 phases in one call → both renamed, atomic write

### Exceptions

- Cannot test: atomicWrite's fs.renameSync POSIX atomicity. UT verifies file existence before/after rename. Not a design defect.

### Plan Test Code (concrete code)

- // C001-PRE: renamePhases() updates phase.name
- const input = {
-   title: 'test',
-   metadata: { source: 's', generatedAt: '2026-07-30' },
-   phases: [
-     { id: 6, name: 'P6', tickets: [] },
-     { id: 7, name: 'P7', tickets: [] }
-   ]
- };
- const result = renamePhases(input, [{ phaseId: 6, newName: 'Storage Layer' }]);
- assert.strictEqual(result.phases[0].name, 'Storage Layer', 'name updated');
- 
- // C001-POST: deep clone, original unchanged
- assert.strictEqual(input.phases[0].name, 'P6', 'original unchanged');
- 
- // C001-INV: only name field changed
- assert.strictEqual(result.phases[0].id, input.phases[0].id, 'id preserved');
- 
- // C001-ERR: nonexistent phaseId throws
- assert.throws(() => renamePhases(input, [{ phaseId: 99, newName: 'X' }]), /phaseId/, 'throws on bad id');
- 
- // C001-ERR: empty name throws
- assert.throws(() => renamePhases(input, [{ phaseId: 6, newName: '' }]), /name/, 'throws on empty name');
- 
- // C001-ERR: phaseId=-1 (PX) throws
- const pxPhases = { title: 't', metadata: { source: 's', generatedAt: '2026-07-30' }, phases: [{ id: -1, name: 'PX', tickets: [] }, { id: 0, name: 'P0', tickets: [] }] };
- assert.throws(() => renamePhases(pxPhases, [{ phaseId: -1, newName: 'X' }]), /PX/, 'throws on PX rename');
- 
- // C001: multiple phases in one call
- const multiResult = renamePhases(input, [{ phaseId: 6, newName: 'A' }, { phaseId: 7, newName: 'B' }]);
- assert.strictEqual(multiResult.phases[0].name, 'A', 'multi: phase 6');
- assert.strictEqual(multiResult.phases[1].name, 'B', 'multi: phase 7');
- 
- // C001: rename to same name (no-op, no throw)
- const sameResult = renamePhases(input, [{ phaseId: 6, newName: 'P6' }]);
- assert.strictEqual(sameResult.phases[0].name, 'P6', 'same name preserved');

## Notes

- Implementation steps: (1) renamePhases() pure function in rename-phases.js (2) CLI arg parsing (3) atomic write integration (4) phasify-omissions output extension (5) tests
- Risks: None — pure function + atomic write
- Caveats: rename-phases.js outputs the rename command for the NEXT phase, not for itself. It does not output a 'run another command' message.
- Open items: None
- Future: rename-phases.js could accept --json for pipeline integration (not needed now)

## PX-110 — 0 locations, not implemented

Not found
