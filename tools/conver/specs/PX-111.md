# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-111: phasify-omissions pre-merge snapshot + auto-review existing tickets

**Ticket Key**: PX-111 · **Phase**: -1

---

## Background

Goal: phasify-omissions.js のマージ時に Tickets.json のスナップショット（Tickets-YYYYMMDDhhmmss.json）を永続保存し、全既存チケットを reviewed に一括変換する。

Purpose: (1) マージ前後の状態比較を可能にする (2) conver.js が omission チケット（todo）のみを処理対象とできるようにする。

Motivation: PX-110 までの phasify-omissions はマージのみ。既存チケット状態の保全と、conver.js の開始点を明確にする仕組みが欠けている。

Constraints:
- スナップショットは cleanupFiles で削除しない
- 既に reviewed/done のチケットは変更しない（べき等）
- スナップショット作成失敗は全体を止めない（ベストエフォート）

## Scope

- スナップショット: Tickets-{ts}.json 作成。cleanupFiles 対象外。
- 既存チケット一括 reviewed: offset未満の全チケットを reviewed に。reviewed/done はスキップ。
- dry-run 非対応（マージ時のみ意味を持つ）。
- rollback 非対応（スナップショットは残る）。
- 全テスト追加。
- Scope:
  - Path: .claude/scripts/rfc-graph/phasify-omissions.js
  - Action: modify (add 2 new functions, extend runPhasifyOmissions())
  - Detail: markPreMergeTicketsReviewed() + createSnapshot() + Step O integration
  - Before: atomicWrite直後→cleanup. After: スナップショット→reviewed変換→atomicWrite→cleanup
  - API: No breaking changes. 2 new exported functions.
  - Schema: No change.
  - Config: No change.
  - Dep: No new dependencies.
- Out of scope:
  - Item: --no-snapshot flag
  - Why: Future ticket if needed
- Affected:
  - Component: Tickets.json content (status fields)
  - Impact: Pre-merge tickets status changed to reviewed
  - Response: N/A

## Investigation

Key code patterns:

1. Current pipeline (runPhasifyOmissions):
   - Step P: mergedResult.data.metadata.phasifyMerge = {...} (PX-109)
   → Insert snapshot + markReviewed BEFORE metadata injection

2. backupTickets() (PX-108) already does file copy — reuse for snapshot
   → Different target pattern: 'Tickets-' + ts + '.json' (not 'tmp-Tickets-' + ts + '.json')
   → Exclude from cleanupFiles

3. markPreMergeTicketsReviewed() is a pure function operating on memory data
   → Must be called before atomicWrite
   → Must NOT touch tickets in phases with id >= offset

## Acceptance Criteria

- Happy path: phasify-omissions 実行後、Tickets-{ts}.json が作成される。全既存チケットが reviewed に。omission チケットのみ todo。
- Error case: スナップショット作成失敗 → warning 出力。atomicWrite は継続。
- Edge case: 再実行でも既存チケットは reviewed のまま（べき等）。スナップショットは別名で追加作成。

## Invariants

- [Normal condition] mergedData must have phases array. offset must be integer.
- [Error invariant] Snapshot failure never blocks Tickets.json atomic write.
- [Internal state invariant] markPreMergeTicketsReviewed is pure function (no I/O).
- [Boundary invariant] Phase id=-1 (PX) is included in pre-merge processing (offset starts from 0 or higher).

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — markPreMergeTicketsReviewed(mergedData, offset) → reviewed status on pre-offset tickets

- **Precondition**: Accepts (mergedData: {phases: [{id, tickets}]}, offset: number). mergedData must have phases array. offset must be integer > existing phase IDs.
- **Postcondition**: Returns deep-cloned mergedData where all tickets in phases with id < offset have status='reviewed' (unless already 'reviewed' or 'done'). Tickets in phases with id >= offset are untouched.
- **Invariant**: No mutation of input. Only tickets[].status field is modified — all other fields (id, title, phaseId, etc.) are deep-equal.

### C002 — Snapshot backup → Tickets-{ts}.json

- **Precondition**: createSnapshot(sourcePath, ts) accepts (sourcePath: string, ts: string). sourcePath must be readable JSON file.
- **Postcondition**: Creates Tickets-{ts}.json as byte-for-byte copy of sourcePath. Does NOT throw on failure (best-effort). Returns {success, snapshotPath} or {success: false}.
- **Invariant**: Source file (Tickets.json) is never modified by snapshot creation.

## Boy Scout Rule

1. markPreMergeTicketsReviewed() as pure function.
2. Snapshot via backupTickets() reuse — minimal new code.
3. Verb-prefix naming: markPreMergeTicketsReviewed, createSnapshot.
4. Best-effort snapshot: wrap in try-catch, log warning on failure.

## Test Plan

### Unit Tests

- UT: [Normal] markPreMergeTicketsReviewed() sets status=reviewed for tickets in phases with id < offset — pre-offset tickets have their status changed
- UT: [Normal] Already reviewed/done tickets are not changed (idempotent) — status already reviewed or done is preserved
- UT: [Normal] Tickets in phases with id >= offset (new omission tickets) are not touched — only pre-offset phases are affected
- UT: [Normal] markPreMergeTicketsReviewed() deep-clones — original input is not mutated, all fields besides status are unchanged
- UT: [Normal] createSnapshot() copies source to Tickets-{ts}.json — byte-for-byte identical
- UT: [Edge] createSnapshot() returns {success: false} on failure when source not found (best-effort, no throw)
- UT: [Invariant] Only tickets[].status field is modified by markPreMergeTicketsReviewed — all other fields (id, title, phaseId) are deep-equal before and after

### Integration Tests

- IT: phasify-omissions run → verify Tickets-{ts}.json exists + all pre-offset tickets are reviewed

### Exceptions

- Cannot test: Snapshot disk full. UT verifies copy-on-write logic. Not a design defect.

### Plan Test Code (concrete code)

- // C001-PRE: markPreMergeTicketsReviewed with valid data
- const input = {
-   title: 'test', metadata: { source: 's', generatedAt: '2026-07-30' },
-   phases: [
-     { id: 0, name: 'P0', tickets: [{ id: 1, phaseId: 0, title: 'A', status: 'todo' }] },
-     { id: 1, name: 'P1', tickets: [{ id: 1, phaseId: 1, title: 'B', status: 'done' }] },
-     { id: 6, name: 'P6', tickets: [{ id: 1, phaseId: 6, title: 'New', status: 'todo' }] }
-   ]
- };
- const result = markPreMergeTicketsReviewed(input, 6);
- assert.strictEqual(result.phases[0].tickets[0].status, 'reviewed', 'pre-offset todo -> reviewed');
- 
- // C001-POST: already done unchanged
- assert.strictEqual(result.phases[1].tickets[0].status, 'done', 'done preserved');
- 
- // C001-POST: new omission tickets unchanged
- assert.strictEqual(result.phases[2].tickets[0].status, 'todo', 'offset+ tickets untouched');
- 
- // C001-INV: deep clone, original unchanged
- assert.strictEqual(input.phases[0].tickets[0].status, 'todo', 'original unchanged');
- 
- // C001-INV: only status field modified
- assert.strictEqual(result.phases[0].tickets[0].id, 1, 'id preserved');
- assert.strictEqual(result.phases[0].tickets[0].phaseId, 0, 'phaseId preserved');
- 
- // C002-PRE: createSnapshot creates Tickets-{ts}.json
- const snapResult = createSnapshot('Tickets.json', '20260730120000');
- assert.ok(snapResult.success, 'snapshot created');
- assert.ok(fs.existsSync('Tickets-20260730120000.json'), 'snapshot file exists');
- fs.unlinkSync('Tickets-20260730120000.json');
- 
- // C002-ERR: createSnapshot returns {success: false} on failure
- const failResult = createSnapshot('/nonexistent/path.json', 'test');
- assert.strictEqual(failResult.success, false, 'snapshot fails gracefully');

## Notes

- Implementation steps: (1) markPreMergeTicketsReviewed() pure function (2) createSnapshot() via backupTickets (3) integrate into runPhasifyOmissions Step O (4) tests
- Risks: Snapshot is best-effort; Tickets.json integrity is never at risk
- Caveats: Large Tickets.json (1.3MB) → snapshot copy doubles I/O briefly
- Open items: None
- Future: --no-snapshot flag to skip snapshot (not needed now)

## PX-111 — 0 locations, not implemented

Not found
