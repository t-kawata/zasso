# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-112: phasify-omissions move artifacts to omissions/ and tickets/ directories

**Ticket Key**: PX-112 · **Phase**: -1

---

## Background

Goal: phasify-omissions.js が生成する OMISSIONS-phasified-*.json を omissions/ に、Tickets-*.json（スナップショット）を tickets/ に移動する。

Purpose: カレントディレクトリに散らばる中間生成物を整理し、プロジェクトルートを清潔に保つ。

Motivation: PX-111 までで phasified 出力・Tickets スナップショットが増加。ルートが散らかる。

Constraints:
- 移動は atomicWrite 成功後のみ
- ディレクトリなければ自動作成（recursive mkdir）
- ベストエフォート、失敗時 console.warn
- tmp-Tickets-*.json（rollback backup）は削除

## Scope

- Step Q 書き換え: cleanupFiles を moveArtifacts() に置換。phasified→omissions/ + Tickets-*→tickets/ + tmp-Tickets-*削除。
- omissions/ ディレクトリ自動作成。OMISSIONS-phasified-{ts}.json を移動。
- tickets/ ディレクトリ自動作成。Tickets-{ts}.json（スナップショット）を移動。
- ベストエフォート: try-catch + console.warn。
- テスト: ディレクトリ作成 + ファイル移動のUT。
- Scope:
  - Path: .claude/scripts/rfc-graph/phasify-omissions.js
  - Action: modify (add moveArtifacts(), replace Step Q cleanup)
  - Detail: Replace cleanupFiles call with moveArtifacts that moves artifacts to subdirs
  - Before: cleanupFiles deletes OMISSIONS-phasified and tmp-Tickets. Tickets-* stays in CWD.
  - After: phasified→omissions/, Tickets-*→tickets/, tmp-Tickets deleted
  - API: No breaking changes. New exported function moveArtifacts().
  - Schema: No change.
  - Config: No change.
  - Dep: No new dependencies.
- Out of scope:
  - Item: --keep-artifacts flag
  - Why: Future ticket if needed
- Affected:
  - Component: Artifact file locations
  - Impact: Files move from CWD to subdirs
  - Response: N/A

## Investigation

Key code patterns:

1. Current Step Q (cleanup phase):
   var filesToClean = [phasifiedOutputPath, backupPath];
   cleanupFiles(filesToClean);
   → Replace with: moveArtifacts()

2. moveArtifacts() logic:
   - mkdirSync(omissions, {recursive: true})
   - if (phasifiedOutputPath exists) renameSync(phasifiedOutputPath, omissions/)
   - mkdirSync(tickets, {recursive: true})
   - if (snapshotPath exists) renameSync(snapshotPath, tickets/)
   - if (backupPath exists) unlinkSync(backupPath)
   - All wrapped in try-catch with console.warn

3. fs.renameSync is atomic on same filesystem — src/dst share FS

4. backupPath is tmp-Tickets-{ts}.json — still deleted (not moved)

## Acceptance Criteria

- Happy path: phasify-omissions 実行後、OMISSIONS-phasified-*.json → omissions/、Tickets-*.json → tickets/。tmp-Tickets-*.json 削除。カレントに残らない。
- Error case: 移動失敗時も処理継続、console.warn 出力。
- Edge case: ディレクトリ既存時も正常動作。存在しないファイルの移動試行は警告なくスキップ。

## Invariants

- [Normal condition] Source files must exist before rename. Target dir auto-created.
- [Error invariant] Rename failure never affects Tickets.json or halts pipeline.
- [Internal state invariant] moveArtifacts is pure I/O (no memory side effects).
- [Boundary invariant] Non-existent source files silently skipped (already moved or not created).

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — moveArtifacts() → artifacts moved to subdirectories

- **Precondition**: moveArtifacts() accepts (opts, phasifiedOutputPath, snapshotPath). opts.ticketsPath exists and writable. phasifiedOutputPath and snapshotPath are absolute file paths or null.
- **Postcondition**: Creates omissions/ and tickets/ dirs (if missing). Renames phasified file to omissions/. Renames snapshot to tickets/. Deletes tmp-Tickets-*.json. Returns void. Never throws — failures are logged to console.warn.
- **Invariant**: Tickets.json and its atomic write are complete before moveArtifacts is called. Artifact moves never affect Tickets.json.

## Boy Scout Rule

1. moveArtifacts() as a thin I/O function with best-effort semantics.
2. Verb-prefix: moveArtifacts.
3. All I/O failures are warnings, never crashes.
4. mkdir recursive ensures no pre-requisite setup.
5. Extracts hardcoded directory names to consts.

## Test Plan

### Unit Tests

- UT: [Normal] moveArtifacts() moves OMISSIONS-phasified-*.json to omissions/ dir
- UT: [Normal] moveArtifacts() moves Tickets-*.json to tickets/ dir
- UT: [Normal] moveArtifacts() creates target dirs if they do not exist
- UT: [Edge] moveArtifacts() does not fail when dirs already exist (idempotent mkdir)
- UT: [Edge] moveArtifacts() does not fail when source file is missing (already moved or skipped)
- UT: [Edge] moveArtifacts() with null phasifiedPath (dry-run) does nothing

### Integration Tests

- IT: phasify-omissions run → verify OMISSIONS-phasified in omissions/, Tickets-* in tickets/

### Exceptions

- Cannot test: Disk full during rename. Not a design defect.

### Plan Test Code (concrete code)

- // C001-PRE: moveArtifacts moves phasified file to omissions/
- const baseDir = fs.mkdtempSync('/tmp/px112-test-');
- const srcPhasified = path.join(baseDir, 'OMISSIONS-phasified-20260730.json');
- fs.writeFileSync(srcPhasified, '{}');
- const opts = { ticketsPath: path.join(baseDir, 'Tickets.json') };
- fs.writeFileSync(opts.ticketsPath, '{}');
- moveArtifacts(opts, srcPhasified, null);
- assert(fs.existsSync(path.join(baseDir, 'omissions', 'OMISSIONS-phasified-20260730.json')), 'phasified moved to omissions/');
- assert.strictEqual(fs.existsSync(srcPhasified), false, 'original phasified removed');
- 
- // C001-PRE: moveArtifacts moves snapshot to tickets/
- const srcSnapshot = path.join(baseDir, 'Tickets-20260730.json');
- fs.writeFileSync(srcSnapshot, '{}');
- moveArtifacts(opts, srcPhasified, srcSnapshot);
- assert(fs.existsSync(path.join(baseDir, 'tickets', 'Tickets-20260730.json')), 'snapshot moved to tickets/');
- 
- // C001-EDGE: dirs already exist (idempotent)
- moveArtifacts(opts, null, null); // should not throw
- 
- // C001-EDGE: missing source file silently skipped
- moveArtifacts(opts, '/nonexistent/path.json', null); // should not throw
- 
- // cleanup
- fs.rmSync(baseDir, { recursive: true, force: true });

## Notes

- Implementation: (1) moveArtifacts() function (2) replace Step Q (3) tests
- Risks: None — atomicWrite complete before move
- Caveats: renameSync fails if src/dst on different filesystems. Always same FS within project.
- Open items: None
- Future: --keep-artifacts flag to skip move (not needed)

## PX-112 — 0 locations, not implemented

Not found
