# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-117: conver.js find 後の Slack 通知を list-phases-and-tickets.js 結果＋統合成否へ変更

**Ticket Key**: PX-117 · **Phase**: -1

---

## Background

現状、find 実行完了後は sendOmissionsNotification が生の OMISSIONS-*.json を Slack 通知するが、list-phases-and-tickets.js の結果（sendSlackSuccess、find 前）が既に飛んでおり OMISSIONS の二重通知は冗長。提案（受け入れ済み）: sendOmissionsNotification を廃止し、find 後に list-phases-and-tickets.js の結果（phasify 統合後の Tickets.json を反映）を送信する。さらに通知には『Tickets.json への統合まで全て成功したか、どこで失敗したか』を載せる。

### Goal
find 完了後の Slack 通知を、冗長な OMISSIONS 通知から『list-phases-and-tickets.js の結果＋統合成否』へ置き換える。

### Purpose
sendOmissionsNotification は find 前の sendSlackSuccess（list-phases-and-tickets.js 結果）と重複する冗長通知。さらに統合成否が通知に載らないため、find → phasify-omissions.js → Tickets.json 統合の成否を Slack 上で一瞥できない。

### Motivation
パイプラインの実行状態（特に find の統合成否）を Slack で確認できるようにするため。

### Constraints
- find 工程そのもの（.claude/commands/find-omissions.md・phasify-omissions.js）は変更しない。
- 通知送信は best-effort（失敗時もループを停止しない）。
- 統合成否の判定は決定的で副作用がないこと。

## Scope

- src/notifier.ts: sendOmissionsNotification を廃止し、find 後通知関数 sendFindOutcomeNotification(webhookUrl, FindOutcomeContext) を追加
- src/runner.ts: find 完了後の通知を sendFindOutcomeNotification に置換。buildProgressText の再実行と統合成否判定を組み込む
- src/tickets.ts: 統合成否判定用の決定的ヘルパー countPhasesAndTickets(ticketsPath) を追加（副作用なし・読み取り専用）
- src/runner.test.ts: find 後通知のテスト更新（sendOmissionsNotification が呼ばれない・新関数が progress＋統合成否で呼ばれる）
- src/notifier.test.ts: sendFindOutcomeNotification のペイロードテスト追加
- src/tickets.test.ts: countPhasesAndTickets のテスト追加
- 非変更: .claude/commands/find-omissions.md・phasify-omissions.js（find 工程そのものは変更しない）
- **Scope of changes:** [src/notifier.ts] modify — sendOmissionsNotification 削除、sendFindOutcomeNotification 追加
- **Action:** remove + add — 旧通知関数削除、新通知関数と FindOutcomeContext 型を追加
- **Detail:** FindOutcomeContext{progress: string, integrationSucceeded: boolean, mergedPhases: number, mergedTickets: number}。runner が find 後に countPhasesAndTickets で前後差分を判定し、buildProgressText の結果と共に送信
- **Before → After:** sendOmissionsNotification(webhookUrl, cwd) → sendFindOutcomeNotification(webhookUrl, FindOutcomeContext)。runner の find 後ブロックが再構築
- **API contract change:** notifier.ts の公開関数の変更（sendOmissionsNotification 削除・sendFindOutcomeNotification 追加）。利用者は runner.ts とテストのみ
- **Data schema change:** なし（Tickets.json の読み取りのみ）
- **Config/env change:** なし
- **Dependency added/removed:** なし
- **Out of scope:** find 工程（find-omissions.md・phasify-omissions.js）・sendSlackSuccess・sendSlackError の変更
- **Why excluded:** 冗長なのは find 後通知のみ。find 工程は現行仕様どおり動作させ、成功/失敗通知の構造は維持する
- **Affected areas:** conver.js の find 完了後通知
- **Nature:** 挙動修正（通知内容・タイミング）。外部 API・データ形式・性能への影響なし
- **Corresponding change:** N — find 工程・sendSlackSuccess/sendSlackError は変更不要

## Implementation Target Files

- `src/notifier.ts`
- `src/runner.ts`
- `src/tickets.ts`
- `src/runner.test.ts`
- `src/notifier.test.ts`
- `src/tickets.test.ts`

## Investigation

■ コード調査の証跡
- src/runner.ts:308-319 — sendSlackSuccess が find 前に buildProgressText（list-phases-and-tickets.js）を含めて送信（count/processed/progress）
- src/runner.ts:341 — sendOmissionsNotification が find 後に cwd の OMISSIONS-*.json を送信（冗長）
- src/notifier.ts:296-329 — sendOmissionsNotification は readdirSync(cwd) で OMISSIONS-*.json をスキャンし内容を送信
- src/notifier.ts:206-217 — sendSlackSuccess は SuccessContext{count, processed, progress} を送信
- src/runner.ts:170-185 — buildProgressText は list-phases-and-tickets.js を execSync で実行、失敗時はフォールバック文言を返す（try/catch 済み）
- .claude/commands/find-omissions.md:298-300 — Step 8 で phasify-omissions.js --graph="$ARGUMENTS" が OMISSIONS を Tickets.json に統合
- src/tickets.ts — countPhasesAndTickets は未実装（新規追加）

## Acceptance Criteria

- Happy path: find 完了後、list-phases-and-tickets.js の結果と『統合成功（+N フェーズ）』を含む通知が飛ぶ
- Error case: Tickets.json 統合が発生しなかった場合（フェーズ不変）は『統合なし/失敗』を通知に載せる
- Edge case: list-phases-and-tickets.js 実行失敗時もフォールバック文言で通知は送信される
- **[Happy path]** checkAllReviewed=true・noFind=false で find 完了 → sendFindOutcomeNotification が progress（list-phases-and-tickets.js 出力）と integrationSucceeded=true で呼ばれる
- **[Error case]** find 後もフェーズ/チケット数が不変 → integrationSucceeded=false として『統合なし/失敗』を含む通知が送信される
- **[Edge case]** list-phases-and-tickets.js 不在で buildProgressText がフォールバック文言を返しても、通知は送出されループは停止しない

## Invariants

- [Normal condition] find 完了後は必ず新通知関数 sendFindOutcomeNotification が呼ばれ、sendOmissionsNotification は呼ばれない。
- [Error invariant] 通知送信失敗・progress 生成失敗時もループは停止しない（best-effort・フォールバック文言）。
- [Internal state invariant] countPhasesAndTickets は読み取り専用で副作用を持たない（Tickets.json を書き換えない）。
- [Boundary invariant] 統合成否判定はフェーズ/チケット数比較で決定的（増加 → 成功、不変 → 統合なし/失敗）。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — runner.ts — find 後通知

- **Precondition**: checkAllReviewed(ticketsPath) === true かつ noFind !== true かつ /find-omissions が完了
- **Postcondition**: runCommand 完了後、sendOmissionsNotification は呼ばれない
- **Invariant**: find 後通知は best-effort（catch でループを止めない）

### C002 — runner.ts — find 後通知ペイロード

- **Precondition**: find 完了後
- **Postcondition**: 通知ペイロードの progress は buildProgressText（list-phases-and-tickets.js）の出力
- **Invariant**: 生成失敗時はフォールバック文言

### C003 — runner.ts/tickets.ts — 統合成否判定

- **Precondition**: find 前後の Tickets.json を比較可能
- **Postcondition**: フェーズ/チケット数が増加していれば統合成功、不変なら統合なし/失敗として通知
- **Invariant**: 成功/失敗のいずれかの状態を必ず含む

### C004 — tickets.ts — countPhasesAndTickets

- **Precondition**: Tickets.json が読み取り可能
- **Postcondition**: { phaseCount, ticketCount } を返す
- **Invariant**: 副作用なし（読み取り専用）

## Boy Scout Rule

- src/runner.ts: find 後通知の処理（スナップショット・判定・送信）を独立関数 notifyFindOutcome() として抽出し、runLoop を読みやすく保つ。
- src/notifier.ts: sendOmissionsNotification を削除して未使用コードを除去する。
- src/tickets.ts: countPhasesAndTickets を『フェーズ/チケット数を返す』名詞的関数として明確にし、1関数1責務を維持する。
- ハードコードのない一般化（ファイルパス・閾値の定数化）。

## Test Plan

### Unit Tests

- UT: [Normal] find 完了後、sendOmissionsNotification は呼ばれず sendFindOutcomeNotification が progress＋統合成否付きで呼ばれる
- UT: [Normal] 統合成否判定: Tickets.json のフェーズ/チケット数が増加 → 統合成功（+N フェーズ/+M チケット）
- UT: [Error] 統合成否判定: フェーズ/チケット数が不変 → 統合なし/失敗
- UT: [Boundary] progress（list-phases-and-tickets.js）生成失敗時はフォールバック文言
- UT: [Invariant] 通知送信失敗時もループは停止しない（best-effort catch）
- UT: [Invariant] countPhasesAndTickets は読み取り専用で副作用なし
- **UT: [Normal]** runner.test.ts のモックを sendFindOutcomeNotification に追加し、find 分岐発火時に (1) sendOmissionsNotification が mockState に記録されない (2) sendFindOutcomeNotification が { progress: string, integrationSucceeded: boolean } を含むペイロードで呼ばれる ことを assert する
- **UT: [Error]** countPhasesAndTickets の前後比較で phaseCount/ticketCount が不変のとき integrationSucceeded=false になることをテスト。notifier のペイロードに『統合なし/失敗』が含まれる
- **UT: [Boundary]** buildProgressText が失敗（list-phases-and-tickets.js 不在）時にフォールバック文言を返し、通知が送出されることをテスト
- **UT: [Invariant]** sendFindOutcomeNotification の送信失敗（webhook 例外）が runLoop を中断しないことを mock で検証。countPhasesAndTickets が Tickets.json を書き換えないことを読み取り前後の内容比較で検証
- UT: [Normal] checkAllReviewed(ticketsPath) === true かつ noFind !== true かつ /find-omissions が完了した後、sendOmissionsNotification は呼ばれない（C001）
- UT: [Normal] フェーズ/チケット数が増加していれば統合成功、不変なら統合なし/失敗として通知される（C003）
- UT: [Invariant] 成功/失敗のいずれかの状態を必ず含む通知ペイロードになる（C003）
- UT: [Invariant] countPhasesAndTickets は副作用なし（読み取り専用）で Tickets.json を書き換えない（C004）

### Integration Tests

- IT: runner → notifier 統合 — find 後通知ペイロードに buildProgressText 出力と統合成否が含まれる
- IT: runner → tickets 統合 — countPhasesAndTickets の前後比較で統合成否が判定できる
- IT: 前提: make test-conver が全 green
- **IT: [Integration point]** runner.ts の find 後ブロック → notifier.ts の sendFindOutcomeNotification。ペイロードが FindOutcomeContext と一致する
- **IT: [Verification]** mock.module で sendFindOutcomeNotification を捕捉し、progress に buildProgressText の出力、integrationSucceeded に件数比較結果が渡ることを検証
- **IT: [Prerequisites]** make test-conver（npx tsc → node --test）。runner テストは session.js/tickets.js/notifier.js/step-timer.js をモック
- **IT: [Related tickets]** PX-117（本チケット）。関連: PX-116（find コマンド/GRAPH パス修正）

### Exceptions

- Item: 実際の Slack HTTP 送信（Webhook URL 到達性）/ Reason: 外部 HTTP エンドポイントと認証トークンに依存し CI 単体テストでは実行不可 / Alternative: ペイロード構築（進捗一覧・統合成否の文字列）をユニットテストで検証。これはテスト不能な設計欠陥ではなく外部環境依存によるもの
- **Exception entry:**
- [Item] phasify-omissions.js の実際の統合成功/失敗を exit コードで判定すること
- [Reason] phasify は /find-omissions コマンド内（ACP セッション内）で実行され、runner はその exit コードを直接受け取れないためテスト不能
- [Alternative verification] 統合成否は Tickets.json の件数比較（countPhasesAndTickets の前後差分）で決定的に判定する。これは設計欠陥ではなく、実行環境（ACP セッション内）の制約によるもの

### Plan Test Code (concrete code)

- UT: [Normal] find 完了後、sendOmissionsNotification は呼ばれず sendFindOutcomeNotification が呼ばれる（C001）
  ```ts
  it("checkAllReviewed=true かつ noFind=false → sendOmissionsNotification は呼ばれない", async () => {
    mockState.allReviewed = true;                       // Precondition: 全チケット reviewed
    mockState.omissionsCalled = false;
    mockState.findOutcome = undefined;
    writeTickets([{ id: 0, name: "P0", tickets: [{ id: 1, phaseId: 0, status: "todo", title: "T1" }] }]);
    const commands: string[] = [];
    mockState.runCommandImpl = async (cmd) => { commands.push(cmd); return "ok"; };
    await runLoop(baseOptions({ ticketsPath: ticketPath, noFind: false }));
    const findCmd = commands.find((c) => c.startsWith("/find-omissions "));
    assert.ok(findCmd);                                 // /find-omissions は実行される
    assert.strictEqual(mockState.omissionsCalled, false); // Postcondition: sendOmissionsNotification は呼ばれない
    assert.ok(mockState.findOutcome);                   // 新通知が呼ばれる
    mockState.allReviewed = false;
  });
  ```
- UT: [Normal] 通知ペイロードに list-phases-and-tickets.js の結果が含まれる（C002）
  ```ts
  it("sendFindOutcomeNotification の progress に buildProgressText 出力が含まれる", async () => {
    mockState.findOutcome = undefined;
    mockState.allReviewed = true;
    writeTickets([{ id: 0, name: "P0", tickets: [{ id: 1, phaseId: 0, status: "todo", title: "T1" }] }]);
    mockState.runCommandImpl = async (cmd) => "ok";
    await runLoop(baseOptions({ ticketsPath: ticketPath, noFind: false }));
    assert.ok(typeof mockState.findOutcome.progress === "string"); // Postcondition: progress 文字列
    assert.ok(mockState.findOutcome.progress.length > 0);          // list-phases-and-tickets.js 出力
    mockState.allReviewed = false;
  });
  ```
- UT: [Normal/Error] 統合成否判定 — フェーズ/チケット数増加なら成功、不変なら失敗（C003）
  ```ts
  it("countPhasesAndTickets の前後比較で integrationSucceeded が決まる", () => {
    const p = writeTempTickets({ title: "Test", phases: [] });
    const before = countPhasesAndTickets(p);           // { phaseCount: 0, ticketCount: 0 }
    writeFileSync(p, JSON.stringify({ title: "Test", phases: [{ id: 0, name: "P6", tickets: [{ id: 1, phaseId: 0, status: "todo", title: "O1" }] }] }), "utf-8");
    const after = countPhasesAndTickets(p);            // 統合後: +1 フェーズ
    const succeeded = after.phaseCount > before.phaseCount || after.ticketCount > before.ticketCount;
    assert.strictEqual(succeeded, true);               // Postcondition: 増加 → 統合成功
    const invariant = typeof succeeded === "boolean";  // Invariant: 成功/失敗のいずれか
    assert.ok(invariant);
  });
  ```
- UT: [Invariant] countPhasesAndTickets は副作用なし（C004）
  ```ts
  it("countPhasesAndTickets は読み取り専用で Tickets.json を書き換えない", () => {
    const p = writeTempTickets({ title: "Test", phases: [{ id: 0, name: "P0", tickets: [] }] });
    const before = readFileSync(p, "utf-8");
    const result = countPhasesAndTickets(p);           // Postcondition: { phaseCount, ticketCount }
    assert.deepStrictEqual(result, { phaseCount: 1, ticketCount: 0 });
    const after = readFileSync(p, "utf-8");
    assert.strictEqual(before, after);                 // Invariant: 副作用なし
  });
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| runner.ts find 後: sendOmissionsNotification(webhookUrl, cwd) が OMISSIONS-*.json を送信 | runner.ts find 後: countPhasesAndTickets の前後比較で統合成否を判定し sendFindOutcomeNotification(webhookUrl, FindOutcomeContext) を送信 | find 後通知を進捗一覧＋統合成否に置換 |
| notifier.ts: sendOmissionsNotification / buildOmissionsBlocks / SEVERITY_EMOJI / TYPE_LABEL | notifier.ts: sendFindOutcomeNotification + FindOutcomeContext を追加、旧コード削除 | 冗長通知の廃止と新通知の追加 |
| tickets.ts: 件数取得なし | tickets.ts: countPhasesAndTickets 追加 | 統合成否判定用の決定的ヘルパー |
| テスト: sendOmissionsNotification モック・find 通知テストなし | テスト: sendFindOutcomeNotification モック・C001-C003 find 後通知テスト・C004 countPhasesAndTickets テスト追加 | 契約ベースのテスト追加 |

## Notes in Prior Implementation Rounds

実装手順: (1) tickets.ts に countPhasesAndTickets 追加 (2) notifier.ts に sendFindOutcomeNotification 追加・sendOmissionsNotification 廃止 (3) runner.ts で find 後に progress 再取得＋統合成否判定＋新通知 (4) テスト更新・追加 (5) npm run typecheck + make test-conver。

- [Implementation steps] (1) src/tickets.ts: countPhasesAndTickets(ticketsPath) 追加 (2) src/notifier.ts: FindOutcomeContext 型・sendFindOutcomeNotification 追加、sendOmissionsNotification 削除 (3) src/runner.ts: find 直前にフェーズ/チケット数スナップショット、find 完了後に countPhasesAndTickets で再取得→差分判定→buildProgressText→sendFindOutcomeNotification (4) src/runner.test.ts / src/notifier.test.ts / src/tickets.test.ts 更新 (5) npm run typecheck (6) make test-conver
- [Risks] 統合成否は件数比較で判定するため、omissions 0 件の正常ケースと統合失敗を件数のみで区別できない → 通知文言で『統合なし（omissions なし または 統合失敗）』と併記する
- [Caveats] sendOmissionsNotification は notifier.ts の export として残すと未使用コードになるため削除する。新通知は find コマンド成功後のみ発火（失敗時は既存 sendSlackError が担当）
- [Open items] なし
- [Future improvements] 統合成否を件数ではなく phasify-omissions.js の exit コードや出力マーカーで判定する拡張
Implementation summary:
- Changed files: src/notifier.ts, src/runner.ts, src/tickets.ts, src/notifier.test.ts, src/runner.test.ts, src/tickets.test.ts
- Key changes: sendOmissionsNotification 廃止 → find 後に list-phases-and-tickets.js 結果（buildProgressText）＋統合成否（countPhasesAndTickets 前後比較）を sendFindOutcomeNotification で送信
- Test results: 153/153 passed (make test-conver), typecheck clean
- Quality: 13 console.log は既存 CLI 出力チャネル。新規単一文字変数なし（p→ticketsPath 適用）
Review report:
- Step 3: 0 crimes
- Step 4: 3 stubs in tests/ (pre-existing, unrelated to PX-117)
- Step 5: 7-pattern scan clean (false positives: 'todo' test data status); annotations verified (6 files, missing=0, ambiguous=0)
- Step 6: typecheck clean, 153/153 tests pass
- Step 7: contracts C001-C004 fully satisfied; no risks/omissions/contradictions/deficiencies
- Step 8: quality check passed (13 console.log pre-existing CLI output)
- Step 9: translatability no issues
- Boy Scout: sendOmissionsNotification 削除で未使用コード除去、countPhasesAndTickets を1関数1責務で実装

## PX-117 — implemented at 8 locations

### src/notifier.test.ts

- Line 193
```typescript
  async function capturePayload(
```

### src/notifier.ts

- Line 230
```typescript
export interface FindOutcomeContext {
```

### src/runner.test.ts

- Line 22
```typescript
interface FindOutcomeMock {
```

- Line 31
```typescript
interface MockState {
```

- Line 55
```typescript
function resetFindState(): void {
```

### src/runner.ts

- Line 172
```typescript
function buildProgressText(ticketsPath: string): string {
```

### src/tickets.test.ts

- Line 24
```typescript
function writeTempTickets(data: object): string {
```

### src/tickets.ts

- Line 125
```typescript
function deriveGraphPath(source?: string): string | undefined {
```
