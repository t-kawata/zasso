# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-122: Shared function createTicketFromSource for dedicated ticket-creation scripts

**Ticket Key**: PX-122 · **Phase**: -1

---

## Background

Goal: Provide the shared core used by create-resolving-ticket.js (find) and create-deferral-ticket.js (resolve) to create a quality NEW ticket from a source ticket's deep clone.
Purpose: A new ticket must inherit the source ticket's structural/relational identity (nodeIds, relatedTicketIds, referenceSection, referenceUrls, sourcePaths, rfcDiscrepancies) with ZERO loss, while its content fields are reset so the AI can rewrite them to the NEW work item — never a retry of the past ticket.
Motivation: add-omission-ticket.js already deep-clones (JSON.parse(JSON.stringify) in appendTicket, line 121) and targets the max real phase (lines 113-114), but it writes to _tmp-omissions-*.json, not directly to Tickets.json. find Step 1 and resolve deferral need direct Tickets.json creation (immediate known key for marker rewrite + Check C), non-PX max phase. A shared function avoids duplicating the clone/edit/append/validate logic across two entry scripts.
Constraints: (1) Must never lose relational fields (PRESERVE set: nodeIds, relatedTicketIds, referenceSection, referenceUrls, sourcePaths, rfcDiscrepancies). (2) New ticket must be non-PX max real phase, status='todo'. (3) Must validate via validate-tickets.js before writing (atomic, no partial write). (4) Must return the actual new key. (5) Must strip completed-ticket residue (startedAt/completedAt/round status).

## Scope

- CHANGE: Add tools/conver/.claude/scripts/lib/create-ticket-from-source.js (new shared module exporting createTicketFromSource)
- CHANGE: Deep-clone source ticket via JSON.parse(JSON.stringify()) preserving PRESERVE fields with zero loss
- CHANGE: Strip completed-ticket residue: status→'todo', delete completedAt/startedAt, no round-aware R<round> status
- CHANGE: Apply caller seed edits to content fields (title/scope/background); other content fields stay as clone (old content) for the entry script's field-by-field rewrite guidance
- CHANGE: Determine the non-PX max real phase; create phase 0 if none exists (mirrors add-omission-ticket.js:113-114)
- CHANGE: Append with auto-incremented id (maxId+1 within target phase) and set phaseId=targetPhase.id
- CHANGE: Validate the merged Tickets.json via validate-tickets.js before writing; reject with exit 1 and no write on failure
- CHANGE: Return { key, ticket } where key = (P{phaseId}|PX)-{id}
- NO-CHANGE: Do NOT modify add-omission-ticket.js (its _tmp-omissions-*.json write path stays for find Steps 5-6)
- NO-CHANGE: Do NOT modify phasify-omissions.js or the ticket schema
- IMPACT: Entry scripts PX-123 (create-resolving-ticket.js) and PX-124 (create-deferral-ticket.js) depend on this function's signature
- IMPACT: Marker-key rewrite and Check C (active key) depend on the returned key being the real key
- IMPACT: R<round> freezing by phasify (markPreMergeTicketsReviewed) later marks the ORIGINAL Step-1 tickets as past, so the returned key must be used for markers, not the source key

## Implementation Target Files

- `tools/conver/.claude/scripts/lib/create-ticket-from-source.js`

## Investigation

add-omission-ticket.js:121 deep-clone pattern (JSON.parse(JSON.stringify(ticket))); :113-114 max real phase targeting (filter p.id !== -1, Math.max, find); :24 REQUIRED_FIELDS (title/background/scope/testUnit/acceptanceCriteria/invariants); :130-141 appendTicket sets id=maxId+1 and phaseId=targetPhase.id. add-ticket.js:52 phase resolution (phaseArg==='PX' ? -1 : data.phases.length); :62-64 ticket construction with status='todo' then validateTickets; :85 returns ticketKey 'P'+id+'-'+newId. validate-tickets.js:36-39 hard-required id/phaseId/title/status; :40 arrayFields; :72 strFields; :9 isRoundStatus /^R[1-9]\d*$/. phasify-omissions.js:1013 markPreMergeTicketsReviewed freezes pre-merge phases (id < offset) to 'R'+round — the source of the residue-stripping requirement. ensure-ticket.js:124 hardcodes 'PX' phase for make-ticket Case B — the reason the dedicated scripts must create in a real max phase.

## Acceptance Criteria

- Happy path: given an existing source ticket and seed edits, creates a todo non-PX max-phase ticket preserving all relational fields, returns the real key
- Error case: source ticket not found or invalid seed → exit 1, Tickets.json unchanged
- Edge case: no non-PX phase → phase 0 created; PX-only Tickets.json → phase 0; max-phase id collision → auto-increment avoids duplicate

## Invariants

- Normal establishment: deep clone preserves PRESERVE fields (nodeIds, relatedTicketIds, referenceSection, referenceUrls, sourcePaths, rfcDiscrepancies) exactly; no relational field is dropped or emptied
- Invariant on error: Tickets.json is unchanged on any failure (validate before write; no partial write)
- Internal state invariant: new ticket has status='todo', no completedAt/startedAt, no round-aware status
- Boundary invariant: append targets the max real phase (id=N); never the PX phase (id=-1); creates phase 0 when no non-PX phase exists

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — deep-clone preservation

- **Precondition**: Source ticket exists in Tickets.json with nodeIds, relatedTicketIds, referenceSection, referenceUrls, sourcePaths, rfcDiscrepancies
- **Postcondition**: New ticket carries identical values for all PRESERVE fields (zero loss)
- **Invariant**: No PRESERVE field is dropped, emptied, or mutated in the clone

### C002 — residue stripping

- **Precondition**: Source ticket has a completed status (reviewed, done, or R<round>) and/or completedAt/startedAt
- **Postcondition**: New ticket has status='todo', no completedAt/startedAt, no round-aware status
- **Invariant**: The new ticket is always an active obligation (ACTIVE_STATUSES member)

### C003 — max real phase append

- **Precondition**: Tickets.json has phases with a maximum non-PX phase id N (or none)
- **Postcondition**: New ticket phaseId = N (or 0 when none exists), id = maxId+1 within that phase, key returned
- **Invariant**: The new ticket is never appended to the PX phase (id=-1)

### C004 — schema validation

- **Precondition**: New ticket assembled with all schema-required fields (id, phaseId, title, status)
- **Postcondition**: validate-tickets.js passes on the merged Tickets.json; Tickets.json written only on success
- **Invariant**: A failing validation leaves Tickets.json byte-identical (no partial write)

### C005 — seed edits

- **Precondition**: Caller supplies title and optionally scope/background as seed edits
- **Postcondition**: New ticket's title/scope/background equal the provided seeds; other content fields carry the clone's values
- **Invariant**: The returned key always matches the actually-created ticket (phaseId-id tuple)

## Boy Scout Rule

Functions as verbs: createTicketFromSource, stripCompletedResidue, applySeedEdits, resolveMaxRealPhase, appendTicketToPhase, validateAndWrite.
One responsibility per function — no mixed concerns.
Named constants for the PRESERVE / REWRITE / REGENERATE field sets instead of inline arrays.
No error swallowing: every failure path returns { success:false, error } or exits 1 with an actionable stderr message.
Reuse add-ticket.js/validate-tickets.js rather than re-implementing validation.

## Test Plan

### Unit Tests

- UT: [Normal] deep clone preserves nodeIds/relatedTicketIds/referenceSection/referenceUrls/sourcePaths/rfcDiscrepancies exactly (byte-identical to source)
- UT: [Normal] append targets the non-PX max real phase with id=maxId+1 and phaseId=targetPhase.id; returned key matches the actual ticket
- UT: [Normal] seed edits (title/scope/background) are applied to the new ticket's content fields
- UT: [Error] source ticket not found (key parses but ticket absent) → exit 1, Tickets.json unchanged
- UT: [Error] invalid seed (missing required title) → validateTicket-style rejection, exit 1, no write
- UT: [Boundary] no non-PX phases exist (only PX phase) → creates phase 0 and appends there
- UT: [Boundary] empty Tickets.json (no phases at all) → creates phase 0
- UT: [Invariant] residue stripped: status='todo', completedAt/startedAt absent, no R<round> status
- UT: [Invariant] validate-tickets.js passes on the merged Tickets.json after append
- UT: [Invariant] PRESERVE fields are never emptied by the append (immutability of relational identity)

### Integration Tests

- IT: createTicketFromSource + validate-tickets.js — the created ticket passes full schema validation (id, phaseId, title, status required)
- IT: createTicketFromSource + add-ticket.js phase/id convention — auto-increment id matches the existing per-phase id sequence
- Prerequisites: PX-122 is the foundation; entry scripts PX-123/PX-124 consume its returned key
- Related tickets: PX-123, PX-124

### Exceptions

- Item: phasify's STUB-key re-rewrite (rewriteOutputStubKeys) cannot test here because it is phasify behavior, not this function's.
Reason: It lives in phasify-omissions.js and requires a full phasify merge pipeline; this is not a design defect — the behavior is genuinely external to this function.
Alternative verification: covered by the integration contract in PX-123 (stubs[] embedding) and the existing phasify-omissions tests.

### Plan Test Code (concrete code)

- UT: [C001 deep-clone preservation]
  const source = { id: 1, phaseId: 0, title: 'S', status: 'todo', nodeIds: ['N0001','N0002'], relatedTicketIds: 'P0-2', referenceSection: 'RFC §3', referenceUrls: ['https://x'], sourcePaths: ['src/a.rs'], rfcDiscrepancies: [] };
  const data = { title: 'T', phases: [{ id: 0, name: 'P0', tickets: [source] }] };
  const res = createTicketFromSource({ ticketsData: data, sourceKey: 'P0-1', seed: { title: 'New' } });
  assert.strictEqual(res.success, true);
  assert.deepStrictEqual(res.ticket.nodeIds, source.nodeIds);
  assert.strictEqual(res.ticket.relatedTicketIds, source.relatedTicketIds);
  assert.strictEqual(res.ticket.referenceSection, source.referenceSection);
  assert.deepStrictEqual(res.ticket.referenceUrls, source.referenceUrls);
  assert.deepStrictEqual(res.ticket.sourcePaths, source.sourcePaths);
  assert.deepStrictEqual(res.ticket.rfcDiscrepancies, source.rfcDiscrepancies);
- UT: [C002 residue stripping]
  const source = { id: 1, phaseId: 0, title: 'S', status: 'reviewed', completedAt: '2026-08-01', startedAt: '2026-07-01' };
  const data = { title: 'T', phases: [{ id: 0, name: 'P0', tickets: [source] }] };
  const res = createTicketFromSource({ ticketsData: data, sourceKey: 'P0-1', seed: { title: 'New' } });
  assert.strictEqual(res.ticket.status, 'todo');
  assert.strictEqual(res.ticket.completedAt, undefined);
  assert.strictEqual(res.ticket.startedAt, undefined);
  assert.ok(!/^R[1-9]\d*$/.test(res.ticket.status || ''));
- UT: [C003 max real phase append]
  const source = { id: 1, phaseId: 0, title: 'S', status: 'todo' };
  const data = { title: 'T', phases: [{ id: 0, name: 'P0', tickets: [source] }, { id: 2, name: 'P2', tickets: [{ id: 1, title: 'x', status: 'todo' }, { id: 2, title: 'y', status: 'todo' }] }] };
  const res = createTicketFromSource({ ticketsData: data, sourceKey: 'P0-1', seed: { title: 'New' } });
  assert.strictEqual(res.ticket.phaseId, 2);
  assert.strictEqual(res.ticket.id, 3);
  assert.strictEqual(res.key, 'P2-3');
  assert.strictEqual(res.data.phases[1].tickets.length, 3);
- UT: [C004 schema validation]
  const source = { id: 1, phaseId: 0, title: 'S', status: 'todo' };
  const data = { title: 'T', phases: [{ id: 0, name: 'P0', tickets: [source] }] };
  const res = createTicketFromSource({ ticketsData: data, sourceKey: 'P0-1', seed: { title: 'New' } });
  const validation = validateTickets(res.data);
  assert.strictEqual(validation.valid, true);
  const bad = createTicketFromSource({ ticketsData: data, sourceKey: 'P0-1', seed: {} });
  assert.strictEqual(bad.success, false);
- UT: [C005 seed edits + key match]
  const source = { id: 1, phaseId: 0, title: 'S', status: 'todo', background: 'old', scope: ['old1'] };
  const data = { title: 'T', phases: [{ id: 0, name: 'P0', tickets: [source] }] };
  const res = createTicketFromSource({ ticketsData: data, sourceKey: 'P0-1', seed: { title: 'New', background: 'new-bg', scope: ['new1'] } });
  assert.strictEqual(res.ticket.title, 'New');
  assert.strictEqual(res.ticket.background, 'new-bg');
  assert.deepStrictEqual(res.ticket.scope, ['new1']);
  assert.strictEqual(res.key, 'P' + res.ticket.phaseId + '-' + res.ticket.id);

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| (none) | lib/create-ticket-from-source.js | Added shared pure function createTicketFromSource (deep-clone → residue strip → seed edits → non-PX max phase append → validate → return key) |
| (none) | tests/lib/create-ticket-from-source.test.js | Added 38-assertion test suite covering C001-C005 + boundary cases |

## Notes in Prior Implementation Rounds

Implementation steps: (1) define PRESERVE/REWRITE/REGENERATE field taxonomy as named constants; (2) deep-clone source; (3) strip completed residue; (4) apply seed edits; (5) resolve max real phase; (6) append with auto-id; (7) validate whole Tickets.json; (8) return { key, ticket }.
Risks: entry scripts MUST use the returned key (not the requested key) for marker rewrite — the requested key may be a placeholder phase target.
Caveats: content fields not in the seed intentionally carry the SOURCE's old content; the entry scripts' Markdown output tells the AI to rewrite them field-by-field via update-ticket.js.
Open items: none.
Future improvements: add a post-creation field-density check (check-field-density.js) once content rewrite is complete.
Implementation summary:
- Changed files: .claude/scripts/lib/create-ticket-from-source.js, .claude/tests/lib/create-ticket-from-source.test.js
- Key changes: PRESERVE/STRIP/PX constants; findTicket/stripCompletedResidue/applySeedEdits/resolveMaxRealPhase/appendTicketToPhase helpers; seed-title enforcement
- Test results: all 38 assertions passed (RED confirmed before implementation)
Review report:
- Static quality check: passed (0 issues on changed source)
- Translatability: no issues (verb-phrase function names, no magic numbers, no debug debris)
- Tests: 38 assertions passed; phasify regression all passed
- Dependencies: consistency verified
- Issues found and fixes applied: none introduced by this ticket; cross-ticket annotation verified, AMBIGUOUS 0
- Contracts: C001-C005 fulfilled (verify-final-contracts exit 0)
- Targets: validate-ticket-targets exit 0

## PX-122 — implemented at 9 locations

### .claude/scripts/lib/create-ticket-from-source.js

- Line 53
```javascript
function findTicket(ticketsData, ticketKey) {
```

- Line 75
```javascript
function stripCompletedResidue(ticket) {
```

- Line 94
```javascript
function applySeedEdits(ticket, seed) {
```

- Line 111
```javascript
function resolveMaxRealPhase(result) {
```

- Line 130
```javascript
function appendTicketToPhase(phase, ticket) {
```

- Line 166
```javascript
function createTicketFromSource({ ticketsData, sourceKey, seed = {} }) {
```

### .claude/tests/lib/create-ticket-from-source.test.js

- Line 20
```javascript
function assert(condition, message) {
```

- Line 26
```javascript
function assertStrictEqual(actual, expected, message) {
```

- Line 32
```javascript
function assertDeepStrictEqual(actual, expected, message) {
```
