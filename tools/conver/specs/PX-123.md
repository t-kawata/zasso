# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-123: create-resolving-ticket.js + find-omissions.md Step 1 auto-creation

**Ticket Key**: PX-123 · **Phase**: -1

---

## Background

Goal: find-omissions.md Step 1 (preflight gate) must auto-create resolving tickets via a dedicated create-resolving-ticket.js, never pausing to ask the human.
Purpose: The gate's Check C failure (markers referencing completed tickets, e.g. the 63-stub scenario) requires new resolving tickets so markers can reference active obligations. The AI must execute this via the dedicated script — not present the human a menu of options.
Motivation: The current Step 1 table says Create the resolving ticket and rewrite the marker key (find-omissions.md:117) but names no script, so the AI stopped and asked the human. The script deep-clones the marker's referenced ticket as a template (structure preserved, content reset), embeds stubs[] so phasify's rewriteOutputStubKeys can re-rewrite markers to the phasify duplicates after the Step 9 merge (phasify-omissions.js:742-760), and outputs a Markdown Action-directive.
Constraints: (1) Must never pause to ask the human — every blocker is an AI-executable work item (no-external-excuse rule). (2) Must embed stubs[] (with the marker content + rewritten key) so phasify can re-rewrite markers to duplicates after markPreMergeTicketsReviewed freezes originals as R<round>. (3) Must output the Markdown guidance (old-content warning, field-by-field update-ticket.js rewrite, preserve list). (4) Must return the real new key on stdout. (5) find-omissions.md Step 1 resolvedCandidates NOT-resolved branch and orphans row must reference the script.

## Scope

- CHANGE: Add tools/conver/.claude/scripts/tickets/create-resolving-ticket.js (find entry over lib/create-ticket-from-source.js)
- CHANGE: Parse --source-key (marker's referenced ticket) + stdin ticket JSON (seed edits) + --stubs='[{"file":"...","line":N,"content":"..."}]'
- CHANGE: Call createTicketFromSource with sourceKey + seed edits; embed stubs[] on the created ticket (from the --stubs arg) so phasify rewriteOutputStubKeys can re-rewrite markers to the phasify duplicates
- CHANGE: Output Markdown Action-directive to stdout: ticket key heading, old-content warning (show-ticket-context.js --for-spec), field-by-field update-ticket.js rewrite instructions (max 3 per call, suggested order), PRESERVE list (do not touch)
- CHANGE: Output the new key machine-readably (stdout first line or key in the Markdown heading)
- CHANGE: Modify .claude/commands/find-omissions.md Step 1: resolvedCandidates row gains the NOT-resolved branch (create resolving ticket via the script); orphans row references create-resolving-ticket.js; add a Resolving-ticket creation (auto, never pause to ask the human) block
- CHANGE: Add a never-ask directive to the Step 1 gate loop: when a resolving ticket is required, create it via the script and rewrite the key, then re-run the gate
- NO-CHANGE: Do NOT change Step 9 phasify command semantics or the 4-class classification script behavior
- NO-CHANGE: Do NOT change resolve-ticket.md (covered by PX-124)
- IMPACT: Resolves the original stoppage (AI asking the human for gate direction)
- IMPACT: Relies on PX-122's createTicketFromSource for the clone/append/validate/return-key core
- IMPACT: The stubs[] embedding + phasify rewriteOutputStubKeys + markPreMergeTicketsReviewed (existing) together ensure markers end up referencing the phasify duplicates (active), not the frozen originals (R<round>)

## Implementation Target Files

- `tools/conver/.claude/scripts/tickets/create-resolving-ticket.js`
- `tools/conver/.claude/commands/find-omissions.md`

## Investigation

find-omissions.md Step 1 (lines 101-129): preflight gate, 4-class table, resolvedCandidates row action 'Verify in code that the defect is resolved, then remove-stub.js'; orphans row 'Create the resolving ticket and rewrite the marker key'; gate loop exit-1 → fix marker → re-run; Output message convention (English, self-contained, Action-directive). preflight-stub-cleanup.js:175-179 prints [ACTION] orphans / MUST RESOLVE — create the resolving ticket now, then rewrite the marker key. validate-no-external-excuses.js:66 ACTIVE_STATUSES={todo,in_progress,planned,remanded}; :107-121 Check C key validity; :155-159 Check C action 'Rewrite the marker key to the new resolving ticket'; extractTicketKey exported. create-tmp-omissions.js:126 seeds non-reviewed (incl todo) tickets into _tmp-omissions → phasify duplicates Step-1 tickets; phasify-omissions.js:1013 markPreMergeTicketsReviewed freezes originals to R<round>; :742-760 rewriteOutputStubKeys only processes tickets with non-empty stubs[] and rewrites marker keys; :758 rewriteSourceMarkerLines requires --src-dir. find-omissions.md:331 Step 9 command does NOT pass --src-dir (open item).

## Acceptance Criteria

- Happy path: find Step 1 encountering a NOT-resolved completed-ticket stub auto-creates a resolving ticket via the script, rewrites the marker key, and the gate exits 0 without human input
- Error case: script fails (bad source, missing args) → exit 1, actionable stderr, no partial ticket
- Edge case: keyless MUST RESOLVE marker (no source key) → the script uses a generic template path or the AI removes the marker if dead; the gate must still converge

## Invariants

- Normal establishment: a resolving ticket is created via the script with a real new key and the marker is rewritten to it, so Check C passes
- Invariant on error: script failure exits non-zero with an actionable stderr message; no ticket is created without the stubs[] embedding
- Internal state invariant: the created ticket is todo, non-PX, max phase, with stubs[] embedded; the marker key references the new active ticket
- Boundary invariant: the script is invoked per-marker-group (or per-stub), one ticket at a time; never a batch that loses per-stub stubs[] association

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — auto-creation, never ask

- **Precondition**: find Step 1 detects a marker requiring a new resolving ticket (NOT-resolved completed-ticket reference, orphan, or Check C failure)
- **Postcondition**: A resolving ticket is created via create-resolving-ticket.js and the marker key is rewritten; the gate loops without human input
- **Invariant**: The AI never pauses to present the human a menu; every blocker is an AI-executable work item

### C002 — stubs[] embedding

- **Precondition**: The script is invoked with --source-key (marker's referenced ticket) and --stubs (marker entries with old keys)
- **Postcondition**: The created ticket carries stubs[] (non-empty when stubs passed) so phasify rewriteOutputStubKeys can re-rewrite markers to the phasify duplicates after the merge
- **Invariant**: stubs[] is never dropped when the ticket is created

### C003 — Markdown Action-directive

- **Precondition**: Ticket creation succeeds
- **Postcondition**: stdout contains: the new key, an old-content warning (show-ticket-context.js --for-spec), field-by-field update-ticket.js rewrite instructions (max 3 per call, suggested order), and the PRESERVE list (do not touch)
- **Invariant**: The guidance is English, self-contained, and actionable by a fresh session

### C004 — real key for Check C

- **Precondition**: The script returns a key
- **Postcondition**: The returned key matches the actually-created ticket and is used for the marker rewrite, so Check C (active key) passes
- **Invariant**: The marker never references a frozen or non-existent key

### C005 — find-omissions.md wiring

- **Precondition**: find-omissions.md Step 1 exists
- **Postcondition**: resolvedCandidates NOT-resolved branch and orphans row reference create-resolving-ticket.js; a Resolving-ticket creation (auto, never pause to ask the human) block is present
- **Invariant**: The Step 1 table and gate loop consistently direct to the script

## Boy Scout Rule

Functions as verbs: buildMarkdownGuidance, embedStubReferences, parseStubList, printActionDirective.
One responsibility per function.
Named constant for the suggested rewrite field order.
No error swallowing.
Follow the find-omissions.md Output message convention: every stdout/stderr line is English, self-contained, and Action-directive.

## Test Plan

### Unit Tests

- UT: [Normal] given --source-key + seed edits + --stubs, createTicketFromSource is invoked and stubs[] is embedded on the created ticket
- UT: [Normal] stdout contains the real new key and the Markdown guidance sections (old-content warning, field-by-field rewrite, PRESERVE list)
- UT: [Normal] the Markdown guidance shows a single-field update-ticket.js example and the suggested rewrite order
- UT: [Error] missing --source-key or empty --stubs → exit 1 with actionable stderr
- UT: [Error] source ticket not found → exit 1, no ticket created
- UT: [Boundary] zero stubs passed → still creates the ticket but emits a warning that no marker rewrite will be driven by phasify
- UT: [Invariant] the returned key matches the actually-created ticket (phaseId-id tuple)
- UT: [Invariant] PRESERVE fields from the clone are listed in the Markdown output as do-not-touch
- UT: [Invariant C001] the AI never pauses to present the human a menu — every blocker is an AI-executable work item; the resolving-ticket path is the only route in the gate loop
- UT: [Precondition C004] the script returns a key on stdout that matches the actually-created ticket (phaseId-id tuple)
- UT: [Precondition C005] find-omissions.md Step 1 exists and is the invocation context for the gate loop
- UT: [Postcondition C005] find-omissions.md Step 1 resolvedCandidates NOT-resolved branch and orphans row reference create-resolving-ticket.js; a Resolving-ticket creation (auto, never pause to ask the human) block is present
- UT: [Invariant C005] the Step 1 table and gate loop consistently direct to the script; the command file is the single source of the never-ask rule
- UT: [Invariant C003] the Markdown guidance is English, self-contained, and actionable by a fresh session — every section can be acted on alone

### Integration Tests

- IT: create-resolving-ticket.js + createTicketFromSource — end-to-end ticket creation with stubs[] embedding
- IT: create-resolving-ticket.js + phasify-omissions.js rewriteOutputStubKeys — given a ticket with stubs[] referencing an old key, phasify rewrites the marker to the new key (existing phasify test covers the mechanism; this verifies the stubs[] shape matches)
- IT: find-omissions.md Step 1 gate → validate-no-external-excuses.js — after the script + marker rewrite, Check C passes (active key)
- Prerequisites: PX-122 (common function)
- Related tickets: PX-122, PX-124

### Exceptions

- Item: the full find Step 1 → Step 9 loop (create-tmp-omissions seeding → phasify freeze → re-rewrite) cannot test here.
Reason: It requires the entire find pipeline (get-next, create-tmp-omissions, phasify), which is exercised by the existing phasify-omissions tests; this is not an architectural defect — the scenario is inherently an end-to-end integration.
Alternative verification: unit-test the stubs[] shape contract against phasify's rewriteOutputStubKeys trigger condition (ticket.stubs non-empty), and rely on the existing phasify tests.

### Plan Test Code (concrete code)

- UT: [C001-pre] find Step 1 detects a NOT-resolved completed-ticket stub → resolving ticket created
  const source = { id: 1, phaseId: 0, title: 'Old', status: 'reviewed', nodeIds: ['N0001'], relatedTicketIds: 'P0-2' };
  const data = { title: 'T', round: 1, metadata: { source: 'test', generatedAt: '2026-08-04' }, phases: [{ id: 0, name: 'P0', tickets: [source] }] };
  const stubs = [{ file: 'src/a.rs', line: 5, content: '[::STUB::] P0-1: -- Vendor lib' }];
  const res = createResolvingTicket({ ticketsData: data, sourceKey: 'P0-1', seed: { title: 'New' }, stubs });
  assert(res.success === true, 'C001: resolving ticket auto-created');
- UT: [C002-post] created ticket carries stubs[] for phasify re-rewrite
  const stubs = [{ file: 'src/a.rs', line: 5, content: '[::STUB::] P0-1: -- Vendor lib' }];
  const res = createResolvingTicket({ ticketsData: data, sourceKey: 'P0-1', seed: { title: 'New' }, stubs });
  assert(Array.isArray(res.ticket.stubs), 'C002: stubs[] is an array');
  assertStrictEqual(res.ticket.stubs.length, stubs.length, 'C002: all stubs embedded');
- UT: [C003-post] Markdown Action-directive includes old-content warning + field-by-field rewrite + preserve list
  const md = buildMarkdownGuidance({ key: 'P2-1', sourceKey: 'P0-1' });
  assert(md.includes('show-ticket-context.js'), 'C003: old-content warning command present');
  assert(md.includes('update-ticket.js'), 'C003: field-by-field rewrite instructions present');
  assert(md.includes('ONE FIELD AT A TIME') || md.includes('one field at a time'), 'C003: one-at-a-time guidance present');
  assert(md.includes('nodeIds'), 'C003: preserve list present');
- UT: [C004-inv] returned key matches the actually-created ticket (Check C active key)
  const res = createResolvingTicket({ ticketsData: data, sourceKey: 'P0-1', seed: { title: 'New' }, stubs });
  const expected = 'P' + res.ticket.phaseId + '-' + res.ticket.id;
  assertStrictEqual(res.key, expected, 'C004: real key for marker rewrite');
- UT: [C005-post] find-omissions.md Step 1 references create-resolving-ticket.js
  const md = fs.readFileSync('.claude/commands/find-omissions.md', 'utf8');
  assert(md.includes('create-resolving-ticket.js'), 'C005: Step 1 resolvedCandidates/orphans reference the script');

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| (none) | create-resolving-ticket.js | Added find entry script: createResolvingTicket + buildMarkdownGuidance + parseArgs; embeds stubs[] for phasify re-rewrite, prints Markdown Action-directive |
| (none) | tests/tickets/create-resolving-ticket.test.js | Added 23-assertion test suite covering C001-C005 + parseArgs |
| find-omissions.md Step 1: 'Create the resolving ticket' (no script named) | find-omissions.md Step 1: resolvedCandidates NOT-resolved branch + orphans reference create-resolving-ticket.js + Resolving-ticket creation (auto, never pause to ask) block | find-omissions.md wiring so the AI auto-creates resolving tickets |

## Notes in Prior Implementation Rounds

Implementation steps: (1) parse --source-key/--stubs/stdin; (2) call createTicketFromSource; (3) embed stubs[]; (4) write the Markdown guidance to stdout; (5) print the new key.
Risks: the marker rewrite must use the REAL returned key, and phasify's re-rewrite requires stubs[] content to contain the marker's old key (extractTicketKey pattern).
Caveats: the Markdown guidance must NOT be a one-shot giant update-ticket.js call — one field at a time (max 3).
Open items: whether find-omissions.md Step 9 must pass --src-dir for source-marker rewrite — verify against rewriteSourceMarkerLines.
Future improvements: add a post-create density gate (check-field-density.js) in the find loop.
Implementation summary:
- Changed files: .claude/scripts/tickets/create-resolving-ticket.js, .claude/tests/tickets/create-resolving-ticket.test.js, .claude/commands/find-omissions.md
- Key changes: createResolvingTicket (deep-clone via PX-122 + stubs[] embedding), buildMarkdownGuidance (one-at-a-time rewrite guidance), Step 1 never-ask directive
- Test results: all 23 assertions passed; CLI smoke test created P0-2 with stubs=1 and nodeIds preserved
Review report:
- Static quality check: passed (0 issues on changed source)
- Translatability: no issues (verb-phrase function names, no magic numbers, no debug debris)
- Tests: 23 assertions passed; phasify regression all passed
- Dependencies: consistency verified
- Issues found and fixes applied: none introduced by this ticket; cross-ticket annotation verified, AMBIGUOUS 0
- Contracts: C001-C005 fulfilled (verify-final-contracts exit 0)
- Targets: validate-ticket-targets exit 0

## PX-123 — implemented at 17 locations

### .claude/scripts/lib/create-ticket-from-source.js

- Line 53
```javascript
function findTicket(ticketsData, ticketKey) {
```

- Line 75
```javascript
function stripCompletedResidue(ticket) {
```

- Line 94
```javascript
function applySeedEdits(ticket, seed) {
```

- Line 111
```javascript
function resolveMaxRealPhase(result) {
```

- Line 130
```javascript
function appendTicketToPhase(phase, ticket) {
```

- Line 166
```javascript
function createTicketFromSource({ ticketsData, sourceKey, seed = {} }) {
```

### .claude/scripts/tickets/create-resolving-ticket.js

- Line 45
```javascript
function createResolvingTicket({ ticketsData, sourceKey, seed, stubs = [] }) {
```

- Line 59
```javascript
function buildMarkdownGuidance({ key, sourceKey }) {
```

- Line 98
```javascript
function parseArgs(argv) {
```

- Line 117
```javascript
function readStdin() {
```

- Line 126
```javascript
async function main() {
```

### .claude/tests/lib/create-ticket-from-source.test.js

- Line 20
```javascript
function assert(condition, message) {
```

- Line 26
```javascript
function assertStrictEqual(actual, expected, message) {
```

- Line 32
```javascript
function assertDeepStrictEqual(actual, expected, message) {
```

### .claude/tests/tickets/create-resolving-ticket.test.js

- Line 23
```javascript
function assert(condition, message) {
```

- Line 29
```javascript
function assertStrictEqual(actual, expected, message) {
```

- Line 53
```javascript
function makeData() {
```
