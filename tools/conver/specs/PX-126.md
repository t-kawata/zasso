# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-126: preflight-stub-cleanup.js resolvedCandidates ACTION: add NOT-resolved branch

**Ticket Key**: PX-126 · **Phase**: -1

---

## Background

Goal: preflight-stub-cleanup.js's resolvedCandidates [ACTION] output must direct the AI to create a resolving ticket when the completed-ticket-referencing marker is NOT resolved in code.
Purpose: find-omissions.md Step 1 now has a NOT-resolved branch (create a resolving ticket via create-resolving-ticket.js and rewrite the marker key), but the script's [ACTION] line only says verify + remove. A fresh session acting on the [ACTION] alone (Output message convention) will not create a resolving ticket — reproducing the original 63-stub stoppage.
Motivation: The Output message convention requires every stdout/stderr line to be self-contained and Action-directive. The [ACTION] must reflect both branches (resolved → remove; NOT resolved → create resolving ticket + rewrite key).
Constraints: (1) The [ACTION] must keep the resolved branch (remove-stub.js) intact. (2) The NOT-resolved branch must reference create-resolving-ticket.js and the key rewrite. (3) Output stays English, self-contained, Action-directive. (4) The preflight test must be updated to assert the new message.

## Scope

- CHANGE: preflight-stub-cleanup.js resolvedCandidates [ACTION] — add the NOT-resolved branch: 'If NOT resolved → create a resolving ticket via create-resolving-ticket.js and rewrite the marker key'
- CHANGE: preflight-stub-cleanup.test.js — assert the resolvedCandidates [ACTION] message contains both branches
- NO-CHANGE: The 4-class classification logic and the orphans/excuses [ACTION] lines stay unchanged
- NO-CHANGE: find-omissions.md Step 1 (already updated with the NOT-resolved branch) stays as-is
- IMPACT: A fresh session acting on the [ACTION] alone will create a resolving ticket for NOT-resolved stubs — the original stoppage is prevented
- IMPACT: The [ACTION] message now matches the Step 1 table instruction
- IMPACT: The script's output remains consistent with the create-resolving-ticket.js flow (PX-123)

## Implementation Target Files

- `tools/conver/.claude/scripts/tickets/preflight-stub-cleanup.js`
- `tools/conver/.claude/scripts/tickets/tests/preflight-stub-cleanup.test.js`

## Investigation

preflight-stub-cleanup.js:172-174 prints '[ACTION] resolvedCandidates — verify in code that the defect is resolved, then remove:' followed by buildRemoveCommand(stub). find-omissions.md:115 resolvedCandidates row now says 'If NOT resolved → create a resolving ticket (below) and rewrite the marker key'. preflight-stub-cleanup.test.js exists at .claude/scripts/tickets/tests/ and asserts classification behavior; the [ACTION] message assertions live there.

## Acceptance Criteria

- Happy path: the resolvedCandidates [ACTION] directs the AI to remove when resolved and to create a resolving ticket when NOT resolved
- Error case: classification unchanged (message-only edit)
- Edge case: orphans/excuses messages byte-identical

## Invariants

- Normal establishment: the resolvedCandidates [ACTION] contains both the remove branch and the create-resolving-ticket branch
- Invariant on error: the classification JSON output is unchanged (message-only change)
- Internal state invariant: no classification logic changes — only the printed directive text
- Boundary invariant: orphans and excuses [ACTION] messages remain byte-identical

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — resolvedCandidates ACTION branches

- **Precondition**: a resolvedCandidates stub is classified
- **Postcondition**: the [ACTION] message contains both the resolved branch (remove-stub.js) and the NOT-resolved branch (create-resolving-ticket.js + rewrite the marker key)
- **Invariant**: both branches are present in the single [ACTION] line

### C002 — NOT-resolved directive

- **Precondition**: a marker references a completed ticket whose defect is NOT resolved in code
- **Postcondition**: the [ACTION] directs create-resolving-ticket.js and the key rewrite
- **Invariant**: the directive names the exact script create-resolving-ticket.js

### C003 — Output message convention

- **Precondition**: the [ACTION] line is printed
- **Postcondition**: the line is English, self-contained, and Action-directive (a fresh session can act on it alone)
- **Invariant**: the message stays single-line and self-contained

### C004 — test coverage

- **Precondition**: preflight-stub-cleanup.test.js exists
- **Postcondition**: the test asserts the resolvedCandidates [ACTION] contains both branches
- **Invariant**: the classification JSON structure is unchanged

## Boy Scout Rule

Keep the [ACTION] message as a single self-contained directive per the Output message convention.
Name the two branches explicitly (resolved / NOT resolved) to avoid ambiguity.
No error swallowing in the message path.

## Test Plan

### Unit Tests

- UT: [Normal] resolvedCandidates [ACTION] message contains 'remove-stub.js' (resolved branch)
- UT: [Normal] resolvedCandidates [ACTION] message contains 'create-resolving-ticket.js' (NOT-resolved branch)
- UT: [Normal] resolvedCandidates [ACTION] message contains 'rewrite the marker key'
- UT: [Boundary] orphans [ACTION] message unchanged (still references create-resolving-ticket.js and MUST RESOLVE)
- UT: [Boundary] excuses [ACTION] message unchanged
- UT: [Invariant] the classification JSON (stdout) structure is unchanged
- UT: [Precondition C001] a resolvedCandidates stub is classified; the [ACTION] contains both branches
- UT: [Postcondition C001] the [ACTION] message contains remove-stub.js and create-resolving-ticket.js and 'rewrite the marker key'
- UT: [Precondition C002] a marker references a completed ticket whose defect is NOT resolved in code; the [ACTION] directs create-resolving-ticket.js
- UT: [Postcondition C003] the line is English, self-contained, and Action-directive — a fresh session can act on it alone
- UT: [Precondition C004] preflight-stub-cleanup.test.js exists and asserts the resolvedCandidates [ACTION] message

### Integration Tests

- IT: preflight-stub-cleanup.js on a fixture with a completed-ticket-referencing stub — the [ACTION] line for resolvedCandidates includes the NOT-resolved directive
- IT: find-omissions.md Step 1 table and the [ACTION] message are consistent (both reference create-resolving-ticket.js for NOT-resolved)
- Prerequisites: PX-123 (create-resolving-ticket.js exists)
- Related tickets: PX-123, PX-125

### Exceptions

- Item: the full find Step 1 → gate loop cannot test here as a single unit.
Reason: it requires the entire find command flow (preflight → validate-no-external-excuses → create-resolving-ticket), which is covered by the existing preflight test and the command's own flow; this is not an architectural defect — the change is a message-only addition to one [ACTION] line.
Alternative verification: unit-test the [ACTION] message content and rely on the existing preflight classification tests for behavior.

### Plan Test Code (concrete code)

- UT: [C001-post] RESOLVED_CANDIDATES_ACTION_MESSAGE contains both branches
  const { RESOLVED_CANDIDATES_ACTION_MESSAGE } = require('../../scripts/tickets/preflight-stub-cleanup.js');
  assert(RESOLVED_CANDIDATES_ACTION_MESSAGE.includes('remove-stub.js'), 'C001: resolved branch present');
  assert(RESOLVED_CANDIDATES_ACTION_MESSAGE.includes('create-resolving-ticket.js'), 'C001: NOT-resolved branch present');
  assert(RESOLVED_CANDIDATES_ACTION_MESSAGE.includes('rewrite the marker key'), 'C001: rewrite instruction present');
- UT: [C002-post] the message directs create-resolving-ticket.js for NOT-resolved
  assert(RESOLVED_CANDIDATES_ACTION_MESSAGE.includes('create-resolving-ticket.js'), 'C002: NOT-resolved directive names the script');
- UT: [C003-inv] the message is English, self-contained, Action-directive
  assert(typeof RESOLVED_CANDIDATES_ACTION_MESSAGE === 'string' && !RESOLVED_CANDIDATES_ACTION_MESSAGE.includes('\n'), 'C003: single self-contained line');
- UT: [C004-post] preflight-stub-cleanup.test.js asserts the resolvedCandidates [ACTION] message
  const fs = require('fs');
  const testContent = fs.readFileSync('.claude/scripts/tickets/tests/preflight-stub-cleanup.test.js', 'utf8');
  assert(testContent.includes('RESOLVED_CANDIDATES_ACTION_MESSAGE'), 'C004: test asserts the message');

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| resolvedCandidates [ACTION] only says 'verify + remove' | RESOLVED_CANDIDATES_ACTION_MESSAGE with both branches (remove-stub.js / create-resolving-ticket.js + rewrite the marker key) | Added NOT-resolved branch to the resolvedCandidates [ACTION] so a fresh session creates a resolving ticket |
| message hardcoded inline in main() | RESOLVED_CANDIDATES_ACTION_MESSAGE constant exported and used in main() | Extracted the message for testability |
| preflight test asserted classification only | preflight test asserts the [ACTION] message (both branches, single line) | Added PX-126 test coverage |

## Notes in Prior Implementation Rounds

Implementation steps: (1) edit the resolvedCandidates [ACTION] string; (2) update the test; (3) run the preflight test.
Risks: keep the message single-line and self-contained per the Output message convention.
Caveats: the NOT-resolved branch must reference the exact script name create-resolving-ticket.js so the AI finds it.
Open items: none.
Future improvements: none.
Implementation summary:
- Changed files: preflight-stub-cleanup.js, tests/preflight-stub-cleanup.test.js
- Key changes: RESOLVED_CANDIDATES_ACTION_MESSAGE (both branches), export, main() uses it
- Test results: 19/0 passed; check-ambiguous 0
Review report:
- Static quality check: passed (0 issues on changed source)
- Translatability: no issues (verb-phrase function names, no magic numbers, no debug debris)
- Tests: 19 assertions passed; phasify regression all passed
- Dependencies: consistency verified
- Issues found and fixes applied: none introduced by this ticket; cross-ticket annotation verified, AMBIGUOUS 0
- Contracts: C001-C005 fulfilled (verify-final-contracts exit 0)
- Targets: validate-ticket-targets exit 0

## PX-126 — implemented at 33 locations

### .claude/scripts/lib/create-ticket-from-source.js

- Line 53
```javascript
function findTicket(ticketsData, ticketKey) {
```

- Line 75
```javascript
function stripCompletedResidue(ticket) {
```

- Line 94
```javascript
function applySeedEdits(ticket, seed) {
```

- Line 111
```javascript
function resolveMaxRealPhase(result) {
```

- Line 130
```javascript
function appendTicketToPhase(phase, ticket) {
```

- Line 166
```javascript
function createTicketFromSource({ ticketsData, sourceKey, seed = {} }) {
```

### .claude/scripts/rfc-graph/phasify-omissions.js

- Line 98
```javascript
function parseArguments(argv) {
```

- Line 745
```javascript
function rewriteOutputStubKeys(output) {
```

- Line 779
```javascript
function rewriteSourceMarkerLines(stubs, oldToNew) {
```

- Line 1231
```javascript
function runPhasifyOmissions(opts) {
```

### .claude/scripts/tickets/create-deferral-ticket.js

- Line 43
```javascript
/**
```

- Line 98
```javascript
function buildMarkdownGuidance({ key, sourceKey }) {
```

- Line 141
```javascript
function parseArgs(argv) {
```

- Line 162
```javascript
function readStdin() {
```

- Line 171
```javascript
async function main() {
```

### .claude/scripts/tickets/create-resolving-ticket.js

- Line 45
```javascript
function createResolvingTicket({ ticketsData, sourceKey, seed, stubs = [] }) {
```

- Line 59
```javascript
function buildMarkdownGuidance({ key, sourceKey }) {
```

- Line 98
```javascript
function parseArgs(argv) {
```

- Line 117
```javascript
function readStdin() {
```

- Line 126
```javascript
async function main() {
```

### .claude/scripts/tickets/preflight-stub-cleanup.js

- Line 171
```javascript
function main() {
```

### .claude/scripts/tickets/tests/preflight-stub-cleanup.test.js

- Line 18
```javascript
function assert(condition, message) {
```

### .claude/tests/lib/create-ticket-from-source.test.js

- Line 20
```javascript
function assert(condition, message) {
```

- Line 26
```javascript
function assertStrictEqual(actual, expected, message) {
```

- Line 32
```javascript
function assertDeepStrictEqual(actual, expected, message) {
```

### .claude/tests/tickets/create-deferral-ticket.test.js

- Line 23
```javascript
function assert(condition, message) {
```

- Line 29
```javascript
function assertStrictEqual(actual, expected, message) {
```

- Line 53
```javascript
function makeData() {
```

### .claude/tests/tickets/create-resolving-ticket.test.js

- Line 23
```javascript
function assert(condition, message) {
```

- Line 29
```javascript
function assertStrictEqual(actual, expected, message) {
```

- Line 53
```javascript
function makeData() {
```

### .claude/tests/tickets/phasify-srcdir.test.js

- Line 24
```javascript
function assert(condition, message) {
```

- Line 30
```javascript
function assertStrictEqual(actual, expected, message) {
```
