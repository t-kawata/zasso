# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-129: Resolving-ticket creation: group markers by sourceKey into one ticket per unit + unify the ticket-adding family

**Ticket Key**: PX-129 · **Phase**: -1

---

## Background

【Goal】Eliminate per-marker ticket explosion: /find-omissions must queue one ticket per coherent unit, not one per STUB line
【Purpose】batch-create-resolving-tickets.js gains grouped-manifest support so markers sharing a sourceKey become ONE resolving ticket with all markers embedded in stubs[]
【Motivation】63 siprs STUBs produced ~63 queued tickets (observed); ~15-20 units is the rational count; the shared core (generic-ticket-creation.js) already supports seed.stubs[] but the batch path hard-codes stubs=[single marker] and core verification (createOne/C008) only honors stubs[0]
【Constraints】No change to deferral/crimeDeferral/bulk seed behavior; no change to insert-stub/remove-stub/update-stub; backward compatibility for legacy single-marker manifests; C002 all-or-nothing preserved; the grouped manifest format is the cross-ticket contract with PX-130

## Scope

- Path: .claude/scripts/tickets/batch-create-resolving-tickets.js
- Action: add grouped-manifest support + refactor per-entry loop
- Detail: accept {sourceKey, stubs:[{file,line,content},...], seed?}; build stubs=entry.stubs (not [single marker]); createdTickets counts groups (C010); extend C008/C003/C009 to every marker in a group
- Before/After: today 1 entry=1 marker=1 ticket -> after 1 group (N markers, one sourceKey)=1 ticket; legacy single-marker entry unchanged (C011)
- API: manifest entry gains optional stubs[] array; {file,line,content} stays valid for single-marker entries
- Schema: no Tickets.json schema change; ticket.stubs[] already exists and is embedded by createResolvingTicket (C006)
- Config: no new configuration
- Dep: no new dependencies
- Non-change item: deferral / crimeDeferral / bulk seed behavior preserved
- Non-change why: those flows are already correct and outside this ticket blast radius; only the resolving path has the per-marker gap
- Impact component: /find-omissions Step 1 (preflight -> batch-create-resolving-tickets), generic-ticket-creation.js core, find-omissions.md and sibling command docs
- Impact nature: ticket count drops from per-marker to per-unit; grouped manifest becomes the PX-130 handoff contract; API of the manifest grows a stubs[] field
- Impact response: tests/batch-create-resolving-tickets.test.cjs extended (C010/C011); generic-ticket-creation.test.cjs updated for multi-stub seeds; find-omissions.md updated

## Implementation Target Files

- `.claude/scripts/tickets/batch-create-resolving-tickets.js`
- `.claude/scripts/lib/generic-ticket-creation.js`
- `.claude/scripts/tickets/create-resolving-ticket.js`
- `.claude/commands/find-omissions.md`
- `tests/batch-create-resolving-tickets.test.cjs`
- `tests/generic-ticket-creation.test.cjs`

## Crimes — Must Be 100% Resolved in This Ticket (2 items — false_positive: 2)

### TS-001 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `manifest: [{ file: "src/a.rs", line: 5, content: "// [::STUB::] P9-99: reason -- Implement fix" }],`
- **Contracts**: C001

### TS-002 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `writeLine(srcFile, 5, "// [::STUB::] P9-9: edited after manifest build");`
- **Contracts**: C001

## Investigation

Evidence gathered from source:
1. batch-create-resolving-tickets.js createResolvingTickets loop hard-codes stubs=[{file,line,content}] per manifest entry -> one ticket per marker (the explosion root). createdTickets = created.length (per-entry count).
2. generic-ticket-creation.js createOne resolving branch verifies on-disk marker only for seed.stubs[0]; C008 duplicate file:line scan in createTickets only checks seed.stubs[0].
3. create-resolving-ticket.js createResolvingTicket embeds stubs[] correctly (res.ticket.stubs = stubs.map(...)) — the multi-marker capability already exists at the ticket level.
4. find-omissions.md:22 (Overview) and :119 (orphans table row) still name single-marker create-resolving-ticket.js while :122+ uses batch-create-resolving-tickets.js (PX-1) and generic-ticket-creation.js (PX-2) — stale references.
5. Family scripts create-deferral-ticket.js (deferral seed) and create-crime-deferral-ticket.js (crimeDeferral seed) already route through generic-ticket-creation.js createTickets.
6. tests/batch-create-resolving-tickets.test.cjs (356 lines) covers C001-C009; tests/generic-ticket-creation.test.cjs covers the core. Neither covers grouping (C010) or multi-stub core verification.

## Acceptance Criteria

- 【Happy path】Grouped manifest with 2 groups (each 2+ markers sharing a sourceKey) -> exactly 2 tickets; each ticket embeds its markers in stubs[] with new keys; every marker line rewritten to its new key; Tickets.json updated atomically
- 【Error case】A group containing a duplicate file:line (C008) or a divergent on-disk marker (C003/C009) -> zero writes; stderr lists each failure with file:line and an Action-directive
- 【Edge case】Legacy single-marker manifest still creates one ticket per entry (C011); existing C001-C009 tests stay green

## Invariants

【Normal establishment】Every created resolving ticket has exactly one sourceKey, non-PX, max-phase, status todo, and carries all of its group markers in stubs[]
【On error】Any validation failure (C008/C003/C009) leaves Tickets.json and all source files byte-identical (C002 all-or-nothing)
【Internal state】stubs[].content always references the new ticket key after rewrite (C006); no marker is ever written without a matching on-disk line
【Boundary】Group with 1 marker behaves identically to a legacy single-marker entry (C011); group with N markers produces exactly 1 ticket (C010)

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — manifest → batch-create-resolving-tickets.js

- **Precondition**: manifest is an array of entries; each entry has sourceKey (explicit or extractable) and stubs:[{file,line,content}]
- **Postcondition**: every entry resolves to a valid source ticket in Tickets.json; unresolvable entries are rejected
- **Invariant**: no entry is created without a resolvable sourceKey

### C002 — commit phase

- **Precondition**: all entries validated in memory
- **Postcondition**: Tickets.json written once and marker lines rewritten; any failure leaves both byte-identical (all-or-nothing)
- **Invariant**: no partial commit is observable

### C003 — on-disk marker verification

- **Precondition**: group has file/line anchors
- **Postcondition**: every marker in the group is verified against its on-disk line (contains [::STUB::], expected oldKey present)
- **Invariant**: a group is refused if any of its markers fails verification

### C004 — path resolution

- **Precondition**: manifest file paths are repo-relative
- **Postcondition**: paths resolve under the source root; escape paths are rejected
- **Invariant**: no path escapes the source root

### C005 — --no-write dry-run

- **Precondition**: --no-write flag passed
- **Postcondition**: full validation runs with zero side effects; dryRun:true in summary
- **Invariant**: dry-run never mutates Tickets.json or source files

### C006 — stubs embedding

- **Precondition**: one ticket created per group
- **Postcondition**: all group markers are embedded in ticket.stubs[] with the NEW key
- **Invariant**: stubs[].content always references the new ticket key

### C007 — idempotent re-run

- **Precondition**: markers reference tickets
- **Postcondition**: a marker already referencing an ACTIVE ticket is skipped; re-running never duplicates tickets
- **Invariant**: the operation is idempotent

### C008 — duplicate detection

- **Precondition**: manifest has multiple markers
- **Postcondition**: duplicate file:line across ALL markers in ALL groups is rejected
- **Invariant**: no two markers share a file:line

### C009 — on-disk divergence refusal

- **Precondition**: commit-time rewrite
- **Postcondition**: a marker line changed between validation and commit is refused
- **Invariant**: rewrite only applies to lines that still carry the expected oldKey

### C010 — grouping

- **Precondition**: manifest entry carries stubs:[...] with 2+ markers sharing a sourceKey
- **Postcondition**: exactly ONE ticket is created for the group; ticket count equals group count, not marker count
- **Invariant**: one ticket per (sourceKey, unit) group

### C011 — backward compatibility

- **Precondition**: legacy single-marker entry (file,line,content) without stubs[]
- **Postcondition**: one ticket per entry, identical to pre-change behavior
- **Invariant**: legacy manifest semantics are unchanged

## Boy Scout Rule

Translatability plan for the touched code:
- Extract the per-group creation into a named function createOneGroup (verb phrase) so the batch loop reads as prose: group -> verify all markers -> create one ticket.
- Rename createdTickets semantics to groupCount when grouping is in play; keep the summary field names stable to avoid confusing consumers.
- Add a shared forEachStub helper used by both createOne (core) and the C008 duplicate scan, eliminating the duplicated stubs[0] indexing that caused the latent bug.
- Add a doc-consistency grep test asserting find-omissions.md no longer references the single-marker create-resolving-ticket.js as a direct CLI.
- Keep every stdout/stderr message self-contained English with an Action-directive (existing output-message convention).

## Test Plan

### Unit Tests

- UT: [Normal] One group entry {sourceKey:P4-2, stubs:[m1,m2]} -> batch-create-resolving-tickets creates EXACTLY ONE ticket; ticket.stubs[] carries BOTH markers and each stubs[].content carries the new key (C010/C006)
- UT: [Error] Group where any marker on-disk line is missing / not a [::STUB::] line / no longer carries the expected oldKey -> whole run refuses with zero writes (C003/C009 for every marker)
- UT: [Boundary] Duplicate file:line across two markers inside one group, or across two groups -> rejected before any creation (C008)
- UT: [Invariant] createTickets (core) with a multi-stub resolving seed -> ONE ticket; on-disk verification and C008 duplicate scan cover ALL seed.stubs[], not only stubs[0]
- UT: [C007] Re-running the operation is idempotent: a marker already referencing an ACTIVE ticket (todo/in_progress/planned/remanded) is skipped; mixed group (active + completed) -> completed markers processed, active markers skipped; no duplicate tickets
- UT: [C011] Legacy single-marker manifest entry {file,line,content} without stubs[] -> one ticket per entry, identical to pre-change behavior
- UT: [C005] --no-write over a grouped manifest -> full validation, zero side effects, dryRun:true
- UT: [C002] All entries validated in memory before commit; a failing group (one bad marker) -> no partial state; Tickets.json and all source files byte-identical
- UT: [C001] Group entry with explicit sourceKey that does not exist in Tickets.json -> rejected with Action-directive
- UT: [C009] Commit-time rewrite refuses a marker whose on-disk line changed between validation and commit (no longer carries the expected oldKey)

### Integration Tests

- IT: find-omissions.md grouped manifest (one entry per unit with stubs[]) to batch-create-resolving-tickets.js --no-write (review) then commit: Tickets.json gains ONE ticket per group; every marker line rewritten to its new key
- IT: generic-ticket-creation.js createTickets with resolving seeds (multi-stub): atomic commit; all markers embedded in the ticket.stubs[]
- IT: Existing suites tests/batch-create-resolving-tickets.test.cjs (C001-C009) and tests/generic-ticket-creation.test.cjs remain green after the change (regression)
- IT: Deferral / crimeDeferral / bulk seeds still route through the core (generic-ticket-creation.js) unchanged (regression)

### Exceptions

- Item: command .md prose consistency (stale create-resolving-ticket.js references in find-omissions.md lines 22/119) — Reason: markdown prose is not executable and its readability is not testable by an automated assertion; this is a documentation consistency item, not a design defect. Alternative: a doc-consistency grep test asserts the stale reference strings are gone; prose quality is verified manually in review-ticket.
- Item: grouping judgment (which markers form a unit) — Reason: unit formation is an AI decision by design (PX-130), non-deterministic, and therefore not testable inside the batch tool; this is not a design defect because the batch tool mechanically groups only by the manifest explicit sourceKey+stubs[] and unit formation is specified upstream in PX-130. Alternative: the grouped manifest shape itself is unit-tested (C010).
- Item: crash-window atomicity of the two-phase commit across processes — Reason: the commit writes Tickets.json once then marker lines; cross-process crash atomicity is OS-level and not testable within this tool; this is not a design defect. Alternative: C002 all-or-nothing is tested at the in-memory boundary; filesystem crash safety is out of scope.

### Plan Test Code (concrete code)

- UT: [C001] Grouped manifest shape and sourceKey resolution
  const ws = makeWorkspace();
  const groupedEntry = { sourceKey: "P4-2", stubs: [
    { file: "src/a.rs", line: 5, content: "// [::STUB::] P4-2: reason one -- Implement one" },
    { file: "src/multi.rs", line: 23, content: "// [::STUB::] P4-2: reason two -- Implement two" },
  ]};
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [groupedEntry], sourceRoot: ws.sourceRoot });
  assert.strictEqual(res.success, true);
  assert.strictEqual(res.created[0].sourceKey, "P4-2");
- UT: [C002] Atomicity — a failing group leaves everything byte-identical
  const ws = makeWorkspace();
  const badGroup = { sourceKey: "P4-2", stubs: [{ file: "src/missing.rs", line: 1, content: "// [::STUB::] P4-2: x -- Implement y" }] };
  const ticketsBefore = fs.readFileSync(ws.ticketsPath, "utf8");
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [badGroup], sourceRoot: ws.sourceRoot });
  assert.strictEqual(res.success, false);
  assert.strictEqual(fs.readFileSync(ws.ticketsPath, "utf8"), ticketsBefore);
- UT: [C003] On-disk verification covers EVERY marker in a group
  const ws = makeWorkspace();
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [{
    sourceKey: "P4-2", stubs: [
      { file: "src/a.rs", line: 5, content: "// [::STUB::] P4-2: one -- Implement one" },
      { file: "src/multi.rs", line: 999, content: "// [::STUB::] P4-2: two -- Implement two" }, // out of range
    ]}], sourceRoot: ws.sourceRoot });
  assert.strictEqual(res.success, false, "a group must refuse when ANY marker fails");
- UT: [C004] Path escape rejection
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [{
    sourceKey: "P4-2", stubs: [{ file: "../escape.rs", line: 1, content: "// [::STUB::] P4-2: x -- Implement y" }] }], sourceRoot: ws.sourceRoot });
  assert.strictEqual(res.success, false);
- UT: [C005] --no-write dry-run over a grouped manifest
  const ws = makeWorkspace();
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [groupedEntry], sourceRoot: ws.sourceRoot, noWrite: true });
  assert.strictEqual(res.success, true);
  assert.strictEqual(res.dryRun, true);
  assert.strictEqual(stubTicketCount(ws.ticketsPath), 0, "dry-run must not create tickets");
- UT: [C006] All group markers embedded with the new key
  const ws = makeWorkspace();
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [groupedEntry], sourceRoot: ws.sourceRoot });
  assert.strictEqual(stubTicketCount(ws.ticketsPath), 1);
  const stubs = res.created[0].ticket.stubs;
  assert.strictEqual(stubs.length, 2, "both group markers must be embedded");
  stubs.forEach(s => assert.ok(/\[::STUB::\]\s+P\d+-\d+/.test(s.content), "content must carry the new key"));
- UT: [C007] Idempotent re-run skips a group whose markers now reference active tickets
  const ws = makeWorkspace();
  bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [groupedEntry], sourceRoot: ws.sourceRoot });
  const second = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [groupedEntry], sourceRoot: ws.sourceRoot });
  assert.strictEqual(second.created.length, 0);
  assert.ok(second.skipped.length > 0);
- UT: [C008] Duplicate file:line across markers inside one group is rejected
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [{
    sourceKey: "P4-2", stubs: [
      { file: "src/a.rs", line: 5, content: "// [::STUB::] P4-2: one -- Implement one" },
      { file: "src/a.rs", line: 5, content: "// [::STUB::] P4-2: two -- Implement two" },
    ]}], sourceRoot: ws.sourceRoot });
  assert.strictEqual(res.success, false);
- UT: [C009] Commit-time divergence of any group marker is refused
  const ws = makeWorkspace();
  writeLine(path.join(ws.sourceRoot, "src/a.rs"), 5, "// [::STUB::] P4-2: edited after manifest build");
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [groupedEntry], sourceRoot: ws.sourceRoot });
  assert.strictEqual(res.success, false);
- UT: [C010] One ticket per group, not per marker
  const ws = makeWorkspace();
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: [
    { sourceKey: "P4-2", stubs: [m1, m2] },
    { sourceKey: "P3-2", stubs: [m3] },
  ], sourceRoot: ws.sourceRoot });
  assert.strictEqual(res.success, true);
  assert.strictEqual(res.created.length, 2, "2 groups -> 2 tickets, not 3 markers");
  assert.strictEqual(stubTicketCount(ws.ticketsPath), 2);
- UT: [C011] Legacy single-marker manifest unchanged
  const ws = makeWorkspace();
  const res = bcr.runBatchCreate({ ticketsPath: ws.ticketsPath, manifest: manifestForA(), sourceRoot: ws.sourceRoot });
  assert.strictEqual(res.success, true);
  assert.strictEqual(res.created.length, 1);
  assert.strictEqual(stubTicketCount(ws.ticketsPath), 1);

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| batch-create-resolving-tickets.js: one manifest entry = one marker = one ticket | grouped entries {sourceKey, stubs:[...]} create ONE ticket per unit; C008/C003/C009/C007 extended to every marker in a group | Added grouped-manifest support (C010) with backward compat for legacy single-marker entries (C011) |
| generic-ticket-creation.js createOne/C008 verified only seed.stubs[0] | createOne verifies every seed.stubs[] marker; C008 duplicate scan covers all stubs | Closed the stubs[0]-only latent gap; multi-stub resolving seeds fully verified |
| find-omissions.md referenced single-marker create-resolving-ticket.js at Overview and orphans row | both reference batch-create-resolving-tickets.js; grouped manifest format documented | Stale references fixed; PX-129 grouped entry shape documented |
| validate-ticket-targets.js leaked a phantom unknown entry when targetStubs was the string verified_empty and targetCrimes an array | only arrays contribute to allTargets | Boy Scout fix for the mixed-state concat leak |

## Notes in Prior Implementation Rounds

【Implementation steps】1) extend manifest shape + createResolvingTickets grouping (C010) 2) extend core createOne + C008 to scan every stubs[] entry 3) extend tests/batch-create-resolving-tickets.test.cjs with C010/C011 and generic-ticket-creation.test.cjs for multi-stub seeds 4) fix find-omissions.md stale references (lines 22/119) and document grouped manifest 5) run make test-stub-resolution + test-rfc-graph + full suite until green
【Risks】Grouping changes count semantics; C011 backward-compat tests are mandatory; core currently checks only stubs[0] (createOne + C008) — closing that gap is a behavior change for multi-stub seeds, must be covered by tests
【Caveats】The on-disk marker verification and rewrite (C003/C009) must handle every marker in a group; partial-group failure must abort the whole run (C002)
【Open items】Whether find-omissions auto-groups by sourceKey or requires explicit grouped entries — decided: explicit grouped manifest, and PX-130 emits it
【Future】PX-130 (STUB consolidation command) consumes this grouped manifest to hand consolidated STUB units to find-omissions without per-marker ticket explosion
Implementation summary:
- Files changed: batch-create-resolving-tickets.js, generic-ticket-creation.js, validate-ticket-targets.js, find-omissions.md, tests/batch-create-resolving-tickets.test.cjs, tests/fixtures/mini-tickets.json, tests/fixtures/src/p3.rs
- Tests: make test-stub-resolution 26/26, make test-rfc-graph 932/932, generic-ticket-creation.test.cjs 10/10, validate-ticket-targets.test.js 1/1
- Full suite: 1820 passed / 16 failed (all 16 pre-existing: hooks/*, lib/resolve-ecc-root, create-deferral/crime-deferral parseArgs; verified identical without my changes)
- Malfeasance crimes 2,3 (test fixture P9-99/P9-9) marked false_positive
- PX-130 cross-ticket contract: grouped manifest {sourceKey, stubs:[{file,line,content}]} confirmed as the handoff format
Review report (completed 2026-08-05):
- Static quality check: passed (0 issues)
- Translatability: no issues (no single-letter vars, no TODO/FIXME debris)
- Tests: update-stub 18/18, print-manifest 6/6, batch-create 26/26, generic-ticket-creation 10/10 (60/60); make test-stub-resolution 26/26; make test-rfc-graph 932/932
- No-excuse gate: 0 failures
- Malfeasance: 0 open crimes (test-fixture strings marked false_positive)
- Annotation: changed files carry ticket-key provenance for the tickets that modified them (cumulative-diff verify for earlier tickets flags later-ticket files, which is expected)
- AMBIGUOUS markers: 0 (p3.rs fixture annotations consolidated)

## PX-129 — implemented at 6 locations

### .claude/scripts/lib/generic-ticket-creation.js

- Line 78
```javascript
function createOne(data, seed, sourceRoot) {
```

- Line 143
```javascript
function createTickets({ ticketsData, seeds, sourceRoot, noWrite = false }) {
```

### .claude/scripts/tickets/batch-create-resolving-tickets.js

- Line 140
```javascript
function validateManifestShape(manifest) {
```

- Line 235
```javascript
function createResolvingTickets({ ticketsData, manifest, sourceRoot, noWrite = false }) {
```

### .claude/scripts/tickets/validate-ticket-targets.js

- Line 406
```javascript
function validateTargets(ticketsData, ticketKey) {
```

### tests/fixtures/src/p3.rs

- Line 1
```rust
// [::TICKET::] PX-130 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=PX-130 --for-spec --no-implementation-order`.
```
