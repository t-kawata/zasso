# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-130: STUB consolidation slash command + update-stub.js: normalize STUB markers into ticket-sized units by resolve key

**Ticket Key**: PX-130 · **Phase**: -1

---

## Background

【Goal】Re-organize existing STUB markers so each maps to one ticket-sized unit, eliminating sub-ticket fragments
【Purpose】A new slash command + update-stub.js edit marker lines to normalize the resolve key per unit; markers stay at their sites (provenance preserved); only true duplicates are removed
【Motivation】crates/siprs measurement: 63 markers, only ~44% are at least one ticket of work; fragments split one ticket into many slivers (FFI callbacks x6, wiring x5, constant swaps x5). A human would bundle or do them inline. Key normalization lets /find-omissions (via PX-129 grouped manifest) create one ticket per unit instead of one per marker
【Constraints】No new ticket creation (delegated to /find-omissions); no change to insert-stub/remove-stub/batch-create-resolving-tickets/generic-ticket-creation; completed-key re-pointing allowed as the find-omissions re-ticketization signal; the grouped manifest format is the cross-ticket contract with PX-129

## Scope

- Path: .claude/commands/consolidate-stubs.md
- Action: new slash command (enumerate -> group -> normalize -> remove duplicates -> validate -> emit handoff)
- Detail: workflow runs find-all-stubs.js, AI groups fragments into units by unlock-dependency + domain coherence, update-stub.js re-points each unit to one key, remove-stub.js merges true duplicates, gates run, handoff manifest is printed
- Before/After: 63 scattered markers -> N coherent units each with one resolve key; /find-omissions consumes the grouped manifest via PX-129
- API: update-stub.js CLI --file --line --resolve-by-ticket [--stub-reason] [--resolve-plan]; completed-key re-pointing allowed; Tickets.json is always ./Tickets.json (CLI)
- Schema: no Tickets.json change; the command never writes Tickets.json (C008)
- Config: no new configuration
- Dep: no new dependencies
- Non-change item: no new ticket creation; units without a covering ticket are emitted as find-omissions candidates only
- Non-change why: ticket creation is /find-omissions responsibility (via PX-129 grouped manifest); this command is marker-editing only
- Impact component: existing STUB state under a target directory; /find-omissions preflight input
- Impact nature: STUB markers re-organized into ticket-sized units; the marker-to-ticket ratio becomes rational
- Impact response: /find-omissions Step 1 preflight sees coherent units; PX-129 batch-create creates one ticket per unit

## Implementation Target Files

- `.claude/commands/consolidate-stubs.md`
- `.claude/scripts/tickets/update-stub.js`
- `.claude/scripts/tickets/insert-stub.js`
- `.claude/scripts/tickets/remove-stub.js`
- `tests/update-stub.test.js`

## Crimes — Must Be 100% Resolved in This Ticket (2 items — false_positive: 2)

### TS-001 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `manifest: [{ file: "src/a.rs", line: 5, content: "// [::STUB::] P9-99: reason -- Implement fix" }],`
- **Contracts**: C001

### TS-002 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `writeLine(srcFile, 5, "// [::STUB::] P9-9: edited after manifest build");`
- **Contracts**: C001

## Investigation

Evidence gathered from source and prior conversation:
1. crates/siprs measurement: 63 STUB markers; 44% are at least one ticket of work; 56% are sub-ticket fragments. Fragment clusters: ffi/callback.rs 6 markers (one callback bridge), audio_worker.rs 5 (accessor exposure), reactor.rs 3 (wiring), pj_status_t constants across error/* and state/* (5 mechanical swaps), build.rs 2 (duplicate).
2. find-all-stubs.js enumerates [::STUB::] markers with file:line:content; insert-stub.js validates resolve-by-ticket (future todo, non-PX, no-excuse); remove-stub.js deletes a marker at file:line.
3. No update-stub.js exists today — editing a marker means remove+insert, losing the anchor or risking double markers.
4. PX-129 defines the grouped manifest {sourceKey, stubs:[{file,line,content}]} consumed by batch-create-resolving-tickets.js; find-omissions Step 1 classifies completed-key markers as resolvedCandidates -> create one resolving ticket per group.
5. Design decision: markers stay at their sites (provenance); key normalization is the consolidation mechanism; only true duplicates are removed with survivor enumeration.

## Acceptance Criteria

- 【Happy path】Over a fixture of 10 scattered fragments across 4 files -> grouped into 2-3 units -> every marker re-pointed to its unit key -> handoff manifest has 2-3 entries -> no-excuse gate passes
- 【Error case】update-stub.js with a PX-* key or a terminal-excuse plan -> rejected with an Action-directive and zero writes
- 【Edge case】A true duplicate pair in one file -> merged to one marker whose reason enumerates the covered lines; no location is lost

## Invariants

【Normal establishment】Every STUB site keeps exactly one marker at its file:line; each unit has exactly one resolve key; the handoff manifest is {sourceKey, stubs:[{file,line,content}]}
【On error】update-stub.js validation failure leaves the marker byte-identical; no partial edit is observable
【Internal state】no marker carries a terminal excuse; no stub site loses its location annotation; the command never writes Tickets.json
【Boundary】a unit with one marker behaves like a simple re-point; a unit with N markers emits one manifest entry with N stubs

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — consolidate-stubs → update-stub.js

- **Precondition**: an existing marker line exists at file:line
- **Postcondition**: the marker is edited in place at the same anchor
- **Invariant**: no other line in the file changes (surgical edit)

### C002 — update-stub.js key validation

- **Precondition**: resolve-by-ticket provided
- **Postcondition**: key exists in Tickets.json; MUST RESOLVE and PX-* are rejected; a COMPLETED key is allowed (find-omissions handoff)
- **Invariant**: every marker keeps a resolvable, non-PX, non-MUST key

### C003 — resolve plan validation

- **Precondition**: resolve-plan provided
- **Postcondition**: plan contains an AI-executable work item; terminal excuses are rejected
- **Invariant**: no marker carries a terminal excuse

### C004 — surgical edit

- **Precondition**: update-stub.js invoked
- **Postcondition**: only the target marker line changes; the anchor file:line is preserved
- **Invariant**: source files are not otherwise modified

### C005 — true-duplicate removal

- **Precondition**: two markers share the same defect and same region
- **Postcondition**: one survives; the survivor enumerates the covered line span
- **Invariant**: no stub site loses its location annotation

### C006 — unit key normalization

- **Precondition**: a unit has N markers
- **Postcondition**: all N markers share exactly one resolve key
- **Invariant**: one key per unit

### C007 — handoff manifest

- **Precondition**: consolidation completes
- **Postcondition**: output is {sourceKey, stubs:[{file,line,content},...]} per unit, matching the PX-129 grouped manifest
- **Invariant**: the manifest is directly consumable by batch-create-resolving-tickets.js (PX-129)

### C008 — no ticket creation

- **Precondition**: a unit has no existing covering ticket
- **Postcondition**: the unit is emitted as a find-omissions candidate; no ticket is created
- **Invariant**: this command never writes Tickets.json

### C009 — post-consolidation gates

- **Precondition**: consolidation completes
- **Postcondition**: validate-no-external-excuses.js exits 0 and validate-stub-format.js passes
- **Invariant**: consolidated markers are excuse-free and well-formed

### C010 — idempotency

- **Precondition**: command re-run on an already-consolidated tree
- **Postcondition**: no further edits are made
- **Invariant**: the command converges

## Boy Scout Rule

Translatability plan for the touched code:
- update-stub.js reads as prose: validateKey -> validatePlan -> rewriteMarkerLine, each a named verb-phrase function.
- The consolidate-stubs command workflow is expressed as a sequence of verb phrases: enumerate -> group -> normalize -> mergeDuplicates -> runGates -> emitHandoff.
- Hardcoded validation constants (active/completed statuses, excuse lexicon) are shared with validate-no-external-excuses.js via import, not duplicated.
- Every stdout/stderr message is self-contained English with an Action-directive (existing convention).
- The grouping acceptance test (one implementer, one pass) is documented in the command so future maintainers understand the judgment criterion.

## Test Plan

### Unit Tests

- UT: update-stub.js edits the marker at file:line preserving the anchor; only the marker line changes (C004)
- UT: when resolve-by-ticket is provided, update-stub.js accepts re-pointing to a COMPLETED ticket key (find-omissions handoff) and to an active future todo; rejects MUST RESOLVE, PX-*, non-existent keys (C002)
- UT: when resolve-plan is provided, update-stub.js rejects a terminal-excuse plan and accepts an AI-executable work item (C003)
- UT: consolidation keeps every stub site marker; only a true duplicate (same defect + same region) is removed and the survivor enumerates the covered lines (C005)
- UT: a unit with N markers sharing one resolve key emits exactly one handoff manifest entry {sourceKey, stubs:[N entries]} (C006/C007)
- UT: a unit with no existing covering ticket is emitted as a find-omissions candidate, NOT created as a ticket (C008)
- UT: post-consolidation the no-excuse gate passes (zero terminal excuses) (C009)
- UT: the command is idempotent — re-running on an already-consolidated tree produces no further edits (C010)

### Integration Tests

- IT: fixture resembling crates/siprs (scattered fragments across files) -> consolidate-stubs -> markers normalized to one key per unit -> find-omissions preflight classifies units as resolvedCandidates -> batch-create-resolving-tickets.js (PX-129) creates ONE ticket per unit
- IT: update-stub.js + remove-stub.js interop: a same-file cluster merges to one marker with enumerated lines; cross-file markers keep their sites but share the unit key
- IT: handoff manifest emitted by the command is accepted verbatim by batch-create-resolving-tickets.js --no-write (PX-129 grouped format)

### Exceptions

- Item: unit formation (which markers group together) — Reason: grouping is an AI judgment by design, non-deterministic, and therefore not testable as a mechanical assertion; this is not a design defect because the command separates judgment (AI) from mechanical emission (script). Alternative: the mechanical parts are unit-tested (grouped-manifest emission, key validation, duplicate removal) and unit formation is reviewed in review-ticket.
- Item: slash command orchestration prose — Reason: markdown command instructions are not executable and their readability is not testable by an automated assertion; this is not a design defect. Alternative: each mechanical step maps to a script whose behavior is unit-tested; prose quality verified in review-ticket.

### Plan Test Code (concrete code)

- UT: [C001] update-stub.js edits the marker at file:line preserving the anchor
  const ws = makeWorkspace();
  const res = updateStub({ file: srcA, line: 5, resolveByTicket: "P4-2", stubReason: "reason", resolvePlan: "Implement fix", ticketsPath });
  assert.strictEqual(res.success, true);
  assert.ok(readLine(srcA, 5).includes("// [::STUB::] P4-2: reason -- Implement fix"));
- UT: [C002] when resolve-by-ticket is provided, update-stub.js accepts a COMPLETED key and an active future todo; rejects MUST RESOLVE, PX-*, non-existent keys
  const r1 = updateStub({ file: srcA, line: 5, resolveByTicket: "PX-999", ticketsPath });
  assert.strictEqual(r1.success, false);
  const r2 = updateStub({ file: srcA, line: 5, resolveByTicket: "PX-5", ticketsPath });
  assert.strictEqual(r2.success, false);
  const r3 = updateStub({ file: srcA, line: 5, resolveByTicket: "P4-2", ticketsPath });
  assert.strictEqual(r3.success, true, "completed-key re-pointing allowed");
- UT: [C003] when resolve-plan is provided, update-stub.js rejects a terminal excuse and accepts an AI-executable work item
  const bad = updateStub({ file: srcA, line: 5, resolveByTicket: "P4-2", resolvePlan: "awaiting external approval", ticketsPath });
  assert.strictEqual(bad.success, false);
  const good = updateStub({ file: srcA, line: 5, resolveByTicket: "P4-2", resolvePlan: "Vendor and build PJSIP in build.rs", ticketsPath });
  assert.strictEqual(good.success, true);
- UT: [C004] update-stub.js invoked edits only the target marker line (surgical edit)
  const before = fs.readFileSync(srcA, "utf8");
  updateStub({ file: srcA, line: 5, resolveByTicket: "P4-2", stubReason: "r", resolvePlan: "p", ticketsPath });
  const after = fs.readFileSync(srcA, "utf8");
  assert.strictEqual(after.split("\n")[4], "// [::STUB::] P4-2: r -- p");
  assert.strictEqual(after.split("\n").length, before.split("\n").length);
- UT: [C005] true-duplicate removal: same-defect same-region markers merge; survivor enumerates covered lines
  const merged = mergeTrueDuplicates([{ file: "build.rs", line: 8, content: "// [::STUB::] P4-2: bindgen disabled -- Enable bindgen" }, { file: "build.rs", line: 11, content: "// [::STUB::] P4-2: bindgen disabled -- Enable pjsua-native" }]);
  assert.strictEqual(merged.kept.length, 1);
  assert.ok(merged.kept[0].content.includes("build.rs:8,11"));
  const distinct = mergeTrueDuplicates([{ file: "a.rs", line: 5, content: "// [::STUB::] P4-2: one -- one" }, { file: "b.rs", line: 9, content: "// [::STUB::] P4-2: two -- two" }]);
  assert.strictEqual(distinct.kept.length, 2, "different regions never merge");
- UT: [C006] a unit with N markers shares exactly one resolve key
  const unit = { markers: [m1, m2, m3] };
  const normalized = normalizeUnitKey(unit, ticketsData);
  const keys = new Set(normalized.markers.map(m => m.resolveByTicket));
  assert.strictEqual(keys.size, 1, "one key per unit");
- UT: [C007] consolidation completes emits one handoff manifest entry per unit {sourceKey, stubs:[{file,line,content}]}
  const manifest = emitHandoffManifest(units, ticketsData);
  assert.strictEqual(manifest.length, units.length);
  for (const entry of manifest) {
    assert.ok(entry.sourceKey);
    assert.ok(Array.isArray(entry.stubs) && entry.stubs.length > 0);
    for (const s of entry.stubs) assert.ok(s.file && s.line && s.content.includes("[::STUB::]"));
  }
- UT: [C008] a unit with no existing covering ticket is emitted as a find-omissions candidate; the command never writes Tickets.json
  const before = JSON.stringify(ticketsData);
  runConsolidation({ dir, ticketsData });
  assert.strictEqual(JSON.stringify(ticketsData), before, "Tickets.json untouched");
- UT: [C009] post-consolidation the no-excuse gate passes (zero terminal excuses)
  runConsolidation({ dir, ticketsData });
  const excuses = validateNoExternalExcuses(markers);
  assert.strictEqual(excuses.length, 0);
- UT: [C010] the command is idempotent — re-running on an already-consolidated tree produces no edits
  runConsolidation({ dir, ticketsData });
  const before = fs.readFileSync(srcA, "utf8");
  runConsolidation({ dir, ticketsData });
  const after = fs.readFileSync(srcA, "utf8");
  assert.strictEqual(before, after);

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| no update-stub.js — editing a marker required remove+insert, losing the anchor | update-stub.js edits a marker line in place (key/reason/plan), preserving the anchor; completed-key re-pointing allowed | New marker-edit primitive (C001-C004) |
| no consolidated handoff; /find-omissions queued one ticket per marker | consolidate-stubs.md command + emitHandoffManifest produce the PX-129 grouped manifest {sourceKey, stubs:[...]} — one ticket per unit | Consolidation command and cross-ticket handoff contract (C006-C008) |
| fragment markers duplicated the same defect at multiple lines | mergeTrueDuplicates merges same-defect same-region markers; survivor enumerates covered lines | True-duplicate merging with location preservation (C005) |
| Step 6 described the handoff manifest necessity without a mechanical means | print-manifest-for-find-omissions.js reads units JSON on stdin and emits the grouped manifest (stdout) + counts (stderr); Step 6 is now a one-liner script run | New dedicated manifest printer; update-stub.js stays focused on marker editing |

## Notes in Prior Implementation Rounds

【Implementation steps】1) implement update-stub.js (key/reason/plan edit with completed-key re-pointing) 2) write tests for update-stub.js 3) implement the consolidate-stubs command workflow (enumerate/group/normalize/remove/gate/handoff) 4) emit the PX-129 grouped manifest 5) verify gates green
【Risks】Grouping is AI judgment — the command must separate judgment (AI) from mechanical execution (scripts); unit formation errors surface at review, not mechanically
【Caveats】update-stub.js must never edit a line that is not the target marker (surgical edit); remove-stub.js only for true duplicates with survivor enumeration
【Open items】command name (working title: consolidate-stubs); whether the command also rewrites keys already pointing to an active future todo (yes: normalize to unit key)
【Future】PX-129 (grouped manifest) makes /find-omissions consume this output without per-marker explosion; running this command over crates/siprs is the first real exercise
Implementation summary:
- Files added: .claude/scripts/tickets/update-stub.js, .claude/commands/consolidate-stubs.md, tests/update-stub.test.cjs
- Files touched: tests/fixtures/src/p3.rs (annotations reorganized; STUB stays at line 7)
- Tests: update-stub 16/16, make test-stub-resolution 26/26, make test-rfc-graph 932/932
- Cross-ticket contract verified: grouped manifest {sourceKey, stubs:[...]} -> batch-create-resolving-tickets.js --no-write created 1 ticket for a 2-marker group
- update-stub.js NEVER writes Tickets.json (C008); completed-key re-pointing is the find-omissions re-ticketization signal
- Malfeasance crimes 2,3 remain false_positive (test fixtures)
Follow-up polish: added .claude/scripts/tickets/print-manifest-for-find-omissions.js + tests/print-manifest-for-find-omissions.test.cjs; consolidate-stubs.md Step 6 now runs the script (echo units | node print-manifest-for-find-omissions.js | node batch-create-resolving-tickets.js --no-write). Tests: 57/57 (update-stub 18, print-manifest 3, batch 26, generic 10).
Review report (completed 2026-08-05):
- Static quality check: passed (0 issues)
- Translatability: no issues (no single-letter vars, no TODO/FIXME debris)
- Tests: update-stub 18/18, print-manifest 6/6, batch-create 26/26, generic-ticket-creation 10/10 (60/60); make test-stub-resolution 26/26; make test-rfc-graph 932/932
- No-excuse gate: 0 failures
- Malfeasance: 0 open crimes (test-fixture strings marked false_positive)
- Annotation: changed files carry ticket-key provenance for the tickets that modified them (cumulative-diff verify for earlier tickets flags later-ticket files, which is expected)
- AMBIGUOUS markers: 0 (p3.rs fixture annotations consolidated)

## PX-130 — implemented at 18 locations

### .claude/scripts/lib/generic-ticket-creation.js

- Line 78
```javascript
function createOne(data, seed, sourceRoot) {
```

- Line 143
```javascript
function createTickets({ ticketsData, seeds, sourceRoot, noWrite = false }) {
```

### .claude/scripts/tickets/batch-create-resolving-tickets.js

- Line 140
```javascript
function validateManifestShape(manifest) {
```

- Line 235
```javascript
function createResolvingTickets({ ticketsData, manifest, sourceRoot, noWrite = false }) {
```

### .claude/scripts/tickets/update-stub.js

- Line 48
```javascript
function parseMarker(content) {
```

- Line 76
```javascript
function buildMarkerLine(current, key, reason, plan, unitId) {
```

- Line 90
```javascript
function validateResolveKey(key, ticketsData) {
```

- Line 113
```javascript
function validateResolvePlan(markerText) {
```

- Line 134
```javascript
function updateStub({ file, line, resolveByTicket, stubReason, resolvePlan, unitId, ticketsPath }) {
```

- Line 205
```javascript
function mergeTrueDuplicates(markers) {
```

- Line 241
```javascript
function annotateCoveredLines(content, covered) {
```

- Line 253
```javascript
function normalizeUnitKey(unit) {
```

- Line 267
```javascript
function emitHandoffManifest(units) {
```

- Line 283
```javascript
function parseCliArgs(argv) {
```

- Line 299
```javascript
function readStdin() {
```

- Line 309
```javascript
function main() {
```

### .claude/scripts/tickets/validate-ticket-targets.js

- Line 406
```javascript
function validateTargets(ticketsData, ticketKey) {
```

### tests/fixtures/src/p3.rs

- Line 2
```rust
// [::TICKET::] PX-131 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=PX-131 --for-spec --no-implementation-order`.
```
