# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-131: Consolidated manifest as a file + mechanical [::UNIT::] cleanup: print-manifest-for-find-omissions.js writes ./manifests/CONSOLIDATED-MANIFEST-<timestamp>.json and update-stub.js gains --unit-id

**Ticket Key**: PX-131 · **Phase**: -1

---

## Background

【Goal】Make the consolidation pipeline fully mechanical after the AI grouping decision: no hand-assembled units JSON, no stdin echo
【Purpose】update-stub.js records unit membership as [::UNIT::<id>::] in each marker; print-manifest-for-find-omissions.js scans the tree, groups by unit id, writes ./manifests/CONSOLIDATED-MANIFEST-<ts>.json, then strips the tags on success
【Motivation】The current Step 6 forces the AI to reconstruct the grouping into JSON on stdin — an error-prone, non-mechanical step that contradicts the command design principle (AI judges, scripts execute)
【Constraints】The PX-129 grouped manifest shape is unchanged; emitHandoffManifest / normalizeUnitKey stay as-is; the [::UNIT::] tag is a transient intermediate state

## Scope

- Path: .claude/scripts/tickets/update-stub.js
- Action: add --unit-id=<id> CLI flag + unitId param to updateStub; embed [::UNIT::<id>::] after the reason in buildMarkerLine; preserve an existing unit tag on re-point when --unit-id is omitted
- Detail: buildMarkerLine(current, key, reason, plan, unitId) appends the unit tag; extractUnitId(content) parses it; parseMarker keeps reason clean of the tag
- Before/After: marker without tag // [::STUB::] P4-2: reason -- plan -> with tag // [::STUB::] P4-2: reason [::UNIT::U1::] -- plan
- API: update-stub.js CLI gains --unit-id; print-manifest-for-find-omissions.js loses stdin/stdout and becomes a scanner+writer with no input
- Schema: no Tickets.json change; the manifest file shape stays the PX-129 grouped contract
- Config: no new configuration
- Dep: no new dependencies
- Non-change item: emitHandoffManifest / normalizeUnitKey / mergeTrueDuplicates stay as-is
- Non-change why: those are already correct and C007-tested; only the collection/output path changes
- Impact component: /consolidate-stubs Step 4 and Step 6; validate-stub-format.js; tests/update-stub and tests/print-manifest
- Impact nature: the pipeline becomes fully mechanical after the AI grouping decision; the manifest persists as a file for /find-omissions
- Impact response: update-stub.test.cjs + print-manifest-for-find-omissions.test.cjs extended; consolidate-stubs.md Step 4/6 updated

## Implementation Target Files

- `.claude/scripts/tickets/print-manifest-for-find-omissions.js`
- `.claude/scripts/tickets/update-stub.js`
- `.claude/scripts/tickets/validate-stub-format.js`
- `.claude/commands/consolidate-stubs.md`
- `tests/print-manifest-for-find-omissions.test.cjs`
- `tests/update-stub.test.cjs`

## Crimes — Must Be 100% Resolved in This Ticket (6 items — false_positive: 6)

### TS-001 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `manifest: [{ file: "src/a.rs", line: 5, content: "// [::STUB::] P9-99: reason -- Implement fix" }],`
- **Contracts**: C001

### TS-002 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `writeLine(srcFile, 5, "// [::STUB::] P9-9: edited after manifest build");`
- **Contracts**: C001

### TS-003 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `{ sourceKey: "P3-2", stubs: [{ file: "src/p3.rs", line: 7, content: "// [::STUB::] P3-2: two -- Implement two" }] },`
- **Contracts**: C001

### TS-004 — `src/p3.rs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `// [::STUB::] P3-2: reason two -- Implement two`
- **Contracts**: C001

### TS-005 — `tests/print-manifest-for-find-omissions.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `"// c\npub fn demo3() {\n    let z = 3;\n    // [::STUB::] P3-2: audio [::UNIT::U2::] -- Implement audio worker\n    let _ = z;\n}\n"`
- **Contracts**: C001

### TS-006 — `tests/update-stub.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `{ file: "src/p3.rs", line: 7, content: "// [::STUB::] P3-2: three -- three", resolveByTicket: "P3-2" },`
- **Contracts**: C001

## Investigation

Current state: print-manifest-for-find-omissions.js reads units JSON on stdin and writes the manifest to stdout (PX-130). The grouping (unit membership) is NOT persisted — it lives only in the AI context, so the units JSON cannot be mechanically collected today. validate-stub-format.js uses STUB_REGEX = /^//s*[::STUB::]s+(P[A-Z0-9]+-d+|MUSTs+RESOLVE):s*S.*$/ which accepts any non-empty description after the key, so [::UNIT::<id>::] in the description is tolerated by the regex today — the format gate needs an explicit allowance + test. update-stub.js buildMarkerLine constructs // [::STUB::] <key>: <reason> -- <plan> and can gain a unitId parameter. emitHandoffManifest(units) already produces the PX-129 contract and is C007-tested.

## Acceptance Criteria

- Happy path: a tree with 2 units (3 tagged markers) -> run print-manifest-for-find-omissions.js -> manifests/CONSOLIDATED-MANIFEST-<ts>.json written with 2 entries, all [::UNIT::] tags removed from source, counts reported
- Error case: manifests dir unwritable -> error returned, no unit tags removed, no partial manifest
- Edge case: re-point without --unit-id preserves an existing unit tag; validate-stub-format accepts the tagged intermediate state

## Invariants

【Normal establishment】Every marker carrying a [::UNIT::<id>::] tag keeps a valid non-PX, non-MUST resolve key; the manifest file lands under ./manifests/
【On error】A failed manifest write leaves every [::UNIT::] tag in place and no manifest file (retryable); no partial state
【Internal state】unit membership is persisted in the markers as [::UNIT::<id>::]; the grouped manifest shape always matches the PX-129 contract {sourceKey, stubs:[{file,line,content}]}
【Boundary】a unit with one tagged marker emits one manifest entry with one stub; after cleanup zero [::UNIT::] tags remain

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — update-stub.js --unit-id

- **Precondition**: --unit-id provided
- **Postcondition**: the marker embeds [::UNIT::<id>::] after the reason
- **Invariant**: the unit tag is parseable and survives re-pointing

### C002 — unit membership persistence

- **Precondition**: a marker carries [::UNIT::<id>::]
- **Postcondition**: the unit id is recoverable from the marker content by the printer
- **Invariant**: the grouping decision is persisted as data, not reconstructed by the AI

### C003 — print-manifest scan+group

- **Precondition**: target tree contains tagged markers
- **Postcondition**: markers group by unit id; one {sourceKey, stubs:[...]} entry per unit
- **Invariant**: unit membership is derived mechanically from the tags

### C004 — manifest file write

- **Precondition**: grouping completes
- **Postcondition**: ./manifests/CONSOLIDATED-MANIFEST-<YYYYMMDDhhmmss>.json is written (manifests/ auto-created)
- **Invariant**: the manifest JSON shape matches the PX-129 grouped contract

### C005 — cleanup after write

- **Precondition**: manifest write succeeds
- **Postcondition**: every [::UNIT::<id>::] tag is removed from the source markers
- **Invariant**: a failed write leaves all tags in place (retryable)

### C006 — format gate

- **Precondition**: marker carries [::UNIT::<id>::]
- **Postcondition**: validate-stub-format.js accepts it
- **Invariant**: the intermediate consolidated state passes the format gate

## Boy Scout Rule

Translatability plan: update-stub.js buildMarkerLine reads as prose with an explicit unitId parameter; print-manifest-for-find-omissions.js is structured as scanTaggedMarkers -> groupByUnitId -> writeManifestFile -> stripUnitTags (verb phrases); the strip-tags transform is a named function stripUnitTagsFromMarkers; the timestamp format is a named constant (MANIFEST_TIMESTAMP_FORMAT); every stdout/stderr message is self-contained English with an Action-directive

## Test Plan

### Unit Tests

- UT: [C001] update-stub.js --unit-id=U1 embeds [::UNIT::U1::] in the marker after the reason
  const res = updateStub({ file: srcA, line: 5, resolveByTicket: "P4-2", stubReason: "codec deferred", resolvePlan: "Implement pjsua codec enumeration", unitId: "U1", ticketsPath });
  assert.ok(readLine(srcA, 5).includes("[::UNIT::U1::]"));
- UT: [C002] the grouping decision is persisted as data in the marker, not reconstructed by the AI — a tagged marker survives re-pointing without --unit-id, so the printer can recover the unit id from content
  updateStub({ file: srcA, line: 5, resolveByTicket: "P4-2", unitId: "U1", ticketsPath });
  updateStub({ file: srcA, line: 5, resolveByTicket: "P4-2", ticketsPath });
  assert.ok(readLine(srcA, 5).includes("[::UNIT::U1::]"));
- UT: [C003] print-manifest scans a tagged tree and groups by unit id
  const res = collectTaggedMarkers(ws.dir);
  assert.strictEqual(res.units.length, 2, "2 units");
  assert.strictEqual(res.units[0].sourceKey, "P4-2");
- UT: [C004] when grouping completes, the manifest is written to ./manifests/CONSOLIDATED-MANIFEST-<ts>.json (auto-creates manifests/)
  const out = runPrinter(ws.dir);
  assert.ok(fs.existsSync(out.manifestPath));
  assert.ok(out.manifestPath.includes("manifests/CONSOLIDATED-MANIFEST-"));
  const parsed = JSON.parse(fs.readFileSync(out.manifestPath, "utf8"));
  assert.strictEqual(parsed.length, 2, "one entry per unit");
- UT: [C005] after a successful write every [::UNIT::] tag is removed from source
  runPrinter(ws.dir);
  const lines = fs.readFileSync(srcA, "utf8");
  assert.ok(!lines.includes("[::UNIT::"), "tags stripped");
  assert.ok(lines.includes("[::STUB::]"), "markers remain clean");
- UT: [C005-inv] a failed write leaves all tags in place and returns an error
  fs.chmodSync(ws.dir, 0o444); // make manifests unwritable
  const out = runPrinter(ws.dir);
  assert.strictEqual(out.ok, false);
  assert.ok(fs.readFileSync(srcA, "utf8").includes("[::UNIT::"), "tags preserved for retry");
- UT: [C006] validate-stub-format accepts a marker containing [::UNIT::<id>::] and still rejects a malformed one
  assert.strictEqual(validateStubFormat("// [::STUB::] P4-2: reason [::UNIT::U1::] -- plan"), true);
  assert.strictEqual(validateStubFormat("no marker"), false);

### Integration Tests

- IT: tagged tree -> print-manifest-for-find-omissions.js -> manifests/CONSOLIDATED-MANIFEST-<ts>.json written, unit tags stripped -> the manifest file is accepted by batch-create-resolving-tickets.js --no-write (one ticket per unit)
- IT: consolidate-stubs.md Step 4 (update-stub --unit-id) -> Step 6 (print-manifest) end-to-end over a siprs-like fixture
- IT: Existing suites tests/update-stub.test.cjs, tests/print-manifest-for-find-omissions.test.cjs stay green
- IT: A marker carrying a unit tag passes the format gate after update-stub embeds it and before the printer strips it

### Exceptions

- Item: timestamped filename collision across concurrent runs — Reason: two concurrent runs picking the same second is non-deterministic external timing and therefore not testable as a deterministic assertion; this is not a design defect because the script refuses to overwrite an existing manifest file and errors. Alternative: the collision refusal is unit-tested and the manifest content itself is deterministic.
- Item: filesystem-level atomicity of write-then-cleanup — Reason: a crash between a successful write and the tag cleanup is an OS-level failure mode outside the script, hence not testable within this tool; this is not a design defect because cleanup runs only after a confirmed write. Alternative: the write-then-cleanup ordering is unit-tested; crash-window behavior is out of scope.

### Plan Test Code (concrete code)

- UT: [C001] update-stub.js --unit-id=U1 embeds [::UNIT::U1::] after the reason
  const ws = makeWorkspace();
  const res = updateStub({ file: ws.srcA, line: 5, resolveByTicket: "P4-2", stubReason: "codec deferred", resolvePlan: "Implement pjsua codec enumeration", unitId: "U1", ticketsPath: ws.ticketsPath });
  assert.strictEqual(res.success, true);
  assert.ok(readLine(ws.srcA, 5).includes("[::UNIT::U1::]"), "unit tag embedded");
- UT: [C002] the grouping decision is persisted as data in the marker, not reconstructed by the AI — a tagged marker survives re-pointing without --unit-id
  updateStub({ file: ws.srcA, line: 5, resolveByTicket: "P4-2", unitId: "U1", ticketsPath: ws.ticketsPath });
  updateStub({ file: ws.srcA, line: 5, resolveByTicket: "P4-2", ticketsPath: ws.ticketsPath });
  assert.ok(readLine(ws.srcA, 5).includes("[::UNIT::U1::]"), "tag survives re-point");
- UT: [C003] print-manifest scans a tagged tree and groups by unit id
  const res = collectTaggedMarkers(ws.dir);
  assert.strictEqual(res.units.length, 2, "2 units from 3 tagged markers");
  assert.strictEqual(res.units[0].sourceKey, "P4-2");
- UT: [C004] when grouping completes, the manifest is written to ./manifests/CONSOLIDATED-MANIFEST-<ts>.json (auto-creates manifests/)
  const out = runPrinter(ws.dir);
  assert.ok(fs.existsSync(out.manifestPath));
  assert.ok(out.manifestPath.includes("manifests/CONSOLIDATED-MANIFEST-"));
  const parsed = JSON.parse(fs.readFileSync(out.manifestPath, "utf8"));
  assert.strictEqual(parsed.length, 2, "one entry per unit");
  assert.strictEqual(parsed[0].stubs[0].content.includes("[::UNIT::"), false, "manifest carries clean markers");
- UT: [C005] after a successful write every [::UNIT::] tag is removed from source
  runPrinter(ws.dir);
  assert.ok(!fs.readFileSync(ws.srcA, "utf8").includes("[::UNIT::"), "tags stripped");
  assert.ok(fs.readFileSync(ws.srcA, "utf8").includes("[::STUB::]"), "markers remain clean");
- UT: [C005-inv] a failed write leaves all tags in place and returns an error
  fs.chmodSync(ws.dir, 0o444);
  const out = runPrinter(ws.dir);
  assert.strictEqual(out.ok, false);
  assert.ok(fs.readFileSync(ws.srcA, "utf8").includes("[::UNIT::"), "tags preserved for retry");
- UT: [C006] validate-stub-format accepts a marker containing [::UNIT::<id>::] and still rejects a malformed one
  assert.strictEqual(validateStubFormat("// [::STUB::] P4-2: reason [::UNIT::U1::] -- plan"), true);
  assert.strictEqual(validateStubFormat("no marker"), false);

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| print-manifest-for-find-omissions.js read units JSON on stdin and wrote the manifest to stdout | scans the tree for [::UNIT::<id>::]-tagged markers, groups by unit id, writes ./manifests/CONSOLIDATED-MANIFEST-<ts>.json, then strips all unit tags on success | File-based manifest output + mechanical unit-tag cleanup (C003-C005) |
| update-stub.js had no way to record unit membership | update-stub.js gains --unit-id=<id> which embeds [::UNIT::<id>::] after the reason; an existing tag survives re-pointing | Grouping decision persisted as data (C001-C002) |
| consolidate-stubs.md Step 6 required hand-assembled units JSON via echo | Step 6 is a single no-input script run; Step 4 documents --unit-id | Consolidation pipeline fully mechanical after the AI grouping judgment |

## Notes in Prior Implementation Rounds

【Implementation steps】1) update-stub.js: add --unit-id + preserve existing tag 2) print-manifest-for-find-omissions.js: scan tagged markers -> group by unit id -> write manifest file -> strip tags on success 3) validate-stub-format.js: allow [::UNIT::<id>::] 4) consolidate-stubs.md Step 4/6 5) tests
【Risks】Timestamped filename could collide within the same second — refuse overwrite; the strip-tags cleanup must only run after a confirmed write
【Caveats】The manifest stubs[].content must carry the CLEAN marker (unit tag stripped) so find-omissions rewrites clean lines
【Open items】None — design confirmed with the user
【Future】Running /consolidate-stubs over crates/siprs is the first real exercise; the written manifest feeds /find-omissions directly
Implementation summary:
- Files changed: update-stub.js (unitId), print-manifest-for-find-omissions.js (file output + cleanup), consolidate-stubs.md (Step 4/6), tests/update-stub.test.cjs, tests/print-manifest-for-find-omissions.test.cjs
- validate-stub-format.js needed NO change: STUB_REGEX already accepts [::UNIT::] in the free-form description (verified)
- Tests: update-stub 18 + print-manifest 6 + batch 26 + generic 10 = 60/60; make test-stub-resolution 26/26, make test-rfc-graph 932/932
- Manifest file lands at ./manifests/CONSOLIDATED-MANIFEST-<YYYYMMDDhhmmss>.json; tags stripped only on successful write (C005)
Review report (completed 2026-08-05):
- Static quality check: passed (0 issues)
- Translatability: no issues (no single-letter vars, no TODO/FIXME debris)
- Tests: update-stub 18/18, print-manifest 6/6, batch-create 26/26, generic-ticket-creation 10/10 (60/60); make test-stub-resolution 26/26; make test-rfc-graph 932/932
- No-excuse gate: 0 failures
- Malfeasance: 0 open crimes (test-fixture strings marked false_positive)
- Annotation: changed files carry ticket-key provenance for the tickets that modified them (cumulative-diff verify for earlier tickets flags later-ticket files, which is expected)
- AMBIGUOUS markers: 0 (p3.rs fixture annotations consolidated)

## PX-131 — implemented at 28 locations

### .claude/scripts/lib/generic-ticket-creation.js

- Line 78
```javascript
function createOne(data, seed, sourceRoot) {
```

- Line 143
```javascript
function createTickets({ ticketsData, seeds, sourceRoot, noWrite = false }) {
```

### .claude/scripts/tickets/batch-create-resolving-tickets.js

- Line 140
```javascript
function validateManifestShape(manifest) {
```

- Line 235
```javascript
function createResolvingTickets({ ticketsData, manifest, sourceRoot, noWrite = false }) {
```

### .claude/scripts/tickets/print-manifest-for-find-omissions.js

- Line 38
```javascript
function scanStubs(dir) {
```

- Line 51
```javascript
function stripUnitTag(content) {
```

- Line 61
```javascript
function scanTaggedMarkers(dir) {
```

- Line 73
```javascript
function groupByUnitId(taggedMarkers) {
```

- Line 91
```javascript
function timestamp() {
```

- Line 112
```javascript
function writeManifestFile(manifest, cwd) {
```

- Line 132
```javascript
function stripUnitTags(taggedMarkers) {
```

- Line 157
```javascript
function runPrinter(dir) {
```

- Line 186
```javascript
function main() {
```

### .claude/scripts/tickets/update-stub.js

- Line 48
```javascript
function parseMarker(content) {
```

- Line 60
```javascript
function extractUnitId(content) {
```

- Line 76
```javascript
function buildMarkerLine(current, key, reason, plan, unitId) {
```

- Line 90
```javascript
function validateResolveKey(key, ticketsData) {
```

- Line 113
```javascript
function validateResolvePlan(markerText) {
```

- Line 134
```javascript
function updateStub({ file, line, resolveByTicket, stubReason, resolvePlan, unitId, ticketsPath }) {
```

- Line 205
```javascript
function mergeTrueDuplicates(markers) {
```

- Line 241
```javascript
function annotateCoveredLines(content, covered) {
```

- Line 253
```javascript
function normalizeUnitKey(unit) {
```

- Line 267
```javascript
function emitHandoffManifest(units) {
```

- Line 283
```javascript
function parseCliArgs(argv) {
```

- Line 299
```javascript
function readStdin() {
```

- Line 309
```javascript
function main() {
```

### .claude/scripts/tickets/validate-ticket-targets.js

- Line 406
```javascript
function validateTargets(ticketsData, ticketKey) {
```

### tests/fixtures/src/p3.rs

- Line 3
```rust
// p3 source fixture for batch-create-resolving-tickets tests
```
