# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-134: validate-stub-format.js: add tree-scan mode so the consolidate-stubs Step 5 format gate is runnable

**Ticket Key**: PX-134 · **Phase**: -1

---

## Background

【Goal】Make the /consolidate-stubs Step 5 format gate executable — the documented 'node validate-stub-format.js' with no argument always exits 1 with 'Missing input string'
【Purpose】validate-stub-format.js currently validates a single marker string; the gate needs a tree-wide mode that scans for STUB markers and validates each with the same per-string rule
【Motivation】The blocking three-part gate in consolidate-stubs.md can never pass because the second command fails on invocation alone; the contradiction review (PX-133 parent analysis) identified this as issue #1
【Constraints】validateStubFormat(content) signature stays; the scan mode composes the existing review/find-all-stubs.js scan with the existing validator; skip dirs follow find-all-stubs conventions (.claude, node_modules, fixtures, target, .git); the gate command in consolidate-stubs.md is updated to the scan mode

## Scope

- Path: .claude/scripts/tickets/validate-stub-format.js
- Action: add a --scan <dir> tree-scan mode to the existing single-string validator
- Detail: node validate-stub-format.js --scan . scans the directory via find-all-stubs.js (review/find-all-stubs.js), validates each returned marker with the existing validateStubFormat(content), prints per-failure file:line + each format error to stderr, prints JSON {total, pass, fail} to stdout, and exits 0 iff every marker is valid; the existing single-string invocation stays unchanged
- Before/After: single-string CLI only -> single-string CLI + tree-scan mode; consolidate-stubs.md Step 5 gate 'node validate-stub-format.js' (no arg, always exit 1) -> 'node validate-stub-format.js --scan .'
- API: node validate-stub-format.js --scan <dir> — exit 0 all valid, exit 1 any invalid or missing dir; single-string mode unchanged; no-arg still exits 1 with a usage hint
- Schema: no Tickets.json change
- Config: no new configuration
- Dep: no new dependencies (scanStubs already exists in review/find-all-stubs.js)
- Non-change item: validateStubFormat(content) signature and semantics; the no-arg usage error message
- Non-change why: backward compatibility — existing callers and tests depend on the single-string contract
- Impact component: /consolidate-stubs Step 5 blocking gate; /find-omissions preflight could reuse the scan mode
- Impact nature: gate reliability — the blocking three-part gate no longer fails on invocation-usage alone
- Impact response: validate-stub-format.js extended; consolidate-stubs.md Step 5 gate command updated; tests/validate-stub-format.test.cjs extended

## Implementation Target Files

- `.claude/scripts/tickets/validate-stub-format.js`
- `.claude/commands/consolidate-stubs.md`
- `tests/validate-stub-format.test.cjs`

## Crimes — Must Be 100% Resolved in This Ticket (7 items — false_positive: 7)

### TS-001 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `manifest: [{ file: "src/a.rs", line: 5, content: "// [::STUB::] P9-99: reason -- Implement fix" }],`
- **Contracts**: C004

### TS-002 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `writeLine(srcFile, 5, "// [::STUB::] P9-9: edited after manifest build");`
- **Contracts**: C004

### TS-003 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `{ sourceKey: "P3-2", stubs: [{ file: "src/p3.rs", line: 7, content: "// [::STUB::] P3-2: two -- Implement two" }] },`
- **Contracts**: C004

### TS-004 — `tests/batch-update-stub.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `"// c\npub fn demo3() {\n    let z = 3;\n    // [::STUB::] P3-2: audio -- Implement audio worker\n    let _ = z;\n}\n"`
- **Contracts**: C004

### TS-005 — `src/p3.rs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `// [::STUB::] P3-2: reason two -- Implement two`
- **Contracts**: C004

### TS-006 — `tests/print-manifest-for-find-omissions.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `"// c\npub fn demo3() {\n    let z = 3;\n    // [::STUB::] P3-2: audio [::UNIT::U2::] -- Implement audio worker\n    let _ = z;\n}\n"`
- **Contracts**: C004

### TS-007 — `tests/update-stub.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `{ file: "src/p3.rs", line: 7, content: "// [::STUB::] P3-2: three -- three", resolveByTicket: "P3-2" },`
- **Contracts**: C004

## Investigation

Evidence: validate-stub-format.js main() reads process.argv[2] as a single marker string and exits 1 with 'Missing input string' when absent (empirically confirmed exit=1). validateStubFormat(content) returns {valid, errors[]} and enforces the single-line regex /^\/\/\s*\[::STUB::\]\s+(P[A-Z0-9]+-\d+|MUST\s+RESOLVE):\s*\S.*$/ plus a multi-line rejection. review/find-all-stubs.js already scans a directory and returns {success, count, stubs:[{file,line,content}]}, skipping quoted occurrences (STUB_IN_QUOTES_RE) and dirs {node_modules, target, .git, .claude, fixtures} via SKIP_DIRS. The compose point is trivial: scanStubs(dir) -> validateStubFormat(each). consolidate-stubs.md Step 5 currently invokes 'node validate-stub-format.js' with no argument — the broken gate line this ticket fixes.

## Acceptance Criteria

- Happy path: a tree with valid markers -> node validate-stub-format.js --scan . exits 0
- Error case: a tree with a malformed marker -> exit 1, the failing file:line is reported on stderr
- Edge case: an empty tree -> exit 0 with total 0; a no-arg invocation -> exit 1 with a usage hint; the single-string mode is unchanged

## Invariants

- 【Normal establishment】every scanned STUB marker either passes the format rule or is reported with its file:line
- 【On error】a format failure exits 1 and never writes any file
- 【Internal state】the single-string mode and the scan mode share the same validateStubFormat rule
- 【Boundary】an empty tree exits 0 with total 0; a no-arg invocation exits 1 with a usage hint

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — --scan <dir> -> find-all-stubs -> validateStubFormat

- **Precondition**: --scan is invoked with an existing directory
- **Postcondition**: every STUB marker under the directory is validated with validateStubFormat
- **Invariant**: exit 0 iff all markers are valid

### C002 — malformed marker -> failure report

- **Precondition**: a scanned marker fails validation
- **Postcondition**: stderr lists file:line and each format error
- **Invariant**: the process exits 1

### C003 — no-arg invocation -> backward compat

- **Precondition**: invoked with no args
- **Postcondition**: a usage message is printed on stderr
- **Invariant**: the process exits 1, unchanged from today

### C004 — consolidate-stubs.md Step 5 gate -> scan mode

- **Precondition**: the Step 5 gate command uses the scan mode
- **Postcondition**: the gate command is executable from the Tickets.json root
- **Invariant**: the gate no longer fails on invocation-usage alone

## Boy Scout Rule

Translatability: main() splits into two named verbs — validateMarkerString(argv) and scanValidate(dir) — so the CLI reads as a dispatch on the first token. The scan path reuses scanStubs from review/find-all-stubs.js instead of duplicating directory traversal. Per-failure output is a single named function printMarkerFailure({file, line, errors}) so the loop body reads as prose.

## Test Plan

### Unit Tests

- UT: [Normal] --scan <dir> with an existing directory of all-valid markers returns exit 0 and JSON {total, pass, fail:0}
  const res = runScanValidate(tmpDir);
  assert.strictEqual(res.fail, 0);
  assert.strictEqual(res.pass, res.total);
- UT: [Normal] --scan <dir> with one malformed marker exits 1 and stderr lists file:line + each format error
  const out = execCli(['--scan', tmpDir]);
  assert.strictEqual(out.exit, 1);
  assert.ok(out.stderr.includes(malformedFile + ':' + line));
- UT: [Error] --scan with no directory argument exits 1 with a usage message
  const out = execCli(['--scan']);
  assert.strictEqual(out.exit, 1);
  assert.ok(/--scan <dir>/.test(out.stderr));
- UT: [Boundary] an empty tree (no markers) exits 0 with total 0
  const out = execCli(['--scan', emptyDir]);
  assert.strictEqual(out.exit, 0);
  assert.ok(out.stdout.includes('"total":0'));
- UT: [Invariant] the existing single-string mode is unchanged — validateStubFormat(content) still returns {valid, errors} and a no-arg invocation still exits 1
  assert.deepStrictEqual(validateStubFormat('// [::STUB::] P4-2: reason -- plan'), { valid: true, errors: [] });
  assert.strictEqual(execCli([]).exit, 1);
- UT: [C004] consolidate-stubs.md Step 5 gate runs the scan mode so the gate no longer fails on invocation-usage alone
  const doc = fs.readFileSync('.claude/commands/consolidate-stubs.md', 'utf8');
  const step5 = doc.split('### Step 5')[1];
  assert.ok(step5.includes('validate-stub-format.js --scan'), 'gate uses the scan mode');
  assert.ok(!/^node \.claude\/scripts\/tickets\/validate-stub-format\.js$/m.test(step5), 'no bare no-arg invocation in the gate block');

### Integration Tests

- IT: a fixture tree with valid + malformed markers -> node validate-stub-format.js --scan <dir> reports pass/fail and exits 1
- IT: consolidate-stubs.md Step 5 gate command 'node validate-stub-format.js --scan .' runs from the Tickets.json root and exits 0 on a clean tree
- IT: existing validate-stub-format tests stay green (single-string mode untouched)

### Exceptions

- Item: validating whether a marker key exists in Tickets.json — Reason: key-existence is the validate-no-external-excuses.js Check C responsibility, not the format gate; the format gate cannot test semantic key validity without coupling to Tickets.json. This is not a design defect — format and key-existence are deliberately separated concerns — Alternative: the --for-consolidate gate in validate-no-external-excuses.js covers key validity; the scan mode covers pure format.
- Item: judging whether a [::STUB::] occurrence inside a quoted test string is a real marker — Reason: find-all-stubs.js already skips quoted occurrences (STUB_IN_QUOTES_RE) and fixture dirs (SKIP_DIRS); the scan mode inherits that behavior and cannot test the scanner's own heuristics here. This is not a design defect — the scanner's filtering is separately unit-tested in find-all-stubs — Alternative: the scan mode validates what find-all-stubs returns; find-all-stubs' own tests cover the quoted-string heuristic.

### Plan Test Code (concrete code)

- UT: [C001] --scan <dir> with an existing directory of all-valid markers exits 0 and prints {total, pass, fail:0}
  const ws = makeWorkspace();
  writeFile(path.join(ws.dir, 'src/a.rs'), '// [::STUB::] P4-2: reason -- plan\nfn a() {}\n');
  const out = execFileSync('node', [VALIDATE, '--scan', ws.dir], { encoding: 'utf8' });
  const res = JSON.parse(out);
  assert.strictEqual(res.fail, 0);
  assert.strictEqual(res.pass, res.total);
- UT: [C002] --scan <dir> with one malformed marker exits 1 and stderr lists file:line + each format error
  const ws = makeWorkspace();
  writeFile(path.join(ws.dir, 'src/a.rs'), '// [::STUB::] broken marker\nfn a() {}\n');
  let status = 0, stderr = '';
  try { execFileSync('node', [VALIDATE, '--scan', ws.dir], { encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] }); }
  catch (e) { status = e.status; stderr = e.stderr; }
  assert.strictEqual(status, 1);
  assert.ok(stderr.includes('src/a.rs:1'));
- UT: [C003] no-arg invocation still exits 1 with a usage message (backward compatibility)
  let status = 0, stderr = '';
  try { execFileSync('node', [VALIDATE], { encoding: 'utf8', stdio: ['ignore', 'pipe', 'pipe'] }); }
  catch (e) { status = e.status; stderr = e.stderr; }
  assert.strictEqual(status, 1);
  assert.ok(/Missing input string|usage/i.test(stderr));
- UT: [C004] consolidate-stubs.md Step 5 gate runs the scan mode so the gate no longer fails on invocation-usage alone
  const doc = fs.readFileSync('.claude/commands/consolidate-stubs.md', 'utf8');
  const step5 = doc.split('### Step 5')[1];
  assert.ok(step5.includes('validate-stub-format.js --scan'), 'gate uses the scan mode');
  assert.ok(!/^node \.claude\/scripts\/tickets\/validate-stub-format\.js$/m.test(step5), 'no bare no-arg invocation in the gate block');

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| validate-stub-format.js main() read process.argv[2] as a single marker string only; the documented no-arg gate command always exited 1 | a --scan <dir> mode scans the tree via review/find-all-stubs.js and validates every marker with the same validateStubFormat rule | Tree-scan mode added (C001-C003) |
| consolidate-stubs.md Step 5 gate invoked 'node validate-stub-format.js' with no argument (always exit 1, blocking gate unreachable) | the gate runs 'node validate-stub-format.js --scan .' | Gate command made runnable (C004) |
| validate-stub-format.js exported only validateStubFormat | exports { validateStubFormat, scanValidate } for testability | scanValidate exported |
| no scan-mode test existed | tests/validate-stub-format.test.cjs added with @verifies C001-C004 | Test suite (RED->GREEN) |

## Notes in Prior Implementation Rounds

【Implementation steps】1) validate-stub-format.js: parse --scan, resolve dir, call scanStubs (execFileSync find-all-stubs), validate each with validateStubFormat, print per-failure file:line + errors, exit 0/1 2) consolidate-stubs.md Step 5: replace the bare validate-stub-format.js line with --scan . 3) extend tests/validate-stub-format.test.cjs
【Risks】the scan mode must reuse find-all-stubs.js rather than duplicate traversal so the quoted-string/fixtures heuristics stay single-sourced
【Caveats】an empty tree is a valid pass (total 0, exit 0); a missing dir argument is a usage error (exit 1)
【Open items】whether /find-omissions preflight should also adopt --scan (currently out of scope)
【Future】the scan mode can become the shared format gate for any pipeline step that needs a tree-wide format check
Implementation summary:
- Changed files: .claude/scripts/tickets/validate-stub-format.js, .claude/commands/consolidate-stubs.md, tests/validate-stub-format.test.cjs
- TDD: tests/validate-stub-format.test.cjs written first (RED 5/7 failed), then --scan mode + doc gate change made it green (7/7)
- Contract coverage: verify-red-coverage 4/4 with @verifies annotations
- Tests: 76/76 (validate-stub-format 7 + consolidate-stubs-doc 6 + batch-update-stub + update-stub + print-manifest + batch-create + find-all-stubs)
- Quality: run-quality-checks 0 issues; node -c syntax OK
- Crimes: 0 open; targetCrimes all false_positive (deferredTo PX-132)
- The scan mode reuses review/find-all-stubs.js so quoted-string/fixtures heuristics stay single-sourced
Review report:
- Static quality check: passed
- Translatability: no issues
- Annotation: validate-stub-format.js carries PX-134 provenance
- Stub evaluation: 0 stubs in the tree; no-excuse gate 0 failures
- verify-final-contracts: 100% (1/1 fulfilled)
- verify-red-coverage: 4/4 contracts with @verifies
- The real gate command (node validate-stub-format.js --scan .) exits 0 from the Tickets.json root
- Issues found and fixes applied: none during review

## PX-134 — implemented at 2 locations

### .claude/scripts/tickets/validate-stub-format.js

- Line 110
```javascript
function scanValidate(dir) {
```

- Line 137
```javascript
function main() {
```
