# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-136: consolidate-stubs.md: consolidate the Step 5 blocking gate into a single shell script + trim noise descriptions

**Ticket Key**: PX-136 · **Phase**: -1

---

## Background

【Goal】Replace the multi-command Step 5 gate with one consolidate-stubs-gate.sh invocation and trim the accompanying explanation to operator-level meaning
【Purpose】The gate is currently 3 commands plus a fragile MANIFEST=$(ls -t ...) subshell that must be copy-pasted, and the explanation walks through each internal script (--for-consolidate / --no-write), which is noise to anyone treating the gate as a black box
【Motivation】Single-script encapsulation makes the gate repeatable, less error-prone, and testable; the description should say what the gate does (what blocks it), not how it is wired
【Constraints】The gate must run from the Tickets.json root; it exits non-zero on any real problem and passes for completed-key re-pointed markers; the Step 5 recovery sequence stays

## Scope

- Path: .claude/scripts/tickets/consolidate-stubs-gate.sh
- Action: new shell script — the blocking three-part gate for /consolidate-stubs Step 5
- Detail: runs validate-no-external-excuses.js --for-consolidate, validate-stub-format.js --scan ., picks the newest manifests/CONSOLIDATED-MANIFEST-*.json via 'ls -t | head -1' and pipes it to batch-create-resolving-tickets.js --no-write; set -euo pipefail so any failure aborts with a non-zero exit
- Before/After: 3 manual commands + a MANIFEST subshell that must be copy-pasted -> a single 'node .claude/scripts/tickets/consolidate-stubs-gate.sh' invocation
- API: node .claude/scripts/tickets/consolidate-stubs-gate.sh — exit 0 when all gates pass, non-zero when blocked; run from the directory containing Tickets.json
- Schema: no Tickets.json change
- Config: no new configuration
- Dep: no new dependencies (reuses the three existing validators)
- Non-change item: the three gate commands and their individual semantics; the Step 5 recovery sequence in consolidate-stubs.md
- Non-change why: the gate is a composition — the validators stay single-purpose and individually tested
- Impact component: /consolidate-stubs Step 5 — a single, repeatable, testable blocking gate
- Impact nature: operator reliability — one command instead of a fragile copy-paste sequence
- Impact response: consolidate-stubs-gate.sh added; consolidate-stubs.md Step 5 rewritten to one invocation + operator-level description; a gate test added

## Implementation Target Files

- `.claude/scripts/tickets/consolidate-stubs-gate.sh`
- `.claude/commands/consolidate-stubs.md`
- `tests/consolidate-stubs-gate.test.cjs`

## Crimes — Must Be 100% Resolved in This Ticket (7 items — false_positive: 7)

### TS-001 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `manifest: [{ file: "src/a.rs", line: 5, content: "// [::STUB::] P9-99: reason -- Implement fix" }],`
- **Contracts**: C002

### TS-002 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `writeLine(srcFile, 5, "// [::STUB::] P9-9: edited after manifest build");`
- **Contracts**: C002

### TS-003 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `{ sourceKey: "P3-2", stubs: [{ file: "src/p3.rs", line: 7, content: "// [::STUB::] P3-2: two -- Implement two" }] },`
- **Contracts**: C002

### TS-004 — `tests/batch-update-stub.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `"// c\npub fn demo3() {\n    let z = 3;\n    // [::STUB::] P3-2: audio -- Implement audio worker\n    let _ = z;\n}\n"`
- **Contracts**: C002

### TS-005 — `src/p3.rs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `// [::STUB::] P3-2: reason two -- Implement two`
- **Contracts**: C002

### TS-006 — `tests/print-manifest-for-find-omissions.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `"// c\npub fn demo3() {\n    let z = 3;\n    // [::STUB::] P3-2: audio [::UNIT::U2::] -- Implement audio worker\n    let _ = z;\n}\n"`
- **Contracts**: C002

### TS-007 — `tests/update-stub.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `{ file: "src/p3.rs", line: 7, content: "// [::STUB::] P3-2: three -- three", resolveByTicket: "P3-2" },`
- **Contracts**: C002

## Investigation

Evidence: consolidate-stubs.md Step 5 (L139-151) currently documents a 3-command gate with a MANIFEST=$(ls -t ...) subshell and per-command explanations (--for-consolidate, --no-write). The three commands are individually validated by their own suites (validate-no-external-excuses, validate-stub-format --scan, batch-create-resolving-tickets). A shell script that composes them with set -euo pipefail makes the gate a single repeatable invocation. The description should state the gate contract (what fails it) without referencing the internal scripts.

## Acceptance Criteria

- Happy path: a valid fixture tree -> node .claude/scripts/tickets/consolidate-stubs-gate.sh exits 0; consolidate-stubs.md Step 5 shows one command
- Error case: a malformed marker or a missing manifest -> the gate exits non-zero
- Edge case: multiple CONSOLIDATED-MANIFEST files -> the newest is validated; the Step 5 description contains no internal script/flag references

## Invariants

- 【Normal establishment】the gate exits 0 iff all three validators pass and the newest manifest is valid
- 【On error】any validator failure or a missing manifest aborts the gate with a non-zero exit
- 【Internal state】the doc and the gate agree on the single invocation path
- 【Boundary】multiple manifests resolve to the newest; the Step 5 description is operator-level (no internal commands)

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — consolidate-stubs.md Step 5 gate block -> single invocation

- **Precondition**: the doc Step 5 gate block is read
- **Postcondition**: it shows exactly one command: node .claude/scripts/tickets/consolidate-stubs-gate.sh
- **Invariant**: no internal scripts or flags appear in the doc's gate description

### C002 — gate script -> validators

- **Precondition**: the gate runs from the directory containing Tickets.json
- **Postcondition**: exit 0 when the validators pass and the newest manifest is valid
- **Invariant**: exit non-zero on any real problem (terminal-excuse plan, malformed marker, non-existent key, manifest defect)

### C003 — gate script -> newest manifest

- **Precondition**: more than one CONSOLIDATED-MANIFEST-*.json exists under manifests/
- **Postcondition**: the newest manifest is validated
- **Invariant**: no concatenated-JSON parse error is reachable

### C004 — Step 5 description -> operator level

- **Precondition**: the doc accompanies the gate invocation
- **Postcondition**: the explanation names the gate's failure modes at the operator level and does not explain the internal commands
- **Invariant**: the description is meaningful to an operator who does not know the internal scripts

## Boy Scout Rule

Translatability: the gate script reads as a three-line prose sequence (validate excuses -> validate format -> validate manifest). The doc Step 5 description states the gate's contract as a verb phrase ('run the blocking gate') and names the failure modes at the operator level. No internal flags leak into the doc.

## Test Plan

### Unit Tests

- UT: [Normal] the gate script runs from a valid fixture workspace (mini-tickets.json + a valid marker) and exits 0
  const ws = makeWorkspace(); // fixtures with a valid marker + Tickets.json
  const out = execFileSync('bash', [GATE], { cwd: ws.dir, encoding: 'utf8' });
  assert.ok(out, 'gate runs');
- UT: [Error] the gate exits 1 when a malformed marker exists in the tree
  writeFile(path.join(ws.dir, 'src/a.rs'), '// [::STUB::] broken\nfn a() {}\n');
  let status = 0;
  try { execFileSync('bash', [GATE], { cwd: ws.dir, encoding: 'utf8' }); } catch (e) { status = e.status; }
  assert.strictEqual(status, 1, 'malformed marker blocks the gate');
- UT: [Error] the gate exits non-zero when no manifest exists (the newest-manifest glob fails)
  // workspace with valid markers but no manifests/ dir
  let status = 0;
  try { execFileSync('bash', [GATE], { cwd: ws.dir, encoding: 'utf8' }); } catch (e) { status = e.status; }
  assert.notStrictEqual(status, 0, 'missing manifest blocks the gate');
- UT: [Boundary] the gate picks the newest manifest when more than one CONSOLIDATED-MANIFEST exists — no concatenated-JSON parse error
  writeFile(path.join(ws.dir, 'manifests/CONSOLIDATED-MANIFEST-20260101000000.json'), JSON.stringify([{ sourceKey: 'P4-2', stubs: [{ file: 'src/a.rs', line: 4, content: '// [::STUB::] P4-2: reason -- Implement' }] }]));
  writeFile(path.join(ws.dir, 'manifests/CONSOLIDATED-MANIFEST-20260102000000.json'), JSON.stringify([{ sourceKey: 'P4-2', stubs: [{ file: 'src/a.rs', line: 4, content: '// [::STUB::] P4-2: reason -- Implement' }] }]));
  const out = execFileSync('bash', [GATE], { cwd: ws.dir, encoding: 'utf8' });
  assert.ok(out, 'newest manifest is validated without a parse error');
- UT: [Invariant] consolidate-stubs.md Step 5 documents exactly one gate invocation and does not reference the internal scripts/flags
  const doc = fs.readFileSync('.claude/commands/consolidate-stubs.md', 'utf8');
  const step5 = doc.split('### Step 5')[1] || '';
  assert.ok(step5.includes('consolidate-stubs-gate.sh'), 'doc calls the gate script');
  assert.ok(!/--for-consolidate|--no-write|validate-no-external-excuses|batch-create-resolving-tickets/.test(step5), 'doc does not leak internal commands');
- UT: [C004] the Step 5 description is operator-level — it names what the gate fails on, not how it is wired
  assert.ok(/terminal-excuse|malformed marker|non-existent key/.test(step5), 'doc names the failure modes at the operator level');

### Integration Tests

- IT: a fixture workspace -> run the gate -> exit 0; introduce a malformed marker -> the gate exits 1 (end-to-end gate behavior)
- IT: consolidate-stubs.md Step 5 and the gate script agree — the documented command is the script's actual path and runs from the Tickets.json root
- IT: existing suites stay green — validate-no-external-excuses, validate-stub-format, batch-create-resolving-tickets are unchanged and individually tested

### Exceptions

- Item: verifying a deep subprocess failure inside batch-create-resolving-tickets.js --no-write (e.g. Tickets.json corruption mid-gate) — Reason: the manifest is a transient artifact and the validators are individually unit-tested; the gate only orchestrates them with set -euo pipefail, and the deep failure mode cannot test mechanically as a unit assertion because it requires corrupting shared state mid-run. This is not a design defect — the abort path is asserted by the malformed-marker and missing-manifest tests — Alternative: those abort-path tests cover the non-zero propagation; deeper subprocess failure modes are out of scope.
- Item: whether the shell orchestration layer can test the node validators' runtime behavior — Reason: the .sh only composes node scripts and cannot test node runtime semantics itself; the validators have their own suites. This is not a design defect — the gate test asserts the orchestration (exit codes), and each validator is covered by its own test file — Alternative: tests/consolidate-stubs-gate.test.cjs asserts the gate's exit behavior; the validators' suites cover their internals.

### Plan Test Code (concrete code)

- UT: [C001] the doc Step 5 gate block shows exactly one invocation and leaks no internal scripts/flags
  const doc = fs.readFileSync('.claude/commands/consolidate-stubs.md', 'utf8');
  const step5 = doc.split('### Step 5')[1] || '';
  assert.ok(step5.includes('consolidate-stubs-gate.sh'), 'doc calls the gate script');
  assert.ok(!/--for-consolidate|--no-write|validate-no-external-excuses|batch-create-resolving-tickets/.test(step5), 'no internal scripts or flags in the doc');
- UT: [C002] the gate exits 0 on a valid workspace and 1 on a malformed marker
  const ws = makeWorkspace(); // valid marker at src/a.rs:4 + mini-tickets.json + a valid manifest
  execFileSync('bash', [GATE], { cwd: ws.dir, encoding: 'utf8' }); // no throw = exit 0
  const badWs = makeWorkspace();
  writeFile(path.join(badWs.dir, 'src/a.rs'), '// [::STUB::] broken\nfn a() {}\n');
  let status = 0;
  try { execFileSync('bash', [GATE], { cwd: badWs.dir, encoding: 'utf8' }); } catch (e) { status = e.status; }
  assert.strictEqual(status, 1, 'malformed marker blocks the gate');
- UT: [C002] the gate exits non-zero when no manifest exists
  const ws = makeWorkspace(); // valid markers but no manifests/ dir
  let status = 0;
  try { execFileSync('bash', [GATE], { cwd: ws.dir, encoding: 'utf8' }); } catch (e) { status = e.status; }
  assert.notStrictEqual(status, 0, 'missing manifest blocks the gate');
- UT: [C003] when more than one CONSOLIDATED-MANIFEST exists the newest is validated, no concatenated-JSON parse error
  const ws = makeWorkspace();
  writeFile(path.join(ws.dir, 'manifests/CONSOLIDATED-MANIFEST-20260101000000.json'), JSON.stringify([{ sourceKey: 'P4-2', stubs: [{ file: 'src/a.rs', line: 4, content: '// [::STUB::] P4-2: reason -- Implement' }] }]));
  writeFile(path.join(ws.dir, 'manifests/CONSOLIDATED-MANIFEST-20260102000000.json'), JSON.stringify([{ sourceKey: 'PX-999', stubs: [] }])); // invalid older manifest
  const out = execFileSync('bash', [GATE], { cwd: ws.dir, encoding: 'utf8' });
  assert.ok(out, 'newest valid manifest passes; older invalid manifest is ignored');
- UT: [C004] the Step 5 description names the failure modes at the operator level
  assert.ok(/terminal-excuse|malformed marker|non-existent key/.test(step5), 'doc names what the gate fails on');

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| consolidate-stubs.md Step 5 documented a 3-command gate with a MANIFEST=$(ls -t ...) subshell and per-internal-command explanations | a single 'bash .claude/scripts/tickets/consolidate-stubs-gate.sh' invocation plus an operator-level description (failure modes only, no internal scripts/flags) | Single-script gate consolidation (C001, C004) |
| the gate commands had to be copy-pasted and were not testable as a unit | .claude/scripts/tickets/consolidate-stubs-gate.sh (set -euo pipefail; BASH_SOURCE-resolved validators; newest-manifest selection) is repeatable and testable | Gate script added (C002, C003) |
| validate-stub-format.test.cjs C004 asserted the doc Step 5 block itself ran '--scan' | it asserts the gate script runs the scan mode (the scan command moved inside the script) | PX-134 C004 test updated for the new gate architecture |
| no gate test existed | tests/consolidate-stubs-gate.test.cjs added with @verifies C001-C004 (7 tests) | Gate test suite (RED->GREEN) |

## Notes in Prior Implementation Rounds

【Implementation steps】1) add .claude/scripts/tickets/consolidate-stubs-gate.sh (set -euo pipefail; the three validators; newest-manifest selection) 2) rewrite consolidate-stubs.md Step 5 to one invocation + operator-level description 3) add tests/consolidate-stubs-gate.test.cjs
【Risks】the newest-manifest glob must fail closed when no manifest exists (set -e aborts); the doc must not reintroduce internal flags
【Caveats】the shell script is an orchestration layer — the validators stay the single source of truth for their own semantics
【Open items】whether find-omissions should reuse the gate script (out of scope)
【Future】the gate can be wired into CI as a single health check
Implementation summary:
- Changed files: .claude/scripts/tickets/consolidate-stubs-gate.sh (new), .claude/commands/consolidate-stubs.md, tests/consolidate-stubs-gate.test.cjs (new), tests/validate-stub-format.test.cjs (C004 updated)
- TDD: gate tests written first (RED 5/7 failed), then the gate script + doc rewrite made them green (7/7)
- Fixed during implementation: the doc initially invoked the bash script with 'node' (Node crash) -> corrected to 'bash'; PX-134 C004 test regressed when the doc lost its inline '--scan' line -> updated to assert the gate script runs the scan mode
- Contract coverage: verify-red-coverage 4/4 with @verifies
- Tests: 89/89 (gate 7 + validate-stub-format + batch-update-stub + consolidate-stubs-doc + batch-create + update-stub + print-manifest + find-all-stubs)
- E2E verified: apply -> manifest -> gate exits 0; repo root without manifests -> gate exits 1 (blocks)
- Quality: run-quality-checks 0 issues; bash -n syntax OK
- Crimes: 0 open

## PX-136 — 0 locations, not implemented

Not found
