# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-137: consolidated manifest: emit cwd-relative file paths + fix the find-omissions grouped-shape example

**Ticket Key**: PX-137 · **Phase**: -1

---

## Background

【Goal】Align the generated consolidated manifest with the documented cwd-relative file-path contract and fix the find-omissions grouped-shape example
【Purpose】print-manifest emits absolute stubs[].file paths while batch-create-resolving-tickets.js:23 documents 'manifest file paths are cwd-relative'; find-omissions.md's handoff note points to a legacy single-marker example
【Motivation】A schema review (follow-up to PX-133-136) found the manifest is functionally consumable (gate passes) but the documented contract and the generated output differ; absolute paths bind the manifest to the generation machine
【Constraints】batch-create keeps accepting both absolute and relative paths (backward compat); find-all-stubs keeps returning absolute paths for its other consumers; the manifest shape {sourceKey, stubs[]} stays

## Scope

- Path: .claude/scripts/tickets/print-manifest-for-find-omissions.js
- Action: modify — emit stubs[].file as a path RELATIVE to the Tickets.json root (cwd) instead of the absolute path from find-all-stubs
- Detail: in groupByUnitId / runPrinter, relativize each marker's file against root via path.relative(root, file); the manifest then matches the documented 'manifest file paths are cwd-relative' contract (batch-create-resolving-tickets.js L23)
- Before/After: manifest stubs[].file = '/abs/path/to/src/a.rs' -> 'src/a.rs'; the generated manifest matches the documented contract and becomes relocatable
- API: no CLI change; the manifest file path format changes from absolute to cwd-relative
- Schema: manifest shape stays {sourceKey, stubs:[{file,line,content}]}; only the file value format changes
- Config: no new configuration
- Dep: no new dependencies
- Non-change item: find-all-stubs.js keeps returning absolute paths (other consumers: validate-no-external-excuses, validate-stub-format scan); batch-create keeps accepting BOTH absolute and relative paths (resolveEntryPath)
- Non-change why: find-all-stubs is a shared scanner; the manifest format is owned by print-manifest; batch-create backward-compat avoids breaking legacy/hand-authored manifests
- Impact component: /consolidate-stubs Step 5 gate (batch-create --no-write), /find-omissions Step 1
- Impact nature: the generated manifest matches its documented contract and is no longer bound to the generation machine's absolute paths
- Impact response: print-manifest-for-find-omissions.js modified; find-omissions.md handoff example corrected; tests extended

## Implementation Target Files

- `.claude/scripts/tickets/print-manifest-for-find-omissions.js`
- `.claude/commands/find-omissions.md`
- `tests/print-manifest-for-find-omissions.test.cjs`

## Crimes — Must Be 100% Resolved in This Ticket (7 items — false_positive: 7)

### TS-001 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `manifest: [{ file: "src/a.rs", line: 5, content: "// [::STUB::] P9-99: reason -- Implement fix" }],`
- **Contracts**: C004

### TS-002 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `writeLine(srcFile, 5, "// [::STUB::] P9-9: edited after manifest build");`
- **Contracts**: C004

### TS-003 — `tests/batch-create-resolving-tickets.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `{ sourceKey: "P3-2", stubs: [{ file: "src/p3.rs", line: 7, content: "// [::STUB::] P3-2: two -- Implement two" }] },`
- **Contracts**: C004

### TS-004 — `tests/batch-update-stub.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `"// c\npub fn demo3() {\n    let z = 3;\n    // [::STUB::] P3-2: audio -- Implement audio worker\n    let _ = z;\n}\n"`
- **Contracts**: C004

### TS-005 — `src/p3.rs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `// [::STUB::] P3-2: reason two -- Implement two`
- **Contracts**: C004

### TS-006 — `tests/print-manifest-for-find-omissions.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `"// c\npub fn demo3() {\n    let z = 3;\n    // [::STUB::] P3-2: audio [::UNIT::U2::] -- Implement audio worker\n    let _ = z;\n}\n"`
- **Contracts**: C004

### TS-007 — `tests/update-stub.test.cjs — false_positive (ORPHAN_TICKET_REF)

- **Marker**: `{ file: "src/p3.rs", line: 7, content: "// [::STUB::] P3-2: three -- three", resolveByTicket: "P3-2" },`
- **Contracts**: C004

## Investigation

Evidence from the schema review (2026-08-05): print-manifest-for-find-omissions.js groupByUnitId emits stubs[].file = find-all-stubs' absolute path (scanFile uses path.join(path.resolve(dir), ...)); batch-create-resolving-tickets.js:23 documents 'manifest file paths are cwd-relative'; resolveEntryPath accepts both absolute (under sourceRoot) and relative paths; an E2E confirmed absolute paths pass the gate (exit 0). find-omissions.md L138 handoff note says 'in exactly the grouped shape below' but the JSON example at L124-137 is the legacy single-marker shape (top-level file/line/content/sourceKey/seed).

## Acceptance Criteria

- Happy path: a consolidation emits cwd-relative manifest file paths, and the generated manifest passes batch-create --no-write
- Error case: none (no new failure mode) — the generator change must not break the Step 5 gate
- Edge case: absolute-path legacy manifests still pass batch-create (backward compat); find-omissions.md shows the grouped-shape example

## Invariants

- 【Normal establishment】every manifest stubs[].file is cwd-relative and resolves under sourceRoot
- 【On error】a manifest with escaping paths is refused by resolveEntryPath (unchanged)
- 【Internal state】the manifest shape stays {sourceKey, stubs:[{file,line,content}]}
- 【Boundary】both relative (generated) and absolute (legacy/hand-authored) file paths pass batch-create

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — print-manifest -> manifest file paths

- **Precondition**: print-manifest writes a consolidated manifest under the Tickets.json root
- **Postcondition**: every stubs[].file is relative to the Tickets.json root (no leading /, no '..')
- **Invariant**: the manifest file paths resolve under sourceRoot

### C002 — generated manifest -> batch-create --no-write

- **Precondition**: the generated manifest is piped to batch-create --no-write from the Tickets.json root
- **Postcondition**: validateManifestShape, resolveSourceKey, and checkOnDiskMarker all pass
- **Invariant**: exit 0 on a valid tree

### C003 — find-omissions.md handoff note -> grouped shape

- **Precondition**: the /consolidate-stubs handoff note is read
- **Postcondition**: it shows a grouped-shape example with sourceKey and stubs[] (not the legacy example)
- **Invariant**: the example matches the generated manifest shape

### C004 — legacy absolute-path manifest -> backward compat

- **Precondition**: an absolute-path manifest is piped to batch-create --no-write
- **Postcondition**: it still passes (resolveEntryPath accepts absolute paths under sourceRoot)
- **Invariant**: no regression for legacy or hand-authored manifests

## Boy Scout Rule

Translatability: extract a named verb relativizeToRoot(file, root) so groupByUnitId reads as 'map each marker to a root-relative stub'. The find-omissions.md handoff note becomes a concrete grouped-shape JSON example the reader can compare against the generated output, so the doc and the generator agree.

## Test Plan

### Unit Tests

- UT: [C001] a consolidation emits manifest stubs[].file as a cwd-relative path (no leading /, no '..')
  const res = runBatchUpdate({ decision: [{ unitId: 'U1', resolveByTicket: 'P4-2', markerLines: ['src/a.rs:4'] }], dir: ws.dir, ticketsPath: ws.ticketsPath });
  const manifest = JSON.parse(fs.readFileSync(res.manifestPath, 'utf8'));
  const file = manifest[0].stubs[0].file;
  assert.ok(!path.isAbsolute(file), 'file is relative');
  assert.ok(!file.startsWith('..'), 'file stays under the root');
  assert.strictEqual(file, path.join('src', 'a.rs'), 'relative to the Tickets.json root');
- UT: [C002] the generated relative-path manifest passes batch-create --no-write (exit 0)
  const out = execFileSync('node', [BATCH_CREATE, '--no-write'], { cwd: ws.dir, input: fs.readFileSync(res.manifestPath, 'utf8'), encoding: 'utf8' });
  assert.ok(JSON.parse(out).createdTickets >= 1, 'manifest is consumable');
- UT: [C003] find-omissions.md handoff note shows a grouped-shape example with sourceKey and stubs[]
  const doc = fs.readFileSync(path.resolve(__dirname, '../.claude/commands/find-omissions.md'), 'utf8');
  const handoff = doc.split('**`/consolidate-stubs` handoff**')[1] || '';
  assert.ok(/sourceKey/.test(handoff) && /stubs/.test(handoff), 'handoff shows the grouped shape');
  assert.ok(!/the grouped shape below/.test(handoff), 'no misleading below pointer');
- UT: [C004] an absolute-path legacy manifest still passes batch-create --no-write (backward compat)
  const absManifest = JSON.stringify([{ sourceKey: 'P4-2', stubs: [{ file: ws.srcA, line: 4, content: '// [::STUB::] P4-2: reason -- Implement' }] }]);
  const out = execFileSync('node', [BATCH_CREATE, '--no-write'], { cwd: ws.dir, input: absManifest, encoding: 'utf8' });
  assert.ok(JSON.parse(out).createdTickets >= 1, 'absolute path still accepted');
- UT: [Invariant] the manifest shape stays {sourceKey, stubs:[{file,line,content}]}
  assert.ok(manifest.every(e => typeof e.sourceKey === 'string' && Array.isArray(e.stubs) && e.stubs.every(s => s.file && Number.isInteger(s.line) && typeof s.content === 'string')));

### Integration Tests

- IT: consolidate a fixture tree -> the generated manifest is piped to batch-create --no-write -> exit 0 (the gate passes end-to-end with relative paths)
- IT: find-omissions.md's documented grouped example matches the generated manifest shape ({sourceKey, stubs:[{file,line,content}]})
- IT: existing suites stay green — print-manifest, batch-create, batch-update-stub, consolidate-stubs-gate are unchanged in behavior

### Exceptions

- Item: whether find-all-stubs should also return relative paths — Reason: find-all-stubs is a shared scanner used by validate-no-external-excuses and validate-stub-format --scan, so the cross-consumer impact of changing its output cannot test mechanically as a single unit assertion; the relativization belongs to print-manifest (the manifest generator owns the manifest format). This is not a design defect — the generator-side relativization is unit-tested — Alternative: print-manifest relativizes; find-all-stubs stays absolute for its other consumers.
- Item: verifying actual portability across a filesystem tree relocation — Reason: relocating the tree after generation is an OS-level operation, so simulating a real relocation cannot test mechanically as a unit assertion inside the tool; the cwd-relative fix makes the manifest relocatable by construction. This is not a design defect — the relative-path assertion covers the fix — Alternative: the relative-path unit test asserts the format; relocation is a manual/ops concern.

### Plan Test Code (concrete code)

- UT: [C001] runPrinter emits manifest stubs[].file as a cwd-relative path (no leading /, no '..')
  const ws = makeWorkspace();
  const res = runBatchUpdate({ decision: [{ unitId: 'U1', resolveByTicket: 'P4-2', markerLines: ['src/a.rs:4'] }], dir: ws.dir, ticketsPath: ws.ticketsPath });
  const manifest = JSON.parse(fs.readFileSync(res.manifestPath, 'utf8'));
  const file = manifest[0].stubs[0].file;
  assert.ok(!path.isAbsolute(file), 'file is relative');
  assert.ok(!file.startsWith('..'), 'file stays under the root');
  assert.strictEqual(file, path.join('src', 'a.rs'), 'relative to the Tickets.json root');
- UT: [C002] the generated relative-path manifest passes batch-create --no-write
  const out = execFileSync('node', [BATCH_CREATE, '--no-write'], { cwd: ws.dir, input: fs.readFileSync(res.manifestPath, 'utf8'), encoding: 'utf8' });
  assert.ok(JSON.parse(out).createdTickets >= 1, 'manifest is consumable');
- UT: [C003] find-omissions.md handoff note shows a grouped-shape example and drops the misleading 'below' pointer
  const doc = fs.readFileSync(path.resolve(__dirname, '../.claude/commands/find-omissions.md'), 'utf8');
  const handoff = doc.split('**`/consolidate-stubs` handoff**')[1] || '';
  assert.ok(handoff.includes('"sourceKey"') && handoff.includes('"stubs"'), 'handoff shows a grouped JSON example');
  assert.ok(!/the grouped shape below/.test(handoff), 'no misleading below pointer');
- UT: [C004] an absolute-path legacy manifest still passes batch-create --no-write
  const absManifest = JSON.stringify([{ sourceKey: 'P4-2', stubs: [{ file: ws.srcA, line: 4, content: '// [::STUB::] P4-2: reason -- Implement' }] }]);
  const out = execFileSync('node', [BATCH_CREATE, '--no-write'], { cwd: ws.dir, input: absManifest, encoding: 'utf8' });
  assert.ok(JSON.parse(out).createdTickets >= 1, 'absolute path still accepted');

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| print-manifest emitted stubs[].file as absolute paths while batch-create documents 'cwd-relative' | runPrinter relativizes stubs[].file against the Tickets.json root (path.relative) | cwd-relative manifest paths (C001) |
| macOS /var -> /private/var resolution difference could make a generated absolute-path manifest escape the sourceRoot | relative paths resolve under any sourceRoot; the generated manifest is relocatable | Portability fix validated by C001/C002 |
| find-omissions.md handoff note said 'the grouped shape below' but pointed at a legacy single-marker example | the handoff note shows a real grouped JSON example ({sourceKey, stubs[]}) with cwd-relative file | find-omissions grouped-shape example fixed (C003) |
| no test asserted the manifest path format or backward compatibility | C001-C004 tests added (relative generation, consumability, doc example, absolute-path backward compat) | Test suite extended (RED->GREEN) |

## Notes in Prior Implementation Rounds

【Implementation steps】1) print-manifest-for-find-omissions.js: relativize stubs[].file against root in groupByUnitId/runPrinter 2) find-omissions.md: correct the handoff note to show a real grouped-shape example and drop the misleading 'below' pointer 3) extend tests/print-manifest-for-find-omissions.test.cjs
【Risks】relativization must happen against the same root batch-create uses as sourceRoot (the Tickets.json root); find-all-stubs absolute paths must not leak into the manifest
【Caveats】batch-create accepts both path forms; the fix only changes what print-manifest emits
【Open items】whether validate-stub-format --scan output should also be relative (out of scope)
【Future】the manifest becomes relocatable; the gate can run from a copied tree
Implementation summary:
- Changed files: .claude/scripts/tickets/print-manifest-for-find-omissions.js, .claude/commands/find-omissions.md, tests/print-manifest-for-find-omissions.test.cjs
- TDD: C001-C004 tests written first (RED 4/4 failed), then runPrinter relativization + doc fix made them green (8/8)
- Root cause discovered: macOS /var -> /private/var symlink made generated absolute paths escape the child process's resolved sourceRoot — the cwd-relative fix eliminates this
- Contract coverage: verify-red-coverage 4/4 with @verifies
- Tests: 93/93 (print-manifest 8 + batch-update-stub + batch-create + consolidate-stubs-gate + consolidate-stubs-doc + validate-stub-format + update-stub + find-all-stubs)
- Quality: run-quality-checks 0 issues; node -c syntax OK
- Crimes: 0 open

## PX-137 — implemented at 1 locations

### .claude/scripts/tickets/print-manifest-for-find-omissions.js

- Line 159
```javascript
function runPrinter(dir) {
```
