# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-148: require-consolidated-manifest: PASS when stub scan finds 0 stubs, instruct skip Step 1

**Ticket Key**: PX-148 · **Phase**: -1

---

## Background

The /find-omissions pre-flight gate require-consolidated-manifest.js blocks (exit 2) whenever no ./manifests/CONSOLIDATED-MANIFEST-*.json exists. But /consolidate-stubs only writes that manifest when there are [::STUB::] markers to group into units — print-manifest-for-find-omissions.js refuses to write when 0 tagged markers exist. Therefore a 0-stub tree legitimately has no manifest, yet /find-omissions is blocked from running its manifest-independent core: Steps 2-7 (the ABC contract-to-test inspection loop over reviewed/remanded tickets), Steps 8-10 (cleanup, phasify merge, phase rename). The prerequisite the gate actually enforces is 'no un-consolidated stubs', which is vacuously satisfied when the stub scan finds 0 stubs.

### Goal
- 【Goal】Make /find-omissions runnable in a 0-stub tree: the pre-flight gate passes (exit 0) when no CONSOLIDATED-MANIFEST-*.json exists AND the stub scan finds 0 stubs, and stdout tells the operator to skip Step 1 and start the Step 2 inspection loop.

### Purpose
- 【Purpose】The current gate blocks the entire /find-omissions pipeline whenever the manifest is absent, but the manifest is only produced when stubs exist. With 0 stubs the prerequisite is vacuously true, yet the ABC contract-to-test inspection loop (Steps 2-7) — the actual purpose of /find-omissions — is unreachable, so contract-to-test gaps go undetected in the pipeline's steady state.

### Motivation
- 【Motivation】A tree with all stubs resolved is the normal steady state of the pipeline. Without this change, /find-omissions can never run again after a full resolve, silently disabling omission discovery for reviewed/remanded tickets.

### Constraints
- 【Constraints】(1) Behavior must not change when a manifest exists or when stubs exist without a manifest — exit codes and the BLOCKED message stay identical. (2) The stub scan must reuse the same scan /consolidate-stubs Step 1 uses (find-all-stubs.js) so the decision is consistent with the consolidate inventory. (3) No new dependencies. (4) find-omissions.md Pre-flight must keep both gate lines (validate-graph-arg then require-consolidated-manifest), in order — the doc test asserts this.

## Scope

- CHANGE .claude/scripts/tickets/require-consolidated-manifest.js: add a 0-stub PASS branch that exits 0 and prints a friendly actionable English stdout message (skip Step 1, proceed to Step 2)
- CHANGE tests/require-consolidated-manifest.test.cjs: the existing no-manifest-exit-2 test must create a workspace WITH a stub; add tests for 0-stub PASS (exit 0 + stdout instruction), stub-present BLOCK (exit 2), scan-error BLOCK (exit 2)
- CHANGE .claude/commands/find-omissions.md: document the 0-stub pass-through in the Pre-flight section, keeping both gate lines (validate-graph-arg then require-consolidated-manifest) in order
- NON-CHANGE print-manifest-for-find-omissions.js: still refuses to write a manifest for 0 stubs — that is the intended trigger of the new branch
- NON-CHANGE batch-create-resolving-tickets.js: Step 1 still consumes the manifest; in the 0-stub case Step 1 is skipped, never invoked
- IMPACT /find-omissions pre-flight: a 0-stub tree now runs to completion — Step 1 no-op skipped, Steps 2-10 proceed unchanged
- **Scope of changes (describe each change comprehensively):**
  - [File/module path] .claude/scripts/tickets/require-consolidated-manifest.js (production) + tests/require-consolidated-manifest.test.cjs (Red) + .claude/commands/find-omissions.md (docs)
  - [Action: add/modify/remove/rename/refactor] modify — add a 0-stub PASS branch to the gate; keep the existing manifest check and the BLOCKED path byte-identical
  - [What specifically changes] main() is split into a pure decision function decideGate(dir, countStubs) -> 'pass-manifest' | 'pass-zero-stubs' | 'block'; countStubs defaults to scanning cwd with find-all-stubs.scanDirectory('.', []).length; a throwing scan is caught and classified 'block' (C004). main() maps the decision to exit code + stdout/stderr messages
  - [Before → After (behavior/signature)] Before: exit 0 iff a CONSOLIDATED-MANIFEST-*.json exists, else exit 2. After: exit 0 iff (manifest exists) OR (no manifest AND stub count === 0); exit 2 when no manifest AND stubs exist, and when the scan itself fails
  - [API contract change (if any)] module.exports grows from { findConsolidatedManifest, MANIFESTS_DIR, MANIFEST_PREFIX } to also export decideGate and countStubs (and the two stdout phrase constants), enabling in-process C004 testing without a subprocess
  - [Data schema change (if any)] None — no Tickets.json, manifest, or JSON file shape changes
  - [Config/env change (if any)] None — no env vars; the scan root stays cwd (the Tickets.json root), matching /consolidate-stubs
  - [Dependency added/removed (if any)] None — find-all-stubs.js is in-tree and already exports scanDirectory; no new package
- **Out of scope (items intentionally excluded, with justification):**
  - [Excluded item] print-manifest-for-find-omissions.js 0-stub refusal
  - [Why excluded — separate ticket / future phase / not applicable] It is the intended trigger of the new branch: a 0-stub tree must NOT receive a manifest; the gate compensates instead of the printer changing
- **Affected areas (components/systems impacted, even without direct modification):**
  - [Affected component] /find-omissions command (find-omissions.md Pre-flight section) + the tests/require-consolidated-manifest.test.cjs suite
  - [Nature of impact: performance / security / API surface / data format / …] Behavioral: pre-flight now passes in 0-stub trees (previously blocked). Documentation: Pre-flight prose must describe the 0-stub pass-through and the skip-Step-1 instruction
  - [Corresponding change needed Y/N + details] Yes — find-omissions.md Pre-flight prose is updated in this ticket; the two gate lines (validate-graph-arg || exit 2, require-consolidated-manifest || exit 2) must remain present and ordered, which the existing doc test asserts

## Implementation Target Files

- `.claude/scripts/tickets/require-consolidated-manifest.js`
- `.claude/scripts/tickets/review/find-all-stubs.js`
- `tests/require-consolidated-manifest.test.cjs`
- `.claude/commands/find-omissions.md`

## Investigation

- require-consolidated-manifest.js:55-61 — main() calls findConsolidatedManifest(cwd); if null, prints '[find-omissions] BLOCKED: No consolidated manifest found under ./manifests/ (CONSOLIDATED-MANIFEST-*.json).' + 'Action: run /consolidate-stubs first (Steps 1-5), then re-run /find-omissions.' to stderr and process.exit(2); else exit 0. module.exports = { findConsolidatedManifest, MANIFESTS_DIR, MANIFEST_PREFIX }.
- findConsolidatedManifest (lines 34-50) — reads ./manifests/ and returns the lexicographically newest CONSOLIDATED-MANIFEST-<YYYYMMDDhhmmss>.json (filenames are chronological; matches the gate's ls -t | head -1); returns null when manifests/ is missing or no matching file exists.
- print-manifest-for-find-omissions.js:168-169 — refuses to write a manifest when 0 [::UNIT::]-tagged markers exist: 'no [::UNIT::] tagged markers found under ' + root. A 0-stub tree therefore legitimately has NO CONSOLIDATED-MANIFEST-*.json — the root cause of the gap.
- consolidate-stubs.md Steps 4-5 — the manifest is emitted only by batch-update-stub.js (via print-manifest); with no stubs there are no units, so no manifest is ever written.
- find-all-stubs.js — scanDirectory(dir, results) pushes { file, line, content } into results, skipping dot-entries and SKIP_DIRS = { node_modules, target, .git, .claude, fixtures }, scanning only CFG.review.targetExtensions; scanFile matches containsStubMarker && !isStubInQuotes (single-sourced in lib/stub-marker-scan.js). module.exports = { main, scanDirectory, scanFile } → in-process reuse with no subprocess is possible.
- Step 2+ independence (verified by repo-wide grep for CONSOLIDATED-MANIFEST/manifests): only 5 files under .claude/scripts/tickets/ reference the manifest — consolidate-stubs-gate.sh, clean-consolidation-artifacts.js, batch-update-stub.js, require-consolidated-manifest.js, print-manifest-for-find-omissions.js. None of the Step 2-10 scripts do: get-next-check-target-ticket.js builds its queue from Tickets.json statuses 'reviewed'/'remanded' (create-check-target-tickets-cmds.js:52); add-omission-ticket.js always sets newTicket.stubs = [] (line 124); phasify-omissions.js:752 skips tickets with stubs.length === 0, so its stub-key rewrite is a no-op on empty stubs; clean-consolidation-artifacts.js returns [] when manifests/ is missing (idempotent exit 0). Only Step 1 batch-create-resolving-tickets.js consumes the manifest — and it is skipped in the 0-stub flow.
- tests/require-consolidated-manifest.test.cjs — 6 existing tests. The no-manifest test (lines 49-61) creates an EMPTY workspace and expects exit 2; under the new 0-stub PASS this workspace would exit 0, so this test MUST be rewritten to seed a stub file (workspace with a [::STUB::] marker).
- find-omissions.md:11-20 — Pre-flight runs validate-graph-arg.js then require-consolidated-manifest.js (both || exit 2); the doc test asserts both lines present in order. The Pre-flight prose needs a 0-stub pass-through sentence.

## Acceptance Criteria

- Happy path: no manifest + 0 stubs -> require-consolidated-manifest.js exits 0 and prints a clear English explanation (no stubs to consolidate → manifest legitimately absent) plus the actionable instruction 'SKIP Step 1' and 'Proceed to Step 2'
- Error case: no manifest + 1+ stubs -> exits 2; stderr carries '[find-omissions] BLOCKED:' and the action 'run /consolidate-stubs first' (unchanged from today)
- Edge case: a failing stub scan exits 2 — a scan error is never treated as 0 stubs (C004)

## Invariants

- 【Normal】The gate exits 0 iff (a) a CONSOLIDATED-MANIFEST-*.json exists, or (b) no manifest exists AND the stub scan counts exactly 0 stubs.
- 【On error】A stub-scan failure never yields a 0-stub PASS: the gate blocks (exit 2) instead of misclassifying an unknown tree state as clean.
- 【Internal state】The gate is pure over the tree: same tree (manifest set + stub set) → same decision; no mutable state, no dependence on wall-clock time beyond manifest filename ordering.
- 【Boundary】The stub-count threshold is exactly 0: count === 0 → PASS, count >= 1 → BLOCK. The count must come from the same scan /consolidate-stubs Step 1 uses.

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — pre-flight manifest gate -> stub-count fallback

- **Precondition**: no CONSOLIDATED-MANIFEST-*.json under ./manifests/ AND stub scan finds 0 stubs
- **Postcondition**: process exits 0 and stdout instructs skip Step 1 / proceed to Step 2
- **Invariant**: the gate never exits 0 while un-consolidated stubs exist

### C002 — pre-flight manifest gate -> stub-count block

- **Precondition**: no manifest AND stub scan finds 1+ stubs
- **Postcondition**: process exits 2 with a stderr BLOCKED cause/action message (run /consolidate-stubs first)
- **Invariant**: the gate never exits 0 while un-consolidated stubs exist

### C003 — pre-flight manifest gate -> legacy pass

- **Precondition**: a CONSOLIDATED-MANIFEST-*.json exists under ./manifests/
- **Postcondition**: process exits 0
- **Invariant**: the gate decision is deterministic for identical tree state

### C004 — pre-flight manifest gate -> scan-error guard

- **Precondition**: the stub scan throws or fails
- **Postcondition**: process exits 2 (a failed scan is never treated as 0 stubs)
- **Invariant**: the gate decision is deterministic for identical tree state

## Boy Scout Rule

- Refactor require-consolidated-manifest.js main() into a pure, verb-named decision function decideGate(dir, countStubs) -> 'pass-manifest' | 'pass-zero-stubs' | 'block' so the gate reads top-to-bottom as prose: find the manifest → if present pass; else count stubs → if 0 pass with a skip-Step-1 instruction; else block. Extract the two stable stdout phrases into named constants (SKIP_STEP_1_PHRASE, PROCEED_TO_STEP_2_PHRASE) so the message text is single-sourced and the tests assert the same constants. Extract countStubs(dir) wrapping find-all-stubs.scanDirectory so the scan contract is explicit and injectable for C004. Keep findConsolidatedManifest byte-identical. No unrelated refactoring of sibling scripts (Boy Scout confined to the touched file).

## Test Plan

### Unit Tests

- UT: [Normal] no manifest + 0 stubs -> exit 0: makeWorkspace() with no manifests/ dir and no stub files, execFileSync require-consolidated-manifest.js in that cwd -> exitStatus === 0
- UT: [Output] no manifest + 0 stubs -> stdout carries the skip-Step-1 instruction: capture stdout from execFileSync and assert it includes both stable phrases 'SKIP Step 1' and 'Proceed to Step 2' (single-sourced constants, not full-text equality)
- UT: [Error] no manifest + 1+ stubs -> exit 2: workspace containing src/a.js with a marker line '// [::STUB::] P0-1: reason -- Implement', no manifest -> exitStatus === 2; stderr includes '[find-omissions] BLOCKED:' and 'run /consolidate-stubs first'
- UT: [Boundary] a CONSOLIDATED-MANIFEST-*.json exists under ./manifests/ -> the gate process exits 0 (unchanged legacy behavior): writeManifest(dir) then exitStatus === 0
- UT: [Boundary] ROLLBACK-*.json alone does not satisfy the gate -> exit 2: write only manifests/ROLLBACK-20260101000000.json, expect exit 2
- UT: [Error] stub scan failure is not treated as 0 stubs -> exit 2 (C004): call the exported decision function with a scan that throws -> decision 'block'; a failed scan never yields a 0-stub PASS
- UT: [Invariant] findConsolidatedManifest returns the newest matching manifest path / null when absent (existing unit coverage), and gate decision is deterministic for identical tree state

### Integration Tests

- IT: [Integration point] require-consolidated-manifest.js <-> find-all-stubs.js scanDirectory: the gate reuses the exact scan /consolidate-stubs Step 1 uses, so the 0-stub decision is consistent with the consolidate inventory
- IT: [Verification] end-to-end in a temp workspace: add src/a.js with a [::STUB::] marker -> gate exits 2; remove the file -> gate exits 0 with the skip-Step-1 stdout; proves the scan result (not just manifest presence) drives the decision
- IT: [Prerequisites] the gate runs from the Tickets.json root (cwd); the scan root is cwd and respects SKIP_DIRS (node_modules, target, .git, .claude, fixtures) and CFG.review.targetExtensions
- IT: [Related tickets] PX-138 (manifest pre-flight contract), PX-2 / PX-139 (find-all-stubs / stub-marker-scan single-sourcing)

### Exceptions

- Full-text equality of the skip-Step-1 stdout message is not testable as an exact-string assertion: the message is operator-facing prose that may be reworded independently of behavior, and asserting the exact text couples the test to cosmetic copy. This is not a design defect — the behavioral requirement (exit 0) and the two stable action phrases ('SKIP Step 1', 'Proceed to Step 2') ARE asserted against the same constants the gate prints, so the requirement is fully testable.

### Plan Test Code (concrete code)

- UT: [Normal] C001 precondition+postcondition — no manifest + 0 stubs -> exit 0 with skip-Step-1 stdout
```js
const dir = makeWorkspace();  // precondition: no manifests/ dir and no stub files
const res = runGate(dir);     // helper: execFileSync with stdio capture -> { status, stdout, stderr }
assert.strictEqual(res.status, 0);  // postcondition: process exits 0
assert.ok(res.stdout.includes('SKIP Step 1'));        // postcondition: stdout instructs skip Step 1
assert.ok(res.stdout.includes('Proceed to Step 2'));  // postcondition: stdout instructs proceed to Step 2
```
- UT: [Error] C002 precondition+postcondition — no manifest + 1+ stubs -> exit 2 with BLOCKED stderr
```js
const dir = makeWorkspace();
writeStubFile(dir);  // precondition: a [::STUB::] marker exists under the tree
const res = runGate(dir);
assert.strictEqual(res.status, 2);  // postcondition: process exits 2
assert.ok(res.stderr.includes('[find-omissions] BLOCKED:'));  // postcondition: cause message
assert.ok(res.stderr.includes('run /consolidate-stubs first'));  // postcondition: action directive
```
- UT: [Boundary] C003 precondition+postcondition — a CONSOLIDATED-MANIFEST-*.json exists under ./manifests/ -> exit 0
```js
const dir = makeWorkspace();
writeManifest(dir);  // precondition: CONSOLIDATED-MANIFEST-20260101000000.json exists
assert.strictEqual(exitStatus(dir), 0);  // postcondition: process exits 0 (legacy unchanged)
```
- UT: [Error] C004 precondition+postcondition — a throwing stub scan is never treated as 0 stubs
```js
const { decideGate } = require('../.claude/scripts/tickets/require-consolidated-manifest.js');
const throwingScan = () => { throw new Error('scan failed'); };  // precondition: the stub scan throws
assert.strictEqual(decideGate(dir, throwingScan), 'block');  // postcondition: decision is 'block', never 'pass-zero-stubs'
```
- UT: [Invariant] C001/C002 — the gate never exits 0 while un-consolidated stubs exist
```js
const dir = makeWorkspace();
writeStubFile(dir);  // un-consolidated stub present, no manifest
assert.strictEqual(decideGate(dir), 'block');  // invariant: not 'pass-zero-stubs'
writeManifest(dir);
assert.strictEqual(decideGate(dir), 'pass-manifest');  // manifest present -> deterministic pass
```
- UT: [Invariant] C003/C004 — the gate decision is deterministic for identical tree state
```js
const dirA = makeWorkspace();
const dirB = makeWorkspace();
assert.strictEqual(decideGate(dirA), decideGate(dirB));  // same empty tree -> same decision ('pass-zero-stubs')
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| require-consolidated-manifest.js: main() blocked (exit 2) whenever no CONSOLIDATED-MANIFEST-*.json exists; exported only findConsolidatedManifest/MANIFESTS_DIR/MANIFEST_PREFIX | gate passes (exit 0) when no manifest AND the stub scan finds 0 stubs, printing a skip-Step-1 stdout instruction; exports countStubs/decideGate/SKIP_STEP_1_PHRASE/PROCEED_TO_STEP_2_PHRASE | Split main() into decideGate(dir, countStubsImpl) + countStubs(dir); added the 0-stub PASS branch (C001), kept the stub-present BLOCK (C002) and manifest-present PASS (C003) byte-identical, mapped a throwing scan to 'block' (C004) |
| tests/require-consolidated-manifest.test.cjs: 6 tests; the no-manifest test expected exit 2 on an empty workspace | 10 tests; the no-manifest and ROLLBACK tests seed a stub file (the 0-stub fallback would otherwise PASS them); added the C001 0-stub PASS + stdout instruction, C004 throw->block, and both invariant tests | Red phase: rewrote the suite to cover contracts C001-C004 with concrete node:test code |
| find-omissions.md Pre-flight: 'require-consolidated-manifest.js exits 0 when ... exists and exits 2 otherwise' | Pre-flight documents the 0-stub pass-through and the SKIP Step 1 / Proceed to Step 2 instruction; both gate lines kept in order | Docs: explained the 0-stub branch in the Pre-flight section |

## Notes in Prior Implementation Rounds

Implementation steps: (1) Red — update tests/require-consolidated-manifest.test.cjs first (rewrite the no-manifest test to create a stub file; add 0-stub PASS, stdout-instruction, stub-present BLOCK, scan-failure BLOCK via injectable scan). (2) Green — refactor require-consolidated-manifest.js to decideGate + 0-stub branch. (3) Docs — update find-omissions.md Pre-flight. (4) Run make test. Risks: the stub scan must be cheap and deterministic; a scan error must conservatively block. Caveat: re-running /find-omissions after full success still requires a fresh /consolidate-stubs when stubs exist.

- 【Implementation steps】(1) Red: update the no-manifest test to seed a stub file so it still expects exit 2; add the 0-stub PASS (exit 0 + stdout), stub-present BLOCK, and scan-failure BLOCK (injectable scan) tests. (2) Green: split main() into decideGate(dir, countStubs); add the 0-stub branch; wire main() to it. (3) Docs: Pre-flight section gains the 0-stub pass-through + skip-Step-1 instruction. (4) make test → all green.
- 【Risks】Scan cost on large trees is bounded by the same SKIP_DIRS/targetExtensions /consolidate-stubs already accepts; a scan regression that hides stubs would wrongly PASS — mitigated by C004 (throw → block) and the stub-present BLOCK test.
- 【Caveats】The 0-stub pass is the ONLY new path; every other branch keeps today's exit code and message. Re-running /find-omissions after full success still requires a fresh /consolidate-stubs whenever stubs exist. The doc test asserts both gate lines remain in order.
- 【Open items】None — the design is fully determined by contracts C001-C004.
- 【Future improvements】Consider a tombstone manifest for a future 0-stub run if a later step ever needs to distinguish 'never consolidated' from 'consolidated empty'; not needed today.
Implementation summary:
- Changed files: .claude/scripts/tickets/require-consolidated-manifest.js, tests/require-consolidated-manifest.test.cjs, .claude/commands/find-omissions.md
- Key changes: 0-stub PASS branch with stdout skip-Step-1 instruction (C001); stub-present BLOCK preserved (C002); manifest-present PASS preserved (C003); throwing-scan -> 'block' guard (C004).
- Test results: 10/10 tests pass (node --test tests/require-consolidated-manifest.test.cjs); quality checks 0 issues.
- Verification command: node --test tests/require-consolidated-manifest.test.cjs (no Makefile test target covers this file).
Review report:
- Static quality check: passed (0 issues)
- Translatability: no issues
- Tests: 16/16 passed
- Annotation verify: 1 source file, 0 missing, 0 AMBIGUOUS
- Contracts C001-C004: all fulfilled (verify-final-contracts coverage 100%)
- targetStubs/targetCrimes: verified_empty
- Issues found and fixes applied: none (implementation clean)

## PX-148 — implemented at 3 locations

### .claude/scripts/tickets/require-consolidated-manifest.js

- Line 64
```javascript
function countStubs(dir) {
```

- Line 77
```javascript
function decideGate(dir, countStubsImpl) {
```

- Line 91
```javascript
function main() {
```
