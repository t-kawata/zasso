# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-154: Persist confirmed TOC tree in CRYSTALIZE-Status.json; hierarchical-path heading IDs with parent-existence enforcement

**Ticket Key**: PX-154 · **Phase**: -1

---

## Background

- 【Goal】Step 1 グリルで確定した見出しを、AI の会話記憶に依存せず実際の JSON ファイル（CRYSTALIZE-Status.json）に階層構造＋確定内容つきで永続化する。ID スキームを階層パス（H1, H1-1, H1-2, H1-2-1, H2, H2-1, ...）に変更し、親不在の子（H2 無しの H2-1）を構造違反として拒否する。
- 【Purpose】現状の update-step-status.js は confirm-heading で ID 文字列のみを confirmedIds に記録し、見出しタイトル・確定内容・階層が永続化されない。AI の記憶だけに依存するため、セッションを跨いだ Step 4（README 生成）ができない。また ID スキームが「レベル＋連番」解釈で、親子関係が ID から導出できず構造が曖昧。
- 【Motivation】ユーザー指摘により確定: ID は階層パス（親 = 最後の -<n> を除去、子は親の後にのみ存在可能）。派生値（parentId 等）は保存しない。
- 【Constraints】既存の CRYSTALIZE-Status.json の grill セクションを拡張（proposedIds/confirmedIds → toc.nodes[]）。validate-toc-proposal.js の ID パターンと親存在チェックを更新。TDD Red→Green→Refactor 必須。

## Scope

- .claude/scripts/crystalize-readme/validate-toc-proposal.js — modify: ID_PATTERN を階層パス /^H[1-9][0-9]*(-[1-9][0-9]*)*$/（1〜6 セグメント）に変更。入力の seenIds を existingIds（既存ノード ID 全体）に置換し、親存在チェック（parent(id) ∈ existingIds、トップレベルは親不要）と一意性を検証。エラーに Fix 指示を維持・拡充。
- .claude/scripts/crystalize-readme/update-step-status.js — modify: grill.proposedIds/confirmedIds を廃止し grill.toc.nodes[]（id/heading/level/confirmedContent/status）に置換。propose-heading は提案 JSON（validate と同じ形）を stdin で受け、親存在チェック付きで status=proposed として記録。confirm-heading は {id, confirmedContent} を受けて status=confirmed。level と親は ID から導出（parentId は保存しない）。tocApproved は全ノード確定から導出。reset-toc はツリーをクリア。
- .claude/commands/crystalize-readme.md — modify: Step 1 の提案 JSON 例を階層パス ID（H1, H1-1, ...）に更新し、propose→confirm で確定内容を CRYSTALIZE-Status.json に記録する手順を明記。
- docs/DESIGN-OF-CRYSTALIZE-README.md — modify: §5 決定論表 / §6 Step 1 / §8 スクリプト一覧 / §12 変更履歴 を階層パス ID と toc.nodes 永続化に合わせて更新。
- tests/crystalize-readme/validate-toc-proposal.test.cjs — modify: 階層パス ID の正常系（H1, H1-1, H1-2-1）・親不在エラー・一意性・決定論・parentIdOf 導出をカバー。
- tests/crystalize-readme/update-step-status.test.cjs — modify: toc.nodes の propose/confirm/完了判定をカバー（proposedIds/confirmedIds を削除）。
- API contract change: validate-toc-proposal.js の入力が seenIds → existingIds（既存ノード ID 全体）に変更。update-step-status.js の propose-heading が提案 JSON 入力、confirm-heading が {id, confirmedContent} 入力に変更。
- Data schema change: CRYSTALIZE-Status.json の grill を {tocApproved, examplesApproved, toc:{nodes:[{id,heading,level,confirmedContent,status}]}} に拡張。既存ファイルは readStatus で toc デフォルト補完（後方互換）。
- Config/env change: なし。依存追加・削除なし（node:fs, node:path, node:child_process のみ）。
- Out of scope: Step 2（examples グリル）の作り直し、README 生成（Step 4）の toc.nodes 消費実装は対象外。
- Out of scope reason: examples グリルと Step 4 の README 生成は本チケットの ID スキーム修正・永続化とは独立した工程。
- Affected: /crystalize-readme を実行する全セッション — Step 1 の確定記録が ID 一覧から構造化ツリー（toc.nodes）に変わる。
- Impact nature: CRYSTALIZE-Status.json の grill スキーマ拡張と propose/confirm コマンド形状の変更。
- Response: readStatus で旧形式（proposedIds/confirmedIds）を toc.nodes に移行補完。Step 1 を再実行する場合は reset-to-step 1 で grill をリセット。

## Implementation Target Files

- `.claude/scripts/crystalize-readme/validate-toc-proposal.js`
- `.claude/scripts/crystalize-readme/update-step-status.js`
- `.claude/commands/crystalize-readme.md`
- `docs/DESIGN-OF-CRYSTALIZE-README.md`
- `tests/crystalize-readme/validate-toc-proposal.test.cjs`
- `tests/crystalize-readme/update-step-status.test.cjs`

## Investigation

- validate-toc-proposal.js（PX-153 実装）: 現状 ID_PATTERN=/^H[1-6](-[1-9][0-9]*)?$/、入力 seenIds（同一ターン内の提案済み ID）。親存在の概念なし。ID は「レベル＋連番」解釈で、親子関係が ID から導出できない。
- update-step-status.js（PX-153 実装）: grill={tocApproved, examplesApproved, proposedIds[], confirmedIds[]}。propose-heading <id> / confirm-heading <id> は ID 文字列のみを記録。isTocComplete() は proposedIds ⊆ confirmedIds で判定。見出しタイトル・確定内容・階層は永続化されない。
- ユーザー指摘（PX-154 確定）: ID は階層パス（H1, H1-1, H1-2, H1-2-1, H2, H2-1, ...）。親 = 最後の -<n> を除去して導出。子は親の後にのみ存在可能（H2 無しの H2-1 は構造違反）。派生値 parentId は保存しない。
- 既存テスト: validate-toc-proposal.test.cjs（PX-153）は seenIds を使用。update-step-status.test.cjs は proposedIds/confirmedIds を使用。どちらも PX-154 で書き換えが必要。

## Acceptance Criteria

- Happy path: Step 1 で各見出しが階層パス ID（H1, H1-1, ...）で提案され、親存在チェックを通過したものだけが提示される。ユーザー回答ごとに confirm-heading が confirmedContent を CRYSTALIZE-Status.json の grill.toc.nodes に記録し、全確定で tocApproved=true → Step 2 へ。
- Error case: 親不在の ID（H2 無しの H2-1）は validate-toc-proposal.js と propose-heading の両方で拒否され exit 1。確定内容は AI の記憶ではなく JSON に永続化される。
- Edge case: トップレベル見出し（H1, H2）は親不要。同一親配下の兄弟は 1..n の連番。セッション再開時に status から確定済みツリーを復元できる。

## Invariants

- 【Normal establishment】各見出し提案は validate-toc-proposal.js で valid:true になるまで提示されない。ID は階層パス /^H[1-9][0-9]*(-[1-9][0-9]*)*$/（1〜6 セグメント）に従い、existingIds に対して一意。
- 【Invariant on error】親不在の ID（H2 無しの H2-1）は validate-toc-proposal.js と propose-heading の両方で拒否され、exit 1。検証失敗の提案はユーザーに提示されない。
- 【Internal state invariant】status.grill.toc.nodes に提案/確定ノード（id/heading/level/confirmedContent/status）が文書順で記録される。parentId は保存しない（ID から導出）。
- 【Boundary invariant】全ノードが confirmed になるまで tocApproved=true にならない。Step 2 への進行はブロックされる。TOC の末尾の見出しは常に「examples（実装サンプル）の仕様と設計」。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — Step 1 grill → per-proposal validation gate

- **Precondition**: A heading proposal {id, heading, contentOptions[], recommendation, reason, existingIds?} is passed to validate-toc-proposal.js. id は階層パス（H1, H1-1, H1-2-1, ...）、existingIds は既存ノード ID 全体。
- **Postcondition**: Returns {valid:boolean, errors:string[]}. valid=true only when: id matches hierarchical path pattern (1-6 segments), id not in existingIds, parent(id) in existingIds (top-level has no parent), heading non-empty, contentOptions 2-4 options, recommendation in contentOptions, reason non-empty.
- **Invariant**: Validation is deterministic. A valid proposal always has a hierarchical-path id whose parent (if any) is already in existingIds.

### C002 — Step 1 grill → completion gate (CRYSTALIZE-Status.json)

- **Precondition**: status.grill.toc.nodes records the proposed heading nodes (status=proposed) in document order. update-step-status.js implements confirm-heading.
- **Postcondition**: confirm-heading {id, confirmedContent} は該当ノードに confirmedContent をセットし status=confirmed にする。全ノード確定時に tocApproved=true となり、end-step 1 で Step 2 へ進める。
- **Invariant**: 親不在のノードは propose-heading で拒否される。未確定ノードが残る限り tocApproved は false。level と親は ID から導出（保存しない）。

## Boy Scout Rule

- validate-toc-proposal.js: parentIdOf(id) を純関数として抽出（ID の最後のセグメント除去、トップレベルは null）。ID_PATTERN / MAX_ID_DEPTH(6) を名前付き定数化。validateProposal は「一意性→親存在→その他」の順で検証し、エラーを errors[] に構造化。
- update-step-status.js: readGrill / writeGrill / isTocComplete を関数抽出し、toc.nodes の操作（findNode / upsertNode）を単一責務関数に分割。propose/confirm の入力を JSON 化し、exitWithError で構造化エラーを出力。
- 既存の seenIds / proposedIds / confirmedIds 参照を existingIds / toc.nodes に置換し、デッドフィールドを残さない。

## Test Plan

### Unit Tests

- UT: [Normal] validate-toc-proposal accepts a top-level hierarchical-path id — {id:'H1', heading:'クイックスタート', contentOptions:['...','...','...'], recommendation:'...', reason:'...', existingIds:[]} → valid:true
- UT: [Normal] validate-toc-proposal accepts a child whose parent is in existingIds — {id:'H1-1', existingIds:['H1']} → valid:true
- UT: [Normal] validate-toc-proposal accepts a grandchild whose parent exists — {id:'H1-2-1', existingIds:['H1','H1-2']} → valid:true
- UT: [Error] validate-toc-proposal rejects a child whose parent is absent — {id:'H2-1', existingIds:['H1']} → valid:false, errors に parent-not-found (C001: 親存在)
- UT: [Error] validate-toc-proposal rejects an id violating the path pattern — H0, H1-0, 'free text', 42, H1-2-3-4-5-6-7 (7 セグメント) → valid:false (C001-Pre: /^H[1-9][0-9]*(-[1-9][0-9]*)*$/ かつ 1〜6 セグメント)
- UT: [Error] validate-toc-proposal rejects a duplicate id against existingIds — {id:'H1', existingIds:['H1']} → valid:false (C001: 一意性)
- UT: [Error] validate-toc-proposal rejects contentOptions outside 2-4 / empty option / recommendation not in options / empty reason / empty heading (既存ルール維持)
- UT: [Boundary] validate-toc-proposal derives parent by dropping the last -<n> segment — parentIdOf('H1-2-1') = 'H1-2', parentIdOf('H1') = null
- UT: [Invariant] validate-toc-proposal is deterministic — identical input yields identical output (C001-Inv)
- UT: [Normal] update-step-status propose-heading records a node {id, heading, level, status:proposed} into grill.toc.nodes from a proposal JSON; level derived from id (H1-1 → 2)
- UT: [Error] update-step-status propose-heading rejects a child whose parent node is not in the tree — propose H2-1 without H2 → exit 1 (C002-Inv)
- UT: [Normal] update-step-status confirm-heading sets confirmedContent and status=confirmed on the node; isTocComplete true only when every node is confirmed
- UT: [Error] update-step-status confirm-heading rejects an unproposed id → exit 1 (C002-Inv)
- UT: [Invariant] update-step-status stores no parentId — level and parent are derived from the id; the toc tree is reconstructable from document-ordered nodes (C002-Inv)

### Integration Tests

- IT: [Integration point] Step 1 grill — the same proposal JSON passed to validate-toc-proposal.js is recorded by update-step-status.js propose-heading; confirm-heading then persists confirmedContent into CRYSTALIZE-Status.json grill.toc.nodes
- IT: [Verification] a scripted propose(H1) → propose(H1-1) → confirm(H1,content) → confirm(H1-1,content) sequence leaves grill.toc.nodes with two confirmed nodes and tocApproved=true; status output prints the tree with id/heading/confirmedContent/status
- IT: [Prerequisites] both scripts run under node --test (CommonJS), no new dependencies, no network
- IT: [Related tickets] PX-153 introduced the per-heading grill and the validation gate; PX-154 corrects the ID scheme to hierarchical paths and replaces id-only lists with the durable toc.nodes tree

### Exceptions

- ユーザーによる ID 単位の対話確認（A/B/C/Yes/No 回答）は自動テスト不能 — 人間が介在する操作のため cannot test。設計欠陥ではない: 決定論ゲート（validate-toc-proposal）と完了ゲート（update-step-status confirm-heading）が単体/統合テストで検証される。
- 使い方に絞った TOC の AI 合成（非決定論）は単体テスト不能 — 内容合成そのもののため not testable。設計欠陥ではない: 非決定論ステップは決定論的な validate-toc-proposal ゲートで制約される。

### Plan Test Code (concrete code)

- UT: [C001-Pre] 階層パス ID + 親存在の提案を受理
```js
const { validateProposal, parentIdOf } = require('../../.claude/scripts/crystalize-readme/validate-toc-proposal.js');
const proposal = { id: 'H1-1', heading: 'アカウントの追加', contentOptions: ['コードA', 'コードB', 'コードC'], recommendation: 'コードA', reason: 'README の使い方に即しているため', existingIds: ['H1'] };
const result = validateProposal(proposal);
assert.deepEqual(result, { valid: true, errors: [] });
```
- UT: [C001-Post] valid の条件を assert — パターン一致・一意・親存在
```js
assert.match(proposal.id, /^H[1-9][0-9]*(-[1-9][0-9]*)*$/);
assert.ok(!proposal.existingIds.includes(proposal.id));
assert.ok(proposal.existingIds.includes(parentIdOf(proposal.id))); // H1-1 → H1
assert.equal(result.valid, true);
```
- UT: [C001-Inv] 決定論と parentIdOf 導出（保存しない派生値）
```js
assert.deepEqual(validateProposal(proposal), validateProposal(proposal));
assert.equal(parentIdOf('H1-2-1'), 'H1-2');
assert.equal(parentIdOf('H1'), null);
```
- UT: [C002-Pre] grill.toc.nodes に提案ノードが文書順で記録される
```js
const { createDefaultStatus, executeProposeHeading } = require('../../.claude/scripts/crystalize-readme/update-step-status.js');
const status = createDefaultStatus(graphPath);
executeProposeHeading(status, { id: 'H1', heading: 'クイックスタート' });
executeProposeHeading(status, { id: 'H1-1', heading: 'アカウントの追加' });
assert.equal(status.grill.toc.nodes.length, 2);
assert.equal(status.grill.toc.nodes[0].status, 'proposed');
assert.equal(status.grill.toc.nodes[1].level, 2); // H1-1 → level 2
```
- UT: [C002-Post] confirm-heading が confirmedContent をセットし、全確定で tocApproved
```js
const { executeConfirmHeading, isTocComplete } = require('../../.claude/scripts/crystalize-readme/update-step-status.js');
executeConfirmHeading(status, { id: 'H1', confirmedContent: 'クイックスタート本文' });
assert.equal(status.grill.toc.nodes[0].confirmedContent, 'クイックスタート本文');
assert.equal(status.grill.toc.nodes[0].status, 'confirmed');
assert.equal(isTocComplete(status), false); // H1-1 未確定
executeConfirmHeading(status, { id: 'H1-1', confirmedContent: 'アカウント追加本文' });
assert.equal(isTocComplete(status), true);
assert.equal(status.grill.tocApproved, true);
```
- UT: [C002-Inv] 親不在の propose は拒否され、level/親は ID から導出（parentId 非保存）
```js
const { executeProposeHeading } = require('../../.claude/scripts/crystalize-readme/update-step-status.js');
assert.throws(() => executeProposeHeading(status, { id: 'H2-1', heading: 'X' }), /parent/);
assert.equal(status.grill.toc.nodes.find(n => n.id === 'H1-1').level, 2);
assert.ok(!('parentId' in status.grill.toc.nodes[0]));
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| ID_PATTERN=/^H[1-6](-[1-9][0-9]*)?$/ (レベル＋連番) | ID_PATTERN=/^H[1-9][0-9]*(-[1-9][0-9]*)*$/ (階層パス 1〜6 セグメント) | validate-toc-proposal.js の ID スキームを階層パスに変更。parentIdOf() で親を ID から導出。existingIds に対する親存在チェックを追加。 |
| 入力 seenIds（同一ターン内の提案済み ID） | 入力 existingIds（既存ノード ID 全体）に置換し、親存在チェックを追加 | validate-toc-proposal.js の C001 を更新。親不在の ID（H2 無しの H2-1）は valid:false。 |
| grill={tocApproved,examplesApproved,proposedIds[],confirmedIds[]}（ID のみ） | grill={tocApproved,examplesApproved,toc:{nodes:[{id,heading,level,confirmedContent,status}]}} | update-step-status.js を toc.nodes 化。propose-heading は提案 JSON、confirm-heading は {id,confirmedContent} を stdin で受け、level/親は ID から導出（parentId 非保存）。readStatus で旧形式を移行補完。 |
| Step 1 の例は H1 クイックスタート / seenIds | Step 1 の例は H1-1 アカウントの追加 / existingIds、propose→confirm の記録手順を明記 | crystalize-readme.md と設計書を階層パス ID + toc.nodes 永続化に更新。 |

## Notes in Prior Implementation Rounds

- 【Implementation steps】(1) TDD: validate-toc-proposal.js の ID_PATTERN 変更・existingIds・親存在チェック・parentIdOf 導出を RED→GREEN→REFACTOR。(2) TDD: update-step-status.js の toc.nodes 化・propose/confirm JSON 入力・完了判定を RED→GREEN→REFACTOR。(3) コマンドファイル Step 1 を更新。(4) 設計書を更新。(5) make test-crystalize-readme で全緑確認。
- 【Risks】proposedIds/confirmedIds の削除は既存 CRYSTALIZE-Status.json と非互換 → readStatus で旧形式を toc.nodes に移行補完し後方互換を保つ。
- 【Caveats】parentId は保存しない（ID の最後のセグメント除去で導出）。DRY 維持。既存 PX-153 のテスト（seenIds 使用）は existingIds に置換。
- 【Open items】README 生成（Step 4）が grill.toc.nodes を消費する実装は後続チケットで。
- 【Future improvements】目次の並べ替え・削除（兄弟の再採番）を後続で検討。
Implementation summary (PX-154):
- Changed files: validate-toc-proposal.js, update-step-status.js, crystalize-readme.md, DESIGN-OF-CRYSTALIZE-README.md, validate-toc-proposal.test.cjs, update-step-status.test.cjs
- Key changes: hierarchical-path heading IDs (parent = drop last -<n> segment, child requires existing parent); durable confirmed-TOC tree persisted in CRYSTALIZE-Status.json grill.toc.nodes (id/heading/level/confirmedContent/status)
- Test results: make test-crystalize-readme → 137/137 passed; validate-toc-proposal 17 tests, update-step-status 30 tests; quality checks 0 issues
Review report (PX-154):
- Static quality check: passed (run-quality-checks 0 issues)
- Translatability: all functions verb phrases, domain-concept names, named constants (ID_PATTERN/MAX_ID_DEPTH/MIN_OPTIONS/MAX_OPTIONS), no debug output. Renamed generic data → statusData in readStatus (Boy Scout)
- Tests: make test-crystalize-readme 137/137 passed; validate-toc-proposal 17 tests (hierarchical-path ID, parent-existence, uniqueness, deterministic), update-step-status 30 tests (toc.nodes propose/confirm/completion, no stored parentId)
- Annotation: 2 source files verified, 0 missing, 0 AMBIGUOUS
- Contracts: C001 (hierarchical-path validation gate) and C002 (durable toc.nodes completion gate) fulfilled; smoke test confirmed propose→confirm→approve-toc→end-step flow and parent-absent rejection
- Issues found and fixes applied: generic variable data in readStatus renamed to statusData

## PX-154 — implemented at 12 locations

### .claude/scripts/crystalize-readme/update-step-status.js

- Line 85
```javascript
function parseArguments(args) {
```

- Line 150
```javascript
function createDefaultStatus(graphPath) {
```

- Line 174
```javascript
function readStatus(statusPath, graphPath) {
```

- Line 273
```javascript
function isTocComplete(status) {
```

- Line 288
```javascript
function executeProposeHeading(status, proposal) {
```

- Line 322
```javascript
function executeConfirmHeading(status, confirmation) {
```

- Line 342
```javascript
function executeResetToc(status) {
```

- Line 442
```javascript
function printUsage() {
```

- Line 477
```javascript
function main() {
```

### .claude/scripts/crystalize-readme/validate-toc-proposal.js

- Line 39
```javascript
function parentIdOf(id) {
```

- Line 67
```javascript
function validateProposal(proposal) {
```

- Line 109
```javascript
function main() {
```
