# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-155: Preflight mode detection (fresh vs refine) with Markdown output; update-step-status delete-heading

**Ticket Key**: PX-155 · **Phase**: -1

---

## Background

- 【Goal】Preflight で README.md / CRYSTALIZE-Status.json の実在を確認し、過去実行（洗練・更新）か新規かを AI に伝える。derive-output-paths.js の出力を分かりやすく整形された Markdown に変更。update-step-status.js を UPSERT 基調とし、ノード削除の手段（delete-heading）を追加する。
- 【Purpose】現在の Preflight はパスの JSON を出力するだけで過去の実行有無を判定しない。2 回目以降の /crystalize-readme 実行（README/Status が既に存在）は洗練・更新、初回は新規として AI が処理を切り替えられる必要がある。
- 【Motivation】ユーザー指摘により確定。出力は全て英語・親切丁寧。Markdown は AI が読みやすい整形済み形式。
- 【Constraints】derive-output-paths.js の出力を JSON から Markdown に変更（--field=sourceFile はスクリプト消費用にパス文字列出力のまま）。update-step-status.js は UPSERT 基本 + delete-heading（ノードと全子孫を削除）。TDD Red→Green→Refactor 必須。

## Scope

- .claude/scripts/crystalize-readme/derive-output-paths.js — modify: README.md（readmePath）と CRYSTALIZE-Status.json（rfcDir 内）の実在をチェックし mode(fresh/refine) を判定。出力を Markdown 化（Mode 行 + パス表 + 存在フラグの英語メッセージ）。--field=sourceFile は従来のパス文字列出力のまま。
- .claude/scripts/crystalize-readme/update-step-status.js — modify: delete-heading サブコマンドを追加（stdin で {id} を受け、該当ノードと全子孫（id.startsWith(id + -)）を toc.nodes から削除。削除後 isTocComplete() で tocApproved を再計算）。propose-heading / confirm-heading の UPSERT 動作をコメント・テストで明文化。
- .claude/commands/crystalize-readme.md — modify: Preflight 節を Markdown 出力例（fresh / refine）とモード判定の説明に更新。
- docs/DESIGN-OF-CRYSTALIZE-README.md — modify: §5 決定論表 / §6 Preflight / §8 スクリプト一覧 / §12 変更履歴 を更新。
- tests/crystalize-readme/derive-output-paths.test.cjs — modify: Markdown 出力と mode 判定（fresh/refine）・存在フラグをカバー（JSON 期待テストを置換）。
- tests/crystalize-readme/update-step-status.test.cjs — modify: delete-heading（ノード+子孫削除）・UPSERT をカバー。
- API contract change: derive-output-paths.js のデフォルト出力が JSON → Markdown に変更（--field=sourceFile は不変）。update-step-status.js に delete-heading サブコマンドを追加。
- Data schema change: なし（toc.nodes 構造は PX-154 で確立済み。delete-heading は既存構造を操作）。
- Config/env change: なし。依存追加・削除なし（node:fs, node:path, node:child_process のみ）。
- Out of scope: README 生成（Step 4）の refine モード更新ロジック、兄弟の再採番。
- Out of scope reason: README 生成と再採番は本チケットの Preflight 判定・delete 手段とは独立した工程。
- Affected: /crystalize-readme を実行する全セッション — Preflight 出力が Markdown になり、モード判定が可能に。
- Impact nature: Preflight の出力形式変更（JSON→Markdown）と update-step-status のサブコマンド追加。
- Response: 既存の JSON 期待テストを Markdown 期待に書き換え。CRYSTALIZE-Status.json のスキーマは変更なし。

## Implementation Target Files

- `.claude/scripts/crystalize-readme/derive-output-paths.js`
- `.claude/scripts/crystalize-readme/update-step-status.js`
- `.claude/commands/crystalize-readme.md`
- `docs/DESIGN-OF-CRYSTALIZE-README.md`
- `tests/crystalize-readme/derive-output-paths.test.cjs`
- `tests/crystalize-readme/update-step-status.test.cjs`

## Investigation

- derive-output-paths.js（PX-152/PX-153 実装）: 現在のデフォルト出力は JSON {sourceFile, rfcDir, examplesDir, residuesDir, readmePath}。--field=sourceFile でパス文字列出力。rfcDir は sourceFile の dirname、readmePath は <rfcDir>/README.md。
- CRYSTALIZE-Status.json（PX-154 実装）: rfcDir 内に生成され、grill.toc.nodes を保持。
- update-step-status.js（PX-154 実装）: propose-heading / confirm-heading は stdin JSON を受け UPSERT 動作。isTocComplete() が tocApproved を導出。delete-heading は未実装。
- ユーザー指摘（PX-155 確定）: Preflight が README.md / CRYSTALIZE-Status.json の実在で fresh/refine を判定し、AI に伝える。出力は Markdown・全て英語。delete 手段が必要。

## Acceptance Criteria

- Happy path: Preflight が README.md / CRYSTALIZE-Status.json の実在から mode(fresh/refine) を判定し、英語の Markdown（パス表 + モード + 存在状態）を出力する。AI はモードに応じて新規/洗練・更新を切り替えられる。
- Error case: グラフ欠落・sourceFile 不在・JSON 不正は exit 1 で英語メッセージ。delete-heading はツリー外 id を exit 1 で拒否。
- Edge case: README.md と CRYSTALIZE-Status.json の一方のみ存在する場合も refine と判定。delete-heading はノードと全子孫を削除し、tocApproved を再計算。

## Invariants

- 【Normal establishment】Preflight は README.md / CRYSTALIZE-Status.json の実在から mode(fresh/refine) を決定論的に判定し、英語の Markdown を出力する。
- 【Invariant on error】グラフ欠落・sourceFile 不在・JSON 不正は exit 1 で英語メッセージ。delete-heading はツリー外 id を exit 1 で拒否。
- 【Internal state invariant】toc.nodes の削除（delete-heading）はノードと全子孫を削除し、tocApproved を isTocComplete() で再計算する。propose/confirm は UPSERT 動作。
- 【Boundary invariant】README.md と CRYSTALIZE-Status.json の一方のみ存在する場合も refine と判定。出力は全て英語。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — Preflight → fresh/refine mode detection

- **Precondition**: derive-output-paths.js がグラフを読み、rfcDir / readmePath / CRYSTALIZE-Status.json のパスを導出できる。
- **Postcondition**: Markdown を stdout に出力する。Mode は readmePath または CRYSTALIZE-Status.json が実在なら refine、両方不在なら fresh。パス表・存在フラグ・英語メッセージを含む。exit 0。
- **Invariant**: モード判定は決定論的（同一ファイル状態→同一モード）。Markdown には sourceFile / rfcDir / examplesDir / residuesDir / readmePath が必ず含まれる。

### C002 — Step 1 grill → delete-heading (toc.nodes)

- **Precondition**: status.grill.toc.nodes contains the node to delete. The delete-heading subcommand is implemented.
- **Postcondition**: delete-heading {id} は該当ノードと全子孫（id.startsWith(id + -)）を toc.nodes から削除し、tocApproved を isTocComplete() で再計算する。exit 0。
- **Invariant**: ツリー外 id の delete-heading は exit 1 で拒否される。propose-heading / confirm-heading は UPSERT 動作（同一 id は重複せず更新）。

## Boy Scout Rule

- derive-output-paths.js: formatPreflightMarkdown(paths, mode) を純関数として抽出（Markdown 組み立て）。mode 判定（detectMode(paths)）を単一責務関数に分離。存在フラグのチェックをヘルパー化。
- update-step-status.js: executeDeleteHeading を追加し、子孫判定（isDescendant(id, parentId)）を純関数化。propose/confirm の UPSERT 動作をコメントで明示。
- 既存の JSON 出力テストを Markdown 期待に置き換え、デッドコード（JSON 整形）を残さない。

## Test Plan

### Unit Tests

- UT: [Normal] derive-output-paths detects mode fresh when neither README.md nor CRYSTALIZE-Status.json exists — creates rfcDir with only RFC-ROOT.md and the graph; stdout contains '**Mode: fresh**' and 'No previous run was detected' (C001-Post)
- UT: [Normal] derive-output-paths detects mode refine when README.md exists (CRYSTALIZE-Status.json absent) — stdout contains '**Mode: refine**' and 'A previous /crystalize-readme run was detected' (C001-Post)
- UT: [Normal] derive-output-paths detects mode refine when CRYSTALIZE-Status.json exists (README.md absent) — stdout contains '**Mode: refine**'
- UT: [Boundary] derive-output-paths Markdown lists the existence flags for sourceFile / README.md / CRYSTALIZE-Status.json (e.g. 'README.md exists: yes')
- UT: [Error] derive-output-paths exits 1 with an English message when the graph file is missing or sourceFile does not exist (既存契約維持)
- UT: [Invariant] derive-output-paths Markdown output contains the exact derived paths (sourceFile / rfcDir / examplesDir / residuesDir / readmePath) and a 'Mode:' line that is deterministic for a given file state (C001-Inv)
- UT: [Normal] update-step-status delete-heading removes a node and all its descendants — with H1, H1-1, H1-1-1 in the tree, delete-heading H1 leaves the tree empty (C002-Post)
- UT: [Error] update-step-status delete-heading rejects an id that is not in the tree → exit 1 (C002-Inv)
- UT: [Normal] update-step-status propose-heading upserts — re-proposing the same id updates the heading without duplicating the node (C002: UPSERT)
- UT: [Invariant] update-step-status delete-heading recomputes tocApproved via isTocComplete — deleting a confirmed node can make tocApproved false again (C002-Inv)

### Integration Tests

- IT: [Integration point] Preflight Markdown output drives Step 0/1 — the AI reads the Mode from derive-output-paths output and switches fresh vs refine behavior
- IT: [Verification] a scripted propose(H1) → propose(H1-1) → confirm(H1,content) → delete-heading(H1) sequence leaves toc.nodes empty and tocApproved false
- IT: [Prerequisites] both scripts run under node --test (CommonJS), no new dependencies, no network
- IT: [Related tickets] PX-154 introduced the durable toc.nodes tree and hierarchical-path IDs; PX-155 adds fresh/refine mode detection, Markdown Preflight output, and delete-heading

### Exceptions

- AI が fresh/refine モードに応じて実行内容を切り替える判断自体は非決定論 — 合成・判断のため not testable。設計欠陥ではない: 決定論部分（モード判定と Markdown 出力）は単体テストで検証される。
- 実ユーザーとの対話グリル全体は自動テスト不能 — 人間が介在するため cannot test。設計欠陥ではない: 決定論ゲート（validate-toc-proposal / delete-heading）が単体/統合テストで検証される。

### Plan Test Code (concrete code)

- UT: [C001-Pre] fresh モード（README.md / CRYSTALIZE-Status.json なし）の Markdown 出力
```js
const { formatPreflightMarkdown } = require('../../.claude/scripts/crystalize-readme/derive-output-paths.js');
const paths = { sourceFile: '/rfc/RFC-ROOT.md', rfcDir: '/rfc', examplesDir: '/rfc/examples', residuesDir: '/rfc/residues', readmePath: '/rfc/README.md' };
const md = formatPreflightMarkdown(paths, 'fresh');
assert.match(md, /\*\*Mode: fresh\*\*/);
assert.match(md, /No previous run was detected/);
assert.ok(md.includes('| readmePath | /rfc/README.md |'));
```
- UT: [C001-Post] refine モード（README.md 実在）の Markdown 出力
```js
const { formatPreflightMarkdown, detectMode } = require('../../.claude/scripts/crystalize-readme/derive-output-paths.js');
const md = formatPreflightMarkdown(paths, 'refine');
assert.match(md, /\*\*Mode: refine\*\*/);
assert.match(md, /A previous \/crystalize-readme run was detected/);
```
- UT: [C001-Inv] 決定論とパス表の完全性
```js
assert.equal(detectMode({ readmeExists: false, statusExists: false }), 'fresh');
assert.equal(detectMode({ readmeExists: true, statusExists: false }), 'refine');
assert.equal(detectMode({ readmeExists: false, statusExists: true }), 'refine');
const md = formatPreflightMarkdown(paths, 'fresh');
assert.ok(md.includes('sourceFile') && md.includes('rfcDir') && md.includes('examplesDir') && md.includes('residuesDir') && md.includes('readmePath'));
```
- UT: [C002-Pre] toc.nodes に削除対象ノードと子孫が存在
```js
const { createDefaultStatus, executeProposeHeading } = require('../../.claude/scripts/crystalize-readme/update-step-status.js');
const status = createDefaultStatus(graphPath);
executeProposeHeading(status, { id: 'H1', heading: 'A' });
executeProposeHeading(status, { id: 'H1-1', heading: 'B' });
executeProposeHeading(status, { id: 'H1-1-1', heading: 'C' });
assert.equal(status.grill.toc.nodes.length, 3);
```
- UT: [C002-Post] delete-heading がノードと全子孫を削除し tocApproved を再計算
```js
const { executeConfirmHeading, executeDeleteHeading, isTocComplete } = require('../../.claude/scripts/crystalize-readme/update-step-status.js');
executeConfirmHeading(status, { id: 'H1', confirmedContent: 'x' });
executeConfirmHeading(status, { id: 'H1-1', confirmedContent: 'y' });
executeConfirmHeading(status, { id: 'H1-1-1', confirmedContent: 'z' });
assert.equal(isTocComplete(status), true);
executeDeleteHeading(status, { id: 'H1-1' });
assert.equal(status.grill.toc.nodes.length, 1); // H1 のみ残る
assert.equal(isTocComplete(status), false);
```
- UT: [C002-Inv] ツリー外 id の delete-heading は拒否、UPSERT は重複しない
```js
assert.throws(() => executeDeleteHeading(status, { id: 'H9' }), /not found/);
executeProposeHeading(status, { id: 'H1', heading: '新タイトル' });
assert.equal(status.grill.toc.nodes.filter(n => n.id === 'H1').length, 1);
assert.equal(status.grill.toc.nodes.find(n => n.id === 'H1').heading, '新タイトル');
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| Preflight 出力は JSON（パスのみ、モード判定なし） | Preflight 出力は英語 Markdown（Mode fresh/refine + パス表 + 存在フラグ） | derive-output-paths.js に detectMode() と formatPreflightMarkdown() を追加。README.md / CRYSTALIZE-Status.json の実在で fresh/refine を判定。 |
| delete-heading 未実装 | delete-heading サブコマンドを追加（ノード + 全子孫削除、tocApproved 再計算） | update-step-status.js に executeDeleteHeading() / isDescendant() を追加。propose/confirm の UPSERT 動作を明文化。 |
| Preflight 節は JSON 出力例 | Preflight 節は Markdown 出力例（fresh/refine）とモード判定の説明 | crystalize-readme.md と設計書を更新。 |

## Notes in Prior Implementation Rounds

- 【Implementation steps】(1) TDD: derive-output-paths.js の mode 判定 + Markdown 出力を RED→GREEN→REFACTOR。(2) TDD: update-step-status.js の delete-heading を RED→GREEN→REFACTOR。(3) コマンドファイル Preflight 節を更新。(4) 設計書を更新。(5) make test-crystalize-readme で全緑確認。
- 【Risks】Markdown 出力への変更は既存の JSON 期待テストと非互換 → テストを書き換え。--field=sourceFile はパス文字列出力のまま維持。
- 【Caveats】出力は全て英語。モード判定はファイル実在ベースで決定論的に。
- 【Open items】README 生成（Step 4）が refine モード時に既存 README を更新する詳細は後続で検討。
- 【Future improvements】delete-heading の後、兄弟の再採番を後続で検討。
Implementation summary (PX-155):
- Changed files: derive-output-paths.js, update-step-status.js, crystalize-readme.md, DESIGN-OF-CRYSTALIZE-README.md, derive-output-paths.test.cjs, update-step-status.test.cjs
- Key changes: Preflight fresh/refine mode detection + English Markdown output; delete-heading subcommand (node + descendants, tocApproved recompute)
- Test results: make test-crystalize-readme → 145/145 passed; derive-output-paths 20 tests, update-step-status 35 tests; quality checks 0 issues
Review report (PX-155):
- Static quality check: passed (run-quality-checks 0 issues)
- Translatability: all functions verb phrases, domain-concept names, named constants (CRYSTALIZE_STATUS_FILENAME), no debug output
- Tests: make test-crystalize-readme 145/145 passed; derive-output-paths 20 tests (fresh/refine detection, Markdown output, existence flags, --field=sourceFile), update-step-status 35 tests (delete-heading node+descendants, UPSERT, tocApproved recompute)
- Annotation: 2 source files verified, 0 missing, 0 AMBIGUOUS
- Contracts: C001 (fresh/refine + Markdown Preflight) and C002 (delete-heading + UPSERT) fulfilled; smoke test confirmed fresh/refine detection and delete-heading descendant removal
- Issues found and fixes applied: none (implementation clean)

## PX-155 — implemented at 7 locations

### .claude/scripts/crystalize-readme/derive-output-paths.js

- Line 143
```javascript
function detectMode({ readmeExists, statusExists }) {
```

- Line 156
```javascript
function formatPreflightMarkdown(paths, mode, flags) {
```

- Line 193
```javascript
function main() {
```

### .claude/scripts/crystalize-readme/update-step-status.js

- Line 350
```javascript
function isDescendant(id, parentId) {
```

- Line 362
```javascript
function executeDeleteHeading(status, request) {
```

- Line 480
```javascript
function printUsage() {
```

- Line 517
```javascript
function main() {
```
