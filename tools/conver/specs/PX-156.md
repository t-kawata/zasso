# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-156: Rewrite /crystalize-readme Step 2+ into per-section inspection loop with marker-based README/residue generation

**Ticket Key**: PX-156 · **Phase**: -1

---

## Background

### Goal
Replace the whole-document binary branching (Step 3 check-readme-writable.js -> README-or-RESIDUE file) with a per-section inspection loop. At the end of Step 1 a script (emit-readme-skeleton.js) mechanically outputs a README.md skeleton containing the confirmed heading group plus the trailing 'examples (implementation samples) spec and design' section, each section carrying a work-unit marker. Step 2 analyzes the actual src implementation ticket-by-ticket and transitions each section to either a complete description (marker removed) or an in-place residue record (marker replaced). A dedicated post-loop step resolves the examples section.

### Purpose
Never publish usage prose that cannot be confirmed to operate correctly. Every README section must be backed by implementation evidence (ticket -> spec -> src). Sections that cannot yet be guaranteed are not silently dropped or deferred to a whole-document file — they are recorded in place with concrete evidence of danger/omission/contradiction/deficiency plus the implementation reinforcement required. The in-README markers keep unprocessed work visible and machine-checkable.

### Motivation
The current all-or-nothing document-level decision discards every writable section into a single RESIDUE file when any one section is unwritable, and cannot express 'this section is guaranteed, that one is not' within one README. The redesign allows per-section convergence: writable sections are completed immediately while unwritable sections are recorded with evidence, and the loop keeps running until every TEMPLATE-README marker is resolved.

### Constraints
RESIDUE-<timestamp>.md output is abolished (residue is solely in-README markers). Marker taxonomy is a 1:1 pairing: usage sections carry TEMPLATE-README and resolve to complete or README-RESIDUE; the examples section carries TEMPLATE-EXAMPLES and resolves to complete or EXAMPLES-RESIDUE. The loop is driven by a NEW independent script (loop-drive-readme.js), not an extension of validate-readme-output.js / update-step-status.js. The loop exits only when every TEMPLATE-README marker is resolved and README.md contains only the two allowed section states; then the examples dedicated step runs. Step 2 entry first runs list-phases-and-tickets.js Tickets.json whose ticket keys map to specs/<ticket>.md for src analysis. No new external dependencies (Node.js built-ins only).

## Scope

- [Path] .claude/commands/crystalize-readme.md — rewrite Step 1 end (mechanical template output with markers), Step 2 (per-section inspection loop), post-loop examples dedicated step; abolish RESIDUE file generation
- [Path] .claude/scripts/crystalize-readme/ — add NEW independent loop-driving script (marker scan, per-section state transition, completion check, exit-condition validation)
- [Path] .claude/scripts/crystalize-readme/ — add Step-1-end template-output script (writes confirmed headings + examples section into README.md with TEMPLATE-README / TEMPLATE-EXAMPLES markers)
- [Path] .claude/scripts/crystalize-readme/ — add post-loop examples dedicated step script (examples grill: candidate extraction -> AI synthesis -> validate-examples-spec.js; resolves TEMPLATE-EXAMPLES to complete or EXAMPLES-RESIDUE)
- [Path] .claude/scripts/crystalize-readme/check-readme-writable.js — obsolete document-level writability check; removed or repurposed
- [Path] .claude/scripts/crystalize-readme/generate-residue-filename.js, validate-residue-output.js — obsolete (RESIDUE file abolished); removed
- [Schema] CRYSTALIZE-Status.json — gain per-section marker/state tracking consumed by the loop-driving script
- [API] /crystalize-readme command interface unchanged (first argument remains the graph path)
- [Config] no new external dependencies; Node.js built-ins + existing tickets scripts (list-phases-and-tickets.js) only
- [Excluded] Step 0 (sourceFile read) and Step 1 TOC grill (propose/confirm-heading) remain unchanged
- [Why] the redesign begins after headings are confirmed; the TOC grill itself is out of scope
- [Component] validate-readme-output.js — was extended to the 4-state marker grammar, but is subsequently REMOVED (2026-08-18): the independent output-validation step is redundant because loop-drive-readme.js --check validates the marker grammar at Step 2 exit and Step 3 resolves the examples section
- [Component] update-step-status.js — new section-state subcommands (e.g. resolve-section, mark-residue) aligned with the loop-driving script
- - [File/module path] `.claude/scripts/crystalize-readme/emit-readme-skeleton.js` (new)
  - [Action] create
  - [What specifically changes] reads CRYSTALIZE-Status.json grill.toc.nodes + the mandatory trailing examples heading; writes README.md skeleton where each usage heading is followed by a `<::TEMPLATE-README::>` marker line and the examples heading by `<::TEMPLATE-EXAMPLES::>`; the README path is derived internally (rfcDir/README.md from the status sourceFile) — no --readme flag; runs in both fresh and refine modes — refine mode re-emits the skeleton and overwrites the existing README.md so Step 2 re-analyzes every section from scratch
  - [Before → After] N/A (new file)
  - [API contract change] CLI `--graph=<path>` (or `--status=<path>`); README path derived internally, never a flag
  - [Data schema change] consumes grill.toc.nodes
  - [Config/env change] none
  - [Dependency] fs, path (Node built-ins)
- - [File/module path] `.claude/scripts/crystalize-readme/emit-readme-skeleton.js` (new)
  - [Action] create
  - [What specifically changes] reads CRYSTALIZE-Status.json grill.toc.nodes + the mandatory trailing examples heading; writes README.md skeleton where each usage heading is followed by a `<::TEMPLATE-README::>` marker line and the examples heading by `<::TEMPLATE-EXAMPLES::>`; the README path is derived internally (rfcDir/README.md from the status sourceFile) — no --readme flag; runs in both fresh and refine modes — refine mode re-emits the skeleton and overwrites the existing README.md so Step 2 re-analyzes every section from scratch
  - [Before → After] N/A (new file)
  - [API contract change] CLI `--graph=<path>` (or `--status=<path>`); README path derived internally, never a flag
  - [Data schema change] consumes grill.toc.nodes
  - [Config/env change] none
  - [Dependency] fs, path (Node built-ins)
- - [File/module path] `.claude/scripts/crystalize-readme/loop-drive-readme.js` (new, INDEPENDENT)
  - [Action] create
  - [What specifically changes] the loop-driving script: scans README.md markers by heading, reports unresolved section IDs, verifies the exit condition (zero `<::TEMPLATE-README::>` AND a clean marker grammar via validateMarkerGrammar — no cross-contamination or dual markers, so usage sections are only complete or residue; the trailing examples section's `<::TEMPLATE-EXAMPLES::>` is allowed); it also carries the final output validation (the separate validate-readme-output.js is removed)
  - [Before → After] N/A (new file)
  - [API contract change] CLI `--graph=<path>|--status=<path> [--check] [--list] [resolve-section|mark-residue]`; README path derived internally (rfcDir/README.md from the status sourceFile) — no `--readme` flag; resolve-section/mark-residue read stdin `{id, heading, content}` and transition the README section plus grill.sections in one scripted step
  - [Data schema change] reads README.md markers; optionally syncs CRYSTALIZE-Status.json.grill.sections
  - [Config/env change] none
  - [Dependency] fs, path (Node built-ins)
- - [File/module path] `.claude/scripts/crystalize-readme/validate-marker-grammar.js` (new)
  - [Action] create
  - [What specifically changes] validates the 4-marker grammar: correct marker per section kind (usage vs examples), no cross-contamination, no dual markers, marker line must follow a heading
  - [Before → After] N/A (new file)
  - [API contract change] CLI `--readme=<path>`; exports `validateMarkerGrammar(text)`
  - [Data schema change] none
  - [Config/env change] none
  - [Dependency] fs, path (Node built-ins)
- - [File/module path] `.claude/scripts/crystalize-readme/check-readme-writable.js`
  - [Action] remove (or keep only as a deprecated wrapper documenting its replacement)
  - [What specifically changes] the document-level binary writability check (evaluateWritableConditions, verifyGraph, hasUnresolvedOmissions, hasExamples, readGrillStatus, collectConditions) is obsolete; writability is now judged per section inside the loop
  - [Before → After] whole-document README-or-RESIDUE binary -> per-section loop transition
  - [API contract change] removed from the command workflow
  - [Data schema change] none
  - [Config/env change] none
  - [Dependency] none
- - [File/module path] `.claude/scripts/crystalize-readme/generate-residue-filename.js`, `.claude/scripts/crystalize-readme/validate-residue-output.js`
  - [Action] remove
  - [What specifically changes] the RESIDUE file artifact is abolished; residue is represented solely by in-README markers (`<::README-RESIDUE::>` / `<::EXAMPLES-RESIDUE::>`)
  - [Before → After] RESIDUE-<timestamp>.md file -> in-README residue markers
  - [API contract change] removed
  - [Data schema change] none
  - [Config/env change] none
  - [Dependency] none
- - [File/module path] `.claude/scripts/crystalize-readme/update-step-status.js`
  - [Action] extend
  - [What specifically changes] add per-section state subcommands: `resolve-section` (mark a section complete) and `mark-residue` (record a section as residue); keep the atomicWrite / exitWithError pattern and preserve existing `[::TICKET::]` annotations (PX-152/153/154)
  - [Before → After] 13 subcommands -> +2 section-state subcommands; grill gains `sections[]`
  - [API contract change] new subcommands
  - [Data schema change] grill.sections[] added
  - [Config/env change] none
  - [Dependency] fs, path (Node built-ins)
- - [File/module path] `.claude/scripts/crystalize-readme/validate-readme-output.js`
  - [Action] extend
  - [What specifically changes] final-state validation updated to the 4-state marker grammar: zero `<::TEMPLATE-*::>` markers; every section complete or residue; trailing examples section title preserved (TRAILING_SECTION_TITLE)
  - [Before → After] validates H1 / target-RFC / graph line / trailing title -> additionally validates the marker grammar
  - [API contract change] unchanged (--readme=, stdin)
  - [Data schema change] none
  - [Config/env change] none
  - [Dependency] fs, path (Node built-ins)
- **Out of scope:**
- [Excluded item] Step 0 (sourceFile read) and Step 1 TOC grill (propose-heading / confirm-heading / delete-heading / reset-toc / approve-toc)
  - [Why excluded] the redesign begins after headings are confirmed; the TOC grill itself was completed in PX-153 / PX-154
- [Excluded item] the AI synthesis quality of the examples grill content
  - [Why excluded] synthesis remains non-deterministic by design; only the marker lifecycle is made mechanical (validate-examples-spec.js structure check stays)
- **Affected areas:**
- [Affected component] tests/crystalize-readme/*.test.cjs
  - [Nature of impact] e2e.test.cjs, validate-readme-output.test.cjs, check-readme-writable.test.cjs, generate-residue-filename.test.cjs, validate-residue-output.test.cjs cover the obsolete RESIDUE flow
  - [Corresponding change needed] Y — rewrite tests to assert the new marker-based flow; remove tests for deleted scripts; add loop-drive-readme.test.cjs, validate-marker-grammar.test.cjs, emit-readme-skeleton.test.cjs
- [Affected component] Makefile
  - [Nature of impact] test-crystalize-readme target glob stays `tests/crystalize-readme/*.test.cjs`
  - [Corresponding change needed] N unless new test file naming changes the glob
- [Affected component] crystalize-readme.md frontmatter Role + Preflight
  - [Nature of impact] stale references to residues/RESIDUE-<timestamp>.md and residuesDir
  - [Corresponding change needed] Y — rewrite to reflect in-README marker residue and the new step structure

## Implementation Target Files

- `.claude/commands/crystalize-readme.md`
- `.claude/scripts/crystalize-readme/`

## Investigation

- crystalize-readme.md currently ends at the Step 1 completion condition (line 128); the user deleted Step 2+ (examples grill, check-readme-writable.js binary decision, RESIDUE-<timestamp>.md generation) in preparation for this rewrite.
- update-step-status.js exposes 13 subcommands: start-step, end-step, fail-step, reset-to-step, propose-heading, confirm-heading, delete-heading, reset-toc, approve-toc, approve-examples, status, cleanup, backup. createDefaultStatus() returns { sourceFile, graphFile, currentStep, steps, grill: { tocApproved, examplesApproved, toc: { nodes: [] } } }; state writes go through atomicWrite(); errors through exitWithError(); existing [::TICKET::] annotations reference PX-152/153/154.
- validate-readme-output.js exports { parseArguments, validateReadmeOutput, main, normalize, README_ARG_PREFIX='--readme=', TRAILING_SECTION_TITLE='examples (implementation samples) spec and design' }; it validates an H1 title, a target-RFC line, a graph path line, and that the last heading is the trailing examples section.
- check-readme-writable.js (260 lines) implements the whole-document binary decision: evaluateWritableConditions, verifyGraph, hasUnresolvedOmissions, hasExamples, readGrillStatus, collectConditions, checkReadmeWritable — the exact mechanism this ticket replaces.
- Tests live in tests/crystalize-readme/*.test.cjs (Makefile target test-crystalize-readme = `node --test "tests/crystalize-readme/*.test.cjs"`); current files: check-readme-writable.test.cjs, derive-output-paths.test.cjs, e2e.test.cjs, generate-residue-filename.test.cjs, update-step-status.test.cjs, validate-examples-spec.test.cjs, validate-graph-arg.test.cjs, validate-readme-output.test.cjs, validate-residue-output.test.cjs, validate-toc-proposal.test.cjs, fixtures/.
- No existing reference to TEMPLATE-README / README-RESIDUE / TEMPLATE-EXAMPLES / EXAMPLES-RESIDUE exists anywhere in the repo — the 4-marker taxonomy is entirely new.
- The ticket lives in the independent PX phase (id=-1); ticket keys display as PX-{id}; the spec outputs to specs/PX-{id}.md.

## Acceptance Criteria

- [Happy path] All usage sections writable -> README.md contains complete usage sections only, zero residue markers
- [Happy path] Mixed writability -> writable sections are complete; unwritable sections carry README-RESIDUE with concrete evidence + reinforcement design
- [Error case] Loop-driving script finds an unresolved TEMPLATE-README -> reports remaining sections and does not exit
- [Edge case] Examples section unwritable -> TEMPLATE-EXAMPLES resolves to EXAMPLES-RESIDUE
- [Edge case] README.md contains only the two allowed section states and zero template markers -> loop exits, examples step runs
- **[Happy path]** All usage sections writable -> README.md contains complete usage sections only, zero residue markers, trailing examples section resolved; loop exits on the first pass and validate-readme-output.js passes
- **[Happy path]** Mixed writability -> writable sections are complete; unwritable sections carry `<::README-RESIDUE::>` with concrete evidence + reinforcement design; loop exits only after all sections resolved
- **[Error case]** loop-drive-readme.js finds an unresolved `<::TEMPLATE-README::>` -> reports remaining section IDs and does not exit (non-zero exit code)
- **[Error case]** marker grammar violation (cross-contamination or dual markers) -> rejected with an error naming the offending section
- **[Edge case]** examples section unwritable -> `<::TEMPLATE-EXAMPLES::>` resolves to `<::EXAMPLES-RESIDUE::>`
- **[Edge case]** refine-mode rerun -> emit-readme-skeleton.js re-emits the skeleton and overwrites the existing README.md; every section is re-marked `<::TEMPLATE-*::>` so Step 2 re-analyzes all sections from scratch
- **[Edge case]** README.md contains only the two allowed section states and zero template markers -> loop exits and the examples dedicated step runs

## Invariants

- [Normal condition] Every README usage section is backed by implementation evidence: the section's described usage maps to at least one ticket whose spec (specs/<ticket>.md) exists and whose referenced src symbols resolve.
- [Error invariant] A section judged unwritable must never be silently removed or left as a TEMPLATE marker; it must transition to a residue marker carrying evidence + reinforcement design.
- [Internal state invariant] CRYSTALIZE-Status.json.grill.sections stays in lockstep with README.md markers after every loop iteration (atomicWrite); a section's state is one of pending / complete / residue.
- [Boundary invariant] Marker grammar: no section carries both a work-unit and a residue marker; TEMPLATE-EXAMPLES / EXAMPLES-RESIDUE appear only in the trailing examples section; the loop exit requires zero TEMPLATE-README markers.

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — template-output -> loop

- **Precondition**: Section carries TEMPLATE-README marker
- **Postcondition**: Section is complete (no marker) or carries README-RESIDUE
- **Invariant**: No section carries both a work-unit marker and a residue marker

### C002 — loop -> examples step

- **Precondition**: Every TEMPLATE-README marker has been resolved
- **Postcondition**: Loop reports completion and proceeds to the examples dedicated step
- **Invariant**: README.md contains only two kinds of sections: complete or residue

### C003 — examples dedicated step

- **Precondition**: Loop has exited
- **Postcondition**: Examples section is complete or carries EXAMPLES-RESIDUE
- **Invariant**: TEMPLATE-EXAMPLES / EXAMPLES-RESIDUE appear only in the examples section

## Boy Scout Rule

- Extract the marker grammar into a single source of truth (exported constants in validate-marker-grammar.js) so emit-readme-skeleton.js, loop-drive-readme.js, and validate-readme-output.js share the exact marker strings — no duplicated literals.
- Keep function names as verb phrases and one responsibility per function (emitSkeleton, scanMarkers, resolveSection, exitLoop); avoid the 260-line check-readme-writable.js anti-pattern by keeping each new script under 200 lines.
- Use path.resolve() for all file paths (per the PX-100 ticket-script convention); const only, no var; immutable read-modify-write of status JSON via JSON.parse(JSON.stringify()).
- While touching crystalize-readme.md, remove the stale frontmatter Role text and Preflight table rows referencing residues/RESIDUE-<timestamp>.md so the command doc never lies about the abolished RESIDUE file.

## Test Plan

### Unit Tests

- UT: [Normal] template-output script writes the confirmed heading group + examples heading into README.md, each section carrying the correct work-unit marker (TEMPLATE-README for usage, TEMPLATE-EXAMPLES for examples)
- UT: [Normal] loop-driving script scans README.md, counts TEMPLATE-README markers, and reports unresolved sections
- UT: [Normal] loop-driving script recognizes a resolved section (marker removed) as complete
- UT: [Normal] loop-driving script recognizes TEMPLATE-README -> README-RESIDUE transition as resolved
- UT: [Error] loop-driving script refuses to exit while any TEMPLATE-README marker remains (exit condition is mechanical)
- UT: [Error] marker grammar validator rejects cross-contamination — a usage section carrying TEMPLATE-EXAMPLES / EXAMPLES-RESIDUE is a structure violation
- UT: [Boundary] loop exit condition passes only when README.md contains zero TEMPLATE markers and every section is complete or residue
- UT: [Boundary] examples dedicated step resolves TEMPLATE-EXAMPLES to complete (marker removed via resolve-examples) or EXAMPLES-RESIDUE (via mark-examples-residue)
- UT: [Invariant] no section carries both a work-unit marker and a residue marker simultaneously
- UT: [Invariant] Step-1-end template output overwrites an existing README.md (refine mode) with the new skeleton, re-marking every section TEMPLATE for a full re-analysis
- UT: [Normal] emit-readme-skeleton.js writes the confirmed heading group (from CRYSTALIZE-Status.json grill.toc.nodes) plus the trailing 'examples (implementation samples) spec and design' heading into README.md; each usage heading is followed by a <::TEMPLATE-README::> marker line, the examples heading by <::TEMPLATE-EXAMPLES::>
- UT: [Normal] loop-drive-readme.js scans README.md by heading, identifies sections, and counts unresolved <::TEMPLATE-README::> markers
- UT: [Normal] a usage section whose <::TEMPLATE-README::> marker line was deleted is reported as 'complete' (C001 postcondition, complete branch)
- UT: [Normal] a usage section whose <::TEMPLATE-README::> marker was replaced by <::README-RESIDUE::> is reported as 'resolved-residue' (C001 postcondition, residue branch)
- UT: [Normal] resolve-examples removes <::TEMPLATE-EXAMPLES::> and writes the complete examples design (C003 complete branch)
- UT: [Normal] mark-examples-residue replaces <::TEMPLATE-EXAMPLES::> with <::EXAMPLES-RESIDUE::> + evidence (C003 residue branch)
- UT: [Error] loop-drive-readme.js exits non-zero and prints remaining section IDs when any <::TEMPLATE-README::> marker remains (C002 precondition violation)
- UT: [Error] validate-marker-grammar.js rejects cross-contamination: a usage section containing <::TEMPLATE-EXAMPLES::> or <::EXAMPLES-RESIDUE::> is a structure violation
- UT: [Error] validate-marker-grammar.js rejects a section carrying both <::TEMPLATE-README::> and <::README-RESIDUE::> (C001 invariant violation)
- UT: [Error] emit-readme-skeleton.js re-emits the skeleton over an existing README.md (refine mode) — the previous body is replaced and every section is re-marked for re-analysis (no overwrite refusal)
- UT: [Boundary] zero usage sections (only the examples heading) -> loop exits immediately; the examples step is the only work unit
- UT: [Boundary] exactly one usage section -> loop runs a single iteration and reports completion
- UT: [Boundary] README.md with zero <::TEMPLATE-*::> markers and all sections complete or residue -> loop exit condition passes (C002 invariant)
- UT: [Boundary] a marker line not preceded by a heading is rejected by validate-marker-grammar.js
- UT: [Invariant] C001 — no section carries both a work-unit marker and a residue marker; property-checked across generated README fixtures
- UT: [Invariant] C002 — README.md contains only two kinds of sections (complete | residue) when the loop reports completion
- UT: [Invariant] C003 — <::TEMPLATE-EXAMPLES::> / <::EXAMPLES-RESIDUE::> appear only in the trailing examples section
- UT: [C001-Precondition] emit-readme-skeleton.js emits <::TEMPLATE-README::> exactly once per confirmed usage heading (input schema: grill.toc.nodes)
- UT: [C001-Postcondition] after loop-drive-readme.js resolves a section, the section body is non-empty and carries no <::TEMPLATE-README::> (complete) or carries <::README-RESIDUE::>
- UT: [C001-Invariant] for every section, at most one marker line exists in its body (work-unit XOR residue)
- UT: [C002-Precondition] loop-drive-readme.js --check returns unresolved section IDs when TEMPLATE-README count > 0
- UT: [C002-Postcondition] loop-drive-readme.js reports 'loop complete' and CRYSTALIZE-Status.json.grill.sections has no 'pending' entries
- UT: [C002-Invariant] the count of sections equals the count of confirmed headings plus one examples section at all times
- UT: [C003-Precondition] resolve-examples / mark-examples-residue refuse to run while loop-drive-readme.js reports unresolved TEMPLATE-README markers (entry gate)
- UT: [C003-Precondition] resolve-examples / mark-examples-residue refuse when the examples section no longer carries <::TEMPLATE-EXAMPLES::>
- UT: [C003-Postcondition] examples section is non-empty and carries <::TEMPLATE-EXAMPLES::> removed or <::EXAMPLES-RESIDUE::> present
- UT: [C003-Postcondition] resolve-examples sets grill.examplesApproved=true and upserts grill.sections EXAMPLES/complete; mark-examples-residue sets examplesApproved=false and EXAMPLES/residue
- UT: [C003-Invariant] <::TEMPLATE-EXAMPLES::> appears at most once in README.md and only under the trailing examples heading
- UT: [C003-Boundary] --check-examples reports ready only when the loop converged AND the examples section no longer carries <::TEMPLATE-EXAMPLES::> (and is still present)

### Integration Tests

- IT: [Integration point] fixture graph -> Step 1 template output -> loop resolves every usage section -> examples step -> final README.md passes validate-readme-output.js
- IT: [Integration point] Step 2 entry: list-phases-and-tickets.js Tickets.json output maps a section to ticket keys and specs/<ticket>.md before src analysis
- IT: [Verification] fixture graph with one unwritable usage section -> that section becomes README-RESIDUE (evidence + reinforcement design) while the others are complete
- IT: [Verification] fixture graph with unwritable examples -> TEMPLATE-EXAMPLES becomes EXAMPLES-RESIDUE
- IT: [Prerequisites] a fixture *-GRAPH.json + CRYSTALIZE-Status.json with confirmed headings
- IT: [Prerequisites] Tickets.json + specs/ fixtures referenced by list-phases-and-tickets.js
- IT: [Related tickets] PX-152 (crystalize-readme implementation), PX-153 (TOC grill redesign), PX-154 (confirmed TOC persistence), PX-155 (preflight fresh/refine)
- IT: [Integration point] Step 1 end: update-step-status.js confirm-heading writes grill.toc.nodes -> emit-readme-skeleton.js consumes CRYSTALIZE-Status.json to emit the skeleton README.md
- IT: [Integration point] Step 2 entry: loop-drive-readme.js runs `node .claude/scripts/tickets/list-phases-and-tickets.js Tickets.json`; ticket keys (PX-{id}) map to specs/PX-{id}.md for src analysis entry before the writability judgment
- IT: [Integration point] loop-drive-readme.js <-> update-step-status.js new subcommands (resolve-section / mark-residue) keep CRYSTALIZE-Status.json.grill.sections consistent with README.md markers after every iteration
- IT: [Verification] fixture graph + confirmed TOC -> full pipeline (template output -> per-section loop -> examples step) -> final README.md passes updated validate-readme-output.js (zero TEMPLATE markers, only complete/residue sections, trailing examples section title)
- IT: [Verification] fixture with one unwritable usage section -> that section becomes <::README-RESIDUE::> carrying concrete evidence + reinforcement design while the other sections are complete
- IT: [Verification] fixture with unwritable examples -> <::TEMPLATE-EXAMPLES::> becomes <::EXAMPLES-RESIDUE::>
- IT: [Prerequisites] a fixture *-GRAPH.json with nodes/edges/sourceFile plus a CRYSTALIZE-Status.json with grill.tocApproved=true and confirmed grill.toc.nodes
- IT: [Prerequisites] Tickets.json + specs/ fixtures referenced by list-phases-and-tickets.js exist under the working directory
- IT: [Prerequisites] tests run via `make test-crystalize-readme` (node --test "tests/crystalize-readme/*.test.cjs")
- IT: [Related tickets] PX-152 (crystalize-readme implementation), PX-153 (per-heading validated TOC grill), PX-154 (confirmed TOC persistence in CRYSTALIZE-Status.json), PX-155 (preflight fresh/refine detection)

### Exceptions

- Item: AI judgment of writable/not-writable per section. Reason: not testable automatically — the judgment is non-deterministic by design (LLM synthesis), which is a legitimate non-testable category, not a design defect. Alt verification: the marker grammar (complete vs residue, exit condition) is fully covered by UT; AI judgment is constrained to producing valid marker transitions.
- Item: semantic correctness of the usage prose itself (does the text accurately describe the API). Reason: prose accuracy is not testable as text — natural-language correctness cannot be asserted by unit tests, which is a legitimate non-testable category, not a design defect. Alt verification: contract tests assert marker lifecycle and exit conditions mechanically; the loop requires the described usage to be backed by the ticket -> spec -> src evidence chain.
- Item: end-to-end runtime proof that a README section's described usage actually compiles/runs in the target crate. Reason: impossible to test inside crystalize-readme — building and executing the target crate is environment-dependent and out of scope, which is a legitimate non-testable category, not a design defect. Alt verification: mechanical checks assert the evidence chain (specs/PX-{id}.md exists, ticket status is not 'todo', referenced src symbols resolve).

### Plan Test Code (concrete code)

- UT: [C001-Precondition] emit-readme-skeleton.js emits exactly one <::TEMPLATE-README::> per confirmed usage heading
  ```cjs
  const status = createDefaultStatus(graphPath);
  executeProposeHeading(status, { id: 'H1', heading: 'クイックスタート' });
  executeConfirmHeading(status, { id: 'H1', confirmedContent: '本文' });
  const text = emitSkeleton(status, graphPath);
  assert.equal((text.match(/<::TEMPLATE-README::>/g) || []).length, 1);
  assert.ok(text.includes('<::TEMPLATE-EXAMPLES::>')); // trailing examples section
  ```
- UT: [C001-Postcondition] resolve-section replaces a section's <::TEMPLATE-README::> with the complete prose (marker removed)
  ```cjs
  const readme = '## H1\n\n<::TEMPLATE-README::>\n';
  const out = resolveSection(readme, 'H1', '## H1\n\nComplete usage prose.');
  assert.ok(!out.includes('<::TEMPLATE-README::>'));
  assert.ok(out.includes('Complete usage prose.'));
  ```
- UT: [C001-Postcondition] mark-residue replaces <::TEMPLATE-README::> with <::README-RESIDUE::>
  ```cjs
  const readme = '## H1\n\n<::TEMPLATE-README::>\n';
  const out = markResidue(readme, 'H1', 'Evidence: spec missing; reinforcement: add API.');
  assert.ok(!out.includes('<::TEMPLATE-README::>'));
  assert.ok(out.includes('<::README-RESIDUE::>'));
  ```
- UT: [C001-Invariant] validate-marker-grammar rejects a section carrying both <::TEMPLATE-README::> and <::README-RESIDUE::>
  ```cjs
  const bad = '## H1\n\n<::TEMPLATE-README::>\n\n<::README-RESIDUE::>\n';
  const r = validateMarkerGrammar(bad);
  assert.equal(r.ok, false);
  assert.ok(r.errors.some(e => /cross/i.test(e) || /both/i.test(e)));
  ```
- UT: [C002-Precondition] loop-drive-readme.js --check reports unresolved sections when a TEMPLATE-README remains
  ```cjs
  const readme = '## H1\n\n<::TEMPLATE-README::>\n## Examples (implementation samples) spec and design\n\n<::TEMPLATE-EXAMPLES::>\n';
  const verdict = checkLoopReady(readme);
  assert.equal(verdict.ready, false);
  assert.deepEqual(verdict.unresolved, ['H1']);
  ```
- UT: [C002-Postcondition] loop reports complete when zero TEMPLATE markers and only complete|residue sections remain
  ```cjs
  const readme = '## H1\n\nComplete prose.\n## Examples (implementation samples) spec and design\n\n<::EXAMPLES-RESIDUE::>\n';
  assert.equal(checkLoopReady(readme).ready, true);
  ```
- UT: [C002-Invariant] README.md contains only two kinds of sections (complete | residue) when the loop exits
  ```cjs
  const readme = '## H1\n\nComplete prose.\n## H1-1\n\n<::README-RESIDUE::> evidence\n';
  const r = validateMarkerGrammar(readme);
  assert.equal(r.ok, true);
  assert.equal(r.templateCount, 0);
  ```
- UT: [C003-Precondition] examples dedicated step refuses to run while the loop has not exited
  ```cjs
  const readme = '## H1\n\n<::TEMPLATE-README::>\n## Examples (implementation samples) spec and design\n\n<::TEMPLATE-EXAMPLES::>\n';
  // executeExamplesTransition throws /loop has not converged/ before any transition;
  // the --check-examples verdict is also not ready
  assert.equal(checkExamplesReady(readme).ready, false);
  ```
- UT: [C003-Postcondition] examples step resolves <::TEMPLATE-EXAMPLES::> to complete or <::EXAMPLES-RESIDUE::>
  ```cjs
  const readme = '## Examples (implementation samples) spec and design\n\n<::TEMPLATE-EXAMPLES::>\n';
  const out = resolveSection(readme, TRAILING_SECTION_TITLE, '## Examples (implementation samples) spec and design\n\nSample code.');
  assert.ok(!out.includes('<::TEMPLATE-EXAMPLES::>'));
  const out2 = markExamplesResidue(readme, TRAILING_SECTION_TITLE, 'examples/ missing');
  assert.ok(out2.includes('<::EXAMPLES-RESIDUE::>'));
  ```
- UT: [C003-Invariant] TEMPLATE-EXAMPLES / EXAMPLES-RESIDUE appear only in the trailing examples section
  ```cjs
  const bad = '## H1\n\n<::EXAMPLES-RESIDUE::>\n';
  const r = validateMarkerGrammar(bad);
  assert.equal(r.ok, false);
  assert.ok(r.errors.some(e => /examples/i.test(e)));
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| Whole-document binary branch (Step 3 check-readme-writable.js -> README or RESIDUE file) | Per-section inspection loop with marker-based README/residue generation | Replaced the document-level README-or-RESIDUE decision with a per-section loop driven by loop-drive-readme.js; residue lives as in-README markers, not a RESIDUE file. |
| No marker grammar | 4-marker taxonomy (TEMPLATE-README/README-RESIDUE/TEMPLATE-EXAMPLES/EXAMPLES-RESIDUE) as single source of truth in validate-marker-grammar.js | New validate-marker-grammar.js exports the marker constants and splitSections/validateMarkerGrammar; validate-readme-output.js and the new scripts consume it. |
| Step 2 examples grill + Step 3 binary check + Step 4 RESIDUE file output | Step 1 end skeleton emission (emit-readme-skeleton.js), Step 2 per-section loop, Step 3 post-loop examples step, Step 4 marker-aware final validation | Rewrote crystalize-readme.md Step 2+ and added emit-readme-skeleton.js and loop-drive-readme.js. |
| approve-examples subcommand in update-step-status.js | resolve-section / mark-residue subcommands; grill.sections[] state | Extended update-step-status.js with per-section state tracking; removed the obsolete approve-examples. |
| residuesDir derived by derive-output-paths.js | no residuesDir; Preflight table has sourceFile/rfcDir/examplesDir/readmePath | RESIDUE file abolished; derive-output-paths.js no longer derives residuesDir. |
| check-readme-writable.js / generate-residue-filename.js / validate-residue-output.js exist | removed | Obsolete whole-document decision and RESIDUE-file helpers deleted; their tests removed. |
| emit-readme-skeleton.js is fresh-mode-only: an existing README.md (refine mode) is never overwritten and Step 2 preserves previously completed sections | emit-readme-skeleton.js runs in both modes and overwrites the existing README.md with the confirmed-heading skeleton; Step 2 re-analyzes every section from scratch; update-step-status.js gains reset-sections | Resolved the fresh/refine contradiction: Step 1 grill output (1-2..1-7) is no longer ignored in refine mode — the confirmed headings always drive the README skeleton and the previous per-section state is cleared for a full re-analysis. |
| validate-readme-output.js is the Step 4 output-validation script; crystalize-readme.md has Steps 0-4 | validate-readme-output.js and validate-readme-output.test.cjs are removed; Step 4 (出力検証) is deleted from the command doc; update-step-status.js MAX_STEP 4 -> 3 | The final state is guaranteed by loop-drive-readme.js --check (marker-grammar validation at Step 2 exit) plus Step 3 (examples resolution) and the deterministic skeleton header — an independent output-validation step is redundant. |
| Examples step delivered as command-doc orchestration (AI hand-replaces <::TEMPLATE-EXAMPLES::> via text.replace) | Examples step scripted in loop-drive-readme.js: resolve-examples / mark-examples-residue (stdin {content}, C003 entry gate + template guard) and --check-examples completion check | Closed review findings 1/2: the post-loop examples transition is deterministic and the AI never hand-edits the README. |

## Notes in Prior Implementation Rounds

- [Implementation steps] 1) emit-readme-skeleton.js + emit-readme-skeleton.test.cjs; 2) validate-marker-grammar.js + validate-marker-grammar.test.cjs; 3) loop-drive-readme.js + loop-drive-readme.test.cjs; 4) update-step-status.js new subcommands (resolve-section / mark-residue) + tests; 5) update validate-readme-output.js; 6) rewrite crystalize-readme.md Step 2+; 7) remove check-readme-writable.js, generate-residue-filename.js, validate-residue-output.js and their tests; 8) run `make test-crystalize-readme`.
- [Risks] e2e.test.cjs depends on the old RESIDUE flow and must be rewritten; removing check-readme-writable.js may break references in crystalize-readme.md and existing tests.
- [Caveats] Step 2 entry requires Tickets.json and specs/ to exist under the working directory (tools/conver); list-phases-and-tickets.js is invoked with a path relative to cwd.
- [Open items] whether check-readme-writable.js is deleted outright or kept as a deprecated wrapper; whether CRYSTALIZE-Status.json.grill.sections replaces grill.toc.nodes or is additive.
- [Future improvements] emit collected residue markers into a machine-readable summary for /drill-rfc-down; add a --dry-run to loop-drive-readme.js.
Implementation summary:
- New scripts: validate-marker-grammar.js, emit-readme-skeleton.js, loop-drive-readme.js
- Modified: update-step-status.js (+resolve-section/mark-residue, -approve-examples, grill.sections), validate-readme-output.js (+marker grammar), derive-output-paths.js (-residuesDir)
- Removed: check-readme-writable.js, generate-residue-filename.js, validate-residue-output.js
- Rewrote: crystalize-readme.md Step 2+
- Tests: 3 new test files, updated update-step-status/validate-readme-output/derive-output-paths/e2e tests, removed 3 obsolete test files
- Test result: make test-crystalize-readme = 153 passed, 0 failed
- Quality check: 0 issues
Review report:
- Static quality check: passed (0 issues)
- Translatability: no issues
- Tests: 153 passed (make test-crystalize-readme)
- Contracts: C001/C002/C003 covered with @verifies; verify-final-contracts 100%
- Annotation: 6/6 source files carry [::TICKET::] PX-156; no AMBIGUOUS markers
- Crimes/STUBs: 0 / 0; no-excuse gate passed
- Minor findings:
  1. ~~markResidue emits <::README-RESIDUE::> only (usage sections); examples residue <::EXAMPLES-RESIDUE::> is applied by direct marker replacement in command doc Step 3.~~ RESOLVED (2026-08-18): loop-drive-readme.js gained markExamplesResidue (<::EXAMPLES-RESIDUE::>) plus resolve-examples / mark-examples-residue / --check-examples; Step 3 no longer hand-edits the README.
  2. ~~Scope item 'post-loop examples dedicated step script' was delivered as command-doc orchestration (validate-examples-spec.js + marker grammar) rather than a new script; functionally complete, AC4 verified by e2e.~~ RESOLVED (2026-08-18): the post-loop examples step is scripted in loop-drive-readme.js (resolve-examples / mark-examples-residue / --check-examples) with a C003 entry gate and template guard; e2e uses the scripted transitions.
  3. replaceSection may not preserve a blank line between a replaced section and the next (cosmetic markdown formatting).

## PX-156 — implemented at 29 locations

### .claude/scripts/crystalize-readme/derive-output-paths.js

- Line 86
```javascript
function deriveOutputPaths(graph) {
```

### .claude/scripts/crystalize-readme/emit-readme-skeleton.js

- Line 50
```javascript
function parseArguments(args) {
```

- Line 79
```javascript
function resolveStatusPath(parsed) {
```

- Line 93
```javascript
function readStatus(statusPath) {
```

- Line 104
```javascript
function deriveReadmePath(status) {
```

- Line 115
```javascript
function emitSkeleton(status) {
```

- Line 143
```javascript
function emitSkeletonToFile(status) {
```

- Line 159
```javascript
function main() {
```

### .claude/scripts/crystalize-readme/loop-drive-readme.js

- Line 40
```javascript
function parseArguments(args) {
```

- Line 69
```javascript
function normalize(text) {
```

- Line 82
```javascript
function scanMarkers(text) {
```

- Line 125
```javascript
function checkLoopReady(text) {
```

- Line 140
```javascript
function replaceSection(text, headingText, newSectionText) {
```

- Line 162
```javascript
function resolveSection(text, headingText, newSectionText) {
```

- Line 176
```javascript
function markResidue(text, headingText, residueBody) {
```

- Line 192
```javascript
function main() {
```

### .claude/scripts/crystalize-readme/update-step-status.js

- Line 152
```javascript
function createDefaultStatus(graphPath) {
```

- Line 176
```javascript
function readStatus(statusPath, graphPath) {
```

- Line 405
```javascript
function executeResolveSection(status, request) {
```

- Line 432
```javascript
function executeMarkResidue(status, request) {
```

- Line 531
```javascript
function printUsage() {
```

- Line 569
```javascript
function main() {
```

### .claude/scripts/crystalize-readme/validate-marker-grammar.js

- Line 47
```javascript
function parseArguments(args) {
```

- Line 72
```javascript
function normalize(text) {
```

- Line 83
```javascript
function splitSections(text) {
```

- Line 112
```javascript
function countMarkers(body) {
```

- Line 128
```javascript
function validateMarkerGrammar(text) {
```

- Line 177
```javascript
function main() {
```

### .claude/scripts/crystalize-readme/validate-readme-output.js

- Line 64
```javascript
function validateReadmeOutput(text) {
```
