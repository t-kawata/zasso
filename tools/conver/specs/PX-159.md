# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-159: drill-rfc-down Step 1 integration - command file Step 1 (sub-steps 1-1..1-12) definition + handoff doc update

**Ticket Key**: PX-159 · **Phase**: -1

---

## Background

### Goal
Define Step 1 (grill) in the drill-rfc-down.md command file as sub-steps 1-1 through 1-12, integrating all implemented scripts, and update the handoff doc.

### Purpose
- Encode the complete grill procedure: session init -> baseline capture -> input understanding -> DesignTree generation -> grill -> end judgment -> CheckList -> RFC append -> verification -> re-grill -> evolution verify -> completion.
- Each sub-step calls update-status.js set-step so the AI can never skip required work.
- The command file must be self-contained (no references to /grill-me-for-rfc or /drill-rfc-down-old).

### Motivation
- The grill experience must match the strict grill procedure without depending on other commands.
- The AI must be mechanically guided through every sub-step, eliminating oversight.

### Constraints
- Command file style: concise (purpose + one-line script call per sub-step); detailed specs live in scripts, JSDoc, tests, and the handoff doc.
- All script references use $DRILL_DIR only; all variables come from the Step 0 [VARIABLES] block.
- No modification to Step 0 or Steps 2-5 (unimplemented).

## Scope

- **Scope of changes:**
- - [File/module path] .claude/commands/drill-rfc-down.md
- - [Action] modify — replace the Step 1 placeholder with sub-steps 1-1..1-12
- - [Detail] each sub-step: purpose sentence + one-line script call + update-status.js set-step; variables bound from the [VARIABLES] block; self-contained (no other command references)
- - [Before] Step 1 is a placeholder with a <!-- ? --> comment
- - [After] Step 1 is a fully defined 12-sub-step procedure
- - [File/module path] docs/drill-rfc-down-implementation-handoff.md
- - [Action] modify — update section 5 (Step 1: grill)
- - [Detail] reflect the sub-step structure and the script mapping (session-init, rfc-evolution, update-status, relocated session scripts)
- 
- **Out of scope:**
- - [Excluded item] Step 0 (already implemented)
- - [Why] unchanged
- - [Excluded item] Steps 2-5 (not yet implemented)
- - [Why] future tickets; update-status step table in PX-158 already reserves them
- 
- **Affected areas:**
- - [Affected component] drill-rfc-down command (Step 1 becomes fully defined and executable)
- - [Affected component] handoff doc (maintainability for the next implementer)
- - [Nature of impact] AI execution correctness; reduced oversight risk
- - [Corresponding change] none beyond this ticket

## Implementation Target Files

- `.claude/commands/drill-rfc-down.md`
- `docs/drill-rfc-down-implementation-handoff.md`

## Investigation

- .claude/commands/drill-rfc-down.md: Step 0 implemented (one-line preflight.js call); Step 1 is a placeholder with a <!-- ? --> comment enumerating the details to write (input understanding, grill progression, RFC edit safety, script safety design); Steps 2-5 are also placeholders.
- docs/drill-rfc-down-implementation-handoff.md section 5 (Step 1: grill): describes the strict grill approach, RFC edit safety, and the need for a script safety measure; this ticket turns that into the concrete command text.
- Variables come exclusively from the PX-157 preflight [VARIABLES] block: RFC_PATH/RFC_DIR/GRAPH_PATH/DIRS_TREE_PATH/README_PATH/TICKETS_PATH/SESSION_DIR/DRILL_DIR.
- The grill procedure sub-steps map to the relocated session scripts (update-tree.js, update-status.js, session-status.js, validate-question-format.js, generate-checklist.js) plus session-init.js and rfc-evolution.js from PX-157/PX-158.

## Acceptance Criteria

- [Happy path] Step 1 definition is fully self-contained with 12 sub-steps
- [Happy path] each sub-step advances status via update-status.js set-step
- [Error case] no broken script references (all resolve under $DRILL_DIR)
- [Edge case] Step 1 works when run after Step 0 using the [VARIABLES] block

## Invariants

- [Normal condition] Step 1 definition contains sub-steps 1-1 through 1-12, each with an update-status.js set-step call.
- [Error invariant] Step 1 never references /grill-me-for-rfc or /drill-rfc-down-old; all script references resolve under $DRILL_DIR.
- [Internal state invariant] every variable used in Step 1 appears in the Step 0 [VARIABLES] block; no invented variable names.
- [Boundary invariant] Step 1 does not modify Step 0 or Steps 2-5 definitions.

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — Step 1 definition -> sub-steps

- **Precondition**: Step 1 definition exists
- **Postcondition**: sub-steps 1-1..1-12 each have an update-status.js set-step call
- **Invariant**: no sub-step lacks a status advance

### C002 — Step 1 -> self-containment

- **Precondition**: Step 1 definition
- **Postcondition**: no reference to /grill-me-for-rfc or /drill-rfc-down-old
- **Invariant**: only $DRILL_DIR scripts and [VARIABLES] variables are used

## Boy Scout Rule

- The command file is Markdown; translatability applies to the scripts it invokes. No code refactor here, but the Step 1 definition must read top-to-bottom as a procedure.
- Replace the <!-- ? --> placeholder with prose that reads as instructions, and keep each sub-step to one script call so the command stays translatable.

## Test Plan

### Unit Tests

- UT: [Normal] Step 1 definition contains sub-steps 1-1 through 1-12
- UT: [Normal] every sub-step includes an update-status.js set-step call
- UT: [Normal] all script references in Step 1 are under $DRILL_DIR
- UT: [Error] Step 1 definition contains no reference to /grill-me-for-rfc or /drill-rfc-down-old
- UT: [Normal] every variable used in Step 1 appears in the [VARIABLES] block contract
- UT: [Boundary] Step 1 sub-step count is exactly 12 (1-1..1-12)
- UT: [Invariant] the set-step sequence 1-1..1-12 is monotonic (no backward step in the normal path)

### Integration Tests

- IT: [Integration point] Step 0 [VARIABLES] output -> Step 1 sub-steps reference only bound variables and $DRILL_DIR scripts (static cross-check)
- IT: [Verification] running the Step 1 procedure from 1-1 through 1-12 drives update-status currentStep 1-1..1-12 without drift
- IT: [Prerequisites] PX-157 (foundation) and PX-158 (machinery) complete
- IT: [Related tickets] PX-157, PX-158

### Exceptions

- Item: interactive grill quality (whether the AI questions are genuinely strict). Reason: questioning is AI-driven and non-deterministic, which automated tests cannot test directly; this is not a design defect. Alt verification: the mechanical gates (validate-question-format.js and the set-step sequence) are enforced by scripts; the command file encodes the first-class rules as instructions.

### Plan Test Code (concrete code)

- UT: [C001-pre] Step 1 definition exists in the command file
  ```js
  const md = fs.readFileSync(COMMAND_FILE, 'utf8');
  assert.match(md, /### Step 1: grill/);
  ```
- UT: [C001-post] sub-steps 1-1..1-12 each have an update-status.js set-step call
  ```js
  for (let i = 1; i <= 12; i++) {
    assert.match(md, new RegExp(`#### 1-${i}\.`), `sub-step 1-${i} present`);
    assert.match(md, new RegExp(`1-${i}`), `sub-step 1-${i} advanced by set-step`);
  }
  assert.match(md, /update-status\.js set-step/);
  ```
- UT: [C001-inv] no sub-step lacks a status advance
  ```js
  const stepRegex = /#### (1-\d+)\./g;
  const steps = [...md.matchAll(stepRegex)].map(m => m[1]);
  for (const step of steps) {
    const section = md.slice(md.indexOf(`#### ${step}.`));
    assert.match(section, /update-status\.js set-step/, `step ${step} has a set-step call`);
  }
  ```
- UT: [C002-post] no reference to /grill-me-for-rfc or /drill-rfc-down-old in Step 1
  ```js
  const step1 = md.slice(md.indexOf('### Step 1: grill'), md.indexOf('### Step 2: graphify'));
  assert.doesNotMatch(step1, /grill-me-for-rfc|drill-rfc-down-old/);
  ```
- UT: [C002-inv] only $DRILL_DIR scripts and [VARIABLES] variables are used in Step 1
  ```js
  const scriptRefs = [...step1.matchAll(/\$DRILL_DIR\/([\w.-]+)/g)].map(m => m[1]);
  assert.ok(scriptRefs.length > 0);
  const allowedVars = ['RFC_PATH','RFC_DIR','GRAPH_PATH','DIRS_TREE_PATH','README_PATH','TICKETS_PATH','SESSION_DIR','DRILL_DIR'];
  const usedVars = [...step1.matchAll(/\$([A-Z_]+)/g)].map(m => m[1]);
  for (const v of usedVars) assert.ok(allowedVars.includes(v), `variable $${v} comes from the [VARIABLES] block`);
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| Step 1 was a placeholder with a <!-- ? --> comment referencing /grill-me-for-rfc and drill-rfc-down-old | Step 1 is a fully defined 12-sub-step procedure (1-1..1-12) using only $DRILL_DIR scripts and [VARIABLES] variables | Rewrote Step 1 in .claude/commands/drill-rfc-down.md as sub-steps each with a purpose, a one-line script call, and an update-status.js set-step; self-contained (no cross-command references) |
| handoff doc section 5 described the pre-implementation approach (init-for-drill-rfc-down.js, cross-command references) | section 5 documents the sub-step structure, script mapping table, evolution safety mechanism (rfc-evolution.js), and update-status.js progress management | Updated docs/drill-rfc-down-implementation-handoff.md section 5 to reflect the implemented Step 1 |
| Step 1-4 gave no DesignTree JSON guidance (placeholder id "...") | Step 1-4 documents the Q<number> id convention, node structure (id/title/status/children/questions), and automatic validation | Review fix: added DesignTree JSON rules to Step 1-4 in .claude/commands/drill-rfc-down.md |
| Step 1-5 was a one-paragraph summary (4-part question + gate + resolve) | Step 1-5 documents the full First-Class Rules: 4-part question structure, coarse-granularity bundling (3-5 nodes/question, 5-10/turn), two-pass, per-turn summary, immediate node updates, no RFC during grill, full DesignTree operation set, tree-query visualization | Review fix: expanded Step 1-5 to match /grill-me-for-rfc STEP 2 rigor |
| tree-query.js absent from $DRILL_DIR (relocation set missed it) | tree-query.js relocated into $DRILL_DIR (identical copy) and added to the smoke test | Review fix: tree-query.js is now referenceable from Step 1-5 |
| 1-6 only proposed ending the grill; 1-7 was a terse summary | 1-6 also asks the user whether to generate the checklist; 1-7 requires full AI review (all items, ambiguous nodes, project constraints) and user approval | Review fix: 1-6/1-7 now match the original STEP 3/STEP 4 rigor |
| 1-8 only required I/O boundary info + no TBD/TODO/stub; 1-9 only said fix until all items checked | 1-8 requires code snippets for every design decision + self-contained coverage of the evolution scope; 1-9 issues an immediate warning on TBD/TODO and defers completion until written | Review fix: 1-8/1-9 now match the original STEP 5/STEP 6 rigor |
| 1-5 DesignTree operation list lacked the standalone add (grill-time new top-level nodes) | 1-5 operation list and command block now include add <json> (e.g. Q5) alongside resolve/batch-resolve/add-child/refine/delete | Review fix: added the add operation to Step 1-5 to match the original STEP 2 DesignTree Updates |
| 1-11 was script-only verification (append-only/delta/well-formedness/contradiction) with no AI judgment | 1-11 now combines the script deterministic gate with an AI engineering-expert judgment (danger/omission/contradiction/deficiency) and a no-compromise quality loop back to 1-8, all as prose within a single sub-step (no nested headings) | Review fix: added the non-deterministic AI expert judgment layer and quality loop to 1-11 |

## Notes in Prior Implementation Rounds

- [Implementation steps] 1) write Step 1 sub-steps in the command file; 2) update the handoff doc section 5
- [Risks] the Step 1 definition must stay consistent with the scripts; a script-name change breaks the command
- [Caveats] Step 1 relies on scripts from PX-157 and PX-158; must run after those tickets are complete
- [Open items] none
- [Future improvements] after this ticket, Steps 2-5 can be implemented by extending the update-status.js step table (PX-158)
Implementation summary (PX-159):
- Files: .claude/commands/drill-rfc-down.md (Step 1 rewritten), docs/drill-rfc-down-implementation-handoff.md (section 5 updated), tests/drill-rfc-down/step1-command.test.cjs (5 static checks), tests/drill-rfc-down/handoff-step1.test.cjs (3 static checks)
- Key decisions: Step 1 is self-contained (no /grill-me-for-rfc or /drill-rfc-down-old references); all script calls use $DRILL_DIR; all variables come from the Step 0 [VARIABLES] block; each sub-step advances status via update-status.js set-step
- Test results: 76 drill tests + 6 io-boundary tests all green; quality checks 0 issues
- Note: Step 1 now fully executable once PX-157/PX-158 scripts are in place
Review fix (applied after done):
- Step 1-4 now documents: 1 node = 1 design decision, Q<number> id convention (Q1a children), status open->resolved, questions filled on resolve, add-child for sub-decisions
- Notes that check-all-schema.js validateDesignTree auto-enforces the rules on every write
Review fix (after done):
- Step 1-5 now matches the original STEP 2 First-Class Rules (question structure, bundling granularity, two-pass, per-turn summary, no RFC during grill, immediate node updates)
- Full DesignTree operation set documented: resolve/batch-resolve/add-child/refine/delete/open-count
- tree-query.js relocated from grill-me-for-rfc (identical) and added to session-scripts-smoke.test.cjs (8 tests)
- Boy Scout: cleaned tree-query.js (single-letter vars, console.log -> process.stdout.write, quality 0)
Review fix (after done):
- 1-6: proposes ending the grill AND asks the user whether to start checklist generation (original STEP 3)
- 1-7: AI reviews ALL checklist items, appends notes for ambiguous nodes + project-specific constraints, presents to the user for approval (original STEP 4)
- step1-command.test.cjs now has 12 tests (added 2 for 1-6/1-7)
Review fix (after done):
- 1-8: each new section must include code snippets for every design decision, I/O boundary reference info, and no TBD/TODO/stub/delegation; appended content must self-containedly cover the evolution scope (original STEP 5)
- 1-9: on TBD/TODO/"deferred to a later version" detection, issue an immediate warning and do not declare completion until fully written (original STEP 6)
- step1-command.test.cjs now has 14 tests (added 2 for 1-8/1-9)
Review fix (after done):
- 1-5 now includes the standalone add operation for grill-time new top-level nodes (original STEP 2)
- step1-command.test.cjs now has 15 tests (added 1 for the add operation)
Review fix (after done):
- 1-11 now has 3 phases within one sub-step (no grandchild headings): (1) script deterministic verification provides information to the AI, (2) AI engineering-expert judgment on danger/omission/contradiction/deficiency, (3) no-compromise quality loop back to 1-8 until all pass
- step1-command.test.cjs now has 17 tests (added 2 for the expert judgment and loop)

## PX-159 — implemented at 48 locations

### .claude/scripts/drill-rfc-down/check-all-schema.js

- Line 33
```javascript
const Q_ID_PATTERN = /^Q\d+[a-z]?$/;
```

- Line 116
```javascript
function validateNodeArray(nodes, pathPrefix, seenIds) {
```

### .claude/scripts/drill-rfc-down/generate-checklist.js

- Line 49
```javascript
function nodeChecks(title) {
```

- Line 58
```javascript
function sectionChecks() {
```

- Line 67
```javascript
function renderChildren(children, sectionPrefix, depth) {
```

### .claude/scripts/drill-rfc-down/rfc-evolution.js

- Line 42
```javascript
function resolveSessionDir(rfcPath) {
```

- Line 49
```javascript
function captureSnapshot(rfcPath) {
```

- Line 69
```javascript
function alignLines(baselineLines, currentLines) {
```

- Line 92
```javascript
function verifyAppendOnly(baselineLines, currentLines) {
```

- Line 99
```javascript
function extractDelta(baselineLines, currentLines) {
```

- Line 123
```javascript
function validateWellFormed(delta, currentContent) {
```

- Line 137
```javascript
function tokenize(text) {
```

- Line 147
```javascript
function detectContradictionCandidates(delta, graph, dirsTree, tickets) {
```

- Line 184
```javascript
function loadContradictionSources(cwd) {
```

- Line 208
```javascript
function formatDeltaReport({ appendOnly, delta, violations, candidates, contradictionChecked }) {
```

- Line 244
```javascript
function main() {
```

### .claude/scripts/drill-rfc-down/session-init.js

- Line 34
```javascript
function resolveSessionDir(rfcPath) {
```

- Line 41
```javascript
function classifySessionFiles(sessionDir) {
```

- Line 59
```javascript
function writeSessionTemplates(sessionDir, rfcPath, rfcDir) {
```

- Line 86
```javascript
function main() {
```

### .claude/scripts/drill-rfc-down/session-status.js

- Line 52
```javascript
function countAll(ns) {
```

- Line 57
```javascript
function countOpen(ns) {
```

- Line 68
```javascript
function deriveStep(state, nodes, openCount, loopCount) {
```

### .claude/scripts/drill-rfc-down/tree-query.js

- Line 34
```javascript
function findNodeWithPath(nodes, id, trail = []) {
```

- Line 47
```javascript
function countNodes(nodes) {
```

- Line 52
```javascript
function countStatus(nodes, status) {
```

- Line 60
```javascript
function maxDepth(nodes, depth = 1) {
```

- Line 72
```javascript
function renderTree(nodes, prefix = "") {
```

- Line 87
```javascript
function renderPath(pathNodes) {
```

- Line 119
```javascript
    function searchNodes(nodes) {
```

### .claude/scripts/drill-rfc-down/update-status.js

- Line 65
```javascript
function readStatus(sessionDir) {
```

- Line 76
```javascript
function saveStatus(sessionDir, status) {
```

- Line 88
```javascript
function printStatus(status) {
```

- Line 103
```javascript
function main() {
```

### .claude/scripts/drill-rfc-down/update-tree.js

- Line 33
```javascript
function findNode(nodes, id) {
```

- Line 45
```javascript
function countOpen(nodes) {
```

- Line 55
```javascript
function save() {
```

- Line 61
```javascript
function saveAndValidate() {
```

- Line 142
```javascript
    function removeNode(nodes, id) {
```

### .claude/scripts/drill-rfc-down/validate-question-format.js

- Line 24
```javascript
// [::TICKET::] PX-159 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=PX-159 --for-spec --no-implementation-order`.
```

- Line 25
```javascript
// [::TICKET::] PX-159 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=PX-159 --for-spec --no-implementation-order`.
```

- Line 26
```javascript
const question = process.argv[2];
```

### .claude/scripts/grill-me-for-rfc/check-io-stubs.js

- Line 11
```javascript
// [::TICKET::] PX-159 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=PX-159 --for-spec --no-implementation-order`.
```

- Line 12
```javascript
// [::TICKET::] PX-159 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=PX-159 --for-spec --no-implementation-order`.
```

- Line 13
```javascript
import fs from "node:fs";
```

### .claude/scripts/grill-me-for-rfc/insert-io-boundary-template.js

- Line 16
```javascript
// [::TICKET::] PX-159 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=PX-159 --for-spec --no-implementation-order`.
```

- Line 17
```javascript
// [::TICKET::] PX-159 changes. Details: `node .claude/scripts/tickets/show-ticket-context.js --ticket-key=PX-159 --for-spec --no-implementation-order`.
```

- Line 18
```javascript
import fs from "node:fs";
```
