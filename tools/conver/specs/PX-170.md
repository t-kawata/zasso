# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-170: drill-rfc-down Step 3: delta-only directory/file tree generation with boundify-style template headers

**Ticket Key**: PX-170 · **Phase**: -1

---

## Background

drill-rfc-down Step 3 (boundify-step.js --approve) currently creates src files for NEW Dirs-Tree file nodes via commitSrcStubs(), which writes only a minimal one-line declaration stub (// <title> — declaration stub). Unlike the parent /boundify-graph (generate-dir-template.js + boundify-helpers.js), it does not generate the Initial Design Artifact — RFC-driven Implementation header (graph metadata, mapped node IDs, prose cross-references, query.js exploration commands) or kind/language-appropriate declaration stubs. This severs the provenance chain protected by supreme law rule 4 for files created by drill-rfc-down. This ticket adds a delta-only generator that reuses the parent boundify's exported module functions (discover, buildHeaderContext, generateHeaderComment, getDeclarationStub) to create ONLY the new files from dirs-tree-delta.json newFiles, with the full template header, while never touching existing files or directories (no overwrite, no delete, no --force).

### Goal
Give drill-rfc-down Step 3 the same rich, traceable file generation as /boundify-graph: new files carry the Initial Design Artifact header (graph metadata, mapped node IDs, stub markers) while touching only the delta.

### Purpose
Restore the provenance chain (supreme law rule 4) for files created by drill-rfc-down and give the implementation loop a self-contained starting template per new file.

### Motivation
The current commitSrcStubs writes a one-line stub with no header, so new files lack the design-traceability header /boundify-graph guarantees; Step 5 only checks file existence, not header content, so the gap went undetected.

### Constraints
Must never modify/overwrite/delete existing files or directories; must reuse parent boundify modules read-only; must not call generate-all-dir-templates.js (a CLI wrapper, not a module) and never pass --force/--delete; must run inside boundify-step.js --approve after validation and before promote.

## Scope

- Change: Add .claude/scripts/drill-rfc-down/generate-dir-templates-delta.js (new delta-only template generator module)
- Change: Modify .claude/scripts/drill-rfc-down/boundify-step.js --approve to replace commitSrcStubs with the delta generator
- Change: Add tests/drill-rfc-down/generate-dir-templates-delta.test.cjs (new test suite)
- Change: Update tests/drill-rfc-down/boundify-step.test.cjs to assert the Initial Design Artifact header on the new src stub
- Non-change: Existing src files/directories — never modified, overwritten, or deleted
- Non-change: Parent /boundify-graph scripts (generate-dir-template.js / generate-all-dir-templates.js / boundify-helpers.js) — unchanged; imported read-only as modules
- Non-change: Modified (pre-existing) Dirs-Tree file nodes — not regenerated; only the newFiles delta is created
- Non-change: Step 5 verify logic and the 5-consistency checks — unchanged
- Impact: drill-rfc-down Step 3 creates new files with full template headers instead of minimal one-line stubs
- Impact: Provenance chain (Initial Design Artifact header) restored for files created by drill-rfc-down
- Impact: New files carry [::STUB::] MUST RESOLVE markers (normalized later by /consolidate-stubs)
- Impact: The delta generator never uses --force or --delete
- Impact: Prose-kind (rationale/glossary/requirement) file nodes never produce src files
- **Scope of changes (describe each change comprehensively):**
  - Path: new .claude/scripts/drill-rfc-down/generate-dir-templates-delta.js; modify .claude/scripts/drill-rfc-down/boundify-step.js (--approve path)
  - Action: add a new module; replace the commitSrcStubs(delta, srcDir) call with generateDirsTreeDelta({ dirsTreePath, stagedDirsTree, srcDir, graphPath, deltaPath }) inside --approve after verifyStagingDirsTree and before promoteStagingToReal
  - Detail: the new module imports buildHeaderContext + discover from generate-dir-template.js and getDeclarationStub from boundify-helpers.js via createRequire; renders all file contents, filters to newFiles paths, skips paths already on disk, skips Prose-kind file nodes, fills missing declarationStub, then mkdir(recursive)+write only missing files
  - Before → After: before → new files get // <title> — declaration stub; after → new files get the full Initial Design Artifact header + declaration stub + // TODO: [::STUB::] MUST implement NODE_ID=... markers
  - API: new exported function generateDirsTreeDelta({ dirsTreePath, stagedDirsTree, srcDir, graphPath, deltaPath }) → { created: string[], skipped: string[], excluded: string[] }
  - Schema: no Tickets.json / Dirs-Tree schema change; the generated file content embeds graph-derived metadata read-only
  - Config: no config/env change
  - Dependency: no new dependency; reuses existing rfc-graph modules
- **Out of scope (items intentionally excluded, with justification):**
  - Item: existing src files/directories — never modified/overwritten/deleted (C002 invariant); the implementation loop owns existing files
  - Item: parent /boundify-graph scripts (generate-dir-template.js / generate-all-dir-templates.js / boundify-helpers.js) — imported read-only; changing them would alter upstream boundify behavior
  - Item: modified (pre-existing) Dirs-Tree file nodes — only the newFiles delta is created; re-templating existing files is a separate concern
- **Affected areas (components/systems impacted, even without direct modification):**
  - Component: drill-rfc-down Step 3 (boundify-step.js) and the generated src tree
  - Nature: new files carry the traceability header → provenance restored; no behavior change to existing files
  - Response: no corresponding change needed to Step 5 verify (existence-based checks already pass); /consolidate-stubs normalizes the new MUST RESOLVE markers

## Implementation Target Files

- `.claude/scripts/drill-rfc-down/generate-dir-templates-delta.js`
- `.claude/scripts/drill-rfc-down/boundify-step.js`
- `tests/drill-rfc-down/generate-dir-templates-delta.test.cjs`
- `tests/drill-rfc-down/boundify-step.test.cjs`

## Investigation

Evidence gathered from code investigation:
1. generate-dir-template.js module.exports = { parseArgs, discover, runDryRun, confirmPrompt, createItems, buildHeaderContext, main }. discover(node, currentPath, headerContext) returns [{type: directory|file, path, size?, content?}]; for file nodes it renders headerComment via helpers.generateHeaderComment(headerPaths, mappedNodeIds, nodeMetaList, crossRefs, graphBasename, sourceBasename, lang) then appends node.declarationStub then // TODO: [::STUB::] MUST implement NODE_ID=... markers.
2. buildHeaderContext(dirsTree, dirsTreePath, rootDir, lang) returns null when dirsTree.sourceGraph is missing; it reads dirsTree.sourceFile for the source basename and dirsTree.trees[lang].crossReferences for cross-references and nodeMetaMap.
3. boundify-helpers.js exports getDeclarationStub(kind, language), resolveHeaderPaths, generateHeaderComment (module.exports confirmed); getDeclarationStub looks up DECLARATION_STUB_TABLE[kind][language].
4. createItems(created, isForce) throws ERROR_FILE_EXISTS on an existing file when isForce is false (generate-dir-template.test.cjs test 16) and overwrites with --force (test 17); therefore the delta generator must NOT reuse createItems as-is — it must skip existing paths instead of throwing.
5. boundify-step.js is ESM (import syntax); dirs-tree-crud.js already uses createRequire to import the CommonJS ../rfc-graph/validate-dirs-tree-schema.js — the same pattern applies for importing generate-dir-template.js and boundify-helpers.js.
6. Real siprs Dirs-Tree (crates/siprs/RFC-ROOT-Dirs-Tree.json) has sourceGraph: ~/shyme/zasso/crates/siprs/RFC-ROOT-GRAPH.json, sourceFile, crossReferences (8 entries), tree root src → buildHeaderContext works for the real project.
7. Current commitSrcStubs writes // <title> — declaration stub (no header); its path strip (split(/).slice(1)) equals the parent convention rootDir = dirname(dirsTreePath) + tree root src.

## Acceptance Criteria

- Happy path: new files listed in dirs-tree-delta newFiles are created with the Initial Design Artifact header (graph metadata, mapped node IDs, stub markers); all pre-existing files/directories remain byte-identical
- Error case: an existing file at a new-file path is skipped without overwrite or error, and the remaining new files are still created
- Edge case: Prose-kind file nodes produce no files; missing parent directories are created recursively; an empty newFiles array produces no writes
- **[Happy path]** a dirs-tree-delta newFiles of N new paths → after --approve, exactly the non-existing N paths exist under src with the Initial Design Artifact header (marker + mapped node IDs), and all pre-existing files/directories are byte-identical
- **[Error case]** a new-file path already present on disk → the generator skips it (byte-identical), continues with the remaining new files, and --approve still succeeds
- **[Edge case]** a Prose-kind file node produces no file; a nested new path creates the parent directory chain; an empty newFiles array produces zero writes

## Invariants

- [Normal condition] C001 invariant: the set of files written by the delta generator is exactly { p | p ∈ newFiles and p does not exist on disk }; no path outside that set is created or modified
- [Error invariant] C002 invariant: no file is ever overwritten or deleted; an existing file at a new-file path is skipped without error and left byte-identical
- [Internal state invariant] the generator is a pure function of (staged Dirs-Tree, dirs-tree-delta, srcRoot): identical inputs yield identical written-file sets; it never mutates the staged Dirs-Tree or the delta objects passed in
- [Boundary invariant] Prose-kind (rationale/glossary/requirement) file nodes never produce src files; an empty newFiles array produces zero writes

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — newFiles→file-creation

- **Precondition**: a valid staged Dirs-Tree, graph, and dirs-tree-delta with a non-empty newFiles array are provided
- **Postcondition**: every new-file path not already on disk is created with a template header; no existing file or directory is modified
- **Invariant**: the set of written files is exactly the subset of newFiles not present on disk

### C002 — existing-path→skip

- **Precondition**: a new-file path already exists on disk
- **Postcondition**: the existing file is left byte-identical (skipped)
- **Invariant**: no file is ever overwritten or deleted

### C003 — prose-kind→exclude

- **Precondition**: a file node in newFiles has kind rationale, glossary, or requirement
- **Postcondition**: no file is created for that node
- **Invariant**: Prose-kind file nodes never produce src files

## Boy Scout Rule

Translatability improvements for the touched code:
1. Extract the delta pipeline into named verb-phrase helpers: buildHeaderContextAndRender (context + discover), filterToNewFiles(items, deltaNewFiles), skipExistingFiles(items, srcRoot), writeMissingFiles(items, srcRoot) so the flow reads top-to-bottom as prose: build context → render contents → filter delta → skip existing → write missing.
2. Name variables by domain concept (pendingFile, srcRoot, deltaNewFiles, createdPaths) instead of generic x / data / tmp.
3. Extract the hardcoded first-segment strip into a named helper stripTreeRoot(path) with a comment documenting the tree-root (src) assumption shared with the parent boundify.
4. Replace the now-unused commitSrcStubs with the new generator call and delete the dead function (Boy Scout dead-code removal).
5. Keep each helper under 50 lines and single-responsibility so the module is translatable line-by-line.

## Test Plan

### Unit Tests

- UT: [Normal] fixture: staged Dirs-Tree with tree root src containing one existing file src/api/auth.rs and one new file src/api/session.rs mapped to N0003, and a dirs-tree-delta.json whose newFiles = [src/api/session.rs] → run generateDirsTreeDelta → assert src/api/session.rs is created, its content contains the Initial Design Artifact marker and N0003, and src/api/auth.rs is byte-identical
- UT: [Error] same fixture but pre-create src/api/session.rs with sentinel content SENTINEL → run generateDirsTreeDelta → assert the file content is still SENTINEL (skipped, not overwritten), the call exits 0, and any other new file in newFiles is still created
- UT: [Boundary] a new file node at src/a/b/c.rs where src/a/b does not exist → assert the generator creates the directory chain src/a/b recursively and writes c.rs; an empty newFiles array → assert no file is written and no new directory is created
- UT: [Invariant] snapshot the recursive file listing + sha256 of every file under the src root before and after the generator run → assert the only added paths are exactly the newFiles that did not previously exist and every pre-existing file sha256 is unchanged
- UT: [Invariant] a file node in newFiles whose kind is rationale, glossary, or requirement → assert no file is created for it and the result.excluded lists it
- UT: [Invariant] a new file node without node.declarationStub → assert the written content includes getDeclarationStub(node.kind, lang); with node.declarationStub set → assert the node value is used verbatim
- UT: [Boundary] the generated header of a new file contains the exact marker Initial Design Artifact — RFC-driven Implementation and the mapped node ID (e.g. N0003)
- UT: [Normal] C001 Precondition input-schema test: calling generateDirsTreeDelta with a dirs-tree-delta missing the newFiles array throws a clear English Error/Cause/Action message and writes nothing
- UT: [Error] C002 Postcondition test: an existing file at a new-file path is left byte-identical and result.skipped records its path
- UT: [Boundary] C003 Invariant test: a Prose-kind file node never produces a src file even when it is the only entry in newFiles
- UT: [Invariant] the generator never calls unlinkSync or writeFileSync on a path that already exists (assert via fs.statSync existence before write)

### Integration Tests

- IT: boundify-step --approve end-to-end: stage → dirs-tree-crud add-file src/session_storage.rs --kind=architecture --mapped=N0003:Session storage → approve → assert src/session_storage.rs exists with the Initial Design Artifact header and all pre-existing src files are byte-identical
- IT: after the delta generation, run verify-consistencies.js (--rfc/--graph/--dirs-tree/--src/--tickets) → assert no high Dirs-Tree↔src finding for the generated file
- IT: [Integration point] generate-dir-templates-delta.js is invoked from boundify-step.js --approve immediately after verifyStagingDirsTree passes and before promoteStagingToReal, with the STAGED Dirs-Tree, srcDir, graphPath, and the derived dirs-tree-delta.json
- IT: [Verification] the integration test verifies the delta generator runs on the staging Dirs-Tree (not the promoted real one) so perfect-before-write holds: on validation failure nothing is written and the real Dirs-Tree/src stay byte-identical
- IT: [Prerequisites] a staged Dirs-Tree that passes validate-dirs-tree-schema; a graph file satisfying all mappedNodeIds; a dirs-tree-delta.json whose newFiles contains at least one path
- IT: [Related tickets] PX-169 (Step 5 verify) — generated files must satisfy the Dirs-Tree↔src existence check; the MUST RESOLVE stub markers are normalized by /consolidate-stubs in a later phase

### Exceptions

- The prose cross-reference section for NEW graph nodes in the generated header cannot test deterministically because crossReferences in the staged Dirs-Tree reflects the pre-drill state and is not rebuilt in this ticket; the header still contains graph metadata + mapped node IDs + stub markers. This is not a design defect — the alternative verification asserts the header contains the Initial Design Artifact marker and the mapped node IDs.
- The full generated file content cannot test against a golden file because the header embeds graph paths and timestamps that vary per environment. This is not an architectural defect — the alternative verification asserts the invariant header markers (Initial Design Artifact, mapped node IDs, stub markers) and ignores variable fields.
- A real-project integration run against siprs is not testable in the unit suite (external repository dependency). This is not a design defect — the delta generator is tested on temp fixtures and the existing full suite (1710 tests) is kept green.
- Exception entry: [Item] the prose cross-reference section for NEW graph nodes in the generated header; [Reason] cannot test deterministically because crossReferences reflects the pre-drill state and is not rebuilt in this ticket; [Alternative verification] assert the header contains the Initial Design Artifact marker and the mapped node IDs. This is not a design defect.

### Plan Test Code (concrete code)

- C001 newFiles→file-creation test code (precondition input + postcondition assertion + invariant predicate)
  ```js
  // Arrange: fixture — staged Dirs-Tree root 'src' with existing src/api/auth.rs and delta newFiles=[src/api/session.rs]
  const srcRoot = path.join(tmp, 'src');
  fs.mkdirSync(path.join(srcRoot, 'api'), { recursive: true });
  fs.writeFileSync(path.join(srcRoot, 'api', 'auth.rs'), '// existing\n');
  const deltaPath = path.join(tmp, 'dirs-tree-delta.json');
  fs.writeFileSync(deltaPath, JSON.stringify({ newFiles: [{ path: 'src/api/session.rs', kind: 'architecture', nodeId: 'N0003', title: 'Session storage' }] }));
  // Act
  const result = generateDirsTreeDelta({ dirsTreePath, stagedDirsTree, srcDir: srcRoot, graphPath, deltaPath });
  // Assert postcondition: created with header + mapped node id
  assert.ok(fs.existsSync(path.join(srcRoot, 'api', 'session.rs')));
  const body = fs.readFileSync(path.join(srcRoot, 'api', 'session.rs'), 'utf8');
  assert.match(body, /Initial Design Artifact — RFC-driven Implementation/);
  assert.match(body, /N0003/);
  // Assert existing file untouched
  assert.equal(fs.readFileSync(path.join(srcRoot, 'api', 'auth.rs'), 'utf8'), '// existing\n');
  // Assert invariant: written set === newFiles not on disk
  assert.deepEqual(result.created, ['src/api/session.rs']);
  ```
- C002 existing-path→skip test code (precondition input + postcondition assertion + invariant predicate)
  ```js
  // Arrange: pre-create the new-file path with sentinel content
  fs.writeFileSync(path.join(srcRoot, 'api', 'session.rs'), 'SENTINEL');
  // Act
  const result = generateDirsTreeDelta({ dirsTreePath, stagedDirsTree, srcDir: srcRoot, graphPath, deltaPath });
  // Assert postcondition: the existing file is left byte-identical (skipped)
  assert.equal(fs.readFileSync(path.join(srcRoot, 'api', 'session.rs'), 'utf8'), 'SENTINEL');
  // Assert invariant: no overwrite/delete; the path is recorded as skipped
  assert.deepEqual(result.skipped, ['src/api/session.rs']);
  assert.deepEqual(result.created, []);
  ```
- C003 prose-kind→exclude test code (precondition input + postcondition assertion + invariant predicate)
  ```js
  // Arrange: delta newFiles=[src/api/note.rs] with kind 'rationale' (a Prose kind)
  fs.writeFileSync(deltaPath, JSON.stringify({ newFiles: [{ path: 'src/api/note.rs', kind: 'rationale', nodeId: 'N0010', title: 'Design note' }] }));
  // Act
  const result = generateDirsTreeDelta({ dirsTreePath, stagedDirsTree, srcDir: srcRoot, graphPath, deltaPath });
  // Assert postcondition: no file is created for the Prose-kind node
  assert.ok(!fs.existsSync(path.join(srcRoot, 'api', 'note.rs')));
  // Assert invariant: Prose-kind file nodes never produce src files; recorded as excluded
  assert.deepEqual(result.excluded, ['src/api/note.rs']);
  ```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| boundify-step.js --approve wrote a minimal one-line declaration stub (// <title> — declaration stub) for new Dirs-Tree file nodes via commitSrcStubs() | boundify-step.js --approve calls generateDirsTreeDelta() (new generate-dir-templates-delta.js) which creates ONLY the delta newFiles with the Initial Design Artifact header, kind/language declaration stub, and [::STUB::] MUST RESOLVE markers; existing files/dirs are never touched | Replaced commitSrcStubs with the delta-only template generator (PX-170) |
| new files created by drill-rfc-down Step 3 lacked the provenance header (supreme law rule 4 chain severed) | new files carry the Initial Design Artifact — RFC-driven Implementation header with graph metadata, mapped node IDs, and stub markers | Restored the design-traceability header for drill-rfc-down-created files |
| no delta-only file generator existed for drill-rfc-down | added .claude/scripts/drill-rfc-down/generate-dir-templates-delta.js with contracts C001 (create delta not on disk), C002 (skip existing, no overwrite/delete), C003 (exclude Prose kinds) | New module + 11 unit tests; boundify-step test extended to assert the header |

## Notes in Prior Implementation Rounds

Design: reuse generate-dir-template.js exports (discover / buildHeaderContext) and boundify-helpers.js exports (generateHeaderComment / getDeclarationStub) read-only; do not modify parent scripts. Implementation steps: (1) buildHeaderContext(stagedDirsTree, dirsTreePath, rootDir, lang); (2) discover(tree, rootDir, headerContext) to render all file contents; (3) filter to newFiles paths; (4) skip paths already on disk; (5) skip Prose-kind file nodes; (6) fill missing declarationStub via getDeclarationStub(kind, lang); (7) mkdir recursive + write only missing files. Risks: new-node prose cross-references absent (crossReferences not rebuilt in this ticket); declarationStub absent on new nodes unless supplied by the AI via dirs-tree-crud update-node or filled by getDeclarationStub; path root assumed to be the Dirs-Tree tree root (e.g. src) consistent with commitSrcStubs. Caveats: never call generate-all-dir-templates.js (CLI wrapper, not a module) and never pass --force/--delete; TTY confirmPrompt never fires because module functions are imported. Open items: rebuild crossReferences from the GRAPH delta in a future ticket. Future: optionally run the delta generator as a distinct Step 3.5 so it can be invoked standalone.

- [Implementation steps] 1. buildHeaderContext(stagedDirsTree, dirsTreePath, rootDir, lang); 2. discover(tree, rootDir, headerContext) → all items; 3. build the newFiles path set from delta; 4. filter items to newFiles, drop Prose-kind file nodes, drop paths already on disk; 5. content = node.declarationStub ?? getDeclarationStub(node.kind, lang) appended after the header; 6. mkdirSync(parent, recursive) and writeFileSync only missing files; 7. return { created, skipped, excluded }
- [Risks] new-node prose cross-references absent from the header (crossReferences not rebuilt); declarationStub absent on new nodes unless supplied or filled by getDeclarationStub; the tree-root (src) path-strip assumption must stay consistent with commitSrcStubs
- [Caveats] boundify-step.js is ESM → import the CommonJS parent modules with createRequire (same pattern as dirs-tree-crud.js); never pass --force/--delete; the generator never throws on an existing file (it skips)
- [Open items] rebuild crossReferences from the GRAPH delta so new-node prose cross-references appear in headers (future ticket)
- [Future improvements] run the delta generator as a distinct Step 3.5 so it can be invoked standalone and reused by the parent boundify
Implementation summary:
- Changed files: .claude/scripts/drill-rfc-down/generate-dir-templates-delta.js (new), .claude/scripts/drill-rfc-down/boundify-step.js (replaced commitSrcStubs with generator call), tests/drill-rfc-down/generate-dir-templates-delta.test.cjs (new), tests/drill-rfc-down/boundify-step.test.cjs (header assertion added)
- Key changes: generateDirsTreeDelta({ dirsTreePath, stagedDirsTree, srcDir, graphPath, deltaPath }) → { created, skipped, excluded }; reuses generate-dir-template.js discover/buildHeaderContext and boundify-helpers.js getDeclarationStub via createRequire; deep-clones the tree with auto declaration stubs; filters to delta newFiles; skips existing paths; excludes Prose kinds; writes only missing files
- Test results: delta generator 11/11, boundify-step 8/8, full drill-rfc-down suite 214/214, full conver suite 1721/1721 all green
- Quality checks: 0 issues on changed files
- Annotations: [::TICKET::] PX-170 injected into the new module and merged into boundify-step.js
Review report:
- Static quality check: passed (0 issues on changed source files)
- Translatability: no debug output / single-letter vars / magic numbers; function names are verb phrases
- Dependencies: consistency verified; all owned-file [::TICKET::] annotations present; 0 ambiguous markers
- Tests: full conver suite 1738/1738 green; per-ticket verify-red-coverage passed
- Contracts: verify-final-contracts 100% coverage, fulfilled
- Stubs/Crimes: 0 unresolved; no-excuse gate passed
- Issues found and fixes applied: none in review (annotations confirmed on owned files; combined-key verify cross-ticket artifacts are expected for a shared uncommitted diff)

## PX-170 — implemented at 6 locations

### .claude/scripts/drill-rfc-down/boundify-step.js

- Line 282
```javascript
function main() {
```

### .claude/scripts/drill-rfc-down/generate-dir-templates-delta.js

- Line 31
```javascript
function resolveDeltaNewFiles(deltaPath) {
```

- Line 49
```javascript
function findFileNodeByPath(dirsTree, relPath) {
```

- Line 65
```javascript
function stripTreeRoot(dirsTreeRelPath) {
```

- Line 76
```javascript
function cloneTreeWithAutoStubs(node, lang) {
```

- Line 87
```javascript
function fallbackHeaderContext(dirsTreePath, graphPath, lang) {
```
