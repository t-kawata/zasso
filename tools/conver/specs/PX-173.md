# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-173: conver.js Watcher: 曜日指定 (daysOfWeek) サポートと node-cron timezone 統一

**Ticket Key**: PX-173 · **Phase**: -1

---

## Background

### Goal
Watcher モードの定期実行設定（WatcherConfig）に曜日指定 daysOfWeek を追加し、特定の曜日にのみ定期実行できるようにする。同時に node-cron の発火時刻・曜日判定を config.timezone 基準に統一し、サーバTZと config.timezone の不一致による曜日境界ずれを根絶する。

### Purpose
現在は intervalMinutes / startTime / endTime / timezone の4項目のみで「平日のみ実行」のようなスケジュールが組めない。また node-cron は timezone オプション未指定のためサーバローカルTZで発火を判定しており、サーバTZ ≠ config.timezone の環境では、時間窓層（config.timezone 基準）と cron 層（サーバTZ基準）で曜日・時刻の解釈が食い違い、設定した曜日と実際に実行される曜日がずれる潜在バグがある。

### Motivation
ユーザーから「曜日は指定できないのか」という要求があり、調査の結果 node-cron v4.5.0 が schedule(expr, fn, { timezone }) で Intl.DateTimeFormat ベースの完全なタイムゾーン対応（曜日含む全フィールドのローカライズ）を実装していること、@types/node-cron v3 の ScheduleOptions に timezone が既に存在することを確認した。曜日指定と timezone 統一は同一の設定フィールド（config.timezone）を軸に一体で実装するのが自然である。

### Constraints
- daysOfWeek はオプショナル。未指定 = 従来動作（全曜日）。既存設定ファイル・既存テスト（watcher系80件）を一切壊さない。
- 新規 npm 依存を追加しない（node-cron v4.5.0 / @types/node-cron v3 は導入済み）。
- 曜日判定の基準は config.timezone を唯一のソースとする（サーバローカルTZに依存させない）。
- 曜日は cron 式（第5フィールド）ではなく時間窓層（time-window.ts）で判定する。cron 式に曜日を埋め込むと日跨ぎ窓（例: 月22:00-火06:00 + daysOfWeek=[1]）の翌日未明が cron 上「曜日不一致」で発火しないため。
- 日跨ぎ窓（startTime > endTime）の曜日は「開始日帰属」で判定し、窓の連続性を保つ。
- TDD Red-Green-Refactor を厳守。make test-conver で全テストがグリーンであること。

## Scope

- src/watcher.ts (modify): WatcherConfig に `daysOfWeek?: number[]`（0=日曜〜6=土曜の整数配列、任意）を追加。validateWatcherConfig に曜日検証を追加（非配列・範囲外0-6・非整数・空配列・重複を個別エラーで拒否）。loadWatcherConfig は変更不要（既存の型経路で透過）。
- src/cron-scheduler.ts (modify): start() の cron.schedule 呼び出しを `cron.schedule(this.#cronExpression, callback, { timezone: this.#config.timezone })` に変更。曜日は cron 式には埋め込まない（#buildCronExpression は intervalMinutes のみで不変）。
- src/time-window.ts (modify): isInTimeWindow のシグネチャに第5引数 `daysOfWeek?: number[]` を追加。Intl.DateTimeFormat.formatToParts で config.timezone の時・分・曜日（weekday）を取得し、①曜日フィルタ ②日跨ぎ窓の開始日帰属ロジックを実装。既存の時刻窓判定ロジックは維持。
- src/step-timer.ts (modify): isWithinTimeWindow(config) が isInTimeWindow の第5引数に config.daysOfWeek を渡す。checkStepDeadline はシグネチャ不変（内部で isWithinTimeWindow を呼ぶ）。
- テスト4ファイル (modify): watcher.test.ts（バリデーション）、cron-scheduler.test.ts（options.timezone 捕捉）、time-window.test.ts（曜日・日跨ぎ・TZ境界）、step-timer.test.ts（daysOfWeek 伝播）に RED テストを追加。conver.test.ts は変更不要（isWithinTimeWindow / CronScheduler をモック済み）。
- **Scope of changes:**
  - File/module path: src/watcher.ts, src/cron-scheduler.ts, src/time-window.ts, src/step-timer.ts（＋テスト4ファイル）
  - Action: modify（4モジュール＋4テストファイルに追加・変更）
  - Detail: WatcherConfig に daysOfWeek 追加 / validateWatcherConfig に曜日検証 / cron.schedule に { timezone } 付与 / isInTimeWindow に第5引数と曜日ロジック / isWithinTimeWindow の伝播
  - Before → After: watcher.ts「4フィールドのみ → daysOfWeek オプショナル追加」/ cron-scheduler.ts「schedule(expr, cb) → schedule(expr, cb, { timezone })」/ time-window.ts「isInTimeWindow(now,start,end,tz) → (…, daysOfWeek?)」/ step-timer.ts「config 4項目を渡す → daysOfWeek も渡す」
  - API contract: WatcherConfig.daysOfWeek?: number[]（公開型）、isInTimeWindow 第5引数（公開関数）を追加。他は互換維持。
  - Data schema: Tickets.json のスキーマ変更なし。Watcher 設定JSON にオプショナル daysOfWeek を追加。
  - Config/env: Watcher 設定ファイルに daysOfWeek（0=日〜6=土の整数配列、任意）を追加。env 変更なし。
  - Dependency: 変更なし（node-cron v4.5.0 / @types/node-cron v3 既存）。
- **Out of scope:**
  - cron 式の第5フィールドへの曜日埋め込み（日跨ぎ窓と干渉するため不採用）。
  - node-cron への timezone 以外のオプション（例: タイムゾーン別実行時刻の独自変換）。
  - 通常モード（-w 未指定）の挙動変更。Watcher 概念の外。
  - WatcherConfig の既存必須フィールド（intervalMinutes/startTime/endTime/timezone）の変更。後方互換を壊すため。
  - conver.ts の起動パス構造変更（isWithinTimeWindow 呼び出しは透過的に波及するためシグネチャ不変）。
- **Affected areas:**
  - Affected component: watcher.ts / cron-scheduler.ts / time-window.ts / step-timer.ts（直接）。conver.ts（起動時 isWithinTimeWindow に波及）、runner.ts（checkStepDeadline に波及）はシグネチャ不変で透過。
  - Nature of impact: API 表面（WatcherConfig フィールド追加、isInTimeWindow シグネチャ拡張）、動作（曜日フィルタ追加、cron 発火時刻の config.timezone アライメント）。パフォーマンス影響なし（判定は O(1)）。
  - Corresponding change: Y — 該当4テストファイルに RED テスト追加。conver.ts / runner.ts は変更不要。

## Implementation Target Files

- `src/watcher.ts`
- `src/cron-scheduler.ts`
- `src/time-window.ts`
- `src/step-timer.ts`

## Investigation

- 呼び出しグラフ（変更波及経路）: isInTimeWindow(now, start, end, tz) ← step-timer.ts:31 isWithinTimeWindow ← conver.ts:73（Watcher 起動時チェック）と runner.ts:399 checkStepDeadline（ステップ前チェック）。isWithinTimeWindow は conver.test.ts で0引数モック化。
- node-cron v4.5.0 の timezone 対応（実機検証）: schedule(expr, fn, { timezone }) が動作。内部 TimeMatcher（_shared.js:823-837）は Intl.DateTimeFormat の timeZone で秒/分/時/日/月/曜日（matchesDayOfWeek）をローカライズ。DST 対応は localTimeToTimestamp / getOffsetMinutes（_shared.js:549-560）。
- 実証データ: NY=日曜20:30 / JST=月曜09:30 の同一瞬間（2026-08-24T00:30:00Z）で `* * * * 1`（月曜のみ）を timezone=America/New_York で match → false、timezone 未指定（サーバ=JST）→ true。getNextMatch が JST 00:31 を返すことを確認。
- 型: @types/node-cron v3 の ScheduleOptions.timezone?: string が index.d.ts:42 に既存。コンパイル障害なし。
- モック構造: cron-scheduler.test.ts の schedule モックは (expression, callback) の2引数で第3引数（options）を無視するため、options 捕捉用に拡張が必要。step-timer.test.ts の isInTimeWindow モックは (_now,_start,_end,_tz) の4引数で第5引数を無視するため既存テストは無傷。conver.test.ts の isWithinTimeWindow / CronScheduler モックは変更不要。
- 既存テスト状態: watcher 系4テストファイル＋conver 系の計80件が全グリーン（make test-conver 実測）。
- 重要設計判断: 曜日を cron 式に埋め込むと、日跨ぎ窓（例: 月22:00-火06:00 + daysOfWeek=[1]）の火曜未明が cron 上「曜日不一致」で発火しない。よって曜日フィルタは時間窓層（isInTimeWindow）に置き、cron は intervalMinutes ベースのまま timezone のみ統一する。
- 日跨ぎ窓の開始日帰属定義: startMinutes > endMinutes かつ nowMin <= endMinutes のとき「前日開始の窓インスタンス」として扱い、窓開始日 = 前日曜日。それ以外（nowMin >= startMinutes）は当日開始。

## Acceptance Criteria

- **[Happy path]** daysOfWeek=[1,2,3,4,5] の Watcher 設定で、config.timezone の月〜金のみ実行され、土日は cron 発火後の時間窓チェック（起動時 isWithinTimeWindow / ステップ前 checkStepDeadline）でスキップされる。node-cron は config.timezone 基準で発火する（cron.schedule の第3引数に timezone が渡る）。
- **[Error case]** daysOfWeek に不正値（-1, 7, 1.5, "abc", [], [1,1], 非配列）が入った設定JSONは loadWatcherConfig がバリデーションエラーで拒否し、conver の Watcher 起動は exit(1) になる。既存の不正 timezone と同じ失敗経路。
- **[Edge case]** 日跨ぎ窓（例: 22:00-06:00 + daysOfWeek=[1]）で月曜夜〜火曜未明が連続して active し、火曜昼以降は inactive。曜日境界（JST=月曜00:30 / NY=日曜20:30）で config.timezone 基準の判定が正しい。日跨ぎ窓の開始・終了時刻ちょうどで active。

## Invariants

- 【Normal establishment】daysOfWeek 未指定（undefined）のとき、cron は全曜日に発火し、isInTimeWindow は従来と同一の結果を返す。daysOfWeek 指定時、曜日判定は config.timezone の壁時計（Intl.DateTimeFormat）で行われる。
- 【Invariant on error】不正な daysOfWeek（非配列・範囲外・小数・空配列・重複）は validateWatcherConfig で検出され、loadWatcherConfig が Error を throw する。バリデーション通過後の daysOfWeek は常に 0-6 の一意整数配列。
- 【Internal state invariant】isInTimeWindow は daysOfWeek が undefined または空配列のとき全曜日として扱い、常に boolean を返す（例外を投げない・部分適用しない）。CronScheduler の cron 式生成は timezone オプションの有無に依存しない。
- 【Boundary invariant】曜日境界（JST=月曜00:30 / NY=日曜20:30 の同一瞬間）で判定が config.timezone 基準で正しい。日跨ぎ窓では開始時刻ちょうど（22:00）・終了時刻ちょうど（06:00）で active。日跨ぎ窓の翌日未明は開始日帰属で連続性を保つ。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — P6-1 (WatcherConfig) → P8-3 (Watcher起動)

- **Precondition**: WatcherConfig.daysOfWeek が undefined
- **Postcondition**: cron は全曜日に発火し、isInTimeWindow は従来と同一の結果を返す
- **Invariant**: daysOfWeek 未設定時の挙動が設定前と完全に等価（後方互換）

### C002 — P7-1 (CronScheduler) → node-cron

- **Precondition**: CronScheduler.start(callback) が呼ばれる
- **Postcondition**: cron.schedule の第3引数 options.timezone が config.timezone と一致する
- **Invariant**: 生成される cron 式（5フィールド）は timezone オプションによって変化しない

### C003 — P6-2 (time-window) → P8-1 (ステップ境界)

- **Precondition**: daysOfWeek が非空の 0-6 整数配列かつ startTime<=endTime
- **Postcondition**: isInTimeWindow は config.timezone の曜日が daysOfWeek に含まれ、かつ時刻が [start,end] 内のとき true
- **Invariant**: 曜日判定は Intl.DateTimeFormat の timeZone=config.timezone で行われる（サーバローカルTZに依存しない）

### C004 — P6-2 (time-window) → P8-1 (ステップ境界)

- **Precondition**: daysOfWeek が非空かつ startTime>endTime（日跨ぎ窓）
- **Postcondition**: 窓は開始日帰属で判定され、連続性を保つ（例: 月22:00-火06:00 + daysOfWeek=[1] は月23時も火03時も active、火23時は inactive）
- **Invariant**: 日跨ぎ窓の各時点は『その窓インスタンスの開始日』の曜日で判定される

## Boy Scout Rule

- time-window.ts: isInTimeWindow は現在「時刻パース」「TZ変換」「窓判定」を1関数に混在。曜日判定追加に伴い、`getLocalizedDateTimeParts(now, timezone)`（時・分・曜日を返す内部関数）と `isWithinSameDayWindow(currentMinutes, startMinutes, endMinutes)` / `isWithinCrossDayWindow(currentMinutes, startMinutes, endMinutes)` に抽出し、各関数を単一責務にする。関数名を動詞句にし、翻訳可能性を高める。
- watcher.ts: validateWatcherConfig の曜日検証ブロックを `validateDaysOfWeek(value, errors)` として抽出。4フィールドの検証が縦に並ぶ現状を関数化し、テスト容易性も向上。
- cron-scheduler.ts: start() の cron.schedule 呼び出しに options を渡す際、`#scheduleOptions` ゲッター（readonly、{ timezone: config.timezone }）を導入してオプション生成を分離。
- step-timer.ts: isWithinTimeWindow の isInTimeWindow 呼び出しで config から引数を並べる現状を `toTimeWindowArgs(config)` ヘルパーで整理し、第5引数（daysOfWeek）の伝播をコード上で明示する。

## Test Plan

### Unit Tests

- UT: [Normal] C001-Precondition: WatcherConfig.daysOfWeek が undefined の設定を validateWatcherConfig が valid と判定する（optional フィールド）
- UT: [Normal] C001-Postcondition: daysOfWeek が undefined のとき cron は全曜日に発火し、isInTimeWindow は従来と同一の結果を返すことを全サンプル時刻で検証する（後方互換）
- UT: [Normal] C001-Invariant: daysOfWeek 未設定時の挙動が設定前と完全に等価（後方互換）— 4引数時代の isInTimeWindow 出力と daysOfWeek=undefined 出力が一致する
- UT: [Normal] C002-Precondition: CronScheduler.start(callback) が呼ばれたとき cron.schedule が実行される
- UT: [Normal] C002-Postcondition: cron.schedule の第3引数 options.timezone が config.timezone と一致することを node-cron モックで検証する
- UT: [Normal] C002-Invariant: 生成される cron 式（5フィールド）は timezone オプションによって変化しない — 同一 intervalMinutes で config.timezone が異なっても式が同一
- UT: [Normal] C003-Precondition: daysOfWeek が非空の 0-6 整数配列かつ startTime<=endTime の設定で isInTimeWindow が正常動作する
- UT: [Normal] C003-Postcondition: isInTimeWindow は config.timezone の曜日が daysOfWeek に含まれ、かつ時刻が [start,end] 内のとき true を返す（平日 09:00-17:00 で月10時 true / 土10時 false）
- UT: [Normal] daysOfWeek=[0,1,2,3,4,5,6]（全曜日明示）で未指定時と等価に active する
- UT: [Error] validateWatcherConfig: daysOfWeek が非配列（number / string / object）→ 型エラー
- UT: [Error] validateWatcherConfig: daysOfWeek に 7（範囲外）→ 範囲エラー
- UT: [Error] validateWatcherConfig: daysOfWeek に -1（負値）→ 範囲エラー
- UT: [Error] validateWatcherConfig: daysOfWeek に小数（1.5）→ 整数エラー
- UT: [Error] validateWatcherConfig: daysOfWeek が空配列 → エラー
- UT: [Error] validateWatcherConfig: daysOfWeek に重複値（[1,1,2]）→ 重複エラー
- UT: [Boundary] daysOfWeek=[0]（日曜のみ）で日曜 active、月曜 inactive
- UT: [Boundary] daysOfWeek=[6]（土曜のみ）で土曜 active、日曜 inactive
- UT: [Boundary] C003-Invariant: 曜日判定は Intl.DateTimeFormat の timeZone=config.timezone で行われる（サーバローカルTZに依存しない）— JST=月曜00:30 / NY=日曜20:30 の同一瞬間で Asia/Tokyo は true、America/New_York は false
- UT: [Boundary] 日跨ぎ窓の開始時刻ちょうど（22:00）と終了時刻ちょうど（06:00）で active
- UT: [Invariant] C004-Precondition: daysOfWeek が非空かつ startTime>endTime（日跨ぎ窓）の設定で isInTimeWindow が正常動作する
- UT: [Invariant] C004-Postcondition: 窓は開始日帰属で判定され、連続性を保つ（例: 月22:00-火06:00 + daysOfWeek=[1] は月23時も火03時も active、火23時は inactive）
- UT: [Invariant] C004-Invariant: 日跨ぎ窓の各時点は『その窓インスタンスの開始日』の曜日で判定される — 22:00-06:00 + daysOfWeek=[1] で 火03:00 は前日(月)帰属で active、日23:00 は当日(日)帰属で inactive

### Integration Tests

- IT: [Integration point] loadWatcherConfig → validateWatcherConfig → isWithinTimeWindow → isInTimeWindow の一連で daysOfWeek が伝播する
- IT: [Verification] watcher.test.ts で daysOfWeek 付きJSON設定ファイルを loadWatcherConfig で読み込み、config.daysOfWeek が配列として返ることを検証する
- IT: [Verification] step-timer.test.ts の isInTimeWindow モックで、isWithinTimeWindow(config) が config.daysOfWeek を第5引数として isInTimeWindow に渡すことを検証する
- IT: [Verification] cron-scheduler.test.ts の node-cron モックで schedule の第3引数 options.timezone を捕捉し、config.timezone と一致することを検証する
- IT: [Prerequisites] make test-conver（dist/*.test.js 全12ファイル）が既存グリーンであること。node-cron v4.5.0 の timezone オプション対応が前提（実機検証済み）
- IT: [Related tickets] PX-173 単独で完結（先行/後続なし）。依存モジュールは watcher.ts / cron-scheduler.ts / time-window.ts / step-timer.ts の4ファイル

### Exceptions

- IT: [Item] node-cron の実発火タイミング（時刻境界で実際に schedule が発火するか）を単体テストで検証することは不可能 / [Reason] node-cron v4.5.0 の内部 matcher（TimeMatcher）は package.json の exports で公開されておらず直接 import できず、実発火は分単位の実時間待ちが必須のためテストランナーで高速化できない。これは「決定論的だが根本的にテスト不能」という設計欠陥ではなく、外部ライブラリ（node-cron）の内部実装境界による制約であり設計欠陥ではない / [Alternative verification] ①cron.schedule に options.timezone=config.timezone が渡ることのモック検証 ②自前の isInTimeWindow 曜日ロジックを全契約で網羅 ③調査時に TimeMatcher を直接 import した手動検証（JST/NY 境界で曜日判定が config.timezone 基準になることを実証済み）で担保
- IT: [Item] node-cron の DST 遷移（spring-forward の 02:00-03:00 欠落等）時の発火挙動を単体テストで検証することは不可能 / [Reason] node-cron の DST 処理は内部の LocalizedTime / localTimeToTimestamp（_shared.js）に実装されており外部から注入できないためテスト不能。これは設計欠陥ではなく外部ライブラリ実装への依存によるものであり設計欠陥ではない / [Alternative verification] 自前の isInTimeWindow は Intl.DateTimeFormat（DST 安全）で構成するため、America/New_York の DST 切替日を渡すテストで自前ロジックの DST 安全性を担保する。node-cron 側は v4.5.0 で DST 対応実装を調査で確認済み

### Plan Test Code (concrete code)

- UT: [C001-Pre] daysOfWeek undefined の WatcherConfig がバリデーションを通過する
```ts
const legacyConfig = { intervalMinutes: 60, startTime: "09:00", endTime: "17:00", timezone: "UTC" };
const result = validateWatcherConfig(legacyConfig);
assert.strictEqual(result.valid, true);
```
- UT: [C001-Post] daysOfWeek undefined → 全曜日で active（後方互換）
```ts
const sunday = new Date("2026-08-23T10:00:00Z");
const wednesday = new Date("2026-08-26T10:00:00Z");
const saturday = new Date("2026-08-29T10:00:00Z");
for (const now of [sunday, wednesday, saturday]) {
  assert.strictEqual(isInTimeWindow(now, "09:00", "17:00", "UTC", undefined), true);
}
```
- UT: [C001-Inv] undefined と全曜日明示 [0..6] が全サンプルで等価
```ts
const instants = ["2026-08-23T10:00:00Z", "2026-08-24T00:30:00Z", "2026-08-29T23:00:00Z"];
for (const iso of instants) {
  const base = isInTimeWindow(new Date(iso), "09:00", "17:00", "UTC", undefined);
  const allDays = isInTimeWindow(new Date(iso), "09:00", "17:00", "UTC", [0, 1, 2, 3, 4, 5, 6]);
  assert.strictEqual(base, allDays);
}
```
- UT: [C002-Pre] CronScheduler.start() が cron.schedule を呼ぶ（node-cron モック）
```ts
const scheduler = new CronScheduler(validConfig());
scheduler.start(() => {});
assert.strictEqual(mockState.scheduleCallCount, 1);
```
- UT: [C002-Post] cron.schedule の第3引数 options.timezone が config.timezone と一致
```ts
const scheduler = new CronScheduler({ ...validConfig(), timezone: "Asia/Tokyo" });
scheduler.start(() => {});
assert.strictEqual(mockState.lastScheduleOptions.timezone, "Asia/Tokyo");
```
- UT: [C002-Inv] 同一 intervalMinutes で timezone が異なっても cron 式は同一
```ts
const jst = new CronScheduler({ ...validConfig(), timezone: "Asia/Tokyo" });
jst.start(() => {});
const exprJst = mockState.lastExpression;
const utc = new CronScheduler({ ...validConfig(), timezone: "UTC" });
utc.start(() => {});
const exprUtc = mockState.lastExpression;
assert.strictEqual(exprJst, exprUtc);
```
- UT: [C003-Pre] daysOfWeek 非空 + startTime<=endTime で正常動作
```ts
assert.strictEqual(
  isInTimeWindow(new Date("2026-08-24T10:00:00Z"), "09:00", "17:00", "UTC", [1, 2, 3, 4, 5]),
  true
);
```
- UT: [C003-Post] config.timezone の曜日が daysOfWeek に含まれ、かつ時刻が [start,end] 内のとき true
```ts
assert.strictEqual(isInTimeWindow(new Date("2026-08-24T10:00:00Z"), "09:00", "17:00", "UTC", [1, 2, 3, 4, 5]), true);
assert.strictEqual(isInTimeWindow(new Date("2026-08-29T10:00:00Z"), "09:00", "17:00", "UTC", [1, 2, 3, 4, 5]), false);
assert.strictEqual(isInTimeWindow(new Date("2026-08-24T08:00:00Z"), "09:00", "17:00", "UTC", [1, 2, 3, 4, 5]), false);
```
- UT: [C003-Inv] 曜日判定は config.timezone 基準（2026-08-24T00:30:00Z = JST月09:30 / NY日20:30）
```ts
const instant = new Date("2026-08-24T00:30:00Z");
assert.strictEqual(isInTimeWindow(instant, "00:00", "23:59", "Asia/Tokyo", [1]), true);
assert.strictEqual(isInTimeWindow(instant, "00:00", "23:59", "America/New_York", [1]), false);
```
- UT: [C004-Pre] daysOfWeek 非空 + startTime>endTime（日跨ぎ窓）で正常動作
```ts
assert.strictEqual(isInTimeWindow(new Date("2026-08-24T23:00:00Z"), "22:00", "06:00", "UTC", [1]), true);
```
- UT: [C004-Post] 開始日帰属で連続性を保つ（月22:00-火06:00 + daysOfWeek=[1]）
```ts
assert.strictEqual(isInTimeWindow(new Date("2026-08-24T23:00:00Z"), "22:00", "06:00", "UTC", [1]), true);
assert.strictEqual(isInTimeWindow(new Date("2026-08-25T03:00:00Z"), "22:00", "06:00", "UTC", [1]), true);
assert.strictEqual(isInTimeWindow(new Date("2026-08-25T23:00:00Z"), "22:00", "06:00", "UTC", [1]), false);
```
- UT: [C004-Inv] 各時点は『窓インスタンスの開始日』の曜日で判定
```ts
assert.strictEqual(isInTimeWindow(new Date("2026-08-25T03:00:00Z"), "22:00", "06:00", "UTC", [1]), true);
assert.strictEqual(isInTimeWindow(new Date("2026-08-23T23:00:00Z"), "22:00", "06:00", "UTC", [1]), false);
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| WatcherConfig は intervalMinutes/startTime/endTime/timezone の4フィールドのみ、daysOfWeek 非対応 | daysOfWeek?: number[] を追加。validateDaysOfWeek で非配列・範囲外0-6・小数・空配列・重複を検証 | src/watcher.ts: 曜日指定フィールドとバリデーション追加 |
| isInTimeWindow(now,start,end,tz) は曜日フィルタなし、日跨ぎ窓は開始日帰属なし | 第5引数 daysOfWeek を追加。config.timezone の壁時計で曜日判定。日跨ぎ窓は開始日帰属で連続性を維持 | src/time-window.ts: 曜日フィルタ + 開始日帰属ロジック実装 |
| isWithinTimeWindow は isInTimeWindow に4引数のみ渡す | config.daysOfWeek を第5引数として伝播 | src/step-timer.ts: daysOfWeek 伝播 |
| cron.schedule(expr, cb) は timezone オプション未指定（サーバローカルTZで判定） | cron.schedule(expr, cb, { timezone: config.timezone }) で config.timezone 基準に統一。MINUTES_PER_HOUR 定数抽出 | src/cron-scheduler.ts: timezone 統一 + 翻訳可能性改善 |
| README の Watcher セクションに daysOfWeek の記述なし | daysOfWeek のフィールド表・JSON例・動作フロー・日跨ぎ窓注記を追記 | README.md: Watcher 設定のドキュメント更新 |
| 曜日指定のテストなし | watcher/time-window/step-timer/cron-scheduler の4テストファイルに RED テスト追加（232件に増加） | テスト4ファイル: daysOfWeek バリデーション・曜日ロジック・timezone 統一・伝播の検証 |

## Notes in Prior Implementation Rounds

- Implementation steps: (1) RED — watcher.test.ts（daysOfWeek バリデーション）/ cron-scheduler.test.ts（options.timezone 捕捉と同一式）/ time-window.test.ts（曜日・日跨ぎ・TZ境界）/ step-timer.test.ts（第5引数伝播）を追加し、全REDを確認する。 (2) GREEN — watcher.ts → time-window.ts → step-timer.ts → cron-scheduler.ts の順に実装。 (3) REFACTOR — make test-conver 全グリーンを確認後、boyScoutPlan の抽出リファクタリングを適用し再グリーン確認。
- Risks: node-cron の timezone オプションは、サーバTZと config.timezone が一致する環境では見かけ上無変更のため回帰が検知されにくい。JST/NY 境界テスト（UT-Boundary）で明示的に担保する。@types/node-cron v3 と node-cron v4 の型ズレは現状コンパイル通過済みだが、新オプション使用時に型チェックを通すこと。
- Caveats: daysOfWeek は 0=日〜6=土 の JS 曜日規約。cron の 0/7=日 とは異なるため、config に 7 を書くとバリデーションエラー（意図的）。日跨ぎ窓は開始日帰属。空配列 daysOfWeek:[] は「全曜日」ではなくエラー（曖昧な設定を拒否）。
- Open items: 日跨ぎ窓 × 複数日指定（例: daysOfWeek=[5,6] + 金22:00-土06:00）は単一窓として自然に開始日帰属で決まるが、実ユースケースでの想定挙動の確認が望ましい。
- Future improvements: ①非日跨ぎ窓に限定した cron 式への曜日埋め込み（不要曜日のプロセス発火自体を省略する最適化）②曜日名エイリアス（"mon"等）の受付 ③checkStepDeadline のスキップ警告に曜日・時間窓の詳細を含める。


=== Implementation summary (PX-173) ===
- Changed files: src/watcher.ts, src/time-window.ts, src/step-timer.ts, src/cron-scheduler.ts, src/watcher.test.ts, src/time-window.test.ts, src/step-timer.test.ts, src/cron-scheduler.test.ts, README.md
- Key changes: WatcherConfig.daysOfWeek 追加 / isInTimeWindow 第5引数と曜日フィルタ・日跨ぎ開始日帰属 / cron.schedule に { timezone } 付与 / validateDaysOfWeek 検証
- TDD: RED（コンパイルエラーで確認）→ GREEN（232テスト全パス）→ REFACTOR（obj→candidate, MINUTES_PER_HOUR）
- Test results: make test-conver 232 pass / 0 fail。run-quality-checks 0 issues
- README: Watcher セクションに daysOfWeek を追記


=== Review report (PX-173) ===
- Found & fixed: timezone validation rejected valid IANA name 'UTC' (Node Intl.supportedValuesOf omits UTC). Fixed isValidTimezone to validate via Intl.DateTimeFormat construction; added test 'timezone=UTC → valid'.
- Found & fixed: spec instrumentation (startup log with daysOfWeek) was unimplemented. Added days=... to 'Watcher mode started' message in conver.ts.
- Minor note: spec testUnit text 'JST=月曜00:30' is inaccurate (actual JST 09:30 at that instant); planTestCode and tests use the correct value. No code impact.
- All contracts C001-C004 verified (verify-red-coverage 4/4, planTestCode concrete, @verifies present).
- No crimes, no stubs introduced.

## PX-173 — implemented at 14 locations

### src/conver.ts

- Line 57
```typescript
async function runWatcherMode(cli: CliOptions): Promise<void> {
```

### src/cron-scheduler.test.ts

- Line 21
```typescript
interface MockState {
```

### src/cron-scheduler.ts

- Line 36
```typescript
export class CronScheduler {
```

### src/step-timer.test.ts

- Line 14
```typescript
const mockIsInTimeWindow = mock.fn(
```

### src/step-timer.ts

- Line 23
```typescript
export function isWithinTimeWindow(
```

### src/time-window.test.ts

- Line 148
```typescript
describe("isInTimeWindow — daysOfWeek", () => {
```

### src/time-window.ts

- Line 94
```typescript
function getLocalizedDateTimeParts(
```

- Line 127
```typescript
function isAllowedDay(
```

- Line 146
```typescript
function isWithinSameDayWindow(
```

- Line 156
```typescript
function isWithinCrossDayWindow(
```

### src/watcher.test.ts

- Line 234
```typescript
describe("validateWatcherConfig — daysOfWeek", () => {
```

### src/watcher.ts

- Line 25
```typescript
export interface WatcherConfig {
```

- Line 127
```typescript
function validateDaysOfWeek(value: unknown, errors: string[]): void {
```

- Line 162
```typescript
function isValidTimezone(tz: string): boolean {
```
