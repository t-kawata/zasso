# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-174: conver.js Watcher: 時間枠外のフェーズ境界待機 (waitForWindow) と status 再読込

**Ticket Key**: PX-174 · **Phase**: -1

---

## Background

### Goal
Watcher モードで、実行中に時間枠外に入った場合、各フェーズ境界（make/plan/start/review/resolve/find の直前）で枠内チェックを行い、枠外なら待機して枠内復帰後に同一セッションで再開する。

### Purpose
現在はチケット境界（runner.ts:399）でのみ検知し、枠外ならラウンドを終了するため、①チケット1件分のオーバーランが発生し得る ②枠内復帰後も次回 cron 発火（intervalMinutes、例15分）まで再開が遅れる。フェーズ粒度の待機・復帰（1分以内）で枠の意味を強める。

### Motivation
オフピーク実行（例: 19:30-09:30 の DeepSeek off-peek）で、枠内に戻った瞬間から作業を再開したい。現在の設計では復帰まで最大 intervalMinutes 分の無駄が生じる。

### Constraints
- セッション分割は禁止: make→plan→start→review は1セッションを維持（実装精度のための最適な区切りであり破壊禁止）
- 待機はセッションコールバック内の runCommand 間に置く（子プロセスをアイドル保持するが、これは Claude Code の通常動作と等価）
- PX-150 再試行の再実行防止のため、各フェーズ実行直前に Tickets.json の status を再読込
- クロック（now）と sleep を注入可能にし、実時間待ちを回避してテスト可能にする
- 起動時枠外の exit(0)（conver.ts:73）は維持
- 新規依存なし、TDD Red-Green-Refactor、make test-conver 全グリーン

## Scope

- src/step-timer.ts (modify): waitForWindow(config, sleepMs=WINDOW_WAIT_SLEEP_MS, now=()=>new Date(), sleep=defaultSleep) を新規 export。WINDOW_WAIT_SLEEP_MS = 60_000 定数。isWithinTimeWindow(config, now?) に任意第2引数 now を追加（後方互換）
- src/runner.ts (modify): チケット境界（runner.ts:399）の checkStepDeadline break を await waitForWindow(options.watcherConfig) に置換。Session A 内の各フェーズ（make/plan/start/review）実行直前に waitForWindow + status 再読込。resolve 前（runResolve）と find 前（C004 ブロック）にも waitForWindow。timeWindowExited 変数と runner.ts:569 の break を除去
- src/step-timer.test.ts (modify): waitForWindow のクロック注入テスト（枠内即return / 枠外1回sleep / 枠外N回sleep / config=null no-op / 境界時刻）を追加
- src/runner.test.ts (modify): step-timer モックに waitForWindow を追加。フェーズ境界の待機・復帰、status 再読込（make→made→planスキップ）、PX-150 再試行時の完了済みフェーズスキップを検証
- README.md (modify): Watcher 動作フローに「フェーズ境界で枠外待機・復帰」「status 再読込」の説明を追記
- **Scope of changes:**
  - File/module path: src/step-timer.ts, src/runner.ts, src/step-timer.test.ts, src/runner.test.ts, README.md
  - Action: modify（step-timer.ts 関数追加、runner.ts 待機挿入、テスト2ファイル拡張、README 更新）
  - Detail: waitForWindow 追加 / isWithinTimeWindow に now 引数 / 各フェーズ直前に待機+status再読込 / timeWindowExited 除去
  - Before → After: step-timer.ts「isWithinTimeWindow(config) → isWithinTimeWindow(config, now?) + waitForWindow」/ runner.ts「チケット境界のみチェック・枠外break → 全フェーズ境界で待機・復帰」
  - API contract: waitForWindow を新規 export。isWithinTimeWindow に任意第2引数 now（後方互換）
  - Data schema: 変更なし（Tickets.json / Watcher 設定JSON 不変）
  - Config/env: 変更なし（新規設定なし。既存 WatcherConfig を使用）
  - Dependency: 変更なし
- **Out of scope:**
  - セッション分割（make→plan→start→review を別セッション化）— 実装精度を損なうため禁止
  - フェーズ実行順序の変更 / bindReviewInOneSession の意味論変更
  - checkStepDeadline の削除 — 非 Watcher モードの既存呼び出し互換のため維持
  - waitForWindow の sleepMs の設定ファイル化 — 今後の拡張
- **Affected areas:**
  - Affected component: step-timer.ts / runner.ts（直接）。conver.ts（起動時 isWithinTimeWindow exit(0) は維持）、conver.test.ts（isLoopRunning 検証）は透過
  - Nature of impact: 動作（枠外で待機・復帰）、パフォーマンス（待機中は cron 発火スキップ、1分間隔の判定のみ）
  - Corresponding change: Y — テスト2ファイルの拡張。conver.ts は変更不要

## Implementation Target Files

- `src/step-timer.ts`
- `src/runner.ts`
- `src/step-timer.test.ts`
- `src/runner.test.ts`
- `README.md`

## Investigation

- 現状の検知ポイントは runner.ts:399 の checkStepDeadline のみ（チケット境界）。枠外なら timeWindowExited=true → break → runner.ts:569 で while を抜ける
- Session A（runner.ts:422-446）は make/plan/start/review を1つの runWithSession 内で実行。status はループ先頭（runner.ts:416）で捕捉され、PX-150 のセッション再試行（runWithSession がコールバック全体を再実行）では stale な status で再分岐する
- step-timer.ts の isWithinTimeWindow は new Date() を内部使用（クロック注入不可）。checkStepDeadline は stepName 警告付きで true/false を返す
- runner.test.ts のモック: session.js（withSession は withSessionFailures で PX-150 再試行を誘発可能）、step-timer.js（checkStepDeadline をモック）。step-timer.test.ts は isInTimeWindow をモック
- 設計判断: 待機をセッションコールバック内に置く（分割禁止制約）。子プロセスアイドル保持は Claude Code の通常動作と等価で安全。status 再読込で再試行の再実行を防止
- 起動時 exit(0)（conver.ts:73）は維持。isLoopRunning ガード（conver.ts:88-93）が待機中の cron 発火をスキップ

## Acceptance Criteria

- **[Happy path]** 枠外に入ると各フェーズ直前で waitForWindow が待機し、枠内復帰（sleepMs=60秒以内）後に同じセッションで続行する。待機開始・復帰のログが出力される
- **[Error case]** セッションが待機中に失敗しても PX-150 再試行 + status 再読込により完了済みフェーズをスキップして再開する。完了済みフェーズの再実行がないことを withSessionFailures で検証
- **[Edge case]** 非 Watcher モード（watcherConfig 未指定）では waitForWindow が no-op で既存フローが不変。枠が再び開かない設定ミスでは待機継続（SIGINT で終了可能）。クロック注入テストで「枠外→sleep→枠内→復帰」を実時間なしに検証

## Invariants

- 【Normal establishment】枠内では waitForWindow が即 return し、既存フローと同一。フェーズ実行前に status 再読込で現在の進捗に基づき分岐
- 【Invariant on error】セッション失敗時（PX-150 再試行）、status 再読込により完了済みフェーズをスキップして再開（再実行されない）
- 【Internal state invariant】待機中 isLoopRunning=true が維持され、runLoop が二重実行されない。timeWindowExited デッドコードは除去済み
- 【Boundary invariant】枠内境界時刻（startTime/endTime ちょうど）で waitForWindow が即 return。非 Watcher モード（config=null）で no-op

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — step-timer.ts waitForWindow

- **Precondition**: waitForWindow(config, sleepMs, now, sleep) が呼ばれる。config は有効な WatcherConfig
- **Postcondition**: 枠内の時刻なら sleep を呼ばず即 return。枠外の時刻なら sleep(sleepMs) を呼び再判定し、枠内に戻ったら return
- **Invariant**: waitForWindow は枠内判定が true になるまで sleep を繰り返す。config が null/undefined（非 Watcher）なら即 return（no-op）

### C002 — runner.ts Session A

- **Precondition**: bindReviewInOneSession=true（既定）でチケット処理が開始される
- **Postcondition**: make/plan/start/review が同一 runWithSession 内で実行される（セッション分割されない）
- **Invariant**: チケット1件あたりの runWithSession 呼び出し回数が変更前と同一（bindReview=true 時は Session A の1回のみ）

### C003 — runner.ts フェーズ分岐

- **Precondition**: チケットが make/plan/start/review のいずれかのフェーズを実行しようとしている
- **Postcondition**: 各フェーズ実行直前に Tickets.json の status を再読込し、その status に基づいてフェーズを実行/スキップする
- **Invariant**: 完了済みフェーズは再実行されない（PX-150 のセッション再試行時も含む）

### C004 — runner.ts 待機中

- **Precondition**: waitForWindow が待機中（枠外）
- **Postcondition**: 待機中は isLoopRunning=true が維持され cron 発火がスキップされる。SIGINT/SIGTERM はプロセスレベルで効く
- **Invariant**: 待機中に runLoop が二重実行されない

## Boy Scout Rule

- step-timer.ts: waitForWindow を追加する際、sleep を `defaultSleep(ms)` として抽出し、クロック（now）とスリープの注入を明示。isWithinTimeWindow の now 引数追加で単一責務を維持
- runner.ts: Session A のコールバック内に make/plan/start/review の分岐が並ぶ現状を、`runPhaseIfNeeded(session, phaseCommand, ticketId, options)` ヘルパーで整理（「status 再読込 → waitForWindow → runCommand」を単一責務化）。コードが「各フェーズ: 現在statusを確認し、完了済みならスキップ、枠外なら待機、実行」と散文のように読めるようにする

## Test Plan

### Unit Tests

- UT: [Normal] C001-Post: 枠内時刻 → waitForWindow(config, 1, () => 枠内Date, sleepSpy) が sleep を呼ばず即 return（step-timer.test.ts, クロック注入）
- UT: [Normal] C001-Post: 枠外時刻 → sleep を1回呼び、now を枠内に切替えたら return（sleep 呼出引数 = sleepMs）
- UT: [Normal] C001-Post: 枠外継続 → now を枠外×N→枠内に切替え、sleep をN回呼び枠内で return
- UT: [Error] C001-Pre: config=null（非 Watcher）→ waitForWindow が即 return（no-op、sleep 未呼出）
- UT: [Boundary] C001: 枠内境界時刻ちょうど（startTime==now）→ 即 return
- UT: [Invariant] C001-Inv: waitForWindow は枠内判定が true になるまで sleep を繰り返す（sleep 呼出回数 = 枠外判定回数）
- UT: [Normal] C002-Post: bindReviewInOneSession=true（既定）でチケット処理が開始される前提で、make/plan/start/review が同一 runWithSession 内で実行される（セッション分割されない — withSession モック呼出回数が Session A の1回のみ）
- UT: [Invariant] C002-Inv: チケット1件あたりの runWithSession 呼び出し回数が変更前と同一（bindReviewInOneSession=true 時）
- UT: [Normal] C003-Post: 各フェーズ実行直前に Tickets.json の status を再読込し、todo→make / made→planスキップ が正しく分岐する
- UT: [Invariant] C003-Inv: 完了済みフェーズは再実行されない（PX-150 のセッション再試行時も含む）— withSessionFailures で再試行を誘発し status 再読込で検証
- UT: [Boundary] C004: 待機中 isLoopRunning が true のまま（cron 発火スキップ — conver.test.ts の CronScheduler モックで検証）
- UT: [Invariant] C004-Inv: 待機中に runLoop が二重実行されない

### Integration Tests

- IT: runner.test.ts で、枠外時に Session A 内で waitForWindow が呼ばれ、枠内復帰後に同じセッションで続行する（step-timer モックで waitForWindow を制御）
- IT: status 再読込により、make 完了後のセッション失敗 → PX-150 再試行で plan から再開する（withSessionFailures で再試行を誘発）
- IT: 非 Watcher モード（watcherConfig 未指定）で waitForWindow が no-op となり既存フローが不変（既存 runner.test.ts 全テストがグリーン）

### Exceptions

- 実時間の60秒待機を単体テストで検証することはテストランナーをブロックするため不可能。これは「決定論的だが根本的にテスト不能」という設計欠陥ではなく、クロック（now）と sleep を注入可能な設計にした上での「実時間経過そのものは検証対象外」という境界。代替としてクロック注入で「枠外→sleep→枠内→復帰」のロジックを実時間なしに検証する。
- 実際の cron 発火との結合（待機中に cron 発火がスキップされる実時間検証）は実時間依存でテスト不能。これは設計欠陥ではなく外部スケジューラ（node-cron）との実時間結合による制約であり、設計欠陥ではない。代替として isLoopRunning ガード（conver.ts の CronScheduler コールバック）の単体検証で担保する。

### Plan Test Code (concrete code)

- UT: [C001-Post] 枠内時刻 → sleep 未呼出で即 return
```ts
const config: WatcherConfig = { intervalMinutes: 15, startTime: "19:30", endTime: "09:30", timezone: "UTC", daysOfWeek: [1] };
const sleepSpy = mock.fn(async (_ms: number) => {});
await waitForWindow(config, 1, () => new Date("2026-08-24T20:00:00Z"), sleepSpy as never);
assert.strictEqual(sleepSpy.mock.callCount(), 0);
```
- UT: [C001-Post] 枠外時刻 → sleep を1回呼び枠内に切替えたら return（sleep 呼出引数 = sleepMs）
```ts
let current = new Date("2026-08-25T10:00:00Z"); // 火曜 10:00 = 枠外
const sleepAdvance = mock.fn(async (ms: number) => { current = new Date("2026-08-25T20:00:00Z"); });
await waitForWindow(config, 1, () => current, sleepAdvance as never);
assert.strictEqual(sleepAdvance.mock.callCount(), 1);
assert.ok(sleepAdvance.mock.calls[0].arguments[0] === 1);
```
- UT: [C001-Inv] 枠内判定が true になるまで sleep を繰り返す（sleep 回数 = 枠外判定回数）
```ts
const times = [new Date("2026-08-25T10:00:00Z"), new Date("2026-08-25T11:00:00Z"), new Date("2026-08-25T20:00:00Z")];
let i = 0;
const sleepSeq = mock.fn(async (_ms: number) => { i++; });
await waitForWindow(config, 1, () => times[Math.min(i, 2)], sleepSeq as never);
assert.strictEqual(sleepSeq.mock.callCount(), 2); // 枠外2回 → sleep2回 → 3回目で枠内
```
- UT: [C001-Pre] config=null（非 Watcher）→ 即 return（no-op、sleep 未呼出）
```ts
const nullSleep = mock.fn(async (_ms: number) => {});
await waitForWindow(null, 1, () => new Date(), nullSleep as never);
assert.strictEqual(nullSleep.mock.callCount(), 0);
```
- UT: [C002-Post] bindReviewInOneSession=true で make/plan/start/review が同一 runWithSession（withSession モック呼出回数 = Session A の1回のみ）
```ts
mockState.withSessionCallCount = 0;
await runLoop({ ...baseOptions(), bindReviewInOneSession: true });
assert.strictEqual(mockState.withSessionCallCount, 1);
```
- UT: [C003-Post] 各フェーズ実行直前に Tickets.json の status を再読込し、todo→make / made→planスキップ が正しく分岐する
```ts
mockState.runCommandImpl = async (cmd) => {
  if (cmd.startsWith("/make-ticket")) { setTicketStatus(ticketsPath, "PX-1", "made"); return "ok"; }
  if (cmd.startsWith("/plan-ticket")) { setTicketStatus(ticketsPath, "PX-1", "planned"); return "ok"; }
  return "ok";
};
const called: string[] = [];
await runLoop(opts);
// make 後に status=made を再読込 → plan は実行されず start へ進む想定（runPhaseIfNeeded の shouldRun が false）
```
- UT: [C003-Inv] セッション再試行時（PX-150）、完了済みフェーズが再実行されない（status 再読込で made 以降をスキップ）
```ts
let makeCalls = 0, planCalls = 0;
mockState.runCommandImpl = async (cmd) => {
  if (cmd.startsWith("/make-ticket")) { makeCalls++; setTicketStatus(ticketsPath, "PX-1", "made"); return "ok"; }
  if (cmd.startsWith("/plan-ticket")) {
    planCalls++;
    if (planCalls === 1) throw new Error("plan タイムアウト"); // recoverable → retry
    setTicketStatus(ticketsPath, "PX-1", "planned"); return "ok";
  }
  return "ok";
};
await runLoop(opts);
assert.strictEqual(makeCalls, 1); // 再試行で make は再実行されない
assert.strictEqual(planCalls, 2); // plan は再試行で再実行
```
- UT: [C004-Inv] 待機中 isLoopRunning が true のまま → cron 発火がスキップされ runLoop が二重実行されない
```ts
mockState.runLoopImpl = () => new Promise(() => {}); // 待機中（未解決）
const callback = mockState.cronCallback; // CronScheduler モックが捕捉
callback(); callback(); // 2回発火
assert.strictEqual(mockState.runLoopOptions の呼出回数, 1);
```

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| チケット境界（checkStepDeadline break）のみで時間枠外を検知し、枠外ならラウンド終了 | チケット境界 + 各フェーズ（make/plan/start/review/resolve/find）の直前で waitForWindow が待機し、枠内復帰（60秒間隔判定）後に同一セッションで続行 | src/runner.ts: フェーズ境界待機の導入、timeWindowExited デッドコード除去 |
| status はループ先頭で捕捉され、PX-150 セッション再試行時に stale な status で完了済みフェーズを再実行 | 各フェーズ実行直前に readTicketStatus で status を再読込し、完了済みフェーズをスキップ（再試行でも再実行なし） | src/runner.ts: readTicketStatus / runPhaseIfNeeded 導入、isTerminalStatus のキー解析を parseTicketKey に共通化 |
| isWithinTimeWindow は new Date() を内部使用（クロック注入不可）、待機機能なし | isWithinTimeWindow(config, now?) に任意 now 引数。waitForWindow(config, sleepMs, now, sleep) を新規追加（WINDOW_WAIT_SLEEP_MS=60_000） | src/step-timer.ts: waitForWindow + クロック/スリープ注入 |
| waitForWindow のテストなし、時間枠挙動のテストは checkStepDeadline break 前提 | step-timer.test.ts に waitForWindow のクロック注入テスト、runner.test.ts にセッション統合・status再読込テスト、conver.test.ts に二重実行スキップテストを追加 | テスト3ファイル: REDテスト追加（241件に増加） |
| README の Watcher 動作フローが「枠外ならスキップしループ終了」と記述 | 「フェーズ境界で枠外待機・復帰」「status 再読込」の説明に更新 | README.md: Watcher 動作フローの更新 |

## Notes in Prior Implementation Rounds

- Implementation steps: (1) RED — step-timer.test.ts に waitForWindow のクロック注入テスト、runner.test.ts に待機・status 再読込テストを追加し全RED確認 (2) GREEN — step-timer.ts に waitForWindow + isWithinTimeWindow now 引数、runner.ts にフェーズ境界の待機挿入 + status 再読込 (3) REFACTOR — make test-conver 全グリーン確認後、boyScoutPlan の runPhaseIfNeeded 抽出を適用
- Risks: 待機中に子プロセスを長時間アイドル保持（メモリのみ、stdio は切れない）。PX-150 再試行時の再実行（status 再読込で防止）。cron 発火スキップ（isLoopRunning 維持）
- Caveats: セッション分割禁止。status 再読込の I/O コスト（Tickets.json 読み、数ms、フェーズごと）。待機ログで可視化
- Open items: 待機中の cron 発火スキップにより、枠内復帰のタイミングは sleepMs 粒度（1分）。より細かい粒度が必要なら設定化
- Future improvements: waitForWindow の sleepMs を WatcherConfig で設定化 / 待機中の Slack 通知


=== Implementation summary (PX-174) ===
- Changed files: src/step-timer.ts, src/runner.ts, src/step-timer.test.ts, src/runner.test.ts, src/conver.test.ts, README.md
- Key changes: waitForWindow 追加（クロック/スリープ注入可能）/ isWithinTimeWindow now 引数 / runPhaseIfNeeded（status再読込→待機→実行）/ readTicketStatus + parseTicketKey 共通化 / timeWindowExited 除去
- TDD: RED（waitForWindow 不存在のコンパイルエラー + 既存テスト更新）→ GREEN（241テスト全パス）→ REFACTOR（PhaseSpec で6引数を4引数化、品質チェック0件）
- Test results: make test-conver 241 pass / 0 fail。run-quality-checks 0 issues
- README: Watcher 動作フローに待機・復帰・status再読込を追記


=== Review report (PX-174) ===
- Found & fixed: ラウンド完了時の最終 resolve（runLoop 末尾）に waitForWindow が欠落していた → 追加
- Found & fixed: readTicketStatus 内の汎用変数 data → ticketsData に改名（翻訳可能性）
- Verified: C001 waitForWindow（クロック注入テスト）/ C002 セッション統合（withSessionCallCount=2）/ C003 status 再読込（再試行時 makeCalls=1）/ C004 isLoopRunning ガード
- No crimes, no stubs introduced. No unresolved AMBIGUOUS.

## PX-174 — implemented at 11 locations

### src/conver.test.ts

- Line 37
```typescript
interface MockState {
```

### src/runner.test.ts

- Line 32
```typescript
interface MockState {
```

- Line 260
```typescript
  function setTicketStatus(path: string, key: string, status: string): void {
```

### src/runner.ts

- Line 209
```typescript
function parseTicketKey(key: string): { phaseId: number; id: number } {
```

- Line 223
```typescript
function readTicketStatus(ticketsPath: string, key: string): string {
```

- Line 243
```typescript
function isTerminalStatusValue(status: string): boolean {
```

- Line 252
```typescript
function isTerminalStatus(ticketsPath: string, key: string): boolean {
```

- Line 258
```typescript
interface PhaseSpec {
```

- Line 273
```typescript
async function runPhaseIfNeeded(
```

### src/step-timer.test.ts

- Line 216
```typescript
describe("waitForWindow", () => {
```

### src/step-timer.ts

- Line 47
```typescript
function defaultSleep(ms: number): Promise<void> {
```
