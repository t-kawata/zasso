# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-175: workspacify-tree Parsing & Integrity 基盤 — 入力正規化・hash・見出し・segment再構成 (G0/G1)

**Ticket Key**: PX-175 · **Phase**: -1

---

## RFC Reference

docs/WORKSPACIFY-TREE_Complete_Implementation_Instruction.md §3.1(G0/G1) / §4.3 / §5(lib配置) / §7.1 / §7.2 / §12.1 / §14.1(該当) / §14.2(fixture 1,3,4)

## Background

### Goal
/workspacify-tree の最下層として、単一 Markdown 仕様書パスを読み込み、BOM/改行正規化・SHA-256・ATX 見出しツリー・##単位 segment 化と「再構成完全一致」検査を担う決定論的 Node ESM モジュール群(8 lib)を Red→Green で実装する。

### Purpose
上位工程(抽出 PX-176 / ゲート PX-177 / publish PX-178)が「固定された入力 + 位置 traceability」だけへ依存できる基盤を作る。正規化後 hash は入力固定の唯一の根拠であり、以降の全工程の entry gate が参照する。

### Motivation
長大仕様書を「AI の一回限りの印象的応答」ではなく再現可能な工程へ変えるため、まず入力の完全性(復号可能/正規化可能/構造を完全分割可能/再結合で元の bytes に戻る)を機械保証する必要がある。ここが壊れると以後の全てが無根拠になる。

### Constraints
plain JS ESM(.mjs)・Node 標準ライブラリ(node:fs/promises, node:path, node:crypto, node:process, node:url)のみ・外部 package/network/LLM API/hook/Python 禁止・source range は正規化後テキストの 1 始まり line/column と 0 始まり UTF-8 byte offset・汎用性制約により特定プロジェクト語(conver/zasso/siprs 等)をコード・変数名・テスト・fixture に埋め込まない。

## Scope

- NEW: .claude/scripts/workspacify-tree/lib/errors.mjs — 全 lib 共通のエラー基盤。export: class WorkSpacifyTreeError extends Error { constructor(message, { gateId, exitCode }) }, GATE_STATUS(enum: PASS/FAIL/REVIEW_REQUIRED/BLOCKED/COMPLETE), EXIT_CODES(0=OK, 1=FAIL, 2=USAGE)。gateId は指示書§3.1 の G0..G5 識別子。
- NEW: .claude/scripts/workspacify-tree/lib/fs-safe.mjs — 入力ロック(G0)。export: isReadableRegularFile(absPath)->boolean, readSpecInput(absPath)->{absPath, rawBuffer, bytes, encoding:'UTF-8'}。存在しない/ディレクトリ/空/復号不能/読取不能は WorkSpacifyTreeError(gateId:'G0')。
- NEW: .claude/scripts/workspacify-tree/lib/hash.mjs — export: sha256Hex(bytes|Uint8Array)->lowercase hex。crypto.createHash('sha256') のみ。
- NEW: .claude/scripts/workspacify-tree/lib/normalization.mjs — export: normalizeTextBytes(buffer)->{bytes:Uint8Array, hadBom:boolean, hadCrLf:boolean}。BOM 除去→\r\n, \r を \n へ→末尾改行は内容として保持(付加/削除しない)。
- NEW: .claude/scripts/workspacify-tree/lib/markdown.mjs — 行単位 code fence 状態機械。export: scanFenceStates(lines)->Array<{line, inFence:boolean, fenceChar:'`'|'~'|null}>。``` / ~~~ を fence 開始/終了とし、fence 内の # は見出しとして扱わない。
- NEW: .claude/scripts/workspacify-tree/lib/headings.mjs — export: parseAtxHeading(line)->{level:1..6, text:string}|null(closing # 列を Markdown 規則で除去、空 text は null+警告候補), buildHeadingTree(lines, fenceStates)->Array<headingNode>。headingNode={id:'h-######', level, text, line_start, line_end, byte_start, byte_end, parent_id, children:[]}。id は行位置由来で一意、親不在は root へ、level jump は警告候補リストへ(即時 FAIL にしない)。
- NEW: .claude/scripts/workspacify-tree/lib/segmentation.mjs — export: segmentAtHeadings({sourceText, headings}, {segmentLevel:2})->{segments, warnings}, verifyReconstruction({sourceBytes, sourceHash, segments})->{status:'PASS'|'FAIL', reconstructedHash, exactMatch}。segment={id:'s-####', heading_id, title, level, line_start, line_end, byte_start, byte_end, sha256, subheading_ids}。byte 範囲の非重複/全被覆/byte_start 昇順再連結 bytes==正規化 bytes/再連結 hash==source_hash を検査し、違反は G1.3 FAIL を返す。
- NEW: .claude/scripts/workspacify-tree/lib/canonical-json.mjs — export: canonicalSerialize(value)->string(UTF-8/LF/2space indent/object key 辞書順/配列は stable canonical key 順/末尾 1 LF/前後 whitespace なし)。整数は finite、日時は UTC ISO 8601(YYYY-MM-DDTHH:mm:ss.sssZ)、hash は lowercase hex。自己 hash は integrity.manifest_hash を空文字列として canonical serialize した bytes の SHA-256(指示書§12 integrity 定義)。
- NEW: tests/workspacify-tree/fixtures/small-spec.md — 小規模な正仕様書(題名+複数 ## 章+本文)。G0/G1 正常系の基準。
- NEW: tests/workspacify-tree/fixtures/fence-headings.md — code fence(```)内に '#' を含む仕様書。fence 無視の検証用。
- NEW: tests/workspacify-tree/fixtures/duplicate-headings.md — 同名見出しを複数含む仕様書。id 一意性の検証用。
- NEW: tests/workspacify-tree/fixtures/multi-chapter-spec.md — 複数 ## 章を持つ長めの仕様書(segment 再構成の基準、後続チケットの統合にも再利用)。
- NEW: tests/workspacify-tree/unit/parse-foundation.test.mjs — 上記 lib の unit test。node --test で実行。テストは ESM(.test.mjs)。
- NEW: tests/workspacify-tree/property/segmentation-reconstruction.test.mjs — 系統生成 Markdown(見出し数/本文長/改行種別を変化)で verifyReconstruction が常に PASS する property test。
- NEW: tests/workspacify-tree/property/canonical-json-idempotence.test.mjs — canonicalSerialize の冪等性 + 1 byte 改竄で manifest hash が変わる property test。
- Out of scope: JSON Schema 検証/decisions スキーマ/所有権/DAG/抽出(§8)/manifest 全体レンダリング/atomic publish — それぞれ PX-176/PX-177/PX-178 のスコープ。
- Out of scope: 実ファイルの EACCES 再現・fsync 永続性保証 — testExceptions 参照。
- Affected: 新規ディレクトリ .claude/scripts/workspacify-tree/ と tests/workspacify-tree/ を新設するが既存 rfc-graph/tickets スクリプトには非影響。
- Affected: 後続 PX-176..178 が本 lib の exports を input として使用するため、export 名と source range 形式(1始まり line/0始まり byte)を契約として固定する。

## Investigation

指示書§7.1/§7.2 に heading/segment の必須検査(G1.3: 非重複・全被覆・再連結一致・hash 一致)を確認。既存の管理スクリプト(.claude/scripts/rfc-graph/*.js)は CommonJS だが、指示書§4.1 により本実装は ESM(.mjs) が必須。テスト実行は memory のユーザー指示により Makefile でなく node --test を使用。Tickets.json phase -1 に PX-175 を追加済み(先頭 #1 確認)。対象パス .claude/scripts/workspacify-tree/ と tests/workspacify-tree/ は未存在のため既存コード衝突なし。

## Acceptance Criteria

- Happy path: node スクリプトから multi-chapter-spec.md を読み込み→正規化→hash→見出し→segment し、reconstruction.status=='PASS' かつ reconstructed_hash==source_hash を返す
- Error case: 存在しないパス/ディレクトリ/空ファイルを渡すと WorkSpacifyTreeError(gateId 'G0')で非 0 exit となり、いかなる成果物も作成・更新しない
- Edge case: fence-headings.md / duplicate-headings.md で fence 内 # が無視され、同名見出しが全て一意 id を持つことを確認する

## Invariants

- [Normal condition] 正規化は BOM 除去→改行 LF 化の順で決定的であり、末尾改行の有無を変えない。
- [Error invariant] 入力不適格時は WorkSpacifyTreeError を返し、成果物の作成・更新・置換を一切行わない。
- [Internal state invariant] heading id・segment id は常に行位置由来の一意値であり、再実行で同一になる(決定論)。
- [Boundary invariant] segment の byte 範囲は 0 始まり offset で隣接し、重複も欠落もない。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — G0 Input lock (指示書§1.1/§2-1)

- **Precondition**: path points to a missing file or a directory or an empty file
- **Postcondition**: readSpecInput returns WorkSpacifyTreeError and writes no artifact
- **Invariant**: an input validation failure never writes or replaces any manifest file

### C002 — §4.3 hash と正規化

- **Precondition**: input bytes contain a UTF-8 BOM and mixed CRLF line endings
- **Postcondition**: normalizeTextBytes strips the BOM and converts every newline to LF and preserves the trailing newline
- **Invariant**: sha256Hex over normalized bytes equals the recorded source_hash

### C003 — §7.1 ATX heading 解析

- **Precondition**: a markdown text contains hash signs inside a code fence and duplicate heading texts
- **Postcondition**: buildHeadingTree ignores fence lines and assigns unique line-derived ids
- **Invariant**: heading ids remain unique and parent child relations form a forest

### C004 — §7.2 分割と再構成

- **Precondition**: any normalized markdown that contains at least one heading
- **Postcondition**: segments have non-overlapping byte ranges that cover the whole input and reconstructing them in byte order equals the normalized bytes
- **Invariant**: reconstruction hash equals source_hash and exact_match is true

### C005 — §12.1 canonical JSON 規則 + integrity

- **Precondition**: an arbitrary JSON serializable manifest object
- **Postcondition**: the output uses LF two-space indentation sorted keys and a single trailing LF
- **Invariant**: parse of the serialized text equals the input and reserializing is idempotent

## Boy Scout Rule

関数は動詞句(parseAtxHeading / buildHeadingTree / verifyReconstruction)、データは名詞句(segment / headingNode)で命名し、翻訳可能性を確保する。マジックナンバーは定数(SEGMENT_LEVEL=2, MAX_ATX_LEVEL=6)へ抽出。error swallowing 禁止。既存の rfc-graph スクリプトは本スコープ外のため、触る場合のみ対象に含める(本チケットでは新規作成のみ)。

## Test Plan

### Unit Tests

- UT: [Normal] fs-safe — readSpecInput が既存の正仕様書 fixture(small-spec.md)を bytes で返し absPath を保持する
- UT: [Error] fs-safe — path points to a missing file or a directory or an empty file: readSpecInput returns WorkSpacifyTreeError with gateId 'G0' and writes no artifact
- UT: [Error] fs-safe — 非 UTF-8(不正バイト列)入力で復号エラーとなり WorkSpacifyTreeError を返す
- UT: [Boundary] fs-safe — 空ファイル・1byte のみ・BOM のみの入力で入力ガードが FAIL する
- UT: [Invariant] fs-safe — an input validation failure never writes or replaces any manifest file(出力パスに touch しないことを検証)
- UT: [Normal] normalization — input bytes contain a UTF-8 BOM and mixed CRLF line endings: normalizeTextBytes strips the BOM and converts every newline to LF and preserves the trailing newline
- UT: [Boundary] normalization — 末尾改行なし/改行のみ/\r 単独混在の各入力で正規化 bytes が期待値と一致する
- UT: [Invariant] hash — sha256Hex over normalized bytes equals the recorded source_hash(既知ベクタ 'abc' と空 bytes で検証)
- UT: [Normal] markdown+headings — 通常見出し(#..######)が level/text 正しく解析され closing '#' が除去される
- UT: [Error] headings — 空見出し(# のみ)は null となり警告候補に記録される
- UT: [Boundary] headings — a markdown text contains hash signs inside a code fence and duplicate heading texts: buildHeadingTree ignores fence lines and assigns unique line-derived ids
- UT: [Invariant] headings — heading ids remain unique and parent child relations form a forest(root 複数可、cycle なし)
- UT: [Normal] segmentation — multi-chapter-spec.md を ## で分割し segments が byte 範囲非重複かつ全被覆となる
- UT: [Error] segmentation — 見出し 0 件の入力で G1.3 FAIL を返す
- UT: [Invariant] segmentation — segments have non-overlapping byte ranges that cover the whole input and reconstructing them in byte order equals the normalized bytes
- UT: [Invariant] segmentation — reconstruction hash equals source_hash and exact_match is true(byte_start 昇順再連結で検証)
- UT: [Normal] canonical-json — canonicalSerialize が LF/2space/辞書順 key/末尾 LF で出力する
- UT: [Invariant] canonical-json — parse of the serialized text equals the input and reserializing is idempotent
- UT: [Boundary] canonical-json — ネスト object/空配列/空 object/日本語 key を含む値で canonical 出力が安定する
- UT: [Error] canonical-json — 循環参照や BigInt など JSON 化不能値に対し WorkSpacifyTreeError を返す

### Integration Tests

- IT: [Integration point] fs-safe→normalization→hash→headings→segmentation の直列パイプラインを multi-chapter-spec.md に適用する
- IT: [Verification] exit 0 で reconstruction.status=='PASS' かつ reconstructed_hash==source_hash かつ segment_count>=## 章数かつ heading_id の親子参照が segmentation 結果と整合する
- IT: [Prerequisites] PX-175 の実装のみ。実行は node --test "tests/workspacify-tree/**/*.test.mjs" (Makefile 不使用)。
- IT: [Related tickets] PX-176(抽出)が segments/headings を input に、PX-177(ゲート)が reconstruction 結果を、PX-178(統合)が canonical-json を利用する

### Exceptions

- 実ファイルの permission denied(EACCES) を全プラットフォームで自動再現するのはテスト不能(root 実行時は chmod 000 でも読めるため)。設計欠陥ではない。代替検証: 存在しない path・directory path・空ファイル・非 UTF-8 で入力ガードを自動テストし、EACCES は readSpecInput の catch 分岐として同型の WorkSpacifyTreeError を返す実装とし、エラー型の一意性を unit で担保する。
- fsync 直後のディスク永続性を unit で保証するのはテスト不能(OS/FS 依存)。設計欠陥ではない。代替検証: atomic 書込の順序(temp 書込→fsync→rename→再読込検証)は PX-178 の atomic-publish.mjs で temp ディレクトリを使い自動テストし、fsync 呼び出しは可能な範囲で行う実装として統合 acceptance で確認する。

### Plan Test Code (concrete code)

- UT: C001 (G0 Input lock) 共通セットアップ
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtempSync, writeFileSync, existsSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { readSpecInput } from '../../../.claude/scripts/workspacify-tree/lib/fs-safe.mjs';
import { WorkSpacifyTreeError } from '../../../.claude/scripts/workspacify-tree/lib/errors.mjs';
const dir = mkdtempSync(join(tmpdir(), 'wst-'));
const specDir = join(dir, 'spec');
```

- UT: C001 Precondition→入力コード
```js
const missing = join(dir, 'no-such-spec.md');
const dirPath = specDir;              // directory, not a regular file
const emptyFile = join(dir, 'empty.md');
writeFileSync(emptyFile, '');         // empty file
```

- UT: C001 Postcondition→assertion
```js
assert.throws(() => readSpecInput(missing), (e) => e instanceof WorkSpacifyTreeError && e.gateId === 'G0');
assert.throws(() => readSpecInput(dirPath), (e) => e instanceof WorkSpacifyTreeError && e.gateId === 'G0');
assert.throws(() => readSpecInput(emptyFile), (e) => e instanceof WorkSpacifyTreeError && e.gateId === 'G0');
```

- UT: C001 Invariant→述語コード(never writes or replaces any manifest file)
```js
try { readSpecInput(missing); } catch { /* expected */ }
assert.equal(existsSync(join(specDir, 'WORKSPACIFY-TREE-MANIFEST.json')), false, 'an input validation failure never writes or replaces any manifest file');
```

- UT: C002 Precondition→入力コード(BOM + CRLF)
```js
import { normalizeTextBytes } from '../../../.claude/scripts/workspacify-tree/lib/normalization.mjs';
import { sha256Hex } from '../../../.claude/scripts/workspacify-tree/lib/hash.mjs';
const bom = Buffer.from([0xef, 0xbb, 0xbf]);
const input = Buffer.concat([bom, Buffer.from('title\r\n## A\r\nbody\r\n')]);
```

- UT: C002 Postcondition→assertion
```js
const { bytes, hadBom, hadCrLf } = normalizeTextBytes(input);
assert.equal(hadBom, true);
assert.equal(hadCrLf, true);
assert.equal(Buffer.from(bytes).toString('utf8'), 'title\n## A\nbody\n');   // every newline converted to LF
assert.equal(Buffer.from(bytes).toString('utf8').endsWith('\n'), true);       // preserves the trailing newline
```

- UT: C002 Invariant→述語コード(sha256Hex over normalized bytes)
```js
const sourceHash = sha256Hex(bytes);
assert.equal(sourceHash, sha256Hex(Buffer.from('title\n## A\nbody\n')));  // equals the recorded source_hash
assert.match(sourceHash, /^[0-9a-f]{64}$/);
```

- UT: C003 Precondition→入力コード(fence 内 # と同名見出し)
```js
import { scanFenceStates } from '../../../.claude/scripts/workspacify-tree/lib/markdown.mjs';
import { buildHeadingTree } from '../../../.claude/scripts/workspacify-tree/lib/headings.mjs';
const text = '# Title\n```\n# not a heading\n```\n## Status\n## Status\n';
const lines = text.split('\n');
const fence = scanFenceStates(lines);
```

- UT: C003 Postcondition→assertion
```js
const headings = buildHeadingTree(lines, fence);
assert.equal(headings.some((h) => h.text === 'not a heading'), false);  // ignores fence lines
assert.equal(new Set(headings.map((h) => h.id)).size, headings.length); // unique line-derived ids
assert.ok(headings.every((h) => /^h-\d{6}$/.test(h.id)));
```

- UT: C003 Invariant→述語コード(parent child relations form a forest)
```js
for (const h of headings) {
  if (h.parent_id) assert.ok(headings.some((x) => x.id === h.parent_id));
  assert.ok(!headings.some((x) => x.id === h.id && x !== h));            // ids remain unique
}
```

- UT: C004 Precondition→入力コード(見出し 1 つ以上の任意 Markdown)
```js
import { segmentAtHeadings, verifyReconstruction } from '../../../.claude/scripts/workspacify-tree/lib/segmentation.mjs';
const src = Buffer.from('# T\n## A\ncontent a\n## B\ncontent b\n');
const srcHash = sha256Hex(src);
const { segments } = segmentAtHeadings({ sourceText: src, headings }, { segmentLevel: 2 });
```

- UT: C004 Postcondition→assertion
```js
segments.forEach((s) => assert.ok(s.byte_start >= 0 && s.byte_end > s.byte_start));
for (let i = 1; i < segments.length; i++) assert.ok(segments[i].byte_start >= segments[i - 1].byte_end); // non-overlapping
assert.equal(segments[0].byte_start, 0);
assert.equal(segments[segments.length - 1].byte_end, src.length);          // cover the whole input
const joined = Buffer.concat(segments.map((s) => src.subarray(s.byte_start, s.byte_end)));
assert.deepEqual(joined, src);                                            // byte order equals normalized bytes
```

- UT: C004 Invariant→述語コード(reconstruction hash equals source_hash)
```js
const r = verifyReconstruction({ sourceBytes: src, sourceHash: srcHash, segments });
assert.equal(r.status, 'PASS');
assert.equal(r.exactMatch, true);
assert.equal(r.reconstructedHash, srcHash);
```

- UT: C005 Precondition→入力コード(canonical 対象 object)
```js
import { canonicalSerialize } from '../../../.claude/scripts/workspacify-tree/lib/canonical-json.mjs';
const sample = { z: 1, a: { n: 2, m: [3, 4] }, s: 'text' };
const once = canonicalSerialize(sample);
const twice = canonicalSerialize(JSON.parse(once));
```

- UT: C005 Postcondition→assertion
```js
assert.match(once, /^\{\n  "a": \{\n/);        // two-space indentation
assert.ok(once.indexOf('"z"') > once.indexOf('"a"')); // sorted keys (lexicographic)
assert.ok(once.endsWith('\n'));                          // single trailing LF
assert.equal(once.includes('\r'), false);               // LF only
```

- UT: C005 Invariant→述語コード(idempotent + parse roundtrip)
```js
assert.equal(twice, once, 'reserializing is idempotent');
assert.deepEqual(JSON.parse(once), sample, 'parse of the serialized text equals the input');
const tampered = once.replace('text', 'Text');
assert.notEqual(sha256Hex(Buffer.from(tampered)), sha256Hex(Buffer.from(once)), '1-byte tamper changes the hash');
```


## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| (absent) | NEW lib/errors.mjs | WorkSpacifyTreeError + GATE_STATUS + EXIT_CODES |
| (absent) | NEW lib/fs-safe.mjs | readSpecInput / isReadableRegularFile (G0 input lock) |
| (absent) | NEW lib/hash.mjs | sha256Hex over bytes |
| (absent) | NEW lib/normalization.mjs | normalizeTextBytes: BOM strip + CRLF/CR->LF, trailing newline preserved |
| (absent) | NEW lib/markdown.mjs | scanFenceStates + lineByteOffsets |
| (absent) | NEW lib/headings.mjs | parseAtxHeading + buildHeadingTree + collectHeadingWarnings |
| (absent) | NEW lib/segmentation.mjs | segmentAtHeadings + verifyReconstruction (G1.3) |
| (absent) | NEW lib/canonical-json.mjs | canonicalSerialize: LF/2-space/sorted keys/idempotent |
| (absent) | NEW tests/workspacify-tree/fixtures/*.md (4 files) | small-spec / fence-headings / duplicate-headings / multi-chapter-spec |
| (absent) | NEW tests/workspacify-tree/unit/parse-foundation.test.mjs | unit tests with @verifies C001..C005 |
| (absent) | NEW tests/workspacify-tree/property/*.test.mjs (2 files) | segmentation reconstruction + canonical-json idempotence property tests |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) errors/fs-safe/hash/normalization → (2) markdown/headings → (3) segmentation/verifyReconstruction → (4) canonical-json → (5) fixture・unit・property を各段の Red で先行作成。
- [Risks] fence 状態機械の曖昧性(fence 内 fence)は仕様に無いため対象外とし、単純な 3 連バッククォート/チルダのみ扱う。
- [Caveats] source range の line/column は 1 始まり、byte offset は 0 始まり。UTF-8 なので日本語も byte 単位で数える。
- [Open items] level jump の即時 FAIL 化は設定可能にする(本チケットでは警告候補のみ)。
- [Future improvements] 高速化(巨大仕様書向けストリーミング)は acceptance 後に検討。
Implementation summary:
- Implemented Parsing & Integrity foundation for /workspacify-tree as ESM(.mjs) modules under .claude/scripts/workspacify-tree/lib/.
- TDD: wrote unit+property tests first (RED: module-not-found), then implemented to GREEN, then refactored (single-letter vars removed, buildSegment 3 params).
- Test results: node --test -> 35/35 PASS (unit 33 + property 2 files).
- Quality: run-quality-checks -> totalIssues 0.
- Provenance: annotate-ticket-context-by-git-diff does not support .mjs (SOURCE_EXTENSIONS has no .mjs), so canonical [::TICKET::] headers were added manually to the 8 lib files and test files.
- No stubs/crimes introduced; no [::AMBIGUOUS::] markers.
Review report:
- Static quality check: passed
- Translatability: no issues
- Contract coverage: @verifies verified via verify-red-coverage
- No open crimes, no stubs, no [::AMBIGUOUS::] markers
- Annotation: [::TICKET::] provenance present on all source files

## PX-175 — 0 locations, not implemented

Not found
