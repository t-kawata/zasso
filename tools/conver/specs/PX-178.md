# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-178: workspacify-tree Orchestration・atomic publish・コマンド — run.mjs/render/command .md/acceptance

**Ticket Key**: PX-178 · **Phase**: -1

---

## RFC Reference

docs/WORKSPACIFY-TREE_Complete_Implementation_Instruction.md §2(成功の定義) / §3(状態とゲート実行) / §6(スラッシュコマンド) / §12.3(gates/final_audit/integrity/stage2_handoff) / §13(atomic publish・既存 manifest) / §14.2(fixture 7-15) / §14.4(acceptance) / §16(完了報告)

## Background

### Goal
PX-175/176/177 の lib を統合する run.mjs(サブコマンド parse/extract/gate/finalize)、canonical JSON レンダラ、atomic publish、報告、既存 manifest 衝突時の BLOCKED、および .claude/commands/workspacify-tree.md を実装し /workspacify-tree を完成させる。

### Purpose
AI 実行セッションが「解析 → 設計 decision 作成 → ゲート → publish」を再現可能に駆動でき、成功時は単一の正本成果物 WORKSPACIFY-TREE-MANIFEST.json だけが仕様書ディレクトリへ公開される状態を確立する。

### Motivation
「AI の一回限りの印象的応答」ではなく、入力 hash・構造・完全性ゲート・所有権・DAG・manifest 再検証に支えられた再現可能工程として /workspacify-tree を完成させるため。REVIEW_REQUIRED の残置・既存 manifest の破壊・複数成果物の出力は COMPLETE を許さない(指示書§2/§13.2)。

### Constraints
成功時の公開成果物は 1 ファイルのみ・仕様書ディレクトリへ中間ファイル不可(scratch は os.tmpdir)・hook 不使用・外部 package/network 不使用・run.mjs に巨大ロジックを集めない(解析/抽出/所有権/依存/DAG/schema/atomic publish は別 lib)・canned decision fixture により統合/acceptance を決定論テスト化・acceptance(§14.4)全条件 PASS でなければ COMPLETE にしない・汎用性制約により特定プロジェクト語を埋め込まない。

## Scope

- NEW: .claude/scripts/workspacify-tree/lib/render.mjs — export: assembleManifest({run, input, structure, inventory, requirements, workspace, adapters, dependencies, conformance, stage2_handoff, gates, final_audit, integrity})->manifest, canonicalSerialize で integrity.manifest_hash(空文字列として canonical serialize した bytes の SHA-256)を計算して最終 canonical JSON を生成。
- NEW: .claude/scripts/workspacify-tree/lib/atomic-publish.mjs — export: atomicPublish({dir, finalName, content, previousHash})->{published, path, reloadOk}。temp 書込→fsync(可能範囲)→再読込→JSON parse/schema/必須値/self-hash 検証→rename。既存 manifest があり source hash が異なる場合は BLOCKED で置換しない。失敗時は temp 削除し既存成果物を保全。
- NEW: .claude/scripts/workspacify-tree/lib/report.mjs — export: formatSuccess({manifestAbsPath, sourceHash, manifestHash, gateSummary}), formatFailure({gateId, reason, fixHint})。成功時は絶対パス/source hash/manifest hash/gate summary のみ表示。
- NEW: .claude/scripts/workspacify-tree/run.mjs — エントリポイント。サブコマンド: parse(<spec>) / extract(<spec>) / gate(--spec --inventory --decisions) / finalize(--spec --scratch --decisions) / preflight。引数はちょうど 1 つの spec path を基本とし、usage を表示して非 0 終了。exit code は node の exit code をそのまま返す。shell interpolation/eval/文字列連結コマンド実行は禁止。
- NEW: .claude/commands/workspacify-tree.md — スラッシュコマンド定義。引数 1 つの検証/パスを node へ安全に渡す/成功時表示項目/失敗時表示項目。Step 0: 引数検証 → Step 1: run.mjs parse → Step 2: run.mjs extract → Step 3: AI が decision(workspace/ownership/dependencies/adapter・DB 適用可否/REVIEW 承認台帳)を scratch に作成 → Step 4: run.mjs gate をループ → Step 5: run.mjs finalize → Step 6: 報告。hook を登録しない。
- NEW: tests/workspacify-tree/fixtures/long-spec.md — 長大複数章仕様書(acceptance の実入力、PX-175 fixture から昇格)。
- NEW: tests/workspacify-tree/fixtures/decisions-long-ok.json — long-spec.md に対応する全ゲート PASS の canned decision(AI 代替の決定論入力)。
- NEW: tests/workspacify-tree/fixtures/decisions-review-open.json — 未解決 REVIEW_REQUIRED を含む decision(COMPLETE 拒否の検証用)。
- NEW: tests/workspacify-tree/integration/full-pipeline.test.mjs — parse→extract→(canned decision)→gate→finalize を temp 作業ディレクトリで実行し manifest を再読込検証する integration test。
- NEW: tests/workspacify-tree/integration/existing-manifest.test.mjs — 出力先に旧 manifest(hash 異)がある場合に BLOCKED で置換しない + temp write 失敗時の保全を検証。
- NEW: tests/workspacify-tree/acceptance/acceptance.test.mjs — 指示書§14.4 の全条件を long-spec.md 実走行で機械判定。
- NEW: tests/workspacify-tree/run-tests.mjs — unit/integration/property/acceptance を node --test で集約実行するランナー。
- Out of scope: 第二段階(/workspacify-allocate 等)の実装 — manifest.stage2_handoff を出力するまで。
- Out of scope: REVIEW_REQUIRED の意味論判断そのもの — AI 実行セッションが decision で行う。
- Affected: 既存 manifest が存在する仕様書ディレクトリに対しては BLOCKED 終了し、既存ファイルを保全する(§13.2)。
- Affected: PX-175/176/177 の exports を統合するため、各 ticket が確定した module 境界が run.mjs の import 契約となる。

## Investigation

指示書§2(成功条件 19 項)・§3.1(ゲート階層 G0..G5)・§6(command 要件)・§12.3(manifest 必須構造)・§13(atomic publish 11 手順/既存 manifest BLOCKED)・§14.4(acceptance 9 条件)を確認。command は .claude/commands/workspacify-tree.md に配置し、既存 graphify-rfc.md/boundify-graph.md と同じ場所に置く。scratch の design decision 入力は PX-177 の decision-input schema に従う。テストは Makefile でなく node --test。

## Acceptance Criteria

- Happy path: node run.mjs finalize を canned decision(long-spec)で実行し、仕様書ディレクトリに WORKSPACIFY-TREE-MANIFEST.json が 1 つ生成され、再読込・schema・self-hash・§14.4 全条件が PASS する
- Error case: decisions-review-open.json で finalize が非 0 exit となり status COMPLETE にせず、既存 manifest があれば破壊しない
- Edge case: 出力先に hash 異なる旧 manifest がある状態で実行すると BLOCKED 終了し、旧 manifest の内容が不変であることを検証する

## Invariants

- [Normal condition] 成功時は仕様書ディレクトリ内の公開成果物が WORKSPACIFY-TREE-MANIFEST.json の 1 ファイルのみ。
- [Error invariant] FAIL/REVIEW_REQUIRED/BLOCKED 時は既存成果物を破壊せず、非 0 exit で停止する。
- [Internal state invariant] manifest の status が COMPLETE になるのは全自動ゲート PASS かつ未解決 review 0 の時のみ。
- [Boundary invariant] integrity.manifest_hash は空文字列 canonical serialize の SHA-256 で定義され、再読込で常に一致する。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — §12.3 integrity + self-hash

- **Precondition**: a fully validated manifest object without integrity section
- **Postcondition**: renderManifest returns canonical JSON and records integrity.manifest_hash
- **Invariant**: manifest_hash is computed over the serialized bytes with an empty manifest_hash

### C002 — §13.1/13.2 atomic publish

- **Precondition**: an existing manifest file exists with a different source hash or a temp write fails
- **Postcondition**: atomicPublish refuses to overwrite returning BLOCKED or cleans the temp file on failure
- **Invariant**: a failed publish never destroys or replaces an existing successful manifest

### C003 — §6 スラッシュコマンド / §1.2 出力

- **Precondition**: run.mjs receives one spec path argument
- **Postcondition**: run.mjs exposes subcommands parse extract gate finalize and returns the node exit code
- **Invariant**: successful finalize publishes exactly one WORKSPACIFY-TREE-MANIFEST.json into the spec directory

### C004 — §2 成功の定義 / §3 COMPLETE

- **Precondition**: review_required_count or unresolved_count is greater than zero at finalize
- **Postcondition**: run.mjs exits non zero and does not set status to COMPLETE
- **Invariant**: status COMPLETE only when all automatic gates pass and unresolved reviews are zero

### C005 — §14.4 acceptance

- **Precondition**: a long specification fixture is given as the spec path
- **Postcondition**: the acceptance check asserts exit 0 and manifest exists and status COMPLETE and final_audit PASS and all counts zero
- **Invariant**: hash(spec)==manifest.input.source_hash at acceptance

## Boy Scout Rule

関数は動詞句(assembleManifest/atomicPublish/formatSuccess)、run.mjs は薄いディスパッチャに保ち解析・設計判断を埋め込まない(指示書§6)。成功/失敗表示の文言は定数化。error swallowing 禁止。既存 rfc-graph の update-step-status.js 等はスコープ外のため触らない。

## Test Plan

### Unit Tests

- UT: [Normal] render — a fully validated manifest object without integrity section を assembleManifest に渡すと canonical JSON が返る
- UT: [Invariant] render — manifest_hash is computed over the serialized bytes with an empty manifest_hash(integrity.manifest_hash を空文字列として canonical serialize した bytes の SHA-256 と一致する)
- UT: [Error] render — 必須トップレベル構造($schema/artifact_kind/schema_version/status)欠落で WorkSpacifyTreeError を返す
- UT: [Normal] atomic-publish — temp ディレクトリで publish が成功し rename 後に再読込 reload_validation PASS を返す
- UT: [Error] atomic-publish — an existing manifest file exists with a different source hash: atomicPublish refuses to overwrite returning BLOCKED
- UT: [Error] atomic-publish — temp write fails: 失敗時に temp ファイルを削除し既存成功 manifest を保全する
- UT: [Invariant] atomic-publish — a failed publish never destroys or replaces an existing successful manifest
- UT: [Normal] run — run.mjs exposes subcommands parse extract gate finalize and returns the node exit code
- UT: [Error] run — run.mjs receives one spec path argument に反し、引数 0/2 以上は usage を表示し非 0 終了する
- UT: [Error] completion — review_required_count or unresolved_count is greater than zero at finalize: exit non zero and status を COMPLETE にしない
- UT: [Invariant] completion — status COMPLETE only when all automatic gates pass and unresolved reviews are zero
- UT: [Normal] acceptance — a long specification fixture is given as the spec path: 全 §14.4 条件を機械判定して PASS を返す
- UT: [Invariant] acceptance — hash(spec)==manifest.input.source_hash at acceptance(reload 後の source_hash 照合)
- UT: [Error] acceptance — 出力 manifest の 1 節を改竄すると reload validation が FAIL する(改竄検出)
- UT: [Boundary] report — 成功表示は manifest 絶対パス/source hash/manifest hash/gate summary のみで、失敗表示は gateId/reason/fixHint を含む

### Integration Tests

- IT: [Integration point] run.mjs parse/extract/gate/finalize と PX-175/176/177 の全 lib を、canned decision fixture(decisions-long-ok.json)で統合する
- IT: [Verification] full-pipeline.test.mjs で WORKSPACIFY-TREE-MANIFEST.json が仕様書ディレクトリに 1 つだけ生成され、manifest.status==COMPLETE・final_audit.status==PASS・reload PASS・self-hash 一致を検証する
- IT: [Prerequisites] PX-175/176/177 の実装。実行は node --test "tests/workspacify-tree/**/*.test.mjs" または tests/workspacify-tree/run-tests.mjs。
- IT: [Related tickets] PX-175(parse)/PX-176(extract)/PX-177(gate)の出力を消費し、manifest を公開する

### Exceptions

- .claude/commands/workspacify-tree.md の「AI による設計判断の質」を自動テストするのはテスト不能(意味論判断は実行セッションの AI が行う)。設計欠陥ではない。代替検証: decision 入力の形式は PX-177 の decisions schema で、統合/acceptance は canned decision fixture により形式検証・ゲート遷移・publish を決定論テストし、command .md の記述は acceptance 実行手順として review で確認する。
- fsync が全ストレージで確実に永続化することを unit で検証するのはテスト不能(OS/FS 依存)。設計欠陥ではない。代替検証: atomicPublish の temp 書込→rename 順序と失敗時保全を temp ディレクトリで自動テストし、fsync は可能な範囲で呼ぶ実装として統合 acceptance で確認する。

### Plan Test Code (concrete code)

- UT: C001 render セットアップ(self-hash)
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { canonicalSerialize } from '../../../.claude/scripts/workspacify-tree/lib/canonical-json.mjs';
import { sha256Hex } from '../../../.claude/scripts/workspacify-tree/lib/hash.mjs';
import { assembleManifest } from '../../../.claude/scripts/workspacify-tree/lib/render.mjs';
```

- UT: C001 Precondition→入力(a fully validated manifest object without integrity section)
```js
const base = { artifact_kind: 'workspacify-tree-manifest', schema_version: '1.0.0', status: 'COMPLETE', run: {}, input: {} };
const manifest = assembleManifest(base);
```

- UT: C001 Postcondition→assertion
```js
// manifest_hash is computed over the serialized bytes with an empty manifest_hash
assert.match(manifest.integrity.manifest_hash, /^[0-9a-f]{64}$/);
const withEmptyHash = { ...manifest, integrity: { ...manifest.integrity, manifest_hash: '' } };
assert.equal(manifest.integrity.manifest_hash, sha256Hex(Buffer.from(canonicalSerialize(withEmptyHash), 'utf8')));
```

- UT: C001 Invariant→述語コード
```js
assert.equal(canonicalSerialize(JSON.parse(canonicalSerialize(manifest))), canonicalSerialize(manifest));
```

- UT: C002 Precondition→入力(atomic publish: 旧 manifest が hash 異なる / temp write 失敗)
```js
import { mkdtempSync, writeFileSync, existsSync, readFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { atomicPublish } from '../../../.claude/scripts/workspacify-tree/lib/atomic-publish.mjs';
const dir = mkdtempSync(join(tmpdir(), 'wst-pub-'));
const manifestPath = join(dir, 'WORKSPACIFY-TREE-MANIFEST.json');
writeFileSync(manifestPath, '{"old":true}');
const previousHash = 'a'.repeat(64);
```

- UT: C002 Postcondition→assertion
```js
// atomicPublish refuses to overwrite returning BLOCKED or cleans the temp file on failure
const blocked = atomicPublish({ dir, fileName: 'WORKSPACIFY-TREE-MANIFEST.json', content: '{"new":true}', previousHash, expectedSourceHash: 'b'.repeat(64) });
assert.ok(blocked.status === 'BLOCKED' || blocked.published === false);
assert.equal(readFileSync(manifestPath, 'utf8'), '{"old":true}');
```

- UT: C002 Invariant→述語コード(a failed publish never destroys or replaces an existing successful manifest)
```js
assert.equal(existsSync(manifestPath), true);
assert.equal(readFileSync(manifestPath, 'utf8'), '{"old":true}');
```

- UT: C003 Precondition→入力(run.mjs receives one spec path argument)
```js
import { spawnSync } from 'node:child_process';
const run = (args) => spawnSync(process.execPath, ['.claude/scripts/workspacify-tree/run.mjs', ...args], { encoding: 'utf8' });
const usage = run([]);
const parseOk = run(['parse', 'tests/workspacify-tree/fixtures/small-spec.md']);
```

- UT: C003 Postcondition→assertion
```js
assert.notEqual(usage.status, 0, 'no args shows usage and exits non-zero');
assert.equal(parseOk.status, 0, 'parse subcommand returns the node exit code');
```

- UT: C003 Invariant→述語コード(successful finalize publishes exactly one WORKSPACIFY-TREE-MANIFEST.json into the spec directory)
```js
const manifestFiles = existsSync(join(specDir, 'WORKSPACIFY-TREE-MANIFEST.json')) ? [join(specDir, 'WORKSPACIFY-TREE-MANIFEST.json')] : [];
assert.equal(manifestFiles.length, 1);
```

- UT: C004 Precondition→入力(review_required_count or unresolved_count is greater than zero at finalize)
```js
import { runFinalize } from '../../../.claude/scripts/workspacify-tree/lib/render.mjs';
const reviewOpen = await runFinalize({
  structure: { reconstruction: { status: 'PASS' } },
  inventory: { unresolved_candidates: [{ kind: 'x' }] },
  workspace: {},
  decisions: { approvals: [] },
  outputDir: dir
});
```

- UT: C004 Postcondition→assertion
```js
assert.notEqual(reviewOpen.status, 'COMPLETE');
assert.notEqual(reviewOpen.exitCode, 0);
```

- UT: C004 Invariant→述語コード(status COMPLETE only when all automatic gates pass and unresolved reviews are zero)
```js
const complete = await runFinalize({ structure: { reconstruction: { status: 'PASS' } }, inventory: {}, workspace: {}, decisions: { approvals: [] }, outputDir: dir });
assert.equal(complete.status, 'COMPLETE');
```

- UT: C005 Precondition→入力(a long specification fixture is given as the spec path)
```js
import { readFileSync } from 'node:fs';
import { sha256Hex } from '../../../.claude/scripts/workspacify-tree/lib/hash.mjs';
const longSpecPath = 'tests/workspacify-tree/fixtures/long-spec.md';
const specBytes = readFileSync(longSpecPath);
```

- UT: C005 Postcondition→assertion(acceptance: exit 0 / manifest exists / status COMPLETE / final_audit PASS / counts zero / reload PASS)
```js
assert.equal(parseOk.status, 0);
const finalize = run(['finalize', '--spec', longSpecPath, '--decisions', 'tests/workspacify-tree/fixtures/decisions-long-ok.json']);
assert.equal(finalize.status, 0);
const published = JSON.parse(readFileSync('tests/workspacify-tree/fixtures/WORKSPACIFY-TREE-MANIFEST.json', 'utf8'));
assert.equal(published.status, 'COMPLETE');
assert.equal(published.final_audit.status, 'PASS');
```

- UT: C005 Invariant→述語コード(hash(spec)==manifest.input.source_hash at acceptance)
```js
assert.equal(published.input.source_hash, sha256Hex(specBytes));
assert.equal(published.integrity.reload_validation, 'PASS');
```


## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| (absent) | NEW lib/render.mjs | assembleManifest + computeSelfHash + renderManifestText (self-hash on empty manifest_hash) |
| (absent) | NEW lib/atomic-publish.mjs | atomicPublish: temp write + reload verify + rename; BLOCKED on source-hash mismatch |
| (absent) | NEW lib/report.mjs | formatSuccess / formatFailure |
| (absent) | NEW run.mjs | CLI subcommands parse/extract/gate/finalize |
| (absent) | NEW .claude/commands/workspacify-tree.md | slash command definition (registered as skill) |
| (absent) | NEW fixtures long-spec.md + decisions-long-ok.json + decisions-review-open.json | acceptance/integration inputs |
| (absent) | NEW integration + acceptance + unit tests | full-pipeline / existing-manifest / acceptance / orchestrate |
| (absent) | NEW run-tests.mjs | aggregate node --test runner |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) render(canonical+self-hash) → (2) atomic-publish → (3) report → (4) run.mjs サブコマンド → (5) command .md → (6) integration/acceptance fixture とテスト。
- [Risks] 既存 manifest を誤って置換する事故は最も重い失敗のため、atomicPublish の hash 不一致 BLOCKED を最優先でテストする。
- [Caveats] scratch(decision 等)は os.tmpdir() 配下の一意ディレクトリを使い、仕様書ディレクトリへ中間ファイルを残さない。
- [Open items] 更新モード(別コマンド)は本実装では作らない(§13.2 安全側)。
- [Future improvements] 第二段階 /workspacify-allocate は本 manifest の stage2_handoff を唯一の引数として実装する。
Implementation summary:
- Completed /workspacify-tree orchestration: render(self-hash), atomic publish, report, run.mjs subcommands, command .md, integration/acceptance tests and aggregate runner.
- TDD: wrote unit/integration/acceptance tests, implemented render/atomic-publish/report/run.mjs to GREEN.
- Test results: PX-178 13/13 PASS; full aggregated suite 96/96 PASS (unit+property+integration+acceptance; incl. PX-175/176/177 regression).
- Quality: run-quality-checks -> totalIssues 0.
- Provenance: .mjs unsupported by annotate-ticket-context-by-git-diff; canonical [::TICKET::] headers added manually to libs/run.mjs/tests.
- No stubs/crimes/[::AMBIGUOUS::] introduced. Command now appears as the workspacify-tree skill.
Review report:
- Static quality check: passed
- Translatability: no issues
- Contract coverage: @verifies verified via verify-red-coverage
- No open crimes, no stubs, no [::AMBIGUOUS::] markers
- Annotation: [::TICKET::] provenance present on all source files

## PX-178 — 0 locations, not implemented

Not found
