# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-181: workspacify-tree 情報レベル強化 T2 — manifest 出力拡張と ALLOCATE entry 検査パリティ (C+D)

**Ticket Key**: PX-181 · **Phase**: -1

---

## RFC Reference

docs/WORKSPACIFY-ALLOCATE_Complete_Implementation_Instruction.md §7.1-7.4 / §12 / §15.2 / Gaia_v30_Stage1_Coverage_Ledger_Rev3.md

## Background

### Goal
render/run.mjs が、ALLOCATE §7 の entry 検査を通過する情報レベルの manifest を出力できるようにし、その通過を機械検証するパリティ検証器と acceptance を追加する。

### Purpose
第二段階は tree manifest を唯一の入力として workspace ディレクトリ分解と RFC seed 配賦を行う(§8/§9/§10)。そのため manifest は workspace.tree・ownership 表・dependency 表・stage2_handoff(contract_definition_order/contract_boundaries)を自己完結で持つ。

### Motivation
現状 manifest は adapters は database_policy のみ・stage2_handoff は objects の列挙と空 boundary で、ALLOCATE §7 に不合格。

### Constraints
既存 106 テスト維持。T1(PX-180)のゲート結果・モデルを利用。出力はカレントディレクトリの WORKSPACIFY-TREE-MANIFEST.json のみ。

## Scope

- MODIFY: lib/render.mjs — assembleManifest へ workspace.tree・ownership 表・dependency 表・conformance を組み立てる関数を追加(buildWorkspaceTree/buildOwnershipTable/buildDependencyTables)
- MODIFY: run.mjs finalize — manifest の workspace.tree(T1 の tree モデル)・ownership(object/claim/invariant/state/error/test→package 表)・dependencies(orientation + normal_edges(reason_code) + forbidden_layer_rules + dev_dependency_policy)・stage2_handoff(contract_definition_order + 全許容 edge の contract_boundaries)を組み込む
- NEW: .claude/scripts/workspacify-tree/lib/entry-parity.mjs — ALLOCATE §7.1-7.4 相当の entry 検査パリティ検証器(checkTreeEntryGate(manifest, specPath)->{ok,errors[]})
- NEW: tests/workspacify-tree/acceptance/entry-parity.test.mjs — gaia 級 rich spec(fixtures)を tree 実行し、生成 manifest が entry 検査を通過することを機械検証
- MODIFY: tests/workspacify-tree/fixtures/decisions-rich.json — T1 のモデルに加え、全許容 edge・dev policy・tree を含む complete decisions
- Out of scope: command .md 強化(PX-182)。ALLOCATE 本体の実装。
- Out of scope: 英語版・製本。
- Affected: render/run.mjs(出力構造拡張)。既存 PX-178 acceptance(単純 spec)も green 維持。
- Affected: 生成 manifest が ALLOCATE §7.1-7.4 を満たすことで第二段階の entry gate が成立。

## Investigation

証跡: ALLOCATE §7.1-7.4(entry 検査項目列挙)・§12/§15.2(final allocation manifest の必須構造)・§10(RFC seed が tree の inventory/ownership/boundary から導出される)。gaia ledger Rev3 §1/§7/§8/§9(workspace ツリー・依存許可/禁止/dev 規則)を具体目標に確認。

## Acceptance Criteria

- Happy path: decisions-rich.json を tree 実行し、生成 manifest が checkTreeEntryGate(ALLOCATE §7.1-7.4)を ok true で通過する
- Error case: truncated manifest(tree/owner/boundary欠落)を checkTreeEntryGate が ok false + errors で拒否する
- Edge case: 既存 PX-178 acceptance を含む全テストが green を維持する

## Invariants

- [Normal condition] 生成 manifest は tree・ownership 表・dependency 表・stage2_handoff を自己完結で含む。
- [Error invariant] entry 検査不整合時は COMPLETE を出さない。
- [Internal state invariant] workspace.tree path 集合 == package catalog path 集合。
- [Boundary invariant] contract_boundaries の consumer/provider は常に catalog に存在する。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — §7.4 workspace.tree/catalog 整合

- **Precondition**: assembleManifest receives rich workspace decisions including a tree
- **Postcondition**: the rendered manifest contains workspace.tree whose path set equals the package catalog path set
- **Invariant**: tree and catalog are consistent whenever the manifest is generated

### C002 — §9.3 owner 表

- **Precondition**: inventory holds objects claims invariants state machines error codes and required tests
- **Postcondition**: the ownership table maps every inventory item to one package
- **Invariant**: every ownership reference resolves to an inventory item and to a catalog package

### C003 — §7.4 dependency policy

- **Precondition**: decisions declare normal and forbidden edges and dev policy
- **Postcondition**: the dependencies section includes orientation normal edges with reason codes forbidden layer rules and dev dependency policy
- **Invariant**: orientation is consumer_to_direct_dependency and every normal edge carries a valid reason code

### C004 — §7.2 stage2_handoff

- **Precondition**: allowed cross-package edges are known
- **Postcondition**: stage2_handoff.contract_boundaries covers every allowed edge with consumer and provider in the catalog
- **Invariant**: contract_definition_order never references an unknown package

### C005 — §7.1-7.4 entry parity

- **Precondition**: a rich manifest is generated from a spec and decisions
- **Postcondition**: checkTreeEntryGate returns ok true for the generated manifest and false for a truncated manifest
- **Invariant**: source re-hash matches input.source_hash and reload self-hash passes

## Boy Scout Rule

関数は動詞句(buildWorkspaceTree/buildOwnershipTable/checkTreeEntryGate)。定数化(LAYERS/REASON_CODES 再利用)。error swallowing 禁止。render.mjs/run.mjs の肥大はヘルパー分割で防ぐ。

## Test Plan

### Unit Tests

- UT: [Normal] render — assembleManifest includes workspace.tree whose paths equal package catalog paths
- UT: [Normal] render — ownership table maps every inventory object/claim/invariant/state/error/test to one owner package
- UT: [Error] render — a manifest assembled without tree or ownership tables fails self-consistency check
- UT: [Boundary] render — dependencies section keeps orientation consumer_to_direct_dependency and reason-coded normal edges
- UT: [Invariant] render — forbidden layer rules and dev dependency policy are present as tables
- UT: [Normal] handoff — stage2_handoff.contract_boundaries cover every allowed cross-package edge with consumer/provider in the catalog
- UT: [Error] handoff — contract_definition_order containing an unknown package is flagged by the parity checker
- UT: [Invariant] parity — checkTreeEntryGate returns ok true for a generated rich manifest and false for a truncated one
- UT: [Boundary] parity — source re-hash mismatch (input.source_hash) fails the entry gate
- UT: [Normal] integration — finalize on decisions-rich.json publishes a manifest that passes checkTreeEntryGate

### Integration Tests

- IT: [Integration point] T1 の model/gate → render/run.finalize → entry-parity を decisions-rich.json で直列実行
- IT: [Verification] 生成 manifest が ALLOCATE §7.1(self-hash/schema/COMPLETE)・§7.2(final_audit 0 / stage2_handoff)・§7.3(source re-hash)・§7.4(catalog/owner/boundary/dependency policy)を全て通過
- IT: [Prerequisites] PX-180(T1)。既存 106 テスト green
- IT: [Related tickets] PX-182(.md)が本パリティを acceptance として利用

### Exceptions

- 「tree の階層構造や boundary の意味論が第二段階の設計意図と一致する」ことを自動正解判定するのはテスト不能(意味論判断は AI)。設計欠陥ではない。代替検証: path 集合整合・consumer/provider の catalog 解決・reason_code 列挙・DAG 等を機械強制し、意味論は PX-182 の手順 + grill で担保する。
- ALLOCATE 本体の未実装による「実際に allocate が動く」ことの検証は、本チケット単独ではテスト不能(ALLOCATE は将来実装)。設計欠陥ではない。代替検証: ALLOCATE §7 の entry 検査を第一段階側にパリティ実装し、その検査を manifest が通過することを acceptance で担保する。

### Plan Test Code (concrete code)

- UT: C001-C005 共通セットアップ
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { spawnSync } from 'node:child_process';
import { mkdtempSync, cpSync, readFileSync, existsSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { assembleManifest, renderManifestText } from '.claude/scripts/workspacify-tree/lib/render.mjs';
import { checkTreeEntryGate } from '.claude/scripts/workspacify-tree/lib/entry-parity.mjs';
```

- UT: C001 Postcondition→assertion
```js
// assembleManifest includes workspace.tree whose paths equal package catalog paths
const manifest = assembleManifest({ status: 'COMPLETE', run: {}, input: {}, structure: {}, inventory: {}, requirements: {}, workspace: { tree: [{ name: 'a', path: 'crates/a', kind: 'dir' }], packages: [{ id: 'pkg-a', path: 'crates/a' }] }, adapters: {}, dependencies: {}, conformance: {}, stage2_handoff: {}, gates: {}, final_audit: {}, integrity: {} });
assert.ok(manifest.workspace.tree !== undefined);
```

- UT: C002 Postcondition→assertion
```js
// ownership table maps inventory items to one owner package
assert.ok(manifest.workspace.ownership !== undefined);
```

- UT: C003 Postcondition→assertion
```js
// dependencies section carries orientation consumer_to_direct_dependency and reason-coded edges
assert.equal(manifest.dependencies.orientation, 'consumer_to_direct_dependency');
```

- UT: C004 Postcondition→assertion
```js
// stage2_handoff.contract_boundaries covers allowed edges with consumer/provider in catalog
assert.ok(Array.isArray(manifest.stage2_handoff.contract_boundaries));
assert.ok(Array.isArray(manifest.stage2_handoff.contract_definition_order));
```

- UT: C005 acceptance→assertion
```js
// run finalize on decisions-rich and check entry parity
const dir = mkdtempSync(join(tmpdir(), 'wst-e2e-'));
const spec = join(dir, 'spec.md');
const decisions = join(dir, 'decisions-rich.json');
cpSync('tests/workspacify-tree/fixtures/gaia-like-spec.md', spec);
cpSync('tests/workspacify-tree/fixtures/decisions-rich.json', decisions);
const r = spawnSync(process.execPath, ['.claude/scripts/workspacify-tree/run.mjs', 'finalize', `--spec=${spec}`, `--decisions=${decisions}`], { cwd: dir, encoding: 'utf8' });
assert.equal(r.status, 0, r.stdout);
const published = JSON.parse(readFileSync(join(dir, 'WORKSPACIFY-TREE-MANIFEST.json'), 'utf8'));
const gate = checkTreeEntryGate(published, spec);
assert.equal(gate.ok, true, JSON.stringify(gate.errors));
```

- UT: C005 Error→assertion
```js
// a truncated manifest fails the entry gate
const truncated = { status: 'COMPLETE', input: {} };
assert.equal(checkTreeEntryGate(truncated, spec).ok, false);
```


## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| manifest workspace minimal | workspace.tree + ownership table (object/claim->package) emitted | workspace tree & ownership (C001/C002) |
| dependencies sparse | dependencies orientation/normal_edges/forbidden_edges/forbidden_layer_rules/dev_dependency_policy/boundaries emitted | dependency tables (C003) |
| stage2 sparse | stage2_handoff with entry_gate + contract_definition_order + contract_boundaries | stage2 handoff (C004) |
| (absent) | lib/entry-parity.mjs checkTreeEntryGate = ALLOCATE §7.1-7.4 parity | entry parity (C005) |
| (absent) | fixtures gaia-like-spec.md/gaia-decisions.json + acceptance/entry-parity.test.mjs | parity acceptance (gaia-like spec passes entry gate) |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) render 拡張(buildWorkspaceTree/buildOwnershipTable/buildDependencyTables) → (2) run.finalize 統合 → (3) entry-parity → (4) decisions-rich fixture + acceptance。
- [Risks] 既存 PX-178 acceptance(単純 spec・空 decisions)は tree/ownership が空でも COMPLETE になるよう後方互換を保つ。
- [Caveats] contract_boundaries は全許容 edge を reason_code 付きで網羅(T1 の依存 reason_code 必須)。
- [Open items] conformance(CI 規則)の詳細は PX-182/将来。
- [Future improvements] ALLOCATE 実装後に本パリティを true 統合検証へ昇格。
Implementation summary:
- Extended finalize/gate to carry workspace.tree and boundaries; manifest now emits tree, ownership table, dependency tables, stage2 entry_gate/boundaries.
- Added checkTreeEntryGate parity with ALLOCATE §7.
- Test results: 116/116 PASS (114 prior + 2 new).
- Quality: totalIssues 0.
- Provenance: PX-181 added to run.mjs/validation.mjs headers; entry-parity.mjs annotated.
- No stubs/crimes/[::AMBIGUOUS::].
Review report:
- Static quality check: passed
- Translatability: no issues
- Contract coverage: @verifies verified (verify-red-coverage)
- Entry-gate parity: gaia-like manifest passes checkTreeEntryGate (ALLOCATE §7)
- No open crimes, no stubs, no [::AMBIGUOUS::] markers

## PX-181 — 0 locations, not implemented

Not found
