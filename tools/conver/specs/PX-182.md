# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-182: workspacify-tree 情報レベル強化 T3 — command .md 強化と反復到達ループ統合 (E)

**Ticket Key**: PX-182 · **Phase**: -1

---

## RFC Reference

docs/WORKSPACIFY-TREE_Complete_Implementation_Instruction.md §0-§17 / Gaia_v30_Stage1_Coverage_Ledger_Rev3.md / conver README grill 手順

## Background

### Goal
.claude/commands/workspacify-tree.md(日本語)を、Step 3 の「情報レベル向上反復手順」と各ゲートの意味・終了条件を明記した実用手順書へ強化し、手順どおり反復すれば生成 manifest が ALLOCATE entry 検査(PX-181 のパリティ)に到達することを end-to-end で検証する。

### Purpose
AI 実行セッションが「設計 decision 作成 → ゲート反復 → 情報レベル向上 → COMPLETE」を再現可能に遂行し、第二段階(allocate)と grill が高精度で進められる manifest を安定的に生み出す。

### Motivation
現行 .md の Step 3 は decision の書き方を示すが「どう情報レベルを上げるか・いつ完了か」が不明確で、反復が収束しない。

### Constraints
日本語正文。削除済み記述(言語方針・英訳注記など)は復活させない。常にカレントディレクトリ出力。T1/PX-180 のモデル・ゲートと T2/PX-181 の出力・パリティを利用。既存 106+ テスト維持。

## Scope

- MODIFY: .claude/commands/workspacify-tree.md(日本語) — Step 3 を「情報レベル向上の反復手順」へ拡張: ①候補分類→②package 設計(layer/kind/responsibilities/seed_required)→③owner 割当(object/claim/invariant/state/error/test)→④依存と boundary の全網羅(reason_code・forbidden alternative・dev policy)→⑤approval 台帳、を各ゲート(完全性・tree整合・unallocated・boundary解決)の意味と終了条件とともに記載。gaia 台帳を「到達目標の具体例」として明記
- MODIFY: .claude/commands/workspacify-tree.md — Step 4 のゲート結果の読み方(gates/finalAudit の各 count が指す欠陥と修正対象)を表で整理
- MODIFY: .claude/commands/workspacify-tree.md — Step 5 に「出力 manifest が ALLOCATE entry 検査を通ること」を成功条件として追記
- NEW: tests/workspacify-tree/integration/info-level-loop.test.mjs — T1/T2 を利用し、手順どおり decision を段階的に濃化していくと finalize が COMPLETE となり、生成 manifest が PX-181 の checkTreeEntryGate を通過することを段階ごとに検証
- MODIFY: tests/workspacify-tree/unit/command-realize.test.mjs — .md 構造テストを拡張(Step 3 が反復手順・各ゲート説明・gaia 到達目標に言及していること)
- Out of scope: 英語版・製本。
- Out of scope: ALLOCATE 本体の実装と grill の自動実行(将来)。
- Affected: .claude/commands/workspacify-tree.md(手順書)と tests(構造・統合)。
- Affected: 第5層の入り口として、tree の情報レベルが gaia 台帳級へ到達する基準を文書化。

## Investigation

証跡: 現行 .md は Step 3 が decision の書き方のみで「どう情報レベルを上げるか・いつ完了か」が不明。ALLOCATE §7 entry 検査と §10 seed 15見出しが tree に要求する情報を確認。gaia ledger Rev3 が「到達目標の具体例」。grill は conver README のディレクトリ単位実装手順。

## Acceptance Criteria

- Happy path: info-level-loop が「不完全 decision → 段階的濃化 → finalize COMPLETE → checkTreeEntryGate PASS」を機械検証する
- Error case: 不完全 decision のままでは gate が COMPLETE を出さず、その原因を finalAudit の count から特定できることを検証する
- Edge case: .md 構造テストで「削除済み記述が存在しない」「カレントディレクトリ出力・BLOCKED 契約が維持」を保証し、既存 106+ テストが green を維持する

## Invariants

- [Normal condition] .md の手順は PX-180/181 のゲート・出力と一対一で対応する。
- [Error invariant] 情報レベルが不足する限り COMPLETE を出さない手順である。
- [Internal state invariant] 反復は gate 結果に基づいて収束する(無限ループしない)。
- [Boundary invariant] 削除済み記述は復活させない。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — Step 3 手順の具体化

- **Precondition**: the command markdown contains Step 3 and Step 4
- **Postcondition**: Step 3 describes an iteration that raises the information level (package design, ownership for invariants and tests, dependency coverage) and Step 4 explains each completeness gate
- **Invariant**: the markdown references the Gaia ledger as the target information level and never resurrects removed notes

### C002 — ゲート説明と終了条件

- **Precondition**: the completeness gates from PX-180 are implemented
- **Postcondition**: the markdown explains each gate meaning and its exit condition in relation to finalAudit counts
- **Invariant**: the markdown ties gate PASS to zero unresolved unallocated and boundary failures

### C003 — end-to-end 到達

- **Precondition**: a spec and an initially incomplete decisions object are given
- **Postcondition**: iterating the documented workflow raises the manifest to COMPLETE and the generated manifest passes the ALLOCATE entry check
- **Invariant**: the workflow converges to COMPLETE only when the entry check passes

### C004 — 既存制約の維持

- **Precondition**: the command markdown is edited
- **Postcondition**: the output contract (single manifest in the current directory, BLOCKED on hash mismatch) is retained
- **Invariant**: the removed language-policy note stays absent

### C005 — 回帰

- **Precondition**: all prior workspacify-tree features are present
- **Postcondition**: the structural and end-to-end tests pass
- **Invariant**: all previously green tests remain green

## Boy Scout Rule

日本語記述は翻訳可能性(可読性=翻訳可能性)を保ち、将来の英訳に耐える構造にする。手順・ゲート表は定型的で冗長にならないよう整理。

## Test Plan

### Unit Tests

- UT: [Normal] .md — Step 3 が package 設計(responsibilities/seed_required)と owner 割当(invariant/error/test 含む)と依存 boundary 全網羅の手順に言及する
- UT: [Normal] .md — 各ゲート(完全性/tree整合/unallocated/boundary解決)の意味と終了条件を記載する
- UT: [Boundary] .md — gaia 台帳を到達目標の具体例として言及する
- UT: [Error] .md — 削除済み記述(言語方針・英訳注記)が存在しないことを構造検査で保証する
- UT: [Invariant] .md — 成功/失敗表示・カレントディレクトリ出力・BLOCKED 非置換の契約を維持している
- UT: [Normal] loop — 不完全 decision では gate が FAIL/REVIEW_REQUIRED を返す
- UT: [Error] loop — 段階的に decision を濃化すると unallocated/boundary 未解決が減少する
- UT: [Invariant] loop — 完成 decision で finalize が COMPLETE となり生成 manifest が entry 検査(checkTreeEntryGate)を通過する

### Integration Tests

- IT: [Integration point] command .md の手順(parse→extract→decision 濃化→gate→finalize)を T1(モデル/ゲート)+T2(出力/パリティ)上で実行
- IT: [Verification] info-level-loop で「不完全→段階的濃化→COMPLETE→entry 検査 PASS」を一連の run.mjs 呼び出しで検証
- IT: [Prerequisites] PX-180(T1)・PX-181(T2)。既存 106 テスト green
- IT: [Related tickets] PX-180/PX-181 を利用し、第5層の入口としての到達度を定義

### Exceptions

- 「.md の日本語記述がユーザーにとって意味的に完全である」ことを自動正解判定するのはテスト不能(自然言語の意味論判断)。設計欠陥ではない。代替検証: 必須節・必須手順・ゲート説明・到達目標・削除禁止項目の不在を構造的に自動検査し、内容の意味論品質は日本語 human/AI review で担保する。
- 「grill が各ディレクトリの RFC seed に対して正しく実行される」ことを本チケット単独で自動検証するのはテスト不能(grill と allocate は将来実装)。設計欠陥ではない。代替検証: .md 手順が generate する manifest が ALLOCATE entry 検査を通過することを end-to-end で担保し、seed 生成以後は将来チケットで検証する。

### Plan Test Code (concrete code)

- UT: C001 セットアップ(.md 構造)
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
const md = readFileSync('.claude/commands/workspacify-tree.md', 'utf8');
```

- UT: C001 Postcondition→assertion
```js
// Step 3 describes an iteration that raises the information level
assert.match(md, /## Step 3/);
assert.match(md, /responsibilities|seed_required/);
assert.match(md, /owner|所有権/);
assert.match(md, /invariant|不変条件/);
assert.match(md, /boundary|契約境界|contract_boundaries/);
```

- UT: C002 Postcondition→assertion
```js
// Step 4 explains each gate and its exit condition
assert.match(md, /## Step 4/);
assert.match(md, /unallocated|未割当/);
assert.match(md, /tree|ツリー/);
assert.match(md, /COMPLETE/);
```

- UT: C003 end-to-end
```js
import { spawnSync } from 'node:child_process';
import { mkdtempSync, cpSync, readFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { checkTreeEntryGate } from '.claude/scripts/workspacify-tree/lib/entry-parity.mjs';
const dir = mkdtempSync(join(tmpdir(), 'wst-loop-'));
const spec = join(dir, 'gaia-like-spec.md');
const dec = join(dir, 'gaia-decisions.json');
cpSync('tests/workspacify-tree/fixtures/gaia-like-spec.md', spec);
cpSync('tests/workspacify-tree/fixtures/gaia-decisions.json', dec);
const r = spawnSync(process.execPath, ['.claude/scripts/workspacify-tree/run.mjs', 'finalize', `--spec=${spec}`, `--decisions=${dec}`], { cwd: dir, encoding: 'utf8' });
assert.equal(r.status, 0, r.stdout);
const manifest = JSON.parse(readFileSync(join(dir, 'WORKSPACIFY-TREE-MANIFEST.json'), 'utf8'));
assert.equal(checkTreeEntryGate(manifest, spec).ok, true);
```

- UT: C004 Postcondition→assertion
```js
// output contract retained and removed notes stay absent
assert.match(md, /カレントディレクトリ/);
assert.match(md, /BLOCKED|置換/);
assert.ok(!/言語方針/.test(md));
assert.ok(!/English translation/.test(md));
```

- UT: C005 回帰
```js
// all previously green tests remain green is run externally; structural check below
assert.match(md, /## Step 1: parse/);
```


## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| Step 3 decision の書き方のみ | Step 3 に情報レベルを上げる反復手順①-⑤(approvals確定/package設計/owner完全化/boundary網羅/approval台帳) | command doc information-raising workflow (C001) |
| Step 4 ゲート説明不足 | finalAudit count→修正対象の対応表(unallocated/tree/responsibilities/boundary)を記載 | gate explanation & exit conditions (C002) |
| Step 5 到達条件不明 | ALLOCATE entry 検査通過 + Gaia 台帳を到達目標例として明記 | entry-check success condition (C003) |
| (absent) | tests/integration/info-level-loop.test.mjs | incomplete gate FAIL -> complete finalize COMPLETE -> entry parity PASS を検証 |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) .md の Step 3/4/5 拡張 → (2) 構造テスト拡張 → (3) info-level-loop 統合テスト → (4) 全回帰。
- [Risks] .md の長大化。反復手順はゲート駆動で収束させ、AI 裁量の無限ループを防ぐ。
- [Caveats] gaia 台帳は「例」であって対象 spec を gaia に固定しない(汎用性維持)。
- [Open items] allocate/grill との結合は将来。
- [Future improvements] 英訳・製本はユーザー指示後に別対応。
Implementation summary:
- Rewrote Step 3-5 of the Japanese command doc to drive the information level to gaia-ledger grade via gate-driven iteration.
- Test results: 121/121 PASS (116 prior + 5 new).
- Quality: totalIssues 0. No stubs/crimes/[::AMBIGUOUS::].
- Removed notes (language-policy, English translation) remain absent.
Review report:
- Static quality check: passed
- Translatability: no issues
- Contract coverage: @verifies verified (verify-red-coverage)
- Entry-gate parity: gaia-like manifest passes checkTreeEntryGate (ALLOCATE §7)
- No open crimes, no stubs, no [::AMBIGUOUS::] markers

## PX-182 — 0 locations, not implemented

Not found
