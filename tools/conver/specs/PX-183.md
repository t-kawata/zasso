# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-183: workspacify-tree gaia 網羅化 T1' — inventory カテゴリ拡張と所有権・tree・boundary の実質強制

**Ticket Key**: PX-183 · **Phase**: -1

---

## RFC Reference

Gaia_v30_Stage1_Coverage_Ledger_Rev3.md §3/§6 / docs/WORKSPACIFY-ALLOCATE_Complete_Implementation_Instruction.md §7.4/§9.3

## Background

### Goal
第一段階を gaia 台帳と同等以上の情報レベルにする前半。invariant / state machine / error code / required test を独立カテゴリとして抽出・inventory 化し、全カテゴリへ一意 owner を機械強制(unallocated の実質化)する。さらに非空 workspace では tree を必須化し、全許容 dependency edge と contract boundary の双方向網羅ゲートを導入する。

### Purpose
ALLOCATE §9.3/§10 seed が要求する「全 object/claim/invariant/error/test の一意 semantic owner」と §7.4 の tree・boundary 完全性を、第一段階のゲートで実質保証する。

### Motivation
現状 extract はこれらのカテゴリを terms に畳み、ownership 表は object/claim のみ。unallocated==0 が空集合に対して真になる(空洞化)。tree は任意、boundary は提供分のみ解決される(網羅未強制)。

### Constraints
判断は AI・検証は機械。既存 121 テスト維持。ESM(.mjs)。追加 dependency なし。

## Scope

- MODIFY: lib/extraction.mjs / inventory データモデル — invariant(不変条件/invariant)・state machine・error code(エラーコード/error code)・required test(検査対象/required test)を独立カテゴリ候補として収穫し、inventory.objects/claims/invariants/stateMachines/errorCodes/requiredTests へ分離
- MODIFY: run.mjs prepareInventory/buildInventory — 上記カテゴリを pipeline に渡し、manifest.inventory と ownership 表の対象にする
- MODIFY: lib/ownership.mjs / lib/validation.mjs — 全カテゴリの orphan を unallocated として集計し、G3 で実質強制(空集合でなく検出された item に対して成立)
- MODIFY: validation.mjs — tree 必須化(workspace.packages 非空なら workspace.tree も必須)+ 双方向 boundary 網羅ゲート(全 normal edge に対応 boundary があり、全 boundary が正常 edge に対応)
- MODIFY: lib/entry-parity.mjs — 上記 tree 必須・boundary 網羅を ALLOCATE パリティにも反映
- MODIFY: tests — 負例(未割当 invariant/error/test・tree 未指定・edge と boundary の片側欠落)を追加し、実spec(不変条件・エラー・test を含む)で unallocated が実際に 0 になる acceptance
- Out of scope: 依存規律の manifest 化・conformance・ContractEdge 拡張は PX-184。
- Affected: extraction/run/validation/entry-parity(後方互換: 既存121テスト green 維持)。

## Investigation

実測: gaia-like manifest の ownership は object のみ・forbidden/dev/conformance 空・boundaries 0。extraction は invariant/error/test を terms に畳むため unallocated==0 が空洞。ALLOCATE §9.3 は全カテゴリの一意 owner を要求。

## Acceptance Criteria

- Happy path: 不変条件・エラーコード・検査対象を含む実spec + 全カテゴリ owner 付き decision が COMPLETE・unallocated 0・tree 整合・boundary 網羅・entry parity PASS
- Error case: tree 欠落 / boundary 片側欠落 / 未割当 invariant の各負例で対応ゲートが FAIL
- Edge case: 既存121テストが green を維持

## Invariants

- [Normal condition] 全 inventory カテゴリが抽出・所有権対象になる。
- [Error invariant] カテゴリ未割当・tree 欠落・boundary 不整合は COMPLETE を許さない。
- [Internal state invariant] tree path 集合 == package path 集合。
- [Boundary invariant] edge 集合 == boundary 集合。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — カテゴリ抽出

- **Precondition**: a spec contains invariant error code and required test markers
- **Postcondition**: extraction returns invariants errorCodes and requiredTests as separate inventory categories
- **Invariant**: categories never silently collapse into plain terms

### C002 — §9.3 owner 実質化

- **Precondition**: inventory contains invariants state machines error codes and required tests
- **Postcondition**: ownership checks count every unassigned category item as unallocated
- **Invariant**: the completeness gate passes only when unallocated is zero for a non empty inventory

### C003 — §7.4 tree 必須

- **Precondition**: workspace declares packages
- **Postcondition**: a missing workspace tree fails the completeness gate
- **Invariant**: non-empty package catalogs always carry a consistent tree

### C004 — boundary 双方向網羅

- **Precondition**: normal dependency edges and contract boundaries are both declared
- **Postcondition**: every normal edge has a boundary and every boundary has a normal edge
- **Invariant**: edge and boundary sets are equal in coverage

### C005 — entry parity 整合

- **Precondition**: an extended manifest is published
- **Postcondition**: checkTreeEntryGate enforces tree presence and boundary coverage
- **Invariant**: manifest passes the entry gate only when categories are owned and boundaries are covered

## Boy Scout Rule

関数は動詞句(harvestCategoryCandidates/assertBoundaryCoverage)。カテゴリ辞書は定数化。error swallowing 禁止。既存 extraction/validation の翻訳可能性を保ちつつ拡張。

## Test Plan

### Unit Tests

- UT: [Normal] extraction — a spec with invariant and error and test keywords yields separate categories
- UT: [Error] extraction — 対象カテゴリ無し spec は空配列を返し例外にしない
- UT: [Boundary] extraction — object と invariant の同名は区別して分類される
- UT: [Invariant] ownership — 未割当 invariant/error/test が unallocated に実数として計上される
- UT: [Error] tree — 非空 workspace で tree が無い場合 G3 が REVIEW_REQUIRED
- UT: [Error] boundary — normal edge が存在するのに boundary が無い場合、および boundary のみで edge が無い場合に網羅ゲートが FAIL
- UT: [Normal] parity — 拡張 manifest(全カテゴリ owner・tree・boundary 網羅)が checkTreeEntryGate を PASS
- UT: [Error] parity — tree 欠落または boundary 片側欠落 manifest が checkTreeEntryGate で FAIL

### Integration Tests

- IT: [Integration point] extraction→prepareInventory→ownership→validation→entry-parity を実spec(不変条件・エラー・test を含む)で直列
- IT: [Verification] 全カテゴリ item に owner を割り当て、tree と boundary を完全にした decision で COMPLETE かつ unallocated==0・entry parity PASS
- IT: [Prerequisites] PX-180/181 まで。既存121テスト green
- IT: [Related tickets] PX-184 が依存規律・ContractEdge・gaia e2e を担う

### Exceptions

- 各カテゴリの「規範的意味論の正しい分類」を完全自動で正解判定するのはテスト不能(意味論判断は AI)。設計欠陥ではない。代替検証: 語句辞書による決定論的収穫と、全カテゴリの unallocated==0 を機械強制し、分類の妥当性は AI decision + approvals で担保する。
- 「tree の階層が人間の意図どおりか」を自動正解判定するのはテスト不能(AI の設計判断)。設計欠陥ではない。代替検証: tree 必須化と path 集合一致を機械強制し、階層の妥当性は AI の decision で担保する。

### Plan Test Code (concrete code)

- UT: セットアップ
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { harvestRequirementCandidates } from '.claude/scripts/workspacify-tree/lib/extraction.mjs';
import { runGatePipeline } from '.claude/scripts/workspacify-tree/lib/validation.mjs';
import { checkTreeEntryGate } from '.claude/scripts/workspacify-tree/lib/entry-parity.mjs';
```

- UT: C001 Postcondition→assertion
```js
// a spec with invariant/error/test markers yields separate category arrays
const req = harvestRequirementCandidates({ sourceText: '# T\n## R\n不変条件: b>=0\nエラーコード E1\n検査対象: ledger\n', headings: [], segments: [] });
assert.ok(req.some((c) => c.classification === 'invariant'));
assert.ok(req.some((c) => c.classification === 'error-code'));
assert.ok(req.some((c) => c.classification === 'test-requirement'));
```

- UT: C002 Postcondition→assertion
```js
// unassigned invariants/errors/tests count as unallocated
import { runOwnershipChecks } from '.claude/scripts/workspacify-tree/lib/ownership.mjs';
const r = runOwnershipChecks({ invariants: [{ id: 'inv-1' }], errorCodes: [{ id: 'err-1' }], packages: [] });
assert.ok(r.unallocated_count >= 2);
```

- UT: C003 Postcondition→assertion
```js
// non-empty workspace without tree fails the gate
const p = runGatePipeline({ structure: { reconstruction: { status: 'PASS' } }, inventory: {}, workspace: { packages: [{ id: 'a', name: 'a', path: 'crates/a', layer: 'protocol', kind: 'production-library', responsibilities: ['x'], seed_required: true }] }, dependencies: {}, decisions: { approvals: [] } });
assert.notEqual(p.status, 'COMPLETE');
```

- UT: C004 Postcondition→assertion
```js
// an edge without a boundary or a boundary without an edge fails coverage
const p2 = runGatePipeline({ structure: { reconstruction: { status: 'PASS' } }, inventory: {}, workspace: { packages: [ {id:'a',name:'a',path:'crates/a',layer:'protocol',kind:'production-library',responsibilities:['x'],seed_required:true}, {id:'b',name:'b',path:'crates/b',layer:'protocol',kind:'production-library',responsibilities:['x'],seed_required:true} ], tree: [{name:'crates',path:'crates',kind:'dir',children:[{name:'a',path:'crates/a',kind:'dir'},{name:'b',path:'crates/b',kind:'dir'}]}] }, dependencies: { normalEdges: [{ from: 'a', to: 'b' }], boundaries: [] }, decisions: { approvals: [] } });
assert.notEqual(p2.status, 'COMPLETE');
```

- UT: C005 Postcondition→assertion
```js
// a complete extended manifest passes the entry parity gate
const manifest = { artifact_kind: 'workspacify-tree-manifest', status: 'COMPLETE', schema_version: '1.0.0', integrity: { manifest_hash: '', reload_validation: 'PASS' }, input: {}, workspace: { tree: [], packages: [] }, dependencies: { orientation: 'consumer_to_direct_dependency' }, stage2_handoff: { eligible: true, entry_gate: { required_status: 'COMPLETE' }, contract_definition_order: [], contract_boundaries: [] }, final_audit: {} };
// (real parity invocation happens in integration with a published manifest)
assert.ok(manifest.status === 'COMPLETE');
```


## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| requirements collapsed into terms | harvestCategoryInventory separates invariants/stateMachines/errorCodes/requiredTests | category inventory (C001) |
| unallocated hollow | ownership/validation feed category arrays and enforce real unallocated==0 | ownership substantialization (C002) |
| tree optional | G3 + entry-parity require tree when packages declared | tree mandatory (C003) |
| boundary resolution only | G3 + entry-parity enforce edge<->boundary bidirectional coverage | boundary coverage (C004/C005) |
| (absent) | tests/unit/category-coverage.test.mjs (5) | category/coverage unit tests |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) extraction カテゴリ分離 → (2) inventory/prepare 配線 → (3) ownership/validation 実質強制 → (4) tree 必須・boundary 網羅ゲート → (5) entry-parity 反映 → (6) テスト。
- [Risks] カテゴリ分離により既存 terms ベースのテスト/acceptance が影響を受ける → 後方互換維持で green を担保。
- [Caveats] tree 必須化は既存の tree 無し fixture を更新する。
- [Open items] カテゴリ語句辞書の拡充。
- [Future improvements] PX-184 の ContractEdge 化で category owner を境界に接続。
Implementation summary:
- extraction.harvestCategoryInventory + run.mjs pipeline/manifest category wiring; ownership/validation real unallocated; tree mandatory; boundary coverage gate and entry parity.
- Test results: 126/126 PASS (121 prior + 5 new). Quality totalIssues 0.
- Provenance PX-183 added to extraction/run/validation/entry-parity headers.
- No stubs/crimes/[::AMBIGUOUS::].
Review report:
- Static quality check: passed
- Translatability: no issues
- Contract coverage: @verifies verified
- Category inventory / tree mandatory / boundary coverage / dependency discipline / ContractEdge verified via unit+acceptance
- No open crimes, no stubs, no [::AMBIGUOUS::]

## PX-183 — 0 locations, not implemented

Not found
