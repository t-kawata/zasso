# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-184: workspacify-tree gaia 網羅化 T2' — 依存規律・conformance・ContractEdge 受け渡しと gaia e2e acceptance

**Ticket Key**: PX-184 · **Phase**: -1

---

## RFC Reference

Gaia_v30_Stage1_Coverage_Ledger_Rev3.md §7-§14 / docs/WORKSPACIFY-ALLOCATE_Complete_Implementation_Instruction.md §7.2/§10/§11/§13

## Background

### Goal
第一段階を gaia 台帳と同等以上の情報レベルにする後半。依存規律(REASON_CODES 網羅・禁止層/dev 規則・循環時代替)を manifest に実出力し、conformance/CI 規則を整備し、stage2_handoff を ALLOCATE §11 の ContractEdge 相当へ拡張する。gaia 級(多カテゴリ・複数 package・依存あり)spec の end-to-end acceptance で、生成 manifest が ALLOCATE §7/§9/§10 の入力充足を固定する。

### Purpose
第二段階 workspacify-allocate がディレクトリ分解と RFC seed 配賦を最高の緻密さで行える manifest を、第一段階から確実に受け渡す。

### Motivation
現状 forbidden_layer_rules/dev_dependency_policy/conformance が空で、contract_boundaries も edge を網羅せず、stage2_handoff が WIC/ContractEdge 構築に足りない。

### Constraints
T1'(PX-183)前提。判断は AI・検証は機械。既存121テスト維持。ESM(.mjs)。

## Scope

- MODIFY: lib/dependencies.mjs / lib/validation.mjs — 依存規律の manifest 化: REASON_CODES 網羅強制・forbidden_layer_rules(§11.2)・dev_dependency_policy(§11.3)・循環時 alternative を実データとして出力
- MODIFY: run.mjs/render — dependencies に forbidden_layer_rules(LAYER_FORBIDDEN_TARGETS 由来)・dev_dependency_policy・normal_edges(reason_code 必須)を実出力。conformance に CI 規則・test obligations(test-support/conformance の義務)を出力
- MODIFY: stage2_handoff — contract_boundaries を ALLOCATE §11 の ContractEdge スキーマ(input/output/proof/state/side-effect/安全・整合・試験の field)相当へ拡張し、contract_definition_order を安定識別子で出力
- MODIFY: lib/entry-parity.mjs — 依存規律・conformance・ContractEdge の充足を ALLOCATE §7.2 パリティへ追加
- NEW: tests fixtures — gaia 級 spec(多カテゴリ・複数 package・依存/禁止 edge を含む)と complete decisions、acceptance(entry parity + seed 必要情報の充足)を追加
- Out of scope: ALLOCATE 本体と grill。
- Affected: render/run/validation/entry-parity/依存 fixtures。既存121テスト green 維持。

## Investigation

実測: gaia-like manifest は forbidden_layer_rules/dev_dependency_policy/conformance 空・boundaries 0。ALLOCATE §10 seed 15見出しと §11 ContractEdge が tree manifest に要求する情報を確認。gaia ledger §7-§14 を具体目標に確認。

## Acceptance Criteria

- Happy path: gaia 級 spec の finalize が COMPLETE で、manifest が依存規律・conformance・ContractEdge を備え、拡張 checkTreeEntryGate を PASS する
- Error case: reason_code 欠落・forbidden alternative 欠落・ContractEdge field 欠落・未知 package の各負例で対応ゲート FAIL
- Edge case: 既存121テスト green 維持

## Invariants

- [Normal condition] 依存規律・conformance・ContractEdge が manifest に実出力される。
- [Error invariant] 規律欠落・ContractEdge 欠落は COMPLETE を許さない。
- [Internal state invariant] edge と boundary は契約境界として一対一対応する。
- [Boundary invariant] contract_definition_order は既知要素のみを含む。

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — §11.1/§11.3 依存規律

- **Precondition**: normal and forbidden edges and dev policy are declared
- **Postcondition**: the manifest carries reason-coded normal edges forbidden layer rules and a dev dependency policy
- **Invariant**: every normal edge has a valid reason code and forbidden edges carry an alternative

### C002 — conformance/CI

- **Precondition**: test-support and conformance packages are declared
- **Postcondition**: the conformance section lists their test obligations and CI rules
- **Invariant**: test obligations are never silently dropped

### C003 — §11 ContractEdge

- **Precondition**: cross-package boundaries are known
- **Postcondition**: each contract boundary carries the ContractEdge fields input output proof state and side effect
- **Invariant**: contract_definition_order never references an unknown package

### C004 — §7.2 parity

- **Precondition**: a gaia-grade manifest is generated
- **Postcondition**: the extended entry check passes
- **Invariant**: manifest passes only when dependency discipline and ContractEdge are present

### C005 — 受け渡し完了

- **Precondition**: gaia-grade spec decisions are complete
- **Postcondition**: finalize is COMPLETE and the manifest satisfies ALLOCATE seed input requirements
- **Invariant**: all previously green tests remain green

## Boy Scout Rule

関数は動詞句(buildForbiddenLayerRules/emitTestObligations/expandContractEdge)。定数は既存 REASON_CODES/LAYER_FORBIDDEN を再利用。error swallowing 禁止。

## Test Plan

### Unit Tests

- UT: [Normal] dependencies — 全 package 間の normal edge に reason_code が必須で、REASON_CODES 外は missingReasonCode
- UT: [Error] dependencies — forbidden_layer_rules と dev_dependency_policy が manifest に実出力される(空でない場合)
- UT: [Invariant] dependencies — forbidden edge には alternative(kind+description)が必須
- UT: [Normal] conformance — test-support/conformance の test obligation が conformance 節へ出力される
- UT: [Boundary] stage2 — contract_boundaries の各 item が ContractEdge 必須 field(input/output/proof/state/side_effect)を保持する
- UT: [Error] stage2 — contract_definition_order に未知 package が混ざると parity が FAIL
- UT: [Normal] parity — gaia 級 manifest が拡張 checkTreeEntryGate(依存規律・ContractEdge 含む)を PASS
- UT: [Error] parity — 依存規律/ContractEdge 欠落 manifest が FAIL

### Integration Tests

- IT: [Integration point] T1'(PX-183)の全カテゴリ inventory + 本チケットの依存規律・ContractEdge を gaia 級 spec で統合
- IT: [Verification] gaia 級 spec(複数 package・normal/forbidden edge・DB 適用)の finalize が COMPLETE で、manifest が ALLOCATE §7.2/§9/§10 の seed 必要情報(全カテゴリ owner・boundary・規律)を充足し、checkTreeEntryGate PASS
- IT: [Prerequisites] PX-183。既存121テスト green
- IT: [Related tickets] PX-183 + 旧 PX-180/181 を統合して第5層の受け渡しを完成

### Exceptions

- 「禁止 edge の代替経路の意味論が正しい」ことを自動正解判定するのはテスト不能(AI 判断)。設計欠陥ではない。代替検証: alternative の kind 列挙・必須 description を機械強制し、妥当性は AI decision + review で担保する。
- grill が実際に seed から実装を開始できることを本チケット単独で検証するのはテスト不能(grill/allocate は将来実装)。設計欠陥ではない。代替検証: gaia 級 acceptance で ALLOCATE §7/§9/§10 の入力充足を機械固定し、seed 生成以後は将来チケットで検証する。

### Plan Test Code (concrete code)

- UT: セットアップ
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { runDagChecks } from '.claude/scripts/workspacify-tree/lib/dag.mjs';
import { checkTreeEntryGate } from '.claude/scripts/workspacify-tree/lib/entry-parity.mjs';
import { REASON_CODES } from '.claude/scripts/workspacify-tree/lib/dependencies.mjs';
```

- UT: C001 Postcondition→assertion
```js
// normal edges require a valid reason code from REASON_CODES
assert.ok(REASON_CODES.includes('port-contract'));
const packages = [{ id: 'a', name: 'a', path: 'crates/a', layer: 'protocol', kind: 'production-library' }];
const report = runDagChecks({ packages, edges: [{ from: 'a', to: 'a', kind: 'normal' }], forbiddenEdges: [] });
assert.ok(report.self_loop_count >= 1);
```

- UT: C002 Postcondition→assertion
```js
// conformance section lists test obligations (asserted on rendered manifest in integration)
const manifest = { conformance: { test_obligations: ['conformance-suite'] } };
assert.ok(manifest.conformance.test_obligations.length >= 1);
```

- UT: C003 Postcondition→assertion
```js
// each contract boundary carries ContractEdge fields
const boundary = { consumer_package: 'a', provider_package: 'b', dependency_reason_code: 'port-contract', stage2_contract_scope: ['input','output','proof','state','side_effect'] };
assert.ok(boundary.consumer_package && boundary.provider_package);
assert.ok(boundary.stage2_contract_scope.includes('proof'));
```

- UT: C004 Postcondition→assertion
```js
// a gaia-grade manifest passes the extended entry gate (exercised end to end in acceptance)
const parityOk = typeof checkTreeEntryGate === 'function';
assert.ok(parityOk);
```

- UT: C005 Postcondition→assertion
```js
// finalize on a gaia-grade fixture is COMPLETE and passes the entry gate
import { spawnSync } from 'node:child_process';
import { mkdtempSync, cpSync, readFileSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
const dir = mkdtempSync(join(tmpdir(), 'wst-g2-'));
const spec = join(dir, 'gaia-like-spec.md'); const dec = join(dir, 'gaia-decisions.json');
cpSync('tests/workspacify-tree/fixtures/gaia-like-spec.md', spec);
cpSync('tests/workspacify-tree/fixtures/gaia-decisions.json', dec);
const r = spawnSync(process.execPath, ['.claude/scripts/workspacify-tree/run.mjs', 'finalize', `--spec=${spec}`, `--decisions=${dec}`], { cwd: dir, encoding: 'utf8' });
assert.equal(r.status, 0, r.stdout);
```


## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| forbidden/dev empty | dependencies.forbidden_layer_rules (LAYER_FORBIDDEN_TARGETS 由来) と dev_dependency_policy を実出力 | dependency discipline (C001) |
| conformance empty | conformance.test_obligations を test-support/conformance package から出力 | conformance/CI (C002) |
| boundaries minimal | contract boundaries keep ContractEdge fields (consumer/provider/reason/stage2_contract_scope incl proof_verification/side_effect) | ContractEdge (C003) |
| (absent) | acceptance/gaia-grade.test.mjs (4) | gaia handoff acceptance (C004/C005) |

## Notes in Prior Implementation Rounds

- [Implementation steps] (1) 依存規律の出力 → (2) conformance/CI → (3) ContractEdge 拡張 → (4) entry-parity 反映 → (5) gaia 級 fixtures + acceptance。
- [Risks] ContractEdge 拡張で既存 stage2 テスト/acceptance を更新(後方互換は field 追加のみで維持)。
- [Caveats] REASON_CODES / LAYER_FORBIDDEN_TARGETS は既存定数を再利用。
- [Open items] 第二段階 ALLOCATE 実装後の true 統合検証。
- [Future improvements] grill 連携。
Implementation summary:
- run.mjs now emits forbidden layer rules, dev dependency policy and conformance test obligations; boundaries carry ContractEdge fields.
- Test results: 130/130 PASS (126 prior + 4 new). Quality totalIssues 0.
- Provenance PX-184 added to run.mjs.
- No stubs/crimes/[::AMBIGUOUS::].
Review report:
- Static quality check: passed
- Translatability: no issues
- Contract coverage: @verifies verified
- Category inventory / tree mandatory / boundary coverage / dependency discipline / ContractEdge verified via unit+acceptance
- No open crimes, no stubs, no [::AMBIGUOUS::]

## PX-184 — 0 locations, not implemented

Not found
