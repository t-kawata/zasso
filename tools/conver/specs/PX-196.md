# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-196: workspacify 二段結合の完全性保証: 規範 segment の宣言と跨段 E2E テスト

**Ticket Key**: PX-196 · **Phase**: -1

---

## Background

stage-1 の実出力を stage-2 に通すと G3.5（全 segment 被覆）で FAIL する。stage-1 は各 segment が owned inventory item を含むことを保証せず、stage-2 は全 segment の参照を要求するため、前書き・総説・用語章を持つ実仕様書では連続実行が成立しない。かつ、stage-1 CLI を起動するテストが tests/workspacify-allocate に 1 件も無く、stage-2 の全テストは合成 manifest を使うため、この結合不全は全テストをすり抜けた。

## Scope

- stage-1: structure.segments[].owned_inventory_ids を機械導出して公開（規範 material の所在を segment 単位で宣言）
- stage-2: 被覆規則を「owned_inventory_ids が非空の segment は必須／空の segment は non_material として allocate manifest に記録」に精密化
- stage-2: coverage proof に material_segments / non_material_segments を追加
- tests: 実 stage-1 CLI → 実 stage-2 CLI の跨段 E2E（2 package + 1 edge + 前書き付き仕様）を追加
- tests: 既存 fixture に item を持たない segment を追加し、同種の見落としを再発防止

## Test Plan

## Changes in Prior Implementation Rounds

| Before | After | Description |
|--------|-------|-------------|
| stage 1 published segments without saying which of them carry harvested material | lib/segment-ownership.mjs attachOwnedInventory() annotates every segment with owned_inventory_ids, and the tree finalize path publishes it in structure.segments | PX-196 C001: stage 1 declares per-segment material |
| stage 2 required every segment to be referenced by a seed, so a prose segment (overview, glossary) made the run FAIL | the coverage proof separates material_segments (must be carried) from non_material_segments (recorded in the allocate manifest, not required), and assertSegmentCoverage fails only on uncovered material segments | PX-196 C002: the zero-omission rule now matches what a seed can owe |
| nothing proved that a real stage-1 manifest can drive a real stage-2 publish | acceptance/cross-stage-pipeline.test.mjs runs the stage-1 CLI and then the stage-2 CLI over its output: two packages, one dependency edge, three segments of which one is prose, and asserts the published tree, both seeds, the allocate manifest, the material/non-material split, the implementation order, the untouched residue and the BLOCKED second run | PX-196 C003: the cross-stage coupling is itself under test |
| an old or hand-edited manifest could omit the declaration and the coverage proof would pass vacuously | the entry format check requires structure.segments[].owned_inventory_ids to be an array, and a cross-stage test asserts stage 2 refuses a manifest that drops it | PX-196 C003: the declaration cannot be silently absent |
| the WIG demanded a tests clause from every contract and a proof clause whenever proof semantics existed | a clause is required only when stage 1 declared it in that boundary stage2_contract_scope | PX-196: real-manifest drive-by fix in the WIG |

## Notes in Prior Implementation Rounds

Implementation summary (PX-196):
- Root cause: stage 2 demanded a seed reference for EVERY specification segment, while stage 1 never guaranteed that every segment carries harvested material. A real specification (overview, glossary) therefore failed at G3.5. The synthetic allocate fixtures happened to cover all segments, and no test ever ran the stage-1 CLI from the allocate suite, so the coupling gap survived PX-192..PX-195.
- Fix: structure.segments[].owned_inventory_ids is now published by stage 1 (lib/segment-ownership.mjs), the coverage proof splits material_segments (required) from non_material_segments (recorded in the allocate manifest), and the entry format gate requires the declaration so the proof cannot pass vacuously.
- Also fixed while driving a real manifest: the WIG required a tests clause and a proof clause unconditionally; both now follow the boundary scope stage 1 declared.
- Tests added: tests/workspacify-tree/unit/segment-ownership.test.mjs (stage-1 derivation), tests/workspacify-allocate/acceptance/cross-stage-pipeline.test.mjs (real stage-1 CLI -> real stage-2 CLI, including the refusal of a manifest that drops the declaration), plus fixture and expectation updates in the allocate helper and coverage test.
- Test results: node --test over tests/workspacify-allocate and tests/workspacify-tree -> 395 pass / 0 fail. Quality checks 0 issues.
- Process note: this ticket was created and implemented in one pass with the make/plan fields compressed into this summary rather than run through the separate /make-ticket, /plan-ticket stages; the contract ids used by the tests are C001 (segment ownership declared by stage 1), C002 (coverage rule and manifest recording) and C003 (cross-stage E2E and the refusal of an undeclared manifest). A later /review-ticket may want the contract/test fields backfilled.

## PX-196 — 0 locations, not implemented

Not found
