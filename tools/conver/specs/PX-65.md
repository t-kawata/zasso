# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-65: Fix list-definitions output: Markdown with git diff context

**Ticket Key**: PX-65 · **Phase**: -1

---

## Background

The current list-definitions outputs JSON with {success, definitions, ambiguousLine, ambiguousLineNumber}. This is useless for AI judgment because AI reads natural language, not structured data.

### Goal
Replace JSON output with Markdown containing git diff -U5 context + definitions table.

### Purpose
Give AI the information it actually needs: which lines changed (git diff) and where definitions are (table). AI reads the diff, decides which definition the changed lines belong to, picks the line number, and runs inject-at.

### Motivation
PX-64 shipped with a broken design. The list-definitions output was designed as machine-readable JSON, but the consumer is AI, not a machine. AI needs visual context, not structured fields.

### Constraints
- inject-at mode output must remain JSON (it is consumed programmatically by scripts, not by AI)
- Must reuse existing git diff infrastructure (execFileSync, not a new git wrapper)
- Output goes to stdout; errors to stderr
- Must handle files outside git repos gracefully

## Scope

- [Change] .claude/scripts/tickets/resolve-ambiguous-markers.js — listDefinitions() output format
- [Change] modify — Replace console.log(JSON.stringify(result)) with Markdown output via console.log in listDefinitions mode
- [Change] Markdown output: ## Changed lines (git diff -U5) block showing diff + ## Definitions table with Line/Name/Kind columns
- [Change] git diff -U5 is obtained via execFileSync('git', ['diff', '-U5', '--', filePath]) inside listDefinitions
- [Before] AI sees JSON {definitions: [...], ambiguousLine: ...}. [After] AI sees Markdown with diff context and definitions table.
- [Non-change] inject-at mode: output format unchanged (still JSON), logic unchanged
- [Non-change] listAllDefinitions() pure function unchanged
- [Impact] AI workflow: reads diff output -> picks definition line -> runs inject-at. No more JSON parsing.

## Investigation

- [Evidence from code analysis]

Current listDefinitions() at line 99-126 of resolve-ambiguous-markers.js:
```javascript
function listDefinitions(filePath, ticketKey) {
  const content = fs.readFileSync(filePath, 'utf8');
  const lines = content.split('\n');
  const definitions = listAllDefinitions(lines);
  // Find AMBIGUOUS marker (not needed by AI)
  return { success: true, definitions, ambiguousLine, ambiguousLineNumber };
}
```
Then CLI at line ~200:
```javascript
console.log(JSON.stringify(result));  // ← AI has to parse JSON
```

Current main() output: JSON to stdout. AI has to mentally parse this.

Required change: replace JSON output with:
```
## Changed lines (git diff -U5)

```diff
...git diff output...
```

## Definitions in this file

| Line | Name | Kind |
|------|------|------|
| 5 | process_input | function |
| 14 | validate_id | function |
```

## Acceptance Criteria

- [Happy path] listDefinitions in git repo outputs Markdown beginning with ## Changed lines (git diff -U5) and containing ## Definitions table
- [Error case] listDefinitions outside git repo outputs Markdown with definitions table and note: *git diff unavailable*
- [Edge case] Empty file outputs Markdown with empty definitions table

## Invariants

- [Normal condition] listDefinitions always outputs Markdown with a definitions table
- [Error invariant] On file read error, outputs error message to stderr and exits 1
- [Internal state invariant] inject-at output format (JSON) NEVER changes — only list-definitions changes to Markdown
- [Boundary invariant] File with 0 definitions outputs table with header row only and appropriate message

## Boy Scout Rule

- listDefinitions function gets a clearer Markdown builder helper (buildMarkdownOutput) separated from the data-collection logic
- Output format is self-documenting: AI reads Markdown, not JSON

## Test Plan

### Unit Tests

- UT: [Normal] listDefinitions on file in git repo outputs Markdown starting with ## Changed lines and containing definitions table with correct line numbers
- UT: [Normal] listDefinitions output includes git diff -U5 section showing changed lines with +/- markers
- UT: [Normal] inject-at mode still outputs JSON {success: true, insertedAtLine: N} (unchanged from PX-64)
- UT: [Error] listDefinitions on file not in git repo outputs definitions table with note that git diff is unavailable
- UT: [Error] listDefinitions on non-existent file outputs error to stderr and exits 1
- UT: [Boundary] listDefinitions on empty file outputs definitions table with header only and appropriate message
- UT: [Boundary] listDefinitions on file with single definition outputs table with 1 row
- UT: [Invariant] listDefinitions never outputs JSON object (must always start with # or text)
- UT: [Invariant] inject-at never outputs Markdown (must always be JSON)

### Integration Tests

- IT: Create temp git repo with source file -> listDefinitions shows diff section -> inject-at on chosen line succeeds

### Exceptions

- git diff -U5 requires a git repository. The non-git case is covered by unit test (Error: non-git file). Full diff correctness relies on git being available on the system.

## Notes

- [Implementation steps] 1. Add git diff -U5 execution in listDefinitions mode 2. Replace JSON output with Markdown string construction 3. Update tests 4. Verify inject-at unchanged
- [Risks] Low — output format change only, no logic change
- [Caveats] git diff -U5 may fail for files outside git repo; catch and show note
- [Open items] None
- [Future improvements] None

