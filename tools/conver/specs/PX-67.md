# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-67: annotate-contracts — graphify 後のエッジ契約注釈スクリプト

**Ticket Key**: PX-67 · **Phase**: -1

---

## Background

### Goal
GRAPH.json の全エッジに対して AI が contracts 配列（precondition/postcondition/invariant）を付与するためのスクリプト群を実装する。annotate-contracts.js がエッジ一覧を Markdown 出力し、crud.js の update-edge で contracts を書き戻す。verify-contracts-format.js が graphify 完了後に全エッジの契約フォーマットを検証する。

### Purpose
PX-66 でスキーマを定義した contracts 配列を、実際の GRAPH.json に書き込むための運用スクリプト。AI が各エッジの契約を記述するための情報表示と、crud.js による永続化を提供する。

### Motivation
graphify のStep 2（Edge Assignment）直後にこのスクリプト群を実行することで、エッジ作成と契約注釈を同一フローで完結させる。

### Constraints
- crud.js の既存サブコマンド（create-nodes / create-edges / list-nodes 等）は変更しない
- update-edge のみ contracts フィールドの追加・更新・削除をサポート
- annotate-contracts.js は読み取り専用（GRAPH.json を変更しない）

## Scope

- 【変更】.claude/scripts/rfc-graph/annotate-contracts.js（新規）— GRAPH.json を読み込み全エッジの kind/type/from/to/summary を Markdown 一覧出力。AI が各エッジの契約（precondition/postcondition/invariant）を記述するための情報源。GRAPH.json の変更は行わない（読み取り専用）。出力形式は show-graph-summary-markdown.js と同一の Markdown スタイル。
- 【変更】.claude/scripts/rfc-graph/crud.js — update-edge サブコマンドを追加。エッジ識別は from+to+type の複合キー。contracts 配列の追加・更新・削除をサポート。既存サブコマンド（create-nodes / delete-edges 等）は一切変更しない。update-node と同一の --file 引数パターンを踏襲。
- 【変更】.claude/scripts/rfc-graph/verify-contracts-format.js（新規）— GRAPH.json の全エッジの contracts 配列をスキーマ検証。precondition/postcondition/invariant が非空文字列であることを確認。contracts.id の重複チェック。違反時は 3-line error template（[ERROR] / Cause: / Action:）で報告し exit 1。
- 【非スコープ】annotate-contracts.js の契約内容自動生成（AI の契約記述を支援するが、契約内容自体は AI が判断）。
- 【影響範囲】graphify-rfc.md — PX-71 で annotate-contracts.js + verify-contracts-format.js + verify-graph-contracts.js の呼び出しが Step 2 完了ブロックに追記される。このチケットではスクリプト実装のみ。
- 【影響範囲】Tickets.json の contracts フィールド — PX-68 で merge-contracts-to-tickets.js がこのスクリプト群が生成した contracts を入力として使用。

## Investigation

[Existing crud.js]
- 609行、単一ファイル。サブコマンド: create-nodes, create-edges, list-nodes, get-node, update-node, delete-node, delete-edges
- update-node は --id, --field, --value で単一フィールド更新をサポート
- update-edge は未実装。delete-edges → create-edges の再作成パターンのみ
- 変更点: update-edge サブコマンドを追加。エッジ識別は from+to+type。contracts 配列の追加・更新・削除をサポート

[Existing verify.js]
- GRAPH.json の機械検証スクリプト。3-line error template 使用
- 変更点: なし。verify-contracts-format.js は独立した新規スクリプトとして同形式を採用

## Acceptance Criteria

- **[Happy path]** annotate-contracts.js が全エッジ情報を Markdown で出力。crud.js update-edge で contracts を追加。verify-contracts-format.js が PASS。
- **[Error case]** precondition 空文字の contracts → verify-contracts-format.js が BLOCK。crud.js が不正フォーマットを拒否。
- **[Edge case]** contracts.id 重複 → verify-contracts-format.js が BLOCK。空配列 contracts → crud.js が拒否。

## Invariants

- 【Normal establishment】全エッジが contracts 配列を持ち、各契約の precondition/postcondition/invariant が非空で記述される。contracts.id は C000 形式で全グラフ内で一意。
- 【Invariant on error】crud.js update-edge の contracts 書き込み失敗時も、GRAPH.json の他のフィールドは一切変更されない（atomic write）。
- 【Internal state invariant】全スクリプトは副作用ゼロ。ファイル読み取り＋処理＋ファイル書き戻しのみ。
- 【Boundary invariant】contracts 配列は minItems:1。1エッジに複数契約を含める場合は C001, C002 と連番。

## Boy Scout Rule

- crud.js: update-edge サブコマンドを追加する際、既存の update-node と同一のパターン（--id --field --value）を踏襲。エッジ識別には from+to+type の複合キーを使用し、既存の create-edges との一貫性を保つ。
- annotate-contracts.js（新規）: 出力フォーマットは既存の show-graph-summary-markdown.js と同一の Markdown スタイルを採用。AI が読みやすい形式。
- verify-contracts-format.js（新規）: エラー出力形式は既存の verify.js と同一の 3-line template。

## Test Plan

### Unit Tests

- UT: [Normal] annotate-contracts.js が GRAPH.json を読み込み全エッジの kind/type/from/to/summary を Markdown 出力
- UT: [Normal] crud.js update-edge が contracts 配列を正しく追加・更新・削除できる
- UT: [Normal] verify-contracts-format.js が適正な contracts フォーマットを PASS
- UT: [Error] crud.js update-edge が不正な contracts フォーマットを拒否
- UT: [Error] verify-contracts-format.js が precondition 空文字のエッジを BLOCK
- UT: [Boundary] crud.js update-edge が空配列 contracts を拒否（minItems:1）
- UT: [Boundary] verify-contracts-format.js が contracts.id 重複を BLOCK
- UT: [Invariant] crud.js update-edge の書き込みが atomic（失敗時に元ファイルを変更しない）

### Integration Tests

- IT: [Integration point] annotate-contracts.js → AI が契約記述 → crud.js update-edge で GRAPH.json に書き戻し → verify-contracts-format.js で検証の一連フロー
- IT: [Verification] crud.js create-edges 後に update-edge で contracts を追加し、list-nodes / get-node で一貫性確認
- IT: [Prerequisites] PX-66 完了（edge.schema.json に contracts 必須）
- IT: [Related tickets] PX-66（スキーマ依存）, PX-71（graphify-rfc.md への統合）

### Exceptions

- Exception 1 — Item: annotate-contracts.js の AI 補助情報（全エッジの kind と type からの契約推奨）の完全性テスト。Reason: AI による契約記述の品質はテストで検証不可能。Alternative: 出力フォーマットが正しいこと（JSON としてパース可能、Markdown 構造が崩れていない）のみテストする。

## Notes

依存: PX-66（edge.schema.json 改修済み）。
- [Implementation steps] 1) annotate-contracts.js 新規作成, 2) crud.js に update-edge サブコマンド追加, 3) verify-contracts-format.js 新規作成, 4) 全 UT 実行, 5) 既存グラフで動作確認
- [Risks] crud.js は609行の大規模スクリプト。update-edge 追加時に既存サブコマンドを壊さないよう注意。
- [Caveats] annotate-contracts.js は AI の契約記述を補助するものであり、契約内容の自動生成は行わない。

## PX-67 — 0 locations, not implemented

Not found
