# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-69: Gate L1 + Gate M — グラフ契約検証と make 段階整合性

**Ticket Key**: PX-69 · **Phase**: -1

---

## Background

### Goal
2つのゲートスクリプトを実装する。verify-graph-contracts.js（Gate L1）は graphify 完了直後に全エッジの契約フォーマットを検証。verify-make-contracts.js（Gate M）は make-ticket 完了直後にチケット契約とテスト計画の整合性を検証。

### Purpose
Gate L1: グラフレベルの契約—全エッジが contracts を持ち、そのフォーマットが正しいことを graphify 完了直後に保証する。Gate M: チケットレベルの契約—make-ticket で作成された spec の testUnit/testIntegration がチケット契約に言及していることを保証する。

### Motivation
Gate L1 がなければ不正な contracts フォーマットが下流（PX-68 以降）に流れるリスクがある。Gate M がなければ spec 作成時点で契約とテスト計画の乖離が放置される。

### Constraints
- 両ゲートとも読み取り専用（Tickets.json / GRAPH.json を変更しない）
- BLOCK/PASS の二値判定。WARNING は出力しない。
- 既存の status.json 制御（start-step / end-step / reset-to-step）を利用。ゲート FAIL 時は fail-step 遷移。

## Scope

- 【新規】.claude/scripts/tickets/verify-graph-contracts.js — Gate L1 スクリプト。検証: (1) 全エッジに contracts[] 存在, (2) 全 precondition/postcondition/invariant 非空, (3) contracts.id 重複チェック, (4) type-contract 矛盾（depends_on/constrains なのに contracts 空は矛盾。references/refines は許容）。exit 1 on failure。
- 【新規】.claude/scripts/tickets/verify-make-contracts.js — Gate M スクリプト。検証: (1) 全 precondition が testUnit にキーフレーズ「setup」「前提」で言及, (2) 全 postcondition が testUnit に「assert」「確認」で言及, (3) 全 invariant が testUnit に「不変」で言及, (4) testExceptions 各項目に「テスト不能理由」と「設計欠陥でない理由」の両方のキーフレーズ存在。exit 1 on failure。
- 【変更】graphify-rfc.md（PX-71で実施）— Step 2 完了ブロックに verify-graph-contracts.js 呼び出し追記。
- 【変更】make-ticket.md（PX-71で実施）— Step 6 status=made 直前に verify-make-contracts.js 呼び出し追記。
- 【非スコープ】Gate L1 と verify-contracts-format.js（PX-67）の重複 — format.js はスキーマ検証、L1 は type-contract 矛盾の高次検証。役割が異なるため両方維持。

## Investigation

[Gate L1 検証ロジック設計]
- 検証1: 全エッジに contracts[] がある（スキーマ required 確認）
- 検証2: 全 contract の precondition/postcondition/invariant 非空
- 検証3: contracts.id 重複チェック
- 検証4: type-contract 矛盾（depends_on/constrains → contracts 空は矛盾、references/refines → contracts 空は許容）

[Gate M 検証ロジック設計]
- 検証1: チケット.contracts の全 precondition が testUnit に「setup」または「前提」として言及
- 検証2: チケット.contracts の全 postcondition が testUnit に「assert」または「確認」として言及
- 検証3: チケット.contracts の全 invariant が testUnit に「不変」として言及
- 検証4: testExceptions 各項目に「テスト不能理由」と「設計欠陥でない理由」の両方のキーフレーズ

## Acceptance Criteria

- **[Happy path]** Gate L1 が全エッジ正常で PASS。Gate M が全チケット正常で PASS。
- **[Error case]** precondition空文字 → Gate L1 BLOCK。testUnit未カバー契約 → Gate M BLOCK。
- **[Edge case]** type-contract 矛盾（depends_on なのに空）→ Gate L1 BLOCK。type 不明なエッジ → Gate L1 の検証対象外。testExceptions 説明不足 → Gate M BLOCK。

## Invariants

- 【Normal establishment】Gate L1: 全エッジの contracts フォーマットがスキーマ適合。Gate M: 全テスト計画がチケット契約に言及。
- 【Invariant on error】ゲート FAIL 時も GRAPH.json / Tickets.json は一切変更されない。
- 【Internal state invariant】両スクリプトは副作用ゼロ。ファイル読み取り＋検証＋結果出力のみ。
- 【Boundary invariant】空の contracts（minItems 違反）・空文字 precondition は BLOCK。

## Boy Scout Rule

- verify-graph-contracts.js: 検証ロジックを1検証1関数に分割（検証1〜4で4関数）。テスト容易性確保。エラー出力は不正エッジIDでソート。
- verify-make-contracts.js: testExceptions のキーフレーズ判定は設定可能な定数配列として定義し、将来の拡張に備える。
- 両スクリプトとも既存の verify.js と同一の exit code 規約（0=PASS, 1=FAIL）と出力形式を採用。

## Test Plan

### Unit Tests

- UT: [Normal] verify-graph-contracts が全エッジ正常フォーマットを PASS
- UT: [Normal] verify-make-contracts が正常なテスト計画＋契約を PASS
- UT: [Error] verify-graph-contracts が precondition 空文字エッジを BLOCK（+ エッジID一覧）
- UT: [Error] verify-graph-contracts が contracts.id 重複を BLOCK
- UT: [Error] verify-make-contracts が testUnit 未カバー契約を BLOCK
- UT: [Error] verify-make-contracts が testExceptions の正当性説明不足を BLOCK
- UT: [Boundary] verify-graph-contracts が depends_on なのに契約が空（type と contract の矛盾）を検出
- UT: [Boundary] verify-make-contracts が precondition を UT でカバーしていないケースを検出
- UT: [Invariant] 両スクリプトとも読み取り専用であることを確認

### Integration Tests

- IT: [Integration point] GRAPH.json → verify-graph-contracts（Gate L1）→ PASS→ graphify 継続
- IT: [Integration point] spec 作成後 → verify-make-contracts（Gate M）→ PASS→ status=made
- IT: [Verification] 人為的に不正 contracts を混入した GRAPH.json で Gate L1 が BLOCK することを確認
- IT: [Prerequisites] PX-66（スキーマ）, PX-67（annotate-contracts 完了）, PX-68（merge 完了）
- IT: [Related tickets] PX-71（graphify-rfc.md / make-ticket.md にゲート呼び出し追記）

### Exceptions

- Exception 1 — Item: verify-make-contracts の「testExceptions 正当性説明不足」検出の網羅性。Reason: 「なぜテスト不能か」と「なぜ設計欠陥でないか」の両方があるかの判定は自然言語解析に依存。Alternative: 「because test is impossible」と「not a design defect」のキーフレーズ有無で判定。フレーズがない場合のみ BLOCK。
- Exception 2 — Item: verify-graph-contracts の type-contract 矛盾検出（depends_on なのに契約が空等）。Reason: 矛盾の定義はエッジ type と契約内容の組み合わせに依存。Alternative: 矛盾検出は「contracts が空かつ type が depends_on/constrains/conflicts_with」の最小限に限定。

## Notes

依存: PX-67（GRAPH.json に contracts 必要）, PX-68（チケット.contracts 必要）。
- [Implementation steps] 1) verify-graph-contracts.js 実装, 2) verify-make-contracts.js 実装, 3) 全 UT 作成・実行, 4) 不正データでの BLOCK 確認
- [Risks] verify-make-contracts の testExceptions 正当性判定はキーフレーズ一致に依存。テストデータで偽陽性・偽陰性がないことを確認。
- [Caveats] Gate L1 は verify-contracts-format.js と役割が異なる（format.js = スキーマ検証、L1 = type-contract 矛盾等の高次検証）。

## PX-69 — 0 locations, not implemented

Not found
