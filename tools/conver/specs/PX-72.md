# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-72: Contracts-aware Implementation Order expansion across 6 files

**Ticket Key**: PX-72 · **Phase**: -1

---

## Background

Contracts section has been added to show-ticket-context.js output (Precondition/Postcondition/Invariant from graph edge annotation). However, the Implementation Order (TDD Red-Green-Refactor) in all 6 files still treats the Red phase as covering only the original 7 elements (Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, Invariants). Contracts — as a concrete first-class input carrying Precondition/Postcondition/Invariant — must be explicitly referenced in the Red phase: each Contract Precondition/Postcondition/Invariant must first be translated into testable form (input schemas, output assertions, invariant predicates) before being implemented as concrete test code.

## Scope

- [CLAUDE.md] Insert Contracts-aware paragraph into Supreme Law §1.1 Red phase
- [make-ticket.md] Insert Contracts-aware paragraph + add Contracts expansion substeps + Contract-to-template correspondence rules + clarify verify-make-contracts.js role
- [plan-ticket.md] Insert Contracts-aware paragraph into Step 4 Reference
- [split-to-tickets.md] Insert Contracts-aware paragraph into Reference section
- [start-ticket.md] Insert Contracts-aware paragraph + align with Step 5b Red contract coverage
- [show-ticket-context.js] Insert Contracts-aware paragraph into --for-spec Implementation Order output

## Investigation

All 6 target files were read and analyzed:

1. **CLAUDE.md** (§1.1 Red — Supreme law): Contains the canonical Implementation Order. The Red phase lists 7 elements (Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, Invariants) but has no reference to Contracts.

2. **make-ticket.md** (Step 4 Reference): Contains a verbatim copy of CLAUDE.md Implementation Order. Additionally, Step 5b defines 11 template fields with marker counts but no Contract correspondence rule. Step 6 calls verify-make-contracts.js without explaining its role. The output table in Step 1 now lists Contracts section (added in prior conversation).

3. **plan-ticket.md** (Step 4 Reference): Contains verbatim Implementation Order. No Contracts reference.

4. **split-to-tickets.md** (Reference section): Contains verbatim Implementation Order. Schema defines contracts as required non-empty array. merge-contracts-to-tickets.js auto-populates contracts after Step 5-2.

5. **start-ticket.md** (Step 5 Reference): Contains verbatim Implementation Order. Step 5b already has a Red contract coverage verification step (verify-red-coverage.js). However, the Implementation Order in Step 5 does not mention Contracts as a first-class input.

6. **show-ticket-context.js** (forSpec mode): The --for-spec code path (lines 354-392) outputs Implementation Order as Markdown. This is the same verbatim text. No Contracts reference.

**Common insertion point**: In all 6 files, after the sentence "Coverage of these seven elements is mandatory; partial implementation is not acceptable." and before the bullet "- Tests must cover all observable behaviors..."

**Current Contracts section**: show-ticket-context.js already outputs Contracts section (lines 544-555) when ticket.contracts is non-empty. This PX-72 ticket itself has a C001 contract to demonstrate the pattern.

## Acceptance Criteria

- All 6 files have the Contracts-aware paragraph inserted in the Red phase
- Text is consistent (byte-identical) across files
- Existing content preserved (no deletions, only additive)
- JS file syntax remains valid

## Invariants

- [Normal condition] All 6 files remain syntactically valid after insertion — Markdown files render correctly; JS file passes node --check
- [Error invariant] On insertion failure (e.g., file locked), no partial writes occur; the change is atomic per file
- [Internal state invariant] The document structure (headings, bullet hierarchy, code blocks) is preserved — existing content is never deleted or reordered
- [Boundary invariant] The Contracts paragraph is inserted at exactly the same logical position (after the 7-elements sentence) in all 6 files, preventing drift

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — PX-72 (design decision)

- **Precondition**: Contracts section is already present in show-ticket-context.js output
- **Postcondition**: All 6 files contain the Contracts-aware paragraph in the Red phase; make-ticket.md additionally has Contracts expansion substeps and correspondence rules
- **Invariant**: Contracts paragraph is byte-identical across all 6 files; existing content is preserved

## Boy Scout Rule

This ticket modifies only documentation and configuration files (Markdown descriptions and JavaScript string literals). No code-level translatability issues to address — the change is purely additive (single paragraph insertion) and does not introduce functions, variables, or control flow that could violate translatability. The existing Implementation Order text is preserved verbatim; only addition, no modification.

## Test Plan

### Unit Tests

- UT: grep each of the 6 files for the Contracts paragraph in the Red phase section
- UT: diff the Contracts paragraph across all 6 files — must be byte-identical
- UT: existing Implementation Order content is fully preserved (grep for original sentences still present)
- UT: for JS file — syntax check via node --check

### Exceptions

- Documentation/config change — no runtime test framework. Verification is by file-content assertion (grep/diff/node --check).

## Notes

PX-72 (cross-cutting, not tied to a specific phase). Purely additive change — no existing content modified or deleted. Contracts paragraph must be byte-identical across all 6 files to prevent drift. make-ticket.md needs structural expansion beyond paragraph insertion (expansion substeps, correspondence rules, verify-make-contracts.js clarification).

## PX-72 — 0 locations, not implemented

Not found
