# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-75: Separate plan-level test code into dedicated planTestCode field

**Ticket Key**: PX-75 · **Phase**: -1

---

## Background

### Goal
Make the distinction between spec-level testUnit entries and plan-level test code mechanical (script-enforced), not convention-based (AI discipline).

### Purpose
Currently plan-ticket Phase 1.5 appends concrete test code to testUnit via --append, mixing spec-level prose entries and code-level entries in the same array. There is no way for show-ticket-context.js, Gate P, or downstream tools to programmatically distinguish the two levels. A dedicated planTestCode field solves this: make-ticket writes testUnit, plan-ticket writes planTestCode, and each tool reads the correct source.

### Motivation
During PX-74 review, it was identified that plan-ticket Phase 1.5 appends test code to testUnit, creating a heterogeneous array. The user explicitly rejected a prefix-convention-based approach (AI discipline), requiring a mechanical script-level solution instead.

### Constraints
Must not change existing Tickets.json behavior — tickets without planTestCode must work unchanged. update-ticket.js already supports arbitrary fields via stdin JSON. No schema validation change needed (Tickets.json has no $schema).

## Scope

- Change: .claude/commands/plan-ticket.md — Phase 1.5 (Step 3.5) writes to planTestCode field instead of appending to testUnit
- Change: .claude/scripts/tickets/show-ticket-context.js — Display planTestCode as dedicated '### Plan Test Code (concrete code)' section under '## Test Plan' when field exists
- Change: .claude/scripts/tickets/verify-plan-contracts.js — Gate P reads from planTestCode instead of testUnit for concrete code pattern verification
- Non-change: update-ticket.js — already supports arbitrary fields via stdin JSON (no modification needed)
- Non-change: Tickets.json schema — no schema change needed
- Non-change: make-ticket.md — testUnit behavior unchanged
- Non-change: insert-field-template.js — planTestCode created at plan time, not make time; no template stub needed
- Impact: All tickets going forward will have testUnit (spec-level) + planTestCode (code-level) after plan phase
- Impact: Existing tickets without planTestCode show no extra section in show-ticket-context.js (graceful omission)

## Acceptance Criteria

- Happy path: plan-ticket Phase 1.5 writes to planTestCode -> show-ticket-context displays '### Plan Test Code' section -> Gate P reads planTestCode -> passes
- Error case: planTestCode contains prose-only entries (no code patterns) -> Gate P rejects with clear error
- Edge case: Ticket has no planTestCode field -> show-ticket-context omits section, Gate P handles gracefully

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — show-ticket-context.js -> planTestCode display

- **Precondition**: Ticket object has planTestCode field as a non-empty array with planTestCode entries
- **Postcondition**: show-ticket-context.js outputs a '### Plan Test Code' section listing planTestCode entries with planTestCode rendered
- **Invariant**: show-ticket-context.js output must contain planTestCode entries verbatim with planTestCode section

### C002 — verify-plan-contracts.js -> planTestCode field

- **Precondition**: Ticket object has planTestCode field with planTestCode entries (string array)
- **Postcondition**: verifyPlanContracts reads from planTestCode field, not testUnit; uses planTestCode patterns
- **Invariant**: Gate P behavior for planTestCode must match Gate P behavior for testUnit (same pattern detection logic, different source field planTestCode)

### C003 — Backward compat — no planTestCode

- **Precondition**: Ticket object has no planTestCode field or planTestCode is empty or planTestCode is absent
- **Postcondition**: show-ticket-context.js renders no extra section; Gate P handles absent planTestCode gracefully
- **Invariant**: No regression — backward compat for existing tickets without planTestCode

### C004 — plan-ticket.md Phase 1.5 writes planTestCode

- **Precondition**: plan-ticket Step 3.5 is executed for a ticket with contracts defining planTestCode entries
- **Postcondition**: The phase writes to planTestCode field (not --append to testUnit); planTestCode gets stored
- **Invariant**: testUnit remains spec-level only (make-ticket content unchanged); planTestCode receives plan-level test code

## Test Plan

### Unit Tests

- UT: [Normal] show-ticket-context.js displays '### Plan Test Code' section when planTestCode is non-empty array
- UT: [Normal] show-ticket-context.js omits section when planTestCode is absent or empty (backward compat)
- UT: [Normal] Gate P (verify-plan-contracts.js) reads from planTestCode field and passes with concrete code patterns
- UT: [Normal] Gate P passes when planTestCode is absent and contracts array is empty (backward compat)
- UT: [Normal] plan-ticket.md Step 3.5 command example writes to planTestCode field
- UT: [Error] Gate P rejects when planTestCode contains only prose (no code patterns)
- UT: [Error] Gate P rejects when contracts exist but planTestCode is empty
- UT: [Boundary] Gate P skips verification when planTestCode is absent and contracts are non-empty (graceful)
- IT: [Integration] plan-ticket Phase 1.5 writes planTestCode -> show-ticket-context displays it in separate section
- IT: [Integration] plan-ticket Phase 1.5 writes planTestCode -> Gate P verifies code patterns -> passes

### Integration Tests

- IT: [Integration point] plan-ticket.md Step 3.5 writes to planTestCode
- IT: [Integration point] show-ticket-context.js renders planTestCode section
- IT: [Integration point] verify-plan-contracts.js reads planTestCode
- IT: [Prerequisites] PX-74 (Gate P + Phase 1.5) must be in place (already committed)

### Exceptions

- Cannot test that show-ticket-context.js correctly renders markdown output in end-user view by unit tests alone — verify by inspecting the output format. This is not a design defect; the script is deterministic and the output can be verified by checking the JSON output.

## Notes

Implementation order: (1) Update show-ticket-context.js to render planTestCode section, (2) Update verify-plan-contracts.js to read from planTestCode, (3) Update plan-ticket.md Step 3.5 to write to planTestCode, (4) Run all existing tests + new tests. update-ticket.js needs no changes — it already accepts arbitrary field names from stdin.

## PX-75 — 0 locations, not implemented

Not found
