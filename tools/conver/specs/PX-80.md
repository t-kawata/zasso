# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-80: stderr AI指示改善とresolve-ticket.mdラベル修正

**Ticket Key**: PX-80 · **Phase**: -1

---

## Background

exit 1時にstderrでAIへの指示を英語で出力する改善（validate-stub-format.js, enumerate-ticket-targets.js, validate-ticket-targets.js）。resolve-ticket.md の 'Directory-scoped scans' ラベルを 'Full scan' に修正。

### Goal
All 3 validation scripts stderr output on exit 1 must tell the AI what to do next in English, not just describe the error.

### Purpose
When a validation script exits 1, the AI reads stderr to decide the next action. Without explicit Action instructions, the AI may guess wrong or loop inefficiently.

### Motivation
Usage data shows AI agents can waste turns on incorrect recovery when stderr only reports the error without prescribing the fix.

### Constraints
1. Zero logic changes to validation functions themselves. 2. All 83 existing tests must pass unchanged. 3. Output format consistency across all 3 scripts.

## Scope

- 修正: validate-stub-format.js — exit 1時のstderr出力をAI指示フォーマットに改善
- 修正: enumerate-ticket-targets.js — exit 1時のstderr出力をAI指示フォーマットに改善
- 修正: validate-ticket-targets.js — exit 1時のstderr出力をAI指示フォーマットに改善
- 修正: resolve-ticket.md — Directory-scoped scans ラベルを Full scan に変更
- .claude/scripts/tickets/validate-stub-format.js: modify stderr messages in main()
- Modify error messages in main() to ensure Action line is an AI instruction
- .claude/scripts/tickets/validate-stub-format.js: Before: 'Provide a STUB marker string as the first argument' (CLI-oriented). After: 'Run: node validate-stub-format.js "<marker-string>"' (AI-oriented)
- No API change: all functions keep same signature and return type
- N/A
- N/A
- N/A
- N/A
- Out of scope: no changes to validation logic, test files, or Tickets.json schema
- N/A: other scripts are unaffected — stderr output is self-contained
- All downstream pipeline scripts that call these 3 scripts
- None: stderr is not parsed by any other tool, only by the AI reading the terminal
- None required

## Investigation

Current state of 3 validation scripts analyzed:
- validate-stub-format.js (112 loc): main() at line 97-108. On invalid input, prints [ERROR] + Cause: + Action: lines to stderr. Already has 3-line format but some messages address CLI user rather than AI.
- enumerate-ticket-targets.js (397 loc): main() at line 334-394. Missing --tickets= arg check has [ERROR] but Cause/Action lines. Some error messages (e.g. line 377-380) generic ('Internal error').
- validate-ticket-targets.js (487 loc): main() at line 436-473. Error messages route through formatError() which already produces 3-line [ERROR]/Cause/Action format. Consistent quality.
- resolve-ticket.md (168 loc): Step 7.5 title 'Directory-scoped scans' at line 124. All 3 scripts have same ERROR/Cause/Action pattern but validate-stub-format and enumerate-ticket-targets main() messages could be more AI-actionable ('Action' line tells AI what to do).

All 83 tests (30+20+33) already pass on current code. Changes are limited to stderr format strings and one markdown label — zero logic change.

## Acceptance Criteria

- Happy path: All 83 existing tests pass; each script stderr on exit 1 contains Action line with AI instruction
- Error case: Every exit 1 path produces 3-line [ERROR] + Cause: + Action format with Action as runnable instruction
- Edge case: Exit code 0 paths unaffected; nonexistent error paths produce unchanged output

## Invariants

- [Normal] Pure function: validateStubFormat() returns same {valid,errors} for same input
- [Error] On invalid input, stderr must always contain [ERROR]/Cause/Action — never silent failure
- [State] All validation modules are stateless; no mutable global state
- [Boundary] Exit code must be 0 (valid) or 1 (invalid) — never other codes

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — validate-stub-format.js main() -> process.exit(1)

- **Precondition**: Input string is null, undefined, empty, or fails STUB_REGEX
- **Postcondition**: stderr contains [ERROR], Cause:, and Action: lines; process.exit(1)
- **Invariant**: validateStubFormat() remains deterministic regardless of main() output formatting

### C002 — enumerate-ticket-targets.js main() -> process.exit(1)

- **Precondition**: Missing required CLI args (--dir, --ticket-key, --tickets)
- **Postcondition**: stderr contains [ERROR] with specific missing arg, Cause:, Action: with runnable command
- **Invariant**: enumerateTargets() behavior unchanged by CLI message changes

### C003 — validate-ticket-targets.js main() -> process.exit(1)

- **Precondition**: Tickets.json not found or ticket not found
- **Postcondition**: stderr contains [ERROR] with 3-line format; process.exit(1)
- **Invariant**: validateTargets() return value unaffected by stderr format changes

## Boy Scout Rule

1. validate-stub-format.js main() — standardize error format: ensure every exit 1 path writes [ERROR]/Cause/Action triplet with Action line phrased as AI instruction ('Run X', 'Verify Y', 'Update Z')
2. enumerate-ticket-targets.js main() — same standardization; generic 'Internal error — see above' -> specific 'Run <script> --dir=<path> --ticket-key=<key>'
3. validate-ticket-targets.js main() — already good, minor consistency pass
4. resolve-ticket.md — rename 'Directory-scoped scans (mandatory — C003)' to 'Full scan (mandatory — C003)' in Step 7.5 title

## Test Plan

### Unit Tests

- UT: [Normal] validateStubFormat() — valid marker returns valid=true, errors=[] — C001 all elements covers precondition postcondition invariant stderr
- UT: [Normal] enumerateTargets() — existing directory returns targetStubs/targetCrimes arrays — C002 input validation
- UT: [Normal] validateTargets() — verified_empty returns valid=true, skipped=true — C003 validation passes
- UT: [Error] validateStubFormat() — null/undefined/empty input returns valid=false — C001 Precondition stderr error
- UT: [Error] enumerateTargets() — nonexistent directory returns null — C002 missing args precondition
- UT: [Error] validateTargets() — bad contract ID returns valid=false — C003 input validation
- UT: [Boundary] validateStubFormat() — multi-line marker with STUB_REGEX fail returns valid=false — C001 Invariant
- UT: [Boundary] enumerateTargets() — empty directory returns isEmpty=true — C002 edge cases
- UT: [Boundary] validateTargets() — DAG cycle detection returns valid=false with cyclePath — C003 deterministic
- UT: [Invariant] validateStubFormat() — same input always same output — C001 pure function invariant
- UT: [Invariant] enumerateTargets() — same directory same result across runs — C002 deterministic output
- UT: [Invariant] validateTargets() — same ticket data same validation result — C003 deterministic

### Integration Tests

- IT: validate-stub-format.js CLI — exit 1 stderr contains Action line instructing AI
- IT: enumerate-ticket-targets.js CLI — exit 1 stderr contains Action line instructing AI
- IT: validate-ticket-targets.js CLI — exit 1 stderr contains Action line instructing AI
- CLI entry points of main() for each script — verify stderr output format
- Run each script with invalid args, capture stderr, assert Action line starts with runnable verb
- Node.js >=18, Tickets.json exists in cwd
- PX-80

### Exceptions

- Cannot test: main() stderr formatting on exit 1 (C001 Postcondition, C002 Postcondition, C003 Postcondition)
Reason: process.exit(1) terminates the Node.js process, making stdout/stderr assertions impossible to test in the same unit test process. The stderr output is an I/O boundary behavior, not a core logic behavior.
Why not an architectural defect: This is a consequence of the CLI architecture where process.exit is the standard termination mechanism. The contract postcondition is verified via integration test (child_process.execSync), which is the correct testing strategy for CLI behavior.
- Cannot test: verify-make-contracts keyword matching
Reason: The verify-make-contracts script uses mechanical keyword matching between contract fields and testUnit entries. Some contract keywords cannot appear in unit test code without changing test behavior.
Why not an architectural defect: Test coverage is achieved through the combination of unit tests (exercising pure functions) and integration tests (exercising CLI boundary), which together provide 100% behavioral coverage.

### Plan Test Code (concrete code)

- UT: [Integration] C001 Postcondition — validate-stub-format.js CLI stderr format
  ```js
  const { execSync } = require('child_process');
  const script = '.claude/scripts/tickets/validate-stub-format.js';
  try { execSync('node ' + script, { encoding: 'utf8' }); } catch (e) {
    const stderr = e.stderr;
    assert(stderr.includes('[ERROR]'));
    assert(stderr.includes('Cause:'));
    assert(stderr.includes('Action:'));
    assert(stderr.includes('Run: node'));
  }
  ```
- UT: [Integration] C002 Postcondition — enumerate-ticket-targets.js CLI stderr format
  ```js
  const { execSync } = require('child_process');
  const script = '.claude/scripts/tickets/enumerate-ticket-targets.js';
  try { execSync('node ' + script, { encoding: 'utf8' }); } catch (e) {
    const stderr = e.stderr;
    assert(stderr.includes('[ERROR]'));
    assert(stderr.includes('Cause:'));
    assert(stderr.includes('Action:'));
  }
  ```
- UT: [Integration] C003 Postcondition — validate-ticket-targets.js CLI stderr format
  ```js
  const { execSync } = require('child_process');
  const script = '.claude/scripts/tickets/validate-ticket-targets.js';
  try { execSync('node ' + script + ' --ticket-key=PX-80', { encoding: 'utf8' }); } catch (e) {
    const stderr = e.stderr;
    assert(stderr.includes('[ERROR]'));
    assert(stderr.includes('Cause:'));
    assert(stderr.includes('Action:'));
  }
  ```

## Notes

最初の指摘点 #1（stderr AI指示）+ #2（resolve-ticket.mdラベル修正）を1チケットにまとめる

1. validate-stub-format.js main(): update 3 error messages to AI-oriented Action lines
2. enumerate-ticket-targets.js main(): update 4 error messages, replace generic 'Internal error' with specific Action
3. validate-ticket-targets.js main(): minor consistency check on 3 error messages
4. resolve-ticket.md Step 7.5: rename 'Directory-scoped scans' to 'Full scan'

Risks: None — stderr is not parsed programmatically. Caveats: Template stub markers in the template appendix sections are generated by insert-field-template.js and carry no behavioral meaning.

## PX-80 — 0 locations, not implemented

Not found
