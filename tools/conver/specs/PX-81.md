# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-81: findTicket() DRY共通化とexistingScriptsCalled疑似フラグ除去

**Ticket Key**: PX-81 · **Phase**: -1

---

## Background

enumerate-ticket-targets.js と validate-ticket-targets.js で全く同一のfindTicket()/ticketExists()/ticketIsDone()ロジックが重複している。共通モジュールscripts/lib/find-ticket.jsに抽出する。validateTargets()のexistingScriptsCalledフラグは実際にスクリプトを呼んでいない疑似フラグであるため削除する。

### Goal
Eliminate DRY violation: identical findTicket()/ticketExists()/ticketIsDone() across 2 scripts extracted to shared module.

### Purpose
Duplicate code increases maintenance burden — schema changes to Tickets.json require fixing both copies. Shared module ensures single source of truth.

### Motivation
Spotted in code review — the 2 copies already diverged in error message style. Without extraction, future changes will introduce silent inconsistencies.

### Constraints
1. Zero behavioral change to exported functions. 2. All 53 existing tests (20+33) must pass unchanged. 3. The shared module must be pure CommonJS (require/module.exports).

## Scope

- 新規: .claude/scripts/lib/find-ticket.js — findTicket()/ticketExists()/ticketIsDone()
- 修正: enumerate-ticket-targets.js — import shared module, remove local copies
- 修正: validate-ticket-targets.js — import shared module, remove local copy, remove existingScriptsCalled flag
- 修正: テストファイル — 3 files: enumerate/validate tests import from shared module
- .claude/scripts/lib/find-ticket.js: create new file
- add — new shared module with 3 exported functions
- Pure module: 3 functions (findTicket, ticketExists, ticketIsDone) extracted from both scripts without modification
- No behavior change: functions are exact copies. Before: local copies in 2 scripts. After: one shared copy imported via require()
- N/A — no API surface change
- N/A
- N/A
- Dependency: both scripts now depend on scripts/lib/find-ticket.js
- Out of scope: no changes to validation logic, test structure, or Tickets.json schema
- No structural changes needed — shared module pattern already used in project
- Both scripts require() paths change — verify with test run
- Dependency graph change: both scripts depend on shared module. No behavioral impact
- None required

## Investigation

Current duplication analysis:
- enumerate-ticket-targets.js: findTicket() at lines 40-62, ticketExists() at lines 71-94, ticketIsDone() at lines 103-125 — total ~70 lines duplicated
- validate-ticket-targets.js: findTicket() at lines 311-333 — ~23 lines duplicated (only findTicket exists here)
- The 2 findTicket() implementations are functionally identical (same regex, same loop logic)
- validateTargets() at line 419-423 sets existingScriptsCalled flag but never calls verifyMakeContracts/verifyPlanContracts/verifyRedCoverage — the actual calls happen in command files
- .claude/scripts/lib/ directory already exists (contains shared utilities)

## Acceptance Criteria

- Happy path: All 53 existing tests pass after refactor; findTicket() from shared module produces identical results
- Error case: Shared module handles invalid keys (PX-invalid, P-N) returns null same as original
- Edge case: validateTargets() return value has no existingScriptsCalled field after removal

## Invariants

- [Normal] findTicket() returns same result for same ticket key regardless of which script calls it
- [Error] On Tickets.json parse failure, all 3 functions propagate error to caller
- [State] Shared module is stateless — no mutable global state
- [Boundary] Input: valid ticket key (PX-N or P{N}-{N}) -> found ticket|null; invalid key format -> null

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — enumerate-ticket-targets.js imports shared findTicket()

- **Precondition**: Shared module file exists at .claude/scripts/lib/find-ticket.js
- **Postcondition**: enumerate-ticket-targets.js exports same findTicket/ticketExists/ticketIsDone functions; all 20 tests pass
- **Invariant**: Shared module functions are pure and deterministic

### C002 — validate-ticket-targets.js imports shared findTicket()

- **Precondition**: Shared module file exists
- **Postcondition**: validate-ticket-targets.js exports same findTicket function; existingScriptsCalled removed from return value; all 33 tests pass
- **Invariant**: findTicket() behavior unchanged after extraction

### C003 — Removal of existingScriptsCalled flag

- **Precondition**: validateTargets() currently returns existingScriptsCalled
- **Postcondition**: validateTargets() returns {valid, errors, formattedErrors, checks} without existingScriptsCalled
- **Invariant**: Removal is pure deletion — no behavioral side effects

## Boy Scout Rule

1. Extract 3 functions (findTicket, ticketExists, ticketIsDone) to .claude/scripts/lib/find-ticket.js
2. Update enumerate-ticket-targets.js: require() from shared module, remove local copies
3. Update validate-ticket-targets.js: require findTicket from shared module, remove local copy
4. Remove existingScriptsCalled flag from validateTargets() return value (3 lines)
5. Update tests to reference shared module for findTicket() tests

## Test Plan

### Unit Tests

- UT: [Normal] findTicket() — PX-77 format finds ticket with phaseId=-1, id=77
- UT: [Normal] findTicket() — P3-2 format finds ticket with phaseId=3, id=2
- UT: [Normal] ticketExists() — existing ticket returns true
- UT: [Normal] ticketIsDone() — status=done ticket returns true
- UT: [Error] findTicket() — invalid key format returns null
- UT: [Error] ticketExists() — nonexistent ticket key returns false
- UT: [Error] validateTargets() — existingScriptsCalled flag absent after refactor deletion
- UT: [Boundary] findTicket() — null/undefined ticketsData returns null
- UT: [Boundary] ticketIsDone() — ticket with no status field returns false
- UT: [Invariant] findTicket() — same key always returns same ticket (deterministic)

### Integration Tests

- IT: enumerate-ticket-targets.js existing 20 tests still pass with shared module import
- IT: validate-ticket-targets.js existing 33 tests still pass with shared module import
- Integration point: The shared module replaces local functions — verify both scripts produce identical output for same input
- Run all 53 existing tests after migration
- Both scripts: require() the shared module; function signatures are identical
- Node.js >=18, module exists at scripts/lib/find-ticket.js
- PX-81

### Exceptions

- Cannot test: The refactored shared module has identical logic to original; all behavioral tests already pass via existing test files
Why not an architectural defect: The extraction is purely structural — no new behavior introduced. All existing tests serve as regression coverage.

### Plan Test Code (concrete code)

- UT: [Normal] C001 Postcondition — shared findTicket() returns same result
  ```js
  const findTicket = require('../../lib/find-ticket').findTicket;
  const data = { phases: [{ phaseId: -1, tickets: [{ id: 77, phaseId: -1 }] }] };
  const result = findTicket(data, 'PX-77');
  assert.strictEqual(result.id, 77);
  assert.strictEqual(result.phaseId, -1);
  ```
- UT: [Normal] C001/C002 Invariant — shared module is deterministic
  ```js
  const ft = require('../../lib/find-ticket');
  const data = { phases: [{ phaseId: -1, tickets: [{ id: 77, phaseId: -1 }] }] };
  const r1 = ft.findTicket(data, 'PX-77');
  const r2 = ft.findTicket(data, 'PX-77');
  assert.deepStrictEqual(r1, r2);
  ```
- UT: [Error] C003 Postcondition — existingScriptsCalled removed
  ```js
  const { validateTargets } = require('../validate-ticket-targets');
  const result = validateTargets(someValidData, 'PX-81');
  assert.strictEqual(result.existingScriptsCalled, undefined);
  ```

## Notes

1. Create .claude/scripts/lib/find-ticket.js with findTicket/ticketExists/ticketIsDone
2. Update enumerate-ticket-targets.js: remove local functions, require(../../lib/find-ticket)
3. Update validate-ticket-targets.js: remove local findTicket, require from shared module
4. Remove existingScriptsCalled from validateTargets() return
5. Update tests to import from shared module

Risks: Low — pure structural refactor with regression tests. Caveats: Path resolution differences between run dirs must be tested.

## PX-81 — 0 locations, not implemented

Not found
