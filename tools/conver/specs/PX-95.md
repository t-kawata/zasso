# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-95: パイプライン内STUB挿入の insert-stub.js 強制 + validate-ticket-targets.js に ghost ticket 防止チェック追加

**Ticket Key**: PX-95 · **Phase**: -1

---

## Background

PX-94 で作成した insert-stub.js をパイプラインの全工程に組み込み、AIが直接ソースコードを編集してSTUBを挿入する経路を完全に絶つ。同時に validate-ticket-targets.js に3つのチェックを追加し、既存の犯罪レコードの false_positive 逃げを機械的に防止する。

## Scope

- [File] .claude/commands/plan-ticket.md
- [Action] modify — Step 3 の「If you find a stub without [::STUB::] marker, add the marker」を「insert-stub.js を使用せよ」に修正
- [Detail] AIが直接マーカーを書き込む指示を削除し、insert-stub.js の使用を強制する
- [File] .claude/commands/start-ticket.md
- [Action] modify — Step 6（能動的探索）と Step 7（suppression時のSTUB付与）を insert-stub.js 強制に修正
- [Detail] 同様に直接編集を禁止
- [File] .claude/commands/review-ticket.md
- [Action] modify — Step 4（評価）と Step 7（suppression時）を insert-stub.js 強制に修正
- [File] .claude/commands/resolve-ticket.md
- [Action] modify — Step 2（後続チケット委譲）と Step 4（未解決stub登録）を insert-stub.js 強制に修正
- [Detail] Step 2 の「planned resolution ticket ID」指定箇所も insert-stub.js の --ticket-ref で行うよう修正
- [File] .claude/scripts/tickets/validate-ticket-targets.js
- [Action] modify — 3つのチェック関数を追加
- [Detail] Check 9: ORPHAN_TICKET_REF の false_positive 禁止。deferredTo に実在するチケットが無い限り false_positive 不可。新規チケット作成による先送りも禁止
- [Detail] Check 10: COMPLETED_TICKET_STALE の false_positive 禁止。実装して解決するしかない
- [Detail] Check 11: false_positive.note 内に存在しないチケット参照があればブロック
- [Before→After] Before: AIが任意の文字列を ticketRef として書けていた。After: insert-stub.js 経由でなければSTUB挿入不可。存在しないチケット参照は挿入時にエラー
- [API contract change] validate-ticket-targets.js の validateTargets() 戻り値に checks 配列の index 9,10,11 が追加
- [Data schema change] なし
- [Config/env change] なし
- [Dependency added/removed] なし（validate-ticket-targets.js の依存先に find-ticket.js の ticketExists が含まれるが既存）
- [Non-change scope] insert-stub.js の作成は PX-94 で完了済み。PX-95 は組み込みと検証のみ
- [Non-change scope reason] 依存関係を明確にするため
- [Impact] 4つの skill markdown ファイル + validate-ticket-targets.js の計5ファイル
- [Impact nature] パイプラインの動作を変更する。AIのSTUB挿入方法が insert-stub.js に制限される
- [Impact response] 各skillの指示変更により、AI従来の動作がエラーになる。動作確認は PX-95 の plan→start→review で行う

## Acceptance Criteria

- 全4つのskill markdown（plan/start/review/resolve）でSTUB挿入が insert-stub.js 経由に統一されていること
- validate-ticket-targets.js に3チェック（Check 9/10/11）が追加され、既存の全テストが pass すること
- insert-stub.js を使用せず直接ソースコードを編集する指示が全てのskillから削除されていること

## Test Plan

### Unit Tests

- UT: [Normal] Check 9 — ORPHAN_TICKET_REF かつ status=false_positive かつ deferredTo が実在チケット → pass
- UT: [Normal] Check 9 — ORPHAN_TICKET_REF かつ status=false_positive かつ deferredTo なし → fail
- UT: [Normal] Check 9 — ORPHAN_TICKET_REF かつ status=resolved → pass
- UT: [Normal] Check 10 — COMPLETED_TICKET_STALE かつ status=false_positive → fail
- UT: [Normal] Check 10 — COMPLETED_TICKET_STALE かつ status=resolved → pass
- UT: [Normal] Check 11 — false_positive.note に実在するチケット参照 → pass
- UT: [Normal] Check 11 — false_positive.note に存在しないチケット参照 → fail
- UT: [Normal] Check 11 — status が false_positive 以外 → pass（note未チェック）
- UT: [Edge] Check 9 — ORPHAN_TICKET_REF かつ deferredTo に実在チケット → pass（唯一許容される false_positive）
- UT: [Error] ticketExists() に null/undefined を渡した場合の挙動確認

### Integration Tests

- IT: plan-ticket Step 3 の指示変更後、insert-stub.js なしでSTUB挿入しようとしたAI動作がエラーになることを確認（手動レビュー）
- IT: start-ticket Step 7（suppression時）の insert-stub.js 必須化により、挿入時に存在しないチケット指定がエラーになることを確認（手動レビュー）

### Exceptions

- testExceptions["AIの指示遵守は自動テスト不可能。コードレビューとパイプライン実行ログで検証する"]

## Notes

PX-95 complete: validate-ticket-targets.js Check 9/10/11 added. 4 skill markdowns updated to enforce insert-stub.js. Tests 44/44 pass.

## PX-95 — implemented at 12 locations

### .claude/scripts/tickets/insert-stub.js

- Line 38
```javascript
class InsertStubError extends Error {
```

- Line 40
```javascript
  constructor(message) {
```

- Line 59
```javascript
function insertStub({ file, line, ticketRef, description, ticketsPath }) {
```

- Line 169
```javascript
function parseArgs() {
```

- Line 198
```javascript
function main() {
```

### .claude/scripts/tickets/tests/insert-stub.test.js

- Line 23
```javascript
function assert(condition, message) {
```

- Line 29
```javascript
function assertStrictEqual(actual, expected, message) {
```

- Line 41
```javascript
function assertRejects(fn, expectedMsgSubstring, message) {
```

### .claude/scripts/tickets/validate-ticket-targets.js

- Line 212
```javascript
function checkOrphanTicketRefNotFp(item, ticketsData) {
```

- Line 246
```javascript
function checkCompletedTicketStaleNotFp(item) {
```

- Line 273
```javascript
function checkFalsePositiveNoteIsNotGhostTicket(item, ticketsData) {
```

- Line 406
```javascript
function validateTargets(ticketsData, ticketKey) {
```
