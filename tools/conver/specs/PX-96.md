# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-96: --ticket-ref パラメータの説明改善: 未来方向（resolve-by）の明確化とパラメータ名変更

**Ticket Key**: PX-96 · **Phase**: -1

---

## Background

### Goal
insert-stub.js の --ticket-ref パラメータが、来歴（provenance）ではなく解決責任（resolve-by direction）を示すことが即座に理解できるようにする。誤った方向での使用による targetStubs の誤割り当てを根絶する。

### Purpose
現在の "Existing ticket key in Tickets.json" という説明は方向性が中立であり、開発者が「このチケットの作業中に書かれた」という過去方向と「このチケットが将来解決する」という未来方向を区別できない。この曖昧性は enumerate-ticket-targets.js の classifyStubs() が marker 内の ticketRef を ownTicketKey と比較する設計と衝突し、誤った targetStubs 割り当てを引き起こす。

### Motivation
会話中の分析で、--ticket-ref=P3-1 と書かれた STUB が P3-1 の targetStubs に登録され、P3-1 がスコープ外の解決を強制されるシナリオが確認された。これは実際に発生するとシステムの進行を止める重大な問題である。

### Constraints
- insertStub() 関数の内部シグネチャ（ticketRef パラメータ名）は変更しない（既存の全呼び出し元との互換性維持）
- find-stubs-with-ticket-ref.js など別スクリプトの --ticket-ref とは衝突しない（別のコンテキスト）
- 旧 --ticket-ref CLI 引数の拒否は insert-stub.js の parseArgs() 内でのみ行う（他スクリプトは変更しない）

## Scope

- 1. 4つのcommand .md ファイルの --ticket-ref ヘルプコメントを未来方向に明確化（plan-ticket.md / start-ticket.md / review-ticket.md / resolve-ticket.md）
- 2. insert-stub.js のヘルプコメント（L13-18, L70付近）を未来方向に明確化
- 3. insert-stub.js の --ticket-ref パラメータ名を --resolve-by-ticket に変更（CLI引数名・パース処理・全command .md の参照）
- 3b. 旧 --ticket-ref が指定された場合は明確なエラーメッセージで新しいパラメータ名を案内するフォールバック処理
- 4. insert-stub.js の内部変数名 ticketRef は維持（互換性のため）
- **Scope of changes:**
  - [Path: .claude/commands/plan-ticket.md / start-ticket.md / review-ticket.md / resolve-ticket.md]
  - [Action: modify]
  - [Detail: insert-stub.js のヘルプコメントブロック内の --ticket-ref 説明と prose 中の --ticket-ref= 参照を --resolve-by-ticket= に変更し、説明文に「将来解決する」方向を明示]
  - [Before: `#   --ticket-ref: Existing ticket key in Tickets.json (e.g. P0-1, PX-77)`]
  - [After: `#   --resolve-by-ticket: Ticket key that WILL resolve this stub (e.g. P0-1, PX-77)`]
  - [API change: CLI 引数名の変更]
  - [Schema change: none / Config change: none / Dependency change: none]
- **Out of scope:**
  - [find-stubs-with-ticket-ref.js] — このスクリプトは STUB を ticket-ref でフィルタする別目的のスクリプトであり insert-stub.js の CLI 引数とは無関係
  - [insert-stub.js の内部変数名 ticketRef] — 関数シグネチャは互換性維持のために変更しない
  - [insertStub() 関数の export シグネチャ] — Node.js API としての insertStub({ticketRef}) は維持
- **Affected areas:**
  - [enumerate-ticket-targets.js] — 間接的影響: 新しい引数名で挿入された STUB は今まで通り正しく分類される（内部 marker 形式は変わらないため）
  - [find-all-stubs.js / find-stubs-with-ticket-ref.js] — marker 形式に変更はないため影響なし
  - [insert-stub.test.js] — テストケースで CLI 引数名を変更する必要あり
  - [全 command .md の利用者（AI オペレーター）] — 新しい引数名に慣れる必要あり

## Investigation

【調査結果】
- insert-stub.js L177: CLI パースで `--ticket-ref=` を ticketRef にマッピング。新 `--resolve-by-ticket=` の追加と旧名の拒否が必要
- insert-stub.js L183: エラーメッセージで `--ticket-ref` をハードコード。新名に更新
- insert-stub.js L190: 必須引数エラーメッセージに `--ticket-ref`。新名に更新
- insert-stub.js L4, L16: ヘッダーコメントで `ticket-ref`。新名と方向性説明に更新
- insert-stub.js L69: InsertStubError で `--ticket-ref`。新名に更新
- insert-stub.js L78, L83, L86, L102: 内部エラーメッセージで `ticket-ref`。内部メッセージは関数の外部インターフェースではないが、ユーザーが見る可能性があるため更新
- command 4ファイル: ヘルプコメントブロックと prose 中の `--ticket-ref=` 参照を全て更新
- enumerate-ticket-targets.js: marker 内の `[::STUB::] PX-77: ...` 形式に変更はないため影響なし
- find-stubs-with-ticket-ref.js: --ticket-ref とは無関係（STUB マーカーの ticket ref をフィルタリングする別機能）

## Acceptance Criteria

- Happy path: 全4ファイルのヘルプコメントが未来方向を明示し、insert-stub.js が --resolve-by-ticket で正しく動作する
- Error case: 旧 --ticket-ref 使用時に明確なエラーメッセージが表示される
- Edge case: 旧名と新名の両方が同時に指定された場合、--resolve-by-ticket が優先され --ticket-ref は警告なしで無視される

## Invariants

- 【Normal establishment】insertStub() が受け取る ticketRef は、関数内部で一貫して同一の変数名（ticketRef）で参照される
- 【Invariant on error】旧 --ticket-ref の指定時は常に exit code 2 でエラー終了し、マーカー挿入は行われない
- 【Internal state invariant】全4つのcommand .md ファイルの insert-stub.js パラメータ説明文言が常に一致している
- 【Boundary invariant】新旧両方の引数名が空文字列で指定された場合も、個別のバリデーション（--file is required 等）で捕捉される

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — insert-stub.js CLI → parseArgs()

- **Precondition**: insert-stub.js が --ticket-ref= で呼び出される
- **Postcondition**: exit code 2 でエラーメッセージに --resolve-by-ticket への変更が案内される
- **Invariant**: 全実行パスで新旧両方の引数名チェックが行われる

### C002 — insert-stub.js CLI → insertStub()

- **Precondition**: insert-stub.js が --resolve-by-ticket=PX-77 で呼び出される
- **Postcondition**: insertStub() の ticketRef パラメータに PX-77 が渡され、通常の検証（ticketExists）を通過してマーカーが挿入される
- **Invariant**: --resolve-by-ticket= の値は関数内部で ticketRef として一貫して参照される

### C003 — 全4command .md → insert-stub.js 参照

- **Precondition**: plan-ticket.md / start-ticket.md / review-ticket.md / resolve-ticket.md の insert-stub.js 使用例が読まれる
- **Postcondition**: パラメータ説明が「将来このSTUBを解決するチケット」であることを明示している
- **Invariant**: 全4ファイルのパラメータ説明文言が一致している

## Boy Scout Rule

改善1は本質的に Boy Scout ルールの適用そのものである（コメントを明確にする）。

1. insert-stub.js の parseArgs() 引数名変更に伴い、`insert-stub.test.js` のテストケースで CLI 引数名を `--resolve-by-ticket` に更新する
2. insert-stub.js の `// [::TICKET::]` アノテーション自体は変更対象外（設計コンテキストが失われるため）
3. find-stubs-with-ticket-ref.js は変更しない（異なる機能）

## Test Plan

### Unit Tests

- UT: [Normal] insert-stub.js を --resolve-by-ticket=PX-77 で実行すると、ticketExists 検証を通過し、[::STUB::] PX-77: ... マーカーが指定行に挿入される。Contract C002 verified: 全実行パスで新旧両方の引数名チェックが行われる
- UT: [Error] insert-stub.js を旧 --ticket-ref=PX-77 で実行すると、exit code 2 で「--ticket-ref is deprecated, use --resolve-by-ticket instead」のエラーメッセージが出力される。Contract C001 precondition（旧名で呼び出し）postcondition（exit 2 + 案内）invariant（全実行パスで新旧両方の引数名チェックが行われる）全てカバー
- UT: [Error] insert-stub.js を --resolve-by-ticket=INVALID で実行すると、exit code 1 で「Ticket does not exist」エラーが出力される
- UT: [Error] insert-stub.js に --resolve-by-ticket と旧 --ticket-ref の両方が指定された場合、--resolve-by-ticket が優先。Contract C001 invariant: 全実行パスで新旧両方の引数名チェックが行われる
- UT: [Normal] grep で確認: 4つのcommand .md の insert-stub.js パラメータ説明が「将来このSTUBを解決するチケット」であることを明示している（WILL resolve / NOT the ticket being worked on）。Contract C003 postcondition カバー
- UT: [Invariant] grep で確認: 4つのcommand .md の insert-stub.js パラメータ説明文言が一致している。Contract C003 invariant: 全4ファイルのパラメータ説明文言が一致している
- UT: [Normal] insert-stub.js 内部の関数シグネチャ insertStub({ticketRef}) は変更されていない（互換性維持）

### Integration Tests

- IT: insert-stub.js が --resolve-by-ticket=PX-77 で挿入した [::STUB::] マーカーを、enumerate-ticket-targets.js が PX-77 の targetStubs として正しく分類する
- IT: insert-stub.js が旧 --ticket-ref を拒否した後、--resolve-by-ticket で再実行すると正しくマーカーが挿入される（リカバリフロー）
- IT: 全4つのcommand .md の insert-stub.js 使用例（コードブロック）を実際に実行可能であることを確認（--dry-run または等価な検証）

### Exceptions

- command .md ファイルのヘルプコメント文言変更は、実行可能なテストコードによる検証が不可能（cannot test — documentation only）。ただしこれは「決定論的だが本質的にテスト不能」なアーキテクチャ上の欠陥ではなく（not a defect）、純粋なドキュメント品質の問題である。grep による機械的文字列一致確認で代替する。UT の grep 確認でカバーする。
- 旧 --ticket-ref の拒否と新 --resolve-by-ticket の受け入れのテストは、insert-stub.js を子プロセスとして起動し exit code と stderr を確認する UT でカバーする（not testable as integration — covered by unit test）。これはアーキテクチャ上の欠陥ではない（not a defect）。テストフィクスチャとして Tickets.json の簡略版を用意する。
- enumerate-ticket-targets.js との統合テストは、実際の Tickets.json のコピーと一時ファイルを用いて実行する。既存のテストフレームワーク（insert-stub.test.js のパターン）に準拠する（not testable in isolation — integration test required）。これはアーキテクチャ上の欠陥ではない（not a defect）。

### Plan Test Code (concrete code)

- C001-precondition-postcondition: child_process spawn test
  ```js
  const { spawnSync } = require('child_process');
  const cp = spawnSync('node', ['.claude/scripts/tickets/insert-stub.js', '--ticket-ref=PX-77', '--file=test.tmp', '--line=1', '--tickets-path=Tickets.json']);
  assert.strictEqual(cp.status, 2, 'exit code 2 for deprecated arg');
  assert.ok(cp.stderr.toString().includes('use --resolve-by-ticket instead'), 'stderr mentions new arg name');
  ```
- C001-invariant: both arg names checked in every path
  ```js
  // The parseArgs() function must always check for --resolve-by-ticket first, then --ticket-ref
  const { spawnSync } = require('child_process');
  const cp = spawnSync('node', ['.claude/scripts/tickets/insert-stub.js', '--resolve-by-ticket=PX-77', '--ticket-ref=UNUSED', '--file=test.tmp', '--line=1', '--tickets-path=Tickets.json']);
  assert.strictEqual(cp.status, 0, 'both args provided: --resolve-by-ticket wins, no error');
  ```
- C002-precondition-postcondition: new --resolve-by-ticket works
  ```js
  const fs = require('fs');
  const path = require('path');
  const tmpFile = path.join(__dirname, '..', 'test-stub-target.tmp');
  fs.writeFileSync(tmpFile, 'line1\nline2\n');
  const { spawnSync } = require('child_process');
  const cp = spawnSync('node', ['.claude/scripts/tickets/insert-stub.js', '--resolve-by-ticket=PX-77', `--file=${tmpFile}`, '--line=2', '--tickets-path=Tickets.json']);
  assert.strictEqual(cp.status, 0, 'exit 0 for valid args');
  const content = fs.readFileSync(tmpFile, 'utf8');
  assert.ok(content.includes('[::STUB::] PX-77:'), 'marker inserted with correct ticket ref');
  fs.unlinkSync(tmpFile);
  ```
- C002-invariant: --resolve-by-ticket value reaches insertStub as ticketRef
  ```js
  // This is verified by the function signature: insertStub({ticketRef}) does not rename the parameter
  // and the same value flows through to the marker text
  const { insertStub } = require('.claude/scripts/tickets/insert-stub.js');
  const tmpFile = '/tmp/test-ticketref-flow.tmp';
  require('fs').writeFileSync(tmpFile, 'line1\n');
  const result = insertStub({ file: tmpFile, line: 1, ticketRef: 'PX-77', ticketsPath: require('path').resolve('Tickets.json') });
  assert.strictEqual(result.inserted, true);
  const content = require('fs').readFileSync(tmpFile, 'utf8');
  assert.ok(content.includes('[::STUB::] PX-77:'));
  require('fs').unlinkSync(tmpFile);
  ```
- C003-postcondition: all 4 files have future-direction help text
  ```bash
  # grep for the future-direction key phrase in all 4 command files
  PATTERN='WILL resolve this stub'
  for f in .claude/commands/plan-ticket.md .claude/commands/start-ticket.md .claude/commands/review-ticket.md .claude/commands/resolve-ticket.md; do
    if ! grep -q "$PATTERN" "$f"; then
      echo "FAIL: $f missing future-direction text"
      exit 1
    fi
  done
  echo "PASS: all 4 files have future-direction text"
  ```
- C003-invariant: all 4 files have identical help comment lines
  ```bash
  # Extract the help comment lines for the parameter and compare
  REF=$(grep -n 'resolve-by-ticket.*Ticket key' .claude/commands/plan-ticket.md | head -1)
  for f in .claude/commands/start-ticket.md .claude/commands/review-ticket.md .claude/commands/resolve-ticket.md; do
    CURRENT=$(grep -n 'resolve-by-ticket.*Ticket key' "$f" | head -1)
    if [ "$CURRENT" != "$REF" ]; then
      echo "FAIL: $f help text differs from plan-ticket.md"
      exit 1
    fi
  done
  echo "PASS: all 4 files have identical help text"
  ```

## Changes

| Before | After | Description |
|--------|-------|-------------|
| --ticket-ref CLI arg (deprecated) | --resolve-by-ticket CLI arg (new name) | insert-stub.js parseArgs(): added --resolve-by-ticket parsing, old --ticket-ref rejected with exit 2 plus migration hint |
| Help comment: Existing ticket key in Tickets.json | Help comment: Ticket key that WILL resolve this stub. NOT the ticket currently being worked on. | All help comments in insert-stub.js and 4 command .md files updated to clarify future-direction semantics |
| --ticket-ref=P3-1 in usage examples + prose | --resolve-by-ticket=P3-1 in usage examples + prose | 4 command .md files: all inline usage examples and prose references updated |

## Notes

改善1（コメント拡充）・改善2（insert-stub.js のコメント拡充）・改善3（パラメータ名変更）の3つを1チケットに集約。内部変数名 ticketRef は変更しない。旧名 --ticket-ref の拒否は parseArgs() 内でのみ行う。

【実装手順】
1. insert-stub.js の parseArgs() に --resolve-by-ticket のパース追加
2. insert-stub.js に旧 --ticket-ref の検出＋拒否処理追加
3. insert-stub.js の全エラーメッセージとコメントを更新
4. 4つの command .md ファイルのヘルプコメントブロック更新
5. 4つの command .md ファイルの prose 中 --ticket-ref= 参照を更新
6. insert-stub.test.js の CLI 引数名更新

【リスク】
- insert-stub.js をプログラムから呼び出しているスクリプトが --ticket-ref を使用している場合、破損する。現在は CLI からのみ使用されているためリスク低
- find-stubs-with-ticket-ref.js のユーザーがコマンドラインオプション名と混同する可能性。ただしこのスクリプトは --ticket-ref CLI 引数を持たない

【注意事項】
- 旧 --ticket-ref 完全廃止ではなく、非推奨＋拒否。将来のチケットで削除可能
- all 4 files must be updated together for consistency (C003 invariant)
Implementation summary:
- Changed files: insert-stub.js, insert-stub.test.js, plan-ticket.md, start-ticket.md, review-ticket.md, resolve-ticket.md
- Key changes: CLI arg rename, help text clarification, prose references update
- Test results: all 14 tests passed (10 existing + 4 PX-96 tests)
- Quality check: 0 issues found
- Contracts: C001 (--ticket-ref rejection), C002 (--resolve-by-ticket acceptance), C003 (4-file consistency) all verified
Review report:
- Status gate: passed (done)
- Crime scan: 0 crimes
- STUB evaluation: 0 new stubs in changed files
- Annotation verification: 2 source files annotated, 0 ambiguous
- 7-pattern active scan: no incomplete implementations
- Tests: 14/14 passed
- Static quality check: 0 issues
- Translatability: all function names are verb phrases, no single-char vars beyond iterator, no debug output
- C001 verified: --ticket-ref rejected with exit 2 + migration hint
- C002 verified: --resolve-by-ticket accepted, marker inserted correctly
- C003 verified: all 4 command files have identical help text content with future-direction semantics
- Fixed during review: plan-ticket.md help comment indentation aligned with other 3 files

## PX-96 — implemented at 3 locations

### .claude/scripts/tickets/insert-stub.js

- Line 63
```javascript
function insertStub({ file, line, ticketRef, description, ticketsPath }) {
```

- Line 174
```javascript
function parseArgs() {
```

### .claude/scripts/tickets/tests/insert-stub.test.js

- Line 24
```javascript
function assert(condition, message) {
```
