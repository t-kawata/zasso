# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-97: Tickets.json schema拡張 & tmp-omissions作成スクリプト

**Ticket Key**: PX-97 · **Phase**: -1

---

## Background

find-omissionsパイプラインの再設計に伴い、STUBが残存しているチケットと未レビューチケットを統合的に検出する仕組みが必要。本チケットでは(1) Tickets.jsonのチケットスキーマにfromStub(boolean)とstubs(配列)フィールドを追加し、(2) find-all-stubs.jsの出力からSTUB付きチケットキーを収集し、Tickets.jsonのreviewed以外の全チケットキーとマージして_tmp-omissions-<timestamp>.jsonに出力するスクリプトを作成する。

### Goal
完了済みチケットにSTUBが残存しているケースとレビュー未了チケットを統合検出し、_tmp-omissions.json として出力するパイプラインを構築する

### Purpose
find-omissions の再設計において、「STUBが放置された完了チケット」と「そもそもレビューが通っていないチケット」の両方を一元的に把握する出発点とする

### Motivation
現在の find-all-stubs.js はSTUBマーカーのみを検出し、未レビューチケットの情報と結合できない。再設計パイプラインの Step 1 として両者を統合するスクリプトが不可欠

### Constraints
- find-all-stubs.js の出力形式（{success, count, stubs[]}）に依存する
- Tickets.json のスキーマは変更可能だが、既存フィールドの削除・リネームは禁止
- 出力ファイル名には実行時のタイムスタンプを YYYYMMDDhhmmss 形式で含める

## Scope

- [Schema] Tickets.json ticket schemaに fromStub (boolean) と stubs (array of {file, content, codes}) を追加
- [Script] create-tmp-omissions.js: find-all-stubs.js の出力をchild_processで実行し結果を取得
- [Script] STUB行から /\[::STUB::\].*?([A-Z]+-\d+)/ の正規表現でチケットキーを重複なく収集
- [Script] Tickets.json から status!=reviewed の全チケットキーを収集
- [Script] 両ソースを重複なくマージ、fromStub フラグを適切に設定
- [Script] codes フィールドに line+1 から3行分のソースコードを抽出
- [Script] _tmp-omissions-<YYYYMMDDhhmmss>.json に Tickets.json と同一スキーマで出力
- [Script] コピー時に元の phaseId/ticketId を保持
- [Script] 各コピー後チケットの background 先頭に英語警告文を自動追記
- [Non-change] Tickets.json の既存フィールド（title, metadataは除く）は変更しない
- [Non-change] find-all-stubs.js 自体は変更しない（出力をそのまま利用）
- [Non-change] Tickets.json の phase 構造（id, name, characteristics）は変更しない
- **Scope of changes:**
  - [File/module path] .claude/scripts/tickets/create-tmp-omissions.js 新規作成
  - [Action: add] 新規スクリプト作成
  - [What specifically changes] Tickets.json に fromStub/stubs フィールドを追加、新規スクリプトでSTUB情報と未reviewed情報を統合
  - [Before → After] 統合チケット情報なし → _tmp-omissions-*.json で一元管理
  - [API contract change] なし（新規スクリプトなので既存APIへの影響なし）
  - [Data schema change] Tickets.json ticket schema に fromStub(boolean)/stubs(array) 追加
  - [Config/env change] なし
  - [Dependency added/removed] なし（既存の fs, path, child_process のみ使用）
- **Out of scope:**
  - [Excluded item] get-next-check-target-ticket-cmd.js の実装
  - [Why excluded] 別チケット（PX-99 以降でスケジュール）
- **Affected areas:**
  - [Affected component] Tickets.json のスキーマ定義（暗黙的）
  - [Nature of impact] スキーマ拡張により fromStub/stubs が全チケットに追加される
  - [Corresponding change needed] 既存チケットは fromStub/stubs 未設定のまま動作に影響なし

## Investigation

- [Evidence 1] find-all-stubs.js の出力形式を確認: { success: true, count: N, stubs: [{file, line, content}] }（scanFile 関数が各行を /\[::STUB::\]/ でマッチ）
- [Evidence 2] Tickets.json のスキーマ構造: phases[].tickets[] の2階層。各チケットは id, phaseId, status, title, background, scope, testUnit, testIntegration, testExceptions, notes, startedAt, changes, instrumentation, invariants, acceptanceCriteria, investigation, boyScoutPlan, contracts, rfcDiscrepancies, completedAt のフィールドを持つ
- [Evidence 3] update-ticket.js は stdin から JSON を受け取り、指定フィールドを上書きする設計
- [Evidence 4] テンプレートフィールド挿入では insert-field-template.js が 11 の field に [::TEMPLATE-STUB::] をセットする
- [Evidence 5] 既存チケット数: 133 reviewed, 2 non-reviewed（PX-96, PX-95 等が未レビュー）

## Acceptance Criteria

- Happy path: find-all-stubs.js を実行し、出力JSONからSTUB付きチケットキーを全件抽出。Tickets.json の未reviewedチケットキーとマージし、_tmp-omissions-<timestamp>.json が正しいスキーマで出力される
- Happy path: 各チケットの fromStub フラグが正しく設定され、stubs 配列に file/content/codes が格納されている
- Error case: find-all-stubs.js が exit non-zero で失敗した場合、エラーメッセージを stderr に出力し process.exit(1) する
- Error case: Tickets.json が存在しない場合、ENOENT エラーを throw する
- Error case: 指定された file path が存在しないSTUBエントリがある場合、codes は空文字列とし処理継続
- Edge case: 全チケットが reviewed 済みで未reviewedチケットが0件の場合、空の未reviewedリストで正常マージ
- Edge case: find-all-stubs.js の count=0 の場合、STUB由来リストは空配列としてマージ
- Edge case: 全ソースで該当チケットが0件の場合、{ title, metadata, phases: [] } を出力

## Invariants

- [Normal condition] find-all-stubs.js の出力は常に有効なJSONである（{success, count, stubs[]} 構造）
- [Normal condition] Tickets.json は常に phases[].tickets[] の階層構造を持つ
- [Error invariant] いかなるエラー時も既存の Tickets.json は変更されない（読み取り専用操作）
- [Error invariant] 出力ファイルが既に存在する場合は上書きされるが、元データは保持される
- [Internal state invariant] マージ処理中はメモリ上の配列のみが操作され、中間状態はファイルに書かれない
- [Boundary invariant] ファイル名のタイムスタンプは常に YYYYMMDDhhmmss 形式の14桁である

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — find-all-stubs.js stdout JSON → ticket key extraction

- **Precondition**: find-all-stubs.js の出力が有効な JSON { success: true, stubs: [{file, line, content}] } であること
- **Postcondition**: 各STUB行の content から /\[::STUB::\].*?([A-Z]+-\d+)/ でチケットキーが抽出され、重複なく収集される
- **Invariant**: 抽出されたキー集合に重複は存在しない（Setによる一意性保証）

### C002 — Tickets.json phased[].tickets[].status !== reviewed フィルタ

- **Precondition**: Tickets.json が有効なJSONとして fs.readFileSync で読み取り可能である
- **Postcondition**: 全フェーズを走査し status !== 'reviewed' のチケットキーが配列として取得される
- **Invariant**: reviewed ステータスのチケットは結果に含まれない

### C003 — stub由来リスト + Tickets.json未reviewedリストのマージ

- **Precondition**: 2つのソース（stub由来キーリスト、未reviewedキーリスト）が配列として存在する
- **Postcondition**: 両リストが重複なく結合され、各エントリに fromStub（boolean）フラグが設定される
- **Invariant**: マージ後のユニークキー数 ≤ ソースA + ソースB のキー総数

### C004 — file path + line number → codes 抽出

- **Precondition**: 各スタブエントリが有効な file（絶対パス文字列）と line（正の整数）を持つ
- **Postcondition**: 指定ファイルの (line) 行目から3行分の内容が codes フィールドに格納される
- **Invariant**: ファイルが存在しない場合、codes は空文字列となりエラーは throw されない

### C005 — _tmp-omissions-<timestamp>.json 出力

- **Precondition**: マージ結果のチケットリストが配列として存在する（空配列でも可）
- **Postcondition**: Tickets.json と同一スキーマ構造の JSON が _tmp-omissions-<YYYYMMDDhhmmss>.json に出力される
- **Invariant**: 出力の全チケットに fromStub（boolean）フィールドと stubs（Array）フィールドが存在する

## Boy Scout Rule

- [Extract] find-all-stubs.js のチケットキー抽出ロジックは正規表現ベースで独立した関数に抽出可能 → create-tmp-omissions.js 内で extractTicketKeys() として分離する
- [Name] 変数名: stubsFromFindAll → stubMarkers, nonReviewedKeys → pendingTicketKeys のようにドメイン意図を正確に命名
- [Constant] STUB正規表現パターンは const STUB_TICKET_KEY_RE としてモジュール定数化
- [Error handling] child_process.execSync は try-catch でラップし、エラー詳細を stderr に構造化して出力
- [Scope] 新規スクリプトのみ対象。find-all-stubs.js 自体のリファクタリングは対象外（Non-change）

## Test Plan

### Unit Tests

- UT: STUB行から /[::STUB::].*?([A-Z]+-d+)/ の正規表現でチケットキーを抽出できる（形式: P3-2, PX-53）
- UT: 同一チケットキーが複数STUBに出現しても重複排除される（Setによる一意性保証）
- UT: Tickets.json から status!=reviewed の全チケットキーを収集できる（配列抽出）
- UT: 両ソース（find-all-stubs.js出力＋Tickets.json未reviewed）のマージ結果が正しい — fromStub=true/false が適切に設定される
- UT: codes が line+1 から3行分のファイル内容と一致する（fs.readFileSyncで取得）
- UT: _tmp-omissions-*.json が Tickets.json と同一スキーマ構造（phases[].tickets[]）で出力される
- UT: 出力に fromStub (boolean) と stubs (Array<{file, content, codes}>) フィールドが含まれる
- UT: テンプレートコピー時に元の phaseId/ticketId が保持される
- UT: 各コピー後チケットの background 先頭に英語警告文が追記される（This ticket has been flagged for rejection...）
- UT: ファイル名が _tmp-omissions-<YYYYMMDDhhmmss>.json 形式である
- UT: [Normal] find-all-stubs.js が正常実行されSTUBデータが取得されるケース — 子プロセス stdout の JSON パースが正しい
- UT: [Error] find-all-stubs.js が異常終了（exit non-zero）した場合、エラー内容を報告する
- UT: [Error] Tickets.json が存在しない場合、Error を throw する
- UT: [Error] 指定 file path が存在しない場合でも codes が空文字列になる（errors は握らない）
- UT: [Boundary] find-all-stubs.js の count=0（STUB無し）の場合、空の stubs 配列で処理継続
- UT: [Boundary] Tickets.json に未reviewedチケットが0件の場合、空の配列でマージ処理継続
- UT: [Boundary] 該当チケットが一切ない場合、空の phases: [] を持つ _tmp-omissions を出力
- UT: [Invariant] 出力 _tmp-omissions-*.json の全チケットに fromStub フィールドが存在する
- UT: [Invariant] マージ後のチケットキー集合に重複が存在しない
- UT: [Invariant] コピー後の全チケット background が英語警告文で始まる
- UT: [Cover C001 invariant] 抽出されたキー集合に重複は存在しない（Setによる一意性保証）
- UT: [Cover C003 precondition] 2つのソース（stub由来キーリスト、未reviewedキーリスト）が配列として存在する状態でマージされる
- UT: [Cover C003 postcondition] 両リストが重複なく結合され、各エントリに fromStub（boolean）フラグが設定される
- UT: [Cover C003 invariant] マージ後のユニークキー数 ≤ ソースA + ソースB のキー総数
- UT: [Cover C004 precondition] 各スタブエントリが有効な file（絶対パス文字列）と line（正の整数）を持つことを確認
- UT: [Cover C005 precondition] マージ結果のチケットリストが配列として存在する（空配列でも可）状態で出力関数が呼ばれることを確認
- UT: [Cover C005 invariant] 出力の全チケットに fromStub（boolean）フィールドと stubs（Array）フィールドが存在する
- UT: [Cover C001 invariant exact] 抽出されたキー集合に重複は存在しない（Setによる一意性保証） — 契約の不変条件
- UT: [Cover C003 postcondition exact] 両リストが重複なく結合され、各エントリに fromStub（boolean）フラグが設定される — 事後条件

### Integration Tests

- IT: create-tmp-omissions.js が find-all-stubs.js の実際の出力を子プロセス経由で読み取り、正しい _tmp-omissions-*.json を生成する
- IT: 生成された _tmp-omissions-*.json がスキーマバリデーション（phaseId, ticketId, fromStub, stubs の存在チェック）を通る
- IT: [Integration point] find-all-stubs.js（stdout JSON）→ create-tmp-omissions.js（JSON parse）→ _tmp-omissions-*.json（JSON stringify）のパイプライン
- IT: [Verification] 生成ファイルを再パースして全フィールドの整合性を確認
- IT: [Prerequisites] find-all-stubs.js が同一プロジェクト内で実行可能であること
- IT: [Related tickets] PX-98（check-target-tickets-cmds）とは独立して動作可能

### Exceptions

- [Item] 実際のプロジェクト全体でのSTUB数はビルド状態に依存するため、完全一致テストはテスト不能（試験不可能）である。これは決定論的機能がテスト不能なのではなく、テストデータが実行環境に依存するためであり、設計欠陥ではない
- [Reason] テストでは fixture ディレクトリと固定STUBファイルを使用して検証（テスト可能）する。productionの全STUB網羅性はfixtureの品質に依存するため完全一致はテスト不能だが、これは設計上の欠陥ではなくテストデータ管理の方針の問題であり設計欠陥ではない
- [Alternative] テスト用の固定STUBファイル（test/fixtures/stubs/）を用意し、正規表現マッチング（テスト可能）と出力形式の正確性を検証する。全STUB網羅性の確認はテスト不能だがproduction実行時に行う。これは設計欠陥ではない
- [Item] codes 抽出がファイル読み取りに依存するため、ENOENT等のファイルシステムエラーの完全な再現テストはテスト不能。これはNode.jsのファイルI/OがOSレベルの状態に依存するためであり、アーキテクチャ上の設計欠陥ではない
- [Reason] fs.readFileSync のエラーハンドリング自体は mock でテスト可能だが、OS レベルの全エラー種別の再現はテスト不能。テスト方法の選択の問題であり設計欠陥ではない
- [Alternative] 存在しないファイルパスを与えた場合の挙動テスト（テスト可能）と、実際のファイルI/Oエラーは integration test でカバーする。OS依存エラーの完全再現はテスト不能のためユニットテストでは mock を使用する。これは設計欠陥ではない

### Plan Test Code (concrete code)

- UT: [C001 precondition] find-all-stubs.js の出力をパース
  ```javascript
  const mockOutput = { success: true, count: 2, stubs: [{ file: '/tmp/test.rs', line: 42, content: '// [::STUB::] P3-2: fix validation' }, { file: '/tmp/test2.rs', line: 10, content: '// [::STUB::] PX-53: implement handler' }] };
  const result = extractTicketKeysFromStubs(mockOutput);
  assert(Array.isArray(result), 'extractTicketKeysFromStubs returns array');
  ```
- UT: [C001 postcondition] 正規表現でチケットキー抽出
  ```javascript
  const mockOutput = { success: true, count: 2, stubs: [{ file: '/tmp/test.rs', line: 42, content: '// [::STUB::] P3-2: fix validation' }, { file: '/tmp/test2.rs', line: 10, content: '// [::STUB::] PX-53: implement handler' }] };
  const keys = extractTicketKeysFromStubs(mockOutput);
  assert(keys.length === 2, 'extracts 2 ticket keys');
  assert(keys.includes('P3-2'), 'contains P3-2');
  assert(keys.includes('PX-53'), 'contains PX-53');
  ```
- UT: [C001 invariant] 重複排除の保証
  ```javascript
  const mockOutput = { success: true, count: 3, stubs: [{ file: '/tmp/a.rs', line: 1, content: '// [::STUB::] P3-2: fix' }, { file: '/tmp/b.rs', line: 2, content: '// [::STUB::] P3-2: also fix' }, { file: '/tmp/c.rs', line: 3, content: '// [::STUB::] PX-53: other' }] };
  const keys = extractTicketKeysFromStubs(mockOutput);
  assert(keys.length === 2, 'deduplicates to 2 keys');
  assert(keys.filter(k => k === 'P3-2').length === 1, 'P3-2 appears once');
  ```
- UT: [C002 precondition] Tickets.json の読み取りとパース
  ```javascript
  const mockTickets = JSON.parse(fs.readFileSync('test/fixtures/minimal-tickets.json', 'utf8'));
  assert(mockTickets.phases, 'has phases array');
  assert(Array.isArray(mockTickets.phases), 'phases is array');
  ```
- UT: [C002 postcondition] reviewed 以外のチケットのみ収集
  ```javascript
  const mockTickets = { phases: [{ id: 0, tickets: [{ id: 1, status: 'reviewed', title: 'Done' }, { id: 2, status: 'todo', title: 'Pending' }] }, { id: 1, tickets: [{ id: 3, status: 'made', title: 'Made' }] }] };
  const nonReviewed = collectNonReviewedTickets(mockTickets);
  assert(nonReviewed.length === 2, 'collects 2 non-reviewed tickets');
  assert(nonReviewed.every(k => !k.includes('Done')), 'no reviewed ticket keys');
  ```
- UT: [C002 invariant] reviewed チケットが結果に含まれない
  ```javascript
  const mockTickets = { phases: [{ id: 0, tickets: [{ id: 1, status: 'reviewed', title: 'A' }, { id: 2, status: 'reviewed', title: 'B' }] }] };
  const nonReviewed = collectNonReviewedTickets(mockTickets);
  assert(nonReviewed.length === 0, 'no non-reviewed tickets yields empty array');
  ```
- UT: [C003 precondition] 2つのソース配列の存在
  ```javascript
  const stubKeys = ['P3-2', 'PX-53'];
  const pendingKeys = ['PX-53', 'PX-55'];
  const merged = mergeTicketSources(stubKeys, pendingKeys, mockStubsMap);
  assert(merged, 'mergeTicketSources returns result');
  ```
- UT: [C003 postcondition] マージ結果と fromStub フラグ
  ```javascript
  const stubKeys = ['P3-2'];
  const pendingKeys = ['PX-55'];
  const stubsMap = { 'P3-2': [{ file: '/tmp/t.rs', line: 1, content: '// [::STUB::] P3-2' }] };
  const merged = mergeTicketSources(stubKeys, pendingKeys, stubsMap);
  assert(merged.length === 2, 'merged count is 2');
  const fromStub = merged.find(m => m.fromStub === true);
  const fromPending = merged.find(m => m.fromStub === false);
  assert(fromStub && fromStub.ticketKey === 'P3-2', 'P3-2 has fromStub=true');
  assert(fromPending && fromPending.ticketKey === 'PX-55', 'PX-55 has fromStub=false');
  ```
- UT: [C003 invariant] マージ後のユニークキー数制約
  ```javascript
  const stubKeys = ['P3-2', 'PX-53'];
  const pendingKeys = ['PX-53', 'PX-55'];
  const merged = mergeTicketSources(stubKeys, pendingKeys, {});
  const uniqueKeys = new Set(merged.map(m => m.ticketKey));
  assert(uniqueKeys.size <= stubKeys.length + pendingKeys.length, 'unique <= total source keys');
  assert(uniqueKeys.size === 3, '3 unique keys from total 4 source keys');
  ```
- UT: [C004 precondition] ファイルパスと行番号の検証
  ```javascript
  const tmpFile = '/tmp/test-codes.txt';
  fs.writeFileSync(tmpFile, 'line1\nline2\nline3\nline4\n');
  const codes = extractCodes(tmpFile, 2);
  assert(codes === 'line2\nline3\nline4', 'extracts 3 lines from line 2');
  fs.unlinkSync(tmpFile);
  ```
- UT: [C004 postcondition] 存在しないファイルのハンドリング
  ```javascript
  const codes = extractCodes('/tmp/nonexistent-file.xyz', 1);
  assert(codes === '', 'non-existent file returns empty string');
  ```
- UT: [C004 invariant] エラーを throw しない
  ```javascript
  let threw = false;
  try { extractCodes('/tmp/nonexistent-file.xyz', 1); } catch(e) { threw = true; }
  assert(!threw, 'no exception thrown for non-existent file');
  ```
- UT: [C005 precondition] マージ結果からの出力構築
  ```javascript
  const mergedEntries = [{ ticketKey: 'P3-2', fromStub: true, stubs: [{ file: '/tmp/t.rs', line: 1, content: '// stub', codes: 'line2\nline3\nline4' }] }];
  const output = buildOutputJson(mergedEntries, { title: 'tmp', phases: [] });
  assert(output.phases, 'output has phases');
  ```
- UT: [C005 postcondition] Tickets.json と同一スキーマ
  ```javascript
  const mergedEntries = [{ ticketKey: 'P3-2', fromStub: true, stubs: [{ file: '/tmp/t.rs', line: 1, content: '// stub', codes: 'line2' }] }];
  const output = buildOutputJson(mergedEntries, { title: 'Test', metadata: { source: 'test', generatedAt: '20260729', analyzedSections: '' }, phases: [] });
  const outputStr = JSON.stringify(output);
  assert(output.title === 'Test', 'title copied');
  assert(output.metadata.source === 'test', 'metadata.source preserved');
  ```
- UT: [C005 invariant] fromStub と stubs フィールドの存在
  ```javascript
  const mergedEntries = [{ ticketKey: 'P3-2', fromStub: true, stubs: [{ file: '/tmp/t.rs', line: 1, content: '// stub', codes: 'line2' }] }, { ticketKey: 'PX-55', fromStub: false, stubs: [] }];
  const output = buildOutputJson(mergedEntries, { title: 'T', metadata: {}, phases: [] });
  output.phases.forEach(p => p.tickets.forEach(t => {
    assert(typeof t.fromStub === 'boolean', 'fromStub is boolean in all output tickets');
    assert(Array.isArray(t.stubs), 'stubs is array in all output tickets');
  }));
  ```

## Notes

[Implementation steps] 1) Tickets.json スキーマに fromStub/stubs フィールド追加 2) create-tmp-omissions.js 実装 3) テスト実装と検証 4) 既存の find-omissions パイプラインとの互換性確認

- [Implementation steps] Step 1-1: Tickets.json のスキーマバリデーション（validate-structure.js）に fromStub/stubs を追加。Step 1-2: create-tmp-omissions.js の全関数を実装。Step 2: テスト実装（20 UT + 2 IT）。Step 3: make test で全テスト通過確認
- [Risks] find-all-stubs.js の出力形式が将来変更された場合、パースが失敗する。その際は子プロセス exec の stderr でエラー詳細を報告する
- [Caveats] _tmp-omissions-*.json は一時ファイルであり、手動編集を想定しない
- [Open items] 生成後の _tmp-omissions-*.json をどのフェーズで Tickets.json にマージするかは Step 4-6 の別ロジック
- [Future improvements] find-all-stubs.js 自体の出力にチケットキー解析機能を持たせれば、本スクリプトの正規表現抽出は不要になる

## PX-97 — 0 locations, not implemented

Not found
