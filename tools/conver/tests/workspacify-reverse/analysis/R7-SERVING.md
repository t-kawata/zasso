## Serving — the claims a human has to decide

100 unresolved claim(s) are set out below, each with the question it raises, the
evidence available for it and the answer that applies if nobody decides. The remaining
745 claim(s) were settled by the analysis and are not repeated here.

The packet is layered. A boundary card leads because the contract level beneath it cannot be
decided while that boundary is open — not because the boundary matters more.

- **boundary** — 100 card(s), covering `src/api`, `src/config`, `src/model`, `src/runtime`

- **contract** — 0 card(s)

- **bundle** — 0 card(s)

### `clm-account_config_spec-boundary_crossing-364`

One falsifiable proposition: src/config consumes src/error through the reference at src/config/account_config_spec.rs:364

**Question** — Which configuration does the crossing at src/config/account_config_spec.rs:364 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/config/account_config_spec.rs:364` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-add_audio_source-boundary_crossing-70`

One falsifiable proposition: src/runtime consumes src/audio through the reference at src/runtime/add_audio_source.rs:70

**Question** — Which configuration does the crossing at src/runtime/add_audio_source.rs:70 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/add_audio_source.rs:70` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-add_audio_source-boundary_crossing-71`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/add_audio_source.rs:71

**Question** — Which configuration does the crossing at src/runtime/add_audio_source.rs:71 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/add_audio_source.rs:71` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-asyncaudiosrc_adapter-boundary_crossing-470`

One falsifiable proposition: src/api consumes src/model through the reference at src/api/asyncaudiosrc_adapter.rs:470

**Question** — Which configuration does the crossing at src/api/asyncaudiosrc_adapter.rs:470 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/api/asyncaudiosrc_adapter.rs:470` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-asyncaudiosrc_adapter-boundary_crossing-579`

One falsifiable proposition: src/api consumes src/runtime through the reference at src/api/asyncaudiosrc_adapter.rs:579

**Question** — Which configuration does the crossing at src/api/asyncaudiosrc_adapter.rs:579 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/api/asyncaudiosrc_adapter.rs:579` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_format_chunkpair-boundary_crossing-350`

One falsifiable proposition: src/model consumes src/audio through the reference at src/model/audio_format_chunkpair.rs:350

**Question** — Which configuration does the crossing at src/model/audio_format_chunkpair.rs:350 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/model/audio_format_chunkpair.rs:350` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_format_chunkpair-boundary_crossing-352`

One falsifiable proposition: src/model consumes src/config through the reference at src/model/audio_format_chunkpair.rs:352

**Question** — Which configuration does the crossing at src/model/audio_format_chunkpair.rs:352 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/model/audio_format_chunkpair.rs:352` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_format_chunkpair-boundary_crossing-367`

One falsifiable proposition: src/model consumes src/audio through the reference at src/model/audio_format_chunkpair.rs:367

**Question** — Which configuration does the crossing at src/model/audio_format_chunkpair.rs:367 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/model/audio_format_chunkpair.rs:367` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_format_chunkpair-boundary_crossing-369`

One falsifiable proposition: src/model consumes src/config through the reference at src/model/audio_format_chunkpair.rs:369

**Question** — Which configuration does the crossing at src/model/audio_format_chunkpair.rs:369 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/model/audio_format_chunkpair.rs:369` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_format_chunkpair-boundary_crossing-384`

One falsifiable proposition: src/model consumes src/audio through the reference at src/model/audio_format_chunkpair.rs:384

**Question** — Which configuration does the crossing at src/model/audio_format_chunkpair.rs:384 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/model/audio_format_chunkpair.rs:384` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_format_chunkpair-boundary_crossing-386`

One falsifiable proposition: src/model consumes src/config through the reference at src/model/audio_format_chunkpair.rs:386

**Question** — Which configuration does the crossing at src/model/audio_format_chunkpair.rs:386 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/model/audio_format_chunkpair.rs:386` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_subscribe_bp-boundary_crossing-239`

One falsifiable proposition: src/api consumes src/model through the reference at src/api/audio_subscribe_bp.rs:239

**Question** — Which configuration does the crossing at src/api/audio_subscribe_bp.rs:239 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/api/audio_subscribe_bp.rs:239` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_subscribe_bp-boundary_crossing-240`

One falsifiable proposition: src/api consumes src/model through the reference at src/api/audio_subscribe_bp.rs:240

**Question** — Which configuration does the crossing at src/api/audio_subscribe_bp.rs:240 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/api/audio_subscribe_bp.rs:240` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_worker-boundary_crossing-1667`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/audio_worker.rs:1667

**Question** — Which configuration does the crossing at src/runtime/audio_worker.rs:1667 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/audio_worker.rs:1667` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_worker-boundary_crossing-1669`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/audio_worker.rs:1669

**Question** — Which configuration does the crossing at src/runtime/audio_worker.rs:1669 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/audio_worker.rs:1669` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_worker-boundary_crossing-1693`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/audio_worker.rs:1693

**Question** — Which configuration does the crossing at src/runtime/audio_worker.rs:1693 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/audio_worker.rs:1693` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_worker-boundary_crossing-1695`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/audio_worker.rs:1695

**Question** — Which configuration does the crossing at src/runtime/audio_worker.rs:1695 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/audio_worker.rs:1695` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-audio_worker-boundary_crossing-667`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/audio_worker.rs:667

**Question** — Which configuration does the crossing at src/runtime/audio_worker.rs:667 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/audio_worker.rs:667` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1003`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1003

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1003 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1003` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1024`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1024

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1024 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1024` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1040`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1040

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1040 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1040` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1056`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1056

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1056 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1056` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1072`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1072

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1072 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1072` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1088`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1088

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1088 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1088` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1104`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1104

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1104 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1104` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1105`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1105

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1105 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1105` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1108`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1108

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1108 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1108` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1131`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1131

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1131 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1131` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1156`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1156

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1156 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1156` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1172`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1172

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1172 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1172` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1246`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1246

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1246 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1246` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1251`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1251

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1251 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1251` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1253`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1253

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1253 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1253` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1261`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1261

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1261 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1261` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1273`

One falsifiable proposition: src/runtime consumes src/error through the reference at src/runtime/backend.rs:1273

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1273 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1273` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1289`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1289

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1289 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1289` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1299`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1299

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1299 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1299` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1306`

One falsifiable proposition: src/runtime consumes src/state through the reference at src/runtime/backend.rs:1306

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1306 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1306` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1311`

One falsifiable proposition: src/runtime consumes src/state through the reference at src/runtime/backend.rs:1311

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1311 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1311` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1319`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1319

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1319 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1319` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1328`

One falsifiable proposition: src/runtime consumes src/state through the reference at src/runtime/backend.rs:1328

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1328 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1328` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1332`

One falsifiable proposition: src/runtime consumes src/state through the reference at src/runtime/backend.rs:1332

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1332 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1332` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1337`

One falsifiable proposition: src/runtime consumes src/state through the reference at src/runtime/backend.rs:1337

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1337 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1337` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1341`

One falsifiable proposition: src/runtime consumes src/state through the reference at src/runtime/backend.rs:1341

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1341 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1341` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1353`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1353

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1353 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1353` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1378`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1378

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1378 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1378` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1393`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1393

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1393 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1393` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1419`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1419

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1419 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1419` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1440`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1440

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1440 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1440` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1454`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1454

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1454 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1454` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1467`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1467

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1467 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1467` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1496`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1496

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1496 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1496` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1508`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1508

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1508 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1508` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1514`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1514

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1514 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1514` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1526`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1526

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1526 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1526` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1537`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1537

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1537 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1537` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1538`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1538

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1538 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1538` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1547`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1547

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1547 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1547` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1550`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1550

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1550 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1550` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1554`

One falsifiable proposition: src/runtime consumes src/ffi through the reference at src/runtime/backend.rs:1554

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1554 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1554` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1571`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1571

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1571 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1571` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1580`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1580

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1580 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1580` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1623`

One falsifiable proposition: src/runtime consumes src/audio through the reference at src/runtime/backend.rs:1623

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1623 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1623` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1625`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1625

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1625 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1625` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1637`

One falsifiable proposition: src/runtime consumes src/audio through the reference at src/runtime/backend.rs:1637

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1637 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1637` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1639`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1639

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1639 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1639` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1651`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/backend.rs:1651

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1651 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1651` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1653`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/backend.rs:1653

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1653 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1653` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1656`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1656

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1656 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1656` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1657`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1657

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1657 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1657` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1659`

One falsifiable proposition: src/runtime consumes src/audio through the reference at src/runtime/backend.rs:1659

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1659 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1659` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1661`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1661

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1661 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1661` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1669`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1669

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1669 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1669` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1670`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1670

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1670 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1670` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1681`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/backend.rs:1681

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1681 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1681` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1683`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/backend.rs:1683

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1683 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1683` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1689`

One falsifiable proposition: src/runtime consumes src/audio through the reference at src/runtime/backend.rs:1689

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1689 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1689` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1691`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1691

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1691 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1691` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1699`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1699

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1699 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1699` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1700`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1700

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1700 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1700` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1711`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/backend.rs:1711

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1711 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1711` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1713`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/backend.rs:1713

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1713 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1713` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1719`

One falsifiable proposition: src/runtime consumes src/audio through the reference at src/runtime/backend.rs:1719

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1719 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1719` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1721`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1721

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1721 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1721` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1729`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1729

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1729 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1729` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1731`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1731

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1731 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1731` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1738`

One falsifiable proposition: src/runtime consumes src/audio through the reference at src/runtime/backend.rs:1738

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1738 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1738` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1740`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1740

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1740 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1740` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1759`

One falsifiable proposition: src/runtime consumes src/audio through the reference at src/runtime/backend.rs:1759

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1759 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1759` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1761`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1761

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1761 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1761` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1964`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/backend.rs:1964

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1964 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1964` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1966`

One falsifiable proposition: src/runtime consumes src/api through the reference at src/runtime/backend.rs:1966

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1966 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1966` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1969`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1969

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1969 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1969` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1970`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1970

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1970 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1970` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1974`

One falsifiable proposition: src/runtime consumes src/audio through the reference at src/runtime/backend.rs:1974

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1974 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1974` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1976`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:1976

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1976 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1976` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1984`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1984

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1984 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1984` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-1985`

One falsifiable proposition: src/runtime consumes src/model through the reference at src/runtime/backend.rs:1985

**Question** — Which configuration does the crossing at src/runtime/backend.rs:1985 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:1985` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-2042`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:2042

**Question** — Which configuration does the crossing at src/runtime/backend.rs:2042 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:2042` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

### `clm-backend-boundary_crossing-2044`

One falsifiable proposition: src/runtime consumes src/config through the reference at src/runtime/backend.rs:2044

**Question** — Which configuration does the crossing at src/runtime/backend.rs:2044 hold in, and is the reference part of the shipped interface?

- **Evidence**
  - `src/runtime/backend.rs:2044` (source_static)
- **Counterexamples**
  - the reference sits behind a configuration gate, so whether it ships is not readable from the text
- **Default** — hand it to the human grill as unresolved

3615 further unresolved claim(s) were not printed here. They are withheld,
not dropped: the JSON beside this report carries every one of them, and the count is stated so that
this page cannot read as the whole of what was left open.

- 3136 withheld — a scope whose boundary is unresolved withholds its contract level whole, covering `src`, `src/api`, `src/architecture`, `src/audio`, `src/build`, `src/config`, `src/error`, `src/ffi`, `src/model`, `src/runtime`, `src/state`, `src/tests`
- 479 withheld — the claim lies beyond the serving limit, covering `examples/common`, `src`, `src/api`, `src/architecture`, `src/audio`, `src/build`, `src/config`, `src/error`, `src/ffi`, `src/model`, `src/runtime`, `src/security`, `src/state`, `src/tests`