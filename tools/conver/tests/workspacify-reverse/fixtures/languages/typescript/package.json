{
  "name": "language-representative-typescript",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The TypeScript representative: chosen for the constructs the syntax layer must reach, not for its size.",
  "scripts": {
    "test": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.4.0"
  }
}
