# Implementation Order (TDD Red-Green-Refactor)

Implementation must strictly follow the **Red → Green → Refactor** sequence. Skipping steps, reordering, or parallel execution is prohibited.

## 1. Red — Fully Implement Failing Tests

Before writing a single line of implementation code, write a failing test suite that achieves 100% coverage of the spec's **Goal, Purpose, Motivation, Constraints, Scope, Acceptance Criteria, and Invariants**. Coverage of these seven elements is mandatory; partial implementation is not acceptable.

When the ticket defines **Contracts** (Precondition/Postcondition/Invariant from graph edge annotation), the Red phase must first translate each Contract into testable form — input schemas, output assertions, and invariant predicates — before implementing them as concrete test code. A Contract whose Precondition/Postcondition/Invariant cannot be expressed as a testable assertion is not yet fully specified.

- Tests must cover all observable behaviors, edge cases, failure modes, and invariants. Any behavior not covered is considered undefined and fails review.
- If a feature is deterministic yet fundamentally untestable, this is not a testing gap but an architectural defect. Redesign the system until it is testable before proceeding to implementation.
- Confirm that all tests fail red due to the absence of implementation. Tests that pass green by accident (e.g., meaningless assertions) are invalid.

## 2. Green — Implement Behavior (No Stubs, No Test Modification)

Implement the **behavior** specified by the tests; do not treat passing the tests as an end in itself. Tests are a means of verifying correctness, not the goal itself.

- Implementations that merely satisfy the literal wording of tests—via hardcoding, input-specific branching, or stubbed return values—are prohibited. The implementation must be a generalized, correct solution.
- If it is impossible to distinguish, via testing, whether an implementation is genuine or a disguised green, this indicates a design flaw caused by insufficient coverage. Add tests until the distinction is possible before proceeding with implementation.
- Modifying, deleting, or weakening tests to make an implementation pass is strictly forbidden. The implementation must conform to the tests; the reverse is never acceptable.
- An implementation whose correctness cannot be proven is invalid. It is not considered complete until it (or its design) is restructured into a provably correct form.

## 3. Refactor — Apply the Boy Scout Rule (Green State Only)

Refactor only after all tests are green. Refactoring in a red state is prohibited.

- Apply the Boy Scout Rule (leave the code cleaner than you found it; readability = translatability) to eliminate `unwrap()` calls, hardcoded values, false comments, and untested code in anything you touch.
- Verify that all tests remain green before and after each refactoring step. If a refactor breaks green, roll it back immediately.

## Definition of Done

Implementation is considered incomplete unless all of the following are satisfied:

- The tests fully and precisely specify the intended behavior.
- The implementation passes all tests green, without exception.
- Correctness is empirically guaranteed by the tests (not a disguised green).
- No gap exists between test coverage and intended behavior.

Green without red, green achieved by modifying tests, and green achieved through stubs are all violations and constitute incomplete work.

# Target ticket is PX-98: 完了済み実装状況検査対象コマンドリスト生成スクリプト

**Ticket Key**: PX-98 · **Phase**: -1

---

## Background

find-omissionsパイプラインの再設計に伴い、reviewedチケットの実装が契約を満たしているかを検査する仕組みが必要。本チケットではTickets.jsonから全reviewedチケットのチケットキーを収集し、_tmp-check-target-tickets-cmds-<timestamp>.jsonに検査コマンドリストを生成するスクリプトを作成する。各エントリは {done: false, cmd: 'node ...'} の形式を持ち、初期状態は全てdone=falseとする。

### Goal
reviewed チケット全件に対応する show-ticket-context.js 呼び出しコマンドリストを生成し、Step 3 の検査パイプラインに渡す

### Purpose
Step 3（AIによる実装状況検査）の入力データを準備する。検査対象チケットの列挙とコマンド形式の標準化を自動化し、検査漏れを防止する

### Motivation
現在133件の reviewed チケットを手動で列挙することは現実的でない。また、各チケットのコマンド形式を統一しないと Step 3 の自動実行が不可能

### Constraints
- show-ticket-context.js の CLI インターフェース（--ticket-key, --for-spec）に依存する
- 生成されるコマンドはシェルで直接実行可能な形式である必要がある
- 出力ファイル名には YYYYMMDDhhmmss 形式のタイムスタンプが必要

## Scope

- [Script] create-check-target-tickets-cmds.js: Tickets.json から全 reviewed チケットを収集
- [Script] 各チケットに対して show-ticket-context.js --ticket-key=KEY --for-spec のコマンド文字列を生成
- [Script] {done, cmd} 形式のエントリ配列を構築（初期状態は全て done: false）
- [Script] _tmp-check-target-tickets-cmds-<YYYYMMDDhhmmss>.json に書き出し
- [Non-change] show-ticket-context.js 自体は変更しない
- [Non-change] Tickets.json の既存フィールドは変更しない
- [Non-change] PX-97 で作成される create-tmp-omissions.js の出力形式は変更しない
- **Scope of changes:**
  - [File/module path] .claude/scripts/tickets/create-check-target-tickets-cmds.js 新規作成
  - [Action: add] 新規スクリプト作成
  - [What specifically changes] なし（新規スクリプトのみ）
  - [Before → After] コマンドリストなし → _tmp-check-target-tickets-cmds-*.json
  - [API contract change] なし
  - [Data schema change] なし
  - [Config/env change] なし
  - [Dependency added/removed] なし
- **Out of scope:**
  - [Excluded item] get-next-check-target-ticket-cmd.js の実装
  - [Why excluded] 別チケットで実装予定
- **Affected areas:**
  - [Affected component] なし（新規スクリプトであり既存システムへの影響なし）
  - [Nature of impact] なし
  - [Corresponding change needed] なし

## Investigation

- [Evidence 1] show-ticket-context.js の CLI インターフェースを確認: --ticket-key=<KEY> と --for-spec フラグをサポート（parseArgs 関数で解析）
- [Evidence 2] Tickets.json の status 値の実績: reviewed (133), todo (PX-97, PX-98), made, in_progress のバリエーション
- [Evidence 3] 既存の類似スクリプト（enumerate-ticket-targets.js, check-ticket-status.js）は同様の Tickets.json 走査パターンを使用
- [Evidence 4] 出力先ディレクトリはカレントディレクトリ（. または process.cwd()）が基準
- [Evidence 5] done フィールドは boolean 型で、get-next-check-target-ticket-cmd.js が処理完了後に true に更新することを前提とする

## Acceptance Criteria

- Happy path: reviewed チケット全件のコマンドリストが _tmp-check-target-tickets-cmds-<timestamp>.json に出力される
- Happy path: 各エントリの done が全て false、cmd 形式が正しい
- Error case: Tickets.json が存在しない場合、ENOENT エラーを throw する
- Error case: reviewed チケットが1件もない場合、空の JSON 配列 [] を出力する
- Edge case: reviewed チケットがちょうど1件の場合でも正しい単一エントリが生成される
- Edge case: 133件の reviewed チケットがあった場合でも同数のエントリが生成される

## Invariants

- [Normal condition] Tickets.json の全フェーズの全チケットから status フィールドが読み取り可能である
- [Normal condition] 各チケットキーは P{phaseId}-{ticketId} 形式である
- [Error invariant] Tickets.json 読み取りに失敗しても、出力ファイルは作成されない（未書き込み状態を保証）
- [Error invariant] 出力ファイルが既に存在する場合、内容は完全に上書きされる
- [Internal state invariant] 処理中の中間JSONはメモリ上のみで管理され、不完全な状態でファイルに書き出されることはない
- [Boundary invariant] ファイル名のタイムスタンプは常に YYYYMMDDhhmmss 形式の14桁である

## Contracts — mandatory 100% test coverage in TDD Red phase

### C001 — Tickets.json phases[].tickets[].status === reviewed フィルタ

- **Precondition**: Tickets.json が有効なJSONとして fs.readFileSync で読み取り可能である
- **Postcondition**: 全フェーズを走査し status === 'reviewed' のチケットキーのみが配列として抽出される
- **Invariant**: reviewed 以外の status のチケットは結果に含まれない（フィルタ条件の健全性）

### C002 — ticketKey → show-ticket-context.js コマンド文字列生成

- **Precondition**: 有効な ticketKey 文字列（P{phaseId}-{ticketId} 形式）が存在する
- **Postcondition**: node .claude/scripts/tickets/show-ticket-context.js --ticket-key=KEY --for-spec 形式のコマンド文字列が生成される
- **Invariant**: 生成された全 cmd 文字列が show-ticket-context.js を含む

### C003 — コマンドエントリ配列 → _tmp-check-target-tickets-cmds-<timestamp>.json 出力

- **Precondition**: コマンドエントリの配列がメモリ上に構築されている（空配列でも可）
- **Postcondition**: [{done: false, cmd: string}] 形式の JSON 配列が _tmp-check-target-tickets-cmds-<YYYYMMDDhhmmss>.json に書き込まれる
- **Invariant**: 全エントリの done が初期状態で false である

## Boy Scout Rule

- [Extract] reviewed チケット収集ロジックは collectReviewedTicketKeys() として独立関数に分離する
- [Name] 変数名: cmds → commandEntries, reviewedTickets → reviewedTicketKeys と命名
- [Constant] コマンドテンプレートは const SHOW_TICKET_CMD_TEMPLATE = 'node .claude/scripts/tickets/show-ticket-context.js --ticket-key=KEY --for-spec' として定数化
- [Error handling] fs.writeFileSync のエラーは try-catch でラップし stderr に構造化エラーを出力
- [Scope] 新規スクリプトのみ対象。show-ticket-context.js のリファクタリングは対象外

## Test Plan

### Unit Tests

- UT: Tickets.json から status=reviewed のチケットのみを抽出できる（phases全走査、配列flat）
- UT: 各エントリの cmd が node .claude/scripts/tickets/show-ticket-context.js --ticket-key=KEY --for-spec の正しい形式である
- UT: 全エントリの done が初期状態で false である
- UT: ファイル名が _tmp-check-target-tickets-cmds-<YYYYMMDDhhmmss>.json 形式である
- UT: reviewed チケットが0件の場合、空配列 [] を出力する
- UT: 複数の reviewed チケットがある場合、全てのエントリが正しく列挙される
- UT: [Normal] reviewed チケットが3件存在する場合、3エントリのコマンドリストが生成される
- UT: [Error] Tickets.json が存在しない／パース失敗の場合、エラーメッセージを表示する
- UT: [Error] reviewed 以外の status（todo, in_progress, made 等）は出力に含まれない
- UT: [Boundary] reviewed チケットが1件のみの場合も正しく単一エントリが生成される
- UT: [Boundary] 大量（100件以上）の reviewed チケットがある場合でも正常に全件列挙される
- UT: [Invariant] 全エントリが {done: boolean, cmd: string} の型に従う
- UT: [Invariant] cmd 文字列が必ず show-ticket-context.js を含む
- UT: [Invariant] 出力 JSON は配列のトップレベル構造を持つ（オブジェクトでラップしない）
- UT: [Cover C002 precondition] 有効な ticketKey 文字列（P{phaseId}-{ticketId} 形式）が存在する場合にコマンド文字列が生成される
- UT: [Cover C003 precondition] コマンドエントリの配列がメモリ上に構築されている（空配列でも可）状態で出力関数が呼ばれる

### Integration Tests

- IT: 生成された _tmp-check-target-tickets-cmds-*.json の各エントリを独立して実行可能である（コマンドが正しいシェル構文）
- IT: [Integration point] create-check-target-tickets-cmds.js → _tmp-check-target-tickets-cmds-*.json → get-next-check-target-ticket-cmd.js（別チケット）のパイプライン
- IT: [Verification] 生成されたコマンドを1件実際に実行し、show-ticket-context.js が正常出力を返すことを確認
- IT: [Prerequisites] show-ticket-context.js が --ticket-key と --for-spec フラグをサポートしていること
- IT: [Related tickets] PX-97（find-all-stubs.js 実行）とは独立。ただし find-omissions 再設計全体の文脈で連携

### Exceptions

- [Item] 実際の reviewed チケット数は実行時の Tickets.json の状態に依存するため、固定値のテストはテスト不能。これは決定論的機能がテスト不能なのではなく、実行時データに依存する設計上やむを得ない理由による。設計欠陥ではない
- [Reason] テストでは fixture の Tickets.json を使用し、任意の数（0, 1, 5, 100件）の reviewed チケットを設定（テスト可能）する。実データの全チケット数を固定値で検証するのはテスト不能だが、これはテストデータ管理の方針の問題であり設計欠陥ではない
- [Alternative] 各fixtureでコマンド形式とdone=false初期状態をアサートする（テスト可能）。実データのチケット数はテスト不能のためproduction実行時に確認する。設計欠陥ではない
- [Item] 生成されたコマンドの実行成功は show-ticket-context.js の動作に依存するため、本スクリプト単体での完全検証はテスト不能。アーキテクチャ上の責務分離であり設計欠陥ではない
- [Reason] 本スクリプトの責務はコマンド文字列の生成である。コマンドの実行自体はテスト不能として別責務に分離しており、これはアーキテクチャ上の責務分離であり設計欠陥ではない
- [Alternative] コマンド文字列のシンタックス検証（テスト可能）と、コマンドが実行可能であることのITでカバーする。実際のコマンド実行結果の検証はテスト不能のため別チケットで行う。設計欠陥ではない

## Notes

[Implementation steps] 1) create-check-target-tickets-cmds.js 実装 2) テスト実装と検証 3) get-next-check-target-ticket-cmd.js（別チケット）との連携確認

- [Implementation steps] Step 1: create-check-target-tickets-cmds.js の全関数実装（collectReviewedKeys, buildCommand, buildEntry, writeOutput）。Step 2: テスト実装（14 UT + 1 IT）。Step 3: 生成ファイルを手動確認
- [Risks] show-ticket-context.js のフラグ名が変更された場合、コマンド文字列の生成ロジックを修正する必要がある
- [Caveats] 生成されるファイルは get-next-check-target-ticket-cmd.js の入力形式に依存する。出力形式を変更する場合は両スクリプトを同時に修正すること
- [Open items] 大規模 Tickets.json でのパフォーマンス（現状 133 reviewed チケットでは問題なし）
- [Future improvements] 生成コマンドの dry-run 検証モードを追加し、事前にコマンドが実行可能かチェックできるようにする

## PX-98 — 0 locations, not implemented

Not found
